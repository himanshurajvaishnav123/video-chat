import { n as __toESM } from "./rolldown-runtime-BPOCksWG.js";
import { t as require_react } from "./react.js";
//#region node_modules/@livekit/components-react/dist/rolldown-runtime-9ZSF5uba.mjs
var c = Object.create;
var l = Object.defineProperty;
var u = Object.getOwnPropertyDescriptor;
var s = Object.getOwnPropertyNames;
var v = Object.getPrototypeOf;
var P = Object.prototype.hasOwnProperty;
var j$2 = (e, r) => () => (r || (e((r = { exports: {} }).exports, r), e = null), r.exports);
var b = (e, r, a, p) => {
	if (r && typeof r == "object" || typeof r == "function") for (var _ = s(r), n = 0, o = _.length, t; n < o; n++) t = _[n], !P.call(e, t) && t !== a && l(e, t, {
		get: ((O) => r[O]).bind(null, t),
		enumerable: !(p = u(r, t)) || p.enumerable
	});
	return e;
};
var f = (e, r, a) => (a = e != null ? c(v(e)) : {}, b(r || !e || !e.__esModule ? l(a, "default", {
	value: e,
	enumerable: !0
}) : a, e));
//#endregion
//#region node_modules/livekit-client/dist/livekit-client.esm.mjs
var import_react = /* @__PURE__ */ __toESM(require_react(), 1);
function _mergeNamespaces(n, m) {
	m.forEach(function(e) {
		e && typeof e !== "string" && !Array.isArray(e) && Object.keys(e).forEach(function(k) {
			if (k !== "default" && !(k in n)) {
				var d = Object.getOwnPropertyDescriptor(e, k);
				Object.defineProperty(n, k, d.get ? d : {
					enumerable: true,
					get: function() {
						return e[k];
					}
				});
			}
		});
	});
	return Object.freeze(n);
}
var e = Object.defineProperty;
var h = (i, s, t) => s in i ? e(i, s, {
	enumerable: true,
	configurable: true,
	writable: true,
	value: t
}) : i[s] = t;
var o = (i, s, t) => h(i, typeof s != "symbol" ? s + "" : s, t);
var _ = class {
	constructor() {
		o(this, "_locking");
		o(this, "_locks");
		this._locking = Promise.resolve(), this._locks = 0;
	}
	isLocked() {
		return this._locks > 0;
	}
	lock() {
		this._locks += 1;
		let s;
		const t = new Promise((l) => s = () => {
			this._locks -= 1, l();
		}), c = this._locking.then(() => s);
		return this._locking = this._locking.then(() => t), c;
	}
};
/**
* Assert that condition is truthy or throw error (with message)
*/
function assert(condition, msg) {
	if (!condition) throw new Error(msg);
}
var FLOAT32_MAX = 34028234663852886e22;
var FLOAT32_MIN = -34028234663852886e22;
var UINT32_MAX = 4294967295;
var INT32_MAX = 2147483647;
var INT32_MIN = -2147483648;
/**
* Assert a valid signed protobuf 32-bit integer.
*/
function assertInt32(arg) {
	if (typeof arg !== "number") throw new Error("invalid int 32: " + typeof arg);
	if (!Number.isInteger(arg) || arg > INT32_MAX || arg < INT32_MIN) throw new Error("invalid int 32: " + arg);
}
/**
* Assert a valid unsigned protobuf 32-bit integer.
*/
function assertUInt32(arg) {
	if (typeof arg !== "number") throw new Error("invalid uint 32: " + typeof arg);
	if (!Number.isInteger(arg) || arg > UINT32_MAX || arg < 0) throw new Error("invalid uint 32: " + arg);
}
/**
* Assert a valid protobuf float value.
*/
function assertFloat32(arg) {
	if (typeof arg !== "number") throw new Error("invalid float 32: " + typeof arg);
	if (!Number.isFinite(arg)) return;
	if (arg > FLOAT32_MAX || arg < FLOAT32_MIN) throw new Error("invalid float 32: " + arg);
}
var enumTypeSymbol = Symbol("@bufbuild/protobuf/enum-type");
/**
* Get reflection information from a generated enum.
* If this function is called on something other than a generated
* enum, it raises an error.
*/
function getEnumType(enumObject) {
	const t = enumObject[enumTypeSymbol];
	assert(t, "missing enum type on enum object");
	return t;
}
/**
* Sets reflection information on a generated enum.
*/
function setEnumType(enumObject, typeName, values, opt) {
	enumObject[enumTypeSymbol] = makeEnumType(typeName, values.map((v) => ({
		no: v.no,
		name: v.name,
		localName: enumObject[v.no]
	})));
}
/**
* Create a new EnumType with the given values.
*/
function makeEnumType(typeName, values, _opt) {
	const names = Object.create(null);
	const numbers = Object.create(null);
	const normalValues = [];
	for (const value of values) {
		const n = normalizeEnumValue(value);
		normalValues.push(n);
		names[value.name] = n;
		numbers[value.no] = n;
	}
	return {
		typeName,
		values: normalValues,
		findName(name) {
			return names[name];
		},
		findNumber(no) {
			return numbers[no];
		}
	};
}
/**
* Create a new enum object with the given values.
* Sets reflection information.
*/
function makeEnum(typeName, values, opt) {
	const enumObject = {};
	for (const value of values) {
		const n = normalizeEnumValue(value);
		enumObject[n.localName] = n.no;
		enumObject[n.no] = n.localName;
	}
	setEnumType(enumObject, typeName, values);
	return enumObject;
}
function normalizeEnumValue(value) {
	if ("localName" in value) return value;
	return Object.assign(Object.assign({}, value), { localName: value.name });
}
/**
* Message is the base class of every message, generated, or created at
* runtime.
*
* It is _not_ safe to extend this class. If you want to create a message at
* run time, use proto3.makeMessageType().
*/
var Message = class {
	/**
	* Compare with a message of the same type.
	* Note that this function disregards extensions and unknown fields.
	*/
	equals(other) {
		return this.getType().runtime.util.equals(this.getType(), this, other);
	}
	/**
	* Create a deep copy.
	*/
	clone() {
		return this.getType().runtime.util.clone(this);
	}
	/**
	* Parse from binary data, merging fields.
	*
	* Repeated fields are appended. Map entries are added, overwriting
	* existing keys.
	*
	* If a message field is already present, it will be merged with the
	* new data.
	*/
	fromBinary(bytes, options) {
		const format = this.getType().runtime.bin, opt = format.makeReadOptions(options);
		format.readMessage(this, opt.readerFactory(bytes), bytes.byteLength, opt);
		return this;
	}
	/**
	* Parse a message from a JSON value.
	*/
	fromJson(jsonValue, options) {
		const type = this.getType(), format = type.runtime.json, opt = format.makeReadOptions(options);
		format.readMessage(type, jsonValue, opt, this);
		return this;
	}
	/**
	* Parse a message from a JSON string.
	*/
	fromJsonString(jsonString, options) {
		let json;
		try {
			json = JSON.parse(jsonString);
		} catch (e) {
			throw new Error("cannot decode ".concat(this.getType().typeName, " from JSON: ").concat(e instanceof Error ? e.message : String(e)));
		}
		return this.fromJson(json, options);
	}
	/**
	* Serialize the message to binary data.
	*/
	toBinary(options) {
		const bin = this.getType().runtime.bin, opt = bin.makeWriteOptions(options), writer = opt.writerFactory();
		bin.writeMessage(this, writer, opt);
		return writer.finish();
	}
	/**
	* Serialize the message to a JSON value, a JavaScript value that can be
	* passed to JSON.stringify().
	*/
	toJson(options) {
		const json = this.getType().runtime.json, opt = json.makeWriteOptions(options);
		return json.writeMessage(this, opt);
	}
	/**
	* Serialize the message to a JSON string.
	*/
	toJsonString(options) {
		var _a;
		const value = this.toJson(options);
		return JSON.stringify(value, null, (_a = options === null || options === void 0 ? void 0 : options.prettySpaces) !== null && _a !== void 0 ? _a : 0);
	}
	/**
	* Override for serialization behavior. This will be invoked when calling
	* JSON.stringify on this message (i.e. JSON.stringify(msg)).
	*
	* Note that this will not serialize google.protobuf.Any with a packed
	* message because the protobuf JSON format specifies that it needs to be
	* unpacked, and this is only possible with a type registry to look up the
	* message type.  As a result, attempting to serialize a message with this
	* type will throw an Error.
	*
	* This method is protected because you should not need to invoke it
	* directly -- instead use JSON.stringify or toJsonString for
	* stringified JSON.  Alternatively, if actual JSON is desired, you should
	* use toJson.
	*/
	toJSON() {
		return this.toJson({ emitDefaultValues: true });
	}
	/**
	* Retrieve the MessageType of this message - a singleton that represents
	* the protobuf message declaration and provides metadata for reflection-
	* based operations.
	*/
	getType() {
		return Object.getPrototypeOf(this).constructor;
	}
};
/**
* Create a new message type using the given runtime.
*/
function makeMessageType(runtime, typeName, fields, opt) {
	var _a;
	const localName = (_a = opt === null || opt === void 0 ? void 0 : opt.localName) !== null && _a !== void 0 ? _a : typeName.substring(typeName.lastIndexOf(".") + 1);
	const type = { [localName]: function(data) {
		runtime.util.initFields(this);
		runtime.util.initPartial(data, this);
	} }[localName];
	Object.setPrototypeOf(type.prototype, new Message());
	Object.assign(type, {
		runtime,
		typeName,
		fields: runtime.util.newFieldList(fields),
		fromBinary(bytes, options) {
			return new type().fromBinary(bytes, options);
		},
		fromJson(jsonValue, options) {
			return new type().fromJson(jsonValue, options);
		},
		fromJsonString(jsonString, options) {
			return new type().fromJsonString(jsonString, options);
		},
		equals(a, b) {
			return runtime.util.equals(type, a, b);
		}
	});
	return type;
}
/**
* Read a 64 bit varint as two JS numbers.
*
* Returns tuple:
* [0]: low bits
* [1]: high bits
*
* Copyright 2008 Google Inc.  All rights reserved.
*
* See https://github.com/protocolbuffers/protobuf/blob/8a71927d74a4ce34efe2d8769fda198f52d20d12/js/experimental/runtime/kernel/buffer_decoder.js#L175
*/
function varint64read() {
	let lowBits = 0;
	let highBits = 0;
	for (let shift = 0; shift < 28; shift += 7) {
		let b = this.buf[this.pos++];
		lowBits |= (b & 127) << shift;
		if ((b & 128) == 0) {
			this.assertBounds();
			return [lowBits, highBits];
		}
	}
	let middleByte = this.buf[this.pos++];
	lowBits |= (middleByte & 15) << 28;
	highBits = (middleByte & 112) >> 4;
	if ((middleByte & 128) == 0) {
		this.assertBounds();
		return [lowBits, highBits];
	}
	for (let shift = 3; shift <= 31; shift += 7) {
		let b = this.buf[this.pos++];
		highBits |= (b & 127) << shift;
		if ((b & 128) == 0) {
			this.assertBounds();
			return [lowBits, highBits];
		}
	}
	throw new Error("invalid varint");
}
/**
* Write a 64 bit varint, given as two JS numbers, to the given bytes array.
*
* Copyright 2008 Google Inc.  All rights reserved.
*
* See https://github.com/protocolbuffers/protobuf/blob/8a71927d74a4ce34efe2d8769fda198f52d20d12/js/experimental/runtime/kernel/writer.js#L344
*/
function varint64write(lo, hi, bytes) {
	for (let i = 0; i < 28; i = i + 7) {
		const shift = lo >>> i;
		const hasNext = !(shift >>> 7 == 0 && hi == 0);
		const byte = (hasNext ? shift | 128 : shift) & 255;
		bytes.push(byte);
		if (!hasNext) return;
	}
	const splitBits = lo >>> 28 & 15 | (hi & 7) << 4;
	const hasMoreBits = !(hi >> 3 == 0);
	bytes.push((hasMoreBits ? splitBits | 128 : splitBits) & 255);
	if (!hasMoreBits) return;
	for (let i = 3; i < 31; i = i + 7) {
		const shift = hi >>> i;
		const hasNext = !(shift >>> 7 == 0);
		const byte = (hasNext ? shift | 128 : shift) & 255;
		bytes.push(byte);
		if (!hasNext) return;
	}
	bytes.push(hi >>> 31 & 1);
}
var TWO_PWR_32_DBL = 4294967296;
/**
* Parse decimal string of 64 bit integer value as two JS numbers.
*
* Copyright 2008 Google Inc.  All rights reserved.
*
* See https://github.com/protocolbuffers/protobuf-javascript/blob/a428c58273abad07c66071d9753bc4d1289de426/experimental/runtime/int64.js#L10
*/
function int64FromString(dec) {
	const minus = dec[0] === "-";
	if (minus) dec = dec.slice(1);
	const base = 1e6;
	let lowBits = 0;
	let highBits = 0;
	function add1e6digit(begin, end) {
		const digit1e6 = Number(dec.slice(begin, end));
		highBits *= base;
		lowBits = lowBits * base + digit1e6;
		if (lowBits >= TWO_PWR_32_DBL) {
			highBits = highBits + (lowBits / TWO_PWR_32_DBL | 0);
			lowBits = lowBits % TWO_PWR_32_DBL;
		}
	}
	add1e6digit(-24, -18);
	add1e6digit(-18, -12);
	add1e6digit(-12, -6);
	add1e6digit(-6);
	return minus ? negate(lowBits, highBits) : newBits(lowBits, highBits);
}
/**
* Losslessly converts a 64-bit signed integer in 32:32 split representation
* into a decimal string.
*
* Copyright 2008 Google Inc.  All rights reserved.
*
* See https://github.com/protocolbuffers/protobuf-javascript/blob/a428c58273abad07c66071d9753bc4d1289de426/experimental/runtime/int64.js#L10
*/
function int64ToString(lo, hi) {
	let bits = newBits(lo, hi);
	const negative = bits.hi & 2147483648;
	if (negative) bits = negate(bits.lo, bits.hi);
	const result = uInt64ToString(bits.lo, bits.hi);
	return negative ? "-" + result : result;
}
/**
* Losslessly converts a 64-bit unsigned integer in 32:32 split representation
* into a decimal string.
*
* Copyright 2008 Google Inc.  All rights reserved.
*
* See https://github.com/protocolbuffers/protobuf-javascript/blob/a428c58273abad07c66071d9753bc4d1289de426/experimental/runtime/int64.js#L10
*/
function uInt64ToString(lo, hi) {
	var _toUnsigned = toUnsigned(lo, hi);
	lo = _toUnsigned.lo;
	hi = _toUnsigned.hi;
	if (hi <= 2097151) return String(TWO_PWR_32_DBL * hi + lo);
	const low = lo & 16777215;
	const mid = (lo >>> 24 | hi << 8) & 16777215;
	const high = hi >> 16 & 65535;
	let digitA = low + mid * 6777216 + high * 6710656;
	let digitB = mid + high * 8147497;
	let digitC = high * 2;
	const base = 1e7;
	if (digitA >= base) {
		digitB += Math.floor(digitA / base);
		digitA %= base;
	}
	if (digitB >= base) {
		digitC += Math.floor(digitB / base);
		digitB %= base;
	}
	return digitC.toString() + decimalFrom1e7WithLeadingZeros(digitB) + decimalFrom1e7WithLeadingZeros(digitA);
}
function toUnsigned(lo, hi) {
	return {
		lo: lo >>> 0,
		hi: hi >>> 0
	};
}
function newBits(lo, hi) {
	return {
		lo: lo | 0,
		hi: hi | 0
	};
}
/**
* Returns two's compliment negation of input.
* @see https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Bitwise_Operators#Signed_32-bit_integers
*/
function negate(lowBits, highBits) {
	highBits = ~highBits;
	if (lowBits) lowBits = ~lowBits + 1;
	else highBits += 1;
	return newBits(lowBits, highBits);
}
/**
* Returns decimal representation of digit1e7 with leading zeros.
*/
var decimalFrom1e7WithLeadingZeros = (digit1e7) => {
	const partial = String(digit1e7);
	return "0000000".slice(partial.length) + partial;
};
/**
* Write a 32 bit varint, signed or unsigned. Same as `varint64write(0, value, bytes)`
*
* Copyright 2008 Google Inc.  All rights reserved.
*
* See https://github.com/protocolbuffers/protobuf/blob/1b18833f4f2a2f681f4e4a25cdf3b0a43115ec26/js/binary/encoder.js#L144
*/
function varint32write(value, bytes) {
	if (value >= 0) {
		while (value > 127) {
			bytes.push(value & 127 | 128);
			value = value >>> 7;
		}
		bytes.push(value);
	} else {
		for (let i = 0; i < 9; i++) {
			bytes.push(value & 127 | 128);
			value = value >> 7;
		}
		bytes.push(1);
	}
}
/**
* Read an unsigned 32 bit varint.
*
* See https://github.com/protocolbuffers/protobuf/blob/8a71927d74a4ce34efe2d8769fda198f52d20d12/js/experimental/runtime/kernel/buffer_decoder.js#L220
*/
function varint32read() {
	let b = this.buf[this.pos++];
	let result = b & 127;
	if ((b & 128) == 0) {
		this.assertBounds();
		return result;
	}
	b = this.buf[this.pos++];
	result |= (b & 127) << 7;
	if ((b & 128) == 0) {
		this.assertBounds();
		return result;
	}
	b = this.buf[this.pos++];
	result |= (b & 127) << 14;
	if ((b & 128) == 0) {
		this.assertBounds();
		return result;
	}
	b = this.buf[this.pos++];
	result |= (b & 127) << 21;
	if ((b & 128) == 0) {
		this.assertBounds();
		return result;
	}
	b = this.buf[this.pos++];
	result |= (b & 15) << 28;
	for (let readBytes = 5; (b & 128) !== 0 && readBytes < 10; readBytes++) b = this.buf[this.pos++];
	if ((b & 128) != 0) throw new Error("invalid varint");
	this.assertBounds();
	return result >>> 0;
}
function makeInt64Support() {
	const dv = /* @__PURE__ */ new DataView(/* @__PURE__ */ new ArrayBuffer(8));
	if (typeof BigInt === "function" && typeof dv.getBigInt64 === "function" && typeof dv.getBigUint64 === "function" && typeof dv.setBigInt64 === "function" && typeof dv.setBigUint64 === "function" && (typeof process != "object" || typeof process.env != "object" || process.env.BUF_BIGINT_DISABLE !== "1")) {
		const MIN = BigInt("-9223372036854775808"), MAX = BigInt("9223372036854775807"), UMIN = BigInt("0"), UMAX = BigInt("18446744073709551615");
		return {
			zero: BigInt(0),
			supported: true,
			parse(value) {
				const bi = typeof value == "bigint" ? value : BigInt(value);
				if (bi > MAX || bi < MIN) throw new Error("int64 invalid: ".concat(value));
				return bi;
			},
			uParse(value) {
				const bi = typeof value == "bigint" ? value : BigInt(value);
				if (bi > UMAX || bi < UMIN) throw new Error("uint64 invalid: ".concat(value));
				return bi;
			},
			enc(value) {
				dv.setBigInt64(0, this.parse(value), true);
				return {
					lo: dv.getInt32(0, true),
					hi: dv.getInt32(4, true)
				};
			},
			uEnc(value) {
				dv.setBigInt64(0, this.uParse(value), true);
				return {
					lo: dv.getInt32(0, true),
					hi: dv.getInt32(4, true)
				};
			},
			dec(lo, hi) {
				dv.setInt32(0, lo, true);
				dv.setInt32(4, hi, true);
				return dv.getBigInt64(0, true);
			},
			uDec(lo, hi) {
				dv.setInt32(0, lo, true);
				dv.setInt32(4, hi, true);
				return dv.getBigUint64(0, true);
			}
		};
	}
	const assertInt64String = (value) => assert(/^-?[0-9]+$/.test(value), "int64 invalid: ".concat(value));
	const assertUInt64String = (value) => assert(/^[0-9]+$/.test(value), "uint64 invalid: ".concat(value));
	return {
		zero: "0",
		supported: false,
		parse(value) {
			if (typeof value != "string") value = value.toString();
			assertInt64String(value);
			return value;
		},
		uParse(value) {
			if (typeof value != "string") value = value.toString();
			assertUInt64String(value);
			return value;
		},
		enc(value) {
			if (typeof value != "string") value = value.toString();
			assertInt64String(value);
			return int64FromString(value);
		},
		uEnc(value) {
			if (typeof value != "string") value = value.toString();
			assertUInt64String(value);
			return int64FromString(value);
		},
		dec(lo, hi) {
			return int64ToString(lo, hi);
		},
		uDec(lo, hi) {
			return uInt64ToString(lo, hi);
		}
	};
}
var protoInt64 = makeInt64Support();
/**
* Scalar value types. This is a subset of field types declared by protobuf
* enum google.protobuf.FieldDescriptorProto.Type The types GROUP and MESSAGE
* are omitted, but the numerical values are identical.
*/
var ScalarType;
(function(ScalarType) {
	ScalarType[ScalarType["DOUBLE"] = 1] = "DOUBLE";
	ScalarType[ScalarType["FLOAT"] = 2] = "FLOAT";
	ScalarType[ScalarType["INT64"] = 3] = "INT64";
	ScalarType[ScalarType["UINT64"] = 4] = "UINT64";
	ScalarType[ScalarType["INT32"] = 5] = "INT32";
	ScalarType[ScalarType["FIXED64"] = 6] = "FIXED64";
	ScalarType[ScalarType["FIXED32"] = 7] = "FIXED32";
	ScalarType[ScalarType["BOOL"] = 8] = "BOOL";
	ScalarType[ScalarType["STRING"] = 9] = "STRING";
	ScalarType[ScalarType["BYTES"] = 12] = "BYTES";
	ScalarType[ScalarType["UINT32"] = 13] = "UINT32";
	ScalarType[ScalarType["SFIXED32"] = 15] = "SFIXED32";
	ScalarType[ScalarType["SFIXED64"] = 16] = "SFIXED64";
	ScalarType[ScalarType["SINT32"] = 17] = "SINT32";
	ScalarType[ScalarType["SINT64"] = 18] = "SINT64";
})(ScalarType || (ScalarType = {}));
/**
* JavaScript representation of fields with 64 bit integral types (int64, uint64,
* sint64, fixed64, sfixed64).
*
* This is a subset of google.protobuf.FieldOptions.JSType, which defines JS_NORMAL,
* JS_STRING, and JS_NUMBER. Protobuf-ES uses BigInt by default, but will use
* String if `[jstype = JS_STRING]` is specified.
*
* ```protobuf
* uint64 field_a = 1; // BigInt
* uint64 field_b = 2 [jstype = JS_NORMAL]; // BigInt
* uint64 field_b = 2 [jstype = JS_NUMBER]; // BigInt
* uint64 field_b = 2 [jstype = JS_STRING]; // String
* ```
*/
var LongType;
(function(LongType) {
	/**
	* Use JavaScript BigInt.
	*/
	LongType[LongType["BIGINT"] = 0] = "BIGINT";
	/**
	* Use JavaScript String.
	*
	* Field option `[jstype = JS_STRING]`.
	*/
	LongType[LongType["STRING"] = 1] = "STRING";
})(LongType || (LongType = {}));
/**
* Returns true if both scalar values are equal.
*/
function scalarEquals(type, a, b) {
	if (a === b) return true;
	if (type == ScalarType.BYTES) {
		if (!(a instanceof Uint8Array) || !(b instanceof Uint8Array)) return false;
		if (a.length !== b.length) return false;
		for (let i = 0; i < a.length; i++) if (a[i] !== b[i]) return false;
		return true;
	}
	switch (type) {
		case ScalarType.UINT64:
		case ScalarType.FIXED64:
		case ScalarType.INT64:
		case ScalarType.SFIXED64:
		case ScalarType.SINT64: return a == b;
	}
	return false;
}
/**
* Returns the zero value for the given scalar type.
*/
function scalarZeroValue(type, longType) {
	switch (type) {
		case ScalarType.BOOL: return false;
		case ScalarType.UINT64:
		case ScalarType.FIXED64:
		case ScalarType.INT64:
		case ScalarType.SFIXED64:
		case ScalarType.SINT64: return longType == 0 ? protoInt64.zero : "0";
		case ScalarType.DOUBLE:
		case ScalarType.FLOAT: return 0;
		case ScalarType.BYTES: return /* @__PURE__ */ new Uint8Array(0);
		case ScalarType.STRING: return "";
		default: return 0;
	}
}
/**
* Returns true for a zero-value. For example, an integer has the zero-value `0`,
* a boolean is `false`, a string is `""`, and bytes is an empty Uint8Array.
*
* In proto3, zero-values are not written to the wire, unless the field is
* optional or repeated.
*/
function isScalarZeroValue(type, value) {
	switch (type) {
		case ScalarType.BOOL: return value === false;
		case ScalarType.STRING: return value === "";
		case ScalarType.BYTES: return value instanceof Uint8Array && !value.byteLength;
		default: return value == 0;
	}
}
function _arrayLikeToArray(r, a) {
	(null == a || a > r.length) && (a = r.length);
	for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e];
	return n;
}
function _arrayWithHoles(r) {
	if (Array.isArray(r)) return r;
}
function _defineProperty(e, r, t) {
	return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, {
		value: t,
		enumerable: true,
		configurable: true,
		writable: true
	}) : e[r] = t, e;
}
function _iterableToArrayLimit(r, l) {
	var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"];
	if (null != t) {
		var e, n, i, u, a = [], f = true, o = false;
		try {
			if (i = (t = t.call(r)).next, 0 === l) {
				if (Object(t) !== t) return;
				f = !1;
			} else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0);
		} catch (r) {
			o = true, n = r;
		} finally {
			try {
				if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return;
			} finally {
				if (o) throw n;
			}
		}
		return a;
	}
}
function _nonIterableRest() {
	throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
function _slicedToArray(r, e) {
	return _arrayWithHoles(r) || _iterableToArrayLimit(r, e) || _unsupportedIterableToArray(r, e) || _nonIterableRest();
}
function _toPrimitive(t, r) {
	if ("object" != typeof t || !t) return t;
	var e = t[Symbol.toPrimitive];
	if (void 0 !== e) {
		var i = e.call(t, r);
		if ("object" != typeof i) return i;
		throw new TypeError("@@toPrimitive must return a primitive value.");
	}
	return ("string" === r ? String : Number)(t);
}
function _toPropertyKey(t) {
	var i = _toPrimitive(t, "string");
	return "symbol" == typeof i ? i : i + "";
}
function _unsupportedIterableToArray(r, a) {
	if (r) {
		if ("string" == typeof r) return _arrayLikeToArray(r, a);
		var t = {}.toString.call(r).slice(8, -1);
		return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0;
	}
}
/**
* Protobuf binary format wire types.
*
* A wire type provides just enough information to find the length of the
* following value.
*
* See https://developers.google.com/protocol-buffers/docs/encoding#structure
*/
var WireType;
(function(WireType) {
	/**
	* Used for int32, int64, uint32, uint64, sint32, sint64, bool, enum
	*/
	WireType[WireType["Varint"] = 0] = "Varint";
	/**
	* Used for fixed64, sfixed64, double.
	* Always 8 bytes with little-endian byte order.
	*/
	WireType[WireType["Bit64"] = 1] = "Bit64";
	/**
	* Used for string, bytes, embedded messages, packed repeated fields
	*
	* Only repeated numeric types (types which use the varint, 32-bit,
	* or 64-bit wire types) can be packed. In proto3, such fields are
	* packed by default.
	*/
	WireType[WireType["LengthDelimited"] = 2] = "LengthDelimited";
	/**
	* Start of a tag-delimited aggregate, such as a proto2 group, or a message
	* in editions with message_encoding = DELIMITED.
	*/
	WireType[WireType["StartGroup"] = 3] = "StartGroup";
	/**
	* End of a tag-delimited aggregate.
	*/
	WireType[WireType["EndGroup"] = 4] = "EndGroup";
	/**
	* Used for fixed32, sfixed32, float.
	* Always 4 bytes with little-endian byte order.
	*/
	WireType[WireType["Bit32"] = 5] = "Bit32";
})(WireType || (WireType = {}));
var BinaryWriter = class {
	constructor(textEncoder) {
		/**
		* Previous fork states.
		*/
		this.stack = [];
		this.textEncoder = textEncoder !== null && textEncoder !== void 0 ? textEncoder : new TextEncoder();
		this.chunks = [];
		this.buf = [];
	}
	/**
	* Return all bytes written and reset this writer.
	*/
	finish() {
		this.chunks.push(new Uint8Array(this.buf));
		let len = 0;
		for (let i = 0; i < this.chunks.length; i++) len += this.chunks[i].length;
		let bytes = new Uint8Array(len);
		let offset = 0;
		for (let i = 0; i < this.chunks.length; i++) {
			bytes.set(this.chunks[i], offset);
			offset += this.chunks[i].length;
		}
		this.chunks = [];
		return bytes;
	}
	/**
	* Start a new fork for length-delimited data like a message
	* or a packed repeated field.
	*
	* Must be joined later with `join()`.
	*/
	fork() {
		this.stack.push({
			chunks: this.chunks,
			buf: this.buf
		});
		this.chunks = [];
		this.buf = [];
		return this;
	}
	/**
	* Join the last fork. Write its length and bytes, then
	* return to the previous state.
	*/
	join() {
		let chunk = this.finish();
		let prev = this.stack.pop();
		if (!prev) throw new Error("invalid state, fork stack empty");
		this.chunks = prev.chunks;
		this.buf = prev.buf;
		this.uint32(chunk.byteLength);
		return this.raw(chunk);
	}
	/**
	* Writes a tag (field number and wire type).
	*
	* Equivalent to `uint32( (fieldNo << 3 | type) >>> 0 )`.
	*
	* Generated code should compute the tag ahead of time and call `uint32()`.
	*/
	tag(fieldNo, type) {
		return this.uint32((fieldNo << 3 | type) >>> 0);
	}
	/**
	* Write a chunk of raw bytes.
	*/
	raw(chunk) {
		if (this.buf.length) {
			this.chunks.push(new Uint8Array(this.buf));
			this.buf = [];
		}
		this.chunks.push(chunk);
		return this;
	}
	/**
	* Write a `uint32` value, an unsigned 32 bit varint.
	*/
	uint32(value) {
		assertUInt32(value);
		while (value > 127) {
			this.buf.push(value & 127 | 128);
			value = value >>> 7;
		}
		this.buf.push(value);
		return this;
	}
	/**
	* Write a `int32` value, a signed 32 bit varint.
	*/
	int32(value) {
		assertInt32(value);
		varint32write(value, this.buf);
		return this;
	}
	/**
	* Write a `bool` value, a variant.
	*/
	bool(value) {
		this.buf.push(value ? 1 : 0);
		return this;
	}
	/**
	* Write a `bytes` value, length-delimited arbitrary data.
	*/
	bytes(value) {
		this.uint32(value.byteLength);
		return this.raw(value);
	}
	/**
	* Write a `string` value, length-delimited data converted to UTF-8 text.
	*/
	string(value) {
		let chunk = this.textEncoder.encode(value);
		this.uint32(chunk.byteLength);
		return this.raw(chunk);
	}
	/**
	* Write a `float` value, 32-bit floating point number.
	*/
	float(value) {
		assertFloat32(value);
		let chunk = /* @__PURE__ */ new Uint8Array(4);
		new DataView(chunk.buffer).setFloat32(0, value, true);
		return this.raw(chunk);
	}
	/**
	* Write a `double` value, a 64-bit floating point number.
	*/
	double(value) {
		let chunk = /* @__PURE__ */ new Uint8Array(8);
		new DataView(chunk.buffer).setFloat64(0, value, true);
		return this.raw(chunk);
	}
	/**
	* Write a `fixed32` value, an unsigned, fixed-length 32-bit integer.
	*/
	fixed32(value) {
		assertUInt32(value);
		let chunk = /* @__PURE__ */ new Uint8Array(4);
		new DataView(chunk.buffer).setUint32(0, value, true);
		return this.raw(chunk);
	}
	/**
	* Write a `sfixed32` value, a signed, fixed-length 32-bit integer.
	*/
	sfixed32(value) {
		assertInt32(value);
		let chunk = /* @__PURE__ */ new Uint8Array(4);
		new DataView(chunk.buffer).setInt32(0, value, true);
		return this.raw(chunk);
	}
	/**
	* Write a `sint32` value, a signed, zigzag-encoded 32-bit varint.
	*/
	sint32(value) {
		assertInt32(value);
		value = (value << 1 ^ value >> 31) >>> 0;
		varint32write(value, this.buf);
		return this;
	}
	/**
	* Write a `fixed64` value, a signed, fixed-length 64-bit integer.
	*/
	sfixed64(value) {
		let chunk = /* @__PURE__ */ new Uint8Array(8), view = new DataView(chunk.buffer), tc = protoInt64.enc(value);
		view.setInt32(0, tc.lo, true);
		view.setInt32(4, tc.hi, true);
		return this.raw(chunk);
	}
	/**
	* Write a `fixed64` value, an unsigned, fixed-length 64 bit integer.
	*/
	fixed64(value) {
		let chunk = /* @__PURE__ */ new Uint8Array(8), view = new DataView(chunk.buffer), tc = protoInt64.uEnc(value);
		view.setInt32(0, tc.lo, true);
		view.setInt32(4, tc.hi, true);
		return this.raw(chunk);
	}
	/**
	* Write a `int64` value, a signed 64-bit varint.
	*/
	int64(value) {
		let tc = protoInt64.enc(value);
		varint64write(tc.lo, tc.hi, this.buf);
		return this;
	}
	/**
	* Write a `sint64` value, a signed, zig-zag-encoded 64-bit varint.
	*/
	sint64(value) {
		let tc = protoInt64.enc(value), sign = tc.hi >> 31;
		varint64write(tc.lo << 1 ^ sign, (tc.hi << 1 | tc.lo >>> 31) ^ sign, this.buf);
		return this;
	}
	/**
	* Write a `uint64` value, an unsigned 64-bit varint.
	*/
	uint64(value) {
		let tc = protoInt64.uEnc(value);
		varint64write(tc.lo, tc.hi, this.buf);
		return this;
	}
};
var BinaryReader = class {
	constructor(buf, textDecoder) {
		this.varint64 = varint64read;
		/**
		* Read a `uint32` field, an unsigned 32 bit varint.
		*/
		this.uint32 = varint32read;
		this.buf = buf;
		this.len = buf.length;
		this.pos = 0;
		this.view = new DataView(buf.buffer, buf.byteOffset, buf.byteLength);
		this.textDecoder = textDecoder !== null && textDecoder !== void 0 ? textDecoder : new TextDecoder();
	}
	/**
	* Reads a tag - field number and wire type.
	*/
	tag() {
		let tag = this.uint32(), fieldNo = tag >>> 3, wireType = tag & 7;
		if (fieldNo <= 0 || wireType < 0 || wireType > 5) throw new Error("illegal tag: field no " + fieldNo + " wire type " + wireType);
		return [fieldNo, wireType];
	}
	/**
	* Skip one element and return the skipped data.
	*
	* When skipping StartGroup, provide the tags field number to check for
	* matching field number in the EndGroup tag.
	*/
	skip(wireType, fieldNo) {
		let start = this.pos;
		switch (wireType) {
			case WireType.Varint:
				while (this.buf[this.pos++] & 128);
				break;
			case WireType.Bit64: this.pos += 4;
			case WireType.Bit32:
				this.pos += 4;
				break;
			case WireType.LengthDelimited:
				let len = this.uint32();
				this.pos += len;
				break;
			case WireType.StartGroup:
				for (;;) {
					const _this$tag2 = _slicedToArray(this.tag(), 2), fn = _this$tag2[0], wt = _this$tag2[1];
					if (wt === WireType.EndGroup) {
						if (fieldNo !== void 0 && fn !== fieldNo) throw new Error("invalid end group tag");
						break;
					}
					this.skip(wt, fn);
				}
				break;
			default: throw new Error("cant skip wire type " + wireType);
		}
		this.assertBounds();
		return this.buf.subarray(start, this.pos);
	}
	/**
	* Throws error if position in byte array is out of range.
	*/
	assertBounds() {
		if (this.pos > this.len) throw new RangeError("premature EOF");
	}
	/**
	* Read a `int32` field, a signed 32 bit varint.
	*/
	int32() {
		return this.uint32() | 0;
	}
	/**
	* Read a `sint32` field, a signed, zigzag-encoded 32-bit varint.
	*/
	sint32() {
		let zze = this.uint32();
		return zze >>> 1 ^ -(zze & 1);
	}
	/**
	* Read a `int64` field, a signed 64-bit varint.
	*/
	int64() {
		return protoInt64.dec(...this.varint64());
	}
	/**
	* Read a `uint64` field, an unsigned 64-bit varint.
	*/
	uint64() {
		return protoInt64.uDec(...this.varint64());
	}
	/**
	* Read a `sint64` field, a signed, zig-zag-encoded 64-bit varint.
	*/
	sint64() {
		let _this$varint2 = _slicedToArray(this.varint64(), 2), lo = _this$varint2[0], hi = _this$varint2[1];
		let s = -(lo & 1);
		lo = (lo >>> 1 | (hi & 1) << 31) ^ s;
		hi = hi >>> 1 ^ s;
		return protoInt64.dec(lo, hi);
	}
	/**
	* Read a `bool` field, a variant.
	*/
	bool() {
		let _this$varint4 = _slicedToArray(this.varint64(), 2), lo = _this$varint4[0], hi = _this$varint4[1];
		return lo !== 0 || hi !== 0;
	}
	/**
	* Read a `fixed32` field, an unsigned, fixed-length 32-bit integer.
	*/
	fixed32() {
		return this.view.getUint32((this.pos += 4) - 4, true);
	}
	/**
	* Read a `sfixed32` field, a signed, fixed-length 32-bit integer.
	*/
	sfixed32() {
		return this.view.getInt32((this.pos += 4) - 4, true);
	}
	/**
	* Read a `fixed64` field, an unsigned, fixed-length 64 bit integer.
	*/
	fixed64() {
		return protoInt64.uDec(this.sfixed32(), this.sfixed32());
	}
	/**
	* Read a `fixed64` field, a signed, fixed-length 64-bit integer.
	*/
	sfixed64() {
		return protoInt64.dec(this.sfixed32(), this.sfixed32());
	}
	/**
	* Read a `float` field, 32-bit floating point number.
	*/
	float() {
		return this.view.getFloat32((this.pos += 4) - 4, true);
	}
	/**
	* Read a `double` field, a 64-bit floating point number.
	*/
	double() {
		return this.view.getFloat64((this.pos += 8) - 8, true);
	}
	/**
	* Read a `bytes` field, length-delimited arbitrary data.
	*/
	bytes() {
		let len = this.uint32(), start = this.pos;
		this.pos += len;
		this.assertBounds();
		return this.buf.subarray(start, start + len);
	}
	/**
	* Read a `string` field, length-delimited data converted to UTF-8 text.
	*/
	string() {
		return this.textDecoder.decode(this.bytes());
	}
};
/**
* Create a new extension using the given runtime.
*/
function makeExtension(runtime, typeName, extendee, field) {
	let fi;
	return {
		typeName,
		extendee,
		get field() {
			if (!fi) {
				const i = typeof field == "function" ? field() : field;
				i.name = typeName.split(".").pop();
				i.jsonName = "[".concat(typeName, "]");
				fi = runtime.util.newFieldList([i]).list()[0];
			}
			return fi;
		},
		runtime
	};
}
/**
* Create a container that allows us to read extension fields into it with the
* same logic as regular fields.
*/
function createExtensionContainer(extension) {
	const localName = extension.field.localName;
	const container = Object.create(null);
	container[localName] = initExtensionField(extension);
	return [container, () => container[localName]];
}
function initExtensionField(ext) {
	const field = ext.field;
	if (field.repeated) return [];
	if (field.default !== void 0) return field.default;
	switch (field.kind) {
		case "enum": return field.T.values[0].no;
		case "scalar": return scalarZeroValue(field.T, field.L);
		case "message":
			const T = field.T, value = new T();
			return T.fieldWrapper ? T.fieldWrapper.unwrapField(value) : value;
		case "map": throw "map fields are not allowed to be extensions";
	}
}
/**
* Helper to filter unknown fields, optimized based on field type.
*/
function filterUnknownFields(unknownFields, field) {
	if (!field.repeated && (field.kind == "enum" || field.kind == "scalar")) {
		for (let i = unknownFields.length - 1; i >= 0; --i) if (unknownFields[i].no == field.no) return [unknownFields[i]];
		return [];
	}
	return unknownFields.filter((uf) => uf.no === field.no);
}
var encTable = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/".split("");
var decTable = [];
for (let i = 0; i < encTable.length; i++) decTable[encTable[i].charCodeAt(0)] = i;
decTable["-".charCodeAt(0)] = encTable.indexOf("+");
decTable["_".charCodeAt(0)] = encTable.indexOf("/");
var protoBase64 = {
	/**
	* Decodes a base64 string to a byte array.
	*
	* - ignores white-space, including line breaks and tabs
	* - allows inner padding (can decode concatenated base64 strings)
	* - does not require padding
	* - understands base64url encoding:
	*   "-" instead of "+",
	*   "_" instead of "/",
	*   no padding
	*/
	dec(base64Str) {
		let es = base64Str.length * 3 / 4;
		if (base64Str[base64Str.length - 2] == "=") es -= 2;
		else if (base64Str[base64Str.length - 1] == "=") es -= 1;
		let bytes = new Uint8Array(es), bytePos = 0, groupPos = 0, b, p = 0;
		for (let i = 0; i < base64Str.length; i++) {
			b = decTable[base64Str.charCodeAt(i)];
			if (b === void 0) switch (base64Str[i]) {
				case "=": groupPos = 0;
				case "\n":
				case "\r":
				case "	":
				case " ": continue;
				default: throw Error("invalid base64 string.");
			}
			switch (groupPos) {
				case 0:
					p = b;
					groupPos = 1;
					break;
				case 1:
					bytes[bytePos++] = p << 2 | (b & 48) >> 4;
					p = b;
					groupPos = 2;
					break;
				case 2:
					bytes[bytePos++] = (p & 15) << 4 | (b & 60) >> 2;
					p = b;
					groupPos = 3;
					break;
				case 3:
					bytes[bytePos++] = (p & 3) << 6 | b;
					groupPos = 0;
			}
		}
		if (groupPos == 1) throw Error("invalid base64 string.");
		return bytes.subarray(0, bytePos);
	},
	/**
	* Encode a byte array to a base64 string.
	*/
	enc(bytes) {
		let base64 = "", groupPos = 0, b, p = 0;
		for (let i = 0; i < bytes.length; i++) {
			b = bytes[i];
			switch (groupPos) {
				case 0:
					base64 += encTable[b >> 2];
					p = (b & 3) << 4;
					groupPos = 1;
					break;
				case 1:
					base64 += encTable[p | b >> 4];
					p = (b & 15) << 2;
					groupPos = 2;
					break;
				case 2:
					base64 += encTable[p | b >> 6];
					base64 += encTable[b & 63];
					groupPos = 0;
			}
		}
		if (groupPos) {
			base64 += encTable[p];
			base64 += "=";
			if (groupPos == 1) base64 += "=";
		}
		return base64;
	}
};
function getExtension(message, extension, options) {
	assertExtendee(extension, message);
	const opt = extension.runtime.bin.makeReadOptions(options);
	const ufs = filterUnknownFields(message.getType().runtime.bin.listUnknownFields(message), extension.field);
	const _createExtensionConta2 = _slicedToArray(createExtensionContainer(extension), 2), container = _createExtensionConta2[0], get = _createExtensionConta2[1];
	for (const uf of ufs) extension.runtime.bin.readField(container, opt.readerFactory(uf.data), extension.field, uf.wireType, opt);
	return get();
}
/**
* Set an extension value on a message. If the message already has a value for
* this extension, the value is replaced.
*
* If the extension does not extend the given message, an error is raised.
*/
function setExtension(message, extension, value, options) {
	assertExtendee(extension, message);
	const readOpt = extension.runtime.bin.makeReadOptions(options);
	const writeOpt = extension.runtime.bin.makeWriteOptions(options);
	if (hasExtension(message, extension)) {
		const ufs = message.getType().runtime.bin.listUnknownFields(message).filter((uf) => uf.no != extension.field.no);
		message.getType().runtime.bin.discardUnknownFields(message);
		for (const uf of ufs) message.getType().runtime.bin.onUnknownField(message, uf.no, uf.wireType, uf.data);
	}
	const writer = writeOpt.writerFactory();
	let f = extension.field;
	if (!f.opt && !f.repeated && (f.kind == "enum" || f.kind == "scalar")) f = Object.assign(Object.assign({}, extension.field), { opt: true });
	extension.runtime.bin.writeField(f, value, writer, writeOpt);
	const reader = readOpt.readerFactory(writer.finish());
	while (reader.pos < reader.len) {
		const _reader$tag2 = _slicedToArray(reader.tag(), 2), no = _reader$tag2[0], wireType = _reader$tag2[1];
		const data = reader.skip(wireType, no);
		message.getType().runtime.bin.onUnknownField(message, no, wireType, data);
	}
}
/**
* Check whether an extension is set on a message.
*/
function hasExtension(message, extension) {
	const messageType = message.getType();
	return extension.extendee.typeName === messageType.typeName && !!messageType.runtime.bin.listUnknownFields(message).find((uf) => uf.no == extension.field.no);
}
function assertExtendee(extension, message) {
	assert(extension.extendee.typeName == message.getType().typeName, "extension ".concat(extension.typeName, " can only be applied to message ").concat(extension.extendee.typeName));
}
/**
* Returns true if the field is set.
*/
function isFieldSet(field, target) {
	const localName = field.localName;
	if (field.repeated) return target[localName].length > 0;
	if (field.oneof) return target[field.oneof.localName].case === localName;
	switch (field.kind) {
		case "enum":
		case "scalar":
			if (field.opt || field.req) return target[localName] !== void 0;
			if (field.kind == "enum") return target[localName] !== field.T.values[0].no;
			return !isScalarZeroValue(field.T, target[localName]);
		case "message": return target[localName] !== void 0;
		case "map": return Object.keys(target[localName]).length > 0;
	}
}
/**
* Resets the field, so that isFieldSet() will return false.
*/
function clearField(field, target) {
	const localName = field.localName;
	const implicitPresence = !field.opt && !field.req;
	if (field.repeated) target[localName] = [];
	else if (field.oneof) target[field.oneof.localName] = { case: void 0 };
	else switch (field.kind) {
		case "map":
			target[localName] = {};
			break;
		case "enum":
			target[localName] = implicitPresence ? field.T.values[0].no : void 0;
			break;
		case "scalar":
			target[localName] = implicitPresence ? scalarZeroValue(field.T, field.L) : void 0;
			break;
		case "message": target[localName] = void 0;
	}
}
/**
* Check whether the given object is any subtype of Message or is a specific
* Message by passing the type.
*
* Just like `instanceof`, `isMessage` narrows the type. The advantage of
* `isMessage` is that it compares identity by the message type name, not by
* class identity. This makes it robust against the dual package hazard and
* similar situations, where the same message is duplicated.
*
* This function is _mostly_ equivalent to the `instanceof` operator. For
* example, `isMessage(foo, MyMessage)` is the same as `foo instanceof MyMessage`,
* and `isMessage(foo)` is the same as `foo instanceof Message`. In most cases,
* `isMessage` should be preferred over `instanceof`.
*
* However, due to the fact that `isMessage` does not use class identity, there
* are subtle differences between this function and `instanceof`. Notably,
* calling `isMessage` on an explicit type of Message will return false.
*/
function isMessage(arg, type) {
	if (arg === null || typeof arg != "object") return false;
	if (!Object.getOwnPropertyNames(Message.prototype).every((m) => m in arg && typeof arg[m] == "function")) return false;
	const actualType = arg.getType();
	if (actualType === null || typeof actualType != "function" || !("typeName" in actualType) || typeof actualType.typeName != "string") return false;
	return type === void 0 ? true : actualType.typeName == type.typeName;
}
/**
* Wrap a primitive message field value in its corresponding wrapper
* message. This function is idempotent.
*/
function wrapField(type, value) {
	if (isMessage(value) || !type.fieldWrapper) return value;
	return type.fieldWrapper.wrapField(value);
}
ScalarType.DOUBLE, ScalarType.FLOAT, ScalarType.INT64, ScalarType.UINT64, ScalarType.INT32, ScalarType.UINT32, ScalarType.BOOL, ScalarType.STRING, ScalarType.BYTES;
var jsonReadDefaults = { ignoreUnknownFields: false };
var jsonWriteDefaults = {
	emitDefaultValues: false,
	enumAsInteger: false,
	useProtoFieldName: false,
	prettySpaces: 0
};
function makeReadOptions$1(options) {
	return options ? Object.assign(Object.assign({}, jsonReadDefaults), options) : jsonReadDefaults;
}
function makeWriteOptions$1(options) {
	return options ? Object.assign(Object.assign({}, jsonWriteDefaults), options) : jsonWriteDefaults;
}
var tokenNull = Symbol();
var tokenIgnoredUnknownEnum = Symbol();
function makeJsonFormat() {
	return {
		makeReadOptions: makeReadOptions$1,
		makeWriteOptions: makeWriteOptions$1,
		readMessage(type, json, options, message) {
			if (json == null || Array.isArray(json) || typeof json != "object") throw new Error("cannot decode message ".concat(type.typeName, " from JSON: ").concat(debugJsonValue(json)));
			message = message !== null && message !== void 0 ? message : new type();
			const oneofSeen = /* @__PURE__ */ new Map();
			const registry = options.typeRegistry;
			for (const _ref of Object.entries(json)) {
				var _ref2 = _slicedToArray(_ref, 2);
				const jsonKey = _ref2[0];
				const jsonValue = _ref2[1];
				const field = type.fields.findJsonName(jsonKey);
				if (field) {
					if (field.oneof) {
						if (jsonValue === null && field.kind == "scalar") continue;
						const seen = oneofSeen.get(field.oneof);
						if (seen !== void 0) throw new Error("cannot decode message ".concat(type.typeName, " from JSON: multiple keys for oneof \"").concat(field.oneof.name, "\" present: \"").concat(seen, "\", \"").concat(jsonKey, "\""));
						oneofSeen.set(field.oneof, jsonKey);
					}
					readField$1(message, jsonValue, field, options, type);
				} else {
					let found = false;
					if ((registry === null || registry === void 0 ? void 0 : registry.findExtension) && jsonKey.startsWith("[") && jsonKey.endsWith("]")) {
						const ext = registry.findExtension(jsonKey.substring(1, jsonKey.length - 1));
						if (ext && ext.extendee.typeName == type.typeName) {
							found = true;
							const _createExtensionConta2 = _slicedToArray(createExtensionContainer(ext), 2), container = _createExtensionConta2[0], get = _createExtensionConta2[1];
							readField$1(container, jsonValue, ext.field, options, ext);
							setExtension(message, ext, get(), options);
						}
					}
					if (!found && !options.ignoreUnknownFields) throw new Error("cannot decode message ".concat(type.typeName, " from JSON: key \"").concat(jsonKey, "\" is unknown"));
				}
			}
			return message;
		},
		writeMessage(message, options) {
			const type = message.getType();
			const json = {};
			let field;
			try {
				for (field of type.fields.byNumber()) {
					if (!isFieldSet(field, message)) {
						if (field.req) throw "required field not set";
						if (!options.emitDefaultValues) continue;
						if (!canEmitFieldDefaultValue(field)) continue;
					}
					const value = field.oneof ? message[field.oneof.localName].value : message[field.localName];
					const jsonValue = writeField$1(field, value, options);
					if (jsonValue !== void 0) json[options.useProtoFieldName ? field.name : field.jsonName] = jsonValue;
				}
				const registry = options.typeRegistry;
				if (registry === null || registry === void 0 ? void 0 : registry.findExtensionFor) for (const uf of type.runtime.bin.listUnknownFields(message)) {
					const ext = registry.findExtensionFor(type.typeName, uf.no);
					if (ext && hasExtension(message, ext)) {
						const value = getExtension(message, ext, options);
						const jsonValue = writeField$1(ext.field, value, options);
						if (jsonValue !== void 0) json[ext.field.jsonName] = jsonValue;
					}
				}
			} catch (e) {
				const m = field ? "cannot encode field ".concat(type.typeName, ".").concat(field.name, " to JSON") : "cannot encode message ".concat(type.typeName, " to JSON");
				const r = e instanceof Error ? e.message : String(e);
				throw new Error(m + (r.length > 0 ? ": ".concat(r) : ""));
			}
			return json;
		},
		readScalar(type, json, longType) {
			return readScalar$1(type, json, longType !== null && longType !== void 0 ? longType : LongType.BIGINT, true);
		},
		writeScalar(type, value, emitDefaultValues) {
			if (value === void 0) return;
			if (emitDefaultValues || isScalarZeroValue(type, value)) return writeScalar$1(type, value);
		},
		debug: debugJsonValue
	};
}
function debugJsonValue(json) {
	if (json === null) return "null";
	switch (typeof json) {
		case "object": return Array.isArray(json) ? "array" : "object";
		case "string": return json.length > 100 ? "string" : "\"".concat(json.split("\"").join("\\\""), "\"");
		default: return String(json);
	}
}
function readField$1(target, jsonValue, field, options, parentType) {
	let localName = field.localName;
	if (field.repeated) {
		assert(field.kind != "map");
		if (jsonValue === null) return;
		if (!Array.isArray(jsonValue)) throw new Error("cannot decode field ".concat(parentType.typeName, ".").concat(field.name, " from JSON: ").concat(debugJsonValue(jsonValue)));
		const targetArray = target[localName];
		for (const jsonItem of jsonValue) {
			if (jsonItem === null) throw new Error("cannot decode field ".concat(parentType.typeName, ".").concat(field.name, " from JSON: ").concat(debugJsonValue(jsonItem)));
			switch (field.kind) {
				case "message":
					targetArray.push(field.T.fromJson(jsonItem, options));
					break;
				case "enum":
					const enumValue = readEnum(field.T, jsonItem, options.ignoreUnknownFields, true);
					if (enumValue !== tokenIgnoredUnknownEnum) targetArray.push(enumValue);
					break;
				case "scalar": try {
					targetArray.push(readScalar$1(field.T, jsonItem, field.L, true));
				} catch (e) {
					let m = "cannot decode field ".concat(parentType.typeName, ".").concat(field.name, " from JSON: ").concat(debugJsonValue(jsonItem));
					if (e instanceof Error && e.message.length > 0) m += ": ".concat(e.message);
					throw new Error(m);
				}
			}
		}
	} else if (field.kind == "map") {
		if (jsonValue === null) return;
		if (typeof jsonValue != "object" || Array.isArray(jsonValue)) throw new Error("cannot decode field ".concat(parentType.typeName, ".").concat(field.name, " from JSON: ").concat(debugJsonValue(jsonValue)));
		const targetMap = target[localName];
		for (const _ref3 of Object.entries(jsonValue)) {
			var _ref4 = _slicedToArray(_ref3, 2);
			const jsonMapKey = _ref4[0];
			const jsonMapValue = _ref4[1];
			if (jsonMapValue === null) throw new Error("cannot decode field ".concat(parentType.typeName, ".").concat(field.name, " from JSON: map value null"));
			let key;
			try {
				key = readMapKey(field.K, jsonMapKey);
			} catch (e) {
				let m = "cannot decode map key for field ".concat(parentType.typeName, ".").concat(field.name, " from JSON: ").concat(debugJsonValue(jsonValue));
				if (e instanceof Error && e.message.length > 0) m += ": ".concat(e.message);
				throw new Error(m);
			}
			switch (field.V.kind) {
				case "message":
					targetMap[key] = field.V.T.fromJson(jsonMapValue, options);
					break;
				case "enum":
					const enumValue = readEnum(field.V.T, jsonMapValue, options.ignoreUnknownFields, true);
					if (enumValue !== tokenIgnoredUnknownEnum) targetMap[key] = enumValue;
					break;
				case "scalar": try {
					targetMap[key] = readScalar$1(field.V.T, jsonMapValue, LongType.BIGINT, true);
				} catch (e) {
					let m = "cannot decode map value for field ".concat(parentType.typeName, ".").concat(field.name, " from JSON: ").concat(debugJsonValue(jsonValue));
					if (e instanceof Error && e.message.length > 0) m += ": ".concat(e.message);
					throw new Error(m);
				}
			}
		}
	} else {
		if (field.oneof) {
			target = target[field.oneof.localName] = { case: localName };
			localName = "value";
		}
		switch (field.kind) {
			case "message":
				const messageType = field.T;
				if (jsonValue === null && messageType.typeName != "google.protobuf.Value") return;
				let currentValue = target[localName];
				if (isMessage(currentValue)) currentValue.fromJson(jsonValue, options);
				else {
					target[localName] = currentValue = messageType.fromJson(jsonValue, options);
					if (messageType.fieldWrapper && !field.oneof) target[localName] = messageType.fieldWrapper.unwrapField(currentValue);
				}
				break;
			case "enum":
				const enumValue = readEnum(field.T, jsonValue, options.ignoreUnknownFields, false);
				switch (enumValue) {
					case tokenNull:
						clearField(field, target);
						break;
					case tokenIgnoredUnknownEnum: break;
					default: target[localName] = enumValue;
				}
				break;
			case "scalar": try {
				const scalarValue = readScalar$1(field.T, jsonValue, field.L, false);
				switch (scalarValue) {
					case tokenNull:
						clearField(field, target);
						break;
					default: target[localName] = scalarValue;
				}
			} catch (e) {
				let m = "cannot decode field ".concat(parentType.typeName, ".").concat(field.name, " from JSON: ").concat(debugJsonValue(jsonValue));
				if (e instanceof Error && e.message.length > 0) m += ": ".concat(e.message);
				throw new Error(m);
			}
		}
	}
}
function readMapKey(type, json) {
	if (type === ScalarType.BOOL) switch (json) {
		case "true":
			json = true;
			break;
		case "false": json = false;
	}
	return readScalar$1(type, json, LongType.BIGINT, true).toString();
}
function readScalar$1(type, json, longType, nullAsZeroValue) {
	if (json === null) {
		if (nullAsZeroValue) return scalarZeroValue(type, longType);
		return tokenNull;
	}
	switch (type) {
		case ScalarType.DOUBLE:
		case ScalarType.FLOAT:
			if (json === "NaN") return NaN;
			if (json === "Infinity") return Number.POSITIVE_INFINITY;
			if (json === "-Infinity") return Number.NEGATIVE_INFINITY;
			if (json === "") break;
			if (typeof json == "string" && json.trim().length !== json.length) break;
			if (typeof json != "string" && typeof json != "number") break;
			const float = Number(json);
			if (Number.isNaN(float)) break;
			if (!Number.isFinite(float)) break;
			if (type == ScalarType.FLOAT) assertFloat32(float);
			return float;
		case ScalarType.INT32:
		case ScalarType.FIXED32:
		case ScalarType.SFIXED32:
		case ScalarType.SINT32:
		case ScalarType.UINT32:
			let int32;
			if (typeof json == "number") int32 = json;
			else if (typeof json == "string" && json.length > 0) {
				if (json.trim().length === json.length) int32 = Number(json);
			}
			if (int32 === void 0) break;
			if (type == ScalarType.UINT32 || type == ScalarType.FIXED32) assertUInt32(int32);
			else assertInt32(int32);
			return int32;
		case ScalarType.INT64:
		case ScalarType.SFIXED64:
		case ScalarType.SINT64:
			if (typeof json != "number" && typeof json != "string") break;
			const long = protoInt64.parse(json);
			return longType ? long.toString() : long;
		case ScalarType.FIXED64:
		case ScalarType.UINT64:
			if (typeof json != "number" && typeof json != "string") break;
			const uLong = protoInt64.uParse(json);
			return longType ? uLong.toString() : uLong;
		case ScalarType.BOOL:
			if (typeof json !== "boolean") break;
			return json;
		case ScalarType.STRING:
			if (typeof json !== "string") break;
			return json;
		case ScalarType.BYTES:
			if (json === "") return /* @__PURE__ */ new Uint8Array(0);
			if (typeof json !== "string") break;
			return protoBase64.dec(json);
	}
	throw new Error();
}
function readEnum(type, json, ignoreUnknownFields, nullAsZeroValue) {
	if (json === null) {
		if (type.typeName == "google.protobuf.NullValue") return 0;
		return nullAsZeroValue ? type.values[0].no : tokenNull;
	}
	switch (typeof json) {
		case "number":
			if (Number.isInteger(json)) return json;
			break;
		case "string":
			const value = type.findName(json);
			if (value !== void 0) return value.no;
			if (ignoreUnknownFields) return tokenIgnoredUnknownEnum;
	}
	throw new Error("cannot decode enum ".concat(type.typeName, " from JSON: ").concat(debugJsonValue(json)));
}
function canEmitFieldDefaultValue(field) {
	if (field.repeated || field.kind == "map") return true;
	if (field.oneof) return false;
	if (field.kind == "message") return false;
	if (field.opt || field.req) return false;
	return true;
}
function writeField$1(field, value, options) {
	if (field.kind == "map") {
		assert(typeof value == "object" && value != null);
		const jsonObj = {};
		const entries = Object.entries(value);
		switch (field.V.kind) {
			case "scalar":
				for (const _ref5 of entries) {
					var _ref6 = _slicedToArray(_ref5, 2);
					const entryKey = _ref6[0];
					const entryValue = _ref6[1];
					jsonObj[entryKey.toString()] = writeScalar$1(field.V.T, entryValue);
				}
				break;
			case "message":
				for (const _ref7 of entries) {
					var _ref8 = _slicedToArray(_ref7, 2);
					const entryKey = _ref8[0];
					const entryValue = _ref8[1];
					jsonObj[entryKey.toString()] = entryValue.toJson(options);
				}
				break;
			case "enum":
				const enumType = field.V.T;
				for (const _ref9 of entries) {
					var _ref0 = _slicedToArray(_ref9, 2);
					const entryKey = _ref0[0];
					const entryValue = _ref0[1];
					jsonObj[entryKey.toString()] = writeEnum(enumType, entryValue, options.enumAsInteger);
				}
		}
		return options.emitDefaultValues || entries.length > 0 ? jsonObj : void 0;
	}
	if (field.repeated) {
		assert(Array.isArray(value));
		const jsonArr = [];
		switch (field.kind) {
			case "scalar":
				for (let i = 0; i < value.length; i++) jsonArr.push(writeScalar$1(field.T, value[i]));
				break;
			case "enum":
				for (let i = 0; i < value.length; i++) jsonArr.push(writeEnum(field.T, value[i], options.enumAsInteger));
				break;
			case "message": for (let i = 0; i < value.length; i++) jsonArr.push(value[i].toJson(options));
		}
		return options.emitDefaultValues || jsonArr.length > 0 ? jsonArr : void 0;
	}
	switch (field.kind) {
		case "scalar": return writeScalar$1(field.T, value);
		case "enum": return writeEnum(field.T, value, options.enumAsInteger);
		case "message": return wrapField(field.T, value).toJson(options);
	}
}
function writeEnum(type, value, enumAsInteger) {
	var _a;
	assert(typeof value == "number");
	if (type.typeName == "google.protobuf.NullValue") return null;
	if (enumAsInteger) return value;
	const val = type.findNumber(value);
	return (_a = val === null || val === void 0 ? void 0 : val.name) !== null && _a !== void 0 ? _a : value;
}
function writeScalar$1(type, value) {
	switch (type) {
		case ScalarType.INT32:
		case ScalarType.SFIXED32:
		case ScalarType.SINT32:
		case ScalarType.FIXED32:
		case ScalarType.UINT32:
			assert(typeof value == "number");
			return value;
		case ScalarType.FLOAT:
		case ScalarType.DOUBLE:
			assert(typeof value == "number");
			if (Number.isNaN(value)) return "NaN";
			if (value === Number.POSITIVE_INFINITY) return "Infinity";
			if (value === Number.NEGATIVE_INFINITY) return "-Infinity";
			return value;
		case ScalarType.STRING:
			assert(typeof value == "string");
			return value;
		case ScalarType.BOOL:
			assert(typeof value == "boolean");
			return value;
		case ScalarType.UINT64:
		case ScalarType.FIXED64:
		case ScalarType.INT64:
		case ScalarType.SFIXED64:
		case ScalarType.SINT64:
			assert(typeof value == "bigint" || typeof value == "string" || typeof value == "number");
			return value.toString();
		case ScalarType.BYTES:
			assert(value instanceof Uint8Array);
			return protoBase64.enc(value);
	}
}
var unknownFieldsSymbol = Symbol("@bufbuild/protobuf/unknown-fields");
var readDefaults = {
	readUnknownFields: true,
	readerFactory: (bytes) => new BinaryReader(bytes)
};
var writeDefaults = {
	writeUnknownFields: true,
	writerFactory: () => new BinaryWriter()
};
function makeReadOptions(options) {
	return options ? Object.assign(Object.assign({}, readDefaults), options) : readDefaults;
}
function makeWriteOptions(options) {
	return options ? Object.assign(Object.assign({}, writeDefaults), options) : writeDefaults;
}
function makeBinaryFormat() {
	return {
		makeReadOptions,
		makeWriteOptions,
		listUnknownFields(message) {
			var _a;
			return (_a = message[unknownFieldsSymbol]) !== null && _a !== void 0 ? _a : [];
		},
		discardUnknownFields(message) {
			delete message[unknownFieldsSymbol];
		},
		writeUnknownFields(message, writer) {
			const c = message[unknownFieldsSymbol];
			if (c) for (const f of c) writer.tag(f.no, f.wireType).raw(f.data);
		},
		onUnknownField(message, no, wireType, data) {
			const m = message;
			if (!Array.isArray(m[unknownFieldsSymbol])) m[unknownFieldsSymbol] = [];
			m[unknownFieldsSymbol].push({
				no,
				wireType,
				data
			});
		},
		readMessage(message, reader, lengthOrEndTagFieldNo, options, delimitedMessageEncoding) {
			const type = message.getType();
			const end = delimitedMessageEncoding ? reader.len : reader.pos + lengthOrEndTagFieldNo;
			let fieldNo, wireType;
			while (reader.pos < end) {
				var _reader$tag2 = _slicedToArray(reader.tag(), 2);
				fieldNo = _reader$tag2[0];
				wireType = _reader$tag2[1];
				if (delimitedMessageEncoding === true && wireType == WireType.EndGroup) break;
				const field = type.fields.find(fieldNo);
				if (!field) {
					const data = reader.skip(wireType, fieldNo);
					if (options.readUnknownFields) this.onUnknownField(message, fieldNo, wireType, data);
					continue;
				}
				readField(message, reader, field, wireType, options);
			}
			if (delimitedMessageEncoding && (wireType != WireType.EndGroup || fieldNo !== lengthOrEndTagFieldNo)) throw new Error("invalid end group tag");
		},
		readField,
		writeMessage(message, writer, options) {
			const type = message.getType();
			for (const field of type.fields.byNumber()) {
				if (!isFieldSet(field, message)) {
					if (field.req) throw new Error("cannot encode field ".concat(type.typeName, ".").concat(field.name, " to binary: required field not set"));
					continue;
				}
				writeField(field, field.oneof ? message[field.oneof.localName].value : message[field.localName], writer, options);
			}
			if (options.writeUnknownFields) this.writeUnknownFields(message, writer);
			return writer;
		},
		writeField(field, value, writer, options) {
			if (value === void 0) return;
			writeField(field, value, writer, options);
		}
	};
}
function readField(target, reader, field, wireType, options) {
	let repeated = field.repeated, localName = field.localName;
	if (field.oneof) {
		target = target[field.oneof.localName];
		if (target.case != localName) delete target.value;
		target.case = localName;
		localName = "value";
	}
	switch (field.kind) {
		case "scalar":
		case "enum":
			const scalarType = field.kind == "enum" ? ScalarType.INT32 : field.T;
			let read = readScalar;
			if (field.kind == "scalar" && field.L > 0) read = readScalarLTString;
			if (repeated) {
				let arr = target[localName];
				if (wireType == WireType.LengthDelimited && scalarType != ScalarType.STRING && scalarType != ScalarType.BYTES) {
					let e = reader.uint32() + reader.pos;
					while (reader.pos < e) arr.push(read(reader, scalarType));
				} else arr.push(read(reader, scalarType));
			} else target[localName] = read(reader, scalarType);
			break;
		case "message":
			const messageType = field.T;
			if (repeated) target[localName].push(readMessageField(reader, new messageType(), options, field));
			else if (isMessage(target[localName])) readMessageField(reader, target[localName], options, field);
			else {
				target[localName] = readMessageField(reader, new messageType(), options, field);
				if (messageType.fieldWrapper && !field.oneof && !field.repeated) target[localName] = messageType.fieldWrapper.unwrapField(target[localName]);
			}
			break;
		case "map":
			let _readMapEntry2 = _slicedToArray(readMapEntry(field, reader, options), 2), mapKey = _readMapEntry2[0], mapVal = _readMapEntry2[1];
			target[localName][mapKey] = mapVal;
	}
}
function readMessageField(reader, message, options, field) {
	const format = message.getType().runtime.bin;
	const delimited = field === null || field === void 0 ? void 0 : field.delimited;
	format.readMessage(message, reader, delimited ? field.no : reader.uint32(), options, delimited);
	return message;
}
function readMapEntry(field, reader, options) {
	const length = reader.uint32(), end = reader.pos + length;
	let key, val;
	while (reader.pos < end) switch (_slicedToArray(reader.tag(), 1)[0]) {
		case 1:
			key = readScalar(reader, field.K);
			break;
		case 2: switch (field.V.kind) {
			case "scalar":
				val = readScalar(reader, field.V.T);
				break;
			case "enum":
				val = reader.int32();
				break;
			case "message": val = readMessageField(reader, new field.V.T(), options, void 0);
		}
	}
	if (key === void 0) key = scalarZeroValue(field.K, LongType.BIGINT);
	if (typeof key != "string" && typeof key != "number") key = key.toString();
	if (val === void 0) switch (field.V.kind) {
		case "scalar":
			val = scalarZeroValue(field.V.T, LongType.BIGINT);
			break;
		case "enum":
			val = field.V.T.values[0].no;
			break;
		case "message": val = new field.V.T();
	}
	return [key, val];
}
function readScalarLTString(reader, type) {
	const v = readScalar(reader, type);
	return typeof v == "bigint" ? v.toString() : v;
}
function readScalar(reader, type) {
	switch (type) {
		case ScalarType.STRING: return reader.string();
		case ScalarType.BOOL: return reader.bool();
		case ScalarType.DOUBLE: return reader.double();
		case ScalarType.FLOAT: return reader.float();
		case ScalarType.INT32: return reader.int32();
		case ScalarType.INT64: return reader.int64();
		case ScalarType.UINT64: return reader.uint64();
		case ScalarType.FIXED64: return reader.fixed64();
		case ScalarType.BYTES: return reader.bytes();
		case ScalarType.FIXED32: return reader.fixed32();
		case ScalarType.SFIXED32: return reader.sfixed32();
		case ScalarType.SFIXED64: return reader.sfixed64();
		case ScalarType.SINT64: return reader.sint64();
		case ScalarType.UINT32: return reader.uint32();
		case ScalarType.SINT32: return reader.sint32();
	}
}
function writeField(field, value, writer, options) {
	assert(value !== void 0);
	const repeated = field.repeated;
	switch (field.kind) {
		case "scalar":
		case "enum":
			let scalarType = field.kind == "enum" ? ScalarType.INT32 : field.T;
			if (repeated) {
				assert(Array.isArray(value));
				if (field.packed) writePacked(writer, scalarType, field.no, value);
				else for (const item of value) writeScalar(writer, scalarType, field.no, item);
			} else writeScalar(writer, scalarType, field.no, value);
			break;
		case "message":
			if (repeated) {
				assert(Array.isArray(value));
				for (const item of value) writeMessageField(writer, options, field, item);
			} else writeMessageField(writer, options, field, value);
			break;
		case "map":
			assert(typeof value == "object" && value != null);
			for (const _ref of Object.entries(value)) {
				var _ref2 = _slicedToArray(_ref, 2);
				const key = _ref2[0];
				const val = _ref2[1];
				writeMapEntry(writer, options, field, key, val);
			}
	}
}
function writeMapEntry(writer, options, field, key, value) {
	writer.tag(field.no, WireType.LengthDelimited);
	writer.fork();
	let keyValue = key;
	switch (field.K) {
		case ScalarType.INT32:
		case ScalarType.FIXED32:
		case ScalarType.UINT32:
		case ScalarType.SFIXED32:
		case ScalarType.SINT32:
			keyValue = Number.parseInt(key);
			break;
		case ScalarType.BOOL:
			assert(key == "true" || key == "false");
			keyValue = key == "true";
	}
	writeScalar(writer, field.K, 1, keyValue);
	switch (field.V.kind) {
		case "scalar":
			writeScalar(writer, field.V.T, 2, value);
			break;
		case "enum":
			writeScalar(writer, ScalarType.INT32, 2, value);
			break;
		case "message":
			assert(value !== void 0);
			writer.tag(2, WireType.LengthDelimited).bytes(value.toBinary(options));
	}
	writer.join();
}
function writeMessageField(writer, options, field, value) {
	const message = wrapField(field.T, value);
	if (field.delimited) writer.tag(field.no, WireType.StartGroup).raw(message.toBinary(options)).tag(field.no, WireType.EndGroup);
	else writer.tag(field.no, WireType.LengthDelimited).bytes(message.toBinary(options));
}
function writeScalar(writer, type, fieldNo, value) {
	assert(value !== void 0);
	let _scalarTypeInfo2 = _slicedToArray(scalarTypeInfo(type), 2), wireType = _scalarTypeInfo2[0], method = _scalarTypeInfo2[1];
	writer.tag(fieldNo, wireType)[method](value);
}
function writePacked(writer, type, fieldNo, value) {
	if (!value.length) return;
	writer.tag(fieldNo, WireType.LengthDelimited).fork();
	let method = _slicedToArray(scalarTypeInfo(type), 2)[1];
	for (let i = 0; i < value.length; i++) writer[method](value[i]);
	writer.join();
}
/**
* Get information for writing a scalar value.
*
* Returns tuple:
* [0]: appropriate WireType
* [1]: name of the appropriate method of IBinaryWriter
* [2]: whether the given value is a default value for proto3 semantics
*
* If argument `value` is omitted, [2] is always false.
*/
function scalarTypeInfo(type) {
	let wireType = WireType.Varint;
	switch (type) {
		case ScalarType.BYTES:
		case ScalarType.STRING:
			wireType = WireType.LengthDelimited;
			break;
		case ScalarType.DOUBLE:
		case ScalarType.FIXED64:
		case ScalarType.SFIXED64:
			wireType = WireType.Bit64;
			break;
		case ScalarType.FIXED32:
		case ScalarType.SFIXED32:
		case ScalarType.FLOAT: wireType = WireType.Bit32;
	}
	const method = ScalarType[type].toLowerCase();
	return [wireType, method];
}
function makeUtilCommon() {
	return {
		setEnumType,
		initPartial(source, target) {
			if (source === void 0) return;
			const type = target.getType();
			for (const member of type.fields.byMember()) {
				const localName = member.localName, t = target, s = source;
				if (s[localName] == null) continue;
				switch (member.kind) {
					case "oneof":
						const sk = s[localName].case;
						if (sk === void 0) continue;
						const sourceField = member.findField(sk);
						let val = s[localName].value;
						if (sourceField && sourceField.kind == "message" && !isMessage(val, sourceField.T)) val = new sourceField.T(val);
						else if (sourceField && sourceField.kind === "scalar" && sourceField.T === ScalarType.BYTES) val = toU8Arr(val);
						t[localName] = {
							case: sk,
							value: val
						};
						break;
					case "scalar":
					case "enum":
						let copy = s[localName];
						if (member.T === ScalarType.BYTES) copy = member.repeated ? copy.map(toU8Arr) : toU8Arr(copy);
						t[localName] = copy;
						break;
					case "map":
						switch (member.V.kind) {
							case "scalar":
							case "enum":
								if (member.V.T === ScalarType.BYTES) for (const _ref of Object.entries(s[localName])) {
									var _ref2 = _slicedToArray(_ref, 2);
									const k = _ref2[0];
									const v = _ref2[1];
									t[localName][k] = toU8Arr(v);
								}
								else Object.assign(t[localName], s[localName]);
								break;
							case "message":
								const messageType = member.V.T;
								for (const k of Object.keys(s[localName])) {
									let val = s[localName][k];
									if (!messageType.fieldWrapper) val = new messageType(val);
									t[localName][k] = val;
								}
						}
						break;
					case "message":
						const mt = member.T;
						if (member.repeated) t[localName] = s[localName].map((val) => isMessage(val, mt) ? val : new mt(val));
						else {
							const val = s[localName];
							if (mt.fieldWrapper) {
								if (mt.typeName === "google.protobuf.BytesValue") t[localName] = toU8Arr(val);
								else t[localName] = val;
							} else t[localName] = isMessage(val, mt) ? val : new mt(val);
						}
				}
			}
		},
		equals(type, a, b) {
			if (a === b) return true;
			if (!a || !b) return false;
			return type.fields.byMember().every((m) => {
				const va = a[m.localName];
				const vb = b[m.localName];
				if (m.repeated) {
					if (va.length !== vb.length) return false;
					switch (m.kind) {
						case "message": return va.every((a, i) => m.T.equals(a, vb[i]));
						case "scalar": return va.every((a, i) => scalarEquals(m.T, a, vb[i]));
						case "enum": return va.every((a, i) => scalarEquals(ScalarType.INT32, a, vb[i]));
					}
					throw new Error("repeated cannot contain ".concat(m.kind));
				}
				switch (m.kind) {
					case "message":
						let a = va;
						let b = vb;
						if (m.T.fieldWrapper) {
							if (a !== void 0 && !isMessage(a)) a = m.T.fieldWrapper.wrapField(a);
							if (b !== void 0 && !isMessage(b)) b = m.T.fieldWrapper.wrapField(b);
						}
						return m.T.equals(a, b);
					case "enum": return scalarEquals(ScalarType.INT32, va, vb);
					case "scalar": return scalarEquals(m.T, va, vb);
					case "oneof":
						if (va.case !== vb.case) return false;
						const s = m.findField(va.case);
						if (s === void 0) return true;
						switch (s.kind) {
							case "message": return s.T.equals(va.value, vb.value);
							case "enum": return scalarEquals(ScalarType.INT32, va.value, vb.value);
							case "scalar": return scalarEquals(s.T, va.value, vb.value);
						}
						throw new Error("oneof cannot contain ".concat(s.kind));
					case "map":
						const keys = Object.keys(va).concat(Object.keys(vb));
						switch (m.V.kind) {
							case "message":
								const messageType = m.V.T;
								return keys.every((k) => messageType.equals(va[k], vb[k]));
							case "enum": return keys.every((k) => scalarEquals(ScalarType.INT32, va[k], vb[k]));
							case "scalar":
								const scalarType = m.V.T;
								return keys.every((k) => scalarEquals(scalarType, va[k], vb[k]));
						}
				}
			});
		},
		clone(message) {
			const type = message.getType(), target = new type(), any = target;
			for (const member of type.fields.byMember()) {
				const source = message[member.localName];
				let copy;
				if (member.repeated) copy = source.map(cloneSingularField);
				else if (member.kind == "map") {
					copy = any[member.localName];
					for (const _ref3 of Object.entries(source)) {
						var _ref4 = _slicedToArray(_ref3, 2);
						const key = _ref4[0];
						const v = _ref4[1];
						copy[key] = cloneSingularField(v);
					}
				} else if (member.kind == "oneof") copy = member.findField(source.case) ? {
					case: source.case,
					value: cloneSingularField(source.value)
				} : { case: void 0 };
				else copy = cloneSingularField(source);
				any[member.localName] = copy;
			}
			for (const uf of type.runtime.bin.listUnknownFields(message)) type.runtime.bin.onUnknownField(any, uf.no, uf.wireType, uf.data);
			return target;
		}
	};
}
function cloneSingularField(value) {
	if (value === void 0) return value;
	if (isMessage(value)) return value.clone();
	if (value instanceof Uint8Array) {
		const c = new Uint8Array(value.byteLength);
		c.set(value);
		return c;
	}
	return value;
}
function toU8Arr(input) {
	return input instanceof Uint8Array ? input : new Uint8Array(input);
}
function makeProtoRuntime(syntax, newFieldList, initFields) {
	return {
		syntax,
		json: makeJsonFormat(),
		bin: makeBinaryFormat(),
		util: Object.assign(Object.assign({}, makeUtilCommon()), {
			newFieldList,
			initFields
		}),
		makeMessageType(typeName, fields, opt) {
			return makeMessageType(this, typeName, fields, opt);
		},
		makeEnum,
		makeEnumType,
		getEnumType,
		makeExtension(typeName, extendee, field) {
			return makeExtension(this, typeName, extendee, field);
		}
	};
}
var InternalFieldList = class {
	constructor(fields, normalizer) {
		this._fields = fields;
		this._normalizer = normalizer;
	}
	findJsonName(jsonName) {
		if (!this.jsonNames) {
			const t = {};
			for (const f of this.list()) t[f.jsonName] = t[f.name] = f;
			this.jsonNames = t;
		}
		return this.jsonNames[jsonName];
	}
	find(fieldNo) {
		if (!this.numbers) {
			const t = {};
			for (const f of this.list()) t[f.no] = f;
			this.numbers = t;
		}
		return this.numbers[fieldNo];
	}
	list() {
		if (!this.all) this.all = this._normalizer(this._fields);
		return this.all;
	}
	byNumber() {
		if (!this.numbersAsc) this.numbersAsc = this.list().concat().sort((a, b) => a.no - b.no);
		return this.numbersAsc;
	}
	byMember() {
		if (!this.members) {
			this.members = [];
			const a = this.members;
			let o;
			for (const f of this.list()) if (f.oneof) {
				if (f.oneof !== o) {
					o = f.oneof;
					a.push(o);
				}
			} else a.push(f);
		}
		return this.members;
	}
};
/**
* Returns the name of a protobuf element in generated code.
*
* Field names - including oneofs - are converted to lowerCamelCase. For
* messages, enumerations and services, the package name is stripped from
* the type name. For nested messages and enumerations, the names are joined
* with an underscore. For methods, the first character is made lowercase.
*/
/**
* Returns the name of a field in generated code.
*/
function localFieldName(protoName, inOneof) {
	const name = protoCamelCase(protoName);
	if (inOneof) return name;
	return safeObjectProperty(safeMessageProperty(name));
}
/**
* Returns the name of a oneof group in generated code.
*/
function localOneofName(protoName) {
	return localFieldName(protoName, false);
}
/**
* Returns the JSON name for a protobuf field, exactly like protoc does.
*/
var fieldJsonName = protoCamelCase;
/**
* Converts snake_case to protoCamelCase according to the convention
* used by protoc to convert a field name to a JSON name.
*/
function protoCamelCase(snakeCase) {
	let capNext = false;
	const b = [];
	for (let i = 0; i < snakeCase.length; i++) {
		let c = snakeCase.charAt(i);
		switch (c) {
			case "_":
				capNext = true;
				break;
			case "0":
			case "1":
			case "2":
			case "3":
			case "4":
			case "5":
			case "6":
			case "7":
			case "8":
			case "9":
				b.push(c);
				capNext = false;
				break;
			default:
				if (capNext) {
					capNext = false;
					c = c.toUpperCase();
				}
				b.push(c);
		}
	}
	return b.join("");
}
/**
* Names that cannot be used for object properties because they are reserved
* by built-in JavaScript properties.
*/
var reservedObjectProperties = /* @__PURE__ */ new Set([
	"constructor",
	"toString",
	"toJSON",
	"valueOf"
]);
/**
* Names that cannot be used for object properties because they are reserved
* by the runtime.
*/
var reservedMessageProperties = /* @__PURE__ */ new Set([
	"getType",
	"clone",
	"equals",
	"fromBinary",
	"fromJson",
	"fromJsonString",
	"toBinary",
	"toJson",
	"toJsonString",
	"toObject"
]);
var fallback = (name) => "".concat(name, "$");
/**
* Will wrap names that are Object prototype properties or names reserved
* for `Message`s.
*/
var safeMessageProperty = (name) => {
	if (reservedMessageProperties.has(name)) return fallback(name);
	return name;
};
/**
* Names that cannot be used for object properties because they are reserved
* by built-in JavaScript properties.
*/
var safeObjectProperty = (name) => {
	if (reservedObjectProperties.has(name)) return fallback(name);
	return name;
};
var InternalOneofInfo = class {
	constructor(name) {
		this.kind = "oneof";
		this.repeated = false;
		this.packed = false;
		this.opt = false;
		this.req = false;
		this.default = void 0;
		this.fields = [];
		this.name = name;
		this.localName = localOneofName(name);
	}
	addField(field) {
		assert(field.oneof === this, "field ".concat(field.name, " not one of ").concat(this.name));
		this.fields.push(field);
	}
	findField(localName) {
		if (!this._lookup) {
			this._lookup = Object.create(null);
			for (let i = 0; i < this.fields.length; i++) this._lookup[this.fields[i].localName] = this.fields[i];
		}
		return this._lookup[localName];
	}
};
/**
* Convert a collection of field info to an array of normalized FieldInfo.
*
* The argument `packedByDefault` specifies whether fields that do not specify
* `packed` should be packed (proto3) or unpacked (proto2).
*/
function normalizeFieldInfos(fieldInfos, packedByDefault) {
	var _a, _b, _c, _d, _e, _f;
	const r = [];
	let o;
	for (const field of typeof fieldInfos == "function" ? fieldInfos() : fieldInfos) {
		const f = field;
		f.localName = localFieldName(field.name, field.oneof !== void 0);
		f.jsonName = (_a = field.jsonName) !== null && _a !== void 0 ? _a : fieldJsonName(field.name);
		f.repeated = (_b = field.repeated) !== null && _b !== void 0 ? _b : false;
		if (field.kind == "scalar") f.L = (_c = field.L) !== null && _c !== void 0 ? _c : LongType.BIGINT;
		f.delimited = (_d = field.delimited) !== null && _d !== void 0 ? _d : false;
		f.req = (_e = field.req) !== null && _e !== void 0 ? _e : false;
		f.opt = (_f = field.opt) !== null && _f !== void 0 ? _f : false;
		if (field.packed === void 0) f.packed = field.kind == "enum" || field.kind == "scalar" && field.T != ScalarType.BYTES && field.T != ScalarType.STRING;
		if (field.oneof !== void 0) {
			const ooname = typeof field.oneof == "string" ? field.oneof : field.oneof.name;
			if (!o || o.name != ooname) o = new InternalOneofInfo(ooname);
			f.oneof = o;
			o.addField(f);
		}
		r.push(f);
	}
	return r;
}
/**
* Provides functionality for messages defined with the proto3 syntax.
*/
var proto3 = makeProtoRuntime("proto3", (fields) => {
	return new InternalFieldList(fields, (source) => normalizeFieldInfos(source));
}, (target) => {
	for (const member of target.getType().fields.byMember()) {
		if (member.opt) continue;
		const name = member.localName, t = target;
		if (member.repeated) {
			t[name] = [];
			continue;
		}
		switch (member.kind) {
			case "oneof":
				t[name] = { case: void 0 };
				break;
			case "enum":
				t[name] = 0;
				break;
			case "map":
				t[name] = {};
				break;
			case "scalar": t[name] = scalarZeroValue(member.T, member.L);
		}
	}
});
/**
* A Timestamp represents a point in time independent of any time zone or local
* calendar, encoded as a count of seconds and fractions of seconds at
* nanosecond resolution. The count is relative to an epoch at UTC midnight on
* January 1, 1970, in the proleptic Gregorian calendar which extends the
* Gregorian calendar backwards to year one.
*
* All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
* second table is needed for interpretation, using a [24-hour linear
* smear](https://developers.google.com/time/smear).
*
* The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
* restricting to that range, we ensure that we can convert to and from [RFC
* 3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
*
* # Examples
*
* Example 1: Compute Timestamp from POSIX `time()`.
*
*     Timestamp timestamp;
*     timestamp.set_seconds(time(NULL));
*     timestamp.set_nanos(0);
*
* Example 2: Compute Timestamp from POSIX `gettimeofday()`.
*
*     struct timeval tv;
*     gettimeofday(&tv, NULL);
*
*     Timestamp timestamp;
*     timestamp.set_seconds(tv.tv_sec);
*     timestamp.set_nanos(tv.tv_usec * 1000);
*
* Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
*
*     FILETIME ft;
*     GetSystemTimeAsFileTime(&ft);
*     UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
*
*     // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
*     // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
*     Timestamp timestamp;
*     timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
*     timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
*
* Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
*
*     long millis = System.currentTimeMillis();
*
*     Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
*         .setNanos((int) ((millis % 1000) * 1000000)).build();
*
* Example 5: Compute Timestamp from Java `Instant.now()`.
*
*     Instant now = Instant.now();
*
*     Timestamp timestamp =
*         Timestamp.newBuilder().setSeconds(now.getEpochSecond())
*             .setNanos(now.getNano()).build();
*
* Example 6: Compute Timestamp from current time in Python.
*
*     timestamp = Timestamp()
*     timestamp.GetCurrentTime()
*
* # JSON Mapping
*
* In JSON format, the Timestamp type is encoded as a string in the
* [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
* format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
* where {year} is always expressed using four digits while {month}, {day},
* {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
* seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
* are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
* is required. A proto3 JSON serializer should always use UTC (as indicated by
* "Z") when printing the Timestamp type and a proto3 JSON parser should be
* able to accept both UTC and other timezones (as indicated by an offset).
*
* For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
* 01:30 UTC on January 15, 2017.
*
* In JavaScript, one can convert a Date object to this format using the
* standard
* [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
* method. In Python, a standard `datetime.datetime` object can be converted
* to this format using
* [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
* the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
* the Joda Time's [`ISODateTimeFormat.dateTime()`](
* http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
* ) to obtain a formatter capable of generating timestamps in this format.
*
*
* @generated from message google.protobuf.Timestamp
*/
var Timestamp = class Timestamp extends Message {
	constructor(data) {
		super();
		/**
		* Represents seconds of UTC time since Unix epoch
		* 1970-01-01T00:00:00Z. Must be from 0001-01-01T00:00:00Z to
		* 9999-12-31T23:59:59Z inclusive.
		*
		* @generated from field: int64 seconds = 1;
		*/
		this.seconds = protoInt64.zero;
		/**
		* Non-negative fractions of a second at nanosecond resolution. Negative
		* second values with fractions must still have non-negative nanos values
		* that count forward in time. Must be from 0 to 999,999,999
		* inclusive.
		*
		* @generated from field: int32 nanos = 2;
		*/
		this.nanos = 0;
		proto3.util.initPartial(data, this);
	}
	fromJson(json, options) {
		if (typeof json !== "string") throw new Error("cannot decode google.protobuf.Timestamp from JSON: ".concat(proto3.json.debug(json)));
		const matches = json.match(/^([0-9]{4})-([0-9]{2})-([0-9]{2})T([0-9]{2}):([0-9]{2}):([0-9]{2})(?:Z|\.([0-9]{3,9})Z|([+-][0-9][0-9]:[0-9][0-9]))$/);
		if (!matches) throw new Error("cannot decode google.protobuf.Timestamp from JSON: invalid RFC 3339 string");
		const ms = Date.parse(matches[1] + "-" + matches[2] + "-" + matches[3] + "T" + matches[4] + ":" + matches[5] + ":" + matches[6] + (matches[8] ? matches[8] : "Z"));
		if (Number.isNaN(ms)) throw new Error("cannot decode google.protobuf.Timestamp from JSON: invalid RFC 3339 string");
		if (ms < Date.parse("0001-01-01T00:00:00Z") || ms > Date.parse("9999-12-31T23:59:59Z")) throw new Error("cannot decode message google.protobuf.Timestamp from JSON: must be from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59Z inclusive");
		this.seconds = protoInt64.parse(ms / 1e3);
		this.nanos = 0;
		if (matches[7]) this.nanos = parseInt("1" + matches[7] + "0".repeat(9 - matches[7].length)) - 1e9;
		return this;
	}
	toJson(options) {
		const ms = Number(this.seconds) * 1e3;
		if (ms < Date.parse("0001-01-01T00:00:00Z") || ms > Date.parse("9999-12-31T23:59:59Z")) throw new Error("cannot encode google.protobuf.Timestamp to JSON: must be from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59Z inclusive");
		if (this.nanos < 0) throw new Error("cannot encode google.protobuf.Timestamp to JSON: nanos must not be negative");
		let z = "Z";
		if (this.nanos > 0) {
			const nanosStr = (this.nanos + 1e9).toString().substring(1);
			if (nanosStr.substring(3) === "000000") z = "." + nanosStr.substring(0, 3) + "Z";
			else if (nanosStr.substring(6) === "000") z = "." + nanosStr.substring(0, 6) + "Z";
			else z = "." + nanosStr + "Z";
		}
		return new Date(ms).toISOString().replace(".000Z", z);
	}
	toDate() {
		return new Date(Number(this.seconds) * 1e3 + Math.ceil(this.nanos / 1e6));
	}
	static now() {
		return Timestamp.fromDate(/* @__PURE__ */ new Date());
	}
	static fromDate(date) {
		const ms = date.getTime();
		return new Timestamp({
			seconds: protoInt64.parse(Math.floor(ms / 1e3)),
			nanos: ms % 1e3 * 1e6
		});
	}
	static fromBinary(bytes, options) {
		return new Timestamp().fromBinary(bytes, options);
	}
	static fromJson(jsonValue, options) {
		return new Timestamp().fromJson(jsonValue, options);
	}
	static fromJsonString(jsonString, options) {
		return new Timestamp().fromJsonString(jsonString, options);
	}
	static equals(a, b) {
		return proto3.util.equals(Timestamp, a, b);
	}
};
Timestamp.runtime = proto3;
Timestamp.typeName = "google.protobuf.Timestamp";
Timestamp.fields = proto3.util.newFieldList(() => [{
	no: 1,
	name: "seconds",
	kind: "scalar",
	T: 3
}, {
	no: 2,
	name: "nanos",
	kind: "scalar",
	T: 5
}]);
var MetricsBatch = /* @__PURE__ */ proto3.makeMessageType("livekit.MetricsBatch", () => [
	{
		no: 1,
		name: "timestamp_ms",
		kind: "scalar",
		T: 3
	},
	{
		no: 2,
		name: "normalized_timestamp",
		kind: "message",
		T: Timestamp
	},
	{
		no: 3,
		name: "str_data",
		kind: "scalar",
		T: 9,
		repeated: true
	},
	{
		no: 4,
		name: "time_series",
		kind: "message",
		T: TimeSeriesMetric,
		repeated: true
	},
	{
		no: 5,
		name: "events",
		kind: "message",
		T: EventMetric,
		repeated: true
	}
]);
var TimeSeriesMetric = /* @__PURE__ */ proto3.makeMessageType("livekit.TimeSeriesMetric", () => [
	{
		no: 1,
		name: "label",
		kind: "scalar",
		T: 13
	},
	{
		no: 2,
		name: "participant_identity",
		kind: "scalar",
		T: 13
	},
	{
		no: 3,
		name: "track_sid",
		kind: "scalar",
		T: 13
	},
	{
		no: 4,
		name: "samples",
		kind: "message",
		T: MetricSample,
		repeated: true
	},
	{
		no: 5,
		name: "rid",
		kind: "scalar",
		T: 13
	}
]);
var MetricSample = /* @__PURE__ */ proto3.makeMessageType("livekit.MetricSample", () => [
	{
		no: 1,
		name: "timestamp_ms",
		kind: "scalar",
		T: 3
	},
	{
		no: 2,
		name: "normalized_timestamp",
		kind: "message",
		T: Timestamp
	},
	{
		no: 3,
		name: "value",
		kind: "scalar",
		T: 2
	}
]);
var EventMetric = /* @__PURE__ */ proto3.makeMessageType("livekit.EventMetric", () => [
	{
		no: 1,
		name: "label",
		kind: "scalar",
		T: 13
	},
	{
		no: 2,
		name: "participant_identity",
		kind: "scalar",
		T: 13
	},
	{
		no: 3,
		name: "track_sid",
		kind: "scalar",
		T: 13
	},
	{
		no: 4,
		name: "start_timestamp_ms",
		kind: "scalar",
		T: 3
	},
	{
		no: 5,
		name: "end_timestamp_ms",
		kind: "scalar",
		T: 3,
		opt: true
	},
	{
		no: 6,
		name: "normalized_start_timestamp",
		kind: "message",
		T: Timestamp
	},
	{
		no: 7,
		name: "normalized_end_timestamp",
		kind: "message",
		T: Timestamp,
		opt: true
	},
	{
		no: 8,
		name: "metadata",
		kind: "scalar",
		T: 9
	},
	{
		no: 9,
		name: "rid",
		kind: "scalar",
		T: 13
	}
]);
var AudioCodec = /* @__PURE__ */ proto3.makeEnum("livekit.AudioCodec", [
	{
		no: 0,
		name: "DEFAULT_AC"
	},
	{
		no: 1,
		name: "OPUS"
	},
	{
		no: 2,
		name: "AAC"
	},
	{
		no: 3,
		name: "AC_MP3"
	}
]);
var VideoCodec = /* @__PURE__ */ proto3.makeEnum("livekit.VideoCodec", [
	{
		no: 0,
		name: "DEFAULT_VC"
	},
	{
		no: 1,
		name: "H264_BASELINE"
	},
	{
		no: 2,
		name: "H264_MAIN"
	},
	{
		no: 3,
		name: "H264_HIGH"
	},
	{
		no: 4,
		name: "VP8"
	}
]);
var ImageCodec = /* @__PURE__ */ proto3.makeEnum("livekit.ImageCodec", [{
	no: 0,
	name: "IC_DEFAULT"
}, {
	no: 1,
	name: "IC_JPEG"
}]);
var BackupCodecPolicy$1 = /* @__PURE__ */ proto3.makeEnum("livekit.BackupCodecPolicy", [
	{
		no: 0,
		name: "PREFER_REGRESSION"
	},
	{
		no: 1,
		name: "SIMULCAST"
	},
	{
		no: 2,
		name: "REGRESSION"
	}
]);
var TrackType = /* @__PURE__ */ proto3.makeEnum("livekit.TrackType", [
	{
		no: 0,
		name: "AUDIO"
	},
	{
		no: 1,
		name: "VIDEO"
	},
	{
		no: 2,
		name: "DATA"
	}
]);
var TrackSource = /* @__PURE__ */ proto3.makeEnum("livekit.TrackSource", [
	{
		no: 0,
		name: "UNKNOWN"
	},
	{
		no: 1,
		name: "CAMERA"
	},
	{
		no: 2,
		name: "MICROPHONE"
	},
	{
		no: 3,
		name: "SCREEN_SHARE"
	},
	{
		no: 4,
		name: "SCREEN_SHARE_AUDIO"
	}
]);
var VideoQuality$1 = /* @__PURE__ */ proto3.makeEnum("livekit.VideoQuality", [
	{
		no: 0,
		name: "LOW"
	},
	{
		no: 1,
		name: "MEDIUM"
	},
	{
		no: 2,
		name: "HIGH"
	},
	{
		no: 3,
		name: "OFF"
	}
]);
var ConnectionQuality$1 = /* @__PURE__ */ proto3.makeEnum("livekit.ConnectionQuality", [
	{
		no: 0,
		name: "POOR"
	},
	{
		no: 1,
		name: "GOOD"
	},
	{
		no: 2,
		name: "EXCELLENT"
	},
	{
		no: 3,
		name: "LOST"
	}
]);
var ClientConfigSetting = /* @__PURE__ */ proto3.makeEnum("livekit.ClientConfigSetting", [
	{
		no: 0,
		name: "UNSET"
	},
	{
		no: 1,
		name: "DISABLED"
	},
	{
		no: 2,
		name: "ENABLED"
	}
]);
var DisconnectReason = /* @__PURE__ */ proto3.makeEnum("livekit.DisconnectReason", [
	{
		no: 0,
		name: "UNKNOWN_REASON"
	},
	{
		no: 1,
		name: "CLIENT_INITIATED"
	},
	{
		no: 2,
		name: "DUPLICATE_IDENTITY"
	},
	{
		no: 3,
		name: "SERVER_SHUTDOWN"
	},
	{
		no: 4,
		name: "PARTICIPANT_REMOVED"
	},
	{
		no: 5,
		name: "ROOM_DELETED"
	},
	{
		no: 6,
		name: "STATE_MISMATCH"
	},
	{
		no: 7,
		name: "JOIN_FAILURE"
	},
	{
		no: 8,
		name: "MIGRATION"
	},
	{
		no: 9,
		name: "SIGNAL_CLOSE"
	},
	{
		no: 10,
		name: "ROOM_CLOSED"
	},
	{
		no: 11,
		name: "USER_UNAVAILABLE"
	},
	{
		no: 12,
		name: "USER_REJECTED"
	},
	{
		no: 13,
		name: "SIP_TRUNK_FAILURE"
	},
	{
		no: 14,
		name: "CONNECTION_TIMEOUT"
	},
	{
		no: 15,
		name: "MEDIA_FAILURE"
	},
	{
		no: 16,
		name: "AGENT_ERROR"
	}
]);
var ReconnectReason = /* @__PURE__ */ proto3.makeEnum("livekit.ReconnectReason", [
	{
		no: 0,
		name: "RR_UNKNOWN"
	},
	{
		no: 1,
		name: "RR_SIGNAL_DISCONNECTED"
	},
	{
		no: 2,
		name: "RR_PUBLISHER_FAILED"
	},
	{
		no: 3,
		name: "RR_SUBSCRIBER_FAILED"
	},
	{
		no: 4,
		name: "RR_SWITCH_CANDIDATE"
	}
]);
var SubscriptionError = /* @__PURE__ */ proto3.makeEnum("livekit.SubscriptionError", [
	{
		no: 0,
		name: "SE_UNKNOWN"
	},
	{
		no: 1,
		name: "SE_CODEC_UNSUPPORTED"
	},
	{
		no: 2,
		name: "SE_TRACK_NOTFOUND"
	}
]);
var AudioTrackFeature = /* @__PURE__ */ proto3.makeEnum("livekit.AudioTrackFeature", [
	{
		no: 0,
		name: "TF_STEREO"
	},
	{
		no: 1,
		name: "TF_NO_DTX"
	},
	{
		no: 2,
		name: "TF_AUTO_GAIN_CONTROL"
	},
	{
		no: 3,
		name: "TF_ECHO_CANCELLATION"
	},
	{
		no: 4,
		name: "TF_NOISE_SUPPRESSION"
	},
	{
		no: 5,
		name: "TF_ENHANCED_NOISE_CANCELLATION"
	},
	{
		no: 6,
		name: "TF_PRECONNECT_BUFFER"
	}
]);
var PacketTrailerFeature = /* @__PURE__ */ proto3.makeEnum("livekit.PacketTrailerFeature", [
	{
		no: 0,
		name: "PTF_USER_TIMESTAMP"
	},
	{
		no: 1,
		name: "PTF_FRAME_ID"
	},
	{
		no: 2,
		name: "PTF_USER_DATA"
	}
]);
var Room$1 = /* @__PURE__ */ proto3.makeMessageType("livekit.Room", () => [
	{
		no: 1,
		name: "sid",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "name",
		kind: "scalar",
		T: 9
	},
	{
		no: 3,
		name: "empty_timeout",
		kind: "scalar",
		T: 13
	},
	{
		no: 14,
		name: "departure_timeout",
		kind: "scalar",
		T: 13
	},
	{
		no: 4,
		name: "max_participants",
		kind: "scalar",
		T: 13
	},
	{
		no: 5,
		name: "creation_time",
		kind: "scalar",
		T: 3
	},
	{
		no: 15,
		name: "creation_time_ms",
		kind: "scalar",
		T: 3
	},
	{
		no: 6,
		name: "turn_password",
		kind: "scalar",
		T: 9
	},
	{
		no: 7,
		name: "enabled_codecs",
		kind: "message",
		T: Codec,
		repeated: true
	},
	{
		no: 8,
		name: "metadata",
		kind: "scalar",
		T: 9
	},
	{
		no: 9,
		name: "num_participants",
		kind: "scalar",
		T: 13
	},
	{
		no: 11,
		name: "num_publishers",
		kind: "scalar",
		T: 13
	},
	{
		no: 10,
		name: "active_recording",
		kind: "scalar",
		T: 8
	},
	{
		no: 13,
		name: "version",
		kind: "message",
		T: TimedVersion
	}
]);
var Codec = /* @__PURE__ */ proto3.makeMessageType("livekit.Codec", () => [{
	no: 1,
	name: "mime",
	kind: "scalar",
	T: 9
}, {
	no: 2,
	name: "fmtp_line",
	kind: "scalar",
	T: 9
}]);
var ParticipantPermission = /* @__PURE__ */ proto3.makeMessageType("livekit.ParticipantPermission", () => [
	{
		no: 1,
		name: "can_subscribe",
		kind: "scalar",
		T: 8
	},
	{
		no: 2,
		name: "can_publish",
		kind: "scalar",
		T: 8
	},
	{
		no: 3,
		name: "can_publish_data",
		kind: "scalar",
		T: 8
	},
	{
		no: 9,
		name: "can_publish_sources",
		kind: "enum",
		T: proto3.getEnumType(TrackSource),
		repeated: true
	},
	{
		no: 7,
		name: "hidden",
		kind: "scalar",
		T: 8
	},
	{
		no: 8,
		name: "recorder",
		kind: "scalar",
		T: 8
	},
	{
		no: 10,
		name: "can_update_metadata",
		kind: "scalar",
		T: 8
	},
	{
		no: 11,
		name: "agent",
		kind: "scalar",
		T: 8
	},
	{
		no: 12,
		name: "can_subscribe_metrics",
		kind: "scalar",
		T: 8
	},
	{
		no: 13,
		name: "can_manage_agent_session",
		kind: "scalar",
		T: 8
	}
]);
var ParticipantInfo = /* @__PURE__ */ proto3.makeMessageType("livekit.ParticipantInfo", () => [
	{
		no: 1,
		name: "sid",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "identity",
		kind: "scalar",
		T: 9
	},
	{
		no: 3,
		name: "state",
		kind: "enum",
		T: proto3.getEnumType(ParticipantInfo_State)
	},
	{
		no: 4,
		name: "tracks",
		kind: "message",
		T: TrackInfo,
		repeated: true
	},
	{
		no: 5,
		name: "metadata",
		kind: "scalar",
		T: 9
	},
	{
		no: 6,
		name: "joined_at",
		kind: "scalar",
		T: 3
	},
	{
		no: 17,
		name: "joined_at_ms",
		kind: "scalar",
		T: 3
	},
	{
		no: 9,
		name: "name",
		kind: "scalar",
		T: 9
	},
	{
		no: 10,
		name: "version",
		kind: "scalar",
		T: 13
	},
	{
		no: 11,
		name: "permission",
		kind: "message",
		T: ParticipantPermission
	},
	{
		no: 12,
		name: "region",
		kind: "scalar",
		T: 9
	},
	{
		no: 13,
		name: "is_publisher",
		kind: "scalar",
		T: 8
	},
	{
		no: 14,
		name: "kind",
		kind: "enum",
		T: proto3.getEnumType(ParticipantInfo_Kind)
	},
	{
		no: 15,
		name: "attributes",
		kind: "map",
		K: 9,
		V: {
			kind: "scalar",
			T: 9
		}
	},
	{
		no: 16,
		name: "disconnect_reason",
		kind: "enum",
		T: proto3.getEnumType(DisconnectReason)
	},
	{
		no: 18,
		name: "kind_details",
		kind: "enum",
		T: proto3.getEnumType(ParticipantInfo_KindDetail),
		repeated: true
	},
	{
		no: 19,
		name: "data_tracks",
		kind: "message",
		T: DataTrackInfo$1,
		repeated: true
	},
	{
		no: 20,
		name: "client_protocol",
		kind: "scalar",
		T: 5
	},
	{
		no: 21,
		name: "capabilities",
		kind: "enum",
		T: proto3.getEnumType(ClientInfo_Capability),
		repeated: true
	}
]);
var ParticipantInfo_State = /* @__PURE__ */ proto3.makeEnum("livekit.ParticipantInfo.State", [
	{
		no: 0,
		name: "JOINING"
	},
	{
		no: 1,
		name: "JOINED"
	},
	{
		no: 2,
		name: "ACTIVE"
	},
	{
		no: 3,
		name: "DISCONNECTED"
	}
]);
var ParticipantInfo_Kind = /* @__PURE__ */ proto3.makeEnum("livekit.ParticipantInfo.Kind", [
	{
		no: 0,
		name: "STANDARD"
	},
	{
		no: 1,
		name: "INGRESS"
	},
	{
		no: 2,
		name: "EGRESS"
	},
	{
		no: 3,
		name: "SIP"
	},
	{
		no: 4,
		name: "AGENT"
	},
	{
		no: 7,
		name: "CONNECTOR"
	},
	{
		no: 8,
		name: "BRIDGE"
	}
]);
var ParticipantInfo_KindDetail = /* @__PURE__ */ proto3.makeEnum("livekit.ParticipantInfo.KindDetail", [
	{
		no: 0,
		name: "CLOUD_AGENT"
	},
	{
		no: 1,
		name: "FORWARDED"
	},
	{
		no: 2,
		name: "CONNECTOR_WHATSAPP"
	},
	{
		no: 3,
		name: "CONNECTOR_TWILIO"
	},
	{
		no: 4,
		name: "BRIDGE_RTSP"
	},
	{
		no: 5,
		name: "SIMULATION"
	}
]);
var Encryption_Type = /* @__PURE__ */ proto3.makeEnum("livekit.Encryption.Type", [
	{
		no: 0,
		name: "NONE"
	},
	{
		no: 1,
		name: "GCM"
	},
	{
		no: 2,
		name: "CUSTOM"
	}
]);
var SimulcastCodecInfo = /* @__PURE__ */ proto3.makeMessageType("livekit.SimulcastCodecInfo", () => [
	{
		no: 1,
		name: "mime_type",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "mid",
		kind: "scalar",
		T: 9
	},
	{
		no: 3,
		name: "cid",
		kind: "scalar",
		T: 9
	},
	{
		no: 4,
		name: "layers",
		kind: "message",
		T: VideoLayer,
		repeated: true
	},
	{
		no: 5,
		name: "video_layer_mode",
		kind: "enum",
		T: proto3.getEnumType(VideoLayer_Mode)
	},
	{
		no: 6,
		name: "sdp_cid",
		kind: "scalar",
		T: 9
	}
]);
var TrackInfo = /* @__PURE__ */ proto3.makeMessageType("livekit.TrackInfo", () => [
	{
		no: 1,
		name: "sid",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "type",
		kind: "enum",
		T: proto3.getEnumType(TrackType)
	},
	{
		no: 3,
		name: "name",
		kind: "scalar",
		T: 9
	},
	{
		no: 4,
		name: "muted",
		kind: "scalar",
		T: 8
	},
	{
		no: 5,
		name: "width",
		kind: "scalar",
		T: 13
	},
	{
		no: 6,
		name: "height",
		kind: "scalar",
		T: 13
	},
	{
		no: 7,
		name: "simulcast",
		kind: "scalar",
		T: 8
	},
	{
		no: 8,
		name: "disable_dtx",
		kind: "scalar",
		T: 8
	},
	{
		no: 9,
		name: "source",
		kind: "enum",
		T: proto3.getEnumType(TrackSource)
	},
	{
		no: 10,
		name: "layers",
		kind: "message",
		T: VideoLayer,
		repeated: true
	},
	{
		no: 11,
		name: "mime_type",
		kind: "scalar",
		T: 9
	},
	{
		no: 12,
		name: "mid",
		kind: "scalar",
		T: 9
	},
	{
		no: 13,
		name: "codecs",
		kind: "message",
		T: SimulcastCodecInfo,
		repeated: true
	},
	{
		no: 14,
		name: "stereo",
		kind: "scalar",
		T: 8
	},
	{
		no: 15,
		name: "disable_red",
		kind: "scalar",
		T: 8
	},
	{
		no: 16,
		name: "encryption",
		kind: "enum",
		T: proto3.getEnumType(Encryption_Type)
	},
	{
		no: 17,
		name: "stream",
		kind: "scalar",
		T: 9
	},
	{
		no: 18,
		name: "version",
		kind: "message",
		T: TimedVersion
	},
	{
		no: 19,
		name: "audio_features",
		kind: "enum",
		T: proto3.getEnumType(AudioTrackFeature),
		repeated: true
	},
	{
		no: 20,
		name: "backup_codec_policy",
		kind: "enum",
		T: proto3.getEnumType(BackupCodecPolicy$1)
	},
	{
		no: 21,
		name: "packet_trailer_features",
		kind: "enum",
		T: proto3.getEnumType(PacketTrailerFeature),
		repeated: true
	}
]);
var DataTrackInfo$1 = /* @__PURE__ */ proto3.makeMessageType("livekit.DataTrackInfo", () => [
	{
		no: 1,
		name: "pub_handle",
		kind: "scalar",
		T: 13
	},
	{
		no: 2,
		name: "sid",
		kind: "scalar",
		T: 9
	},
	{
		no: 3,
		name: "name",
		kind: "scalar",
		T: 9
	},
	{
		no: 4,
		name: "encryption",
		kind: "enum",
		T: proto3.getEnumType(Encryption_Type)
	},
	{
		no: 5,
		name: "frame_encoding",
		kind: "message",
		T: DataTrackFrameEncoding,
		opt: true
	},
	{
		no: 6,
		name: "schema",
		kind: "message",
		T: DataTrackSchemaId,
		opt: true
	}
]);
var DataTrackFrameEncoding = /* @__PURE__ */ proto3.makeMessageType("livekit.DataTrackFrameEncoding", () => [{
	no: 1,
	name: "well_known",
	kind: "enum",
	T: proto3.getEnumType(DataTrackFrameEncoding_WellKnownFrameEncoding),
	oneof: "value"
}, {
	no: 2,
	name: "custom",
	kind: "scalar",
	T: 9,
	oneof: "value"
}]);
var DataTrackFrameEncoding_WellKnownFrameEncoding = /* @__PURE__ */ proto3.makeEnum("livekit.DataTrackFrameEncoding.WellKnownFrameEncoding", [
	{
		no: 0,
		name: "WELL_KNOWN_FRAME_ENCODING_UNSPECIFIED",
		localName: "UNSPECIFIED"
	},
	{
		no: 1,
		name: "WELL_KNOWN_FRAME_ENCODING_ROS1",
		localName: "ROS1"
	},
	{
		no: 2,
		name: "WELL_KNOWN_FRAME_ENCODING_CDR",
		localName: "CDR"
	},
	{
		no: 3,
		name: "WELL_KNOWN_FRAME_ENCODING_PROTOBUF",
		localName: "PROTOBUF"
	},
	{
		no: 4,
		name: "WELL_KNOWN_FRAME_ENCODING_FLATBUFFER",
		localName: "FLATBUFFER"
	},
	{
		no: 5,
		name: "WELL_KNOWN_FRAME_ENCODING_CBOR",
		localName: "CBOR"
	},
	{
		no: 6,
		name: "WELL_KNOWN_FRAME_ENCODING_MSGPACK",
		localName: "MSGPACK"
	},
	{
		no: 7,
		name: "WELL_KNOWN_FRAME_ENCODING_JSON",
		localName: "JSON"
	}
]);
var DataTrackSchemaEncoding = /* @__PURE__ */ proto3.makeMessageType("livekit.DataTrackSchemaEncoding", () => [{
	no: 1,
	name: "well_known",
	kind: "enum",
	T: proto3.getEnumType(DataTrackSchemaEncoding_WellKnownSchemaEncoding),
	oneof: "value"
}, {
	no: 2,
	name: "custom",
	kind: "scalar",
	T: 9,
	oneof: "value"
}]);
var DataTrackSchemaEncoding_WellKnownSchemaEncoding = /* @__PURE__ */ proto3.makeEnum("livekit.DataTrackSchemaEncoding.WellKnownSchemaEncoding", [
	{
		no: 0,
		name: "WELL_KNOWN_SCHEMA_ENCODING_UNSPECIFIED",
		localName: "UNSPECIFIED"
	},
	{
		no: 1,
		name: "WELL_KNOWN_SCHEMA_ENCODING_PROTOBUF",
		localName: "PROTOBUF"
	},
	{
		no: 2,
		name: "WELL_KNOWN_SCHEMA_ENCODING_FLATBUFFER",
		localName: "FLATBUFFER"
	},
	{
		no: 3,
		name: "WELL_KNOWN_SCHEMA_ENCODING_ROS1_MSG",
		localName: "ROS1_MSG"
	},
	{
		no: 4,
		name: "WELL_KNOWN_SCHEMA_ENCODING_ROS2_MSG",
		localName: "ROS2_MSG"
	},
	{
		no: 5,
		name: "WELL_KNOWN_SCHEMA_ENCODING_ROS2_IDL",
		localName: "ROS2_IDL"
	},
	{
		no: 6,
		name: "WELL_KNOWN_SCHEMA_ENCODING_OMG_IDL",
		localName: "OMG_IDL"
	},
	{
		no: 7,
		name: "WELL_KNOWN_SCHEMA_ENCODING_JSON_SCHEMA",
		localName: "JSON_SCHEMA"
	}
]);
var DataTrackSchemaId = /* @__PURE__ */ proto3.makeMessageType("livekit.DataTrackSchemaId", () => [{
	no: 1,
	name: "name",
	kind: "scalar",
	T: 9
}, {
	no: 2,
	name: "encoding",
	kind: "message",
	T: DataTrackSchemaEncoding
}]);
var DataTrackSubscriptionOptions = /* @__PURE__ */ proto3.makeMessageType("livekit.DataTrackSubscriptionOptions", () => [{
	no: 1,
	name: "target_fps",
	kind: "scalar",
	T: 13,
	opt: true
}]);
var DataBlobKey = /* @__PURE__ */ proto3.makeMessageType("livekit.DataBlobKey", () => [{
	no: 1,
	name: "generic",
	kind: "scalar",
	T: 9,
	oneof: "key"
}, {
	no: 2,
	name: "schema_id",
	kind: "message",
	T: DataTrackSchemaId,
	oneof: "key"
}]);
var DataBlob = /* @__PURE__ */ proto3.makeMessageType("livekit.DataBlob", () => [{
	no: 1,
	name: "key",
	kind: "message",
	T: DataBlobKey
}, {
	no: 2,
	name: "contents",
	kind: "scalar",
	T: 12
}]);
var VideoLayer = /* @__PURE__ */ proto3.makeMessageType("livekit.VideoLayer", () => [
	{
		no: 1,
		name: "quality",
		kind: "enum",
		T: proto3.getEnumType(VideoQuality$1)
	},
	{
		no: 2,
		name: "width",
		kind: "scalar",
		T: 13
	},
	{
		no: 3,
		name: "height",
		kind: "scalar",
		T: 13
	},
	{
		no: 4,
		name: "bitrate",
		kind: "scalar",
		T: 13
	},
	{
		no: 5,
		name: "ssrc",
		kind: "scalar",
		T: 13
	},
	{
		no: 6,
		name: "spatial_layer",
		kind: "scalar",
		T: 5
	},
	{
		no: 7,
		name: "rid",
		kind: "scalar",
		T: 9
	},
	{
		no: 8,
		name: "repair_ssrc",
		kind: "scalar",
		T: 13
	}
]);
var VideoLayer_Mode = /* @__PURE__ */ proto3.makeEnum("livekit.VideoLayer.Mode", [
	{
		no: 0,
		name: "MODE_UNUSED"
	},
	{
		no: 1,
		name: "ONE_SPATIAL_LAYER_PER_STREAM"
	},
	{
		no: 2,
		name: "MULTIPLE_SPATIAL_LAYERS_PER_STREAM"
	},
	{
		no: 3,
		name: "ONE_SPATIAL_LAYER_PER_STREAM_INCOMPLETE_RTCP_SR"
	}
]);
var DataPacket = /* @__PURE__ */ proto3.makeMessageType("livekit.DataPacket", () => [
	{
		no: 1,
		name: "kind",
		kind: "enum",
		T: proto3.getEnumType(DataPacket_Kind)
	},
	{
		no: 4,
		name: "participant_identity",
		kind: "scalar",
		T: 9
	},
	{
		no: 5,
		name: "destination_identities",
		kind: "scalar",
		T: 9,
		repeated: true
	},
	{
		no: 2,
		name: "user",
		kind: "message",
		T: UserPacket,
		oneof: "value"
	},
	{
		no: 3,
		name: "speaker",
		kind: "message",
		T: ActiveSpeakerUpdate,
		oneof: "value"
	},
	{
		no: 6,
		name: "sip_dtmf",
		kind: "message",
		T: SipDTMF,
		oneof: "value"
	},
	{
		no: 7,
		name: "transcription",
		kind: "message",
		T: Transcription,
		oneof: "value"
	},
	{
		no: 8,
		name: "metrics",
		kind: "message",
		T: MetricsBatch,
		oneof: "value"
	},
	{
		no: 9,
		name: "chat_message",
		kind: "message",
		T: ChatMessage,
		oneof: "value"
	},
	{
		no: 10,
		name: "rpc_request",
		kind: "message",
		T: RpcRequest,
		oneof: "value"
	},
	{
		no: 11,
		name: "rpc_ack",
		kind: "message",
		T: RpcAck,
		oneof: "value"
	},
	{
		no: 12,
		name: "rpc_response",
		kind: "message",
		T: RpcResponse,
		oneof: "value"
	},
	{
		no: 13,
		name: "stream_header",
		kind: "message",
		T: DataStream_Header,
		oneof: "value"
	},
	{
		no: 14,
		name: "stream_chunk",
		kind: "message",
		T: DataStream_Chunk,
		oneof: "value"
	},
	{
		no: 15,
		name: "stream_trailer",
		kind: "message",
		T: DataStream_Trailer,
		oneof: "value"
	},
	{
		no: 18,
		name: "encrypted_packet",
		kind: "message",
		T: EncryptedPacket,
		oneof: "value"
	},
	{
		no: 16,
		name: "sequence",
		kind: "scalar",
		T: 13
	},
	{
		no: 17,
		name: "participant_sid",
		kind: "scalar",
		T: 9
	}
]);
var DataPacket_Kind = /* @__PURE__ */ proto3.makeEnum("livekit.DataPacket.Kind", [{
	no: 0,
	name: "RELIABLE"
}, {
	no: 1,
	name: "LOSSY"
}]);
var EncryptedPacket = /* @__PURE__ */ proto3.makeMessageType("livekit.EncryptedPacket", () => [
	{
		no: 1,
		name: "encryption_type",
		kind: "enum",
		T: proto3.getEnumType(Encryption_Type)
	},
	{
		no: 2,
		name: "iv",
		kind: "scalar",
		T: 12
	},
	{
		no: 3,
		name: "key_index",
		kind: "scalar",
		T: 13
	},
	{
		no: 4,
		name: "encrypted_value",
		kind: "scalar",
		T: 12
	}
]);
var EncryptedPacketPayload = /* @__PURE__ */ proto3.makeMessageType("livekit.EncryptedPacketPayload", () => [
	{
		no: 1,
		name: "user",
		kind: "message",
		T: UserPacket,
		oneof: "value"
	},
	{
		no: 3,
		name: "chat_message",
		kind: "message",
		T: ChatMessage,
		oneof: "value"
	},
	{
		no: 4,
		name: "rpc_request",
		kind: "message",
		T: RpcRequest,
		oneof: "value"
	},
	{
		no: 5,
		name: "rpc_ack",
		kind: "message",
		T: RpcAck,
		oneof: "value"
	},
	{
		no: 6,
		name: "rpc_response",
		kind: "message",
		T: RpcResponse,
		oneof: "value"
	},
	{
		no: 7,
		name: "stream_header",
		kind: "message",
		T: DataStream_Header,
		oneof: "value"
	},
	{
		no: 8,
		name: "stream_chunk",
		kind: "message",
		T: DataStream_Chunk,
		oneof: "value"
	},
	{
		no: 9,
		name: "stream_trailer",
		kind: "message",
		T: DataStream_Trailer,
		oneof: "value"
	}
]);
var ActiveSpeakerUpdate = /* @__PURE__ */ proto3.makeMessageType("livekit.ActiveSpeakerUpdate", () => [{
	no: 1,
	name: "speakers",
	kind: "message",
	T: SpeakerInfo,
	repeated: true
}]);
var SpeakerInfo = /* @__PURE__ */ proto3.makeMessageType("livekit.SpeakerInfo", () => [
	{
		no: 1,
		name: "sid",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "level",
		kind: "scalar",
		T: 2
	},
	{
		no: 3,
		name: "active",
		kind: "scalar",
		T: 8
	}
]);
var UserPacket = /* @__PURE__ */ proto3.makeMessageType("livekit.UserPacket", () => [
	{
		no: 1,
		name: "participant_sid",
		kind: "scalar",
		T: 9
	},
	{
		no: 5,
		name: "participant_identity",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "payload",
		kind: "scalar",
		T: 12
	},
	{
		no: 3,
		name: "destination_sids",
		kind: "scalar",
		T: 9,
		repeated: true
	},
	{
		no: 6,
		name: "destination_identities",
		kind: "scalar",
		T: 9,
		repeated: true
	},
	{
		no: 4,
		name: "topic",
		kind: "scalar",
		T: 9,
		opt: true
	},
	{
		no: 8,
		name: "id",
		kind: "scalar",
		T: 9,
		opt: true
	},
	{
		no: 9,
		name: "start_time",
		kind: "scalar",
		T: 4,
		opt: true
	},
	{
		no: 10,
		name: "end_time",
		kind: "scalar",
		T: 4,
		opt: true
	},
	{
		no: 11,
		name: "nonce",
		kind: "scalar",
		T: 12
	}
]);
var SipDTMF = /* @__PURE__ */ proto3.makeMessageType("livekit.SipDTMF", () => [{
	no: 3,
	name: "code",
	kind: "scalar",
	T: 13
}, {
	no: 4,
	name: "digit",
	kind: "scalar",
	T: 9
}]);
var Transcription = /* @__PURE__ */ proto3.makeMessageType("livekit.Transcription", () => [
	{
		no: 2,
		name: "transcribed_participant_identity",
		kind: "scalar",
		T: 9
	},
	{
		no: 3,
		name: "track_id",
		kind: "scalar",
		T: 9
	},
	{
		no: 4,
		name: "segments",
		kind: "message",
		T: TranscriptionSegment,
		repeated: true
	}
]);
var TranscriptionSegment = /* @__PURE__ */ proto3.makeMessageType("livekit.TranscriptionSegment", () => [
	{
		no: 1,
		name: "id",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "text",
		kind: "scalar",
		T: 9
	},
	{
		no: 3,
		name: "start_time",
		kind: "scalar",
		T: 4
	},
	{
		no: 4,
		name: "end_time",
		kind: "scalar",
		T: 4
	},
	{
		no: 5,
		name: "final",
		kind: "scalar",
		T: 8
	},
	{
		no: 6,
		name: "language",
		kind: "scalar",
		T: 9
	}
]);
var ChatMessage = /* @__PURE__ */ proto3.makeMessageType("livekit.ChatMessage", () => [
	{
		no: 1,
		name: "id",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "timestamp",
		kind: "scalar",
		T: 3
	},
	{
		no: 3,
		name: "edit_timestamp",
		kind: "scalar",
		T: 3,
		opt: true
	},
	{
		no: 4,
		name: "message",
		kind: "scalar",
		T: 9
	},
	{
		no: 5,
		name: "deleted",
		kind: "scalar",
		T: 8
	},
	{
		no: 6,
		name: "generated",
		kind: "scalar",
		T: 8
	}
]);
var RpcRequest = /* @__PURE__ */ proto3.makeMessageType("livekit.RpcRequest", () => [
	{
		no: 1,
		name: "id",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "method",
		kind: "scalar",
		T: 9
	},
	{
		no: 3,
		name: "payload",
		kind: "scalar",
		T: 9
	},
	{
		no: 4,
		name: "response_timeout_ms",
		kind: "scalar",
		T: 13
	},
	{
		no: 5,
		name: "version",
		kind: "scalar",
		T: 13
	},
	{
		no: 6,
		name: "compressed_payload",
		kind: "scalar",
		T: 12
	}
]);
var RpcAck = /* @__PURE__ */ proto3.makeMessageType("livekit.RpcAck", () => [{
	no: 1,
	name: "request_id",
	kind: "scalar",
	T: 9
}]);
var RpcResponse = /* @__PURE__ */ proto3.makeMessageType("livekit.RpcResponse", () => [
	{
		no: 1,
		name: "request_id",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "payload",
		kind: "scalar",
		T: 9,
		oneof: "value"
	},
	{
		no: 3,
		name: "error",
		kind: "message",
		T: RpcError$1,
		oneof: "value"
	},
	{
		no: 4,
		name: "compressed_payload",
		kind: "scalar",
		T: 12,
		oneof: "value"
	}
]);
var RpcError$1 = /* @__PURE__ */ proto3.makeMessageType("livekit.RpcError", () => [
	{
		no: 1,
		name: "code",
		kind: "scalar",
		T: 13
	},
	{
		no: 2,
		name: "message",
		kind: "scalar",
		T: 9
	},
	{
		no: 3,
		name: "data",
		kind: "scalar",
		T: 9
	}
]);
var ParticipantTracks = /* @__PURE__ */ proto3.makeMessageType("livekit.ParticipantTracks", () => [{
	no: 1,
	name: "participant_sid",
	kind: "scalar",
	T: 9
}, {
	no: 2,
	name: "track_sids",
	kind: "scalar",
	T: 9,
	repeated: true
}]);
var ServerInfo = /* @__PURE__ */ proto3.makeMessageType("livekit.ServerInfo", () => [
	{
		no: 1,
		name: "edition",
		kind: "enum",
		T: proto3.getEnumType(ServerInfo_Edition)
	},
	{
		no: 2,
		name: "version",
		kind: "scalar",
		T: 9
	},
	{
		no: 3,
		name: "protocol",
		kind: "scalar",
		T: 5
	},
	{
		no: 4,
		name: "region",
		kind: "scalar",
		T: 9
	},
	{
		no: 5,
		name: "node_id",
		kind: "scalar",
		T: 9
	},
	{
		no: 6,
		name: "debug_info",
		kind: "scalar",
		T: 9
	},
	{
		no: 7,
		name: "agent_protocol",
		kind: "scalar",
		T: 5
	}
]);
var ServerInfo_Edition = /* @__PURE__ */ proto3.makeEnum("livekit.ServerInfo.Edition", [{
	no: 0,
	name: "Standard"
}, {
	no: 1,
	name: "Cloud"
}]);
var ClientInfo = /* @__PURE__ */ proto3.makeMessageType("livekit.ClientInfo", () => [
	{
		no: 1,
		name: "sdk",
		kind: "enum",
		T: proto3.getEnumType(ClientInfo_SDK)
	},
	{
		no: 2,
		name: "version",
		kind: "scalar",
		T: 9
	},
	{
		no: 3,
		name: "protocol",
		kind: "scalar",
		T: 5
	},
	{
		no: 4,
		name: "os",
		kind: "scalar",
		T: 9
	},
	{
		no: 5,
		name: "os_version",
		kind: "scalar",
		T: 9
	},
	{
		no: 6,
		name: "device_model",
		kind: "scalar",
		T: 9
	},
	{
		no: 7,
		name: "browser",
		kind: "scalar",
		T: 9
	},
	{
		no: 8,
		name: "browser_version",
		kind: "scalar",
		T: 9
	},
	{
		no: 9,
		name: "address",
		kind: "scalar",
		T: 9
	},
	{
		no: 10,
		name: "network",
		kind: "scalar",
		T: 9
	},
	{
		no: 11,
		name: "other_sdks",
		kind: "scalar",
		T: 9
	},
	{
		no: 12,
		name: "client_protocol",
		kind: "scalar",
		T: 5
	},
	{
		no: 13,
		name: "capabilities",
		kind: "enum",
		T: proto3.getEnumType(ClientInfo_Capability),
		repeated: true
	}
]);
var ClientInfo_SDK = /* @__PURE__ */ proto3.makeEnum("livekit.ClientInfo.SDK", [
	{
		no: 0,
		name: "UNKNOWN"
	},
	{
		no: 1,
		name: "JS"
	},
	{
		no: 2,
		name: "SWIFT"
	},
	{
		no: 3,
		name: "ANDROID"
	},
	{
		no: 4,
		name: "FLUTTER"
	},
	{
		no: 5,
		name: "GO"
	},
	{
		no: 6,
		name: "UNITY"
	},
	{
		no: 7,
		name: "REACT_NATIVE"
	},
	{
		no: 8,
		name: "RUST"
	},
	{
		no: 9,
		name: "PYTHON"
	},
	{
		no: 10,
		name: "CPP"
	},
	{
		no: 11,
		name: "UNITY_WEB"
	},
	{
		no: 12,
		name: "NODE"
	},
	{
		no: 13,
		name: "UNREAL"
	},
	{
		no: 14,
		name: "ESP32"
	}
]);
var ClientInfo_Capability = /* @__PURE__ */ proto3.makeEnum("livekit.ClientInfo.Capability", [
	{
		no: 0,
		name: "CAP_UNUSED"
	},
	{
		no: 1,
		name: "CAP_PACKET_TRAILER"
	},
	{
		no: 2,
		name: "CAP_COMPRESSION_DEFLATE_RAW"
	}
]);
var ClientConfiguration = /* @__PURE__ */ proto3.makeMessageType("livekit.ClientConfiguration", () => [
	{
		no: 1,
		name: "video",
		kind: "message",
		T: VideoConfiguration
	},
	{
		no: 2,
		name: "screen",
		kind: "message",
		T: VideoConfiguration
	},
	{
		no: 3,
		name: "resume_connection",
		kind: "enum",
		T: proto3.getEnumType(ClientConfigSetting)
	},
	{
		no: 4,
		name: "disabled_codecs",
		kind: "message",
		T: DisabledCodecs
	},
	{
		no: 5,
		name: "force_relay",
		kind: "enum",
		T: proto3.getEnumType(ClientConfigSetting)
	}
]);
var VideoConfiguration = /* @__PURE__ */ proto3.makeMessageType("livekit.VideoConfiguration", () => [{
	no: 1,
	name: "hardware_encoder",
	kind: "enum",
	T: proto3.getEnumType(ClientConfigSetting)
}]);
var DisabledCodecs = /* @__PURE__ */ proto3.makeMessageType("livekit.DisabledCodecs", () => [{
	no: 1,
	name: "codecs",
	kind: "message",
	T: Codec,
	repeated: true
}, {
	no: 2,
	name: "publish",
	kind: "message",
	T: Codec,
	repeated: true
}]);
var TimedVersion = /* @__PURE__ */ proto3.makeMessageType("livekit.TimedVersion", () => [{
	no: 1,
	name: "unix_micro",
	kind: "scalar",
	T: 3
}, {
	no: 2,
	name: "ticks",
	kind: "scalar",
	T: 5
}]);
var DataStream_OperationType = /* @__PURE__ */ proto3.makeEnum("livekit.DataStream.OperationType", [
	{
		no: 0,
		name: "CREATE"
	},
	{
		no: 1,
		name: "UPDATE"
	},
	{
		no: 2,
		name: "DELETE"
	},
	{
		no: 3,
		name: "REACTION"
	}
]);
var DataStream_CompressionType = /* @__PURE__ */ proto3.makeEnum("livekit.DataStream.CompressionType", [{
	no: 0,
	name: "NONE"
}, {
	no: 1,
	name: "DEFLATE_RAW"
}]);
var DataStream_TextHeader = /* @__PURE__ */ proto3.makeMessageType("livekit.DataStream.TextHeader", () => [
	{
		no: 1,
		name: "operation_type",
		kind: "enum",
		T: proto3.getEnumType(DataStream_OperationType)
	},
	{
		no: 2,
		name: "version",
		kind: "scalar",
		T: 5
	},
	{
		no: 3,
		name: "reply_to_stream_id",
		kind: "scalar",
		T: 9
	},
	{
		no: 4,
		name: "attached_stream_ids",
		kind: "scalar",
		T: 9,
		repeated: true
	},
	{
		no: 5,
		name: "generated",
		kind: "scalar",
		T: 8
	}
], { localName: "DataStream_TextHeader" });
var DataStream_ByteHeader = /* @__PURE__ */ proto3.makeMessageType("livekit.DataStream.ByteHeader", () => [{
	no: 1,
	name: "name",
	kind: "scalar",
	T: 9
}], { localName: "DataStream_ByteHeader" });
var DataStream_Header = /* @__PURE__ */ proto3.makeMessageType("livekit.DataStream.Header", () => [
	{
		no: 1,
		name: "stream_id",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "timestamp",
		kind: "scalar",
		T: 3
	},
	{
		no: 3,
		name: "topic",
		kind: "scalar",
		T: 9
	},
	{
		no: 4,
		name: "mime_type",
		kind: "scalar",
		T: 9
	},
	{
		no: 5,
		name: "total_length",
		kind: "scalar",
		T: 4,
		opt: true
	},
	{
		no: 7,
		name: "encryption_type",
		kind: "enum",
		T: proto3.getEnumType(Encryption_Type)
	},
	{
		no: 8,
		name: "attributes",
		kind: "map",
		K: 9,
		V: {
			kind: "scalar",
			T: 9
		}
	},
	{
		no: 9,
		name: "text_header",
		kind: "message",
		T: DataStream_TextHeader,
		oneof: "content_header"
	},
	{
		no: 10,
		name: "byte_header",
		kind: "message",
		T: DataStream_ByteHeader,
		oneof: "content_header"
	},
	{
		no: 11,
		name: "inline_content",
		kind: "scalar",
		T: 12,
		opt: true
	},
	{
		no: 12,
		name: "compression",
		kind: "enum",
		T: proto3.getEnumType(DataStream_CompressionType)
	}
], { localName: "DataStream_Header" });
var DataStream_Chunk = /* @__PURE__ */ proto3.makeMessageType("livekit.DataStream.Chunk", () => [
	{
		no: 1,
		name: "stream_id",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "chunk_index",
		kind: "scalar",
		T: 4
	},
	{
		no: 3,
		name: "content",
		kind: "scalar",
		T: 12
	},
	{
		no: 4,
		name: "version",
		kind: "scalar",
		T: 5
	},
	{
		no: 5,
		name: "iv",
		kind: "scalar",
		T: 12,
		opt: true
	}
], { localName: "DataStream_Chunk" });
var DataStream_Trailer = /* @__PURE__ */ proto3.makeMessageType("livekit.DataStream.Trailer", () => [
	{
		no: 1,
		name: "stream_id",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "reason",
		kind: "scalar",
		T: 9
	},
	{
		no: 3,
		name: "attributes",
		kind: "map",
		K: 9,
		V: {
			kind: "scalar",
			T: 9
		}
	}
], { localName: "DataStream_Trailer" });
var FilterParams = /* @__PURE__ */ proto3.makeMessageType("livekit.FilterParams", () => [{
	no: 1,
	name: "include_events",
	kind: "scalar",
	T: 9,
	repeated: true
}, {
	no: 2,
	name: "exclude_events",
	kind: "scalar",
	T: 9,
	repeated: true
}]);
var WebhookConfig = /* @__PURE__ */ proto3.makeMessageType("livekit.WebhookConfig", () => [
	{
		no: 1,
		name: "url",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "signing_key",
		kind: "scalar",
		T: 9
	},
	{
		no: 3,
		name: "filter_params",
		kind: "message",
		T: FilterParams
	}
]);
var SubscribedAudioCodec = /* @__PURE__ */ proto3.makeMessageType("livekit.SubscribedAudioCodec", () => [{
	no: 1,
	name: "codec",
	kind: "scalar",
	T: 9
}, {
	no: 2,
	name: "enabled",
	kind: "scalar",
	T: 8
}]);
var JobRestartPolicy = /* @__PURE__ */ proto3.makeEnum("livekit.JobRestartPolicy", [{
	no: 0,
	name: "JRP_ON_FAILURE"
}, {
	no: 1,
	name: "JRP_NEVER"
}]);
var RoomAgentDispatch = /* @__PURE__ */ proto3.makeMessageType("livekit.RoomAgentDispatch", () => [
	{
		no: 1,
		name: "agent_name",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "metadata",
		kind: "scalar",
		T: 9
	},
	{
		no: 3,
		name: "restart_policy",
		kind: "enum",
		T: proto3.getEnumType(JobRestartPolicy)
	},
	{
		no: 4,
		name: "deployment",
		kind: "scalar",
		T: 9
	},
	{
		no: 5,
		name: "attributes",
		kind: "map",
		K: 9,
		V: {
			kind: "scalar",
			T: 9
		}
	}
]);
var EncodingOptionsPreset = /* @__PURE__ */ proto3.makeEnum("livekit.EncodingOptionsPreset", [
	{
		no: 0,
		name: "H264_720P_30"
	},
	{
		no: 1,
		name: "H264_720P_60"
	},
	{
		no: 2,
		name: "H264_1080P_30"
	},
	{
		no: 3,
		name: "H264_1080P_60"
	},
	{
		no: 4,
		name: "PORTRAIT_H264_720P_30"
	},
	{
		no: 5,
		name: "PORTRAIT_H264_720P_60"
	},
	{
		no: 6,
		name: "PORTRAIT_H264_1080P_30"
	},
	{
		no: 7,
		name: "PORTRAIT_H264_1080P_60"
	}
]);
var EncodedFileType = /* @__PURE__ */ proto3.makeEnum("livekit.EncodedFileType", [
	{
		no: 0,
		name: "DEFAULT_FILETYPE"
	},
	{
		no: 1,
		name: "MP4"
	},
	{
		no: 2,
		name: "OGG"
	},
	{
		no: 3,
		name: "MP3"
	}
]);
var StreamProtocol = /* @__PURE__ */ proto3.makeEnum("livekit.StreamProtocol", [
	{
		no: 0,
		name: "DEFAULT_PROTOCOL"
	},
	{
		no: 1,
		name: "RTMP"
	},
	{
		no: 2,
		name: "SRT"
	},
	{
		no: 3,
		name: "WEBSOCKET"
	}
]);
var SegmentedFileProtocol = /* @__PURE__ */ proto3.makeEnum("livekit.SegmentedFileProtocol", [{
	no: 0,
	name: "DEFAULT_SEGMENTED_FILE_PROTOCOL"
}, {
	no: 1,
	name: "HLS_PROTOCOL"
}]);
var SegmentedFileSuffix = /* @__PURE__ */ proto3.makeEnum("livekit.SegmentedFileSuffix", [{
	no: 0,
	name: "INDEX"
}, {
	no: 1,
	name: "TIMESTAMP"
}]);
var ImageFileSuffix = /* @__PURE__ */ proto3.makeEnum("livekit.ImageFileSuffix", [
	{
		no: 0,
		name: "IMAGE_SUFFIX_INDEX"
	},
	{
		no: 1,
		name: "IMAGE_SUFFIX_TIMESTAMP"
	},
	{
		no: 2,
		name: "IMAGE_SUFFIX_NONE_OVERWRITE"
	}
]);
var AudioMixing = /* @__PURE__ */ proto3.makeEnum("livekit.AudioMixing", [
	{
		no: 0,
		name: "DEFAULT_MIXING"
	},
	{
		no: 1,
		name: "DUAL_CHANNEL_AGENT"
	},
	{
		no: 2,
		name: "DUAL_CHANNEL_ALTERNATE"
	}
]);
var EncodingOptions = /* @__PURE__ */ proto3.makeMessageType("livekit.EncodingOptions", () => [
	{
		no: 1,
		name: "width",
		kind: "scalar",
		T: 5
	},
	{
		no: 2,
		name: "height",
		kind: "scalar",
		T: 5
	},
	{
		no: 3,
		name: "depth",
		kind: "scalar",
		T: 5
	},
	{
		no: 4,
		name: "framerate",
		kind: "scalar",
		T: 5
	},
	{
		no: 5,
		name: "audio_codec",
		kind: "enum",
		T: proto3.getEnumType(AudioCodec)
	},
	{
		no: 6,
		name: "audio_bitrate",
		kind: "scalar",
		T: 5
	},
	{
		no: 7,
		name: "audio_frequency",
		kind: "scalar",
		T: 5
	},
	{
		no: 8,
		name: "video_codec",
		kind: "enum",
		T: proto3.getEnumType(VideoCodec)
	},
	{
		no: 9,
		name: "video_bitrate",
		kind: "scalar",
		T: 5
	},
	{
		no: 10,
		name: "key_frame_interval",
		kind: "scalar",
		T: 1
	},
	{
		no: 11,
		name: "audio_quality",
		kind: "scalar",
		T: 5
	},
	{
		no: 12,
		name: "video_quality",
		kind: "scalar",
		T: 5
	}
]);
var StreamOutput = /* @__PURE__ */ proto3.makeMessageType("livekit.StreamOutput", () => [{
	no: 1,
	name: "protocol",
	kind: "enum",
	T: proto3.getEnumType(StreamProtocol)
}, {
	no: 2,
	name: "urls",
	kind: "scalar",
	T: 9,
	repeated: true
}]);
var SegmentedFileOutput = /* @__PURE__ */ proto3.makeMessageType("livekit.SegmentedFileOutput", () => [
	{
		no: 1,
		name: "protocol",
		kind: "enum",
		T: proto3.getEnumType(SegmentedFileProtocol)
	},
	{
		no: 2,
		name: "filename_prefix",
		kind: "scalar",
		T: 9
	},
	{
		no: 3,
		name: "playlist_name",
		kind: "scalar",
		T: 9
	},
	{
		no: 11,
		name: "live_playlist_name",
		kind: "scalar",
		T: 9
	},
	{
		no: 4,
		name: "segment_duration",
		kind: "scalar",
		T: 13
	},
	{
		no: 10,
		name: "filename_suffix",
		kind: "enum",
		T: proto3.getEnumType(SegmentedFileSuffix)
	},
	{
		no: 8,
		name: "disable_manifest",
		kind: "scalar",
		T: 8
	},
	{
		no: 5,
		name: "s3",
		kind: "message",
		T: S3Upload,
		oneof: "output"
	},
	{
		no: 6,
		name: "gcp",
		kind: "message",
		T: GCPUpload,
		oneof: "output"
	},
	{
		no: 7,
		name: "azure",
		kind: "message",
		T: AzureBlobUpload,
		oneof: "output"
	},
	{
		no: 9,
		name: "aliOSS",
		kind: "message",
		T: AliOSSUpload,
		oneof: "output"
	}
]);
var ImageOutput = /* @__PURE__ */ proto3.makeMessageType("livekit.ImageOutput", () => [
	{
		no: 1,
		name: "capture_interval",
		kind: "scalar",
		T: 13
	},
	{
		no: 2,
		name: "width",
		kind: "scalar",
		T: 5
	},
	{
		no: 3,
		name: "height",
		kind: "scalar",
		T: 5
	},
	{
		no: 4,
		name: "filename_prefix",
		kind: "scalar",
		T: 9
	},
	{
		no: 5,
		name: "filename_suffix",
		kind: "enum",
		T: proto3.getEnumType(ImageFileSuffix)
	},
	{
		no: 6,
		name: "image_codec",
		kind: "enum",
		T: proto3.getEnumType(ImageCodec)
	},
	{
		no: 7,
		name: "disable_manifest",
		kind: "scalar",
		T: 8
	},
	{
		no: 8,
		name: "s3",
		kind: "message",
		T: S3Upload,
		oneof: "output"
	},
	{
		no: 9,
		name: "gcp",
		kind: "message",
		T: GCPUpload,
		oneof: "output"
	},
	{
		no: 10,
		name: "azure",
		kind: "message",
		T: AzureBlobUpload,
		oneof: "output"
	},
	{
		no: 11,
		name: "aliOSS",
		kind: "message",
		T: AliOSSUpload,
		oneof: "output"
	}
]);
var S3Upload = /* @__PURE__ */ proto3.makeMessageType("livekit.S3Upload", () => [
	{
		no: 1,
		name: "access_key",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "secret",
		kind: "scalar",
		T: 9
	},
	{
		no: 11,
		name: "session_token",
		kind: "scalar",
		T: 9
	},
	{
		no: 12,
		name: "assume_role_arn",
		kind: "scalar",
		T: 9
	},
	{
		no: 13,
		name: "assume_role_external_id",
		kind: "scalar",
		T: 9
	},
	{
		no: 3,
		name: "region",
		kind: "scalar",
		T: 9
	},
	{
		no: 4,
		name: "endpoint",
		kind: "scalar",
		T: 9
	},
	{
		no: 5,
		name: "bucket",
		kind: "scalar",
		T: 9
	},
	{
		no: 6,
		name: "force_path_style",
		kind: "scalar",
		T: 8
	},
	{
		no: 7,
		name: "metadata",
		kind: "map",
		K: 9,
		V: {
			kind: "scalar",
			T: 9
		}
	},
	{
		no: 8,
		name: "tagging",
		kind: "scalar",
		T: 9
	},
	{
		no: 9,
		name: "content_disposition",
		kind: "scalar",
		T: 9
	},
	{
		no: 10,
		name: "proxy",
		kind: "message",
		T: ProxyConfig
	}
]);
var GCPUpload = /* @__PURE__ */ proto3.makeMessageType("livekit.GCPUpload", () => [
	{
		no: 1,
		name: "credentials",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "bucket",
		kind: "scalar",
		T: 9
	},
	{
		no: 3,
		name: "proxy",
		kind: "message",
		T: ProxyConfig
	}
]);
var AzureBlobUpload = /* @__PURE__ */ proto3.makeMessageType("livekit.AzureBlobUpload", () => [
	{
		no: 1,
		name: "account_name",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "account_key",
		kind: "scalar",
		T: 9
	},
	{
		no: 3,
		name: "container_name",
		kind: "scalar",
		T: 9
	}
]);
var AliOSSUpload = /* @__PURE__ */ proto3.makeMessageType("livekit.AliOSSUpload", () => [
	{
		no: 1,
		name: "access_key",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "secret",
		kind: "scalar",
		T: 9
	},
	{
		no: 3,
		name: "region",
		kind: "scalar",
		T: 9
	},
	{
		no: 4,
		name: "endpoint",
		kind: "scalar",
		T: 9
	},
	{
		no: 5,
		name: "bucket",
		kind: "scalar",
		T: 9
	}
]);
var ProxyConfig = /* @__PURE__ */ proto3.makeMessageType("livekit.ProxyConfig", () => [
	{
		no: 1,
		name: "url",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "username",
		kind: "scalar",
		T: 9
	},
	{
		no: 3,
		name: "password",
		kind: "scalar",
		T: 9
	}
]);
var AutoParticipantEgress = /* @__PURE__ */ proto3.makeMessageType("livekit.AutoParticipantEgress", () => [
	{
		no: 1,
		name: "preset",
		kind: "enum",
		T: proto3.getEnumType(EncodingOptionsPreset),
		oneof: "options"
	},
	{
		no: 2,
		name: "advanced",
		kind: "message",
		T: EncodingOptions,
		oneof: "options"
	},
	{
		no: 3,
		name: "file_outputs",
		kind: "message",
		T: EncodedFileOutput,
		repeated: true
	},
	{
		no: 4,
		name: "segment_outputs",
		kind: "message",
		T: SegmentedFileOutput,
		repeated: true
	}
]);
var AutoTrackEgress = /* @__PURE__ */ proto3.makeMessageType("livekit.AutoTrackEgress", () => [
	{
		no: 1,
		name: "filepath",
		kind: "scalar",
		T: 9
	},
	{
		no: 5,
		name: "disable_manifest",
		kind: "scalar",
		T: 8
	},
	{
		no: 2,
		name: "s3",
		kind: "message",
		T: S3Upload,
		oneof: "output"
	},
	{
		no: 3,
		name: "gcp",
		kind: "message",
		T: GCPUpload,
		oneof: "output"
	},
	{
		no: 4,
		name: "azure",
		kind: "message",
		T: AzureBlobUpload,
		oneof: "output"
	},
	{
		no: 6,
		name: "aliOSS",
		kind: "message",
		T: AliOSSUpload,
		oneof: "output"
	}
]);
var RoomCompositeEgressRequest = /* @__PURE__ */ proto3.makeMessageType("livekit.RoomCompositeEgressRequest", () => [
	{
		no: 1,
		name: "room_name",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "layout",
		kind: "scalar",
		T: 9
	},
	{
		no: 3,
		name: "audio_only",
		kind: "scalar",
		T: 8
	},
	{
		no: 15,
		name: "audio_mixing",
		kind: "enum",
		T: proto3.getEnumType(AudioMixing)
	},
	{
		no: 4,
		name: "video_only",
		kind: "scalar",
		T: 8
	},
	{
		no: 5,
		name: "custom_base_url",
		kind: "scalar",
		T: 9
	},
	{
		no: 6,
		name: "file",
		kind: "message",
		T: EncodedFileOutput,
		oneof: "output"
	},
	{
		no: 7,
		name: "stream",
		kind: "message",
		T: StreamOutput,
		oneof: "output"
	},
	{
		no: 10,
		name: "segments",
		kind: "message",
		T: SegmentedFileOutput,
		oneof: "output"
	},
	{
		no: 8,
		name: "preset",
		kind: "enum",
		T: proto3.getEnumType(EncodingOptionsPreset),
		oneof: "options"
	},
	{
		no: 9,
		name: "advanced",
		kind: "message",
		T: EncodingOptions,
		oneof: "options"
	},
	{
		no: 11,
		name: "file_outputs",
		kind: "message",
		T: EncodedFileOutput,
		repeated: true
	},
	{
		no: 12,
		name: "stream_outputs",
		kind: "message",
		T: StreamOutput,
		repeated: true
	},
	{
		no: 13,
		name: "segment_outputs",
		kind: "message",
		T: SegmentedFileOutput,
		repeated: true
	},
	{
		no: 14,
		name: "image_outputs",
		kind: "message",
		T: ImageOutput,
		repeated: true
	},
	{
		no: 16,
		name: "webhooks",
		kind: "message",
		T: WebhookConfig,
		repeated: true
	}
]);
var EncodedFileOutput = /* @__PURE__ */ proto3.makeMessageType("livekit.EncodedFileOutput", () => [
	{
		no: 1,
		name: "file_type",
		kind: "enum",
		T: proto3.getEnumType(EncodedFileType)
	},
	{
		no: 2,
		name: "filepath",
		kind: "scalar",
		T: 9
	},
	{
		no: 6,
		name: "disable_manifest",
		kind: "scalar",
		T: 8
	},
	{
		no: 3,
		name: "s3",
		kind: "message",
		T: S3Upload,
		oneof: "output"
	},
	{
		no: 4,
		name: "gcp",
		kind: "message",
		T: GCPUpload,
		oneof: "output"
	},
	{
		no: 5,
		name: "azure",
		kind: "message",
		T: AzureBlobUpload,
		oneof: "output"
	},
	{
		no: 7,
		name: "aliOSS",
		kind: "message",
		T: AliOSSUpload,
		oneof: "output"
	}
]);
var RoomEgress = /* @__PURE__ */ proto3.makeMessageType("livekit.RoomEgress", () => [
	{
		no: 1,
		name: "room",
		kind: "message",
		T: RoomCompositeEgressRequest
	},
	{
		no: 3,
		name: "participant",
		kind: "message",
		T: AutoParticipantEgress
	},
	{
		no: 2,
		name: "tracks",
		kind: "message",
		T: AutoTrackEgress
	}
]);
var RoomConfiguration = /* @__PURE__ */ proto3.makeMessageType("livekit.RoomConfiguration", () => [
	{
		no: 1,
		name: "name",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "empty_timeout",
		kind: "scalar",
		T: 13
	},
	{
		no: 3,
		name: "departure_timeout",
		kind: "scalar",
		T: 13
	},
	{
		no: 4,
		name: "max_participants",
		kind: "scalar",
		T: 13
	},
	{
		no: 11,
		name: "metadata",
		kind: "scalar",
		T: 9
	},
	{
		no: 5,
		name: "egress",
		kind: "message",
		T: RoomEgress
	},
	{
		no: 7,
		name: "min_playout_delay",
		kind: "scalar",
		T: 13
	},
	{
		no: 8,
		name: "max_playout_delay",
		kind: "scalar",
		T: 13
	},
	{
		no: 9,
		name: "sync_streams",
		kind: "scalar",
		T: 8
	},
	{
		no: 10,
		name: "agents",
		kind: "message",
		T: RoomAgentDispatch,
		repeated: true
	},
	{
		no: 12,
		name: "tags",
		kind: "map",
		K: 9,
		V: {
			kind: "scalar",
			T: 9
		}
	}
]);
var SignalTarget = /* @__PURE__ */ proto3.makeEnum("livekit.SignalTarget", [{
	no: 0,
	name: "PUBLISHER"
}, {
	no: 1,
	name: "SUBSCRIBER"
}]);
var StreamState = /* @__PURE__ */ proto3.makeEnum("livekit.StreamState", [{
	no: 0,
	name: "ACTIVE"
}, {
	no: 1,
	name: "PAUSED"
}]);
var CandidateProtocol = /* @__PURE__ */ proto3.makeEnum("livekit.CandidateProtocol", [
	{
		no: 0,
		name: "UDP"
	},
	{
		no: 1,
		name: "TCP"
	},
	{
		no: 2,
		name: "TLS"
	}
]);
var SignalRequest = /* @__PURE__ */ proto3.makeMessageType("livekit.SignalRequest", () => [
	{
		no: 1,
		name: "offer",
		kind: "message",
		T: SessionDescription,
		oneof: "message"
	},
	{
		no: 2,
		name: "answer",
		kind: "message",
		T: SessionDescription,
		oneof: "message"
	},
	{
		no: 3,
		name: "trickle",
		kind: "message",
		T: TrickleRequest,
		oneof: "message"
	},
	{
		no: 4,
		name: "add_track",
		kind: "message",
		T: AddTrackRequest,
		oneof: "message"
	},
	{
		no: 5,
		name: "mute",
		kind: "message",
		T: MuteTrackRequest,
		oneof: "message"
	},
	{
		no: 6,
		name: "subscription",
		kind: "message",
		T: UpdateSubscription,
		oneof: "message"
	},
	{
		no: 7,
		name: "track_setting",
		kind: "message",
		T: UpdateTrackSettings,
		oneof: "message"
	},
	{
		no: 8,
		name: "leave",
		kind: "message",
		T: LeaveRequest,
		oneof: "message"
	},
	{
		no: 10,
		name: "update_layers",
		kind: "message",
		T: UpdateVideoLayers,
		oneof: "message"
	},
	{
		no: 11,
		name: "subscription_permission",
		kind: "message",
		T: SubscriptionPermission,
		oneof: "message"
	},
	{
		no: 12,
		name: "sync_state",
		kind: "message",
		T: SyncState,
		oneof: "message"
	},
	{
		no: 13,
		name: "simulate",
		kind: "message",
		T: SimulateScenario,
		oneof: "message"
	},
	{
		no: 14,
		name: "ping",
		kind: "scalar",
		T: 3,
		oneof: "message"
	},
	{
		no: 15,
		name: "update_metadata",
		kind: "message",
		T: UpdateParticipantMetadata,
		oneof: "message"
	},
	{
		no: 16,
		name: "ping_req",
		kind: "message",
		T: Ping,
		oneof: "message"
	},
	{
		no: 17,
		name: "update_audio_track",
		kind: "message",
		T: UpdateLocalAudioTrack,
		oneof: "message"
	},
	{
		no: 18,
		name: "update_video_track",
		kind: "message",
		T: UpdateLocalVideoTrack,
		oneof: "message"
	},
	{
		no: 19,
		name: "publish_data_track_request",
		kind: "message",
		T: PublishDataTrackRequest,
		oneof: "message"
	},
	{
		no: 20,
		name: "unpublish_data_track_request",
		kind: "message",
		T: UnpublishDataTrackRequest,
		oneof: "message"
	},
	{
		no: 21,
		name: "update_data_subscription",
		kind: "message",
		T: UpdateDataSubscription,
		oneof: "message"
	},
	{
		no: 22,
		name: "store_data_blob_request",
		kind: "message",
		T: StoreDataBlobRequest,
		oneof: "message"
	},
	{
		no: 23,
		name: "get_data_blob_request",
		kind: "message",
		T: GetDataBlobRequest,
		oneof: "message"
	}
]);
var SignalResponse = /* @__PURE__ */ proto3.makeMessageType("livekit.SignalResponse", () => [
	{
		no: 1,
		name: "join",
		kind: "message",
		T: JoinResponse,
		oneof: "message"
	},
	{
		no: 2,
		name: "answer",
		kind: "message",
		T: SessionDescription,
		oneof: "message"
	},
	{
		no: 3,
		name: "offer",
		kind: "message",
		T: SessionDescription,
		oneof: "message"
	},
	{
		no: 4,
		name: "trickle",
		kind: "message",
		T: TrickleRequest,
		oneof: "message"
	},
	{
		no: 5,
		name: "update",
		kind: "message",
		T: ParticipantUpdate,
		oneof: "message"
	},
	{
		no: 6,
		name: "track_published",
		kind: "message",
		T: TrackPublishedResponse,
		oneof: "message"
	},
	{
		no: 8,
		name: "leave",
		kind: "message",
		T: LeaveRequest,
		oneof: "message"
	},
	{
		no: 9,
		name: "mute",
		kind: "message",
		T: MuteTrackRequest,
		oneof: "message"
	},
	{
		no: 10,
		name: "speakers_changed",
		kind: "message",
		T: SpeakersChanged,
		oneof: "message"
	},
	{
		no: 11,
		name: "room_update",
		kind: "message",
		T: RoomUpdate,
		oneof: "message"
	},
	{
		no: 12,
		name: "connection_quality",
		kind: "message",
		T: ConnectionQualityUpdate,
		oneof: "message"
	},
	{
		no: 13,
		name: "stream_state_update",
		kind: "message",
		T: StreamStateUpdate,
		oneof: "message"
	},
	{
		no: 14,
		name: "subscribed_quality_update",
		kind: "message",
		T: SubscribedQualityUpdate,
		oneof: "message"
	},
	{
		no: 15,
		name: "subscription_permission_update",
		kind: "message",
		T: SubscriptionPermissionUpdate,
		oneof: "message"
	},
	{
		no: 16,
		name: "refresh_token",
		kind: "scalar",
		T: 9,
		oneof: "message"
	},
	{
		no: 17,
		name: "track_unpublished",
		kind: "message",
		T: TrackUnpublishedResponse,
		oneof: "message"
	},
	{
		no: 18,
		name: "pong",
		kind: "scalar",
		T: 3,
		oneof: "message"
	},
	{
		no: 19,
		name: "reconnect",
		kind: "message",
		T: ReconnectResponse,
		oneof: "message"
	},
	{
		no: 20,
		name: "pong_resp",
		kind: "message",
		T: Pong,
		oneof: "message"
	},
	{
		no: 21,
		name: "subscription_response",
		kind: "message",
		T: SubscriptionResponse,
		oneof: "message"
	},
	{
		no: 22,
		name: "request_response",
		kind: "message",
		T: RequestResponse,
		oneof: "message"
	},
	{
		no: 23,
		name: "track_subscribed",
		kind: "message",
		T: TrackSubscribed,
		oneof: "message"
	},
	{
		no: 24,
		name: "room_moved",
		kind: "message",
		T: RoomMovedResponse,
		oneof: "message"
	},
	{
		no: 25,
		name: "media_sections_requirement",
		kind: "message",
		T: MediaSectionsRequirement,
		oneof: "message"
	},
	{
		no: 26,
		name: "subscribed_audio_codec_update",
		kind: "message",
		T: SubscribedAudioCodecUpdate,
		oneof: "message"
	},
	{
		no: 27,
		name: "publish_data_track_response",
		kind: "message",
		T: PublishDataTrackResponse,
		oneof: "message"
	},
	{
		no: 28,
		name: "unpublish_data_track_response",
		kind: "message",
		T: UnpublishDataTrackResponse,
		oneof: "message"
	},
	{
		no: 29,
		name: "data_track_subscriber_handles",
		kind: "message",
		T: DataTrackSubscriberHandles,
		oneof: "message"
	},
	{
		no: 30,
		name: "store_data_blob_response",
		kind: "message",
		T: StoreDataBlobResponse,
		oneof: "message"
	},
	{
		no: 31,
		name: "get_data_blob_response",
		kind: "message",
		T: GetDataBlobResponse,
		oneof: "message"
	}
]);
var SimulcastCodec = /* @__PURE__ */ proto3.makeMessageType("livekit.SimulcastCodec", () => [
	{
		no: 1,
		name: "codec",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "cid",
		kind: "scalar",
		T: 9
	},
	{
		no: 4,
		name: "layers",
		kind: "message",
		T: VideoLayer,
		repeated: true
	},
	{
		no: 5,
		name: "video_layer_mode",
		kind: "enum",
		T: proto3.getEnumType(VideoLayer_Mode)
	}
]);
var AddTrackRequest = /* @__PURE__ */ proto3.makeMessageType("livekit.AddTrackRequest", () => [
	{
		no: 1,
		name: "cid",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "name",
		kind: "scalar",
		T: 9
	},
	{
		no: 3,
		name: "type",
		kind: "enum",
		T: proto3.getEnumType(TrackType)
	},
	{
		no: 4,
		name: "width",
		kind: "scalar",
		T: 13
	},
	{
		no: 5,
		name: "height",
		kind: "scalar",
		T: 13
	},
	{
		no: 6,
		name: "muted",
		kind: "scalar",
		T: 8
	},
	{
		no: 7,
		name: "disable_dtx",
		kind: "scalar",
		T: 8
	},
	{
		no: 8,
		name: "source",
		kind: "enum",
		T: proto3.getEnumType(TrackSource)
	},
	{
		no: 9,
		name: "layers",
		kind: "message",
		T: VideoLayer,
		repeated: true
	},
	{
		no: 10,
		name: "simulcast_codecs",
		kind: "message",
		T: SimulcastCodec,
		repeated: true
	},
	{
		no: 11,
		name: "sid",
		kind: "scalar",
		T: 9
	},
	{
		no: 12,
		name: "stereo",
		kind: "scalar",
		T: 8
	},
	{
		no: 13,
		name: "disable_red",
		kind: "scalar",
		T: 8
	},
	{
		no: 14,
		name: "encryption",
		kind: "enum",
		T: proto3.getEnumType(Encryption_Type)
	},
	{
		no: 15,
		name: "stream",
		kind: "scalar",
		T: 9
	},
	{
		no: 16,
		name: "backup_codec_policy",
		kind: "enum",
		T: proto3.getEnumType(BackupCodecPolicy$1)
	},
	{
		no: 17,
		name: "audio_features",
		kind: "enum",
		T: proto3.getEnumType(AudioTrackFeature),
		repeated: true
	},
	{
		no: 18,
		name: "packet_trailer_features",
		kind: "enum",
		T: proto3.getEnumType(PacketTrailerFeature),
		repeated: true
	}
]);
var PublishDataTrackRequest = /* @__PURE__ */ proto3.makeMessageType("livekit.PublishDataTrackRequest", () => [
	{
		no: 1,
		name: "pub_handle",
		kind: "scalar",
		T: 13
	},
	{
		no: 2,
		name: "name",
		kind: "scalar",
		T: 9
	},
	{
		no: 3,
		name: "encryption",
		kind: "enum",
		T: proto3.getEnumType(Encryption_Type)
	},
	{
		no: 4,
		name: "frame_encoding",
		kind: "message",
		T: DataTrackFrameEncoding,
		opt: true
	},
	{
		no: 5,
		name: "schema",
		kind: "message",
		T: DataTrackSchemaId,
		opt: true
	}
]);
var PublishDataTrackResponse = /* @__PURE__ */ proto3.makeMessageType("livekit.PublishDataTrackResponse", () => [{
	no: 1,
	name: "info",
	kind: "message",
	T: DataTrackInfo$1
}]);
var UnpublishDataTrackRequest = /* @__PURE__ */ proto3.makeMessageType("livekit.UnpublishDataTrackRequest", () => [{
	no: 1,
	name: "pub_handle",
	kind: "scalar",
	T: 13
}]);
var UnpublishDataTrackResponse = /* @__PURE__ */ proto3.makeMessageType("livekit.UnpublishDataTrackResponse", () => [{
	no: 1,
	name: "info",
	kind: "message",
	T: DataTrackInfo$1
}]);
var DataTrackSubscriberHandles = /* @__PURE__ */ proto3.makeMessageType("livekit.DataTrackSubscriberHandles", () => [{
	no: 1,
	name: "sub_handles",
	kind: "map",
	K: 13,
	V: {
		kind: "message",
		T: DataTrackSubscriberHandles_PublishedDataTrack
	}
}]);
var DataTrackSubscriberHandles_PublishedDataTrack = /* @__PURE__ */ proto3.makeMessageType("livekit.DataTrackSubscriberHandles.PublishedDataTrack", () => [
	{
		no: 1,
		name: "publisher_identity",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "publisher_sid",
		kind: "scalar",
		T: 9
	},
	{
		no: 3,
		name: "track_sid",
		kind: "scalar",
		T: 9
	}
], { localName: "DataTrackSubscriberHandles_PublishedDataTrack" });
var TrickleRequest = /* @__PURE__ */ proto3.makeMessageType("livekit.TrickleRequest", () => [
	{
		no: 1,
		name: "candidateInit",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "target",
		kind: "enum",
		T: proto3.getEnumType(SignalTarget)
	},
	{
		no: 3,
		name: "final",
		kind: "scalar",
		T: 8
	}
]);
var MuteTrackRequest = /* @__PURE__ */ proto3.makeMessageType("livekit.MuteTrackRequest", () => [{
	no: 1,
	name: "sid",
	kind: "scalar",
	T: 9
}, {
	no: 2,
	name: "muted",
	kind: "scalar",
	T: 8
}]);
var JoinResponse = /* @__PURE__ */ proto3.makeMessageType("livekit.JoinResponse", () => [
	{
		no: 1,
		name: "room",
		kind: "message",
		T: Room$1
	},
	{
		no: 2,
		name: "participant",
		kind: "message",
		T: ParticipantInfo
	},
	{
		no: 3,
		name: "other_participants",
		kind: "message",
		T: ParticipantInfo,
		repeated: true
	},
	{
		no: 4,
		name: "server_version",
		kind: "scalar",
		T: 9
	},
	{
		no: 5,
		name: "ice_servers",
		kind: "message",
		T: ICEServer,
		repeated: true
	},
	{
		no: 6,
		name: "subscriber_primary",
		kind: "scalar",
		T: 8
	},
	{
		no: 7,
		name: "alternative_url",
		kind: "scalar",
		T: 9
	},
	{
		no: 8,
		name: "client_configuration",
		kind: "message",
		T: ClientConfiguration
	},
	{
		no: 9,
		name: "server_region",
		kind: "scalar",
		T: 9
	},
	{
		no: 10,
		name: "ping_timeout",
		kind: "scalar",
		T: 5
	},
	{
		no: 11,
		name: "ping_interval",
		kind: "scalar",
		T: 5
	},
	{
		no: 12,
		name: "server_info",
		kind: "message",
		T: ServerInfo
	},
	{
		no: 13,
		name: "sif_trailer",
		kind: "scalar",
		T: 12
	},
	{
		no: 14,
		name: "enabled_publish_codecs",
		kind: "message",
		T: Codec,
		repeated: true
	},
	{
		no: 15,
		name: "fast_publish",
		kind: "scalar",
		T: 8
	}
]);
var ReconnectResponse = /* @__PURE__ */ proto3.makeMessageType("livekit.ReconnectResponse", () => [
	{
		no: 1,
		name: "ice_servers",
		kind: "message",
		T: ICEServer,
		repeated: true
	},
	{
		no: 2,
		name: "client_configuration",
		kind: "message",
		T: ClientConfiguration
	},
	{
		no: 3,
		name: "server_info",
		kind: "message",
		T: ServerInfo
	},
	{
		no: 4,
		name: "last_message_seq",
		kind: "scalar",
		T: 13
	}
]);
var TrackPublishedResponse = /* @__PURE__ */ proto3.makeMessageType("livekit.TrackPublishedResponse", () => [{
	no: 1,
	name: "cid",
	kind: "scalar",
	T: 9
}, {
	no: 2,
	name: "track",
	kind: "message",
	T: TrackInfo
}]);
var TrackUnpublishedResponse = /* @__PURE__ */ proto3.makeMessageType("livekit.TrackUnpublishedResponse", () => [{
	no: 1,
	name: "track_sid",
	kind: "scalar",
	T: 9
}]);
var SessionDescription = /* @__PURE__ */ proto3.makeMessageType("livekit.SessionDescription", () => [
	{
		no: 1,
		name: "type",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "sdp",
		kind: "scalar",
		T: 9
	},
	{
		no: 3,
		name: "id",
		kind: "scalar",
		T: 13
	},
	{
		no: 4,
		name: "mid_to_track_id",
		kind: "map",
		K: 9,
		V: {
			kind: "scalar",
			T: 9
		}
	}
]);
var ParticipantUpdate = /* @__PURE__ */ proto3.makeMessageType("livekit.ParticipantUpdate", () => [{
	no: 1,
	name: "participants",
	kind: "message",
	T: ParticipantInfo,
	repeated: true
}]);
var UpdateSubscription = /* @__PURE__ */ proto3.makeMessageType("livekit.UpdateSubscription", () => [
	{
		no: 1,
		name: "track_sids",
		kind: "scalar",
		T: 9,
		repeated: true
	},
	{
		no: 2,
		name: "subscribe",
		kind: "scalar",
		T: 8
	},
	{
		no: 3,
		name: "participant_tracks",
		kind: "message",
		T: ParticipantTracks,
		repeated: true
	}
]);
var UpdateDataSubscription = /* @__PURE__ */ proto3.makeMessageType("livekit.UpdateDataSubscription", () => [{
	no: 1,
	name: "updates",
	kind: "message",
	T: UpdateDataSubscription_Update,
	repeated: true
}]);
var UpdateDataSubscription_Update = /* @__PURE__ */ proto3.makeMessageType("livekit.UpdateDataSubscription.Update", () => [
	{
		no: 1,
		name: "track_sid",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "subscribe",
		kind: "scalar",
		T: 8
	},
	{
		no: 3,
		name: "options",
		kind: "message",
		T: DataTrackSubscriptionOptions
	}
], { localName: "UpdateDataSubscription_Update" });
var StoreDataBlobRequest = /* @__PURE__ */ proto3.makeMessageType("livekit.StoreDataBlobRequest", () => [{
	no: 1,
	name: "request_id",
	kind: "scalar",
	T: 13
}, {
	no: 2,
	name: "blob",
	kind: "message",
	T: DataBlob
}]);
var StoreDataBlobResponse = /* @__PURE__ */ proto3.makeMessageType("livekit.StoreDataBlobResponse", () => [{
	no: 1,
	name: "request_id",
	kind: "scalar",
	T: 13
}, {
	no: 2,
	name: "key",
	kind: "message",
	T: DataBlobKey
}]);
var GetDataBlobRequest = /* @__PURE__ */ proto3.makeMessageType("livekit.GetDataBlobRequest", () => [
	{
		no: 1,
		name: "request_id",
		kind: "scalar",
		T: 13
	},
	{
		no: 2,
		name: "participant_identity",
		kind: "scalar",
		T: 9
	},
	{
		no: 3,
		name: "key",
		kind: "message",
		T: DataBlobKey
	}
]);
var GetDataBlobResponse = /* @__PURE__ */ proto3.makeMessageType("livekit.GetDataBlobResponse", () => [{
	no: 1,
	name: "request_id",
	kind: "scalar",
	T: 13
}, {
	no: 2,
	name: "blob",
	kind: "message",
	T: DataBlob
}]);
var UpdateTrackSettings = /* @__PURE__ */ proto3.makeMessageType("livekit.UpdateTrackSettings", () => [
	{
		no: 1,
		name: "track_sids",
		kind: "scalar",
		T: 9,
		repeated: true
	},
	{
		no: 3,
		name: "disabled",
		kind: "scalar",
		T: 8
	},
	{
		no: 4,
		name: "quality",
		kind: "enum",
		T: proto3.getEnumType(VideoQuality$1)
	},
	{
		no: 5,
		name: "width",
		kind: "scalar",
		T: 13
	},
	{
		no: 6,
		name: "height",
		kind: "scalar",
		T: 13
	},
	{
		no: 7,
		name: "fps",
		kind: "scalar",
		T: 13
	},
	{
		no: 8,
		name: "priority",
		kind: "scalar",
		T: 13
	}
]);
var UpdateLocalAudioTrack = /* @__PURE__ */ proto3.makeMessageType("livekit.UpdateLocalAudioTrack", () => [{
	no: 1,
	name: "track_sid",
	kind: "scalar",
	T: 9
}, {
	no: 2,
	name: "features",
	kind: "enum",
	T: proto3.getEnumType(AudioTrackFeature),
	repeated: true
}]);
var UpdateLocalVideoTrack = /* @__PURE__ */ proto3.makeMessageType("livekit.UpdateLocalVideoTrack", () => [
	{
		no: 1,
		name: "track_sid",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "width",
		kind: "scalar",
		T: 13
	},
	{
		no: 3,
		name: "height",
		kind: "scalar",
		T: 13
	}
]);
var LeaveRequest = /* @__PURE__ */ proto3.makeMessageType("livekit.LeaveRequest", () => [
	{
		no: 1,
		name: "can_reconnect",
		kind: "scalar",
		T: 8
	},
	{
		no: 2,
		name: "reason",
		kind: "enum",
		T: proto3.getEnumType(DisconnectReason)
	},
	{
		no: 3,
		name: "action",
		kind: "enum",
		T: proto3.getEnumType(LeaveRequest_Action)
	},
	{
		no: 4,
		name: "regions",
		kind: "message",
		T: RegionSettings
	}
]);
var LeaveRequest_Action = /* @__PURE__ */ proto3.makeEnum("livekit.LeaveRequest.Action", [
	{
		no: 0,
		name: "DISCONNECT"
	},
	{
		no: 1,
		name: "RESUME"
	},
	{
		no: 2,
		name: "RECONNECT"
	}
]);
var UpdateVideoLayers = /* @__PURE__ */ proto3.makeMessageType("livekit.UpdateVideoLayers", () => [{
	no: 1,
	name: "track_sid",
	kind: "scalar",
	T: 9
}, {
	no: 2,
	name: "layers",
	kind: "message",
	T: VideoLayer,
	repeated: true
}]);
var UpdateParticipantMetadata = /* @__PURE__ */ proto3.makeMessageType("livekit.UpdateParticipantMetadata", () => [
	{
		no: 1,
		name: "metadata",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "name",
		kind: "scalar",
		T: 9
	},
	{
		no: 3,
		name: "attributes",
		kind: "map",
		K: 9,
		V: {
			kind: "scalar",
			T: 9
		}
	},
	{
		no: 4,
		name: "request_id",
		kind: "scalar",
		T: 13
	}
]);
var ICEServer = /* @__PURE__ */ proto3.makeMessageType("livekit.ICEServer", () => [
	{
		no: 1,
		name: "urls",
		kind: "scalar",
		T: 9,
		repeated: true
	},
	{
		no: 2,
		name: "username",
		kind: "scalar",
		T: 9
	},
	{
		no: 3,
		name: "credential",
		kind: "scalar",
		T: 9
	}
]);
var SpeakersChanged = /* @__PURE__ */ proto3.makeMessageType("livekit.SpeakersChanged", () => [{
	no: 1,
	name: "speakers",
	kind: "message",
	T: SpeakerInfo,
	repeated: true
}]);
var RoomUpdate = /* @__PURE__ */ proto3.makeMessageType("livekit.RoomUpdate", () => [{
	no: 1,
	name: "room",
	kind: "message",
	T: Room$1
}]);
var ConnectionQualityInfo = /* @__PURE__ */ proto3.makeMessageType("livekit.ConnectionQualityInfo", () => [
	{
		no: 1,
		name: "participant_sid",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "quality",
		kind: "enum",
		T: proto3.getEnumType(ConnectionQuality$1)
	},
	{
		no: 3,
		name: "score",
		kind: "scalar",
		T: 2
	}
]);
var ConnectionQualityUpdate = /* @__PURE__ */ proto3.makeMessageType("livekit.ConnectionQualityUpdate", () => [{
	no: 1,
	name: "updates",
	kind: "message",
	T: ConnectionQualityInfo,
	repeated: true
}]);
var StreamStateInfo = /* @__PURE__ */ proto3.makeMessageType("livekit.StreamStateInfo", () => [
	{
		no: 1,
		name: "participant_sid",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "track_sid",
		kind: "scalar",
		T: 9
	},
	{
		no: 3,
		name: "state",
		kind: "enum",
		T: proto3.getEnumType(StreamState)
	}
]);
var StreamStateUpdate = /* @__PURE__ */ proto3.makeMessageType("livekit.StreamStateUpdate", () => [{
	no: 1,
	name: "stream_states",
	kind: "message",
	T: StreamStateInfo,
	repeated: true
}]);
var SubscribedQuality = /* @__PURE__ */ proto3.makeMessageType("livekit.SubscribedQuality", () => [{
	no: 1,
	name: "quality",
	kind: "enum",
	T: proto3.getEnumType(VideoQuality$1)
}, {
	no: 2,
	name: "enabled",
	kind: "scalar",
	T: 8
}]);
var SubscribedCodec = /* @__PURE__ */ proto3.makeMessageType("livekit.SubscribedCodec", () => [{
	no: 1,
	name: "codec",
	kind: "scalar",
	T: 9
}, {
	no: 2,
	name: "qualities",
	kind: "message",
	T: SubscribedQuality,
	repeated: true
}]);
var SubscribedQualityUpdate = /* @__PURE__ */ proto3.makeMessageType("livekit.SubscribedQualityUpdate", () => [
	{
		no: 1,
		name: "track_sid",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "subscribed_qualities",
		kind: "message",
		T: SubscribedQuality,
		repeated: true
	},
	{
		no: 3,
		name: "subscribed_codecs",
		kind: "message",
		T: SubscribedCodec,
		repeated: true
	}
]);
var SubscribedAudioCodecUpdate = /* @__PURE__ */ proto3.makeMessageType("livekit.SubscribedAudioCodecUpdate", () => [{
	no: 1,
	name: "track_sid",
	kind: "scalar",
	T: 9
}, {
	no: 2,
	name: "subscribed_audio_codecs",
	kind: "message",
	T: SubscribedAudioCodec,
	repeated: true
}]);
var TrackPermission = /* @__PURE__ */ proto3.makeMessageType("livekit.TrackPermission", () => [
	{
		no: 1,
		name: "participant_sid",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "all_tracks",
		kind: "scalar",
		T: 8
	},
	{
		no: 3,
		name: "track_sids",
		kind: "scalar",
		T: 9,
		repeated: true
	},
	{
		no: 4,
		name: "participant_identity",
		kind: "scalar",
		T: 9
	}
]);
var SubscriptionPermission = /* @__PURE__ */ proto3.makeMessageType("livekit.SubscriptionPermission", () => [{
	no: 1,
	name: "all_participants",
	kind: "scalar",
	T: 8
}, {
	no: 2,
	name: "track_permissions",
	kind: "message",
	T: TrackPermission,
	repeated: true
}]);
var SubscriptionPermissionUpdate = /* @__PURE__ */ proto3.makeMessageType("livekit.SubscriptionPermissionUpdate", () => [
	{
		no: 1,
		name: "participant_sid",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "track_sid",
		kind: "scalar",
		T: 9
	},
	{
		no: 3,
		name: "allowed",
		kind: "scalar",
		T: 8
	}
]);
var RoomMovedResponse = /* @__PURE__ */ proto3.makeMessageType("livekit.RoomMovedResponse", () => [
	{
		no: 1,
		name: "room",
		kind: "message",
		T: Room$1
	},
	{
		no: 2,
		name: "token",
		kind: "scalar",
		T: 9
	},
	{
		no: 3,
		name: "participant",
		kind: "message",
		T: ParticipantInfo
	},
	{
		no: 4,
		name: "other_participants",
		kind: "message",
		T: ParticipantInfo,
		repeated: true
	}
]);
var SyncState = /* @__PURE__ */ proto3.makeMessageType("livekit.SyncState", () => [
	{
		no: 1,
		name: "answer",
		kind: "message",
		T: SessionDescription
	},
	{
		no: 2,
		name: "subscription",
		kind: "message",
		T: UpdateSubscription
	},
	{
		no: 3,
		name: "publish_tracks",
		kind: "message",
		T: TrackPublishedResponse,
		repeated: true
	},
	{
		no: 4,
		name: "data_channels",
		kind: "message",
		T: DataChannelInfo,
		repeated: true
	},
	{
		no: 5,
		name: "offer",
		kind: "message",
		T: SessionDescription
	},
	{
		no: 6,
		name: "track_sids_disabled",
		kind: "scalar",
		T: 9,
		repeated: true
	},
	{
		no: 7,
		name: "datachannel_receive_states",
		kind: "message",
		T: DataChannelReceiveState,
		repeated: true
	},
	{
		no: 8,
		name: "publish_data_tracks",
		kind: "message",
		T: PublishDataTrackResponse,
		repeated: true
	}
]);
var DataChannelReceiveState = /* @__PURE__ */ proto3.makeMessageType("livekit.DataChannelReceiveState", () => [{
	no: 1,
	name: "publisher_sid",
	kind: "scalar",
	T: 9
}, {
	no: 2,
	name: "last_seq",
	kind: "scalar",
	T: 13
}]);
var DataChannelInfo = /* @__PURE__ */ proto3.makeMessageType("livekit.DataChannelInfo", () => [
	{
		no: 1,
		name: "label",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "id",
		kind: "scalar",
		T: 13
	},
	{
		no: 3,
		name: "target",
		kind: "enum",
		T: proto3.getEnumType(SignalTarget)
	}
]);
var SimulateScenario = /* @__PURE__ */ proto3.makeMessageType("livekit.SimulateScenario", () => [
	{
		no: 1,
		name: "speaker_update",
		kind: "scalar",
		T: 5,
		oneof: "scenario"
	},
	{
		no: 2,
		name: "node_failure",
		kind: "scalar",
		T: 8,
		oneof: "scenario"
	},
	{
		no: 3,
		name: "migration",
		kind: "scalar",
		T: 8,
		oneof: "scenario"
	},
	{
		no: 4,
		name: "server_leave",
		kind: "scalar",
		T: 8,
		oneof: "scenario"
	},
	{
		no: 5,
		name: "switch_candidate_protocol",
		kind: "enum",
		T: proto3.getEnumType(CandidateProtocol),
		oneof: "scenario"
	},
	{
		no: 6,
		name: "subscriber_bandwidth",
		kind: "scalar",
		T: 3,
		oneof: "scenario"
	},
	{
		no: 7,
		name: "disconnect_signal_on_resume",
		kind: "scalar",
		T: 8,
		oneof: "scenario"
	},
	{
		no: 8,
		name: "disconnect_signal_on_resume_no_messages",
		kind: "scalar",
		T: 8,
		oneof: "scenario"
	},
	{
		no: 9,
		name: "leave_request_full_reconnect",
		kind: "scalar",
		T: 8,
		oneof: "scenario"
	}
]);
var Ping = /* @__PURE__ */ proto3.makeMessageType("livekit.Ping", () => [{
	no: 1,
	name: "timestamp",
	kind: "scalar",
	T: 3
}, {
	no: 2,
	name: "rtt",
	kind: "scalar",
	T: 3
}]);
var Pong = /* @__PURE__ */ proto3.makeMessageType("livekit.Pong", () => [{
	no: 1,
	name: "last_ping_timestamp",
	kind: "scalar",
	T: 3
}, {
	no: 2,
	name: "timestamp",
	kind: "scalar",
	T: 3
}]);
var RegionSettings = /* @__PURE__ */ proto3.makeMessageType("livekit.RegionSettings", () => [{
	no: 1,
	name: "regions",
	kind: "message",
	T: RegionInfo,
	repeated: true
}]);
var RegionInfo = /* @__PURE__ */ proto3.makeMessageType("livekit.RegionInfo", () => [
	{
		no: 1,
		name: "region",
		kind: "scalar",
		T: 9
	},
	{
		no: 2,
		name: "url",
		kind: "scalar",
		T: 9
	},
	{
		no: 3,
		name: "distance",
		kind: "scalar",
		T: 3
	}
]);
var SubscriptionResponse = /* @__PURE__ */ proto3.makeMessageType("livekit.SubscriptionResponse", () => [{
	no: 1,
	name: "track_sid",
	kind: "scalar",
	T: 9
}, {
	no: 2,
	name: "err",
	kind: "enum",
	T: proto3.getEnumType(SubscriptionError)
}]);
var RequestResponse = /* @__PURE__ */ proto3.makeMessageType("livekit.RequestResponse", () => [
	{
		no: 1,
		name: "request_id",
		kind: "scalar",
		T: 13
	},
	{
		no: 2,
		name: "reason",
		kind: "enum",
		T: proto3.getEnumType(RequestResponse_Reason)
	},
	{
		no: 3,
		name: "message",
		kind: "scalar",
		T: 9
	},
	{
		no: 4,
		name: "trickle",
		kind: "message",
		T: TrickleRequest,
		oneof: "request"
	},
	{
		no: 5,
		name: "add_track",
		kind: "message",
		T: AddTrackRequest,
		oneof: "request"
	},
	{
		no: 6,
		name: "mute",
		kind: "message",
		T: MuteTrackRequest,
		oneof: "request"
	},
	{
		no: 7,
		name: "update_metadata",
		kind: "message",
		T: UpdateParticipantMetadata,
		oneof: "request"
	},
	{
		no: 8,
		name: "update_audio_track",
		kind: "message",
		T: UpdateLocalAudioTrack,
		oneof: "request"
	},
	{
		no: 9,
		name: "update_video_track",
		kind: "message",
		T: UpdateLocalVideoTrack,
		oneof: "request"
	},
	{
		no: 10,
		name: "publish_data_track",
		kind: "message",
		T: PublishDataTrackRequest,
		oneof: "request"
	},
	{
		no: 11,
		name: "unpublish_data_track",
		kind: "message",
		T: UnpublishDataTrackRequest,
		oneof: "request"
	}
]);
var RequestResponse_Reason = /* @__PURE__ */ proto3.makeEnum("livekit.RequestResponse.Reason", [
	{
		no: 0,
		name: "OK"
	},
	{
		no: 1,
		name: "NOT_FOUND"
	},
	{
		no: 2,
		name: "NOT_ALLOWED"
	},
	{
		no: 3,
		name: "LIMIT_EXCEEDED"
	},
	{
		no: 4,
		name: "QUEUED"
	},
	{
		no: 5,
		name: "UNSUPPORTED_TYPE"
	},
	{
		no: 6,
		name: "UNCLASSIFIED_ERROR"
	},
	{
		no: 7,
		name: "INVALID_HANDLE"
	},
	{
		no: 8,
		name: "INVALID_NAME"
	},
	{
		no: 9,
		name: "DUPLICATE_HANDLE"
	},
	{
		no: 10,
		name: "DUPLICATE_NAME"
	},
	{
		no: 11,
		name: "INVALID_REQUEST"
	}
]);
var TrackSubscribed = /* @__PURE__ */ proto3.makeMessageType("livekit.TrackSubscribed", () => [{
	no: 1,
	name: "track_sid",
	kind: "scalar",
	T: 9
}]);
var ConnectionSettings = /* @__PURE__ */ proto3.makeMessageType("livekit.ConnectionSettings", () => [
	{
		no: 1,
		name: "auto_subscribe",
		kind: "scalar",
		T: 8
	},
	{
		no: 2,
		name: "adaptive_stream",
		kind: "scalar",
		T: 8
	},
	{
		no: 3,
		name: "subscriber_allow_pause",
		kind: "scalar",
		T: 8,
		opt: true
	},
	{
		no: 4,
		name: "disable_ice_lite",
		kind: "scalar",
		T: 8
	},
	{
		no: 5,
		name: "auto_subscribe_data_track",
		kind: "scalar",
		T: 8,
		opt: true
	}
]);
var JoinRequest = /* @__PURE__ */ proto3.makeMessageType("livekit.JoinRequest", () => [
	{
		no: 1,
		name: "client_info",
		kind: "message",
		T: ClientInfo
	},
	{
		no: 2,
		name: "connection_settings",
		kind: "message",
		T: ConnectionSettings
	},
	{
		no: 3,
		name: "metadata",
		kind: "scalar",
		T: 9
	},
	{
		no: 4,
		name: "participant_attributes",
		kind: "map",
		K: 9,
		V: {
			kind: "scalar",
			T: 9
		}
	},
	{
		no: 5,
		name: "add_track_requests",
		kind: "message",
		T: AddTrackRequest,
		repeated: true
	},
	{
		no: 6,
		name: "publisher_offer",
		kind: "message",
		T: SessionDescription
	},
	{
		no: 7,
		name: "reconnect",
		kind: "scalar",
		T: 8
	},
	{
		no: 8,
		name: "reconnect_reason",
		kind: "enum",
		T: proto3.getEnumType(ReconnectReason)
	},
	{
		no: 9,
		name: "participant_sid",
		kind: "scalar",
		T: 9
	},
	{
		no: 10,
		name: "sync_state",
		kind: "message",
		T: SyncState
	}
]);
var WrappedJoinRequest = /* @__PURE__ */ proto3.makeMessageType("livekit.WrappedJoinRequest", () => [{
	no: 1,
	name: "compression",
	kind: "enum",
	T: proto3.getEnumType(WrappedJoinRequest_Compression)
}, {
	no: 2,
	name: "join_request",
	kind: "scalar",
	T: 12
}]);
var WrappedJoinRequest_Compression = /* @__PURE__ */ proto3.makeEnum("livekit.WrappedJoinRequest.Compression", [{
	no: 0,
	name: "NONE"
}, {
	no: 1,
	name: "GZIP"
}]);
var MediaSectionsRequirement = /* @__PURE__ */ proto3.makeMessageType("livekit.MediaSectionsRequirement", () => [{
	no: 1,
	name: "num_audios",
	kind: "scalar",
	T: 13
}, {
	no: 2,
	name: "num_videos",
	kind: "scalar",
	T: 13
}]);
function getDefaultExportFromCjs(x) {
	return x && x.__esModule && Object.prototype.hasOwnProperty.call(x, "default") ? x["default"] : x;
}
var loglevel$1 = { exports: {} };
var loglevel = loglevel$1.exports;
var hasRequiredLoglevel;
function requireLoglevel() {
	if (hasRequiredLoglevel) return loglevel$1.exports;
	hasRequiredLoglevel = 1;
	(function(module) {
		(function(root, definition) {
			if (module.exports) module.exports = definition();
			else root.log = definition();
		})(loglevel, function() {
			var noop = function() {};
			var undefinedType = "undefined";
			var isIE = typeof window !== undefinedType && typeof window.navigator !== undefinedType && /Trident\/|MSIE /.test(window.navigator.userAgent);
			var logMethods = [
				"trace",
				"debug",
				"info",
				"warn",
				"error"
			];
			var _loggersByName = {};
			var defaultLogger = null;
			function bindMethod(obj, methodName) {
				var method = obj[methodName];
				if (typeof method.bind === "function") return method.bind(obj);
				else try {
					return Function.prototype.bind.call(method, obj);
				} catch (e) {
					return function() {
						return Function.prototype.apply.apply(method, [obj, arguments]);
					};
				}
			}
			function traceForIE() {
				if (console.log) {
					if (console.log.apply) console.log.apply(console, arguments);
					else Function.prototype.apply.apply(console.log, [console, arguments]);
				}
				if (console.trace) console.trace();
			}
			function realMethod(methodName) {
				if (methodName === "debug") methodName = "log";
				if (typeof console === undefinedType) return false;
				else if (methodName === "trace" && isIE) return traceForIE;
				else if (console[methodName] !== void 0) return bindMethod(console, methodName);
				else if (console.log !== void 0) return bindMethod(console, "log");
				else return noop;
			}
			function replaceLoggingMethods() {
				var level = this.getLevel();
				for (var i = 0; i < logMethods.length; i++) {
					var methodName = logMethods[i];
					this[methodName] = i < level ? noop : this.methodFactory(methodName, level, this.name);
				}
				this.log = this.debug;
				if (typeof console === undefinedType && level < this.levels.SILENT) return "No console available for logging";
			}
			function enableLoggingWhenConsoleArrives(methodName) {
				return function() {
					if (typeof console !== undefinedType) {
						replaceLoggingMethods.call(this);
						this[methodName].apply(this, arguments);
					}
				};
			}
			function defaultMethodFactory(methodName, _level, _loggerName) {
				return realMethod(methodName) || enableLoggingWhenConsoleArrives.apply(this, arguments);
			}
			function Logger(name, factory) {
				var self = this;
				/**
				* The level inherited from a parent logger (or a global default). We
				* cache this here rather than delegating to the parent so that it stays
				* in sync with the actual logging methods that we have installed (the
				* parent could change levels but we might not have rebuilt the loggers
				* in this child yet).
				* @type {number}
				*/
				var inheritedLevel;
				/**
				* The default level for this logger, if any. If set, this overrides
				* `inheritedLevel`.
				* @type {number|null}
				*/
				var defaultLevel;
				/**
				* A user-specific level for this logger. If set, this overrides
				* `defaultLevel`.
				* @type {number|null}
				*/
				var userLevel;
				var storageKey = "loglevel";
				if (typeof name === "string") storageKey += ":" + name;
				else if (typeof name === "symbol") storageKey = void 0;
				function persistLevelIfPossible(levelNum) {
					var levelName = (logMethods[levelNum] || "silent").toUpperCase();
					if (typeof window === undefinedType || !storageKey) return;
					try {
						window.localStorage[storageKey] = levelName;
						return;
					} catch (ignore) {}
					try {
						window.document.cookie = encodeURIComponent(storageKey) + "=" + levelName + ";";
					} catch (ignore) {}
				}
				function getPersistedLevel() {
					var storedLevel;
					if (typeof window === undefinedType || !storageKey) return;
					try {
						storedLevel = window.localStorage[storageKey];
					} catch (ignore) {}
					if (typeof storedLevel === undefinedType) try {
						var cookie = window.document.cookie;
						var cookieName = encodeURIComponent(storageKey);
						var location = cookie.indexOf(cookieName + "=");
						if (location !== -1) storedLevel = /^([^;]+)/.exec(cookie.slice(location + cookieName.length + 1))[1];
					} catch (ignore) {}
					if (self.levels[storedLevel] === void 0) storedLevel = void 0;
					return storedLevel;
				}
				function clearPersistedLevel() {
					if (typeof window === undefinedType || !storageKey) return;
					try {
						window.localStorage.removeItem(storageKey);
					} catch (ignore) {}
					try {
						window.document.cookie = encodeURIComponent(storageKey) + "=; expires=Thu, 01 Jan 1970 00:00:00 UTC";
					} catch (ignore) {}
				}
				function normalizeLevel(input) {
					var level = input;
					if (typeof level === "string" && self.levels[level.toUpperCase()] !== void 0) level = self.levels[level.toUpperCase()];
					if (typeof level === "number" && level >= 0 && level <= self.levels.SILENT) return level;
					else throw new TypeError("log.setLevel() called with invalid level: " + input);
				}
				self.name = name;
				self.levels = {
					"TRACE": 0,
					"DEBUG": 1,
					"INFO": 2,
					"WARN": 3,
					"ERROR": 4,
					"SILENT": 5
				};
				self.methodFactory = factory || defaultMethodFactory;
				self.getLevel = function() {
					if (userLevel != null) return userLevel;
					else if (defaultLevel != null) return defaultLevel;
					else return inheritedLevel;
				};
				self.setLevel = function(level, persist) {
					userLevel = normalizeLevel(level);
					if (persist !== false) persistLevelIfPossible(userLevel);
					return replaceLoggingMethods.call(self);
				};
				self.setDefaultLevel = function(level) {
					defaultLevel = normalizeLevel(level);
					if (!getPersistedLevel()) self.setLevel(level, false);
				};
				self.resetLevel = function() {
					userLevel = null;
					clearPersistedLevel();
					replaceLoggingMethods.call(self);
				};
				self.enableAll = function(persist) {
					self.setLevel(self.levels.TRACE, persist);
				};
				self.disableAll = function(persist) {
					self.setLevel(self.levels.SILENT, persist);
				};
				self.rebuild = function() {
					if (defaultLogger !== self) inheritedLevel = normalizeLevel(defaultLogger.getLevel());
					replaceLoggingMethods.call(self);
					if (defaultLogger === self) for (var childName in _loggersByName) _loggersByName[childName].rebuild();
				};
				inheritedLevel = normalizeLevel(defaultLogger ? defaultLogger.getLevel() : "WARN");
				var initialLevel = getPersistedLevel();
				if (initialLevel != null) userLevel = normalizeLevel(initialLevel);
				replaceLoggingMethods.call(self);
			}
			defaultLogger = new Logger();
			defaultLogger.getLogger = function getLogger(name) {
				if (typeof name !== "symbol" && typeof name !== "string" || name === "") throw new TypeError("You must supply a name when creating a logger.");
				var logger = _loggersByName[name];
				if (!logger) logger = _loggersByName[name] = new Logger(name, defaultLogger.methodFactory);
				return logger;
			};
			var _log = typeof window !== undefinedType ? window.log : void 0;
			defaultLogger.noConflict = function() {
				if (typeof window !== undefinedType && window.log === defaultLogger) window.log = _log;
				return defaultLogger;
			};
			defaultLogger.getLoggers = function getLoggers() {
				return _loggersByName;
			};
			defaultLogger["default"] = defaultLogger;
			return defaultLogger;
		});
	})(loglevel$1);
	return loglevel$1.exports;
}
var loglevelExports = requireLoglevel();
var LogLevel;
(function(LogLevel) {
	LogLevel[LogLevel["trace"] = 0] = "trace";
	LogLevel[LogLevel["debug"] = 1] = "debug";
	LogLevel[LogLevel["info"] = 2] = "info";
	LogLevel[LogLevel["warn"] = 3] = "warn";
	LogLevel[LogLevel["error"] = 4] = "error";
	LogLevel[LogLevel["silent"] = 5] = "silent";
})(LogLevel || (LogLevel = {}));
var LoggerNames;
(function(LoggerNames) {
	LoggerNames["Default"] = "livekit";
	LoggerNames["Room"] = "livekit-room";
	LoggerNames["TokenSource"] = "livekit-token-source";
	LoggerNames["Participant"] = "livekit-participant";
	LoggerNames["Track"] = "livekit-track";
	LoggerNames["Publication"] = "livekit-track-publication";
	LoggerNames["Engine"] = "livekit-engine";
	LoggerNames["Signal"] = "livekit-signal";
	LoggerNames["PCManager"] = "livekit-pc-manager";
	LoggerNames["PCTransport"] = "livekit-pc-transport";
	LoggerNames["E2EE"] = "lk-e2ee";
	LoggerNames["DataTracks"] = "livekit-data-tracks";
	LoggerNames["Region"] = "livekit-region";
	LoggerNames["ICE"] = "livekit-ice";
})(LoggerNames || (LoggerNames = {}));
var livekitLogger = loglevelExports.getLogger(LoggerNames.Default);
var livekitLoggers = Object.values(LoggerNames).map((name) => loglevelExports.getLogger(name));
livekitLogger.setDefaultLevel(LogLevel.info);
/**
* @internal
*
* Get a named logger. When `ctxFn` is supplied, every log call
* automatically:
* 1. prepends a `[key=value ...]` prefix derived from `ctxFn()` to the
*    message string, so identifiers are visible in browser devtools
*    without expanding the structured context object, and
* 2. merges `ctxFn()` into the structured context passed to any
*    `setLogExtension` consumer, so ingestion pipelines continue to
*    receive the full metadata unchanged.
*/
function getLogger(name, ctxFn) {
	const logger = loglevelExports.getLogger(name);
	logger.setDefaultLevel(livekitLogger.getLevel());
	if (!ctxFn) return logger;
	return wrapWithContext(logger, ctxFn);
}
function wrapWithContext(base, ctxFn) {
	const wrap = (method) => (msg, extra) => {
		const ctx = ctxFn();
		const merged = ctx || extra ? Object.assign(Object.assign({}, ctx), extra) : void 0;
		base[method](msg, merged);
	};
	const proxy = Object.create(base);
	proxy.trace = wrap("trace");
	proxy.debug = wrap("debug");
	proxy.info = wrap("info");
	proxy.warn = wrap("warn");
	proxy.error = wrap("error");
	return proxy;
}
function setLogLevel(level, loggerName) {
	if (loggerName) loglevelExports.getLogger(loggerName).setLevel(level);
	else for (const logger of livekitLoggers) logger.setLevel(level);
}
/**
* use this to hook into the logging function to allow sending internal livekit logs to third party services
* if set, the browser logs will lose their stacktrace information (see https://github.com/pimterry/loglevel#writing-plugins)
*/
function setLogExtension(extension, logger) {
	(logger ? [logger] : livekitLoggers).forEach((logR) => {
		const originalFactory = logR.methodFactory;
		logR.methodFactory = (methodName, configLevel, loggerName) => {
			const rawMethod = originalFactory(methodName, configLevel, loggerName);
			const logLevel = LogLevel[methodName];
			const needLog = logLevel >= configLevel && logLevel < LogLevel.silent;
			return (msg, context) => {
				if (context) rawMethod(msg, context);
				else rawMethod(msg);
				if (needLog) extension(logLevel, msg, context);
			};
		};
		logR.setLevel(logR.getLevel());
	});
}
var workerLogger = loglevelExports.getLogger(LoggerNames.E2EE);
var maxRetryDelay = 7e3;
var DEFAULT_RETRY_DELAYS_IN_MS = [
	0,
	300,
	1200,
	2700,
	4800,
	maxRetryDelay,
	maxRetryDelay,
	maxRetryDelay,
	maxRetryDelay,
	maxRetryDelay
];
var DefaultReconnectPolicy = class {
	constructor(retryDelays) {
		this._retryDelays = retryDelays !== void 0 ? [...retryDelays] : DEFAULT_RETRY_DELAYS_IN_MS;
	}
	nextRetryDelayInMs(context) {
		if (context.retryCount >= this._retryDelays.length) return null;
		const retryDelay = this._retryDelays[context.retryCount];
		if (context.retryCount <= 1) return retryDelay;
		return retryDelay + Math.random() * 1e3;
	}
};
function __rest(s, e) {
	var t = {};
	for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0) t[p] = s[p];
	if (s != null && typeof Object.getOwnPropertySymbols === "function") {
		for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i])) t[p[i]] = s[p[i]];
	}
	return t;
}
function __awaiter(thisArg, _arguments, P, generator) {
	function adopt(value) {
		return value instanceof P ? value : new P(function(resolve) {
			resolve(value);
		});
	}
	return new (P || (P = Promise))(function(resolve, reject) {
		function fulfilled(value) {
			try {
				step(generator.next(value));
			} catch (e) {
				reject(e);
			}
		}
		function rejected(value) {
			try {
				step(generator["throw"](value));
			} catch (e) {
				reject(e);
			}
		}
		function step(result) {
			result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
		}
		step((generator = generator.apply(thisArg, _arguments || [])).next());
	});
}
function __values(o) {
	var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
	if (m) return m.call(o);
	if (o && typeof o.length === "number") return { next: function() {
		if (o && i >= o.length) o = void 0;
		return {
			value: o && o[i++],
			done: !o
		};
	} };
	throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
}
function __await(v) {
	return this instanceof __await ? (this.v = v, this) : new __await(v);
}
function __asyncGenerator(thisArg, _arguments, generator) {
	if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
	var g = generator.apply(thisArg, _arguments || []), i, q = [];
	return i = Object.create((typeof AsyncIterator === "function" ? AsyncIterator : Object).prototype), verb("next"), verb("throw"), verb("return", awaitReturn), i[Symbol.asyncIterator] = function() {
		return this;
	}, i;
	function awaitReturn(f) {
		return function(v) {
			return Promise.resolve(v).then(f, reject);
		};
	}
	function verb(n, f) {
		if (g[n]) {
			i[n] = function(v) {
				return new Promise(function(a, b) {
					q.push([
						n,
						v,
						a,
						b
					]) > 1 || resume(n, v);
				});
			};
			if (f) i[n] = f(i[n]);
		}
	}
	function resume(n, v) {
		try {
			step(g[n](v));
		} catch (e) {
			settle(q[0][3], e);
		}
	}
	function step(r) {
		r.value instanceof __await ? Promise.resolve(r.value.v).then(fulfill, reject) : settle(q[0][2], r);
	}
	function fulfill(value) {
		resume("next", value);
	}
	function reject(value) {
		resume("throw", value);
	}
	function settle(f, v) {
		if (f(v), q.shift(), q.length) resume(q[0][0], q[0][1]);
	}
}
function __asyncDelegator(o) {
	var i, p;
	return i = {}, verb("next"), verb("throw", function(e) {
		throw e;
	}), verb("return"), i[Symbol.iterator] = function() {
		return this;
	}, i;
	function verb(n, f) {
		i[n] = o[n] ? function(v) {
			return (p = !p) ? {
				value: __await(o[n](v)),
				done: false
			} : f ? f(v) : v;
		} : f;
	}
}
function __asyncValues(o) {
	if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
	var m = o[Symbol.asyncIterator], i;
	return m ? m.call(o) : (o = typeof __values === "function" ? __values(o) : o[Symbol.iterator](), i = {}, verb("next"), verb("throw"), verb("return"), i[Symbol.asyncIterator] = function() {
		return this;
	}, i);
	function verb(n) {
		i[n] = o[n] && function(v) {
			return new Promise(function(resolve, reject) {
				v = o[n](v), settle(resolve, reject, v.done, v.value);
			});
		};
	}
	function settle(resolve, reject, d, v) {
		Promise.resolve(v).then(function(v) {
			resolve({
				value: v,
				done: d
			});
		}, reject);
	}
}
var events = { exports: {} };
var hasRequiredEvents;
function requireEvents() {
	if (hasRequiredEvents) return events.exports;
	hasRequiredEvents = 1;
	var R = typeof Reflect === "object" ? Reflect : null;
	var ReflectApply = R && typeof R.apply === "function" ? R.apply : function ReflectApply(target, receiver, args) {
		return Function.prototype.apply.call(target, receiver, args);
	};
	var ReflectOwnKeys;
	if (R && typeof R.ownKeys === "function") ReflectOwnKeys = R.ownKeys;
	else if (Object.getOwnPropertySymbols) ReflectOwnKeys = function ReflectOwnKeys(target) {
		return Object.getOwnPropertyNames(target).concat(Object.getOwnPropertySymbols(target));
	};
	else ReflectOwnKeys = function ReflectOwnKeys(target) {
		return Object.getOwnPropertyNames(target);
	};
	function ProcessEmitWarning(warning) {
		if (console && console.warn) console.warn(warning);
	}
	var NumberIsNaN = Number.isNaN || function NumberIsNaN(value) {
		return value !== value;
	};
	function EventEmitter() {
		EventEmitter.init.call(this);
	}
	events.exports = EventEmitter;
	events.exports.once = once;
	EventEmitter.EventEmitter = EventEmitter;
	EventEmitter.prototype._events = void 0;
	EventEmitter.prototype._eventsCount = 0;
	EventEmitter.prototype._maxListeners = void 0;
	var defaultMaxListeners = 10;
	function checkListener(listener) {
		if (typeof listener !== "function") throw new TypeError("The \"listener\" argument must be of type Function. Received type " + typeof listener);
	}
	Object.defineProperty(EventEmitter, "defaultMaxListeners", {
		enumerable: true,
		get: function() {
			return defaultMaxListeners;
		},
		set: function(arg) {
			if (typeof arg !== "number" || arg < 0 || NumberIsNaN(arg)) throw new RangeError("The value of \"defaultMaxListeners\" is out of range. It must be a non-negative number. Received " + arg + ".");
			defaultMaxListeners = arg;
		}
	});
	EventEmitter.init = function() {
		if (this._events === void 0 || this._events === Object.getPrototypeOf(this)._events) {
			this._events = Object.create(null);
			this._eventsCount = 0;
		}
		this._maxListeners = this._maxListeners || void 0;
	};
	EventEmitter.prototype.setMaxListeners = function setMaxListeners(n) {
		if (typeof n !== "number" || n < 0 || NumberIsNaN(n)) throw new RangeError("The value of \"n\" is out of range. It must be a non-negative number. Received " + n + ".");
		this._maxListeners = n;
		return this;
	};
	function _getMaxListeners(that) {
		if (that._maxListeners === void 0) return EventEmitter.defaultMaxListeners;
		return that._maxListeners;
	}
	EventEmitter.prototype.getMaxListeners = function getMaxListeners() {
		return _getMaxListeners(this);
	};
	EventEmitter.prototype.emit = function emit(type) {
		var args = [];
		for (var i = 1; i < arguments.length; i++) args.push(arguments[i]);
		var doError = type === "error";
		var events = this._events;
		if (events !== void 0) doError = doError && events.error === void 0;
		else if (!doError) return false;
		if (doError) {
			var er;
			if (args.length > 0) er = args[0];
			if (er instanceof Error) throw er;
			var err = /* @__PURE__ */ new Error("Unhandled error." + (er ? " (" + er.message + ")" : ""));
			err.context = er;
			throw err;
		}
		var handler = events[type];
		if (handler === void 0) return false;
		if (typeof handler === "function") ReflectApply(handler, this, args);
		else {
			var len = handler.length;
			var listeners = arrayClone(handler, len);
			for (var i = 0; i < len; ++i) ReflectApply(listeners[i], this, args);
		}
		return true;
	};
	function _addListener(target, type, listener, prepend) {
		var m;
		var events;
		var existing;
		checkListener(listener);
		events = target._events;
		if (events === void 0) {
			events = target._events = Object.create(null);
			target._eventsCount = 0;
		} else {
			if (events.newListener !== void 0) {
				target.emit("newListener", type, listener.listener ? listener.listener : listener);
				events = target._events;
			}
			existing = events[type];
		}
		if (existing === void 0) {
			existing = events[type] = listener;
			++target._eventsCount;
		} else {
			if (typeof existing === "function") existing = events[type] = prepend ? [listener, existing] : [existing, listener];
			else if (prepend) existing.unshift(listener);
			else existing.push(listener);
			m = _getMaxListeners(target);
			if (m > 0 && existing.length > m && !existing.warned) {
				existing.warned = true;
				var w = /* @__PURE__ */ new Error("Possible EventEmitter memory leak detected. " + existing.length + " " + String(type) + " listeners added. Use emitter.setMaxListeners() to increase limit");
				w.name = "MaxListenersExceededWarning";
				w.emitter = target;
				w.type = type;
				w.count = existing.length;
				ProcessEmitWarning(w);
			}
		}
		return target;
	}
	EventEmitter.prototype.addListener = function addListener(type, listener) {
		return _addListener(this, type, listener, false);
	};
	EventEmitter.prototype.on = EventEmitter.prototype.addListener;
	EventEmitter.prototype.prependListener = function prependListener(type, listener) {
		return _addListener(this, type, listener, true);
	};
	function onceWrapper() {
		if (!this.fired) {
			this.target.removeListener(this.type, this.wrapFn);
			this.fired = true;
			if (arguments.length === 0) return this.listener.call(this.target);
			return this.listener.apply(this.target, arguments);
		}
	}
	function _onceWrap(target, type, listener) {
		var state = {
			fired: false,
			wrapFn: void 0,
			target,
			type,
			listener
		};
		var wrapped = onceWrapper.bind(state);
		wrapped.listener = listener;
		state.wrapFn = wrapped;
		return wrapped;
	}
	EventEmitter.prototype.once = function once(type, listener) {
		checkListener(listener);
		this.on(type, _onceWrap(this, type, listener));
		return this;
	};
	EventEmitter.prototype.prependOnceListener = function prependOnceListener(type, listener) {
		checkListener(listener);
		this.prependListener(type, _onceWrap(this, type, listener));
		return this;
	};
	EventEmitter.prototype.removeListener = function removeListener(type, listener) {
		var list, events, position, i, originalListener;
		checkListener(listener);
		events = this._events;
		if (events === void 0) return this;
		list = events[type];
		if (list === void 0) return this;
		if (list === listener || list.listener === listener) {
			if (--this._eventsCount === 0) this._events = Object.create(null);
			else {
				delete events[type];
				if (events.removeListener) this.emit("removeListener", type, list.listener || listener);
			}
		} else if (typeof list !== "function") {
			position = -1;
			for (i = list.length - 1; i >= 0; i--) if (list[i] === listener || list[i].listener === listener) {
				originalListener = list[i].listener;
				position = i;
				break;
			}
			if (position < 0) return this;
			if (position === 0) list.shift();
			else spliceOne(list, position);
			if (list.length === 1) events[type] = list[0];
			if (events.removeListener !== void 0) this.emit("removeListener", type, originalListener || listener);
		}
		return this;
	};
	EventEmitter.prototype.off = EventEmitter.prototype.removeListener;
	EventEmitter.prototype.removeAllListeners = function removeAllListeners(type) {
		var listeners, events = this._events, i;
		if (events === void 0) return this;
		if (events.removeListener === void 0) {
			if (arguments.length === 0) {
				this._events = Object.create(null);
				this._eventsCount = 0;
			} else if (events[type] !== void 0) {
				if (--this._eventsCount === 0) this._events = Object.create(null);
				else delete events[type];
			}
			return this;
		}
		if (arguments.length === 0) {
			var keys = Object.keys(events);
			var key;
			for (i = 0; i < keys.length; ++i) {
				key = keys[i];
				if (key === "removeListener") continue;
				this.removeAllListeners(key);
			}
			this.removeAllListeners("removeListener");
			this._events = Object.create(null);
			this._eventsCount = 0;
			return this;
		}
		listeners = events[type];
		if (typeof listeners === "function") this.removeListener(type, listeners);
		else if (listeners !== void 0) for (i = listeners.length - 1; i >= 0; i--) this.removeListener(type, listeners[i]);
		return this;
	};
	function _listeners(target, type, unwrap) {
		var events = target._events;
		if (events === void 0) return [];
		var evlistener = events[type];
		if (evlistener === void 0) return [];
		if (typeof evlistener === "function") return unwrap ? [evlistener.listener || evlistener] : [evlistener];
		return unwrap ? unwrapListeners(evlistener) : arrayClone(evlistener, evlistener.length);
	}
	EventEmitter.prototype.listeners = function listeners(type) {
		return _listeners(this, type, true);
	};
	EventEmitter.prototype.rawListeners = function rawListeners(type) {
		return _listeners(this, type, false);
	};
	EventEmitter.listenerCount = function(emitter, type) {
		if (typeof emitter.listenerCount === "function") return emitter.listenerCount(type);
		else return listenerCount.call(emitter, type);
	};
	EventEmitter.prototype.listenerCount = listenerCount;
	function listenerCount(type) {
		var events = this._events;
		if (events !== void 0) {
			var evlistener = events[type];
			if (typeof evlistener === "function") return 1;
			else if (evlistener !== void 0) return evlistener.length;
		}
		return 0;
	}
	EventEmitter.prototype.eventNames = function eventNames() {
		return this._eventsCount > 0 ? ReflectOwnKeys(this._events) : [];
	};
	function arrayClone(arr, n) {
		var copy = new Array(n);
		for (var i = 0; i < n; ++i) copy[i] = arr[i];
		return copy;
	}
	function spliceOne(list, index) {
		for (; index + 1 < list.length; index++) list[index] = list[index + 1];
		list.pop();
	}
	function unwrapListeners(arr) {
		var ret = new Array(arr.length);
		for (var i = 0; i < ret.length; ++i) ret[i] = arr[i].listener || arr[i];
		return ret;
	}
	function once(emitter, name) {
		return new Promise(function(resolve, reject) {
			function errorListener(err) {
				emitter.removeListener(name, resolver);
				reject(err);
			}
			function resolver() {
				if (typeof emitter.removeListener === "function") emitter.removeListener("error", errorListener);
				resolve([].slice.call(arguments));
			}
			eventTargetAgnosticAddListener(emitter, name, resolver, { once: true });
			if (name !== "error") addErrorHandlerIfEventEmitter(emitter, errorListener, { once: true });
		});
	}
	function addErrorHandlerIfEventEmitter(emitter, handler, flags) {
		if (typeof emitter.on === "function") eventTargetAgnosticAddListener(emitter, "error", handler, flags);
	}
	function eventTargetAgnosticAddListener(emitter, name, listener, flags) {
		if (typeof emitter.on === "function") {
			if (flags.once) emitter.once(name, listener);
			else emitter.on(name, listener);
		} else if (typeof emitter.addEventListener === "function") emitter.addEventListener(name, function wrapListener(arg) {
			if (flags.once) emitter.removeEventListener(name, wrapListener);
			listener(arg);
		});
		else throw new TypeError("The \"emitter\" argument must be of type EventEmitter. Received type " + typeof emitter);
	}
	return events.exports;
}
var eventsExports = requireEvents();
var logDisabled_ = true;
var deprecationWarnings_ = true;
/**
* Extract browser version out of the provided user agent string.
*
* @param {!string} uastring userAgent string.
* @param {!string} expr Regular expression used as match criteria.
* @param {!number} pos position in the version string to be returned.
* @return {!number} browser version.
*/
function extractVersion(uastring, expr, pos) {
	const match = uastring.match(expr);
	return match && match.length >= pos && parseFloat(match[pos], 10);
}
function wrapPeerConnectionEvent(window, eventNameToWrap, wrapper) {
	if (!window.RTCPeerConnection) return;
	if (!Object.getOwnPropertyDescriptor(EventTarget.prototype, "addEventListener").writable) {
		log$5("Unable to polyfill events");
		return;
	}
	const proto = window.RTCPeerConnection.prototype;
	const nativeAddEventListener = proto.addEventListener;
	proto.addEventListener = function(nativeEventName, cb) {
		if (nativeEventName !== eventNameToWrap) return nativeAddEventListener.apply(this, arguments);
		const wrappedCallback = (e) => {
			const modifiedEvent = wrapper(e);
			if (modifiedEvent) {
				if (cb.handleEvent) cb.handleEvent(modifiedEvent);
				else cb(modifiedEvent);
			}
		};
		this._eventMap = this._eventMap || {};
		if (!this._eventMap[eventNameToWrap]) this._eventMap[eventNameToWrap] = /* @__PURE__ */ new Map();
		this._eventMap[eventNameToWrap].set(cb, wrappedCallback);
		return nativeAddEventListener.apply(this, [nativeEventName, wrappedCallback]);
	};
	const nativeRemoveEventListener = proto.removeEventListener;
	proto.removeEventListener = function(nativeEventName, cb) {
		if (nativeEventName !== eventNameToWrap || !this._eventMap || !this._eventMap[eventNameToWrap]) return nativeRemoveEventListener.apply(this, arguments);
		if (!this._eventMap[eventNameToWrap].has(cb)) return nativeRemoveEventListener.apply(this, arguments);
		const unwrappedCb = this._eventMap[eventNameToWrap].get(cb);
		this._eventMap[eventNameToWrap].delete(cb);
		if (this._eventMap[eventNameToWrap].size === 0) delete this._eventMap[eventNameToWrap];
		if (Object.keys(this._eventMap).length === 0) delete this._eventMap;
		return nativeRemoveEventListener.apply(this, [nativeEventName, unwrappedCb]);
	};
	Object.defineProperty(proto, "on" + eventNameToWrap, {
		get() {
			return this["_on" + eventNameToWrap];
		},
		set(cb) {
			if (this["_on" + eventNameToWrap]) {
				this.removeEventListener(eventNameToWrap, this["_on" + eventNameToWrap]);
				delete this["_on" + eventNameToWrap];
			}
			if (cb) this.addEventListener(eventNameToWrap, this["_on" + eventNameToWrap] = cb);
		},
		enumerable: true,
		configurable: true
	});
}
function disableLog(bool) {
	if (typeof bool !== "boolean") return /* @__PURE__ */ new Error("Argument type: " + typeof bool + ". Please use a boolean.");
	logDisabled_ = bool;
	return bool ? "adapter.js logging disabled" : "adapter.js logging enabled";
}
/**
* Disable or enable deprecation warnings
* @param {!boolean} bool set to true to disable warnings.
*/
function disableWarnings(bool) {
	if (typeof bool !== "boolean") return /* @__PURE__ */ new Error("Argument type: " + typeof bool + ". Please use a boolean.");
	deprecationWarnings_ = !bool;
	return "adapter.js deprecation warnings " + (bool ? "disabled" : "enabled");
}
function log$5() {
	if (typeof window === "object") {
		if (logDisabled_) return;
		if (typeof console !== "undefined" && typeof console.log === "function") console.log.apply(console, arguments);
	}
}
/**
* Shows a deprecation warning suggesting the modern and spec-compatible API.
*/
function deprecated(oldMethod, newMethod) {
	if (!deprecationWarnings_) return;
	console.warn(oldMethod + " is deprecated, please use " + newMethod + " instead.");
}
/**
* Browser detector.
*
* @return {object} result containing browser and version
*     properties.
*/
function detectBrowser(window) {
	const result = {
		browser: null,
		version: null
	};
	if (typeof window === "undefined" || !window.navigator || !window.navigator.userAgent) {
		result.browser = "Not a browser.";
		return result;
	}
	const navigator = window.navigator;
	if (navigator.userAgentData && navigator.userAgentData.brands) {
		const chromium = navigator.userAgentData.brands.find((brand) => {
			return brand.brand === "Chromium";
		});
		if (chromium) {
			const version = parseInt(chromium.version, 10);
			if (version >= 90) return {
				browser: "chrome",
				version
			};
		}
	}
	if (navigator.mozGetUserMedia) {
		result.browser = "firefox";
		result.version = parseInt(extractVersion(navigator.userAgent, /Firefox\/(\d+)\./, 1));
	} else if (navigator.webkitGetUserMedia || window.isSecureContext === false && window.webkitRTCPeerConnection) {
		result.browser = "chrome";
		result.version = parseInt(extractVersion(navigator.userAgent, /Chrom(e|ium)\/(\d+)\./, 2)) || null;
	} else if (window.RTCPeerConnection && navigator.userAgent.match(/AppleWebKit\/(\d+)\./)) {
		result.browser = "safari";
		result.version = parseInt(extractVersion(navigator.userAgent, /AppleWebKit\/(\d+)\./, 1));
		result.supportsUnifiedPlan = window.RTCRtpTransceiver && "currentDirection" in window.RTCRtpTransceiver.prototype;
		result._safariVersion = extractVersion(navigator.userAgent, /Version\/(\d+(\.?\d+))/, 1);
	} else {
		result.browser = "Not a supported browser.";
		return result;
	}
	return result;
}
/**
* Checks if something is an object.
*
* @param {*} val The something you want to check.
* @return true if val is an object, false otherwise.
*/
function isObject$1(val) {
	return Object.prototype.toString.call(val) === "[object Object]";
}
/**
* Remove all empty objects and undefined values
* from a nested object -- an enhanced and vanilla version
* of Lodash's `compact`.
*/
function compactObject(data) {
	if (!isObject$1(data)) return data;
	return Object.keys(data).reduce(function(accumulator, key) {
		const isObj = isObject$1(data[key]);
		const value = isObj ? compactObject(data[key]) : data[key];
		const isEmptyObject = isObj && !Object.keys(value).length;
		if (value === void 0 || isEmptyObject) return accumulator;
		return Object.assign(accumulator, { [key]: value });
	}, {});
}
function walkStats(stats, base, resultSet) {
	if (!base || resultSet.has(base.id)) return;
	resultSet.set(base.id, base);
	Object.keys(base).forEach((name) => {
		if (name.endsWith("Id")) walkStats(stats, stats.get(base[name]), resultSet);
		else if (name.endsWith("Ids")) base[name].forEach((id) => {
			walkStats(stats, stats.get(id), resultSet);
		});
	});
}
function filterStats(result, track, outbound) {
	const streamStatsType = outbound ? "outbound-rtp" : "inbound-rtp";
	const filteredResult = /* @__PURE__ */ new Map();
	if (track === null) return filteredResult;
	const trackStats = [];
	result.forEach((value) => {
		if (value.type === "track" && value.trackIdentifier === track.id) trackStats.push(value);
	});
	trackStats.forEach((trackStat) => {
		result.forEach((stats) => {
			if (stats.type === streamStatsType && stats.trackId === trackStat.id) walkStats(result, stats, filteredResult);
		});
	});
	return filteredResult;
}
var logging = log$5;
function shimGetUserMedia$2(window, browserDetails) {
	if (browserDetails.version >= 64) return;
	const navigator = window && window.navigator;
	if (!navigator.mediaDevices) return;
	const constraintsToChrome_ = function(c) {
		if (typeof c !== "object" || c.mandatory || c.optional) return c;
		const cc = {};
		Object.keys(c).forEach((key) => {
			if (key === "require" || key === "advanced" || key === "mediaSource") return;
			const r = typeof c[key] === "object" ? c[key] : { ideal: c[key] };
			if (r.exact !== void 0 && typeof r.exact === "number") r.min = r.max = r.exact;
			const oldname_ = function(prefix, name) {
				if (prefix) return prefix + name.charAt(0).toUpperCase() + name.slice(1);
				return name === "deviceId" ? "sourceId" : name;
			};
			if (r.ideal !== void 0) {
				cc.optional = cc.optional || [];
				let oc = {};
				if (typeof r.ideal === "number") {
					oc[oldname_("min", key)] = r.ideal;
					cc.optional.push(oc);
					oc = {};
					oc[oldname_("max", key)] = r.ideal;
					cc.optional.push(oc);
				} else {
					oc[oldname_("", key)] = r.ideal;
					cc.optional.push(oc);
				}
			}
			if (r.exact !== void 0 && typeof r.exact !== "number") {
				cc.mandatory = cc.mandatory || {};
				cc.mandatory[oldname_("", key)] = r.exact;
			} else ["min", "max"].forEach((mix) => {
				if (r[mix] !== void 0) {
					cc.mandatory = cc.mandatory || {};
					cc.mandatory[oldname_(mix, key)] = r[mix];
				}
			});
		});
		if (c.advanced) cc.optional = (cc.optional || []).concat(c.advanced);
		return cc;
	};
	const shimConstraints_ = function(constraints, func) {
		if (browserDetails.version >= 61) return func(constraints);
		constraints = JSON.parse(JSON.stringify(constraints));
		if (constraints && typeof constraints.audio === "object") {
			const remap = function(obj, a, b) {
				if (a in obj && !(b in obj)) {
					obj[b] = obj[a];
					delete obj[a];
				}
			};
			constraints = JSON.parse(JSON.stringify(constraints));
			remap(constraints.audio, "autoGainControl", "googAutoGainControl");
			remap(constraints.audio, "noiseSuppression", "googNoiseSuppression");
			constraints.audio = constraintsToChrome_(constraints.audio);
		}
		if (constraints && typeof constraints.video === "object") {
			let face = constraints.video.facingMode;
			face = face && (typeof face === "object" ? face : { ideal: face });
			const getSupportedFacingModeLies = browserDetails.version < 66;
			if (face && (face.exact === "user" || face.exact === "environment" || face.ideal === "user" || face.ideal === "environment") && !(navigator.mediaDevices.getSupportedConstraints && navigator.mediaDevices.getSupportedConstraints().facingMode && !getSupportedFacingModeLies)) {
				delete constraints.video.facingMode;
				let matches;
				if (face.exact === "environment" || face.ideal === "environment") matches = ["back", "rear"];
				else if (face.exact === "user" || face.ideal === "user") matches = ["front"];
				if (matches) return navigator.mediaDevices.enumerateDevices().then((devices) => {
					devices = devices.filter((d) => d.kind === "videoinput");
					let dev = devices.find((d) => matches.some((match) => d.label.toLowerCase().includes(match)));
					if (!dev && devices.length && matches.includes("back")) dev = devices[devices.length - 1];
					if (dev) constraints.video.deviceId = face.exact ? { exact: dev.deviceId } : { ideal: dev.deviceId };
					constraints.video = constraintsToChrome_(constraints.video);
					logging("chrome: " + JSON.stringify(constraints));
					return func(constraints);
				});
			}
			constraints.video = constraintsToChrome_(constraints.video);
		}
		logging("chrome: " + JSON.stringify(constraints));
		return func(constraints);
	};
	const shimError_ = function(e) {
		if (browserDetails.version >= 64) return e;
		return {
			name: {
				PermissionDeniedError: "NotAllowedError",
				PermissionDismissedError: "NotAllowedError",
				InvalidStateError: "NotAllowedError",
				DevicesNotFoundError: "NotFoundError",
				ConstraintNotSatisfiedError: "OverconstrainedError",
				TrackStartError: "NotReadableError",
				MediaDeviceFailedDueToShutdown: "NotAllowedError",
				MediaDeviceKillSwitchOn: "NotAllowedError",
				TabCaptureError: "AbortError",
				ScreenCaptureError: "AbortError",
				DeviceCaptureError: "AbortError"
			}[e.name] || e.name,
			message: e.message,
			constraint: e.constraint || e.constraintName,
			toString() {
				return this.name + (this.message && ": ") + this.message;
			}
		};
	};
	const getUserMedia_ = function(constraints, onSuccess, onError) {
		shimConstraints_(constraints, (c) => {
			navigator.webkitGetUserMedia(c, onSuccess, (e) => {
				if (onError) onError(shimError_(e));
			});
		});
	};
	navigator.getUserMedia = getUserMedia_.bind(navigator);
	if (navigator.mediaDevices.getUserMedia) {
		const origGetUserMedia = navigator.mediaDevices.getUserMedia.bind(navigator.mediaDevices);
		navigator.mediaDevices.getUserMedia = function(cs) {
			return shimConstraints_(cs, (c) => origGetUserMedia(c).then((stream) => {
				if (c.audio && !stream.getAudioTracks().length || c.video && !stream.getVideoTracks().length) {
					stream.getTracks().forEach((track) => {
						track.stop();
					});
					throw new DOMException("", "NotFoundError");
				}
				return stream;
			}, (e) => Promise.reject(shimError_(e))));
		};
	}
}
function shimMediaStream(window) {
	window.MediaStream = window.MediaStream || window.webkitMediaStream;
}
function shimOnTrack$1(window, browserDetails) {
	if (browserDetails.version > 102) return;
	if (typeof window === "object" && window.RTCPeerConnection && !("ontrack" in window.RTCPeerConnection.prototype)) {
		Object.defineProperty(window.RTCPeerConnection.prototype, "ontrack", {
			get() {
				return this._ontrack;
			},
			set(f) {
				if (this._ontrack) this.removeEventListener("track", this._ontrack);
				this.addEventListener("track", this._ontrack = f);
			},
			enumerable: true,
			configurable: true
		});
		const origSetRemoteDescription = window.RTCPeerConnection.prototype.setRemoteDescription;
		window.RTCPeerConnection.prototype.setRemoteDescription = function setRemoteDescription() {
			if (!this._ontrackpoly) {
				this._ontrackpoly = (e) => {
					e.stream.addEventListener("addtrack", (te) => {
						let receiver;
						if (window.RTCPeerConnection.prototype.getReceivers) receiver = this.getReceivers().find((r) => r.track && r.track.id === te.track.id);
						else receiver = { track: te.track };
						const event = new Event("track");
						event.track = te.track;
						event.receiver = receiver;
						event.transceiver = { receiver };
						event.streams = [e.stream];
						this.dispatchEvent(event);
					});
					e.stream.getTracks().forEach((track) => {
						let receiver;
						if (window.RTCPeerConnection.prototype.getReceivers) receiver = this.getReceivers().find((r) => r.track && r.track.id === track.id);
						else receiver = { track };
						const event = new Event("track");
						event.track = track;
						event.receiver = receiver;
						event.transceiver = { receiver };
						event.streams = [e.stream];
						this.dispatchEvent(event);
					});
				};
				this.addEventListener("addstream", this._ontrackpoly);
			}
			return origSetRemoteDescription.apply(this, arguments);
		};
	} else wrapPeerConnectionEvent(window, "track", (e) => {
		if (!e.transceiver) Object.defineProperty(e, "transceiver", { value: { receiver: e.receiver } });
		return e;
	});
}
function shimGetSendersWithDtmf(window) {
	if (typeof window === "object" && window.RTCPeerConnection && !("getSenders" in window.RTCPeerConnection.prototype) && "createDTMFSender" in window.RTCPeerConnection.prototype) {
		const shimSenderWithDtmf = function(pc, track) {
			return {
				track,
				get dtmf() {
					if (this._dtmf === void 0) {
						if (track.kind === "audio") this._dtmf = pc.createDTMFSender(track);
						else this._dtmf = null;
					}
					return this._dtmf;
				},
				_pc: pc
			};
		};
		if (!window.RTCPeerConnection.prototype.getSenders) {
			window.RTCPeerConnection.prototype.getSenders = function getSenders() {
				this._senders = this._senders || [];
				return this._senders.slice();
			};
			const origAddTrack = window.RTCPeerConnection.prototype.addTrack;
			window.RTCPeerConnection.prototype.addTrack = function addTrack(track, stream) {
				let sender = origAddTrack.apply(this, arguments);
				if (!sender) {
					sender = shimSenderWithDtmf(this, track);
					this._senders.push(sender);
				}
				return sender;
			};
			const origRemoveTrack = window.RTCPeerConnection.prototype.removeTrack;
			window.RTCPeerConnection.prototype.removeTrack = function removeTrack(sender) {
				origRemoveTrack.apply(this, arguments);
				const idx = this._senders.indexOf(sender);
				if (idx !== -1) this._senders.splice(idx, 1);
			};
		}
		const origAddStream = window.RTCPeerConnection.prototype.addStream;
		window.RTCPeerConnection.prototype.addStream = function addStream(stream) {
			this._senders = this._senders || [];
			origAddStream.apply(this, [stream]);
			stream.getTracks().forEach((track) => {
				this._senders.push(shimSenderWithDtmf(this, track));
			});
		};
		const origRemoveStream = window.RTCPeerConnection.prototype.removeStream;
		window.RTCPeerConnection.prototype.removeStream = function removeStream(stream) {
			this._senders = this._senders || [];
			origRemoveStream.apply(this, [stream]);
			stream.getTracks().forEach((track) => {
				const sender = this._senders.find((s) => s.track === track);
				if (sender) this._senders.splice(this._senders.indexOf(sender), 1);
			});
		};
	} else if (typeof window === "object" && window.RTCPeerConnection && "getSenders" in window.RTCPeerConnection.prototype && "createDTMFSender" in window.RTCPeerConnection.prototype && window.RTCRtpSender && !("dtmf" in window.RTCRtpSender.prototype)) {
		const origGetSenders = window.RTCPeerConnection.prototype.getSenders;
		window.RTCPeerConnection.prototype.getSenders = function getSenders() {
			const senders = origGetSenders.apply(this, []);
			senders.forEach((sender) => sender._pc = this);
			return senders;
		};
		Object.defineProperty(window.RTCRtpSender.prototype, "dtmf", { get() {
			if (this._dtmf === void 0) {
				if (this.track.kind === "audio") this._dtmf = this._pc.createDTMFSender(this.track);
				else this._dtmf = null;
			}
			return this._dtmf;
		} });
	}
}
function shimSenderReceiverGetStats(window, browserDetails) {
	if (browserDetails.version >= 67) return;
	if (!(typeof window === "object" && window.RTCPeerConnection && window.RTCRtpSender && window.RTCRtpReceiver)) return;
	if (!("getStats" in window.RTCRtpSender.prototype)) {
		const origGetSenders = window.RTCPeerConnection.prototype.getSenders;
		if (origGetSenders) window.RTCPeerConnection.prototype.getSenders = function getSenders() {
			const senders = origGetSenders.apply(this, []);
			senders.forEach((sender) => sender._pc = this);
			return senders;
		};
		const origAddTrack = window.RTCPeerConnection.prototype.addTrack;
		if (origAddTrack) window.RTCPeerConnection.prototype.addTrack = function addTrack() {
			const sender = origAddTrack.apply(this, arguments);
			sender._pc = this;
			return sender;
		};
		window.RTCRtpSender.prototype.getStats = function getStats() {
			const sender = this;
			return this._pc.getStats().then((result) => filterStats(result, sender.track, true));
		};
	}
	if (!("getStats" in window.RTCRtpReceiver.prototype)) {
		const origGetReceivers = window.RTCPeerConnection.prototype.getReceivers;
		if (origGetReceivers) window.RTCPeerConnection.prototype.getReceivers = function getReceivers() {
			const receivers = origGetReceivers.apply(this, []);
			receivers.forEach((receiver) => receiver._pc = this);
			return receivers;
		};
		wrapPeerConnectionEvent(window, "track", (e) => {
			e.receiver._pc = e.srcElement;
			return e;
		});
		window.RTCRtpReceiver.prototype.getStats = function getStats() {
			const receiver = this;
			return this._pc.getStats().then((result) => filterStats(result, receiver.track, false));
		};
	}
	if (!("getStats" in window.RTCRtpSender.prototype && "getStats" in window.RTCRtpReceiver.prototype)) return;
	const origGetStats = window.RTCPeerConnection.prototype.getStats;
	window.RTCPeerConnection.prototype.getStats = function getStats() {
		if (arguments.length > 0 && arguments[0] instanceof window.MediaStreamTrack) {
			const track = arguments[0];
			let sender;
			let receiver;
			let err;
			this.getSenders().forEach((s) => {
				if (s.track === track) {
					if (sender) err = true;
					else sender = s;
				}
			});
			this.getReceivers().forEach((r) => {
				if (r.track === track) {
					if (receiver) err = true;
					else receiver = r;
				}
				return r.track === track;
			});
			if (err || sender && receiver) return Promise.reject(new DOMException("There are more than one sender or receiver for the track.", "InvalidAccessError"));
			else if (sender) return sender.getStats();
			else if (receiver) return receiver.getStats();
			return Promise.reject(new DOMException("There is no sender or receiver for the track.", "InvalidAccessError"));
		}
		return origGetStats.apply(this, arguments);
	};
}
function shimAddTrackRemoveTrackWithNative(window) {
	window.RTCPeerConnection.prototype.getLocalStreams = function getLocalStreams() {
		this._shimmedLocalStreams = this._shimmedLocalStreams || {};
		return Object.keys(this._shimmedLocalStreams).map((streamId) => this._shimmedLocalStreams[streamId][0]);
	};
	const origAddTrack = window.RTCPeerConnection.prototype.addTrack;
	window.RTCPeerConnection.prototype.addTrack = function addTrack(track, stream) {
		if (!stream) return origAddTrack.apply(this, arguments);
		this._shimmedLocalStreams = this._shimmedLocalStreams || {};
		const sender = origAddTrack.apply(this, arguments);
		if (!this._shimmedLocalStreams[stream.id]) this._shimmedLocalStreams[stream.id] = [stream, sender];
		else if (this._shimmedLocalStreams[stream.id].indexOf(sender) === -1) this._shimmedLocalStreams[stream.id].push(sender);
		return sender;
	};
	const origAddStream = window.RTCPeerConnection.prototype.addStream;
	window.RTCPeerConnection.prototype.addStream = function addStream(stream) {
		this._shimmedLocalStreams = this._shimmedLocalStreams || {};
		stream.getTracks().forEach((track) => {
			if (this.getSenders().find((s) => s.track === track)) throw new DOMException("Track already exists.", "InvalidAccessError");
		});
		const existingSenders = this.getSenders();
		origAddStream.apply(this, arguments);
		const newSenders = this.getSenders().filter((newSender) => existingSenders.indexOf(newSender) === -1);
		this._shimmedLocalStreams[stream.id] = [stream].concat(newSenders);
	};
	const origRemoveStream = window.RTCPeerConnection.prototype.removeStream;
	window.RTCPeerConnection.prototype.removeStream = function removeStream(stream) {
		this._shimmedLocalStreams = this._shimmedLocalStreams || {};
		delete this._shimmedLocalStreams[stream.id];
		return origRemoveStream.apply(this, arguments);
	};
	const origRemoveTrack = window.RTCPeerConnection.prototype.removeTrack;
	window.RTCPeerConnection.prototype.removeTrack = function removeTrack(sender) {
		this._shimmedLocalStreams = this._shimmedLocalStreams || {};
		if (sender) Object.keys(this._shimmedLocalStreams).forEach((streamId) => {
			const idx = this._shimmedLocalStreams[streamId].indexOf(sender);
			if (idx !== -1) this._shimmedLocalStreams[streamId].splice(idx, 1);
			if (this._shimmedLocalStreams[streamId].length === 1) delete this._shimmedLocalStreams[streamId];
		});
		return origRemoveTrack.apply(this, arguments);
	};
}
function shimAddTrackRemoveTrack(window, browserDetails) {
	if (!window.RTCPeerConnection) return;
	if (window.RTCPeerConnection.prototype.addTrack && browserDetails.version >= 65) return shimAddTrackRemoveTrackWithNative(window);
	const origGetLocalStreams = window.RTCPeerConnection.prototype.getLocalStreams;
	window.RTCPeerConnection.prototype.getLocalStreams = function getLocalStreams() {
		const nativeStreams = origGetLocalStreams.apply(this);
		this._reverseStreams = this._reverseStreams || {};
		return nativeStreams.map((stream) => this._reverseStreams[stream.id]);
	};
	const origAddStream = window.RTCPeerConnection.prototype.addStream;
	window.RTCPeerConnection.prototype.addStream = function addStream(stream) {
		this._streams = this._streams || {};
		this._reverseStreams = this._reverseStreams || {};
		stream.getTracks().forEach((track) => {
			if (this.getSenders().find((s) => s.track === track)) throw new DOMException("Track already exists.", "InvalidAccessError");
		});
		if (!this._reverseStreams[stream.id]) {
			const newStream = new window.MediaStream(stream.getTracks());
			this._streams[stream.id] = newStream;
			this._reverseStreams[newStream.id] = stream;
			stream = newStream;
		}
		origAddStream.apply(this, [stream]);
	};
	const origRemoveStream = window.RTCPeerConnection.prototype.removeStream;
	window.RTCPeerConnection.prototype.removeStream = function removeStream(stream) {
		this._streams = this._streams || {};
		this._reverseStreams = this._reverseStreams || {};
		origRemoveStream.apply(this, [this._streams[stream.id] || stream]);
		delete this._reverseStreams[this._streams[stream.id] ? this._streams[stream.id].id : stream.id];
		delete this._streams[stream.id];
	};
	window.RTCPeerConnection.prototype.addTrack = function addTrack(track, stream) {
		if (this.signalingState === "closed") throw new DOMException("The RTCPeerConnection's signalingState is 'closed'.", "InvalidStateError");
		const streams = [].slice.call(arguments, 1);
		if (streams.length !== 1 || !streams[0].getTracks().find((t) => t === track)) throw new DOMException("The adapter.js addTrack polyfill only supports a single  stream which is associated with the specified track.", "NotSupportedError");
		if (this.getSenders().find((s) => s.track === track)) throw new DOMException("Track already exists.", "InvalidAccessError");
		this._streams = this._streams || {};
		this._reverseStreams = this._reverseStreams || {};
		const oldStream = this._streams[stream.id];
		if (oldStream) {
			oldStream.addTrack(track);
			Promise.resolve().then(() => {
				this.dispatchEvent(new Event("negotiationneeded"));
			});
		} else {
			const newStream = new window.MediaStream([track]);
			this._streams[stream.id] = newStream;
			this._reverseStreams[newStream.id] = stream;
			this.addStream(newStream);
		}
		return this.getSenders().find((s) => s.track === track);
	};
	function replaceInternalStreamId(pc, description) {
		let sdp = description.sdp;
		Object.keys(pc._reverseStreams || []).forEach((internalId) => {
			const externalStream = pc._reverseStreams[internalId];
			const internalStream = pc._streams[externalStream.id];
			sdp = sdp.replace(new RegExp(internalStream.id, "g"), externalStream.id);
		});
		return new RTCSessionDescription({
			type: description.type,
			sdp
		});
	}
	function replaceExternalStreamId(pc, description) {
		let sdp = description.sdp;
		Object.keys(pc._reverseStreams || []).forEach((internalId) => {
			const externalStream = pc._reverseStreams[internalId];
			const internalStream = pc._streams[externalStream.id];
			sdp = sdp.replace(new RegExp(externalStream.id, "g"), internalStream.id);
		});
		return new RTCSessionDescription({
			type: description.type,
			sdp
		});
	}
	["createOffer", "createAnswer"].forEach(function(method) {
		const nativeMethod = window.RTCPeerConnection.prototype[method];
		const methodObj = { [method]() {
			const args = arguments;
			if (arguments.length && typeof arguments[0] === "function") return nativeMethod.apply(this, [
				(description) => {
					const desc = replaceInternalStreamId(this, description);
					args[0].apply(null, [desc]);
				},
				(err) => {
					if (args[1]) args[1].apply(null, err);
				},
				arguments[2]
			]);
			return nativeMethod.apply(this, arguments).then((description) => replaceInternalStreamId(this, description));
		} };
		window.RTCPeerConnection.prototype[method] = methodObj[method];
	});
	const origSetLocalDescription = window.RTCPeerConnection.prototype.setLocalDescription;
	window.RTCPeerConnection.prototype.setLocalDescription = function setLocalDescription() {
		if (!arguments.length || !arguments[0].type) return origSetLocalDescription.apply(this, arguments);
		arguments[0] = replaceExternalStreamId(this, arguments[0]);
		return origSetLocalDescription.apply(this, arguments);
	};
	const origLocalDescription = Object.getOwnPropertyDescriptor(window.RTCPeerConnection.prototype, "localDescription");
	Object.defineProperty(window.RTCPeerConnection.prototype, "localDescription", { get() {
		const description = origLocalDescription.get.apply(this);
		if (description.type === "") return description;
		return replaceInternalStreamId(this, description);
	} });
	window.RTCPeerConnection.prototype.removeTrack = function removeTrack(sender) {
		if (this.signalingState === "closed") throw new DOMException("The RTCPeerConnection's signalingState is 'closed'.", "InvalidStateError");
		if (!sender._pc) throw new DOMException("Argument 1 of RTCPeerConnection.removeTrack does not implement interface RTCRtpSender.", "TypeError");
		if (!(sender._pc === this)) throw new DOMException("Sender was not created by this connection.", "InvalidAccessError");
		this._streams = this._streams || {};
		let stream;
		Object.keys(this._streams).forEach((streamid) => {
			if (this._streams[streamid].getTracks().find((track) => sender.track === track)) stream = this._streams[streamid];
		});
		if (stream) {
			if (stream.getTracks().length === 1) this.removeStream(this._reverseStreams[stream.id]);
			else stream.removeTrack(sender.track);
			this.dispatchEvent(new Event("negotiationneeded"));
		}
	};
}
function shimPeerConnection$1(window, browserDetails) {
	if (!window.RTCPeerConnection && window.webkitRTCPeerConnection) window.RTCPeerConnection = window.webkitRTCPeerConnection;
	if (!window.RTCPeerConnection) return;
	if (browserDetails.version < 53) [
		"setLocalDescription",
		"setRemoteDescription",
		"addIceCandidate"
	].forEach(function(method) {
		const nativeMethod = window.RTCPeerConnection.prototype[method];
		const methodObj = { [method]() {
			arguments[0] = new (method === "addIceCandidate" ? window.RTCIceCandidate : window.RTCSessionDescription)(arguments[0]);
			return nativeMethod.apply(this, arguments);
		} };
		window.RTCPeerConnection.prototype[method] = methodObj[method];
	});
}
function fixNegotiationNeeded(window, browserDetails) {
	if (browserDetails.version > 102) return;
	wrapPeerConnectionEvent(window, "negotiationneeded", (e) => {
		const pc = e.target;
		if (browserDetails.version < 72 || pc.getConfiguration && pc.getConfiguration().sdpSemantics === "plan-b") {
			if (pc.signalingState !== "stable") return;
		}
		return e;
	});
}
var chromeShim = /*#__PURE__*/ Object.freeze({
	__proto__: null,
	fixNegotiationNeeded,
	shimAddTrackRemoveTrack,
	shimAddTrackRemoveTrackWithNative,
	shimGetSendersWithDtmf,
	shimGetUserMedia: shimGetUserMedia$2,
	shimMediaStream,
	shimOnTrack: shimOnTrack$1,
	shimPeerConnection: shimPeerConnection$1,
	shimSenderReceiverGetStats
});
function shimGetUserMedia$1(window, browserDetails) {
	const navigator = window && window.navigator;
	if (!navigator.mediaDevices) return;
	const MediaStreamTrack = window && window.MediaStreamTrack;
	navigator.getUserMedia = function(constraints, onSuccess, onError) {
		deprecated("navigator.getUserMedia", "navigator.mediaDevices.getUserMedia");
		navigator.mediaDevices.getUserMedia(constraints).then(onSuccess, onError);
	};
	if (!(browserDetails.version > 55 && "autoGainControl" in navigator.mediaDevices.getSupportedConstraints())) {
		const remap = function(obj, a, b) {
			if (a in obj && !(b in obj)) {
				obj[b] = obj[a];
				delete obj[a];
			}
		};
		const nativeGetUserMedia = navigator.mediaDevices.getUserMedia.bind(navigator.mediaDevices);
		navigator.mediaDevices.getUserMedia = function(c) {
			if (typeof c === "object" && typeof c.audio === "object") {
				c = JSON.parse(JSON.stringify(c));
				remap(c.audio, "autoGainControl", "mozAutoGainControl");
				remap(c.audio, "noiseSuppression", "mozNoiseSuppression");
			}
			return nativeGetUserMedia(c);
		};
		if (MediaStreamTrack && MediaStreamTrack.prototype.getSettings) {
			const nativeGetSettings = MediaStreamTrack.prototype.getSettings;
			MediaStreamTrack.prototype.getSettings = function() {
				const obj = nativeGetSettings.apply(this, arguments);
				remap(obj, "mozAutoGainControl", "autoGainControl");
				remap(obj, "mozNoiseSuppression", "noiseSuppression");
				return obj;
			};
		}
		if (MediaStreamTrack && MediaStreamTrack.prototype.applyConstraints) {
			const nativeApplyConstraints = MediaStreamTrack.prototype.applyConstraints;
			MediaStreamTrack.prototype.applyConstraints = function(c) {
				if (this.kind === "audio" && typeof c === "object") {
					c = JSON.parse(JSON.stringify(c));
					remap(c, "autoGainControl", "mozAutoGainControl");
					remap(c, "noiseSuppression", "mozNoiseSuppression");
				}
				return nativeApplyConstraints.apply(this, [c]);
			};
		}
	}
}
function shimGetDisplayMedia(window, preferredMediaSource) {
	if (!window.navigator.mediaDevices) return;
	if (window.navigator.mediaDevices && "getDisplayMedia" in window.navigator.mediaDevices) return;
	window.navigator.mediaDevices.getDisplayMedia = function getDisplayMedia(constraints) {
		if (!(constraints && constraints.video)) {
			const err = new DOMException("getDisplayMedia without video constraints is undefined");
			err.name = "NotFoundError";
			err.code = 8;
			return Promise.reject(err);
		}
		if (constraints.video === true) constraints.video = { mediaSource: preferredMediaSource };
		else constraints.video.mediaSource = preferredMediaSource;
		return window.navigator.mediaDevices.getUserMedia(constraints);
	};
}
function shimOnTrack(window) {
	if (typeof window === "object" && window.RTCTrackEvent && "receiver" in window.RTCTrackEvent.prototype && !("transceiver" in window.RTCTrackEvent.prototype)) Object.defineProperty(window.RTCTrackEvent.prototype, "transceiver", { get() {
		return { receiver: this.receiver };
	} });
}
function shimPeerConnection(window, browserDetails) {
	if (typeof window !== "object" || !(window.RTCPeerConnection || window.mozRTCPeerConnection)) return;
	if (!window.RTCPeerConnection && window.mozRTCPeerConnection) window.RTCPeerConnection = window.mozRTCPeerConnection;
	if (browserDetails.version < 53) [
		"setLocalDescription",
		"setRemoteDescription",
		"addIceCandidate"
	].forEach(function(method) {
		const nativeMethod = window.RTCPeerConnection.prototype[method];
		const methodObj = { [method]() {
			arguments[0] = new (method === "addIceCandidate" ? window.RTCIceCandidate : window.RTCSessionDescription)(arguments[0]);
			return nativeMethod.apply(this, arguments);
		} };
		window.RTCPeerConnection.prototype[method] = methodObj[method];
	});
}
function shimGetStats(window, browserDetails) {
	if (typeof window !== "object" || !(window.RTCPeerConnection || window.mozRTCPeerConnection)) return;
	if (browserDetails.version >= 151) return;
	const modernStatsTypes = {
		inboundrtp: "inbound-rtp",
		outboundrtp: "outbound-rtp",
		candidatepair: "candidate-pair",
		localcandidate: "local-candidate",
		remotecandidate: "remote-candidate"
	};
	const nativeGetStats = window.RTCPeerConnection.prototype.getStats;
	window.RTCPeerConnection.prototype.getStats = function getStats() {
		const _arguments = Array.prototype.slice.call(arguments), selector = _arguments[0], onSucc = _arguments[1], onErr = _arguments[2];
		if (this.signalingState === "closed") return Promise.resolve(/* @__PURE__ */ new Map());
		return nativeGetStats.apply(this, [selector || null]).then((stats) => {
			if (browserDetails.version < 53 && !onSucc) try {
				stats.forEach((stat) => {
					stat.type = modernStatsTypes[stat.type] || stat.type;
				});
			} catch (e) {
				if (e.name !== "TypeError") throw e;
				stats.forEach((stat, i) => {
					stats.set(i, Object.assign({}, stat, { type: modernStatsTypes[stat.type] || stat.type }));
				});
			}
			return stats;
		}).then(onSucc, onErr);
	};
}
function shimSenderGetStats(window) {
	if (!(typeof window === "object" && window.RTCPeerConnection && window.RTCRtpSender)) return;
	if (window.RTCRtpSender && "getStats" in window.RTCRtpSender.prototype) return;
	const origGetSenders = window.RTCPeerConnection.prototype.getSenders;
	if (origGetSenders) window.RTCPeerConnection.prototype.getSenders = function getSenders() {
		const senders = origGetSenders.apply(this, []);
		senders.forEach((sender) => sender._pc = this);
		return senders;
	};
	const origAddTrack = window.RTCPeerConnection.prototype.addTrack;
	if (origAddTrack) window.RTCPeerConnection.prototype.addTrack = function addTrack() {
		const sender = origAddTrack.apply(this, arguments);
		sender._pc = this;
		return sender;
	};
	window.RTCRtpSender.prototype.getStats = function getStats() {
		return this.track ? this._pc.getStats(this.track) : Promise.resolve(/* @__PURE__ */ new Map());
	};
}
function shimReceiverGetStats(window) {
	if (!(typeof window === "object" && window.RTCPeerConnection && window.RTCRtpSender)) return;
	if (window.RTCRtpSender && "getStats" in window.RTCRtpReceiver.prototype) return;
	const origGetReceivers = window.RTCPeerConnection.prototype.getReceivers;
	if (origGetReceivers) window.RTCPeerConnection.prototype.getReceivers = function getReceivers() {
		const receivers = origGetReceivers.apply(this, []);
		receivers.forEach((receiver) => receiver._pc = this);
		return receivers;
	};
	wrapPeerConnectionEvent(window, "track", (e) => {
		e.receiver._pc = e.srcElement;
		return e;
	});
	window.RTCRtpReceiver.prototype.getStats = function getStats() {
		return this._pc.getStats(this.track);
	};
}
function shimRemoveStream(window) {
	if (!window.RTCPeerConnection || "removeStream" in window.RTCPeerConnection.prototype) return;
	window.RTCPeerConnection.prototype.removeStream = function removeStream(stream) {
		deprecated("removeStream", "removeTrack");
		this.getSenders().forEach((sender) => {
			if (sender.track && stream.getTracks().includes(sender.track)) this.removeTrack(sender);
		});
	};
}
function shimRTCDataChannel(window) {
	if (window.DataChannel && !window.RTCDataChannel) window.RTCDataChannel = window.DataChannel;
}
function shimAddTransceiver(window, browserDetails) {
	if (!(typeof window === "object" && window.RTCPeerConnection)) return;
	if (browserDetails.version >= 110) return;
	const origAddTransceiver = window.RTCPeerConnection.prototype.addTransceiver;
	if (origAddTransceiver) window.RTCPeerConnection.prototype.addTransceiver = function addTransceiver() {
		this.setParametersPromises = [];
		let sendEncodings = arguments[1] && arguments[1].sendEncodings;
		if (sendEncodings === void 0) sendEncodings = [];
		sendEncodings = [...sendEncodings];
		const shouldPerformCheck = sendEncodings.length > 0;
		if (shouldPerformCheck) sendEncodings.forEach((encodingParam) => {
			if ("rid" in encodingParam) {
				if (!/^[a-z0-9]{0,16}$/i.test(encodingParam.rid)) throw new TypeError("Invalid RID value provided.");
			}
			if ("scaleResolutionDownBy" in encodingParam) {
				if (!(parseFloat(encodingParam.scaleResolutionDownBy) >= 1)) throw new RangeError("scale_resolution_down_by must be >= 1.0");
			}
			if ("maxFramerate" in encodingParam) {
				if (!(parseFloat(encodingParam.maxFramerate) >= 0)) throw new RangeError("max_framerate must be >= 0.0");
			}
		});
		const transceiver = origAddTransceiver.apply(this, arguments);
		if (shouldPerformCheck) {
			const sender = transceiver.sender;
			const params = sender.getParameters();
			if (!("encodings" in params) || params.encodings.length === 1 && Object.keys(params.encodings[0]).length === 0) {
				params.encodings = sendEncodings;
				sender.sendEncodings = sendEncodings;
				this.setParametersPromises.push(sender.setParameters(params).then(() => {
					delete sender.sendEncodings;
				}).catch(() => {
					delete sender.sendEncodings;
				}));
			}
		}
		return transceiver;
	};
}
function shimGetParameters(window, browserDetails) {
	if (!(typeof window === "object" && window.RTCRtpSender)) return;
	if (browserDetails.version >= 110) return;
	const origGetParameters = window.RTCRtpSender.prototype.getParameters;
	if (origGetParameters) window.RTCRtpSender.prototype.getParameters = function getParameters() {
		const params = origGetParameters.apply(this, arguments);
		if (!("encodings" in params)) params.encodings = [].concat(this.sendEncodings || [{}]);
		return params;
	};
}
function shimCreateOffer(window, browserDetails) {
	if (!(typeof window === "object" && window.RTCPeerConnection)) return;
	if (browserDetails.version >= 110) return;
	const origCreateOffer = window.RTCPeerConnection.prototype.createOffer;
	window.RTCPeerConnection.prototype.createOffer = function createOffer() {
		if (this.setParametersPromises && this.setParametersPromises.length) return Promise.all(this.setParametersPromises).then(() => {
			return origCreateOffer.apply(this, arguments);
		}).finally(() => {
			this.setParametersPromises = [];
		});
		return origCreateOffer.apply(this, arguments);
	};
}
function shimCreateAnswer(window, browserDetails) {
	if (!(typeof window === "object" && window.RTCPeerConnection)) return;
	if (browserDetails.version >= 110) return;
	const origCreateAnswer = window.RTCPeerConnection.prototype.createAnswer;
	window.RTCPeerConnection.prototype.createAnswer = function createAnswer() {
		if (this.setParametersPromises && this.setParametersPromises.length) return Promise.all(this.setParametersPromises).then(() => {
			return origCreateAnswer.apply(this, arguments);
		}).finally(() => {
			this.setParametersPromises = [];
		});
		return origCreateAnswer.apply(this, arguments);
	};
}
var firefoxShim = /*#__PURE__*/ Object.freeze({
	__proto__: null,
	shimAddTransceiver,
	shimCreateAnswer,
	shimCreateOffer,
	shimGetDisplayMedia,
	shimGetParameters,
	shimGetStats,
	shimGetUserMedia: shimGetUserMedia$1,
	shimOnTrack,
	shimPeerConnection,
	shimRTCDataChannel,
	shimReceiverGetStats,
	shimRemoveStream,
	shimSenderGetStats
});
function shimLocalStreamsAPI(window) {
	if (typeof window !== "object" || !window.RTCPeerConnection) return;
	if (!("getLocalStreams" in window.RTCPeerConnection.prototype)) window.RTCPeerConnection.prototype.getLocalStreams = function getLocalStreams() {
		if (!this._localStreams) this._localStreams = [];
		return this._localStreams;
	};
	if (!("addStream" in window.RTCPeerConnection.prototype)) {
		const _addTrack = window.RTCPeerConnection.prototype.addTrack;
		window.RTCPeerConnection.prototype.addStream = function addStream(stream) {
			if (!this._localStreams) this._localStreams = [];
			if (!this._localStreams.includes(stream)) this._localStreams.push(stream);
			stream.getAudioTracks().forEach((track) => _addTrack.call(this, track, stream));
			stream.getVideoTracks().forEach((track) => _addTrack.call(this, track, stream));
		};
		window.RTCPeerConnection.prototype.addTrack = function addTrack(track) {
			for (var _len = arguments.length, streams = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) streams[_key - 1] = arguments[_key];
			if (streams) streams.forEach((stream) => {
				if (!this._localStreams) this._localStreams = [stream];
				else if (!this._localStreams.includes(stream)) this._localStreams.push(stream);
			});
			return _addTrack.apply(this, arguments);
		};
	}
	if (!("removeStream" in window.RTCPeerConnection.prototype)) window.RTCPeerConnection.prototype.removeStream = function removeStream(stream) {
		if (!this._localStreams) this._localStreams = [];
		const index = this._localStreams.indexOf(stream);
		if (index === -1) return;
		this._localStreams.splice(index, 1);
		const tracks = stream.getTracks();
		this.getSenders().forEach((sender) => {
			if (tracks.includes(sender.track)) this.removeTrack(sender);
		});
	};
}
function shimRemoteStreamsAPI(window) {
	if (typeof window !== "object" || !window.RTCPeerConnection) return;
	if (!("getRemoteStreams" in window.RTCPeerConnection.prototype)) window.RTCPeerConnection.prototype.getRemoteStreams = function getRemoteStreams() {
		return this._remoteStreams ? this._remoteStreams : [];
	};
	if (!("onaddstream" in window.RTCPeerConnection.prototype)) {
		Object.defineProperty(window.RTCPeerConnection.prototype, "onaddstream", {
			get() {
				return this._onaddstream;
			},
			set(f) {
				if (this._onaddstream) {
					this.removeEventListener("addstream", this._onaddstream);
					this.removeEventListener("track", this._onaddstreampoly);
				}
				this.addEventListener("addstream", this._onaddstream = f);
				this.addEventListener("track", this._onaddstreampoly = (e) => {
					e.streams.forEach((stream) => {
						if (!this._remoteStreams) this._remoteStreams = [];
						if (this._remoteStreams.includes(stream)) return;
						this._remoteStreams.push(stream);
						const event = new Event("addstream");
						event.stream = stream;
						this.dispatchEvent(event);
					});
				});
			}
		});
		const origSetRemoteDescription = window.RTCPeerConnection.prototype.setRemoteDescription;
		window.RTCPeerConnection.prototype.setRemoteDescription = function setRemoteDescription() {
			const pc = this;
			if (!this._onaddstreampoly) this.addEventListener("track", this._onaddstreampoly = function(e) {
				e.streams.forEach((stream) => {
					if (!pc._remoteStreams) pc._remoteStreams = [];
					if (pc._remoteStreams.indexOf(stream) >= 0) return;
					pc._remoteStreams.push(stream);
					const event = new Event("addstream");
					event.stream = stream;
					pc.dispatchEvent(event);
				});
			});
			return origSetRemoteDescription.apply(pc, arguments);
		};
	}
}
function shimCallbacksAPI(window) {
	if (typeof window !== "object" || !window.RTCPeerConnection) return;
	const prototype = window.RTCPeerConnection.prototype;
	const origCreateOffer = prototype.createOffer;
	const origCreateAnswer = prototype.createAnswer;
	const setLocalDescription = prototype.setLocalDescription;
	const setRemoteDescription = prototype.setRemoteDescription;
	const addIceCandidate = prototype.addIceCandidate;
	prototype.createOffer = function createOffer(successCallback, failureCallback) {
		const options = arguments.length >= 2 ? arguments[2] : arguments[0];
		const promise = origCreateOffer.apply(this, [options]);
		if (!failureCallback) return promise;
		promise.then(successCallback, failureCallback);
		return Promise.resolve();
	};
	prototype.createAnswer = function createAnswer(successCallback, failureCallback) {
		const options = arguments.length >= 2 ? arguments[2] : arguments[0];
		const promise = origCreateAnswer.apply(this, [options]);
		if (!failureCallback) return promise;
		promise.then(successCallback, failureCallback);
		return Promise.resolve();
	};
	let withCallback = function(description, successCallback, failureCallback) {
		const promise = setLocalDescription.apply(this, [description]);
		if (!failureCallback) return promise;
		promise.then(successCallback, failureCallback);
		return Promise.resolve();
	};
	prototype.setLocalDescription = withCallback;
	withCallback = function(description, successCallback, failureCallback) {
		const promise = setRemoteDescription.apply(this, [description]);
		if (!failureCallback) return promise;
		promise.then(successCallback, failureCallback);
		return Promise.resolve();
	};
	prototype.setRemoteDescription = withCallback;
	withCallback = function(candidate, successCallback, failureCallback) {
		const promise = addIceCandidate.apply(this, [candidate]);
		if (!failureCallback) return promise;
		promise.then(successCallback, failureCallback);
		return Promise.resolve();
	};
	prototype.addIceCandidate = withCallback;
}
function shimGetUserMedia(window) {
	const navigator = window && window.navigator;
	if (navigator.mediaDevices && navigator.mediaDevices.getUserMedia) {
		const mediaDevices = navigator.mediaDevices;
		const _getUserMedia = mediaDevices.getUserMedia.bind(mediaDevices);
		navigator.mediaDevices.getUserMedia = (constraints) => {
			return _getUserMedia(shimConstraints(constraints));
		};
	}
	if (!navigator.getUserMedia && navigator.mediaDevices && navigator.mediaDevices.getUserMedia) navigator.getUserMedia = function getUserMedia(constraints, cb, errcb) {
		navigator.mediaDevices.getUserMedia(constraints).then(cb, errcb);
	}.bind(navigator);
}
function shimConstraints(constraints) {
	if (constraints && constraints.video !== void 0) return Object.assign({}, constraints, { video: compactObject(constraints.video) });
	return constraints;
}
function shimRTCIceServerUrls(window) {
	if (!window.RTCPeerConnection) return;
	const OrigPeerConnection = window.RTCPeerConnection;
	window.RTCPeerConnection = function RTCPeerConnection(pcConfig, pcConstraints) {
		if (pcConfig && pcConfig.iceServers) {
			const newIceServers = [];
			for (let i = 0; i < pcConfig.iceServers.length; i++) {
				let server = pcConfig.iceServers[i];
				if (server.urls === void 0 && server.url) {
					deprecated("RTCIceServer.url", "RTCIceServer.urls");
					server = JSON.parse(JSON.stringify(server));
					server.urls = server.url;
					delete server.url;
					newIceServers.push(server);
				} else newIceServers.push(pcConfig.iceServers[i]);
			}
			pcConfig.iceServers = newIceServers;
		}
		return new OrigPeerConnection(pcConfig, pcConstraints);
	};
	window.RTCPeerConnection.prototype = OrigPeerConnection.prototype;
	if ("generateCertificate" in OrigPeerConnection) Object.defineProperty(window.RTCPeerConnection, "generateCertificate", { get() {
		return OrigPeerConnection.generateCertificate;
	} });
}
function shimTrackEventTransceiver(window) {
	if (typeof window === "object" && window.RTCTrackEvent && "receiver" in window.RTCTrackEvent.prototype && !("transceiver" in window.RTCTrackEvent.prototype)) Object.defineProperty(window.RTCTrackEvent.prototype, "transceiver", { get() {
		return { receiver: this.receiver };
	} });
}
function shimCreateOfferLegacy(window) {
	const origCreateOffer = window.RTCPeerConnection.prototype.createOffer;
	window.RTCPeerConnection.prototype.createOffer = function createOffer(offerOptions) {
		if (offerOptions) {
			if (typeof offerOptions.offerToReceiveAudio !== "undefined") offerOptions.offerToReceiveAudio = !!offerOptions.offerToReceiveAudio;
			const audioTransceiver = this.getTransceivers().find((transceiver) => transceiver.receiver.track.kind === "audio");
			if (offerOptions.offerToReceiveAudio === false && audioTransceiver) {
				if (audioTransceiver.direction === "sendrecv") {
					if (audioTransceiver.setDirection) audioTransceiver.setDirection("sendonly");
					else audioTransceiver.direction = "sendonly";
				} else if (audioTransceiver.direction === "recvonly") {
					if (audioTransceiver.setDirection) audioTransceiver.setDirection("inactive");
					else audioTransceiver.direction = "inactive";
				}
			} else if (offerOptions.offerToReceiveAudio === true && !audioTransceiver) this.addTransceiver("audio", { direction: "recvonly" });
			if (typeof offerOptions.offerToReceiveVideo !== "undefined") offerOptions.offerToReceiveVideo = !!offerOptions.offerToReceiveVideo;
			const videoTransceiver = this.getTransceivers().find((transceiver) => transceiver.receiver.track.kind === "video");
			if (offerOptions.offerToReceiveVideo === false && videoTransceiver) {
				if (videoTransceiver.direction === "sendrecv") {
					if (videoTransceiver.setDirection) videoTransceiver.setDirection("sendonly");
					else videoTransceiver.direction = "sendonly";
				} else if (videoTransceiver.direction === "recvonly") {
					if (videoTransceiver.setDirection) videoTransceiver.setDirection("inactive");
					else videoTransceiver.direction = "inactive";
				}
			} else if (offerOptions.offerToReceiveVideo === true && !videoTransceiver) this.addTransceiver("video", { direction: "recvonly" });
		}
		return origCreateOffer.apply(this, arguments);
	};
}
function shimAudioContext(window) {
	if (typeof window !== "object" || window.AudioContext) return;
	window.AudioContext = window.webkitAudioContext;
}
var safariShim = /*#__PURE__*/ Object.freeze({
	__proto__: null,
	shimAudioContext,
	shimCallbacksAPI,
	shimConstraints,
	shimCreateOfferLegacy,
	shimGetUserMedia,
	shimLocalStreamsAPI,
	shimRTCIceServerUrls,
	shimRemoteStreamsAPI,
	shimTrackEventTransceiver
});
var sdp$1 = { exports: {} };
var hasRequiredSdp;
function requireSdp() {
	if (hasRequiredSdp) return sdp$1.exports;
	hasRequiredSdp = 1;
	(function(module) {
		const SDPUtils = {};
		SDPUtils.generateIdentifier = function() {
			return Math.random().toString(36).substring(2, 12);
		};
		SDPUtils.localCName = SDPUtils.generateIdentifier();
		SDPUtils.splitLines = function(blob) {
			return blob.trim().split("\n").map((line) => line.trim());
		};
		SDPUtils.splitSections = function(blob) {
			return blob.split("\nm=").map((part, index) => (index > 0 ? "m=" + part : part).trim() + "\r\n");
		};
		SDPUtils.getDescription = function(blob) {
			const sections = SDPUtils.splitSections(blob);
			return sections && sections[0];
		};
		SDPUtils.getMediaSections = function(blob) {
			const sections = SDPUtils.splitSections(blob);
			sections.shift();
			return sections;
		};
		SDPUtils.matchPrefix = function(blob, prefix) {
			return SDPUtils.splitLines(blob).filter((line) => line.indexOf(prefix) === 0);
		};
		SDPUtils.parseCandidate = function(line) {
			let parts;
			if (line.indexOf("a=candidate:") === 0) parts = line.substring(12).split(" ");
			else parts = line.substring(10).split(" ");
			const candidate = {
				foundation: parts[0],
				component: {
					1: "rtp",
					2: "rtcp"
				}[parts[1]] || parts[1],
				protocol: parts[2].toLowerCase(),
				priority: parseInt(parts[3], 10),
				ip: parts[4],
				address: parts[4],
				port: parseInt(parts[5], 10),
				type: parts[7]
			};
			for (let i = 8; i < parts.length; i += 2) switch (parts[i]) {
				case "raddr":
					candidate.relatedAddress = parts[i + 1];
					break;
				case "rport":
					candidate.relatedPort = parseInt(parts[i + 1], 10);
					break;
				case "tcptype":
					candidate.tcpType = parts[i + 1];
					break;
				case "ufrag":
					candidate.ufrag = parts[i + 1];
					candidate.usernameFragment = parts[i + 1];
					break;
				default: if (candidate[parts[i]] === void 0) candidate[parts[i]] = parts[i + 1];
			}
			return candidate;
		};
		SDPUtils.writeCandidate = function(candidate) {
			const sdp = [];
			sdp.push(candidate.foundation);
			const component = candidate.component;
			if (component === "rtp") sdp.push(1);
			else if (component === "rtcp") sdp.push(2);
			else sdp.push(component);
			sdp.push(candidate.protocol.toUpperCase());
			sdp.push(candidate.priority);
			sdp.push(candidate.address || candidate.ip);
			sdp.push(candidate.port);
			const type = candidate.type;
			sdp.push("typ");
			sdp.push(type);
			if (type !== "host" && candidate.relatedAddress && candidate.relatedPort !== void 0) {
				sdp.push("raddr");
				sdp.push(candidate.relatedAddress);
				sdp.push("rport");
				sdp.push(candidate.relatedPort);
			}
			if (candidate.tcpType && candidate.protocol.toLowerCase() === "tcp") {
				sdp.push("tcptype");
				sdp.push(candidate.tcpType);
			}
			if (candidate.usernameFragment || candidate.ufrag) {
				sdp.push("ufrag");
				sdp.push(candidate.usernameFragment || candidate.ufrag);
			}
			return "candidate:" + sdp.join(" ");
		};
		SDPUtils.parseIceOptions = function(line) {
			return line.substring(14).split(" ");
		};
		SDPUtils.parseRtpMap = function(line) {
			let parts = line.substring(9).split(" ");
			const parsed = { payloadType: parseInt(parts.shift(), 10) };
			parts = parts[0].split("/");
			parsed.name = parts[0];
			parsed.clockRate = parseInt(parts[1], 10);
			parsed.channels = parts.length === 3 ? parseInt(parts[2], 10) : 1;
			parsed.numChannels = parsed.channels;
			return parsed;
		};
		SDPUtils.writeRtpMap = function(codec) {
			let pt = codec.payloadType;
			if (codec.preferredPayloadType !== void 0) pt = codec.preferredPayloadType;
			const channels = codec.channels || codec.numChannels || 1;
			return "a=rtpmap:" + pt + " " + codec.name + "/" + codec.clockRate + (channels !== 1 ? "/" + channels : "") + "\r\n";
		};
		SDPUtils.parseExtmap = function(line) {
			const parts = line.substring(9).split(" ");
			return {
				id: parseInt(parts[0], 10),
				direction: parts[0].indexOf("/") > 0 ? parts[0].split("/")[1] : "sendrecv",
				uri: parts[1],
				attributes: parts.slice(2).join(" ")
			};
		};
		SDPUtils.writeExtmap = function(headerExtension) {
			return "a=extmap:" + (headerExtension.id || headerExtension.preferredId) + (headerExtension.direction && headerExtension.direction !== "sendrecv" ? "/" + headerExtension.direction : "") + " " + headerExtension.uri + (headerExtension.attributes ? " " + headerExtension.attributes : "") + "\r\n";
		};
		SDPUtils.parseFmtp = function(line) {
			const parsed = {};
			let kv;
			const parts = line.substring(line.indexOf(" ") + 1).split(";");
			for (let j = 0; j < parts.length; j++) {
				kv = parts[j].trim().split("=");
				parsed[kv[0].trim()] = kv[1];
			}
			return parsed;
		};
		SDPUtils.writeFmtp = function(codec) {
			let line = "";
			let pt = codec.payloadType;
			if (codec.preferredPayloadType !== void 0) pt = codec.preferredPayloadType;
			if (codec.parameters && Object.keys(codec.parameters).length) {
				const params = [];
				Object.keys(codec.parameters).forEach((param) => {
					if (codec.parameters[param] !== void 0) params.push(param + "=" + codec.parameters[param]);
					else params.push(param);
				});
				line += "a=fmtp:" + pt + " " + params.join(";") + "\r\n";
			}
			return line;
		};
		SDPUtils.parseRtcpFb = function(line) {
			const parts = line.substring(line.indexOf(" ") + 1).split(" ");
			return {
				type: parts.shift(),
				parameter: parts.join(" ")
			};
		};
		SDPUtils.writeRtcpFb = function(codec) {
			let lines = "";
			let pt = codec.payloadType;
			if (codec.preferredPayloadType !== void 0) pt = codec.preferredPayloadType;
			if (codec.rtcpFeedback && codec.rtcpFeedback.length) codec.rtcpFeedback.forEach((fb) => {
				lines += "a=rtcp-fb:" + pt + " " + fb.type + (fb.parameter && fb.parameter.length ? " " + fb.parameter : "") + "\r\n";
			});
			return lines;
		};
		SDPUtils.parseSsrcMedia = function(line) {
			const sp = line.indexOf(" ");
			const parts = { ssrc: parseInt(line.substring(7, sp), 10) };
			const colon = line.indexOf(":", sp);
			if (colon > -1) {
				parts.attribute = line.substring(sp + 1, colon);
				parts.value = line.substring(colon + 1);
			} else parts.attribute = line.substring(sp + 1);
			return parts;
		};
		SDPUtils.parseSsrcGroup = function(line) {
			const parts = line.substring(13).split(" ");
			return {
				semantics: parts.shift(),
				ssrcs: parts.map((ssrc) => parseInt(ssrc, 10))
			};
		};
		SDPUtils.getMid = function(mediaSection) {
			const mid = SDPUtils.matchPrefix(mediaSection, "a=mid:")[0];
			if (mid) return mid.substring(6);
		};
		SDPUtils.parseFingerprint = function(line) {
			const parts = line.substring(14).split(" ");
			return {
				algorithm: parts[0].toLowerCase(),
				value: parts[1].toUpperCase()
			};
		};
		SDPUtils.getDtlsParameters = function(mediaSection, sessionpart) {
			return {
				role: "auto",
				fingerprints: SDPUtils.matchPrefix(mediaSection + sessionpart, "a=fingerprint:").map(SDPUtils.parseFingerprint)
			};
		};
		SDPUtils.writeDtlsParameters = function(params, setupType) {
			let sdp = "a=setup:" + setupType + "\r\n";
			params.fingerprints.forEach((fp) => {
				sdp += "a=fingerprint:" + fp.algorithm + " " + fp.value + "\r\n";
			});
			return sdp;
		};
		SDPUtils.parseCryptoLine = function(line) {
			const parts = line.substring(9).split(" ");
			return {
				tag: parseInt(parts[0], 10),
				cryptoSuite: parts[1],
				keyParams: parts[2],
				sessionParams: parts.slice(3)
			};
		};
		SDPUtils.writeCryptoLine = function(parameters) {
			return "a=crypto:" + parameters.tag + " " + parameters.cryptoSuite + " " + (typeof parameters.keyParams === "object" ? SDPUtils.writeCryptoKeyParams(parameters.keyParams) : parameters.keyParams) + (parameters.sessionParams ? " " + parameters.sessionParams.join(" ") : "") + "\r\n";
		};
		SDPUtils.parseCryptoKeyParams = function(keyParams) {
			if (keyParams.indexOf("inline:") !== 0) return null;
			const parts = keyParams.substring(7).split("|");
			return {
				keyMethod: "inline",
				keySalt: parts[0],
				lifeTime: parts[1],
				mkiValue: parts[2] ? parts[2].split(":")[0] : void 0,
				mkiLength: parts[2] ? parts[2].split(":")[1] : void 0
			};
		};
		SDPUtils.writeCryptoKeyParams = function(keyParams) {
			return keyParams.keyMethod + ":" + keyParams.keySalt + (keyParams.lifeTime ? "|" + keyParams.lifeTime : "") + (keyParams.mkiValue && keyParams.mkiLength ? "|" + keyParams.mkiValue + ":" + keyParams.mkiLength : "");
		};
		SDPUtils.getCryptoParameters = function(mediaSection, sessionpart) {
			return SDPUtils.matchPrefix(mediaSection + sessionpart, "a=crypto:").map(SDPUtils.parseCryptoLine);
		};
		SDPUtils.getIceParameters = function(mediaSection, sessionpart) {
			const ufrag = SDPUtils.matchPrefix(mediaSection + sessionpart, "a=ice-ufrag:")[0];
			const pwd = SDPUtils.matchPrefix(mediaSection + sessionpart, "a=ice-pwd:")[0];
			if (!(ufrag && pwd)) return null;
			return {
				usernameFragment: ufrag.substring(12),
				password: pwd.substring(10)
			};
		};
		SDPUtils.writeIceParameters = function(params) {
			let sdp = "a=ice-ufrag:" + params.usernameFragment + "\r\na=ice-pwd:" + params.password + "\r\n";
			if (params.iceLite) sdp += "a=ice-lite\r\n";
			return sdp;
		};
		SDPUtils.parseRtpParameters = function(mediaSection) {
			const description = {
				codecs: [],
				headerExtensions: [],
				fecMechanisms: [],
				rtcp: []
			};
			const mline = SDPUtils.splitLines(mediaSection)[0].split(" ");
			description.profile = mline[2];
			for (let i = 3; i < mline.length; i++) {
				const pt = mline[i];
				const rtpmapline = SDPUtils.matchPrefix(mediaSection, "a=rtpmap:" + pt + " ")[0];
				if (rtpmapline) {
					const codec = SDPUtils.parseRtpMap(rtpmapline);
					const fmtps = SDPUtils.matchPrefix(mediaSection, "a=fmtp:" + pt + " ");
					codec.parameters = fmtps.length ? SDPUtils.parseFmtp(fmtps[0]) : {};
					codec.rtcpFeedback = SDPUtils.matchPrefix(mediaSection, "a=rtcp-fb:" + pt + " ").map(SDPUtils.parseRtcpFb);
					description.codecs.push(codec);
					switch (codec.name.toUpperCase()) {
						case "RED":
						case "ULPFEC": description.fecMechanisms.push(codec.name.toUpperCase());
					}
				}
			}
			SDPUtils.matchPrefix(mediaSection, "a=extmap:").forEach((line) => {
				description.headerExtensions.push(SDPUtils.parseExtmap(line));
			});
			const wildcardRtcpFb = SDPUtils.matchPrefix(mediaSection, "a=rtcp-fb:* ").map(SDPUtils.parseRtcpFb);
			description.codecs.forEach((codec) => {
				wildcardRtcpFb.forEach((fb) => {
					if (!codec.rtcpFeedback.find((existingFeedback) => {
						return existingFeedback.type === fb.type && existingFeedback.parameter === fb.parameter;
					})) codec.rtcpFeedback.push(fb);
				});
			});
			return description;
		};
		SDPUtils.writeRtpDescription = function(kind, caps) {
			let sdp = "";
			sdp += "m=" + kind + " ";
			sdp += caps.codecs.length > 0 ? "9" : "0";
			sdp += " " + (caps.profile || "UDP/TLS/RTP/SAVPF") + " ";
			sdp += caps.codecs.map((codec) => {
				if (codec.preferredPayloadType !== void 0) return codec.preferredPayloadType;
				return codec.payloadType;
			}).join(" ") + "\r\n";
			sdp += "c=IN IP4 0.0.0.0\r\n";
			sdp += "a=rtcp:9 IN IP4 0.0.0.0\r\n";
			caps.codecs.forEach((codec) => {
				sdp += SDPUtils.writeRtpMap(codec);
				sdp += SDPUtils.writeFmtp(codec);
				sdp += SDPUtils.writeRtcpFb(codec);
			});
			let maxptime = 0;
			caps.codecs.forEach((codec) => {
				if (codec.maxptime > maxptime) maxptime = codec.maxptime;
			});
			if (maxptime > 0) sdp += "a=maxptime:" + maxptime + "\r\n";
			if (caps.headerExtensions) caps.headerExtensions.forEach((extension) => {
				sdp += SDPUtils.writeExtmap(extension);
			});
			return sdp;
		};
		SDPUtils.parseRtpEncodingParameters = function(mediaSection) {
			const encodingParameters = [];
			const description = SDPUtils.parseRtpParameters(mediaSection);
			const hasRed = description.fecMechanisms.indexOf("RED") !== -1;
			const hasUlpfec = description.fecMechanisms.indexOf("ULPFEC") !== -1;
			const ssrcs = SDPUtils.matchPrefix(mediaSection, "a=ssrc:").map((line) => SDPUtils.parseSsrcMedia(line)).filter((parts) => parts.attribute === "cname");
			const primarySsrc = ssrcs.length > 0 && ssrcs[0].ssrc;
			let secondarySsrc;
			const flows = SDPUtils.matchPrefix(mediaSection, "a=ssrc-group:FID").map((line) => {
				return line.substring(17).split(" ").map((part) => parseInt(part, 10));
			});
			if (flows.length > 0 && flows[0].length > 1 && flows[0][0] === primarySsrc) secondarySsrc = flows[0][1];
			description.codecs.forEach((codec) => {
				if (codec.name.toUpperCase() === "RTX" && codec.parameters.apt) {
					let encParam = {
						ssrc: primarySsrc,
						codecPayloadType: parseInt(codec.parameters.apt, 10)
					};
					if (primarySsrc && secondarySsrc) encParam.rtx = { ssrc: secondarySsrc };
					encodingParameters.push(encParam);
					if (hasRed) {
						encParam = JSON.parse(JSON.stringify(encParam));
						encParam.fec = {
							ssrc: primarySsrc,
							mechanism: hasUlpfec ? "red+ulpfec" : "red"
						};
						encodingParameters.push(encParam);
					}
				}
			});
			if (encodingParameters.length === 0 && primarySsrc) encodingParameters.push({ ssrc: primarySsrc });
			let bandwidth = SDPUtils.matchPrefix(mediaSection, "b=");
			if (bandwidth.length) {
				if (bandwidth[0].indexOf("b=TIAS:") === 0) bandwidth = parseInt(bandwidth[0].substring(7), 10);
				else if (bandwidth[0].indexOf("b=AS:") === 0) bandwidth = parseInt(bandwidth[0].substring(5), 10) * 1e3 * .95 - 16e3;
				else bandwidth = void 0;
				encodingParameters.forEach((params) => {
					params.maxBitrate = bandwidth;
				});
			}
			return encodingParameters;
		};
		SDPUtils.parseRtcpParameters = function(mediaSection) {
			const rtcpParameters = {};
			const remoteSsrc = SDPUtils.matchPrefix(mediaSection, "a=ssrc:").map((line) => SDPUtils.parseSsrcMedia(line)).filter((obj) => obj.attribute === "cname")[0];
			if (remoteSsrc) {
				rtcpParameters.cname = remoteSsrc.value;
				rtcpParameters.ssrc = remoteSsrc.ssrc;
			}
			const rsize = SDPUtils.matchPrefix(mediaSection, "a=rtcp-rsize");
			rtcpParameters.reducedSize = rsize.length > 0;
			rtcpParameters.compound = rsize.length === 0;
			rtcpParameters.mux = SDPUtils.matchPrefix(mediaSection, "a=rtcp-mux").length > 0;
			return rtcpParameters;
		};
		SDPUtils.writeRtcpParameters = function(rtcpParameters) {
			let sdp = "";
			if (rtcpParameters.reducedSize) sdp += "a=rtcp-rsize\r\n";
			if (rtcpParameters.mux) sdp += "a=rtcp-mux\r\n";
			if (rtcpParameters.ssrc !== void 0 && rtcpParameters.cname) sdp += "a=ssrc:" + rtcpParameters.ssrc + " cname:" + rtcpParameters.cname + "\r\n";
			return sdp;
		};
		SDPUtils.parseMsid = function(mediaSection) {
			let parts;
			const spec = SDPUtils.matchPrefix(mediaSection, "a=msid:");
			if (spec.length === 1) {
				parts = spec[0].substring(7).split(" ");
				return {
					stream: parts[0],
					track: parts[1]
				};
			}
			const planB = SDPUtils.matchPrefix(mediaSection, "a=ssrc:").map((line) => SDPUtils.parseSsrcMedia(line)).filter((msidParts) => msidParts.attribute === "msid");
			if (planB.length > 0) {
				parts = planB[0].value.split(" ");
				return {
					stream: parts[0],
					track: parts[1]
				};
			}
		};
		SDPUtils.parseSctpDescription = function(mediaSection) {
			const mline = SDPUtils.parseMLine(mediaSection);
			const maxSizeLine = SDPUtils.matchPrefix(mediaSection, "a=max-message-size:");
			let maxMessageSize;
			if (maxSizeLine.length > 0) maxMessageSize = parseInt(maxSizeLine[0].substring(19), 10);
			if (isNaN(maxMessageSize)) maxMessageSize = 65536;
			const sctpPort = SDPUtils.matchPrefix(mediaSection, "a=sctp-port:");
			if (sctpPort.length > 0) return {
				port: parseInt(sctpPort[0].substring(12), 10),
				protocol: mline.fmt,
				maxMessageSize
			};
			const sctpMapLines = SDPUtils.matchPrefix(mediaSection, "a=sctpmap:");
			if (sctpMapLines.length > 0) {
				const parts = sctpMapLines[0].substring(10).split(" ");
				return {
					port: parseInt(parts[0], 10),
					protocol: parts[1],
					maxMessageSize
				};
			}
		};
		SDPUtils.writeSctpDescription = function(media, sctp) {
			let output = [];
			if (media.protocol !== "DTLS/SCTP") output = [
				"m=" + media.kind + " 9 " + media.protocol + " " + sctp.protocol + "\r\n",
				"c=IN IP4 0.0.0.0\r\n",
				"a=sctp-port:" + sctp.port + "\r\n"
			];
			else output = [
				"m=" + media.kind + " 9 " + media.protocol + " " + sctp.port + "\r\n",
				"c=IN IP4 0.0.0.0\r\n",
				"a=sctpmap:" + sctp.port + " " + sctp.protocol + " 65535\r\n"
			];
			if (sctp.maxMessageSize !== void 0) output.push("a=max-message-size:" + sctp.maxMessageSize + "\r\n");
			return output.join("");
		};
		SDPUtils.generateSessionId = function() {
			return Math.random().toString().substr(2, 22);
		};
		SDPUtils.writeSessionBoilerplate = function(sessId, sessVer, sessUser) {
			let sessionId;
			const version = sessVer !== void 0 ? sessVer : 2;
			if (sessId) sessionId = sessId;
			else sessionId = SDPUtils.generateSessionId();
			return "v=0\r\no=" + (sessUser || "thisisadapterortc") + " " + sessionId + " " + version + " IN IP4 127.0.0.1\r\ns=-\r\nt=0 0\r\n";
		};
		SDPUtils.getDirection = function(mediaSection, sessionpart) {
			const lines = SDPUtils.splitLines(mediaSection);
			for (let i = 0; i < lines.length; i++) switch (lines[i]) {
				case "a=sendrecv":
				case "a=sendonly":
				case "a=recvonly":
				case "a=inactive": return lines[i].substring(2);
			}
			if (sessionpart) return SDPUtils.getDirection(sessionpart);
			return "sendrecv";
		};
		SDPUtils.getKind = function(mediaSection) {
			return SDPUtils.splitLines(mediaSection)[0].split(" ")[0].substring(2);
		};
		SDPUtils.isRejected = function(mediaSection) {
			return mediaSection.split(" ", 2)[1] === "0";
		};
		SDPUtils.parseMLine = function(mediaSection) {
			const parts = SDPUtils.splitLines(mediaSection)[0].substring(2).split(" ");
			return {
				kind: parts[0],
				port: parseInt(parts[1], 10),
				protocol: parts[2],
				fmt: parts.slice(3).join(" ")
			};
		};
		SDPUtils.parseOLine = function(mediaSection) {
			const parts = SDPUtils.matchPrefix(mediaSection, "o=")[0].substring(2).split(" ");
			return {
				username: parts[0],
				sessionId: parts[1],
				sessionVersion: parseInt(parts[2], 10),
				netType: parts[3],
				addressType: parts[4],
				address: parts[5]
			};
		};
		SDPUtils.isValidSDP = function(blob) {
			if (typeof blob !== "string" || blob.length === 0) return false;
			const lines = SDPUtils.splitLines(blob);
			for (let i = 0; i < lines.length; i++) if (lines[i].length < 2 || lines[i].charAt(1) !== "=") return false;
			return true;
		};
		module.exports = SDPUtils;
	})(sdp$1);
	return sdp$1.exports;
}
var sdpExports = requireSdp();
var SDPUtils = /*@__PURE__*/ getDefaultExportFromCjs(sdpExports);
var sdp = /*#__PURE__*/ _mergeNamespaces({
	__proto__: null,
	default: SDPUtils
}, [sdpExports]);
function shimRTCIceCandidate(window) {
	if (!window.RTCIceCandidate || window.RTCIceCandidate && "foundation" in window.RTCIceCandidate.prototype) return;
	const NativeRTCIceCandidate = window.RTCIceCandidate;
	window.RTCIceCandidate = function RTCIceCandidate(args) {
		if (typeof args === "object" && args.candidate && args.candidate.indexOf("a=") === 0) {
			args = JSON.parse(JSON.stringify(args));
			args.candidate = args.candidate.substring(2);
		}
		if (args.candidate && args.candidate.length) {
			const nativeCandidate = new NativeRTCIceCandidate(args);
			const parsedCandidate = SDPUtils.parseCandidate(args.candidate);
			for (const key in parsedCandidate) if (!(key in nativeCandidate)) Object.defineProperty(nativeCandidate, key, { value: parsedCandidate[key] });
			nativeCandidate.toJSON = function toJSON() {
				return {
					candidate: nativeCandidate.candidate,
					sdpMid: nativeCandidate.sdpMid,
					sdpMLineIndex: nativeCandidate.sdpMLineIndex,
					usernameFragment: nativeCandidate.usernameFragment
				};
			};
			return nativeCandidate;
		}
		return new NativeRTCIceCandidate(args);
	};
	window.RTCIceCandidate.prototype = NativeRTCIceCandidate.prototype;
	wrapPeerConnectionEvent(window, "icecandidate", (e) => {
		if (e.candidate) Object.defineProperty(e, "candidate", {
			value: new window.RTCIceCandidate(e.candidate),
			writable: "false"
		});
		return e;
	});
}
function shimRTCIceCandidateRelayProtocol(window) {
	if (!window.RTCIceCandidate || window.RTCIceCandidate && "relayProtocol" in window.RTCIceCandidate.prototype) return;
	wrapPeerConnectionEvent(window, "icecandidate", (e) => {
		if (e.candidate) {
			const parsedCandidate = SDPUtils.parseCandidate(e.candidate.candidate);
			if (parsedCandidate.type === "relay") e.candidate.relayProtocol = {
				0: "tls",
				1: "tcp",
				2: "udp"
			}[parsedCandidate.priority >> 24];
		}
		return e;
	});
}
function shimMaxMessageSize(window, browserDetails) {
	if (!window.RTCPeerConnection) return;
	if (browserDetails.browser === "chrome" && browserDetails.version > 102) return;
	if (browserDetails.browser === "firefox" && browserDetails.version >= 113) return;
	if (!("sctp" in window.RTCPeerConnection.prototype)) Object.defineProperty(window.RTCPeerConnection.prototype, "sctp", { get() {
		return typeof this._sctp === "undefined" ? null : this._sctp;
	} });
	const sctpInDescription = function(description) {
		if (!description || !description.sdp) return false;
		const sections = SDPUtils.splitSections(description.sdp);
		sections.shift();
		return sections.some((mediaSection) => {
			const mLine = SDPUtils.parseMLine(mediaSection);
			return mLine && mLine.kind === "application" && mLine.protocol.indexOf("SCTP") !== -1;
		});
	};
	const getRemoteFirefoxVersion = function(description) {
		const match = description.sdp.match(/mozilla...THIS_IS_SDPARTA-(\d+)/);
		if (match === null || match.length < 2) return -1;
		const version = parseInt(match[1], 10);
		return version !== version ? -1 : version;
	};
	const getCanSendMaxMessageSize = function(remoteIsFirefox) {
		let canSendMaxMessageSize = 65536;
		if (browserDetails.browser === "firefox") {
			if (browserDetails.version < 57) {
				if (remoteIsFirefox === -1) canSendMaxMessageSize = 16384;
				else canSendMaxMessageSize = 2147483637;
			} else if (browserDetails.version < 60) canSendMaxMessageSize = browserDetails.version === 57 ? 65535 : 65536;
			else canSendMaxMessageSize = 2147483637;
		}
		return canSendMaxMessageSize;
	};
	const getMaxMessageSize = function(description, remoteIsFirefox) {
		let maxMessageSize = 65536;
		if (browserDetails.browser === "firefox" && browserDetails.version === 57) maxMessageSize = 65535;
		const match = SDPUtils.matchPrefix(description.sdp, "a=max-message-size:");
		if (match.length > 0) maxMessageSize = parseInt(match[0].substring(19), 10);
		else if (browserDetails.browser === "firefox" && remoteIsFirefox !== -1) maxMessageSize = 2147483637;
		return maxMessageSize;
	};
	const origSetRemoteDescription = window.RTCPeerConnection.prototype.setRemoteDescription;
	window.RTCPeerConnection.prototype.setRemoteDescription = function setRemoteDescription() {
		this._sctp = null;
		if (browserDetails.browser === "chrome" && browserDetails.version >= 76) {
			if (this.getConfiguration().sdpSemantics === "plan-b") Object.defineProperty(this, "sctp", {
				get() {
					return typeof this._sctp === "undefined" ? null : this._sctp;
				},
				enumerable: true,
				configurable: true
			});
		}
		if (sctpInDescription(arguments[0])) {
			const isFirefox = getRemoteFirefoxVersion(arguments[0]);
			const canSendMMS = getCanSendMaxMessageSize(isFirefox);
			const remoteMMS = getMaxMessageSize(arguments[0], isFirefox);
			let maxMessageSize;
			if (canSendMMS === 0 && remoteMMS === 0) maxMessageSize = Number.POSITIVE_INFINITY;
			else if (canSendMMS === 0 || remoteMMS === 0) maxMessageSize = Math.max(canSendMMS, remoteMMS);
			else maxMessageSize = Math.min(canSendMMS, remoteMMS);
			const sctp = {};
			Object.defineProperty(sctp, "maxMessageSize", { get() {
				return maxMessageSize;
			} });
			this._sctp = sctp;
		}
		return origSetRemoteDescription.apply(this, arguments);
	};
}
function shimSendThrowTypeError(window, browserDetails) {
	if (!(window.RTCPeerConnection && "createDataChannel" in window.RTCPeerConnection.prototype)) return;
	if (browserDetails.browser === "chrome" && browserDetails.version >= 149) return;
	if (browserDetails.browser === "firefox" && browserDetails.version > 60) return;
	function wrapDcSend(dc, pc) {
		const origDataChannelSend = dc.send;
		dc.send = function send() {
			const data = arguments[0];
			const length = data.length || data.size || data.byteLength;
			if (dc.readyState === "open" && pc.sctp && length > pc.sctp.maxMessageSize) throw new TypeError("Message too large (can send a maximum of " + pc.sctp.maxMessageSize + " bytes)");
			return origDataChannelSend.apply(dc, arguments);
		};
	}
	const origCreateDataChannel = window.RTCPeerConnection.prototype.createDataChannel;
	window.RTCPeerConnection.prototype.createDataChannel = function createDataChannel() {
		const dataChannel = origCreateDataChannel.apply(this, arguments);
		wrapDcSend(dataChannel, this);
		return dataChannel;
	};
	wrapPeerConnectionEvent(window, "datachannel", (e) => {
		wrapDcSend(e.channel, e.target);
		return e;
	});
}
function shimConnectionState(window) {
	if (!window.RTCPeerConnection || "connectionState" in window.RTCPeerConnection.prototype) return;
	const proto = window.RTCPeerConnection.prototype;
	Object.defineProperty(proto, "connectionState", {
		get() {
			return {
				completed: "connected",
				checking: "connecting"
			}[this.iceConnectionState] || this.iceConnectionState;
		},
		enumerable: true,
		configurable: true
	});
	Object.defineProperty(proto, "onconnectionstatechange", {
		get() {
			return this._onconnectionstatechange || null;
		},
		set(cb) {
			if (this._onconnectionstatechange) {
				this.removeEventListener("connectionstatechange", this._onconnectionstatechange);
				delete this._onconnectionstatechange;
			}
			if (cb) this.addEventListener("connectionstatechange", this._onconnectionstatechange = cb);
		},
		enumerable: true,
		configurable: true
	});
	["setLocalDescription", "setRemoteDescription"].forEach((method) => {
		const origMethod = proto[method];
		proto[method] = function() {
			if (!this._connectionstatechangepoly) {
				this._connectionstatechangepoly = (e) => {
					const pc = e.target;
					if (pc._lastConnectionState !== pc.connectionState) {
						pc._lastConnectionState = pc.connectionState;
						const newEvent = new Event("connectionstatechange", e);
						pc.dispatchEvent(newEvent);
					}
					return e;
				};
				this.addEventListener("iceconnectionstatechange", this._connectionstatechangepoly);
			}
			return origMethod.apply(this, arguments);
		};
	});
}
function removeExtmapAllowMixed(window, browserDetails) {
	if (!window.RTCPeerConnection) return;
	if (browserDetails.browser === "chrome" && browserDetails.version >= 71) return;
	if (browserDetails.browser === "safari" && browserDetails._safariVersion >= 13.1) return;
	const nativeSRD = window.RTCPeerConnection.prototype.setRemoteDescription;
	window.RTCPeerConnection.prototype.setRemoteDescription = function setRemoteDescription(desc) {
		if (desc && desc.sdp && desc.sdp.indexOf("\na=extmap-allow-mixed") !== -1) {
			const sdp = desc.sdp.split("\n").filter((line) => {
				return line.trim() !== "a=extmap-allow-mixed";
			}).join("\n");
			if (window.RTCSessionDescription && desc instanceof window.RTCSessionDescription) arguments[0] = new window.RTCSessionDescription({
				type: desc.type,
				sdp
			});
			else desc.sdp = sdp;
		}
		return nativeSRD.apply(this, arguments);
	};
}
function shimAddIceCandidateNullOrEmpty(window, browserDetails) {
	if (!(window.RTCPeerConnection && window.RTCPeerConnection.prototype)) return;
	const nativeAddIceCandidate = window.RTCPeerConnection.prototype.addIceCandidate;
	if (!nativeAddIceCandidate || nativeAddIceCandidate.length === 0) return;
	window.RTCPeerConnection.prototype.addIceCandidate = function addIceCandidate() {
		if (!arguments[0]) {
			if (arguments[1]) arguments[1].apply(null);
			return Promise.resolve();
		}
		if ((browserDetails.browser === "chrome" && browserDetails.version < 78 || browserDetails.browser === "firefox" && browserDetails.version < 68 || browserDetails.browser === "safari") && arguments[0] && arguments[0].candidate === "") return Promise.resolve();
		return nativeAddIceCandidate.apply(this, arguments);
	};
}
function shimParameterlessSetLocalDescription(window, browserDetails) {
	if (!(window.RTCPeerConnection && window.RTCPeerConnection.prototype)) return;
	const nativeSetLocalDescription = window.RTCPeerConnection.prototype.setLocalDescription;
	if (!nativeSetLocalDescription || nativeSetLocalDescription.length === 0) return;
	window.RTCPeerConnection.prototype.setLocalDescription = function setLocalDescription() {
		let desc = arguments[0] || {};
		if (typeof desc !== "object" || desc.type && desc.sdp) return nativeSetLocalDescription.apply(this, arguments);
		desc = {
			type: desc.type,
			sdp: desc.sdp
		};
		if (!desc.type) switch (this.signalingState) {
			case "stable":
			case "have-local-offer":
			case "have-remote-pranswer":
				desc.type = "offer";
				break;
			default: desc.type = "answer";
		}
		if (desc.sdp || desc.type !== "offer" && desc.type !== "answer") return nativeSetLocalDescription.apply(this, [desc]);
		return (desc.type === "offer" ? this.createOffer : this.createAnswer).apply(this).then((d) => nativeSetLocalDescription.apply(this, [d]));
	};
}
var commonShim = /*#__PURE__*/ Object.freeze({
	__proto__: null,
	removeExtmapAllowMixed,
	shimAddIceCandidateNullOrEmpty,
	shimConnectionState,
	shimMaxMessageSize,
	shimParameterlessSetLocalDescription,
	shimRTCIceCandidate,
	shimRTCIceCandidateRelayProtocol,
	shimSendThrowTypeError
});
function adapterFactory() {
	let window = (arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {}).window;
	let options = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {
		shimChrome: true,
		shimFirefox: true,
		shimSafari: true
	};
	const logging = log$5;
	const browserDetails = detectBrowser(window);
	const adapter = {
		browserDetails,
		commonShim,
		extractVersion,
		disableLog,
		disableWarnings,
		sdp
	};
	switch (browserDetails.browser) {
		case "chrome":
			if (!chromeShim || !shimPeerConnection$1 || !options.shimChrome) {
				logging("Chrome shim is not included in this adapter release.");
				return adapter;
			}
			if (browserDetails.version === null) {
				logging("Chrome shim can not determine version, not shimming.");
				return adapter;
			}
			logging("adapter.js shimming chrome.");
			adapter.browserShim = chromeShim;
			shimAddIceCandidateNullOrEmpty(window, browserDetails);
			shimParameterlessSetLocalDescription(window);
			shimGetUserMedia$2(window, browserDetails);
			shimMediaStream(window);
			shimPeerConnection$1(window, browserDetails);
			shimOnTrack$1(window, browserDetails);
			shimAddTrackRemoveTrack(window, browserDetails);
			shimGetSendersWithDtmf(window);
			shimSenderReceiverGetStats(window, browserDetails);
			fixNegotiationNeeded(window, browserDetails);
			shimRTCIceCandidate(window);
			shimRTCIceCandidateRelayProtocol(window);
			shimConnectionState(window);
			shimMaxMessageSize(window, browserDetails);
			shimSendThrowTypeError(window, browserDetails);
			removeExtmapAllowMixed(window, browserDetails);
			break;
		case "firefox":
			if (!firefoxShim || !shimPeerConnection || !options.shimFirefox) {
				logging("Firefox shim is not included in this adapter release.");
				return adapter;
			}
			logging("adapter.js shimming firefox.");
			adapter.browserShim = firefoxShim;
			shimAddIceCandidateNullOrEmpty(window, browserDetails);
			shimParameterlessSetLocalDescription(window);
			shimGetUserMedia$1(window, browserDetails);
			shimPeerConnection(window, browserDetails);
			shimGetStats(window, browserDetails);
			shimOnTrack(window);
			shimRemoveStream(window);
			shimSenderGetStats(window);
			shimReceiverGetStats(window);
			shimRTCDataChannel(window);
			shimAddTransceiver(window, browserDetails);
			shimGetParameters(window, browserDetails);
			shimCreateOffer(window, browserDetails);
			shimCreateAnswer(window, browserDetails);
			shimRTCIceCandidate(window);
			shimConnectionState(window);
			shimMaxMessageSize(window, browserDetails);
			shimSendThrowTypeError(window, browserDetails);
			break;
		case "safari":
			if (!safariShim || !options.shimSafari) {
				logging("Safari shim is not included in this adapter release.");
				return adapter;
			}
			logging("adapter.js shimming safari.");
			adapter.browserShim = safariShim;
			shimAddIceCandidateNullOrEmpty(window, browserDetails);
			shimParameterlessSetLocalDescription(window);
			shimRTCIceServerUrls(window);
			shimCreateOfferLegacy(window);
			shimCallbacksAPI(window);
			shimLocalStreamsAPI(window);
			shimRemoteStreamsAPI(window);
			shimTrackEventTransceiver(window);
			shimGetUserMedia(window);
			shimAudioContext(window);
			shimRTCIceCandidate(window);
			shimRTCIceCandidateRelayProtocol(window);
			shimMaxMessageSize(window, browserDetails);
			shimSendThrowTypeError(window, browserDetails);
			removeExtmapAllowMixed(window, browserDetails);
			break;
		default: logging("Unsupported browser!");
	}
	return adapter;
}
adapterFactory({ window: typeof window === "undefined" ? void 0 : window });
var _a$2;
var _b;
var TypedPromise = class extends (_b = Promise) {
	constructor(executor) {
		super(executor);
	}
	catch(onrejected) {
		return super.catch(onrejected);
	}
	static reject(reason) {
		return super.reject(reason);
	}
	static all(values) {
		return super.all(values);
	}
	static race(values) {
		return super.race(values);
	}
};
_a$2 = TypedPromise;
TypedPromise.resolve = (value) => {
	return Reflect.get(_b, "resolve", _a$2).call(_a$2, value);
};
var commonVersionIdentifier = /version\/(\d+(\.?_?\d+)+)/i;
var browserDetails;
/**
* @internal
*/
function getBrowser(userAgent) {
	let force = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : true;
	if (typeof userAgent === "undefined" && typeof navigator === "undefined") return;
	const ua = (userAgent !== null && userAgent !== void 0 ? userAgent : navigator.userAgent).toLowerCase();
	if (browserDetails === void 0 || force) {
		const browser = browsersList.find((_ref) => {
			return _ref.test.test(ua);
		});
		browserDetails = browser === null || browser === void 0 ? void 0 : browser.describe(ua);
	}
	return browserDetails;
}
var browsersList = [
	{
		test: /firefox|iceweasel|fxios/i,
		describe(ua) {
			return {
				name: "Firefox",
				version: getMatch(/(?:firefox|iceweasel|fxios)[\s/](\d+(\.?_?\d+)+)/i, ua),
				os: ua.toLowerCase().includes("fxios") ? "iOS" : void 0,
				osVersion: getOSVersion(ua)
			};
		}
	},
	{
		test: /chrom|crios|crmo/i,
		describe(ua) {
			return {
				name: "Chrome",
				version: getMatch(/(?:chrome|chromium|crios|crmo)\/(\d+(\.?_?\d+)+)/i, ua),
				os: ua.toLowerCase().includes("crios") ? "iOS" : void 0,
				osVersion: getOSVersion(ua)
			};
		}
	},
	{
		test: /safari|applewebkit/i,
		describe(ua) {
			return {
				name: "Safari",
				version: getMatch(commonVersionIdentifier, ua),
				os: ua.includes("mobile/") ? "iOS" : "macOS",
				osVersion: getOSVersion(ua)
			};
		}
	}
];
function getMatch(exp, ua) {
	let id = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : 1;
	const match = ua.match(exp);
	return match && match.length >= id && match[id] || "";
}
function getOSVersion(ua) {
	return ua.includes("mac os") ? getMatch(/\(.+?(\d+_\d+(:?_\d+)?)/, ua, 1).replace(/_/g, ".") : void 0;
}
var version = "2.22.0";
var LivekitError = class extends Error {
	constructor(code, message, options) {
		super(message || "an error has occurred");
		this.name = "LiveKitError";
		this.code = code;
		if (typeof (options === null || options === void 0 ? void 0 : options.cause) !== "undefined") this.cause = options === null || options === void 0 ? void 0 : options.cause;
	}
};
/**
* LiveKit specific error type representing an error with an associated set of reasons.
* Use this to represent an error with multiple different but contextually related variants.
* */
var LivekitReasonedError = class extends LivekitError {};
var ConnectionErrorReason;
(function(ConnectionErrorReason) {
	ConnectionErrorReason[ConnectionErrorReason["NotAllowed"] = 0] = "NotAllowed";
	ConnectionErrorReason[ConnectionErrorReason["ServerUnreachable"] = 1] = "ServerUnreachable";
	ConnectionErrorReason[ConnectionErrorReason["InternalError"] = 2] = "InternalError";
	ConnectionErrorReason[ConnectionErrorReason["Cancelled"] = 3] = "Cancelled";
	ConnectionErrorReason[ConnectionErrorReason["LeaveRequest"] = 4] = "LeaveRequest";
	ConnectionErrorReason[ConnectionErrorReason["Timeout"] = 5] = "Timeout";
	ConnectionErrorReason[ConnectionErrorReason["WebSocket"] = 6] = "WebSocket";
	ConnectionErrorReason[ConnectionErrorReason["ServiceNotFound"] = 7] = "ServiceNotFound";
})(ConnectionErrorReason || (ConnectionErrorReason = {}));
var ConnectionError = class ConnectionError extends LivekitReasonedError {
	constructor(message, reason, status, context) {
		super(1, message);
		this.name = "ConnectionError";
		this.status = status;
		this.reason = reason;
		this.context = context;
		this.reasonName = ConnectionErrorReason[reason];
	}
	static notAllowed(message, status, context) {
		return new ConnectionError(message, ConnectionErrorReason.NotAllowed, status, context);
	}
	static timeout(message) {
		return new ConnectionError(message, ConnectionErrorReason.Timeout);
	}
	static leaveRequest(message, context) {
		return new ConnectionError(message, ConnectionErrorReason.LeaveRequest, void 0, context);
	}
	static internal(message, context) {
		return new ConnectionError(message, ConnectionErrorReason.InternalError, void 0, context);
	}
	static cancelled(message) {
		return new ConnectionError(message, ConnectionErrorReason.Cancelled);
	}
	static serverUnreachable(message, status) {
		return new ConnectionError(message, ConnectionErrorReason.ServerUnreachable, status);
	}
	static websocket(message, status, reason) {
		return new ConnectionError(message, ConnectionErrorReason.WebSocket, status, reason);
	}
	static serviceNotFound(message, serviceName) {
		return new ConnectionError(message, ConnectionErrorReason.ServiceNotFound, void 0, serviceName);
	}
};
var DeviceUnsupportedError = class extends LivekitError {
	constructor(message) {
		super(21, message !== null && message !== void 0 ? message : "device is unsupported");
		this.name = "DeviceUnsupportedError";
	}
};
var TrackInvalidError = class extends LivekitError {
	constructor(message) {
		super(20, message !== null && message !== void 0 ? message : "track is invalid");
		this.name = "TrackInvalidError";
	}
};
var UnsupportedServer = class extends LivekitError {
	constructor(message) {
		super(10, message || "unsupported server");
		this.name = "UnsupportedServer";
	}
};
var UnexpectedConnectionState = class extends LivekitError {
	constructor(message) {
		super(12, message || "unexpected connection state");
		this.name = "UnexpectedConnectionState";
	}
};
var NegotiationError = class extends LivekitError {
	constructor(message) {
		super(13, message || "unable to negotiate");
		this.name = "NegotiationError";
	}
};
var PublishDataError = class extends LivekitError {
	constructor(message) {
		super(14, message || "unable to publish data");
		this.name = "PublishDataError";
	}
};
var PublishTrackError = class extends LivekitError {
	constructor(message, status) {
		super(15, message);
		this.name = "PublishTrackError";
		this.status = status;
	}
};
var SignalRequestError = class extends LivekitReasonedError {
	constructor(message, reason) {
		super(15, message);
		this.name = "SignalRequestError";
		this.reason = reason;
		this.reasonName = typeof reason === "string" ? reason : RequestResponse_Reason[reason];
	}
};
var DataStreamErrorReason;
(function(DataStreamErrorReason) {
	DataStreamErrorReason[DataStreamErrorReason["AlreadyOpened"] = 0] = "AlreadyOpened";
	DataStreamErrorReason[DataStreamErrorReason["AbnormalEnd"] = 1] = "AbnormalEnd";
	DataStreamErrorReason[DataStreamErrorReason["DecodeFailed"] = 2] = "DecodeFailed";
	DataStreamErrorReason[DataStreamErrorReason["LengthExceeded"] = 3] = "LengthExceeded";
	DataStreamErrorReason[DataStreamErrorReason["Incomplete"] = 4] = "Incomplete";
	DataStreamErrorReason[DataStreamErrorReason["HandlerAlreadyRegistered"] = 7] = "HandlerAlreadyRegistered";
	DataStreamErrorReason[DataStreamErrorReason["EncryptionTypeMismatch"] = 8] = "EncryptionTypeMismatch";
	DataStreamErrorReason[DataStreamErrorReason["HeaderTooLarge"] = 9] = "HeaderTooLarge";
	DataStreamErrorReason[DataStreamErrorReason["PayloadTooLarge"] = 10] = "PayloadTooLarge";
})(DataStreamErrorReason || (DataStreamErrorReason = {}));
var DataStreamError = class extends LivekitReasonedError {
	constructor(message, reason) {
		super(16, message);
		this.name = "DataStreamError";
		this.reason = reason;
		this.reasonName = DataStreamErrorReason[reason];
	}
};
var SignalReconnectError = class extends LivekitError {
	constructor(message) {
		super(18, message);
		this.name = "SignalReconnectError";
	}
};
var MediaDeviceFailure;
(function(MediaDeviceFailure) {
	MediaDeviceFailure["PermissionDenied"] = "PermissionDenied";
	MediaDeviceFailure["NotFound"] = "NotFound";
	MediaDeviceFailure["DeviceInUse"] = "DeviceInUse";
	MediaDeviceFailure["Other"] = "Other";
})(MediaDeviceFailure || (MediaDeviceFailure = {}));
(function(MediaDeviceFailure) {
	function getFailure(error) {
		if (error && "name" in error) {
			if (error.name === "NotFoundError" || error.name === "DevicesNotFoundError") return MediaDeviceFailure.NotFound;
			if (error.name === "NotAllowedError" || error.name === "PermissionDeniedError") return MediaDeviceFailure.PermissionDenied;
			if (error.name === "NotReadableError" || error.name === "TrackStartError") return MediaDeviceFailure.DeviceInUse;
			return MediaDeviceFailure.Other;
		}
	}
	MediaDeviceFailure.getFailure = getFailure;
})(MediaDeviceFailure || (MediaDeviceFailure = {}));
var CriticalTimers = class {};
CriticalTimers.setTimeout = function() {
	return setTimeout(...arguments);
};
CriticalTimers.setInterval = function() {
	return setInterval(...arguments);
};
CriticalTimers.clearTimeout = function() {
	return clearTimeout(...arguments);
};
CriticalTimers.clearInterval = function() {
	return clearInterval(...arguments);
};
var RoomEvent;
(function(RoomEvent) {
	/**
	* When the connection to the server has been established
	*/
	RoomEvent["Connected"] = "connected";
	/**
	* When the connection to the server has been interrupted and it's attempting
	* to reconnect.
	*/
	RoomEvent["Reconnecting"] = "reconnecting";
	/**
	* When the signal connection to the server has been interrupted. This isn't noticeable to users most of the time.
	* It will resolve with a `RoomEvent.Reconnected` once the signal connection has been re-established.
	* If media fails additionally it an additional `RoomEvent.Reconnecting` will be emitted.
	*/
	RoomEvent["SignalReconnecting"] = "signalReconnecting";
	/**
	* Fires when a reconnection has been successful.
	*/
	RoomEvent["Reconnected"] = "reconnected";
	/**
	* When disconnected from room. This fires when room.disconnect() is called or
	* when an unrecoverable connection issue had occurred.
	*
	* DisconnectReason can be used to determine why the participant was disconnected. Notable reasons are
	* - DUPLICATE_IDENTITY: another client with the same identity has joined the room
	* - PARTICIPANT_REMOVED: participant was removed by RemoveParticipant API
	* - ROOM_DELETED: the room has ended via DeleteRoom API
	*
	* args: ([[DisconnectReason]])
	*/
	RoomEvent["Disconnected"] = "disconnected";
	/**
	* Whenever the connection state of the room changes
	*
	* args: ([[ConnectionState]])
	*/
	RoomEvent["ConnectionStateChanged"] = "connectionStateChanged";
	/**
	* When participant has been moved to a different room by the service request.
	* The behavior looks like the participant has been disconnected and reconnected to a different room
	* seamlessly without connection state transition.
	* A new token will be provided for reconnecting to the new room if needed.
	*
	* args: ([[room: string, token: string]])
	*/
	RoomEvent["Moved"] = "moved";
	/**
	* When input or output devices on the machine have changed.
	*/
	RoomEvent["MediaDevicesChanged"] = "mediaDevicesChanged";
	/**
	* When a [[RemoteParticipant]] joins *after* the local
	* participant. It will not emit events for participants that are already
	* in the room
	*
	* args: ([[RemoteParticipant]])
	*/
	RoomEvent["ParticipantConnected"] = "participantConnected";
	/**
	* When a [[RemoteParticipant]] leaves *after* the local
	* participant has joined.
	*
	* args: ([[RemoteParticipant]])
	*/
	RoomEvent["ParticipantDisconnected"] = "participantDisconnected";
	/**
	* When a new track is published to room *after* the local
	* participant has joined. It will not fire for tracks that are already published.
	*
	* A track published doesn't mean the participant has subscribed to it. It's
	* simply reflecting the state of the room.
	*
	* args: ([[RemoteTrackPublication]], [[RemoteParticipant]])
	*/
	RoomEvent["TrackPublished"] = "trackPublished";
	/**
	* The [[LocalParticipant]] has subscribed to a new track. This event will **always**
	* fire as long as new tracks are ready for use.
	*
	* args: ([[RemoteTrack]], [[RemoteTrackPublication]], [[RemoteParticipant]])
	*/
	RoomEvent["TrackSubscribed"] = "trackSubscribed";
	/**
	* Could not subscribe to a track
	*
	* args: (track sid, [[RemoteParticipant]])
	*/
	RoomEvent["TrackSubscriptionFailed"] = "trackSubscriptionFailed";
	/**
	* A [[RemoteParticipant]] has unpublished a track
	*
	* args: ([[RemoteTrackPublication]], [[RemoteParticipant]])
	*/
	RoomEvent["TrackUnpublished"] = "trackUnpublished";
	/**
	* A subscribed track is no longer available. Clients should listen to this
	* event and ensure they detach tracks.
	*
	* args: ([[Track]], [[RemoteTrackPublication]], [[RemoteParticipant]])
	*/
	RoomEvent["TrackUnsubscribed"] = "trackUnsubscribed";
	/**
	* A track that was muted, fires on both [[RemoteParticipant]]s and [[LocalParticipant]]
	*
	* args: ([[TrackPublication]], [[Participant]])
	*/
	RoomEvent["TrackMuted"] = "trackMuted";
	/**
	* A track that was unmuted, fires on both [[RemoteParticipant]]s and [[LocalParticipant]]
	*
	* args: ([[TrackPublication]], [[Participant]])
	*/
	RoomEvent["TrackUnmuted"] = "trackUnmuted";
	/**
	* A local track was published successfully. This event is helpful to know
	* when to update your local UI with the newly published track.
	*
	* args: ([[LocalTrackPublication]], [[LocalParticipant]])
	*/
	RoomEvent["LocalTrackPublished"] = "localTrackPublished";
	/**
	* A local track was unpublished. This event is helpful to know when to remove
	* the local track from your UI.
	*
	* When a user stops sharing their screen by pressing "End" on the browser UI,
	* this event will also fire.
	*
	* args: ([[LocalTrackPublication]], [[LocalParticipant]])
	*/
	RoomEvent["LocalTrackUnpublished"] = "localTrackUnpublished";
	/**
	* When a local audio track is published the SDK checks whether there is complete silence
	* on that track and emits the LocalAudioSilenceDetected event in that case.
	* This allows for applications to show UI informing users that they might have to
	* reset their audio hardware or check for proper device connectivity.
	*/
	RoomEvent["LocalAudioSilenceDetected"] = "localAudioSilenceDetected";
	/**
	* Active speakers changed. List of speakers are ordered by their audio level.
	* loudest speakers first. This will include the LocalParticipant too.
	*
	* Speaker updates are sent only to the publishing participant and their subscribers.
	*
	* args: (Array<[[Participant]]>)
	*/
	RoomEvent["ActiveSpeakersChanged"] = "activeSpeakersChanged";
	/**
	* Participant metadata is a simple way for app-specific state to be pushed to
	* all users.
	* When RoomService.UpdateParticipantMetadata is called to change a participant's
	* state, *all*  participants in the room will fire this event.
	*
	* args: (prevMetadata: string, [[Participant]])
	*
	*/
	RoomEvent["ParticipantMetadataChanged"] = "participantMetadataChanged";
	/**
	* Participant's display name changed
	*
	* args: (name: string, [[Participant]])
	*
	*/
	RoomEvent["ParticipantNameChanged"] = "participantNameChanged";
	/**
	* Participant attributes is an app-specific key value state to be pushed to
	* all users.
	* When a participant's attributes changed, this event will be emitted with the changed attributes and the participant
	* args: (changedAttributes: [[Record<string, string]], participant: [[Participant]])
	*/
	RoomEvent["ParticipantAttributesChanged"] = "participantAttributesChanged";
	/**
	* Emitted when the participant's state changes to ACTIVE and is ready to send/receive data messages
	*
	* args: (participant: [[Participant]])
	*/
	RoomEvent["ParticipantActive"] = "participantActive";
	/**
	* Room metadata is a simple way for app-specific state to be pushed to
	* all users.
	* When RoomService.UpdateRoomMetadata is called to change a room's state,
	* *all*  participants in the room will fire this event.
	*
	* args: (string)
	*/
	RoomEvent["RoomMetadataChanged"] = "roomMetadataChanged";
	/**
	* Data received from another participant.
	* Data packets provides the ability to use LiveKit to send/receive arbitrary payloads.
	* All participants in the room will receive the messages sent to the room.
	*
	* args: (payload: NonSharedUint8Array, participant: [[Participant]], kind: [[DataPacket_Kind]], topic?: string)
	*/
	RoomEvent["DataReceived"] = "dataReceived";
	/**
	* SIP DTMF tones received from another participant.
	*
	* args: (participant: [[Participant]], dtmf: [[DataPacket_Kind]])
	*/
	RoomEvent["SipDTMFReceived"] = "sipDTMFReceived";
	/**
	* Transcription received from a participant's track.
	* @beta
	*/
	RoomEvent["TranscriptionReceived"] = "transcriptionReceived";
	/**
	* Connection quality was changed for a Participant. It'll receive updates
	* from the local participant, as well as any [[RemoteParticipant]]s that we are
	* subscribed to.
	*
	* args: (connectionQuality: [[ConnectionQuality]], participant: [[Participant]])
	*/
	RoomEvent["ConnectionQualityChanged"] = "connectionQualityChanged";
	/**
	* StreamState indicates if a subscribed (remote) track has been paused by the SFU
	* (typically this happens because of subscriber's bandwidth constraints)
	*
	* When bandwidth conditions allow, the track will be resumed automatically.
	* TrackStreamStateChanged will also be emitted when that happens.
	*
	* args: (pub: [[RemoteTrackPublication]], streamState: [[Track.StreamState]],
	*        participant: [[RemoteParticipant]])
	*/
	RoomEvent["TrackStreamStateChanged"] = "trackStreamStateChanged";
	/**
	* One of subscribed tracks have changed its permissions for the current
	* participant. If permission was revoked, then the track will no longer
	* be subscribed. If permission was granted, a TrackSubscribed event will
	* be emitted.
	*
	* args: (pub: [[RemoteTrackPublication]],
	*        status: [[TrackPublication.PermissionStatus]],
	*        participant: [[RemoteParticipant]])
	*/
	RoomEvent["TrackSubscriptionPermissionChanged"] = "trackSubscriptionPermissionChanged";
	/**
	* One of subscribed tracks have changed its status for the current
	* participant.
	*
	* args: (pub: [[RemoteTrackPublication]],
	*        status: [[TrackPublication.SubscriptionStatus]],
	*        participant: [[RemoteParticipant]])
	*/
	RoomEvent["TrackSubscriptionStatusChanged"] = "trackSubscriptionStatusChanged";
	/**
	* LiveKit will attempt to autoplay all audio tracks when you attach them to
	* audio elements. However, if that fails, we'll notify you via AudioPlaybackStatusChanged.
	* `Room.canPlaybackAudio` will indicate if audio playback is permitted.
	*/
	RoomEvent["AudioPlaybackStatusChanged"] = "audioPlaybackChanged";
	/**
	* LiveKit will attempt to autoplay all video tracks when you attach them to
	* a video element. However, if that fails, we'll notify you via VideoPlaybackStatusChanged.
	* Calling `room.startVideo()` in a user gesture event handler will resume the video playback.
	*/
	RoomEvent["VideoPlaybackStatusChanged"] = "videoPlaybackChanged";
	/**
	* When we have encountered an error while attempting to create a track.
	* The errors take place in getUserMedia().
	* Use MediaDeviceFailure.getFailure(error) to get the reason of failure.
	* [[LocalParticipant.lastCameraError]] and [[LocalParticipant.lastMicrophoneError]]
	* will indicate if it had an error while creating the audio or video track respectively.
	*
	* args: (error: Error)
	*/
	RoomEvent["MediaDevicesError"] = "mediaDevicesError";
	/**
	* A participant's permission has changed.
	* args: (prevPermissions: [[ParticipantPermission]], participant: [[Participant]])
	*/
	RoomEvent["ParticipantPermissionsChanged"] = "participantPermissionsChanged";
	/**
	* Signal connected, can publish tracks.
	*/
	RoomEvent["SignalConnected"] = "signalConnected";
	/**
	* Recording of a room has started/stopped. Room.isRecording will be updated too.
	* args: (isRecording: boolean)
	*/
	RoomEvent["RecordingStatusChanged"] = "recordingStatusChanged";
	RoomEvent["ParticipantEncryptionStatusChanged"] = "participantEncryptionStatusChanged";
	RoomEvent["EncryptionError"] = "encryptionError";
	/**
	* Emits whenever the current buffer status of a data channel changes
	* args: (isLow: boolean, kind: [[DataChannelKind]])
	*/
	RoomEvent["DCBufferStatusChanged"] = "dcBufferStatusChanged";
	/**
	* Triggered by a call to room.switchActiveDevice
	* args: (kind: MediaDeviceKind, deviceId: string)
	*/
	RoomEvent["ActiveDeviceChanged"] = "activeDeviceChanged";
	RoomEvent["ChatMessage"] = "chatMessage";
	/**
	* fired when the first remote participant has subscribed to the localParticipant's track
	*/
	RoomEvent["LocalTrackSubscribed"] = "localTrackSubscribed";
	/**
	* fired when the client receives connection metrics from other participants
	*/
	RoomEvent["MetricsReceived"] = "metricsReceived";
	/**
	* Emits when a new data track has been published by a downstream participant.
	*/
	RoomEvent["DataTrackPublished"] = "dataTrackPublished";
	/**
	* Emits when a new data track has been unpublished by a downstream participant.
	*/
	RoomEvent["DataTrackUnpublished"] = "dataTrackUnpublished";
	/**
	* Emits when a new data track has been published locally.
	*/
	RoomEvent["LocalDataTrackPublished"] = "localDataTrackPublished";
	/**
	* Emits when a new data track has been unpublished locally.
	*/
	RoomEvent["LocalDataTrackUnpublished"] = "localDataTrackUnpublished";
})(RoomEvent || (RoomEvent = {}));
var ParticipantEvent;
(function(ParticipantEvent) {
	/**
	* When a new track is published to room *after* the local
	* participant has joined. It will not fire for tracks that are already published.
	*
	* A track published doesn't mean the participant has subscribed to it. It's
	* simply reflecting the state of the room.
	*
	* args: ([[RemoteTrackPublication]])
	*/
	ParticipantEvent["TrackPublished"] = "trackPublished";
	/**
	* Successfully subscribed to the [[RemoteParticipant]]'s track.
	* This event will **always** fire as long as new tracks are ready for use.
	*
	* args: ([[RemoteTrack]], [[RemoteTrackPublication]])
	*/
	ParticipantEvent["TrackSubscribed"] = "trackSubscribed";
	/**
	* Could not subscribe to a track
	*
	* args: (track sid)
	*/
	ParticipantEvent["TrackSubscriptionFailed"] = "trackSubscriptionFailed";
	/**
	* A [[RemoteParticipant]] has unpublished a track
	*
	* args: ([[RemoteTrackPublication]])
	*/
	ParticipantEvent["TrackUnpublished"] = "trackUnpublished";
	/**
	* A subscribed track is no longer available. Clients should listen to this
	* event and ensure they detach tracks.
	*
	* args: ([[RemoteTrack]], [[RemoteTrackPublication]])
	*/
	ParticipantEvent["TrackUnsubscribed"] = "trackUnsubscribed";
	/**
	* A track that was muted, fires on both [[RemoteParticipant]]s and [[LocalParticipant]]
	*
	* args: ([[TrackPublication]])
	*/
	ParticipantEvent["TrackMuted"] = "trackMuted";
	/**
	* A track that was unmuted, fires on both [[RemoteParticipant]]s and [[LocalParticipant]]
	*
	* args: ([[TrackPublication]])
	*/
	ParticipantEvent["TrackUnmuted"] = "trackUnmuted";
	/**
	* A local track was published successfully. This event is helpful to know
	* when to update your local UI with the newly published track.
	*
	* args: ([[LocalTrackPublication]])
	*/
	ParticipantEvent["LocalTrackPublished"] = "localTrackPublished";
	/**
	* A local track was unpublished. This event is helpful to know when to remove
	* the local track from your UI.
	*
	* When a user stops sharing their screen by pressing "End" on the browser UI,
	* this event will also fire.
	*
	* args: ([[LocalTrackPublication]])
	*/
	ParticipantEvent["LocalTrackUnpublished"] = "localTrackUnpublished";
	/**
	* A local track has been constrained by cpu.
	* This event is useful to know when to reduce the capture resolution of the track.
	*
	* This event is emitted on the local participant.
	*
	* args: ([[LocalVideoTrack]], [[LocalTrackPublication]])
	*/
	ParticipantEvent["LocalTrackCpuConstrained"] = "localTrackCpuConstrained";
	/**
	* @internal
	*/
	ParticipantEvent["LocalSenderCreated"] = "localSenderCreated";
	/**
	* Participant metadata is a simple way for app-specific state to be pushed to
	* all users.
	* When RoomService.UpdateParticipantMetadata is called to change a participant's
	* state, *all*  participants in the room will fire this event.
	* To access the current metadata, see [[Participant.metadata]].
	*
	* args: (prevMetadata: string)
	*
	*/
	ParticipantEvent["ParticipantMetadataChanged"] = "participantMetadataChanged";
	/**
	* Participant's display name changed
	*
	* args: (name: string, [[Participant]])
	*
	*/
	ParticipantEvent["ParticipantNameChanged"] = "participantNameChanged";
	/**
	* Data received from this participant as sender.
	* Data packets provides the ability to use LiveKit to send/receive arbitrary payloads.
	* All participants in the room will receive the messages sent to the room.
	*
	* args: (payload: NonSharedUint8Array, kind: [[DataPacket_Kind]])
	*/
	ParticipantEvent["DataReceived"] = "dataReceived";
	/**
	* SIP DTMF tones received from this participant as sender.
	*
	* args: (dtmf: [[DataPacket_Kind]])
	*/
	ParticipantEvent["SipDTMFReceived"] = "sipDTMFReceived";
	/**
	* Transcription received from this participant as data source.
	* @beta
	*/
	ParticipantEvent["TranscriptionReceived"] = "transcriptionReceived";
	/**
	* Has speaking status changed for the current participant
	*
	* args: (speaking: boolean)
	*/
	ParticipantEvent["IsSpeakingChanged"] = "isSpeakingChanged";
	/**
	* Connection quality was changed for a Participant. It'll receive updates
	* from the local participant, as well as any [[RemoteParticipant]]s that we are
	* subscribed to.
	*
	* args: (connectionQuality: [[ConnectionQuality]])
	*/
	ParticipantEvent["ConnectionQualityChanged"] = "connectionQualityChanged";
	/**
	* StreamState indicates if a subscribed track has been paused by the SFU
	* (typically this happens because of subscriber's bandwidth constraints)
	*
	* When bandwidth conditions allow, the track will be resumed automatically.
	* TrackStreamStateChanged will also be emitted when that happens.
	*
	* args: (pub: [[RemoteTrackPublication]], streamState: [[Track.StreamState]])
	*/
	ParticipantEvent["TrackStreamStateChanged"] = "trackStreamStateChanged";
	/**
	* One of subscribed tracks have changed its permissions for the current
	* participant. If permission was revoked, then the track will no longer
	* be subscribed. If permission was granted, a TrackSubscribed event will
	* be emitted.
	*
	* args: (pub: [[RemoteTrackPublication]],
	*        status: [[TrackPublication.SubscriptionStatus]])
	*/
	ParticipantEvent["TrackSubscriptionPermissionChanged"] = "trackSubscriptionPermissionChanged";
	/**
	* One of the remote participants publications has changed its subscription status.
	*
	*/
	ParticipantEvent["TrackSubscriptionStatusChanged"] = "trackSubscriptionStatusChanged";
	/**
	* a local track has been constrained by cpu
	*/
	ParticipantEvent["TrackCpuConstrained"] = "trackCpuConstrained";
	/** @internal */
	ParticipantEvent["MediaDevicesError"] = "mediaDevicesError";
	/** @internal */
	ParticipantEvent["AudioStreamAcquired"] = "audioStreamAcquired";
	/**
	* A participant's permission has changed.
	* args: (prevPermissions: [[ParticipantPermission]])
	*/
	ParticipantEvent["ParticipantPermissionsChanged"] = "participantPermissionsChanged";
	/** @internal */
	ParticipantEvent["PCTrackAdded"] = "pcTrackAdded";
	/**
	* Participant attributes is an app-specific key value state to be pushed to
	* all users.
	* When a participant's attributes changed, this event will be emitted with the changed attributes
	* args: (changedAttributes: [[Record<string, string]])
	*/
	ParticipantEvent["AttributesChanged"] = "attributesChanged";
	/**
	* fired on local participant only, when the first remote participant has subscribed to the track specified in the payload
	*/
	ParticipantEvent["LocalTrackSubscribed"] = "localTrackSubscribed";
	/** only emitted on local participant */
	ParticipantEvent["ChatMessage"] = "chatMessage";
	/**
	* Emitted when the participant's state changes to ACTIVE and is ready to send/receive data messages
	*/
	ParticipantEvent["Active"] = "active";
})(ParticipantEvent || (ParticipantEvent = {}));
/** @internal */
var EngineEvent;
(function(EngineEvent) {
	EngineEvent["TransportsCreated"] = "transportsCreated";
	EngineEvent["Connected"] = "connected";
	EngineEvent["Disconnected"] = "disconnected";
	EngineEvent["Resuming"] = "resuming";
	EngineEvent["Resumed"] = "resumed";
	EngineEvent["Restarting"] = "restarting";
	EngineEvent["Restarted"] = "restarted";
	EngineEvent["SignalResumed"] = "signalResumed";
	EngineEvent["SignalRestarted"] = "signalRestarted";
	EngineEvent["Closing"] = "closing";
	EngineEvent["MediaTrackAdded"] = "mediaTrackAdded";
	EngineEvent["ActiveSpeakersUpdate"] = "activeSpeakersUpdate";
	EngineEvent["DataPacketReceived"] = "dataPacketReceived";
	EngineEvent["RTPVideoMapUpdate"] = "rtpVideoMapUpdate";
	EngineEvent["DCBufferStatusChanged"] = "dcBufferStatusChanged";
	EngineEvent["ParticipantUpdate"] = "participantUpdate";
	EngineEvent["RoomUpdate"] = "roomUpdate";
	EngineEvent["SpeakersChanged"] = "speakersChanged";
	EngineEvent["StreamStateChanged"] = "streamStateChanged";
	EngineEvent["ConnectionQualityUpdate"] = "connectionQualityUpdate";
	EngineEvent["SubscriptionError"] = "subscriptionError";
	EngineEvent["SubscriptionPermissionUpdate"] = "subscriptionPermissionUpdate";
	EngineEvent["RemoteMute"] = "remoteMute";
	EngineEvent["SubscribedQualityUpdate"] = "subscribedQualityUpdate";
	EngineEvent["LocalTrackUnpublished"] = "localTrackUnpublished";
	EngineEvent["LocalTrackSubscribed"] = "localTrackSubscribed";
	EngineEvent["Offline"] = "offline";
	EngineEvent["SignalRequestResponse"] = "signalRequestResponse";
	EngineEvent["SignalConnected"] = "signalConnected";
	EngineEvent["RoomMoved"] = "roomMoved";
	EngineEvent["PublishDataTrackResponse"] = "publishDataTrackResponse";
	EngineEvent["UnPublishDataTrackResponse"] = "unPublishDataTrackResponse";
	EngineEvent["DataTrackSubscriberHandles"] = "dataTrackSubscriberHandles";
	EngineEvent["DataTrackPacketReceived"] = "dataTrackPacketReceived";
	EngineEvent["Joined"] = "joined";
	EngineEvent["TokenRefreshed"] = "tokenRefreshed";
	EngineEvent["ServerRegionsReported"] = "serverRegionsReported";
})(EngineEvent || (EngineEvent = {}));
var TrackEvent;
(function(TrackEvent) {
	TrackEvent["Message"] = "message";
	TrackEvent["Muted"] = "muted";
	TrackEvent["Unmuted"] = "unmuted";
	/**
	* Only fires on LocalTracks
	*/
	TrackEvent["Restarted"] = "restarted";
	TrackEvent["Ended"] = "ended";
	TrackEvent["Subscribed"] = "subscribed";
	TrackEvent["Unsubscribed"] = "unsubscribed";
	TrackEvent["CpuConstrained"] = "cpuConstrained";
	/** @internal */
	TrackEvent["UpdateSettings"] = "updateSettings";
	/** @internal */
	TrackEvent["UpdateSubscription"] = "updateSubscription";
	/** @internal */
	TrackEvent["AudioPlaybackStarted"] = "audioPlaybackStarted";
	/** @internal */
	TrackEvent["AudioPlaybackFailed"] = "audioPlaybackFailed";
	/**
	* @internal
	* Only fires on LocalAudioTrack instances
	*/
	TrackEvent["AudioSilenceDetected"] = "audioSilenceDetected";
	/** @internal */
	TrackEvent["VisibilityChanged"] = "visibilityChanged";
	/** @internal */
	TrackEvent["VideoDimensionsChanged"] = "videoDimensionsChanged";
	/** @internal */
	TrackEvent["VideoPlaybackStarted"] = "videoPlaybackStarted";
	/** @internal */
	TrackEvent["VideoPlaybackFailed"] = "videoPlaybackFailed";
	/** @internal */
	TrackEvent["ElementAttached"] = "elementAttached";
	/** @internal */
	TrackEvent["ElementDetached"] = "elementDetached";
	/**
	* @internal
	* Only fires on LocalTracks
	*/
	TrackEvent["UpstreamPaused"] = "upstreamPaused";
	/**
	* @internal
	* Only fires on LocalTracks
	*/
	TrackEvent["UpstreamResumed"] = "upstreamResumed";
	/**
	* @internal
	* Fires on RemoteTrackPublication
	*/
	TrackEvent["SubscriptionPermissionChanged"] = "subscriptionPermissionChanged";
	/**
	* Fires on RemoteTrackPublication
	*/
	TrackEvent["SubscriptionStatusChanged"] = "subscriptionStatusChanged";
	/**
	* Fires on RemoteTrackPublication
	*/
	TrackEvent["SubscriptionFailed"] = "subscriptionFailed";
	/**
	* @internal
	*/
	TrackEvent["TrackProcessorUpdate"] = "trackProcessorUpdate";
	/**
	* @internal
	*/
	TrackEvent["AudioTrackFeatureUpdate"] = "audioTrackFeatureUpdate";
	/**
	* @beta
	*/
	TrackEvent["TranscriptionReceived"] = "transcriptionReceived";
	/**
	* @experimental
	*/
	TrackEvent["TimeSyncUpdate"] = "timeSyncUpdate";
	/**
	* @internal
	*/
	TrackEvent["PreConnectBufferFlushed"] = "preConnectBufferFlushed";
})(TrackEvent || (TrackEvent = {}));
function cloneDeep(value) {
	if (typeof value === "undefined") return value;
	if (typeof structuredClone === "function") {
		if (typeof value === "object" && value !== null) return structuredClone(Object.assign({}, value));
		return structuredClone(value);
	} else return JSON.parse(JSON.stringify(value));
}
var VideoPreset = class {
	constructor(widthOrOptions, height, maxBitrate, maxFramerate, priority) {
		if (typeof widthOrOptions === "object") {
			this.width = widthOrOptions.width;
			this.height = widthOrOptions.height;
			this.aspectRatio = widthOrOptions.aspectRatio;
			this.encoding = {
				maxBitrate: widthOrOptions.maxBitrate,
				maxFramerate: widthOrOptions.maxFramerate,
				priority: widthOrOptions.priority
			};
		} else if (height !== void 0 && maxBitrate !== void 0) {
			this.width = widthOrOptions;
			this.height = height;
			this.aspectRatio = widthOrOptions / height;
			this.encoding = {
				maxBitrate,
				maxFramerate,
				priority
			};
		} else throw new TypeError("Unsupported options: provide at least width, height and maxBitrate");
	}
	get resolution() {
		return {
			width: this.width,
			height: this.height,
			frameRate: this.encoding.maxFramerate,
			aspectRatio: this.aspectRatio
		};
	}
};
var backupVideoCodecs = ["vp8", "h264"];
var videoCodecs = [
	"vp8",
	"h264",
	"vp9",
	"av1",
	"h265"
];
function isBackupVideoCodec(codec) {
	return !!backupVideoCodecs.find((backup) => backup === codec);
}
/** @deprecated Use {@link isBackupVideoCodec} instead */
var isBackupCodec = isBackupVideoCodec;
var BackupCodecPolicy;
(function(BackupCodecPolicy) {
	BackupCodecPolicy[BackupCodecPolicy["PREFER_REGRESSION"] = 0] = "PREFER_REGRESSION";
	BackupCodecPolicy[BackupCodecPolicy["SIMULCAST"] = 1] = "SIMULCAST";
	BackupCodecPolicy[BackupCodecPolicy["REGRESSION"] = 2] = "REGRESSION";
})(BackupCodecPolicy || (BackupCodecPolicy = {}));
var AudioPresets;
(function(AudioPresets) {
	AudioPresets.telephone = { maxBitrate: 12e3 };
	AudioPresets.speech = { maxBitrate: 24e3 };
	AudioPresets.music = { maxBitrate: 48e3 };
	AudioPresets.musicStereo = { maxBitrate: 64e3 };
	AudioPresets.musicHighQuality = { maxBitrate: 96e3 };
	AudioPresets.musicHighQualityStereo = { maxBitrate: 128e3 };
})(AudioPresets || (AudioPresets = {}));
/**
* Sane presets for video resolution/encoding
*/
var VideoPresets = {
	h90: new VideoPreset(160, 90, 9e4, 20),
	h180: new VideoPreset(320, 180, 16e4, 20),
	h216: new VideoPreset(384, 216, 18e4, 20),
	h360: new VideoPreset(640, 360, 45e4, 20),
	h540: new VideoPreset(960, 540, 8e5, 25),
	h720: new VideoPreset(1280, 720, 17e5, 30),
	h1080: new VideoPreset(1920, 1080, 3e6, 30),
	h1440: new VideoPreset(2560, 1440, 5e6, 30),
	h2160: new VideoPreset(3840, 2160, 8e6, 30)
};
/**
* Four by three presets
*/
var VideoPresets43 = {
	h120: new VideoPreset(160, 120, 7e4, 20),
	h180: new VideoPreset(240, 180, 125e3, 20),
	h240: new VideoPreset(320, 240, 14e4, 20),
	h360: new VideoPreset(480, 360, 33e4, 20),
	h480: new VideoPreset(640, 480, 5e5, 20),
	h540: new VideoPreset(720, 540, 6e5, 25),
	h720: new VideoPreset(960, 720, 13e5, 30),
	h1080: new VideoPreset(1440, 1080, 23e5, 30),
	h1440: new VideoPreset(1920, 1440, 38e5, 30)
};
var ScreenSharePresets = {
	h360fps3: new VideoPreset(640, 360, 2e5, 3, "medium"),
	h360fps15: new VideoPreset(640, 360, 4e5, 15, "medium"),
	h720fps5: new VideoPreset(1280, 720, 8e5, 5, "medium"),
	h720fps15: new VideoPreset(1280, 720, 15e5, 15, "medium"),
	h720fps30: new VideoPreset(1280, 720, 2e6, 30, "medium"),
	h1080fps15: new VideoPreset(1920, 1080, 25e5, 15, "medium"),
	h1080fps30: new VideoPreset(1920, 1080, 5e6, 30, "medium"),
	original: new VideoPreset(0, 0, 7e6, 30, "medium")
};
function mergeDefaultOptions(options, audioDefaults, videoDefaults) {
	var _a, _b;
	var _c, _d;
	const _extractProcessorsFro = extractProcessorsFromOptions(options !== null && options !== void 0 ? options : {}), optionsWithoutProcessor = _extractProcessorsFro.optionsWithoutProcessor, audioProcessor = _extractProcessorsFro.audioProcessor, videoProcessor = _extractProcessorsFro.videoProcessor;
	const defaultAudioProcessor = audioDefaults === null || audioDefaults === void 0 ? void 0 : audioDefaults.processor;
	const defaultVideoProcessor = videoDefaults === null || videoDefaults === void 0 ? void 0 : videoDefaults.processor;
	const clonedOptions = optionsWithoutProcessor !== null && optionsWithoutProcessor !== void 0 ? optionsWithoutProcessor : {};
	if (clonedOptions.audio === true) clonedOptions.audio = {};
	if (clonedOptions.video === true) clonedOptions.video = {};
	if (clonedOptions.audio) {
		mergeObjectWithoutOverwriting(clonedOptions.audio, audioDefaults);
		(_a = (_c = clonedOptions.audio).deviceId) !== null && _a !== void 0 || (_c.deviceId = { ideal: "default" });
		if (audioProcessor || defaultAudioProcessor) clonedOptions.audio.processor = audioProcessor !== null && audioProcessor !== void 0 ? audioProcessor : defaultAudioProcessor;
	}
	if (clonedOptions.video) {
		mergeObjectWithoutOverwriting(clonedOptions.video, videoDefaults);
		(_b = (_d = clonedOptions.video).deviceId) !== null && _b !== void 0 || (_d.deviceId = { ideal: "default" });
		if (videoProcessor || defaultVideoProcessor) clonedOptions.video.processor = videoProcessor !== null && videoProcessor !== void 0 ? videoProcessor : defaultVideoProcessor;
	}
	return clonedOptions;
}
function mergeObjectWithoutOverwriting(mainObject, objectToMerge) {
	Object.keys(objectToMerge).forEach((key) => {
		if (mainObject[key] === void 0) mainObject[key] = objectToMerge[key];
	});
	return mainObject;
}
function constraintsForOptions(options) {
	var _a, _b;
	var _c, _d;
	const constraints = {};
	if (options.video) {
		if (typeof options.video === "object") {
			const videoOptions = {};
			const target = videoOptions;
			const source = options.video;
			Object.keys(source).forEach((key) => {
				switch (key) {
					case "resolution":
						mergeObjectWithoutOverwriting(target, source.resolution);
						break;
					default: target[key] = source[key];
				}
			});
			constraints.video = videoOptions;
			(_a = (_c = constraints.video).deviceId) !== null && _a !== void 0 || (_c.deviceId = { ideal: "default" });
		} else constraints.video = options.video ? { deviceId: { ideal: "default" } } : false;
	} else constraints.video = false;
	if (options.audio) {
		if (typeof options.audio === "object") {
			constraints.audio = options.audio;
			(_b = (_d = constraints.audio).deviceId) !== null && _b !== void 0 || (_d.deviceId = { ideal: "default" });
		} else constraints.audio = { deviceId: { ideal: "default" } };
	} else constraints.audio = false;
	return constraints;
}
/**
* This function detects silence on a given [[Track]] instance.
* Returns true if the track seems to be entirely silent.
*/
function detectSilence(track_1) {
	return __awaiter(this, arguments, void 0, function(track) {
		let timeOffset = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 200;
		return function* () {
			const ctx = getNewAudioContext();
			if (ctx) {
				const analyser = ctx.createAnalyser();
				analyser.fftSize = 2048;
				const bufferLength = analyser.frequencyBinCount;
				const dataArray = new Uint8Array(bufferLength);
				ctx.createMediaStreamSource(new MediaStream([track.mediaStreamTrack])).connect(analyser);
				yield sleep(timeOffset);
				analyser.getByteTimeDomainData(dataArray);
				const someNoise = dataArray.some((sample) => sample !== 128 && sample !== 0);
				ctx.close();
				return !someNoise;
			}
			return false;
		}();
	});
}
/**
* @internal
*/
function getNewAudioContext() {
	var _a;
	const AudioContext = typeof window !== "undefined" && (window.AudioContext || window.webkitAudioContext);
	if (AudioContext) {
		const audioContext = new AudioContext({ latencyHint: "interactive" });
		if (audioContext.state === "suspended" && typeof window !== "undefined" && ((_a = window.document) === null || _a === void 0 ? void 0 : _a.body)) {
			const handleResume = () => __awaiter(this, void 0, void 0, function* () {
				var _a;
				try {
					if (audioContext.state === "suspended") yield audioContext.resume();
				} catch (e) {
					console.warn("Error trying to auto-resume audio context", e);
				} finally {
					(_a = window.document.body) === null || _a === void 0 || _a.removeEventListener("click", handleResume);
				}
			});
			audioContext.addEventListener("statechange", () => {
				var _a;
				if (audioContext.state === "closed") (_a = window.document.body) === null || _a === void 0 || _a.removeEventListener("click", handleResume);
			});
			window.document.body.addEventListener("click", handleResume);
		}
		return audioContext;
	}
}
/**
* @internal
*/
function kindToSource(kind) {
	if (kind === "audioinput") return Track.Source.Microphone;
	else if (kind === "videoinput") return Track.Source.Camera;
	else return Track.Source.Unknown;
}
/**
* @internal
*/
function sourceToKind(source) {
	if (source === Track.Source.Microphone) return "audioinput";
	else if (source === Track.Source.Camera) return "videoinput";
	else return;
}
/**
* @internal
*/
function screenCaptureToDisplayMediaStreamOptions(options) {
	var _a, _b;
	let videoConstraints = (_a = options.video) !== null && _a !== void 0 ? _a : true;
	if (options.resolution && options.resolution.width > 0 && options.resolution.height > 0) {
		videoConstraints = typeof videoConstraints === "boolean" ? {} : videoConstraints;
		if (isSafari()) videoConstraints = Object.assign(Object.assign({}, videoConstraints), {
			width: { max: options.resolution.width },
			height: { max: options.resolution.height },
			frameRate: options.resolution.frameRate
		});
		else videoConstraints = Object.assign(Object.assign({}, videoConstraints), {
			width: { ideal: options.resolution.width },
			height: { ideal: options.resolution.height },
			frameRate: options.resolution.frameRate
		});
	}
	return {
		audio: (_b = options.audio) !== null && _b !== void 0 ? _b : false,
		video: videoConstraints,
		controller: options.controller,
		selfBrowserSurface: options.selfBrowserSurface,
		surfaceSwitching: options.surfaceSwitching,
		systemAudio: options.systemAudio,
		preferCurrentTab: options.preferCurrentTab
	};
}
function mimeTypeToVideoCodecString(mimeType) {
	return mimeType.split("/")[1].toLowerCase();
}
function getTrackPublicationInfo(tracks) {
	const infos = [];
	tracks.forEach((track) => {
		if (track.track !== void 0) infos.push(new TrackPublishedResponse({
			cid: track.track.mediaStreamID,
			track: track.trackInfo
		}));
	});
	return infos;
}
function getLogContextFromTrack(track) {
	if ("mediaStreamTrack" in track) return {
		trackID: track.sid,
		source: track.source,
		muted: track.isMuted,
		enabled: track.mediaStreamTrack.enabled,
		kind: track.kind,
		streamID: track.mediaStreamID,
		streamTrackID: track.mediaStreamTrack.id
	};
	else return {
		trackID: track.trackSid,
		enabled: track.isEnabled,
		muted: track.isMuted,
		trackInfo: Object.assign({
			mimeType: track.mimeType,
			name: track.trackName,
			encrypted: track.isEncrypted,
			kind: track.kind,
			source: track.source
		}, track.track ? getLogContextFromTrack(track.track) : {})
	};
}
function supportsSynchronizationSources() {
	return typeof RTCRtpReceiver !== "undefined" && typeof RTCRtpReceiver.prototype.getSynchronizationSources === "function";
}
function diffAttributes(oldValues, newValues) {
	var _a;
	if (oldValues === void 0) oldValues = {};
	if (newValues === void 0) newValues = {};
	const allKeys = [...Object.keys(newValues), ...Object.keys(oldValues)];
	const diff = {};
	for (const key of allKeys) if (oldValues[key] !== newValues[key]) diff[key] = (_a = newValues[key]) !== null && _a !== void 0 ? _a : "";
	return diff;
}
/** @internal */
function extractProcessorsFromOptions(options) {
	const newOptions = Object.assign({}, options);
	let audioProcessor;
	let videoProcessor;
	if (typeof newOptions.audio === "object" && newOptions.audio.processor) {
		audioProcessor = newOptions.audio.processor;
		newOptions.audio = Object.assign(Object.assign({}, newOptions.audio), { processor: void 0 });
	}
	if (typeof newOptions.video === "object" && newOptions.video.processor) {
		videoProcessor = newOptions.video.processor;
		newOptions.video = Object.assign(Object.assign({}, newOptions.video), { processor: void 0 });
	}
	return {
		audioProcessor,
		videoProcessor,
		optionsWithoutProcessor: cloneDeep(newOptions)
	};
}
function getTrackSourceFromProto(source) {
	switch (source) {
		case TrackSource.CAMERA: return Track.Source.Camera;
		case TrackSource.MICROPHONE: return Track.Source.Microphone;
		case TrackSource.SCREEN_SHARE: return Track.Source.ScreenShare;
		case TrackSource.SCREEN_SHARE_AUDIO: return Track.Source.ScreenShareAudio;
		default: return Track.Source.Unknown;
	}
}
function areDimensionsSmaller(a, b) {
	return a.width * a.height < b.width * b.height;
}
function layerDimensionsFor(trackInfo, quality) {
	var _a;
	return (_a = trackInfo.layers) === null || _a === void 0 ? void 0 : _a.find((l) => l.quality === quality);
}
var BACKGROUND_REACTION_DELAY = 5e3;
var recycledElements = [];
var VideoQuality;
(function(VideoQuality) {
	VideoQuality[VideoQuality["LOW"] = 0] = "LOW";
	VideoQuality[VideoQuality["MEDIUM"] = 1] = "MEDIUM";
	VideoQuality[VideoQuality["HIGH"] = 2] = "HIGH";
})(VideoQuality || (VideoQuality = {}));
var Track = class Track extends eventsExports.EventEmitter {
	/**
	* indicates current state of stream, it'll indicate `paused` if the track
	* has been paused by congestion controller
	*/
	get streamState() {
		return this._streamState;
	}
	/** @internal */
	setStreamState(value) {
		if (this._streamState !== value) this.log.debug("stream state changed: ".concat(this._streamState, " -> ").concat(value));
		this._streamState = value;
	}
	constructor(mediaTrack, kind) {
		let loggerOptions = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
		var _a;
		super();
		this.attachedElements = [];
		this.isMuted = false;
		this._streamState = Track.StreamState.Active;
		this.isInBackground = false;
		this._currentBitrate = 0;
		this.log = livekitLogger;
		this.appVisibilityChangedListener = () => {
			if (this.backgroundTimeout) clearTimeout(this.backgroundTimeout);
			if (document.visibilityState === "hidden") this.backgroundTimeout = setTimeout(() => this.handleAppVisibilityChanged(), BACKGROUND_REACTION_DELAY);
			else this.handleAppVisibilityChanged();
		};
		this.loggerContextCb = loggerOptions.loggerContextCb;
		this.log = getLogger((_a = loggerOptions.loggerName) !== null && _a !== void 0 ? _a : LoggerNames.Track, () => this.logContext);
		this.setMaxListeners(100);
		this.kind = kind;
		this._mediaStreamTrack = mediaTrack;
		this._mediaStreamID = mediaTrack.id;
		this.source = Track.Source.Unknown;
	}
	get logContext() {
		var _a;
		return Object.assign(Object.assign({}, (_a = this.loggerContextCb) === null || _a === void 0 ? void 0 : _a.call(this)), getLogContextFromTrack(this));
	}
	/** current receive bits per second */
	get currentBitrate() {
		return this._currentBitrate;
	}
	get mediaStreamTrack() {
		return this._mediaStreamTrack;
	}
	/**
	* @internal
	* used for keep mediaStream's first id, since it's id might change
	* if we disable/enable a track
	*/
	get mediaStreamID() {
		return this._mediaStreamID;
	}
	attach(element) {
		let elementType = "audio";
		if (this.kind === Track.Kind.Video) elementType = "video";
		if (this.attachedElements.length === 0 && this.kind === Track.Kind.Video) this.addAppVisibilityListener();
		if (!element) {
			if (elementType === "audio") {
				recycledElements.forEach((e) => {
					if (e.parentElement === null && !element) element = e;
				});
				if (element) recycledElements.splice(recycledElements.indexOf(element), 1);
			}
			if (!element) element = document.createElement(elementType);
		}
		if (!this.attachedElements.includes(element)) this.attachedElements.push(element);
		attachToElement(this.mediaStreamTrack, element);
		const allMediaStreamTracks = element.srcObject.getTracks();
		const hasAudio = allMediaStreamTracks.some((tr) => tr.kind === "audio");
		element.play().then(() => {
			this.emit(hasAudio ? TrackEvent.AudioPlaybackStarted : TrackEvent.VideoPlaybackStarted);
		}).catch((e) => {
			if (e.name === "NotAllowedError") this.emit(hasAudio ? TrackEvent.AudioPlaybackFailed : TrackEvent.VideoPlaybackFailed, e);
			else if (e.name === "AbortError") this.log.debug("".concat(hasAudio ? "audio" : "video", " playback aborted, likely due to new play request"));
			else this.log.warn("could not playback ".concat(hasAudio ? "audio" : "video"), { error: e });
			if (hasAudio && element && allMediaStreamTracks.some((tr) => tr.kind === "video") && e.name === "NotAllowedError") {
				element.muted = true;
				element.play().catch(() => {});
			}
		});
		this.emit(TrackEvent.ElementAttached, element);
		return element;
	}
	detach(element) {
		try {
			if (element) {
				detachTrack(this.mediaStreamTrack, element);
				const idx = this.attachedElements.indexOf(element);
				if (idx >= 0) {
					this.attachedElements.splice(idx, 1);
					this.recycleElement(element);
					this.emit(TrackEvent.ElementDetached, element);
				}
				return element;
			}
			const detached = [];
			this.attachedElements.forEach((elm) => {
				detachTrack(this.mediaStreamTrack, elm);
				detached.push(elm);
				this.recycleElement(elm);
				this.emit(TrackEvent.ElementDetached, elm);
			});
			this.attachedElements = [];
			return detached;
		} finally {
			if (this.attachedElements.length === 0) this.removeAppVisibilityListener();
		}
	}
	stop() {
		this.log.debug("stopping track");
		this.stopMonitor();
		this._mediaStreamTrack.stop();
	}
	enable() {
		this._mediaStreamTrack.enabled = true;
	}
	disable() {
		this._mediaStreamTrack.enabled = false;
	}
	stopMonitor() {
		if (this.monitorInterval) clearInterval(this.monitorInterval);
		if (this.timeSyncHandle !== void 0) {
			cancelAnimationFrame(this.timeSyncHandle);
			this.timeSyncHandle = void 0;
		}
	}
	/** @internal */
	updateLoggerOptions(loggerOptions) {
		if (loggerOptions.loggerContextCb) this.loggerContextCb = loggerOptions.loggerContextCb;
		if (loggerOptions.loggerName) this.log = getLogger(loggerOptions.loggerName, () => this.logContext);
	}
	recycleElement(element) {
		if (element instanceof HTMLAudioElement) {
			let shouldCache = true;
			element.pause();
			recycledElements.forEach((e) => {
				if (!e.parentElement) shouldCache = false;
			});
			if (shouldCache) recycledElements.push(element);
		}
	}
	handleAppVisibilityChanged() {
		return __awaiter(this, void 0, void 0, function* () {
			this.isInBackground = document.visibilityState === "hidden";
			if (!this.isInBackground && this.kind === Track.Kind.Video) setTimeout(() => this.attachedElements.forEach((el) => el.play().catch(() => {
				/** catch clause necessary for Safari */
			})), 0);
		});
	}
	addAppVisibilityListener() {
		if (isWeb()) {
			this.isInBackground = document.visibilityState === "hidden";
			document.addEventListener("visibilitychange", this.appVisibilityChangedListener);
		} else this.isInBackground = false;
	}
	removeAppVisibilityListener() {
		if (isWeb()) document.removeEventListener("visibilitychange", this.appVisibilityChangedListener);
	}
};
function attachToElement(track, element) {
	let mediaStream;
	if (element.srcObject instanceof MediaStream) mediaStream = element.srcObject;
	else mediaStream = new MediaStream();
	let existingTracks;
	if (track.kind === "audio") existingTracks = mediaStream.getAudioTracks();
	else existingTracks = mediaStream.getVideoTracks();
	if (!existingTracks.includes(track)) {
		existingTracks.forEach((et) => {
			mediaStream.removeTrack(et);
		});
		mediaStream.addTrack(track);
	}
	if (!isSafari() || !(element instanceof HTMLVideoElement)) element.autoplay = true;
	element.muted = mediaStream.getAudioTracks().length === 0;
	if (element instanceof HTMLVideoElement) element.playsInline = true;
	if (element.srcObject !== mediaStream) {
		element.srcObject = mediaStream;
		if ((isSafari() || isFireFox()) && element instanceof HTMLVideoElement) setTimeout(() => {
			element.srcObject = mediaStream;
			element.play().catch(() => {
				/** do nothing */
			});
		}, 0);
	}
}
/** @internal */
function detachTrack(track, element) {
	if (element.srcObject instanceof MediaStream) {
		const mediaStream = element.srcObject;
		mediaStream.removeTrack(track);
		if (mediaStream.getTracks().length > 0) element.srcObject = mediaStream;
		else element.srcObject = null;
	}
}
(function(Track) {
	let Kind;
	(function(Kind) {
		Kind["Audio"] = "audio";
		Kind["Video"] = "video";
		Kind["Unknown"] = "unknown";
	})(Kind = Track.Kind || (Track.Kind = {}));
	let Source;
	(function(Source) {
		Source["Camera"] = "camera";
		Source["Microphone"] = "microphone";
		Source["ScreenShare"] = "screen_share";
		Source["ScreenShareAudio"] = "screen_share_audio";
		Source["Unknown"] = "unknown";
	})(Source = Track.Source || (Track.Source = {}));
	let StreamState$1;
	(function(StreamState) {
		StreamState["Active"] = "active";
		StreamState["Paused"] = "paused";
		StreamState["Unknown"] = "unknown";
	})(StreamState$1 = Track.StreamState || (Track.StreamState = {}));
	/** @internal */
	function kindToProto(k) {
		switch (k) {
			case Kind.Audio: return TrackType.AUDIO;
			case Kind.Video: return TrackType.VIDEO;
			default: return TrackType.DATA;
		}
	}
	Track.kindToProto = kindToProto;
	/** @internal */
	function kindFromProto(t) {
		switch (t) {
			case TrackType.AUDIO: return Kind.Audio;
			case TrackType.VIDEO: return Kind.Video;
			default: return Kind.Unknown;
		}
	}
	Track.kindFromProto = kindFromProto;
	/** @internal */
	function sourceToProto(s) {
		switch (s) {
			case Source.Camera: return TrackSource.CAMERA;
			case Source.Microphone: return TrackSource.MICROPHONE;
			case Source.ScreenShare: return TrackSource.SCREEN_SHARE;
			case Source.ScreenShareAudio: return TrackSource.SCREEN_SHARE_AUDIO;
			default: return TrackSource.UNKNOWN;
		}
	}
	Track.sourceToProto = sourceToProto;
	/** @internal */
	function sourceFromProto(s) {
		switch (s) {
			case TrackSource.CAMERA: return Source.Camera;
			case TrackSource.MICROPHONE: return Source.Microphone;
			case TrackSource.SCREEN_SHARE: return Source.ScreenShare;
			case TrackSource.SCREEN_SHARE_AUDIO: return Source.ScreenShareAudio;
			default: return Source.Unknown;
		}
	}
	Track.sourceFromProto = sourceFromProto;
	/** @internal */
	function streamStateFromProto(s) {
		switch (s) {
			case StreamState.ACTIVE: return StreamState$1.Active;
			case StreamState.PAUSED: return StreamState$1.Paused;
			default: return StreamState$1.Unknown;
		}
	}
	Track.streamStateFromProto = streamStateFromProto;
})(Track || (Track = {}));
var separator = "|";
var ddExtensionURI = "https://aomediacodec.github.io/av1-rtp-spec/#dependency-descriptor-rtp-header-extension";
function unpackStreamId(packed) {
	const parts = packed.split(separator);
	if (parts.length > 1) return [parts[0], packed.substr(parts[0].length + 1)];
	return [packed, ""];
}
function sleep(duration) {
	return new TypedPromise((resolve) => CriticalTimers.setTimeout(resolve, duration));
}
/** @internal */
function supportsTransceiver() {
	return "addTransceiver" in RTCPeerConnection.prototype;
}
/** @internal */
function supportsAddTrack() {
	return "addTrack" in RTCPeerConnection.prototype;
}
function supportsAV1() {
	if (!("getCapabilities" in RTCRtpSender)) return false;
	if (isSafari() || isFireFox()) return false;
	const capabilities = RTCRtpSender.getCapabilities("video");
	let hasAV1 = false;
	if (capabilities) {
		for (const codec of capabilities.codecs) if (codec.mimeType.toLowerCase() === "video/av1") {
			hasAV1 = true;
			break;
		}
	}
	return hasAV1;
}
function supportsVP9() {
	if (!("getCapabilities" in RTCRtpSender)) return false;
	if (isFireFox()) return false;
	if (isSafari()) {
		const browser = getBrowser();
		if ((browser === null || browser === void 0 ? void 0 : browser.version) && compareVersions(browser.version, "16") < 0) return false;
		if ((browser === null || browser === void 0 ? void 0 : browser.os) === "iOS" && (browser === null || browser === void 0 ? void 0 : browser.osVersion) && compareVersions(browser.osVersion, "16") < 0) return false;
	}
	const capabilities = RTCRtpSender.getCapabilities("video");
	let hasVP9 = false;
	if (capabilities) {
		for (const codec of capabilities.codecs) if (codec.mimeType.toLowerCase() === "video/vp9") {
			hasVP9 = true;
			break;
		}
	}
	return hasVP9;
}
function isSVCCodec(codec) {
	return codec === "av1" || codec === "vp9";
}
/**
* Opts `transceiver` into negotiating the AV1 dependency descriptor, reporting whether it will be.
*
* Chrome only offers the extension on transceivers that can send, so one we create to receive on
* never negotiates it — and Chrome 152 stopped decoding AV1 that arrives without it: frames get
* assembled, none ever decode, and the receiver asks for a keyframe forever. Asking through the
* transceiver rather than munging the extension into the SDP leaves the browser owning the
* extension id, which is what keeps that id consistent across the bundle and across
* renegotiations.
*
* A no-op where the browser offers no such control, or does not know the extension at all.
* @internal
*/
function negotiateDependencyDescriptor(transceiver) {
	var _a;
	const extensions = (_a = transceiver.getHeaderExtensionsToNegotiate) === null || _a === void 0 ? void 0 : _a.call(transceiver);
	if (!extensions || !transceiver.setHeaderExtensionsToNegotiate) return false;
	const dd = extensions.find((ext) => ext.uri === ddExtensionURI);
	if (!dd) return false;
	if (dd.direction !== "stopped") return true;
	dd.direction = "sendrecv";
	try {
		transceiver.setHeaderExtensionsToNegotiate(extensions);
		return true;
	} catch (e) {
		return false;
	}
}
function supportsSetSinkId(elm) {
	if (!document || isSafariBased()) return false;
	if (!elm) elm = document.createElement("audio");
	return "setSinkId" in elm;
}
function isBrowserSupported() {
	if (typeof RTCPeerConnection === "undefined") return false;
	return supportsTransceiver() || supportsAddTrack();
}
function isFireFox() {
	var _a;
	return ((_a = getBrowser()) === null || _a === void 0 ? void 0 : _a.name) === "Firefox";
}
function isChromiumBased() {
	const browser = getBrowser();
	return !!browser && browser.name === "Chrome" && browser.os !== "iOS";
}
function isScriptTransformSupportedForWorker() {
	return typeof window !== "undefined" && typeof window.RTCRtpScriptTransform !== "undefined" && !isChromiumBased();
}
function isSafari() {
	var _a;
	return ((_a = getBrowser()) === null || _a === void 0 ? void 0 : _a.name) === "Safari";
}
function isSafariBased() {
	const b = getBrowser();
	return (b === null || b === void 0 ? void 0 : b.name) === "Safari" || (b === null || b === void 0 ? void 0 : b.os) === "iOS";
}
function isSafari17Based() {
	const b = getBrowser();
	return (b === null || b === void 0 ? void 0 : b.name) === "Safari" && b.version.startsWith("17.") || (b === null || b === void 0 ? void 0 : b.os) === "iOS" && !!(b === null || b === void 0 ? void 0 : b.osVersion) && compareVersions(b.osVersion, "17") >= 0;
}
function isSafariSvcApi(browser) {
	if (!browser) browser = getBrowser();
	return (browser === null || browser === void 0 ? void 0 : browser.name) === "Safari" && compareVersions(browser.version, "18.3") > 0 || (browser === null || browser === void 0 ? void 0 : browser.os) === "iOS" && !!(browser === null || browser === void 0 ? void 0 : browser.osVersion) && compareVersions(browser.osVersion, "18.3") > 0;
}
function isMobile() {
	var _a, _b;
	if (!isWeb()) return false;
	return (_b = (_a = navigator.userAgentData) === null || _a === void 0 ? void 0 : _a.mobile) !== null && _b !== void 0 ? _b : /Tablet|iPad|Mobile|Android|BlackBerry/.test(navigator.userAgent);
}
function isE2EESimulcastSupported() {
	const browser = getBrowser();
	const supportedSafariVersion = "17.2";
	if (browser) {
		if (browser.name !== "Safari" && browser.os !== "iOS") return true;
		else if (browser.os === "iOS" && browser.osVersion && compareVersions(browser.osVersion, supportedSafariVersion) >= 0) return true;
		else if (browser.name === "Safari" && compareVersions(browser.version, supportedSafariVersion) >= 0) return true;
		else return false;
	}
}
function isWeb() {
	return typeof document !== "undefined";
}
function isReactNative() {
	return navigator.product == "ReactNative";
}
function isCloud(serverUrl) {
	return serverUrl.hostname.endsWith(".livekit.cloud") || serverUrl.hostname.endsWith(".livekit.run");
}
function extractProjectFromUrl(serverUrl) {
	if (!isCloud(serverUrl)) return null;
	return serverUrl.hostname.split(".")[0];
}
function getLKReactNativeInfo() {
	if (global && global.LiveKitReactNativeGlobal) return global.LiveKitReactNativeGlobal;
}
function getReactNativeOs() {
	if (!isReactNative()) return;
	let info = getLKReactNativeInfo();
	if (info) return info.platform;
}
function getDevicePixelRatio() {
	if (isWeb()) return window.devicePixelRatio;
	if (isReactNative()) {
		let info = getLKReactNativeInfo();
		if (info) return info.devicePixelRatio;
	}
	return 1;
}
/**
* @param v1 - The first version string to compare.
* @param v2 - The second version string to compare.
* @returns A number indicating the order of the versions:
*   - 1 if v1 is greater than v2
*   - -1 if v1 is less than v2
*   - 0 if v1 and v2 are equal
*/
function compareVersions(v1, v2) {
	const parts1 = v1.split(".");
	const parts2 = v2.split(".");
	const k = Math.min(parts1.length, parts2.length);
	for (let i = 0; i < k; ++i) {
		const p1 = parseInt(parts1[i], 10);
		const p2 = parseInt(parts2[i], 10);
		if (p1 > p2) return 1;
		if (p1 < p2) return -1;
		if (i === k - 1 && p1 === p2) return 0;
	}
	if (v1 === "" && v2 !== "") return -1;
	else if (v2 === "") return 1;
	return parts1.length == parts2.length ? 0 : parts1.length < parts2.length ? -1 : 1;
}
function roDispatchCallback(entries) {
	for (const entry of entries) entry.target.handleResize(entry);
}
function ioDispatchCallback(entries) {
	for (const entry of entries) entry.target.handleVisibilityChanged(entry);
}
var resizeObserver = null;
var getResizeObserver = () => {
	if (!resizeObserver) resizeObserver = new ResizeObserver(roDispatchCallback);
	return resizeObserver;
};
var intersectionObserver = null;
var getIntersectionObserver = () => {
	if (!intersectionObserver) intersectionObserver = new IntersectionObserver(ioDispatchCallback, {
		root: null,
		rootMargin: "0px"
	});
	return intersectionObserver;
};
function getClientInfo(capabilities) {
	var _a;
	const info = new ClientInfo({
		capabilities,
		sdk: ClientInfo_SDK.JS,
		protocol: 17,
		clientProtocol: 2,
		version
	});
	if (isReactNative()) info.os = (_a = getReactNativeOs()) !== null && _a !== void 0 ? _a : "";
	return info;
}
function createDummyVideoStreamTrack() {
	let width = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 16;
	let height = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 16;
	let enabled = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : false;
	let paintContent = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : false;
	const canvas = document.createElement("canvas");
	canvas.width = width;
	canvas.height = height;
	const ctx = canvas.getContext("2d");
	ctx === null || ctx === void 0 || ctx.fillRect(0, 0, canvas.width, canvas.height);
	if (paintContent && ctx) {
		ctx.beginPath();
		ctx.arc(width / 2, height / 2, 50, 0, Math.PI * 2, true);
		ctx.closePath();
		ctx.fillStyle = "grey";
		ctx.fill();
	}
	const dummyTrack = _slicedToArray(canvas.captureStream().getTracks(), 1)[0];
	if (!dummyTrack) throw Error("Could not get empty media stream video track");
	dummyTrack.enabled = enabled;
	return dummyTrack;
}
var emptyAudioStreamTrack;
function getEmptyAudioStreamTrack() {
	if (!emptyAudioStreamTrack) {
		const ctx = new AudioContext();
		const oscillator = ctx.createOscillator();
		const gain = ctx.createGain();
		gain.gain.setValueAtTime(0, 0);
		const dst = ctx.createMediaStreamDestination();
		oscillator.connect(gain);
		gain.connect(dst);
		oscillator.start();
		emptyAudioStreamTrack = _slicedToArray(dst.stream.getAudioTracks(), 1)[0];
		if (!emptyAudioStreamTrack) throw Error("Could not get empty media stream audio track");
		emptyAudioStreamTrack.enabled = false;
	}
	return emptyAudioStreamTrack.clone();
}
/** An object that represents a serialized version of a `new Promise((resolve, reject) => {})`
* constructor. Wait for a promise resolution with `await future.promise` and explicitly resolve or
* reject the inner promise with `future.resolve(...)` or `future.reject(...)`.
*/
var Future = class {
	get isResolved() {
		return this._isResolved;
	}
	constructor(futureBase, onFinally) {
		this._isResolved = false;
		this.onFinally = onFinally;
		this.promise = new Promise((resolve, reject) => __awaiter(this, void 0, void 0, function* () {
			this.resolve = resolve;
			this.reject = reject;
			if (futureBase) yield futureBase(resolve, reject);
		})).finally(() => {
			var _a;
			this._isResolved = true;
			(_a = this.onFinally) === null || _a === void 0 || _a.call(this);
		});
	}
};
/**
* Creates and returns an analyser web audio node that is attached to the provided track.
* Additionally returns a convenience method `calculateVolume` to perform instant volume readings on that track.
* Call the returned `cleanup` function to close the audioContext that has been created for the instance of this helper
*/
function createAudioAnalyser(track, options) {
	const opts = Object.assign({
		cloneTrack: false,
		fftSize: 2048,
		smoothingTimeConstant: .8,
		minDecibels: -100,
		maxDecibels: -80
	}, options);
	const audioContext = getNewAudioContext();
	if (!audioContext) throw new Error("Audio Context not supported on this browser");
	const streamTrack = opts.cloneTrack ? track.mediaStreamTrack.clone() : track.mediaStreamTrack;
	const mediaStreamSource = audioContext.createMediaStreamSource(new MediaStream([streamTrack]));
	const analyser = audioContext.createAnalyser();
	analyser.minDecibels = opts.minDecibels;
	analyser.maxDecibels = opts.maxDecibels;
	analyser.fftSize = opts.fftSize;
	analyser.smoothingTimeConstant = opts.smoothingTimeConstant;
	mediaStreamSource.connect(analyser);
	const dataArray = new Uint8Array(analyser.frequencyBinCount);
	/**
	* Calculates the current volume of the track in the range from 0 to 1
	*/
	const calculateVolume = () => {
		analyser.getByteFrequencyData(dataArray);
		let sum = 0;
		for (const amplitude of dataArray) sum += Math.pow(amplitude / 255, 2);
		return Math.sqrt(sum / dataArray.length);
	};
	const cleanup = () => __awaiter(this, void 0, void 0, function* () {
		yield audioContext.close();
		if (opts.cloneTrack) streamTrack.stop();
	});
	return {
		calculateVolume,
		analyser,
		cleanup
	};
}
function isVideoCodec(maybeCodec) {
	return videoCodecs.includes(maybeCodec);
}
function unwrapConstraint(constraint) {
	if (typeof constraint === "string" || typeof constraint === "number") return constraint;
	if (Array.isArray(constraint)) return constraint[0];
	if (constraint.exact !== void 0) {
		if (Array.isArray(constraint.exact)) return constraint.exact[0];
		return constraint.exact;
	}
	if (constraint.ideal !== void 0) {
		if (Array.isArray(constraint.ideal)) return constraint.ideal[0];
		return constraint.ideal;
	}
	throw Error("could not unwrap constraint");
}
function toWebsocketUrl(url) {
	if (url.startsWith("http")) return url.replace(/^(http)/, "ws");
	return url;
}
function toHttpUrl(url) {
	if (url.startsWith("ws")) return url.replace(/^(ws)/, "http");
	return url;
}
function extractTranscriptionSegments(transcription, firstReceivedTimesMap) {
	return transcription.segments.map((_ref) => {
		let id = _ref.id, text = _ref.text, language = _ref.language, startTime = _ref.startTime, endTime = _ref.endTime, final = _ref.final;
		var _a;
		const firstReceivedTime = (_a = firstReceivedTimesMap.get(id)) !== null && _a !== void 0 ? _a : Date.now();
		const lastReceivedTime = Date.now();
		if (final) firstReceivedTimesMap.delete(id);
		else firstReceivedTimesMap.set(id, firstReceivedTime);
		return {
			id,
			text,
			startTime: Number.parseInt(startTime.toString()),
			endTime: Number.parseInt(endTime.toString()),
			final,
			language,
			firstReceivedTime,
			lastReceivedTime
		};
	});
}
function extractChatMessage(msg) {
	const id = msg.id, timestamp = msg.timestamp, message = msg.message, editTimestamp = msg.editTimestamp;
	return {
		id,
		timestamp: Number.parseInt(timestamp.toString()),
		editTimestamp: editTimestamp ? Number.parseInt(editTimestamp.toString()) : void 0,
		message
	};
}
function getDisconnectReasonFromConnectionError(e) {
	switch (e.reason) {
		case ConnectionErrorReason.LeaveRequest: return e.context;
		case ConnectionErrorReason.Cancelled: return DisconnectReason.CLIENT_INITIATED;
		case ConnectionErrorReason.NotAllowed: return DisconnectReason.USER_REJECTED;
		case ConnectionErrorReason.ServerUnreachable: return DisconnectReason.JOIN_FAILURE;
		default: return DisconnectReason.UNKNOWN_REASON;
	}
}
/** convert bigints to numbers preserving undefined values */
function bigIntToNumber(value) {
	return value !== void 0 ? Number(value) : void 0;
}
/** convert numbers to bigints preserving undefined values */
function numberToBigInt(value) {
	return value !== void 0 ? BigInt(value) : void 0;
}
function isLocalTrack(track) {
	return !!track && !(track instanceof MediaStreamTrack) && track.isLocal;
}
function isAudioTrack(track) {
	return !!track && track.kind == Track.Kind.Audio;
}
function isVideoTrack(track) {
	return !!track && track.kind == Track.Kind.Video;
}
function isLocalVideoTrack(track) {
	return isLocalTrack(track) && isVideoTrack(track);
}
function isLocalAudioTrack(track) {
	return isLocalTrack(track) && isAudioTrack(track);
}
function isRemoteTrack(track) {
	return !!track && !track.isLocal;
}
function isRemotePub(pub) {
	return !!pub && !pub.isLocal;
}
function isRemoteVideoTrack(track) {
	return isRemoteTrack(track) && isVideoTrack(track);
}
function isLocalParticipant(p) {
	return p.isLocal;
}
function splitUtf8(s, n) {
	const result = [];
	let encoded = new TextEncoder().encode(s);
	while (encoded.length > n) {
		let k = n;
		while (k > 0) {
			const byte = encoded[k];
			if (byte !== void 0 && (byte & 192) !== 128) break;
			k--;
		}
		result.push(encoded.slice(0, k));
		encoded = encoded.slice(k);
	}
	if (encoded.length > 0) result.push(encoded);
	return result;
}
/** Wraps a byte array in a `ReadableStream` that yields it as a single chunk and then closes. */
function readableFromBytes(bytes) {
	return new ReadableStream({ start(controller) {
		controller.enqueue(bytes);
		controller.close();
	} });
}
/**
* Re-chunks a byte stream into pieces of exactly `chunkSize` bytes (the final piece may be
* smaller), coalescing or splitting the source's pieces as needed. Memory use is bounded to roughly
* `chunkSize` plus one source read, so it never buffers the whole stream — used to pack
* `CompressionStream`/`file.stream()` output into MTU-sized data-stream chunks.
*/
function readBytesInChunks(source, chunkSize) {
	return __asyncGenerator(this, arguments, function* readBytesInChunks_1() {
		const reader = source.getReader();
		let buffer = /* @__PURE__ */ new Uint8Array(0);
		try {
			while (true) {
				const _yield$__await = yield __await(reader.read()), done = _yield$__await.done, value = _yield$__await.value;
				if (done) break;
				if (value.byteLength === 0) continue;
				const merged = new Uint8Array(buffer.byteLength + value.byteLength);
				merged.set(buffer);
				merged.set(value, buffer.byteLength);
				buffer = merged;
				while (buffer.byteLength >= chunkSize) {
					yield yield __await(buffer.slice(0, chunkSize));
					buffer = buffer.slice(chunkSize);
				}
			}
			if (buffer.byteLength > 0) yield yield __await(buffer);
		} finally {
			reader.releaseLock();
		}
	});
}
function extractMaxAgeFromRequestHeaders(headers) {
	var _a;
	const cacheControl = headers.get("Cache-Control");
	if (cacheControl) {
		const maxAge = (_a = cacheControl.match(/(?:^|[,\s])max-age=(\d+)/)) === null || _a === void 0 ? void 0 : _a[1];
		if (maxAge) return parseInt(maxAge, 10);
	}
}
function isCompressionStreamSupported() {
	return typeof CompressionStream !== "undefined";
}
function isPublisherOfferWithJoinSupported() {
	return isCompressionStreamSupported() && !isFireFox();
}
function extractTrackSid(mediaTrack, stream) {
	const streamId = _slicedToArray(unpackStreamId(stream.id), 2)[1];
	if (streamId === null || streamId === void 0 ? void 0 : streamId.startsWith("TR")) return streamId;
	if (mediaTrack.id.startsWith("TR")) return mediaTrack.id;
}
function createRtcUrl(url, searchParams) {
	let useV0Path = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : false;
	const v0Url = createV0RtcUrl(url, searchParams);
	if (useV0Path) return v0Url;
	else return appendUrlPath(v0Url, "v1");
}
function createV0RtcUrl(url, searchParams) {
	const urlObj = new URL(toWebsocketUrl(url));
	searchParams.forEach((value, key) => {
		urlObj.searchParams.set(key, value);
	});
	return appendUrlPath(urlObj, "rtc");
}
function createValidateUrl(rtcWsUrl) {
	return appendUrlPath(new URL(toHttpUrl(rtcWsUrl)), "validate");
}
function ensureTrailingSlash(path) {
	return path.endsWith("/") ? path : "".concat(path, "/");
}
function appendUrlPath(urlObj, path) {
	urlObj.pathname = "".concat(ensureTrailingSlash(urlObj.pathname)).concat(path);
	return urlObj;
}
function parseSignalResponse(value) {
	if (typeof value === "string") return SignalResponse.fromJson(JSON.parse(value), { ignoreUnknownFields: true });
	else if (value instanceof ArrayBuffer) return SignalResponse.fromBinary(new Uint8Array(value));
	throw new Error("could not decode websocket message: ".concat(typeof value));
}
function getAbortReasonAsString(signal) {
	let defaultMessage = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : "Unknown reason";
	if (!(signal instanceof AbortSignal)) return defaultMessage;
	const reason = signal.reason;
	switch (typeof reason) {
		case "string": return reason;
		case "object": return reason instanceof Error ? reason.message : defaultMessage;
		default: return "toString" in reason ? reason.toString() : defaultMessage;
	}
}
var DECRYPTION_FAILURE_TOLERANCE = 10;
var E2EE_FLAG = "lk_e2ee";
var KEY_PROVIDER_DEFAULTS = {
	sharedKey: false,
	ratchetSalt: "LKFrameEncryptionKey",
	ratchetWindowSize: 8,
	failureTolerance: DECRYPTION_FAILURE_TOLERANCE,
	keyringSize: 16,
	keySize: 128
};
var KeyProviderEvent;
(function(KeyProviderEvent) {
	KeyProviderEvent["SetKey"] = "setKey";
	/** Event for requesting to ratchet the key used to encrypt the stream */
	KeyProviderEvent["RatchetRequest"] = "ratchetRequest";
	/** Emitted when a key is ratcheted. Could be after auto-ratcheting on decryption failure or
	*  following a `RatchetRequest`, will contain the ratcheted key material */
	KeyProviderEvent["KeyRatcheted"] = "keyRatcheted";
})(KeyProviderEvent || (KeyProviderEvent = {}));
var KeyHandlerEvent;
(function(KeyHandlerEvent) {
	/** Emitted when a key has been ratcheted. Is emitted when any key has been ratcheted
	* i.e. when the FrameCryptor tried to ratchet when decryption is failing  */
	KeyHandlerEvent["KeyRatcheted"] = "keyRatcheted";
})(KeyHandlerEvent || (KeyHandlerEvent = {}));
var EncryptionEvent;
(function(EncryptionEvent) {
	EncryptionEvent["ParticipantEncryptionStatusChanged"] = "participantEncryptionStatusChanged";
	EncryptionEvent["EncryptionError"] = "encryptionError";
})(EncryptionEvent || (EncryptionEvent = {}));
var CryptorEvent;
(function(CryptorEvent) {
	CryptorEvent["Error"] = "cryptorError";
})(CryptorEvent || (CryptorEvent = {}));
function isE2EESupported() {
	return isInsertableStreamSupported() || isScriptTransformSupported();
}
function isScriptTransformSupported() {
	return typeof window !== "undefined" && typeof window.RTCRtpScriptTransform !== "undefined";
}
function isInsertableStreamSupported() {
	return typeof window !== "undefined" && typeof window.RTCRtpSender !== "undefined" && typeof window.RTCRtpSender.prototype.createEncodedStreams !== "undefined";
}
function createKeyMaterialFromString(password) {
	return __awaiter(this, void 0, void 0, function* () {
		let enc = new TextEncoder();
		return yield crypto.subtle.importKey("raw", enc.encode(password), { name: "PBKDF2" }, false, ["deriveBits", "deriveKey"]);
	});
}
function createKeyMaterialFromBuffer(cryptoBuffer) {
	return __awaiter(this, void 0, void 0, function* () {
		return yield crypto.subtle.importKey("raw", cryptoBuffer, "HKDF", false, ["deriveBits", "deriveKey"]);
	});
}
function asEncryptablePacket(packet) {
	var _a, _b, _c, _d, _e;
	if (((_a = packet.value) === null || _a === void 0 ? void 0 : _a.case) !== "sipDtmf" && ((_b = packet.value) === null || _b === void 0 ? void 0 : _b.case) !== "metrics" && ((_c = packet.value) === null || _c === void 0 ? void 0 : _c.case) !== "speaker" && ((_d = packet.value) === null || _d === void 0 ? void 0 : _d.case) !== "transcription" && ((_e = packet.value) === null || _e === void 0 ? void 0 : _e.case) !== "encryptedPacket") return new EncryptedPacketPayload({ value: packet.value });
}
var BaseKeyProvider = class extends eventsExports.EventEmitter {
	constructor() {
		let options = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
		super();
		this.latestManuallySetKeyIndex = 0;
		/**
		* Callback being invoked after a key has been ratcheted.
		* Can happen when:
		* - A decryption failure occurs and the key is auto-ratcheted
		* - A ratchet request is sent (see {@link ratchetKey()})
		* @param ratchetResult Contains the ratcheted chain key (exportable to other participants) and the derived new key material.
		* @param participantId
		* @param keyIndex
		*/
		this.onKeyRatcheted = (ratchetResult, participantId, keyIndex) => {
			livekitLogger.debug("key ratcheted event received", {
				ratchetResult,
				participantId,
				keyIndex
			});
		};
		this.keyInfoMap = /* @__PURE__ */ new Map();
		this.options = Object.assign(Object.assign({}, KEY_PROVIDER_DEFAULTS), options);
		this.on(KeyProviderEvent.KeyRatcheted, this.onKeyRatcheted);
	}
	/**
	* callback to invoke once a key has been set for a participant
	* @param key
	* @param participantIdentity
	* @param keyIndex
	*/
	onSetEncryptionKey(key, participantIdentity, keyIndex) {
		const keyInfo = {
			key,
			participantIdentity,
			keyIndex
		};
		if (!this.options.sharedKey && !participantIdentity) throw new Error("participant identity needs to be passed for encryption key if sharedKey option is false");
		this.keyInfoMap.set("".concat(participantIdentity !== null && participantIdentity !== void 0 ? participantIdentity : "shared", "-").concat(keyIndex !== null && keyIndex !== void 0 ? keyIndex : 0), keyInfo);
		if (keyIndex !== void 0) this.latestManuallySetKeyIndex = keyIndex;
		this.emit(KeyProviderEvent.SetKey, keyInfo, keyIndex !== void 0);
	}
	getKeys() {
		return Array.from(this.keyInfoMap.values());
	}
	getLatestManuallySetKeyIndex() {
		return this.latestManuallySetKeyIndex;
	}
	getOptions() {
		return this.options;
	}
	ratchetKey(participantIdentity, keyIndex) {
		this.emit(KeyProviderEvent.RatchetRequest, participantIdentity, keyIndex);
	}
};
/**
* A basic KeyProvider implementation intended for a single shared
* passphrase between all participants
* @experimental
*/
var ExternalE2EEKeyProvider = class extends BaseKeyProvider {
	constructor() {
		let options = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
		const opts = Object.assign(Object.assign({}, options), {
			sharedKey: true,
			ratchetWindowSize: 0,
			failureTolerance: -1
		});
		super(opts);
	}
	/**
	* Accepts a passphrase that's used to create the crypto keys.
	* When passing in a string, PBKDF2 is used. (recommended for maximum compatibility across SDKs)
	* When passing in an ArrayBuffer of cryptographically random numbers, HKDF is used.
	*
	* Note: Not all client SDKS support HKDF.
	* @param key
	*/
	setKey(key) {
		return __awaiter(this, void 0, void 0, function* () {
			const derivedKey = typeof key === "string" ? yield createKeyMaterialFromString(key) : yield createKeyMaterialFromBuffer(key);
			this.onSetEncryptionKey(derivedKey);
		});
	}
};
var CryptorErrorReason;
(function(CryptorErrorReason) {
	CryptorErrorReason[CryptorErrorReason["InvalidKey"] = 0] = "InvalidKey";
	CryptorErrorReason[CryptorErrorReason["MissingKey"] = 1] = "MissingKey";
	CryptorErrorReason[CryptorErrorReason["InternalError"] = 2] = "InternalError";
})(CryptorErrorReason || (CryptorErrorReason = {}));
function shouldUseFrameMetadataScriptTransform() {
	return isScriptTransformSupportedForWorker();
}
function isFrameMetadataSupported(options) {
	return !!(options === null || options === void 0 ? void 0 : options.worker) && (isInsertableStreamSupported() || shouldUseFrameMetadataScriptTransform());
}
function hasFrameMetadataPublishOptions(options) {
	return !!((options === null || options === void 0 ? void 0 : options.timestamp) || (options === null || options === void 0 ? void 0 : options.frameId));
}
function getFrameMetadataFeatures(options) {
	const features = [];
	if (options === null || options === void 0 ? void 0 : options.timestamp) features.push(PacketTrailerFeature.PTF_USER_TIMESTAMP);
	if (options === null || options === void 0 ? void 0 : options.frameId) features.push(PacketTrailerFeature.PTF_FRAME_ID);
	return features;
}
function getFrameMetadataPublishOptions(features) {
	if (!features || features.length === 0) return;
	const options = {};
	if (features.includes(PacketTrailerFeature.PTF_USER_TIMESTAMP)) options.timestamp = true;
	if (features.includes(PacketTrailerFeature.PTF_FRAME_ID)) options.frameId = true;
	return options.timestamp || options.frameId ? options : void 0;
}
function debounce(func) {
	let waitMilliseconds = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 50;
	let options = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
	var _a, _b;
	let timeoutId;
	const isImmediate = (_a = options.isImmediate) !== null && _a !== void 0 ? _a : false;
	const callback = (_b = options.callback) !== null && _b !== void 0 ? _b : false;
	const maxWait = options.maxWait;
	let lastInvokeTime = Date.now();
	let promises = [];
	function nextInvokeTimeout() {
		if (maxWait !== void 0) {
			const timeSinceLastInvocation = Date.now() - lastInvokeTime;
			if (timeSinceLastInvocation + waitMilliseconds >= maxWait) return maxWait - timeSinceLastInvocation;
		}
		return waitMilliseconds;
	}
	const debouncedFunction = function() {
		for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) args[_key] = arguments[_key];
		const context = this;
		return new Promise((resolve, reject) => {
			const invokeFunction = function() {
				timeoutId = void 0;
				lastInvokeTime = Date.now();
				if (!isImmediate) {
					const result = func.apply(context, args);
					callback && callback(result);
					promises.forEach((_ref) => {
						let resolve = _ref.resolve;
						return resolve(result);
					});
					promises = [];
				}
			};
			const shouldCallNow = isImmediate && timeoutId === void 0;
			if (timeoutId !== void 0) CriticalTimers.clearTimeout(timeoutId);
			timeoutId = CriticalTimers.setTimeout(invokeFunction, nextInvokeTimeout());
			if (shouldCallNow) {
				const result = func.apply(context, args);
				callback && callback(result);
				return resolve(result);
			}
			promises.push({
				resolve,
				reject
			});
		});
	};
	debouncedFunction.cancel = function(reason) {
		if (timeoutId !== void 0) CriticalTimers.clearTimeout(timeoutId);
		promises.forEach((_ref2) => {
			let reject = _ref2.reject;
			return reject(reason);
		});
		promises = [];
	};
	return debouncedFunction;
}
var monitorFrequency = 2e3;
function computeBitrate(currentStats, prevStats) {
	if (!prevStats) return 0;
	let bytesNow;
	let bytesPrev;
	if ("bytesReceived" in currentStats) {
		bytesNow = currentStats.bytesReceived;
		bytesPrev = prevStats.bytesReceived;
	} else if ("bytesSent" in currentStats) {
		bytesNow = currentStats.bytesSent;
		bytesPrev = prevStats.bytesSent;
	}
	if (bytesNow === void 0 || bytesPrev === void 0 || currentStats.timestamp === void 0 || prevStats.timestamp === void 0) return 0;
	return (bytesNow - bytesPrev) * 8 * 1e3 / (currentStats.timestamp - prevStats.timestamp);
}
var RemoteTrack = class extends Track {
	constructor(mediaTrack, sid, kind, receiver, loggerOptions) {
		super(mediaTrack, kind, loggerOptions);
		this.timeSyncLoop = () => {
			var _a;
			if (this.listenerCount(TrackEvent.TimeSyncUpdate) === 0) {
				this.timeSyncHandle = void 0;
				return;
			}
			this.timeSyncHandle = requestAnimationFrame(this.timeSyncLoop);
			const sources = (_a = this.receiver) === null || _a === void 0 ? void 0 : _a.getSynchronizationSources()[0];
			if (sources) {
				const timestamp = sources.timestamp, rtpTimestamp = sources.rtpTimestamp;
				if (rtpTimestamp && this.rtpTimestamp !== rtpTimestamp) {
					this.emit(TrackEvent.TimeSyncUpdate, {
						timestamp,
						rtpTimestamp
					});
					this.rtpTimestamp = rtpTimestamp;
				}
			}
		};
		this.onTimeSyncListenerAdded = (event) => {
			if (event === TrackEvent.TimeSyncUpdate && this.timeSyncHandle === void 0) this.timeSyncHandle = requestAnimationFrame(this.timeSyncLoop);
		};
		this.sid = sid;
		this.receiver = receiver;
	}
	get isLocal() {
		return false;
	}
	/** @internal */
	setMuted(muted) {
		if (this.isMuted !== muted) {
			this.isMuted = muted;
			this._mediaStreamTrack.enabled = !muted;
			this.emit(muted ? TrackEvent.Muted : TrackEvent.Unmuted, this);
		}
	}
	/** @internal */
	setMediaStream(stream) {
		this.mediaStream = stream;
		const onRemoveTrack = (event) => {
			if (event.track === this._mediaStreamTrack) {
				stream.removeEventListener("removetrack", onRemoveTrack);
				if (this.receiver && "playoutDelayHint" in this.receiver) this.receiver.playoutDelayHint = void 0;
				this.receiver = void 0;
				this._currentBitrate = 0;
				this.emit(TrackEvent.Ended, this);
			}
		};
		stream.addEventListener("removetrack", onRemoveTrack);
	}
	start() {
		this.startMonitor();
		super.enable();
	}
	stop() {
		this.stopMonitor();
		super.disable();
	}
	/**
	* Gets the RTCStatsReport for the RemoteTrack's underlying RTCRtpReceiver
	* See https://developer.mozilla.org/en-US/docs/Web/API/RTCStatsReport
	*
	* @returns Promise<RTCStatsReport> | undefined
	*/
	getRTCStatsReport() {
		return __awaiter(this, void 0, void 0, function* () {
			var _a;
			if (!((_a = this.receiver) === null || _a === void 0 ? void 0 : _a.getStats)) return;
			return yield this.receiver.getStats();
		});
	}
	/**
	* Allows to set a playout delay (in seconds) for this track.
	* A higher value allows for more buffering of the track in the browser
	* and will result in a delay of media being played back of `delayInSeconds`
	*/
	setPlayoutDelay(delayInSeconds) {
		if (this.receiver) {
			if ("playoutDelayHint" in this.receiver) this.receiver.playoutDelayHint = delayInSeconds;
			else this.log.warn("Playout delay not supported in this browser");
		} else this.log.warn("Cannot set playout delay, track already ended");
	}
	/**
	* Returns the current playout delay (in seconds) of this track.
	*/
	getPlayoutDelay() {
		if (this.receiver) {
			if ("playoutDelayHint" in this.receiver) return this.receiver.playoutDelayHint;
			else this.log.warn("Playout delay not supported in this browser");
		} else this.log.warn("Cannot get playout delay, track already ended");
		return 0;
	}
	startMonitor() {
		if (!this.monitorInterval) this.monitorInterval = setInterval(() => this.monitorReceiver(), monitorFrequency);
		if (supportsSynchronizationSources()) this.registerTimeSyncUpdate();
	}
	stopMonitor() {
		super.stopMonitor();
		this.off("newListener", this.onTimeSyncListenerAdded);
	}
	registerTimeSyncUpdate() {
		const emitter = this;
		emitter.off("newListener", this.onTimeSyncListenerAdded);
		emitter.on("newListener", this.onTimeSyncListenerAdded);
		if (this.timeSyncHandle === void 0) this.timeSyncLoop();
	}
};
var REACTION_DELAY = 100;
var RemoteVideoTrack = class extends RemoteTrack {
	constructor(mediaTrack, sid, receiver, adaptiveStreamSettings, loggerOptions) {
		super(mediaTrack, sid, Track.Kind.Video, receiver, loggerOptions);
		this.elementInfos = [];
		this.monitorReceiver = () => __awaiter(this, void 0, void 0, function* () {
			if (!this.receiver) {
				this._currentBitrate = 0;
				return;
			}
			const stats = yield this.getReceiverStats();
			if (stats && this.prevStats && this.receiver) this._currentBitrate = computeBitrate(stats, this.prevStats);
			this.prevStats = stats;
		});
		this.debouncedHandleResize = debounce(() => {
			this.updateDimensions();
		}, REACTION_DELAY);
		this.adaptiveStreamSettings = adaptiveStreamSettings;
	}
	get isAdaptiveStream() {
		return this.adaptiveStreamSettings !== void 0;
	}
	/**
	* Look up frame-level metadata for a given RTP timestamp.
	* Use with the `TrackEvent.TimeSyncUpdate` event to correlate displayed frames
	* with their capture-time metadata.
	*
	* Requires the room to be configured with the `frameMetadata` worker option
	* and the publishing track to have frame metadata features enabled.
	*
	*/
	lookupFrameMetadata(_ref) {
		let rtpTimestamp = _ref.rtpTimestamp;
		var _a;
		return (_a = this.frameMetadataExtractor) === null || _a === void 0 ? void 0 : _a.lookupMetadata(rtpTimestamp);
	}
	setStreamState(value) {
		super.setStreamState(value);
		this.log.debug("setStreamState", value);
		if (this.isAdaptiveStream && value === Track.StreamState.Active) this.updateVisibility();
	}
	/**
	* Note: When using adaptiveStream, you need to use remoteVideoTrack.attach() to add the track to a HTMLVideoElement, otherwise your video tracks might never start
	*/
	get mediaStreamTrack() {
		return this._mediaStreamTrack;
	}
	/** @internal */
	setMuted(muted) {
		super.setMuted(muted);
		this.attachedElements.forEach((element) => {
			if (muted) detachTrack(this._mediaStreamTrack, element);
			else attachToElement(this._mediaStreamTrack, element);
		});
	}
	attach(element) {
		if (!element) element = super.attach();
		else super.attach(element);
		if (this.adaptiveStreamSettings && this.elementInfos.find((info) => info.element === element) === void 0) {
			const elementInfo = new HTMLElementInfo(element);
			this.observeElementInfo(elementInfo);
		}
		return element;
	}
	/**
	* Observe an ElementInfo for changes when adaptive streaming.
	* @param elementInfo
	* @internal
	*/
	observeElementInfo(elementInfo) {
		if (this.adaptiveStreamSettings && this.elementInfos.find((info) => info === elementInfo) === void 0) {
			elementInfo.handleResize = () => {
				this.debouncedHandleResize();
			};
			elementInfo.handleVisibilityChanged = () => {
				this.updateVisibility();
			};
			this.elementInfos.push(elementInfo);
			elementInfo.observe();
			this.debouncedHandleResize();
			this.updateVisibility();
		} else this.log.warn("visibility resize observer not triggered", this.logContext);
	}
	/**
	* Stop observing an ElementInfo for changes.
	* @param elementInfo
	* @internal
	*/
	stopObservingElementInfo(elementInfo) {
		if (!this.isAdaptiveStream) {
			this.log.warn("stopObservingElementInfo ignored", this.logContext);
			return;
		}
		const stopElementInfos = this.elementInfos.filter((info) => info === elementInfo);
		for (const info of stopElementInfos) info.stopObserving();
		this.elementInfos = this.elementInfos.filter((info) => info !== elementInfo);
		this.updateVisibility();
		this.debouncedHandleResize();
	}
	detach(element) {
		if (element) {
			this.stopObservingElement(element);
			return super.detach(element);
		}
		const detachedElements = super.detach();
		for (const e of detachedElements) this.stopObservingElement(e);
		return detachedElements;
	}
	/** @internal */
	getDecoderImplementation() {
		var _a;
		return (_a = this.prevStats) === null || _a === void 0 ? void 0 : _a.decoderImplementation;
	}
	getReceiverStats() {
		return __awaiter(this, void 0, void 0, function* () {
			if (!this.receiver || !this.receiver.getStats) return;
			const stats = yield this.receiver.getStats();
			let receiverStats;
			let codecID = "";
			let codecs = /* @__PURE__ */ new Map();
			stats.forEach((v) => {
				if (v.type === "inbound-rtp") {
					codecID = v.codecId;
					receiverStats = {
						type: "video",
						streamId: v.id,
						framesDecoded: v.framesDecoded,
						framesDropped: v.framesDropped,
						framesReceived: v.framesReceived,
						packetsReceived: v.packetsReceived,
						packetsLost: v.packetsLost,
						frameWidth: v.frameWidth,
						frameHeight: v.frameHeight,
						pliCount: v.pliCount,
						firCount: v.firCount,
						nackCount: v.nackCount,
						jitter: v.jitter,
						timestamp: v.timestamp,
						bytesReceived: v.bytesReceived,
						decoderImplementation: v.decoderImplementation
					};
				} else if (v.type === "codec") codecs.set(v.id, v);
			});
			if (receiverStats && codecID !== "" && codecs.get(codecID)) receiverStats.mimeType = codecs.get(codecID).mimeType;
			return receiverStats;
		});
	}
	stopObservingElement(element) {
		const stopElementInfos = this.elementInfos.filter((info) => info.element === element);
		for (const info of stopElementInfos) this.stopObservingElementInfo(info);
	}
	handleAppVisibilityChanged() {
		const _super = Object.create(null, { handleAppVisibilityChanged: { get: () => super.handleAppVisibilityChanged } });
		return __awaiter(this, void 0, void 0, function* () {
			yield _super.handleAppVisibilityChanged.call(this);
			if (!this.isAdaptiveStream) return;
			this.updateVisibility();
		});
	}
	updateVisibility(forceEmit) {
		var _a, _b;
		const lastVisibilityChange = this.elementInfos.reduce((prev, info) => Math.max(prev, info.visibilityChangedAt || 0), 0);
		const backgroundPause = ((_b = (_a = this.adaptiveStreamSettings) === null || _a === void 0 ? void 0 : _a.pauseVideoInBackground) !== null && _b !== void 0 ? _b : true) ? this.isInBackground : false;
		const isPiPMode = this.elementInfos.some((info) => info.pictureInPicture);
		const isVisible = this.elementInfos.some((info) => info.visible) && !backgroundPause || isPiPMode;
		if (this.lastVisible === isVisible && !forceEmit) return;
		if (!isVisible && Date.now() - lastVisibilityChange < REACTION_DELAY) {
			CriticalTimers.setTimeout(() => {
				this.updateVisibility();
			}, REACTION_DELAY);
			return;
		}
		this.lastVisible = isVisible;
		this.emit(TrackEvent.VisibilityChanged, isVisible, this);
	}
	updateDimensions() {
		var _a, _b;
		let maxWidth = 0;
		let maxHeight = 0;
		const pixelDensity = this.getPixelDensity();
		for (const info of this.elementInfos) {
			const currentElementWidth = info.width() * pixelDensity;
			const currentElementHeight = info.height() * pixelDensity;
			if (currentElementWidth + currentElementHeight > maxWidth + maxHeight) {
				maxWidth = currentElementWidth;
				maxHeight = currentElementHeight;
			}
		}
		if (((_a = this.lastDimensions) === null || _a === void 0 ? void 0 : _a.width) === maxWidth && ((_b = this.lastDimensions) === null || _b === void 0 ? void 0 : _b.height) === maxHeight) return;
		this.lastDimensions = {
			width: maxWidth,
			height: maxHeight
		};
		this.emit(TrackEvent.VideoDimensionsChanged, this.lastDimensions, this);
	}
	getPixelDensity() {
		var _a;
		const pixelDensity = (_a = this.adaptiveStreamSettings) === null || _a === void 0 ? void 0 : _a.pixelDensity;
		if (pixelDensity === "screen") return getDevicePixelRatio();
		else if (!pixelDensity) {
			if (getDevicePixelRatio() > 2) return 2;
			else return 1;
		}
		return pixelDensity;
	}
};
var HTMLElementInfo = class {
	get visible() {
		return this.isPiP || this.isIntersecting;
	}
	get pictureInPicture() {
		return this.isPiP;
	}
	constructor(element, visible) {
		this.onVisibilityChanged = (entry) => {
			var _a;
			const target = entry.target, isIntersecting = entry.isIntersecting;
			if (target === this.element) {
				this.isIntersecting = isIntersecting;
				this.isPiP = isElementInPiP(this.element);
				this.visibilityChangedAt = Date.now();
				(_a = this.handleVisibilityChanged) === null || _a === void 0 || _a.call(this);
			}
		};
		this.onEnterPiP = () => {
			var _a, _b;
			(_b = (_a = window.documentPictureInPicture) === null || _a === void 0 ? void 0 : _a.window) === null || _b === void 0 || _b.addEventListener("pagehide", this.onLeavePiP);
			queueMicrotask(() => {
				requestAnimationFrame(() => {
					var _a;
					this.isPiP = isElementInPiP(this.element);
					(_a = this.handleVisibilityChanged) === null || _a === void 0 || _a.call(this);
				});
			});
		};
		this.onLeavePiP = () => {
			var _a;
			this.isPiP = isElementInPiP(this.element);
			(_a = this.handleVisibilityChanged) === null || _a === void 0 || _a.call(this);
		};
		this.element = element;
		this.isIntersecting = visible !== null && visible !== void 0 ? visible : isElementInViewport(element);
		this.isPiP = isWeb() && isElementInPiP(element);
		this.visibilityChangedAt = 0;
	}
	width() {
		return this.element.clientWidth;
	}
	height() {
		return this.element.clientHeight;
	}
	observe() {
		var _a, _b, _c;
		this.isIntersecting = isElementInViewport(this.element);
		this.isPiP = isElementInPiP(this.element);
		this.element.handleResize = () => {
			var _a;
			(_a = this.handleResize) === null || _a === void 0 || _a.call(this);
		};
		this.element.handleVisibilityChanged = this.onVisibilityChanged;
		getIntersectionObserver().observe(this.element);
		getResizeObserver().observe(this.element);
		this.element.addEventListener("enterpictureinpicture", this.onEnterPiP);
		this.element.addEventListener("leavepictureinpicture", this.onLeavePiP);
		(_a = window.documentPictureInPicture) === null || _a === void 0 || _a.addEventListener("enter", this.onEnterPiP);
		(_c = (_b = window.documentPictureInPicture) === null || _b === void 0 ? void 0 : _b.window) === null || _c === void 0 || _c.addEventListener("pagehide", this.onLeavePiP);
	}
	stopObserving() {
		var _a, _b, _c, _d, _e;
		(_a = getIntersectionObserver()) === null || _a === void 0 || _a.unobserve(this.element);
		(_b = getResizeObserver()) === null || _b === void 0 || _b.unobserve(this.element);
		this.element.removeEventListener("enterpictureinpicture", this.onEnterPiP);
		this.element.removeEventListener("leavepictureinpicture", this.onLeavePiP);
		(_c = window.documentPictureInPicture) === null || _c === void 0 || _c.removeEventListener("enter", this.onEnterPiP);
		(_e = (_d = window.documentPictureInPicture) === null || _d === void 0 ? void 0 : _d.window) === null || _e === void 0 || _e.removeEventListener("pagehide", this.onLeavePiP);
	}
};
function isElementInPiP(el) {
	var _a, _b;
	if (document.pictureInPictureElement === el) return true;
	if ((_a = window.documentPictureInPicture) === null || _a === void 0 ? void 0 : _a.window) return isElementInViewport(el, (_b = window.documentPictureInPicture) === null || _b === void 0 ? void 0 : _b.window);
	return false;
}
function isElementInViewport(el, win) {
	const viewportWindow = win || window;
	let top = el.offsetTop;
	let left = el.offsetLeft;
	const width = el.offsetWidth;
	const height = el.offsetHeight;
	const hidden = el.hidden;
	const display = getComputedStyle(el).display;
	while (el.offsetParent) {
		el = el.offsetParent;
		top += el.offsetTop;
		left += el.offsetLeft;
	}
	return top < viewportWindow.pageYOffset + viewportWindow.innerHeight && left < viewportWindow.pageXOffset + viewportWindow.innerWidth && top + height > viewportWindow.pageYOffset && left + width > viewportWindow.pageXOffset && !hidden && display !== "none";
}
var E2EEManager = class extends eventsExports.EventEmitter {
	constructor(options, dcEncryptionEnabled) {
		super();
		this.decryptDataRequests = /* @__PURE__ */ new Map();
		this.encryptDataRequests = /* @__PURE__ */ new Map();
		this.onWorkerMessage = (ev) => {
			var _a, _b;
			const _ev$data = ev.data, kind = _ev$data.kind, data = _ev$data.data;
			switch (kind) {
				case "error":
					livekitLogger.error(data.error.message);
					if (data.uuid) {
						const decryptFuture = this.decryptDataRequests.get(data.uuid);
						if (decryptFuture === null || decryptFuture === void 0 ? void 0 : decryptFuture.reject) {
							decryptFuture.reject(data.error);
							break;
						}
						const encryptFuture = this.encryptDataRequests.get(data.uuid);
						if (encryptFuture === null || encryptFuture === void 0 ? void 0 : encryptFuture.reject) {
							encryptFuture.reject(data.error);
							break;
						}
					}
					this.emit(EncryptionEvent.EncryptionError, data.error, data.participantIdentity);
					break;
				case "initAck":
					if (data.enabled) this.keyProvider.getKeys().forEach((keyInfo) => {
						this.postKey(keyInfo, false);
					});
					break;
				case "enable":
					if (data.enabled) this.keyProvider.getKeys().forEach((keyInfo) => {
						this.postKey(keyInfo, false);
					});
					if (this.encryptionEnabled !== data.enabled && data.participantIdentity === ((_a = this.room) === null || _a === void 0 ? void 0 : _a.localParticipant.identity)) {
						this.emit(EncryptionEvent.ParticipantEncryptionStatusChanged, data.enabled, this.room.localParticipant);
						this.encryptionEnabled = data.enabled;
					} else if (data.participantIdentity) {
						const participant = (_b = this.room) === null || _b === void 0 ? void 0 : _b.getParticipantByIdentity(data.participantIdentity);
						if (!participant) throw TypeError("couldn't set encryption status, participant not found".concat(data.participantIdentity));
						this.emit(EncryptionEvent.ParticipantEncryptionStatusChanged, data.enabled, participant);
					}
					break;
				case "ratchetKey":
					this.keyProvider.emit(KeyProviderEvent.KeyRatcheted, data.ratchetResult, data.participantIdentity, data.keyIndex);
					break;
				case "decryptDataResponse":
					const decryptFuture = this.decryptDataRequests.get(data.uuid);
					if (decryptFuture === null || decryptFuture === void 0 ? void 0 : decryptFuture.resolve) decryptFuture.resolve(data);
					break;
				case "encryptDataResponse":
					const encryptFuture = this.encryptDataRequests.get(data.uuid);
					if (encryptFuture === null || encryptFuture === void 0 ? void 0 : encryptFuture.resolve) encryptFuture.resolve(data);
					break;
				case "packetTrailerMetadata": this.handleFrameMetadata(data.trackId, data.rtpTimestamp, data.ssrc, data.metadata);
			}
		};
		this.onWorkerError = (ev) => {
			livekitLogger.error("e2ee worker encountered an error:", { error: ev.error });
			this.emit(EncryptionEvent.EncryptionError, ev.error, void 0);
		};
		this.keyProvider = options.keyProvider;
		this.worker = options.worker;
		this.encryptionEnabled = false;
		this.dataChannelEncryptionEnabled = dcEncryptionEnabled;
	}
	get isEnabled() {
		return this.encryptionEnabled;
	}
	get isDataChannelEncryptionEnabled() {
		return this.isEnabled && this.dataChannelEncryptionEnabled;
	}
	/**
	* @internal
	*/
	setup(room) {
		if (!isE2EESupported()) throw new DeviceUnsupportedError("tried to setup end-to-end encryption on an unsupported browser");
		livekitLogger.info("setting up e2ee");
		if (room !== this.room) {
			this.room = room;
			this.setupEventListeners(room, this.keyProvider);
			const msg = {
				kind: "init",
				data: {
					keyProviderOptions: this.keyProvider.getOptions(),
					loglevel: workerLogger.getLevel()
				}
			};
			if (this.worker) {
				livekitLogger.info("initializing worker", { worker: this.worker });
				this.worker.onmessage = this.onWorkerMessage;
				this.worker.onerror = this.onWorkerError;
				this.worker.postMessage(msg);
			}
		}
	}
	/**
	* @internal
	*/
	setParticipantCryptorEnabled(enabled, participantIdentity) {
		livekitLogger.debug("set e2ee to ".concat(enabled, " for participant ").concat(participantIdentity));
		this.postEnable(enabled, participantIdentity);
	}
	/**
	* @internal
	*/
	setSifTrailer(trailer) {
		if (!trailer || trailer.length === 0) livekitLogger.warn("ignoring server sent trailer as it's empty");
		else this.postSifTrailer(trailer);
	}
	handleFrameMetadata(trackId, rtpTimestamp, ssrc, metadata) {
		if (!this.room) return;
		for (const participant of [this.room.localParticipant, ...this.room.remoteParticipants.values()]) for (const pub of participant.trackPublications.values()) if (pub.track && pub.track.mediaStreamID === trackId && pub.track instanceof RemoteVideoTrack && pub.track.frameMetadataExtractor) {
			pub.track.frameMetadataExtractor.storeMetadata(rtpTimestamp, ssrc, metadata);
			return;
		}
	}
	setupEngine(engine) {
		engine.on(EngineEvent.RTPVideoMapUpdate, (rtpMap) => {
			this.postRTPMap(rtpMap);
		});
	}
	setupEventListeners(room, keyProvider) {
		room.on(RoomEvent.TrackPublished, (pub, participant) => this.setParticipantCryptorEnabled(pub.trackInfo.encryption !== Encryption_Type.NONE, participant.identity));
		room.on(RoomEvent.ConnectionStateChanged, (state) => {
			if (state === ConnectionState.Connected) room.remoteParticipants.forEach((participant) => {
				participant.trackPublications.forEach((pub) => {
					this.setParticipantCryptorEnabled(pub.trackInfo.encryption !== Encryption_Type.NONE, participant.identity);
				});
			});
		}).on(RoomEvent.TrackUnsubscribed, (track, _, participant) => {
			var _a;
			const msg = {
				kind: "removeTransform",
				data: {
					participantIdentity: participant.identity,
					trackId: track.mediaStreamID
				}
			};
			(_a = this.worker) === null || _a === void 0 || _a.postMessage(msg);
		}).on(RoomEvent.TrackSubscribed, (track, pub, participant) => {
			this.setupE2EEReceiver(track, participant.identity, pub.trackInfo);
		}).on(RoomEvent.SignalConnected, () => {
			if (!this.room) throw new TypeError("expected room to be present on signal connect");
			const latestKeyIndex = keyProvider.getLatestManuallySetKeyIndex();
			keyProvider.getKeys().forEach((keyInfo) => {
				var _a;
				this.postKey(keyInfo, latestKeyIndex === ((_a = keyInfo.keyIndex) !== null && _a !== void 0 ? _a : 0));
			});
			this.setParticipantCryptorEnabled(this.room.localParticipant.isE2EEEnabled, this.room.localParticipant.identity);
		});
		room.localParticipant.on(ParticipantEvent.LocalSenderCreated, (sender, track) => __awaiter(this, void 0, void 0, function* () {
			this.setupE2EESender(track, sender);
		}));
		room.localParticipant.on(ParticipantEvent.LocalTrackPublished, (publication) => {
			if (!isVideoTrack(publication.track) || !isSafariBased()) return;
			const msg = {
				kind: "updateCodec",
				data: {
					trackId: publication.track.mediaStreamID,
					codec: mimeTypeToVideoCodecString(publication.trackInfo.codecs[0].mimeType),
					participantIdentity: this.room.localParticipant.identity,
					hasPacketTrailer: false
				}
			};
			this.worker.postMessage(msg);
		});
		keyProvider.on(KeyProviderEvent.SetKey, (keyInfo, updateCurrentKeyIndex) => this.postKey(keyInfo, updateCurrentKeyIndex !== null && updateCurrentKeyIndex !== void 0 ? updateCurrentKeyIndex : true)).on(KeyProviderEvent.RatchetRequest, (participantId, keyIndex) => this.postRatchetRequest(participantId, keyIndex));
	}
	encryptData(data) {
		return __awaiter(this, void 0, void 0, function* () {
			if (!this.worker) throw Error("could not encrypt data, worker is missing");
			const uuid = crypto.randomUUID();
			const msg = {
				kind: "encryptDataRequest",
				data: {
					uuid,
					payload: data,
					participantIdentity: this.room.localParticipant.identity
				}
			};
			const future = new Future();
			future.onFinally = () => {
				this.encryptDataRequests.delete(uuid);
			};
			this.encryptDataRequests.set(uuid, future);
			this.worker.postMessage(msg);
			return future.promise;
		});
	}
	handleEncryptedData(payload, iv, participantIdentity, keyIndex) {
		if (!this.worker) throw Error("could not handle encrypted data, worker is missing");
		const uuid = crypto.randomUUID();
		const msg = {
			kind: "decryptDataRequest",
			data: {
				uuid,
				payload,
				iv,
				participantIdentity,
				keyIndex
			}
		};
		const future = new Future();
		future.onFinally = () => {
			this.decryptDataRequests.delete(uuid);
		};
		this.decryptDataRequests.set(uuid, future);
		this.worker.postMessage(msg);
		return future.promise;
	}
	postRatchetRequest(participantIdentity, keyIndex) {
		if (!this.worker) throw Error("could not ratchet key, worker is missing");
		const msg = {
			kind: "ratchetRequest",
			data: {
				participantIdentity,
				keyIndex
			}
		};
		this.worker.postMessage(msg);
	}
	postKey(_ref, updateCurrentKeyIndex) {
		let key = _ref.key, participantIdentity = _ref.participantIdentity, keyIndex = _ref.keyIndex;
		var _a;
		if (!this.worker) throw Error("could not set key, worker is missing");
		const msg = {
			kind: "setKey",
			data: {
				participantIdentity,
				isPublisher: participantIdentity === ((_a = this.room) === null || _a === void 0 ? void 0 : _a.localParticipant.identity),
				key,
				keyIndex,
				updateCurrentKeyIndex
			}
		};
		this.worker.postMessage(msg);
	}
	postEnable(enabled, participantIdentity) {
		if (this.worker) {
			const enableMsg = {
				kind: "enable",
				data: {
					enabled,
					participantIdentity
				}
			};
			this.worker.postMessage(enableMsg);
		} else throw new ReferenceError("failed to enable e2ee, worker is not ready");
	}
	postRTPMap(map) {
		var _a;
		if (!this.worker) throw TypeError("could not post rtp map, worker is missing");
		if (!((_a = this.room) === null || _a === void 0 ? void 0 : _a.localParticipant.identity)) throw TypeError("could not post rtp map, local participant identity is missing");
		const msg = {
			kind: "setRTPMap",
			data: {
				map,
				participantIdentity: this.room.localParticipant.identity
			}
		};
		this.worker.postMessage(msg);
	}
	postSifTrailer(trailer) {
		if (!this.worker) throw Error("could not post SIF trailer, worker is missing");
		const msg = {
			kind: "setSifTrailer",
			data: { trailer }
		};
		this.worker.postMessage(msg);
	}
	setupE2EEReceiver(track, remoteId, trackInfo) {
		if (!track.receiver) return;
		if (!(trackInfo === null || trackInfo === void 0 ? void 0 : trackInfo.mimeType) || trackInfo.mimeType === "") throw new TypeError("MimeType missing from trackInfo, cannot set up E2EE cryptor");
		const hasPacketTrailer = track.kind === "video" && !!trackInfo.packetTrailerFeatures && trackInfo.packetTrailerFeatures.length > 0;
		this.handleReceiver(track.receiver, track.mediaStreamID, remoteId, track.kind === "video" ? mimeTypeToVideoCodecString(trackInfo.mimeType) : void 0, hasPacketTrailer);
	}
	setupE2EESender(track, sender) {
		var _a, _b, _c;
		if (!isLocalTrack(track) || !sender) {
			if (!sender) livekitLogger.warn("early return because sender is not ready");
			return;
		}
		this.handleSender(sender, track.mediaStreamID, void 0, isVideoTrack(track) ? (_b = (_a = track.publishOptions) === null || _a === void 0 ? void 0 : _a.frameMetadata) !== null && _b !== void 0 ? _b : (_c = track.publishOptions) === null || _c === void 0 ? void 0 : _c.packetTrailer : void 0);
	}
	/**
	* Handles the given {@code RTCRtpReceiver} by creating a {@code TransformStream} which will inject
	* a frame decoder.
	*
	*/
	handleReceiver(receiver, trackId, participantIdentity, codec, hasPacketTrailer) {
		return __awaiter(this, void 0, void 0, function* () {
			if (!this.worker) return;
			if (isScriptTransformSupportedForWorker()) {
				const options = {
					kind: "decode",
					participantIdentity,
					trackId,
					codec,
					hasPacketTrailer
				};
				receiver.transform = new RTCRtpScriptTransform(this.worker, options);
			} else {
				if (E2EE_FLAG in receiver && codec) {
					const msg = {
						kind: "updateCodec",
						data: {
							trackId,
							codec,
							participantIdentity,
							hasPacketTrailer
						}
					};
					this.worker.postMessage(msg);
					return;
				}
				let writable = receiver.writableStream;
				let readable = receiver.readableStream;
				if (!writable || !readable) {
					const receiverStreams = receiver.createEncodedStreams();
					receiver.writableStream = receiverStreams.writable;
					writable = receiverStreams.writable;
					receiver.readableStream = receiverStreams.readable;
					readable = receiverStreams.readable;
				}
				const msg = {
					kind: "decode",
					data: {
						readableStream: readable,
						writableStream: writable,
						trackId,
						codec,
						participantIdentity,
						isReuse: E2EE_FLAG in receiver,
						hasPacketTrailer
					}
				};
				this.worker.postMessage(msg, [readable, writable]);
			}
			receiver[E2EE_FLAG] = true;
		});
	}
	/**
	* Handles the given {@code RTCRtpSender} by creating a {@code TransformStream} which will inject
	* a frame encoder.
	*
	*/
	handleSender(sender, trackId, codec, frameMetadata) {
		var _a;
		if (E2EE_FLAG in sender || !this.worker) return;
		if (!((_a = this.room) === null || _a === void 0 ? void 0 : _a.localParticipant.identity) || this.room.localParticipant.identity === "") throw TypeError("local identity needs to be known in order to set up encrypted sender");
		if (isScriptTransformSupportedForWorker()) {
			livekitLogger.info("initialize script transform");
			const options = {
				kind: "encode",
				participantIdentity: this.room.localParticipant.identity,
				trackId,
				codec,
				hasPacketTrailer: hasFrameMetadataPublishOptions(frameMetadata),
				packetTrailer: frameMetadata
			};
			sender.transform = new RTCRtpScriptTransform(this.worker, options);
		} else {
			livekitLogger.info("initialize encoded streams");
			const senderStreams = sender.createEncodedStreams();
			const msg = {
				kind: "encode",
				data: {
					readableStream: senderStreams.readable,
					writableStream: senderStreams.writable,
					codec,
					trackId,
					participantIdentity: this.room.localParticipant.identity,
					isReuse: false,
					hasPacketTrailer: hasFrameMetadataPublishOptions(frameMetadata),
					packetTrailer: frameMetadata
				}
			};
			this.worker.postMessage(msg, [senderStreams.readable, senderStreams.writable]);
		}
		sender[E2EE_FLAG] = true;
	}
};
var MAX_ENTRIES = 300;
/**
* Caches frame metadata extracted from received video frames,
* keyed by RTP timestamp so it can be looked up when the frame is displayed.
*
* Metadata is populated either by the frame metadata worker managed by
* `FrameMetadataManager` (non-E2EE) or by the E2EE FrameCryptor worker
* after decryption (E2EE).
*
* @experimental
*/
var FrameMetadataExtractor = class {
	constructor() {
		this.metadataMap = /* @__PURE__ */ new Map();
		this.activeSsrc = 0;
	}
	storeMetadata(rtpTimestamp, ssrc, metadata) {
		if (this.activeSsrc !== 0 && this.activeSsrc !== ssrc) this.metadataMap.clear();
		this.activeSsrc = ssrc;
		while (this.metadataMap.size >= MAX_ENTRIES) {
			const evicted = this.metadataMap.keys().next().value;
			this.metadataMap.delete(evicted);
		}
		this.metadataMap.set(rtpTimestamp, metadata);
	}
	lookupMetadata(rtpTimestamp) {
		return this.metadataMap.get(rtpTimestamp);
	}
	dispose() {
		this.metadataMap.clear();
		this.activeSsrc = 0;
	}
};
var FrameMetadataManager = class {
	constructor(options) {
		this.extractors = /* @__PURE__ */ new Map();
		/**
		* Tracks the trackId associated with each receiver that has had its
		* encoded streams handed off to the worker. Used to detect receiver
		* reuse (transceiver recycling) so we can remap trackIds instead of
		* re-transferring already-consumed streams.
		*/
		this.workerPipelines = /* @__PURE__ */ new Map();
		this.onWorkerMessage = (ev) => {
			const msg = ev.data;
			if (msg.kind === "metadata") {
				const extractor = this.extractors.get(msg.data.trackId);
				if (extractor) extractor.storeMetadata(msg.data.rtpTimestamp, msg.data.ssrc, msg.data.metadata);
			}
		};
		this.onWorkerError = (ev) => {
			livekitLogger.error("frame metadata worker encountered an error:", { error: ev.error });
		};
		this.worker = options === null || options === void 0 ? void 0 : options.worker;
	}
	/** @internal */
	setup(room) {
		if (room === this.room) return;
		this.room = room;
		if (this.worker) {
			this.worker.onmessage = this.onWorkerMessage;
			this.worker.onerror = this.onWorkerError;
			this.worker.postMessage({ kind: "init" });
		}
		room.on(RoomEvent.TrackSubscribed, (track, pub, _participant) => {
			if (track.kind !== "video") return;
			this.setupReceiver(track, pub.trackInfo);
		}).on(RoomEvent.TrackUnsubscribed, (track) => {
			this.teardownTrack(track);
		}).on(RoomEvent.Disconnected, () => {
			this.cleanup();
		});
	}
	setupReceiver(track, trackInfo) {
		var _a, _b, _c;
		const receiver = track.receiver;
		if (!receiver) return;
		if (!(!!(trackInfo === null || trackInfo === void 0 ? void 0 : trackInfo.packetTrailerFeatures) && trackInfo.packetTrailerFeatures.length > 0)) {
			if (!((_a = this.room) === null || _a === void 0 ? void 0 : _a.hasE2EESetup)) this.setupPassthroughReceiver(receiver, track.mediaStreamID);
			return;
		}
		if (!isFrameMetadataSupported(this.worker ? { worker: this.worker } : void 0) && !((_b = this.room) === null || _b === void 0 ? void 0 : _b.hasE2EESetup)) {
			livekitLogger.warn("frame metadata transform not supported; skipping extraction");
			return;
		}
		const extractor = new FrameMetadataExtractor();
		const trackId = track.mediaStreamID;
		this.extractors.set(trackId, extractor);
		track.frameMetadataExtractor = extractor;
		if ((_c = this.room) === null || _c === void 0 ? void 0 : _c.hasE2EESetup) return;
		this.setupWorkerReceiver(receiver, trackId, true);
	}
	setupPassthroughReceiver(receiver, trackId) {
		if (shouldUseFrameMetadataScriptTransform()) {
			if ("transform" in receiver) receiver.transform = null;
			return;
		}
		if (this.worker && isFrameMetadataSupported({ worker: this.worker }) && !this.workerPipelines.has(receiver)) {
			this.setupWorkerReceiver(receiver, trackId, false);
			return;
		}
		if (this.worker && this.workerPipelines.has(receiver)) this.setupWorkerReceiver(receiver, trackId, false);
	}
	setupWorkerReceiver(receiver, newTrackId) {
		let hasPacketTrailer = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : true;
		const worker = this.worker;
		if (!worker) return;
		if (shouldUseFrameMetadataScriptTransform()) {
			receiver.transform = new RTCRtpScriptTransform(worker, {
				kind: "decode",
				trackId: newTrackId
			});
			return;
		}
		const existingTrackId = this.workerPipelines.get(receiver);
		if (existingTrackId) {
			const msg = {
				kind: "updateTrackId",
				data: {
					oldTrackId: existingTrackId,
					newTrackId,
					hasPacketTrailer
				}
			};
			worker.postMessage(msg);
			this.workerPipelines.set(receiver, newTrackId);
			return;
		}
		if (!("createEncodedStreams" in receiver)) {
			livekitLogger.warn("createEncodedStreams not supported");
			return;
		}
		let streams;
		try {
			streams = receiver.createEncodedStreams();
		} catch (err) {
			livekitLogger.warn("failed to create encoded streams", { error: err });
			return;
		}
		const msg = {
			kind: "decode",
			data: {
				readableStream: streams.readable,
				writableStream: streams.writable,
				trackId: newTrackId,
				hasPacketTrailer
			}
		};
		worker.postMessage(msg, [streams.readable, streams.writable]);
		this.workerPipelines.set(receiver, newTrackId);
	}
	teardownTrack(track) {
		const trackId = track.mediaStreamID;
		const extractor = this.extractors.get(trackId);
		if (extractor) {
			extractor.dispose();
			this.extractors.delete(trackId);
		}
		if (track instanceof RemoteVideoTrack) track.frameMetadataExtractor = void 0;
	}
	cleanup() {
		var _a;
		for (const extractor of this.extractors.values()) extractor.dispose();
		this.extractors.clear();
		this.workerPipelines.clear();
		(_a = this.worker) === null || _a === void 0 || _a.terminate();
	}
};
var CONNECTION_BACKOFF_MIN_MS = 500;
var CONNECTION_BACKOFF_MAX_MS = 15e3;
/**
* BackOffStrategy implements exponential backoff for connection failures.
*
* When severe connection failures occur (e.g., network issues, server unavailability),
* this strategy introduces increasing delays between reconnection attempts to avoid
* overwhelming the server and to give transient issues time to resolve.
*
* This strategy is only applied to LiveKit Cloud projects. It identifies
* projects by extracting the project name from the connection URL and tracks failures
* per project. Self-hosted deployments (URLs without a project identifier) are not
* subject to backoff delays.
*
* The class is implemented as a singleton to maintain consistent backoff state across
* the entire application lifecycle instead of room instance lifecycle.
*/
var BackOffStrategy = class BackOffStrategy {
	constructor() {
		this.failedConnectionAttempts = /* @__PURE__ */ new Map();
		this.backOffPromises = /* @__PURE__ */ new Map();
	}
	static getInstance() {
		if (!this._instance) this._instance = new BackOffStrategy();
		return this._instance;
	}
	addFailedConnectionAttempt(urlString) {
		var _a;
		const projectName = extractProjectFromUrl(new URL(urlString));
		if (!projectName) return;
		let failureCount = (_a = this.failedConnectionAttempts.get(projectName)) !== null && _a !== void 0 ? _a : 0;
		this.failedConnectionAttempts.set(projectName, failureCount + 1);
		this.backOffPromises.set(projectName, sleep(Math.min(CONNECTION_BACKOFF_MIN_MS * Math.pow(2, failureCount), CONNECTION_BACKOFF_MAX_MS)));
	}
	getBackOffPromise(urlString) {
		const url = new URL(urlString);
		const projectName = url && extractProjectFromUrl(url);
		return projectName && this.backOffPromises.get(projectName) || Promise.resolve();
	}
	resetFailedConnectionAttempts(urlString) {
		const url = new URL(urlString);
		const projectName = url && extractProjectFromUrl(url);
		if (projectName) {
			this.failedConnectionAttempts.set(projectName, 0);
			this.backOffPromises.set(projectName, Promise.resolve());
		}
	}
	resetAll() {
		this.backOffPromises.clear();
		this.failedConnectionAttempts.clear();
	}
};
BackOffStrategy._instance = null;
var defaultId = "default";
var DeviceManager = class DeviceManager {
	constructor() {
		this._previousDevices = [];
	}
	static getInstance() {
		if (this.instance === void 0) this.instance = new DeviceManager();
		return this.instance;
	}
	get previousDevices() {
		return this._previousDevices;
	}
	getDevices(kind_1) {
		return __awaiter(this, arguments, void 0, function(kind) {
			var _this = this;
			let requestPermissions = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : true;
			return function* () {
				var _a;
				if (((_a = DeviceManager.userMediaPromiseMap) === null || _a === void 0 ? void 0 : _a.size) > 0) {
					livekitLogger.debug("awaiting getUserMedia promise");
					try {
						if (kind) yield DeviceManager.userMediaPromiseMap.get(kind);
						else yield Promise.all(DeviceManager.userMediaPromiseMap.values());
					} catch (e) {
						livekitLogger.warn("error waiting for media permissons");
					}
				}
				let devices = yield navigator.mediaDevices.enumerateDevices();
				if (requestPermissions && !(isSafari() && _this.hasDeviceInUse(kind))) {
					if (devices.filter((d) => d.kind === kind).length === 0 || devices.some((device) => {
						const noLabel = device.label === "";
						const isRelevant = kind ? device.kind === kind : true;
						return noLabel && isRelevant;
					})) {
						const permissionsToAcquire = {
							video: kind !== "audioinput" && kind !== "audiooutput",
							audio: kind !== "videoinput" && { deviceId: { ideal: "default" } }
						};
						const stream = yield navigator.mediaDevices.getUserMedia(permissionsToAcquire);
						devices = yield navigator.mediaDevices.enumerateDevices();
						stream.getTracks().forEach((track) => {
							track.stop();
						});
					}
				}
				_this._previousDevices = devices;
				if (kind) devices = devices.filter((device) => device.kind === kind);
				return devices;
			}();
		});
	}
	normalizeDeviceId(kind, deviceId, groupId) {
		return __awaiter(this, void 0, void 0, function* () {
			if (deviceId !== defaultId) return deviceId;
			const devices = yield this.getDevices(kind);
			const defaultDevice = devices.find((d) => d.deviceId === defaultId);
			if (!defaultDevice) {
				livekitLogger.warn("could not reliably determine default device");
				return;
			}
			const device = devices.find((d) => d.deviceId !== defaultId && d.groupId === (groupId !== null && groupId !== void 0 ? groupId : defaultDevice.groupId));
			if (!device) {
				livekitLogger.warn("could not reliably determine default device");
				return;
			}
			return device === null || device === void 0 ? void 0 : device.deviceId;
		});
	}
	hasDeviceInUse(kind) {
		return kind ? DeviceManager.userMediaPromiseMap.has(kind) : DeviceManager.userMediaPromiseMap.size > 0;
	}
};
DeviceManager.mediaDeviceKinds = [
	"audioinput",
	"audiooutput",
	"videoinput"
];
DeviceManager.userMediaPromiseMap = /* @__PURE__ */ new Map();
var U16_MAX_SIZE = 65535;
var U32_MAX_SIZE = 4294967295;
/**
* A number of fields withing the data tracks packet specification assume wrap around behavior when
* an unsigned type is incremented beyond its max size (ie, the packet `sequence` field). This
* wrapper type manually reimplements this wrap around behavior given javascript's lack of fixed
* size integer types.
*/
var WrapAroundUnsignedInt = class WrapAroundUnsignedInt {
	static u16(raw) {
		return new WrapAroundUnsignedInt(raw, U16_MAX_SIZE);
	}
	static u32(raw) {
		return new WrapAroundUnsignedInt(raw, U32_MAX_SIZE);
	}
	constructor(raw, maxSize) {
		this.value = raw;
		if (raw < 0) throw new Error("WrapAroundUnsignedInt: cannot faithfully represent an integer smaller than 0");
		if (maxSize > Number.MAX_SAFE_INTEGER) throw new Error("WrapAroundUnsignedInt: cannot faithfully represent an integer bigger than MAX_SAFE_INTEGER.");
		this.maxSize = maxSize;
		this.clamp();
	}
	/** Manually clamp the given containing value according to the wrap around max size bounds. Use
	* this after out of bounds modification to the contained value by external code. */
	clamp() {
		while (this.value > this.maxSize) this.value -= this.maxSize + 1;
		while (this.value < 0) this.value += this.maxSize + 1;
	}
	clone() {
		return new WrapAroundUnsignedInt(this.value, this.maxSize);
	}
	/** When called, maps the containing value to a new containing value. After mapping, the wrap
	* around external max size bounds are applied. Note that this is a mutative operation. */
	update(updateFn) {
		this.value = updateFn(this.value);
		this.clamp();
	}
	/** Increments the given `n` to the inner value. Note that this is a mutative operation. */
	increment() {
		let n = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 1;
		this.update((value) => value + n);
	}
	/** Decrements the given `n` from the inner value. Note that this is a mutative operation. */
	decrement() {
		let n = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 1;
		this.update((value) => value - n);
	}
	getThenIncrement() {
		const previousValue = this.value;
		this.increment();
		return new WrapAroundUnsignedInt(previousValue, this.maxSize);
	}
	/** Returns true if {@link this} is before the passed other {@link WrapAroundUnsignedInt}. */
	isBefore(other) {
		const a = this.value >>> 0;
		const diff = (other.value >>> 0) - a >>> 0;
		return diff !== 0 && diff < this.maxSize + 1;
	}
};
var DataTrackTimestamp = class DataTrackTimestamp {
	static fromRtpTicks(rtpTicks) {
		return new DataTrackTimestamp(rtpTicks, 9e4);
	}
	/** Generates a timestamp initialized to a non cryptographically secure random value, so that
	* different streams are more difficult to correlate in packet capture. */
	static rtpRandom() {
		const randomValue = Math.round(Math.random() * U32_MAX_SIZE);
		return DataTrackTimestamp.fromRtpTicks(randomValue);
	}
	constructor(raw, rateInHz) {
		this.timestamp = WrapAroundUnsignedInt.u32(raw);
		this.rateInHz = rateInHz;
	}
	asTicks() {
		return this.timestamp.value;
	}
	clone() {
		return new DataTrackTimestamp(this.timestamp.value, this.rateInHz);
	}
	wrappingAdd(n) {
		this.timestamp.increment(n);
	}
	/** Returns true if {@link this} is before the passed other {@link DataTrackTimestamp}. */
	isBefore(other) {
		return this.timestamp.isBefore(other.timestamp);
	}
};
var DataTrackClock = class DataTrackClock {
	constructor(rateInHz, epoch, base) {
		this.epoch = epoch;
		this.base = base;
		this.previous = base.clone();
		this.rateInHz = rateInHz;
	}
	static startingNow(base, rateInHz) {
		return new DataTrackClock(rateInHz, /* @__PURE__ */ new Date(), base);
	}
	static startingAtTime(epoch, base, rateInHz) {
		return new DataTrackClock(rateInHz, epoch, base);
	}
	static rtpStartingNow(base) {
		return DataTrackClock.startingNow(base, 9e4);
	}
	static rtpStartingAtTime(epoch, base) {
		return DataTrackClock.startingAtTime(epoch, base, 9e4);
	}
	now() {
		return this.at(/* @__PURE__ */ new Date());
	}
	at(timestamp) {
		let elapsedMs = timestamp.getTime() - this.epoch.getTime();
		let durationTicks = DataTrackClock.durationInMsToTicks(elapsedMs, this.rateInHz);
		let result = this.base.clone();
		result.wrappingAdd(durationTicks);
		if (result.isBefore(this.previous)) result = this.previous;
		this.previous = result.clone();
		return result.clone();
	}
	/** Convert a duration since the epoch into clock ticks. */
	static durationInMsToTicks(durationMilliseconds, rateInHz) {
		let ticks = (durationMilliseconds * 1e6 * rateInHz + 5e8) / 1e9;
		return Math.round(ticks);
	}
};
function coerceToDataView(input) {
	if (input instanceof DataView) return input;
	else if (input instanceof ArrayBuffer) return new DataView(input);
	else if (input instanceof Uint8Array) return new DataView(input.buffer, input.byteOffset, input.byteLength);
	else throw new Error("Error coercing ".concat(input, " to DataView - input was not DataView, ArrayBuffer, or Uint8Array."));
}
var DataTrackHandleErrorReason;
(function(DataTrackHandleErrorReason) {
	DataTrackHandleErrorReason[DataTrackHandleErrorReason["Reserved"] = 0] = "Reserved";
	DataTrackHandleErrorReason[DataTrackHandleErrorReason["TooLarge"] = 1] = "TooLarge";
})(DataTrackHandleErrorReason || (DataTrackHandleErrorReason = {}));
var DataTrackHandleError = class DataTrackHandleError extends LivekitReasonedError {
	constructor(message, reason) {
		super(19, message);
		this.name = "DataTrackHandleError";
		this.reason = reason;
		this.reasonName = DataTrackHandleErrorReason[reason];
	}
	isReason(reason) {
		return this.reason === reason;
	}
	static tooLarge() {
		return new DataTrackHandleError("Value too large to be a valid track handle", DataTrackHandleErrorReason.TooLarge);
	}
	static reserved(value) {
		return new DataTrackHandleError("0x".concat(value.toString(16), " is a reserved value."), DataTrackHandleErrorReason.Reserved);
	}
};
var DataTrackHandle = { fromNumber(raw) {
	if (raw === 0) throw DataTrackHandleError.reserved(raw);
	if (raw > U16_MAX_SIZE) throw DataTrackHandleError.tooLarge();
	return raw;
} };
/** Manage allocating new handles which don't conflict over the lifetime of the client. */
var DataTrackHandleAllocator = class {
	constructor() {
		this.value = 0;
	}
	/** Returns a unique track handle for the next publication, if one can be obtained. */
	get() {
		this.value += 1;
		if (this.value > U16_MAX_SIZE) return null;
		return this.value;
	}
	reset() {
		this.value = 0;
	}
};
var DataTrackInfo = {
	from(protocolInfo) {
		return {
			sid: protocolInfo.sid,
			pubHandle: protocolInfo.pubHandle,
			name: protocolInfo.name,
			usesE2ee: protocolInfo.encryption !== Encryption_Type.NONE
		};
	},
	toProtobuf(info) {
		return new DataTrackInfo$1({
			sid: info.sid,
			pubHandle: info.pubHandle,
			name: info.name,
			encryption: info.usesE2ee ? Encryption_Type.GCM : Encryption_Type.NONE
		});
	}
};
var QueueTaskStatus;
(function(QueueTaskStatus) {
	QueueTaskStatus[QueueTaskStatus["WAITING"] = 0] = "WAITING";
	QueueTaskStatus[QueueTaskStatus["RUNNING"] = 1] = "RUNNING";
	QueueTaskStatus[QueueTaskStatus["COMPLETED"] = 2] = "COMPLETED";
})(QueueTaskStatus || (QueueTaskStatus = {}));
var AsyncQueue = class {
	constructor() {
		this.pendingTasks = /* @__PURE__ */ new Map();
		this.taskMutex = new _();
		this.nextTaskIndex = 0;
	}
	run(task) {
		return __awaiter(this, void 0, void 0, function* () {
			const taskInfo = {
				id: this.nextTaskIndex++,
				enqueuedAt: Date.now(),
				status: QueueTaskStatus.WAITING
			};
			this.pendingTasks.set(taskInfo.id, taskInfo);
			const unlock = yield this.taskMutex.lock();
			try {
				taskInfo.executedAt = Date.now();
				taskInfo.status = QueueTaskStatus.RUNNING;
				return yield task();
			} finally {
				taskInfo.status = QueueTaskStatus.COMPLETED;
				this.pendingTasks.delete(taskInfo.id);
				unlock();
			}
		});
	}
	flush() {
		return __awaiter(this, void 0, void 0, function* () {
			return this.run(() => __awaiter(this, void 0, void 0, function* () {}));
		});
	}
	snapshot() {
		return Array.from(this.pendingTasks.values());
	}
};
var WebSocketStream = class {
	get readyState() {
		return this.ws.readyState;
	}
	constructor(url) {
		let options = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
		var _a, _b;
		if ((_a = options.signal) === null || _a === void 0 ? void 0 : _a.aborted) throw new DOMException("This operation was aborted", "AbortError");
		this.url = url;
		const ws = new WebSocket(url, (_b = options.protocols) !== null && _b !== void 0 ? _b : []);
		ws.binaryType = "arraybuffer";
		this.ws = ws;
		const closeWithInfo = function() {
			let _ref = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {}, code = _ref.closeCode, reason = _ref.reason;
			return ws.close(code, reason);
		};
		this.opened = new TypedPromise((resolve, reject) => {
			const rejectHandler = () => {
				reject(ConnectionError.websocket("Encountered websocket error during connection establishment"));
			};
			ws.onopen = () => {
				resolve({
					readable: new ReadableStream({
						start(controller) {
							ws.onmessage = (_ref2) => {
								let data = _ref2.data;
								return controller.enqueue(data);
							};
							ws.onerror = (e) => controller.error(e);
						},
						cancel: closeWithInfo
					}),
					writable: new WritableStream({
						write(chunk) {
							ws.send(chunk);
						},
						abort() {
							ws.close();
						},
						close: closeWithInfo
					}),
					protocol: ws.protocol,
					extensions: ws.extensions
				});
				ws.removeEventListener("error", rejectHandler);
			};
			ws.addEventListener("error", rejectHandler);
		});
		this.closed = new TypedPromise((resolve, reject) => {
			const rejectHandler = () => __awaiter(this, void 0, void 0, function* () {
				const closePromise = new TypedPromise((res) => {
					if (ws.readyState === WebSocket.CLOSED) return;
					else ws.addEventListener("close", (closeEv) => {
						res(closeEv);
					}, { once: true });
				});
				const reason = yield TypedPromise.race([sleep(250), closePromise]);
				if (!reason) reject(ConnectionError.websocket("Encountered unspecified websocket error without a timely close event"));
				else resolve(reason);
			});
			ws.onclose = (_ref3) => {
				let code = _ref3.code, reason = _ref3.reason;
				resolve({
					closeCode: code,
					reason
				});
				ws.removeEventListener("error", rejectHandler);
			};
			ws.addEventListener("error", rejectHandler);
		});
		if (options.signal) options.signal.onabort = () => ws.close();
		this.close = closeWithInfo;
	}
};
var passThroughQueueSignals = [
	"syncState",
	"trickle",
	"offer",
	"answer",
	"simulate",
	"leave"
];
function canPassThroughQueue(req) {
	const canPass = passThroughQueueSignals.indexOf(req.case) >= 0;
	livekitLogger.trace("request allowed to bypass queue:", {
		canPass,
		req
	});
	return canPass;
}
var SignalConnectionState;
(function(SignalConnectionState) {
	SignalConnectionState[SignalConnectionState["CONNECTING"] = 0] = "CONNECTING";
	SignalConnectionState[SignalConnectionState["CONNECTED"] = 1] = "CONNECTED";
	SignalConnectionState[SignalConnectionState["RECONNECTING"] = 2] = "RECONNECTING";
	SignalConnectionState[SignalConnectionState["DISCONNECTING"] = 3] = "DISCONNECTING";
	SignalConnectionState[SignalConnectionState["DISCONNECTED"] = 4] = "DISCONNECTED";
})(SignalConnectionState || (SignalConnectionState = {}));
/** specifies how much time (in ms) we allow for the ws to close its connection gracefully before continuing */
var MAX_WS_CLOSE_TIME = 250;
/**
* How long (in ms) to wait for the first message after the WebSocket upgrade.
*/
var JOIN_RESPONSE_TIMEOUT = 5e3;
/** @internal */
var SignalClient = class {
	get currentState() {
		return this.state;
	}
	get isDisconnected() {
		return this.state === SignalConnectionState.DISCONNECTING || this.state === SignalConnectionState.DISCONNECTED;
	}
	get isEstablishingConnection() {
		return this.state === SignalConnectionState.CONNECTING || this.state === SignalConnectionState.RECONNECTING;
	}
	getNextRequestId() {
		this._requestId += 1;
		return this._requestId;
	}
	constructor() {
		let useJSON = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : false;
		let loggerOptions = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
		var _a;
		/** signal rtt in milliseconds */
		this.rtt = 0;
		this.state = SignalConnectionState.DISCONNECTED;
		this.log = livekitLogger;
		this._requestId = 0;
		this.useV0SignalPath = false;
		/** @internal */
		this.resetCallbacks = () => {
			this.onAnswer = void 0;
			this.onLeave = void 0;
			this.onLocalTrackPublished = void 0;
			this.onLocalTrackUnpublished = void 0;
			this.onNegotiateRequested = void 0;
			this.onOffer = void 0;
			this.onRemoteMuteChanged = void 0;
			this.onSubscribedQualityUpdate = void 0;
			this.onTokenRefresh = void 0;
			this.onTrickle = void 0;
			this.onClose = void 0;
			this.onMediaSectionsRequirement = void 0;
		};
		this.loggerContextCb = loggerOptions.loggerContextCb;
		this.log = getLogger((_a = loggerOptions.loggerName) !== null && _a !== void 0 ? _a : LoggerNames.Signal, () => this.logContext);
		this.useJSON = useJSON;
		this.requestQueue = new AsyncQueue();
		this.queuedRequests = [];
		this.closingLock = new _();
		this.connectionLock = new _();
		this.state = SignalConnectionState.DISCONNECTED;
	}
	get logContext() {
		var _a, _b;
		return (_b = (_a = this.loggerContextCb) === null || _a === void 0 ? void 0 : _a.call(this)) !== null && _b !== void 0 ? _b : {};
	}
	join(url_1, token_1, opts_1, abortSignal_1) {
		return __awaiter(this, arguments, void 0, function(url, token, opts, abortSignal) {
			var _this = this;
			let useV0Path = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : false;
			let publisherOffer = arguments.length > 5 ? arguments[5] : void 0;
			return function* () {
				_this.state = SignalConnectionState.CONNECTING;
				_this.options = opts;
				return yield _this.connect(url, token, opts, abortSignal, useV0Path, publisherOffer);
			}();
		});
	}
	reconnect(url, token, sid, reason) {
		return __awaiter(this, void 0, void 0, function* () {
			if (!this.options) {
				this.log.warn("attempted to reconnect without signal options being set, ignoring");
				return;
			}
			this.state = SignalConnectionState.RECONNECTING;
			this.clearPingInterval();
			return yield this.connect(url, token, Object.assign(Object.assign({}, this.options), {
				reconnect: true,
				sid,
				reconnectReason: reason
			}), void 0, this.useV0SignalPath);
		});
	}
	connect(url_1, token_1, opts_1, abortSignal_1) {
		return __awaiter(this, arguments, void 0, function(url, token, opts, abortSignal) {
			var _this2 = this;
			let useV0Path = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : false;
			let publisherOffer = arguments.length > 5 ? arguments[5] : void 0;
			return function* () {
				const unlock = yield _this2.connectionLock.lock();
				_this2.connectOptions = opts;
				_this2.useV0SignalPath = useV0Path;
				const clientInfo = getClientInfo(opts.clientInfoCapabilities);
				const rtcUrl = createRtcUrl(url, useV0Path ? createConnectionParams(token, clientInfo, opts) : yield createJoinRequestConnectionParams(token, clientInfo, opts, publisherOffer), useV0Path).toString();
				const validateUrl = createValidateUrl(rtcUrl).toString();
				return new Promise((resolve, reject) => __awaiter(_this2, void 0, void 0, function* () {
					var _a, _b;
					try {
						let alreadyAborted = false;
						const abortHandler = (eventOrError) => __awaiter(this, void 0, void 0, function* () {
							if (alreadyAborted) return;
							alreadyAborted = true;
							const reason = getAbortReasonAsString(eventOrError instanceof Event ? eventOrError.currentTarget : eventOrError, "Abort handler called");
							if (this.streamWriter && !this.isDisconnected) this.sendLeave().then(() => this.close(reason)).catch((e) => {
								this.log.error(e);
								this.close();
							});
							else this.close();
							cleanupAbortHandlers();
							reject(ConnectionError.cancelled(reason));
						});
						abortSignal === null || abortSignal === void 0 || abortSignal.addEventListener("abort", abortHandler);
						const cleanupAbortHandlers = () => {
							clearTimeout(wsTimeout);
							abortSignal === null || abortSignal === void 0 || abortSignal.removeEventListener("abort", abortHandler);
						};
						const wsTimeout = setTimeout(() => {
							abortHandler(ConnectionError.timeout("room connection has timed out (signal)"));
						}, opts.websocketTimeout);
						const handleSignalConnected = (connection, firstMessage) => {
							this.handleSignalConnected(connection, wsTimeout, firstMessage);
						};
						const redactedUrl = new URL(rtcUrl);
						if (redactedUrl.searchParams.has("access_token")) redactedUrl.searchParams.set("access_token", "<redacted>");
						if (this.ws) {
							const startClose = performance.now();
							yield this.close(false);
							this.log.debug("closed previous ws connection in ".concat(performance.now() - startClose, "ms"));
						}
						this.log.info("signal connecting to ".concat(redactedUrl), {
							reconnect: opts.reconnect,
							reconnectReason: opts.reconnectReason
						});
						this.ws = new WebSocketStream(rtcUrl);
						try {
							this.ws.closed.then((closeInfo) => {
								if (this.isEstablishingConnection) reject(ConnectionError.internal("Websocket got closed during a (re)connection attempt: ".concat(closeInfo.reason)));
								if (closeInfo.closeCode !== 1e3) {
									this.log.warn("websocket closed", {
										reason: closeInfo.reason,
										code: closeInfo.closeCode,
										wasClean: closeInfo.closeCode === 1e3,
										state: this.state
									});
									if (this.state === SignalConnectionState.CONNECTED) this.handleOnClose(closeInfo.reason || "Unexpected WS error");
								}
							}).catch((reason) => {
								if (this.isEstablishingConnection) reject(ConnectionError.internal("Websocket error during a (re)connection attempt: ".concat(reason)));
							});
							const connection = yield this.ws.opened.catch((reason) => __awaiter(this, void 0, void 0, function* () {
								if (this.state !== SignalConnectionState.CONNECTED) {
									this.state = SignalConnectionState.DISCONNECTED;
									clearTimeout(wsTimeout);
									reject(yield this.handleConnectionError(reason, validateUrl));
									return;
								}
								this.handleWSError(reason);
								reject(reason);
							}));
							clearTimeout(wsTimeout);
							if (!connection) return;
							const signalReader = connection.readable.getReader();
							this.streamWriter = connection.writable.getWriter();
							let firstMessage;
							let firstMessageTimeout;
							try {
								firstMessage = yield Promise.race([signalReader.read(), new Promise((_, rejectRead) => {
									firstMessageTimeout = setTimeout(() => {
										rejectRead(ConnectionError.timeout("signal connection timed out while waiting for the first message"));
									}, JOIN_RESPONSE_TIMEOUT);
								})]);
							} catch (e) {
								signalReader.releaseLock();
								reject(e);
								this.close();
								return;
							} finally {
								clearTimeout(firstMessageTimeout);
							}
							signalReader.releaseLock();
							if (!firstMessage.value) throw ConnectionError.internal("no message received as first message");
							const firstSignalResponse = parseSignalResponse(firstMessage.value);
							const validation = this.validateFirstMessage(firstSignalResponse, (_a = opts.reconnect) !== null && _a !== void 0 ? _a : false);
							if (!validation.isValid) {
								reject(validation.error);
								return;
							}
							if (((_b = firstSignalResponse.message) === null || _b === void 0 ? void 0 : _b.case) === "join") {
								this.pingTimeoutDuration = firstSignalResponse.message.value.pingTimeout;
								this.pingIntervalDuration = firstSignalResponse.message.value.pingInterval;
								if (this.pingTimeoutDuration && this.pingTimeoutDuration > 0) this.log.debug("ping config", {
									timeout: this.pingTimeoutDuration,
									interval: this.pingIntervalDuration
								});
								if (this.onJoined) this.onJoined(firstSignalResponse.message.value);
							}
							handleSignalConnected(connection, validation.shouldProcessFirstMessage ? firstSignalResponse : void 0);
							resolve(validation.response);
						} catch (e) {
							reject(e);
						} finally {
							cleanupAbortHandlers();
						}
					} finally {
						unlock();
					}
				}));
			}();
		});
	}
	startReadingLoop(signalReader, firstMessage) {
		return __awaiter(this, void 0, void 0, function* () {
			if (firstMessage) this.handleSignalResponse(firstMessage);
			while (true) {
				if (this.signalLatency) yield sleep(this.signalLatency);
				const _yield$signalReader$r = yield signalReader.read(), done = _yield$signalReader$r.done, value = _yield$signalReader$r.value;
				if (done) break;
				const resp = parseSignalResponse(value);
				this.handleSignalResponse(resp);
			}
		});
	}
	close() {
		return __awaiter(this, arguments, void 0, function() {
			var _this3 = this;
			let updateState = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : true;
			let reason = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : "Close method called on signal client";
			return function* () {
				if ([SignalConnectionState.DISCONNECTING || SignalConnectionState.DISCONNECTED].includes(_this3.state)) {
					_this3.log.debug("ignoring signal close as it's already in disconnecting state");
					return;
				}
				const unlock = yield _this3.closingLock.lock();
				try {
					_this3.clearPingInterval();
					if (updateState) _this3.state = SignalConnectionState.DISCONNECTING;
					if (_this3.ws) {
						_this3.ws.close({
							closeCode: 1e3,
							reason
						});
						const closePromise = _this3.ws.closed;
						_this3.ws = void 0;
						_this3.streamWriter = void 0;
						yield Promise.race([closePromise, sleep(MAX_WS_CLOSE_TIME)]);
					}
				} catch (e) {
					_this3.log.debug("websocket error while closing", { error: e });
				} finally {
					if (updateState) _this3.state = SignalConnectionState.DISCONNECTED;
					unlock();
				}
			}();
		});
	}
	sendOffer(offer, offerId) {
		this.log.debug("sending offer", { offerSdp: offer.sdp });
		this.sendRequest({
			case: "offer",
			value: toProtoSessionDescription(offer, offerId)
		});
	}
	sendAnswer(answer, offerId) {
		this.log.debug("sending answer", { answerSdp: answer.sdp });
		return this.sendRequest({
			case: "answer",
			value: toProtoSessionDescription(answer, offerId)
		});
	}
	sendIceCandidate(candidate, target) {
		this.log.debug("sending ice candidate", { candidate });
		return this.sendRequest({
			case: "trickle",
			value: new TrickleRequest({
				candidateInit: JSON.stringify(candidate),
				target
			})
		});
	}
	sendMuteTrack(trackSid, muted) {
		return this.sendRequest({
			case: "mute",
			value: new MuteTrackRequest({
				sid: trackSid,
				muted
			})
		});
	}
	sendAddTrack(req) {
		return this.sendRequest({
			case: "addTrack",
			value: req
		});
	}
	sendUpdateLocalMetadata(metadata_1, name_1) {
		return __awaiter(this, arguments, void 0, function(metadata, name) {
			var _this4 = this;
			let attributes = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
			return function* () {
				const requestId = _this4.getNextRequestId();
				yield _this4.sendRequest({
					case: "updateMetadata",
					value: new UpdateParticipantMetadata({
						requestId,
						metadata,
						name,
						attributes
					})
				});
				return requestId;
			}();
		});
	}
	sendUpdateTrackSettings(settings) {
		this.sendRequest({
			case: "trackSetting",
			value: settings
		});
	}
	sendUpdateSubscription(sub) {
		return this.sendRequest({
			case: "subscription",
			value: sub
		});
	}
	sendSyncState(sync) {
		return this.sendRequest({
			case: "syncState",
			value: sync
		});
	}
	sendUpdateVideoLayers(trackSid, layers) {
		return this.sendRequest({
			case: "updateLayers",
			value: new UpdateVideoLayers({
				trackSid,
				layers
			})
		});
	}
	sendUpdateSubscriptionPermissions(allParticipants, trackPermissions) {
		return this.sendRequest({
			case: "subscriptionPermission",
			value: new SubscriptionPermission({
				allParticipants,
				trackPermissions
			})
		});
	}
	sendSimulateScenario(scenario) {
		return this.sendRequest({
			case: "simulate",
			value: scenario
		});
	}
	sendPing() {
		/** send both of ping and pingReq for compatibility to old and new server */
		return Promise.all([this.sendRequest({
			case: "ping",
			value: protoInt64.parse(Date.now())
		}), this.sendRequest({
			case: "pingReq",
			value: new Ping({
				timestamp: protoInt64.parse(Date.now()),
				rtt: protoInt64.parse(this.rtt)
			})
		})]);
	}
	sendUpdateLocalAudioTrack(trackSid, features) {
		return this.sendRequest({
			case: "updateAudioTrack",
			value: new UpdateLocalAudioTrack({
				trackSid,
				features
			})
		});
	}
	sendLeave() {
		return this.sendRequest({
			case: "leave",
			value: new LeaveRequest({
				reason: DisconnectReason.CLIENT_INITIATED,
				action: LeaveRequest_Action.DISCONNECT
			})
		});
	}
	sendPublishDataTrackRequest(handle, name, usesE2ee) {
		return this.sendRequest({
			case: "publishDataTrackRequest",
			value: new PublishDataTrackRequest({
				pubHandle: handle,
				name,
				encryption: usesE2ee ? Encryption_Type.GCM : Encryption_Type.NONE
			})
		});
	}
	sendUnPublishDataTrackRequest(handle) {
		return this.sendRequest({
			case: "unpublishDataTrackRequest",
			value: new UnpublishDataTrackRequest({ pubHandle: handle })
		});
	}
	sendUpdateDataSubscription(sid, subscribe) {
		return this.sendRequest({
			case: "updateDataSubscription",
			value: new UpdateDataSubscription({ updates: [new UpdateDataSubscription_Update({
				trackSid: sid,
				subscribe
			})] })
		});
	}
	sendRequest(message_1) {
		return __awaiter(this, arguments, void 0, function(message) {
			var _this5 = this;
			let fromQueue = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : false;
			return function* () {
				if (!fromQueue && !canPassThroughQueue(message) && _this5.state === SignalConnectionState.RECONNECTING) {
					_this5.queuedRequests.push(() => __awaiter(_this5, void 0, void 0, function* () {
						yield this.sendRequest(message, true);
					}));
					return;
				}
				if (!fromQueue) yield _this5.requestQueue.flush();
				if (_this5.signalLatency) yield sleep(_this5.signalLatency);
				if (_this5.isDisconnected) {
					_this5.log.debug("skipping signal request (type: ".concat(message.case, ") - SignalClient disconnected"));
					return;
				}
				if (!_this5.streamWriter) {
					_this5.log.error("cannot send signal request before connected, type: ".concat(message === null || message === void 0 ? void 0 : message.case));
					return;
				}
				const req = new SignalRequest({ message });
				try {
					if (_this5.useJSON) yield _this5.streamWriter.write(req.toJsonString());
					else yield _this5.streamWriter.write(req.toBinary().buffer);
				} catch (e) {
					_this5.log.error("error sending signal message", { error: e });
				}
			}();
		});
	}
	handleSignalResponse(res) {
		var _a, _b;
		const msg = res.message;
		if (msg == void 0) {
			this.log.debug("received unsupported message");
			return;
		}
		let pingHandled = false;
		if (msg.case === "answer") {
			const sd = fromProtoSessionDescription(msg.value);
			if (this.onAnswer) this.onAnswer(sd, msg.value.id, msg.value.midToTrackId);
		} else if (msg.case === "offer") {
			const sd = fromProtoSessionDescription(msg.value);
			if (this.onOffer) this.onOffer(sd, msg.value.id, msg.value.midToTrackId);
		} else if (msg.case === "trickle") {
			const candidate = JSON.parse(msg.value.candidateInit);
			if (this.onTrickle) this.onTrickle(candidate, msg.value.target);
		} else if (msg.case === "update") {
			if (this.onParticipantUpdate) this.onParticipantUpdate((_a = msg.value.participants) !== null && _a !== void 0 ? _a : []);
		} else if (msg.case === "trackPublished") {
			if (this.onLocalTrackPublished) this.onLocalTrackPublished(msg.value);
		} else if (msg.case === "speakersChanged") {
			if (this.onSpeakersChanged) this.onSpeakersChanged((_b = msg.value.speakers) !== null && _b !== void 0 ? _b : []);
		} else if (msg.case === "leave") {
			if (this.onLeave) this.onLeave(msg.value);
		} else if (msg.case === "mute") {
			if (this.onRemoteMuteChanged) this.onRemoteMuteChanged(msg.value.sid, msg.value.muted);
		} else if (msg.case === "roomUpdate") {
			if (this.onRoomUpdate && msg.value.room) this.onRoomUpdate(msg.value.room);
		} else if (msg.case === "connectionQuality") {
			if (this.onConnectionQuality) this.onConnectionQuality(msg.value);
		} else if (msg.case === "streamStateUpdate") {
			if (this.onStreamStateUpdate) this.onStreamStateUpdate(msg.value);
		} else if (msg.case === "subscribedQualityUpdate") {
			if (this.onSubscribedQualityUpdate) this.onSubscribedQualityUpdate(msg.value);
		} else if (msg.case === "subscriptionPermissionUpdate") {
			if (this.onSubscriptionPermissionUpdate) this.onSubscriptionPermissionUpdate(msg.value);
		} else if (msg.case === "refreshToken") {
			if (this.onTokenRefresh) this.onTokenRefresh(msg.value);
		} else if (msg.case === "trackUnpublished") {
			if (this.onLocalTrackUnpublished) this.onLocalTrackUnpublished(msg.value);
		} else if (msg.case === "subscriptionResponse") {
			if (this.onSubscriptionError) this.onSubscriptionError(msg.value);
		} else if (msg.case === "pong");
		else if (msg.case === "pongResp") {
			this.rtt = Date.now() - Number.parseInt(msg.value.lastPingTimestamp.toString());
			this.resetPingTimeout();
			pingHandled = true;
		} else if (msg.case === "requestResponse") {
			if (this.onRequestResponse) this.onRequestResponse(msg.value);
		} else if (msg.case === "trackSubscribed") {
			if (this.onLocalTrackSubscribed) this.onLocalTrackSubscribed(msg.value.trackSid);
		} else if (msg.case === "roomMoved") {
			if (this.onTokenRefresh) this.onTokenRefresh(msg.value.token);
			if (this.onRoomMoved) this.onRoomMoved(msg.value);
		} else if (msg.case === "mediaSectionsRequirement") {
			if (this.onMediaSectionsRequirement) this.onMediaSectionsRequirement(msg.value);
		} else if (msg.case === "publishDataTrackResponse") {
			if (this.onPublishDataTrackResponse) this.onPublishDataTrackResponse(msg.value);
		} else if (msg.case === "unpublishDataTrackResponse") {
			if (this.onUnPublishDataTrackResponse) this.onUnPublishDataTrackResponse(msg.value);
		} else if (msg.case === "dataTrackSubscriberHandles") {
			if (this.onDataTrackSubscriberHandles) this.onDataTrackSubscriberHandles(msg.value);
		} else this.log.debug("unsupported message", { msgCase: msg.case });
		if (!pingHandled) this.resetPingTimeout();
	}
	setReconnected() {
		while (this.queuedRequests.length > 0) {
			const req = this.queuedRequests.shift();
			if (req) this.requestQueue.run(req);
		}
	}
	handleOnClose(reason) {
		return __awaiter(this, void 0, void 0, function* () {
			if (this.state === SignalConnectionState.DISCONNECTED) return;
			const onCloseCallback = this.onClose;
			yield this.close(void 0, reason);
			this.log.info("websocket connection closed: ".concat(reason), { reason });
			if (onCloseCallback) onCloseCallback(reason);
		});
	}
	handleWSError(error) {
		this.log.error("websocket error", { error });
	}
	/**
	* Resets the ping timeout and starts a new timeout.
	* Call this after receiving a pong message
	*/
	resetPingTimeout() {
		this.clearPingTimeout();
		if (!this.pingTimeoutDuration) {
			this.log.warn("ping timeout duration not set");
			return;
		}
		this.pingTimeout = CriticalTimers.setTimeout(() => {
			this.log.warn("ping timeout triggered. last pong received at: ".concat((/* @__PURE__ */ new Date(Date.now() - this.pingTimeoutDuration * 1e3)).toUTCString()));
			this.handleOnClose("ping timeout");
		}, this.pingTimeoutDuration * 1e3);
	}
	/**
	* Clears ping timeout (does not start a new timeout)
	*/
	clearPingTimeout() {
		if (this.pingTimeout) CriticalTimers.clearTimeout(this.pingTimeout);
	}
	startPingInterval() {
		this.clearPingInterval();
		this.resetPingTimeout();
		if (!this.pingIntervalDuration) {
			this.log.warn("ping interval duration not set");
			return;
		}
		this.log.debug("start ping interval");
		this.pingInterval = CriticalTimers.setInterval(() => {
			this.sendPing();
		}, this.pingIntervalDuration * 1e3);
	}
	clearPingInterval() {
		this.log.debug("clearing ping interval");
		this.clearPingTimeout();
		if (this.pingInterval) CriticalTimers.clearInterval(this.pingInterval);
	}
	/**
	* Handles the successful connection to the signal server
	* @param connection The WebSocket connection
	* @param timeoutHandle The timeout handle to clear
	* @param firstMessage Optional first message to process
	* @internal
	*/
	handleSignalConnected(connection, timeoutHandle, firstMessage) {
		this.state = SignalConnectionState.CONNECTED;
		this.log.info("signal connected");
		clearTimeout(timeoutHandle);
		this.startPingInterval();
		this.startReadingLoop(connection.readable.getReader(), firstMessage);
	}
	/**
	* Validates the first message received from the signal server
	* @param firstSignalResponse The first signal response received
	* @param isReconnect Whether this is a reconnection attempt
	* @returns Validation result with response or error
	* @internal
	*/
	validateFirstMessage(firstSignalResponse, isReconnect) {
		var _a, _b, _c, _d, _e;
		if (((_a = firstSignalResponse.message) === null || _a === void 0 ? void 0 : _a.case) === "join") return {
			isValid: true,
			response: firstSignalResponse.message.value
		};
		else if (this.state === SignalConnectionState.RECONNECTING && ((_b = firstSignalResponse.message) === null || _b === void 0 ? void 0 : _b.case) !== "leave") {
			if (((_c = firstSignalResponse.message) === null || _c === void 0 ? void 0 : _c.case) === "reconnect") return {
				isValid: true,
				response: firstSignalResponse.message.value
			};
			else {
				this.log.debug("declaring signal reconnected without reconnect response received");
				return {
					isValid: true,
					response: void 0,
					shouldProcessFirstMessage: true
				};
			}
		} else if (this.isEstablishingConnection && ((_d = firstSignalResponse.message) === null || _d === void 0 ? void 0 : _d.case) === "leave") return {
			isValid: false,
			error: ConnectionError.leaveRequest("Received leave request while trying to (re)connect", firstSignalResponse.message.value.reason)
		};
		else if (!isReconnect) return {
			isValid: false,
			error: ConnectionError.internal("did not receive join response, got ".concat((_e = firstSignalResponse.message) === null || _e === void 0 ? void 0 : _e.case, " instead"))
		};
		return {
			isValid: false,
			error: ConnectionError.internal("Unexpected first message")
		};
	}
	/**
	* Handles WebSocket connection errors by validating with the server
	* @param reason The error that occurred
	* @param validateUrl The URL to validate the connection with
	* @returns A ConnectionError with appropriate reason and status
	* @internal
	*/
	handleConnectionError(reason, validateUrl) {
		return __awaiter(this, void 0, void 0, function* () {
			try {
				const resp = yield fetch(validateUrl);
				switch (resp.status) {
					case 404:
						const errorMsg = yield resp.text();
						if (errorMsg.includes("requested room does not exist")) return ConnectionError.notAllowed(errorMsg, resp.status);
						return ConnectionError.serviceNotFound("v1 RTC path not found. Consider upgrading your LiveKit server version", "v0-rtc");
					case 401:
					case 403:
						const msg = yield resp.text();
						return ConnectionError.notAllowed(msg, resp.status);
				}
				if (reason instanceof ConnectionError) return reason;
				else return ConnectionError.internal("Encountered unknown websocket error during connection: ".concat(reason), {
					status: resp.status,
					statusText: resp.statusText
				});
			} catch (e) {
				return e instanceof ConnectionError ? e : ConnectionError.serverUnreachable(e instanceof Error ? e.message : "server was not reachable");
			}
		});
	}
};
function fromProtoSessionDescription(sd) {
	const rsd = {
		type: "offer",
		sdp: sd.sdp
	};
	switch (sd.type) {
		case "answer":
		case "offer":
		case "pranswer":
		case "rollback": rsd.type = sd.type;
	}
	return rsd;
}
function toProtoSessionDescription(rsd, id) {
	return new SessionDescription({
		sdp: rsd.sdp,
		type: rsd.type,
		id
	});
}
function createConnectionParams(token, info, opts) {
	var _a;
	const params = new URLSearchParams();
	params.set("access_token", token);
	if (opts.reconnect) {
		params.set("reconnect", "1");
		if (opts.sid) params.set("sid", opts.sid);
	}
	params.set("auto_subscribe", opts.autoSubscribe ? "1" : "0");
	params.set("sdk", isReactNative() ? "reactnative" : "js");
	params.set("version", info.version);
	params.set("protocol", info.protocol.toString());
	params.set("client_protocol", info.clientProtocol.toString());
	if (info.deviceModel) params.set("device_model", info.deviceModel);
	if (info.os) params.set("os", info.os);
	if (info.osVersion) params.set("os_version", info.osVersion);
	if (info.browser) params.set("browser", info.browser);
	if (info.browserVersion) params.set("browser_version", info.browserVersion);
	if (opts.adaptiveStream) params.set("adaptive_stream", "1");
	if (opts.reconnectReason) params.set("reconnect_reason", opts.reconnectReason.toString());
	if ((_a = navigator.connection) === null || _a === void 0 ? void 0 : _a.type) params.set("network", navigator.connection.type);
	return params;
}
function createJoinRequestConnectionParams(token, info, opts, publisherOffer) {
	return __awaiter(this, void 0, void 0, function* () {
		const params = new URLSearchParams();
		params.set("access_token", token);
		const joinRequest = new JoinRequest({
			clientInfo: info,
			connectionSettings: new ConnectionSettings({
				autoSubscribe: !!opts.autoSubscribe,
				adaptiveStream: !!opts.adaptiveStream
			}),
			reconnect: !!opts.reconnect,
			participantSid: opts.sid ? opts.sid : void 0,
			publisherOffer
		});
		if (opts.reconnectReason) joinRequest.reconnectReason = opts.reconnectReason;
		const joinRequestBytes = joinRequest.toBinary();
		let requestBytes;
		let compression;
		if (isCompressionStreamSupported()) {
			const stream = new CompressionStream("gzip");
			const writer = stream.writable.getWriter();
			writer.write(new Uint8Array(joinRequestBytes));
			writer.close();
			const chunks = [];
			const reader = stream.readable.getReader();
			while (true) {
				const _yield$reader$read = yield reader.read(), done = _yield$reader$read.done, value = _yield$reader$read.value;
				if (done) break;
				chunks.push(value);
			}
			const totalLength = chunks.reduce((acc, chunk) => acc + chunk.length, 0);
			const result = new Uint8Array(totalLength);
			let offset = 0;
			for (const chunk of chunks) {
				result.set(chunk, offset);
				offset += chunk.length;
			}
			requestBytes = result;
			compression = WrappedJoinRequest_Compression.GZIP;
		} else {
			requestBytes = joinRequestBytes;
			compression = WrappedJoinRequest_Compression.NONE;
		}
		const wrappedBytes = new WrappedJoinRequest({
			joinRequest: requestBytes,
			compression
		}).toBinary();
		const bytesToBase64 = (bytes) => {
			const binString = Array.from(bytes, (byte) => String.fromCodePoint(byte)).join("");
			return btoa(binString);
		};
		params.set("join_request", bytesToBase64(wrappedBytes).replace(/\+/g, "-").replace(/\//g, "_"));
		return params;
	});
}
var TTLMap = class {
	/**
	* @param ttl ttl of the key (ms)
	*/
	constructor(ttl) {
		this._map = /* @__PURE__ */ new Map();
		this._lastCleanup = 0;
		this.ttl = ttl;
	}
	set(key, value) {
		const now = Date.now();
		if (now - this._lastCleanup > this.ttl / 2) this.cleanup();
		const expiresAt = now + this.ttl;
		this._map.set(key, {
			value,
			expiresAt
		});
		return this;
	}
	get(key) {
		const entry = this._map.get(key);
		if (!entry) return void 0;
		if (entry.expiresAt < Date.now()) {
			this._map.delete(key);
			return;
		}
		return entry.value;
	}
	has(key) {
		const entry = this._map.get(key);
		if (!entry) return false;
		if (entry.expiresAt < Date.now()) {
			this._map.delete(key);
			return false;
		}
		return true;
	}
	delete(key) {
		return this._map.delete(key);
	}
	clear() {
		this._map.clear();
	}
	cleanup() {
		const now = Date.now();
		for (const _ref of this._map.entries()) {
			var _ref2 = _slicedToArray(_ref, 2);
			const key = _ref2[0];
			if (_ref2[1].expiresAt < now) this._map.delete(key);
		}
		this._lastCleanup = now;
	}
	get size() {
		this.cleanup();
		return this._map.size;
	}
	forEach(callback) {
		this.cleanup();
		for (const _ref3 of this._map.entries()) {
			var _ref4 = _slicedToArray(_ref3, 2);
			const key = _ref4[0];
			const entry = _ref4[1];
			if (entry.expiresAt >= Date.now()) callback(entry.value, key, this.asValueMap());
		}
	}
	map(callback) {
		this.cleanup();
		const result = [];
		const valueMap = this.asValueMap();
		for (const _ref5 of valueMap.entries()) {
			var _ref6 = _slicedToArray(_ref5, 2);
			const key = _ref6[0];
			const value = _ref6[1];
			result.push(callback(value, key, valueMap));
		}
		return result;
	}
	asValueMap() {
		const result = /* @__PURE__ */ new Map();
		for (const _ref7 of this._map.entries()) {
			var _ref8 = _slicedToArray(_ref7, 2);
			const key = _ref8[0];
			const entry = _ref8[1];
			if (entry.expiresAt >= Date.now()) result.set(key, entry.value);
		}
		return result;
	}
};
var lib = {};
var parser = {};
var grammar = { exports: {} };
var hasRequiredGrammar;
function requireGrammar() {
	if (hasRequiredGrammar) return grammar.exports;
	hasRequiredGrammar = 1;
	var grammar$1 = grammar.exports = {
		v: [{
			name: "version",
			reg: /^(\d*)$/
		}],
		o: [{
			name: "origin",
			reg: /^(\S*) (\d*) (\d*) (\S*) IP(\d) (\S*)/,
			names: [
				"username",
				"sessionId",
				"sessionVersion",
				"netType",
				"ipVer",
				"address"
			],
			format: "%s %s %d %s IP%d %s"
		}],
		s: [{ name: "name" }],
		i: [{ name: "description" }],
		u: [{ name: "uri" }],
		e: [{ name: "email" }],
		p: [{ name: "phone" }],
		z: [{ name: "timezones" }],
		r: [{ name: "repeats" }],
		t: [{
			name: "timing",
			reg: /^(\d*) (\d*)/,
			names: ["start", "stop"],
			format: "%d %d"
		}],
		c: [{
			name: "connection",
			reg: /^IN IP(\d) (\S*)/,
			names: ["version", "ip"],
			format: "IN IP%d %s"
		}],
		b: [{
			push: "bandwidth",
			reg: /^(TIAS|AS|CT|RR|RS):(\d*)/,
			names: ["type", "limit"],
			format: "%s:%s"
		}],
		m: [{
			reg: /^(\w*) (\d*) ([\w/]*)(?: (.*))?/,
			names: [
				"type",
				"port",
				"protocol",
				"payloads"
			],
			format: "%s %d %s %s"
		}],
		a: [
			{
				push: "rtp",
				reg: /^rtpmap:(\d*) ([\w\-.]*)(?:\s*\/(\d*)(?:\s*\/(\S*))?)?/,
				names: [
					"payload",
					"codec",
					"rate",
					"encoding"
				],
				format: function(o) {
					return o.encoding ? "rtpmap:%d %s/%s/%s" : o.rate ? "rtpmap:%d %s/%s" : "rtpmap:%d %s";
				}
			},
			{
				push: "fmtp",
				reg: /^fmtp:(\d*) ([\S| ]*)/,
				names: ["payload", "config"],
				format: "fmtp:%d %s"
			},
			{
				name: "control",
				reg: /^control:(.*)/,
				format: "control:%s"
			},
			{
				name: "rtcp",
				reg: /^rtcp:(\d*)(?: (\S*) IP(\d) (\S*))?/,
				names: [
					"port",
					"netType",
					"ipVer",
					"address"
				],
				format: function(o) {
					return o.address != null ? "rtcp:%d %s IP%d %s" : "rtcp:%d";
				}
			},
			{
				push: "rtcpFbTrrInt",
				reg: /^rtcp-fb:(\*|\d*) trr-int (\d*)/,
				names: ["payload", "value"],
				format: "rtcp-fb:%s trr-int %d"
			},
			{
				push: "rtcpFb",
				reg: /^rtcp-fb:(\*|\d*) ([\w-_]*)(?: ([\w-_]*))?/,
				names: [
					"payload",
					"type",
					"subtype"
				],
				format: function(o) {
					return o.subtype != null ? "rtcp-fb:%s %s %s" : "rtcp-fb:%s %s";
				}
			},
			{
				push: "ext",
				reg: /^extmap:(\d+)(?:\/(\w+))?(?: (urn:ietf:params:rtp-hdrext:encrypt))? (\S*)(?: (\S*))?/,
				names: [
					"value",
					"direction",
					"encrypt-uri",
					"uri",
					"config"
				],
				format: function(o) {
					return "extmap:%d" + (o.direction ? "/%s" : "%v") + (o["encrypt-uri"] ? " %s" : "%v") + " %s" + (o.config ? " %s" : "");
				}
			},
			{
				name: "extmapAllowMixed",
				reg: /^(extmap-allow-mixed)/
			},
			{
				push: "crypto",
				reg: /^crypto:(\d*) ([\w_]*) (\S*)(?: (\S*))?/,
				names: [
					"id",
					"suite",
					"config",
					"sessionConfig"
				],
				format: function(o) {
					return o.sessionConfig != null ? "crypto:%d %s %s %s" : "crypto:%d %s %s";
				}
			},
			{
				name: "setup",
				reg: /^setup:(\w*)/,
				format: "setup:%s"
			},
			{
				name: "connectionType",
				reg: /^connection:(new|existing)/,
				format: "connection:%s"
			},
			{
				name: "mid",
				reg: /^mid:([^\s]*)/,
				format: "mid:%s"
			},
			{
				name: "msid",
				reg: /^msid:(.*)/,
				format: "msid:%s"
			},
			{
				name: "ptime",
				reg: /^ptime:(\d*(?:\.\d*)*)/,
				format: "ptime:%d"
			},
			{
				name: "maxptime",
				reg: /^maxptime:(\d*(?:\.\d*)*)/,
				format: "maxptime:%d"
			},
			{
				name: "direction",
				reg: /^(sendrecv|recvonly|sendonly|inactive)/
			},
			{
				name: "icelite",
				reg: /^(ice-lite)/
			},
			{
				name: "iceUfrag",
				reg: /^ice-ufrag:(\S*)/,
				format: "ice-ufrag:%s"
			},
			{
				name: "icePwd",
				reg: /^ice-pwd:(\S*)/,
				format: "ice-pwd:%s"
			},
			{
				name: "fingerprint",
				reg: /^fingerprint:(\S*) (\S*)/,
				names: ["type", "hash"],
				format: "fingerprint:%s %s"
			},
			{
				push: "candidates",
				reg: /^candidate:(\S*) (\d*) (\S*) (\d*) (\S*) (\d*) typ (\S*)(?: raddr (\S*) rport (\d*))?(?: tcptype (\S*))?(?: generation (\d*))?(?: network-id (\d*))?(?: network-cost (\d*))?/,
				names: [
					"foundation",
					"component",
					"transport",
					"priority",
					"ip",
					"port",
					"type",
					"raddr",
					"rport",
					"tcptype",
					"generation",
					"network-id",
					"network-cost"
				],
				format: function(o) {
					var str = "candidate:%s %d %s %d %s %d typ %s";
					str += o.raddr != null ? " raddr %s rport %d" : "%v%v";
					str += o.tcptype != null ? " tcptype %s" : "%v";
					if (o.generation != null) str += " generation %d";
					str += o["network-id"] != null ? " network-id %d" : "%v";
					str += o["network-cost"] != null ? " network-cost %d" : "%v";
					return str;
				}
			},
			{
				name: "endOfCandidates",
				reg: /^(end-of-candidates)/
			},
			{
				name: "remoteCandidates",
				reg: /^remote-candidates:(.*)/,
				format: "remote-candidates:%s"
			},
			{
				name: "iceOptions",
				reg: /^ice-options:(\S*)/,
				format: "ice-options:%s"
			},
			{
				push: "ssrcs",
				reg: /^ssrc:(\d*) ([\w_-]*)(?::(.*))?/,
				names: [
					"id",
					"attribute",
					"value"
				],
				format: function(o) {
					var str = "ssrc:%d";
					if (o.attribute != null) {
						str += " %s";
						if (o.value != null) str += ":%s";
					}
					return str;
				}
			},
			{
				push: "ssrcGroups",
				reg: /^ssrc-group:([\x21\x23\x24\x25\x26\x27\x2A\x2B\x2D\x2E\w]*) (.*)/,
				names: ["semantics", "ssrcs"],
				format: "ssrc-group:%s %s"
			},
			{
				name: "msidSemantic",
				reg: /^msid-semantic:\s?(\w*) (\S*)/,
				names: ["semantic", "token"],
				format: "msid-semantic: %s %s"
			},
			{
				push: "groups",
				reg: /^group:(\w*) (.*)/,
				names: ["type", "mids"],
				format: "group:%s %s"
			},
			{
				name: "rtcpMux",
				reg: /^(rtcp-mux)/
			},
			{
				name: "rtcpRsize",
				reg: /^(rtcp-rsize)/
			},
			{
				name: "sctpmap",
				reg: /^sctpmap:([\w_/]*) (\S*)(?: (\S*))?/,
				names: [
					"sctpmapNumber",
					"app",
					"maxMessageSize"
				],
				format: function(o) {
					return o.maxMessageSize != null ? "sctpmap:%s %s %s" : "sctpmap:%s %s";
				}
			},
			{
				name: "xGoogleFlag",
				reg: /^x-google-flag:([^\s]*)/,
				format: "x-google-flag:%s"
			},
			{
				push: "rids",
				reg: /^rid:([\d\w]+) (\w+)(?: ([\S| ]*))?/,
				names: [
					"id",
					"direction",
					"params"
				],
				format: function(o) {
					return o.params ? "rid:%s %s %s" : "rid:%s %s";
				}
			},
			{
				push: "imageattrs",
				reg: /* @__PURE__ */ new RegExp("^imageattr:(\\d+|\\*)[\\s\\t]+(send|recv)[\\s\\t]+(\\*|\\[\\S+\\](?:[\\s\\t]+\\[\\S+\\])*)(?:[\\s\\t]+(recv|send)[\\s\\t]+(\\*|\\[\\S+\\](?:[\\s\\t]+\\[\\S+\\])*))?"),
				names: [
					"pt",
					"dir1",
					"attrs1",
					"dir2",
					"attrs2"
				],
				format: function(o) {
					return "imageattr:%s %s %s" + (o.dir2 ? " %s %s" : "");
				}
			},
			{
				name: "simulcast",
				reg: /* @__PURE__ */ new RegExp("^simulcast:(send|recv) ([a-zA-Z0-9\\-_~;,]+)(?:\\s?(send|recv) ([a-zA-Z0-9\\-_~;,]+))?$"),
				names: [
					"dir1",
					"list1",
					"dir2",
					"list2"
				],
				format: function(o) {
					return "simulcast:%s %s" + (o.dir2 ? " %s %s" : "");
				}
			},
			{
				name: "simulcast_03",
				reg: /^simulcast:[\s\t]+([\S+\s\t]+)$/,
				names: ["value"],
				format: "simulcast: %s"
			},
			{
				name: "framerate",
				reg: /^framerate:(\d+(?:$|\.\d+))/,
				format: "framerate:%s"
			},
			{
				name: "sourceFilter",
				reg: /^source-filter: *(excl|incl) (\S*) (IP4|IP6|\*) (\S*) (.*)/,
				names: [
					"filterMode",
					"netType",
					"addressTypes",
					"destAddress",
					"srcList"
				],
				format: "source-filter: %s %s %s %s %s"
			},
			{
				name: "bundleOnly",
				reg: /^(bundle-only)/
			},
			{
				name: "label",
				reg: /^label:(.+)/,
				format: "label:%s"
			},
			{
				name: "sctpPort",
				reg: /^sctp-port:(\d+)$/,
				format: "sctp-port:%s"
			},
			{
				name: "maxMessageSize",
				reg: /^max-message-size:(\d+)$/,
				format: "max-message-size:%s"
			},
			{
				push: "tsRefClocks",
				reg: /^ts-refclk:([^\s=]*)(?:=(\S*))?/,
				names: ["clksrc", "clksrcExt"],
				format: function(o) {
					return "ts-refclk:%s" + (o.clksrcExt != null ? "=%s" : "");
				}
			},
			{
				name: "mediaClk",
				reg: /^mediaclk:(?:id=(\S*))? *([^\s=]*)(?:=(\S*))?(?: *rate=(\d+)\/(\d+))?/,
				names: [
					"id",
					"mediaClockName",
					"mediaClockValue",
					"rateNumerator",
					"rateDenominator"
				],
				format: function(o) {
					var str = "mediaclk:";
					str += o.id != null ? "id=%s %s" : "%v%s";
					str += o.mediaClockValue != null ? "=%s" : "";
					str += o.rateNumerator != null ? " rate=%s" : "";
					str += o.rateDenominator != null ? "/%s" : "";
					return str;
				}
			},
			{
				name: "keywords",
				reg: /^keywds:(.+)$/,
				format: "keywds:%s"
			},
			{
				name: "content",
				reg: /^content:(.+)/,
				format: "content:%s"
			},
			{
				name: "bfcpFloorCtrl",
				reg: /^floorctrl:(c-only|s-only|c-s)/,
				format: "floorctrl:%s"
			},
			{
				name: "bfcpConfId",
				reg: /^confid:(\d+)/,
				format: "confid:%s"
			},
			{
				name: "bfcpUserId",
				reg: /^userid:(\d+)/,
				format: "userid:%s"
			},
			{
				name: "bfcpFloorId",
				reg: /^floorid:(.+) (?:m-stream|mstrm):(.+)/,
				names: ["id", "mStream"],
				format: "floorid:%s mstrm:%s"
			},
			{
				push: "invalid",
				names: ["value"]
			}
		]
	};
	Object.keys(grammar$1).forEach(function(key) {
		grammar$1[key].forEach(function(obj) {
			if (!obj.reg) obj.reg = /(.*)/;
			if (!obj.format) obj.format = "%s";
		});
	});
	return grammar.exports;
}
var hasRequiredParser;
function requireParser() {
	if (hasRequiredParser) return parser;
	hasRequiredParser = 1;
	(function(exports) {
		var toIntIfInt = function(v) {
			return String(Number(v)) === v ? Number(v) : v;
		};
		var attachProperties = function(match, location, names, rawName) {
			if (rawName && !names) location[rawName] = toIntIfInt(match[1]);
			else for (var i = 0; i < names.length; i += 1) if (match[i + 1] != null) location[names[i]] = toIntIfInt(match[i + 1]);
		};
		var parseReg = function(obj, location, content) {
			var needsBlank = obj.name && obj.names;
			if (obj.push && !location[obj.push]) location[obj.push] = [];
			else if (needsBlank && !location[obj.name]) location[obj.name] = {};
			var keyLocation = obj.push ? {} : needsBlank ? location[obj.name] : location;
			attachProperties(content.match(obj.reg), keyLocation, obj.names, obj.name);
			if (obj.push) location[obj.push].push(keyLocation);
		};
		var grammar = requireGrammar();
		var validLine = RegExp.prototype.test.bind(/^([a-z])=(.*)/);
		exports.parse = function(sdp) {
			var session = {}, media = [], location = session;
			sdp.split(/(\r\n|\r|\n)/).filter(validLine).forEach(function(l) {
				var type = l[0];
				var content = l.slice(2);
				if (type === "m") {
					media.push({
						rtp: [],
						fmtp: []
					});
					location = media[media.length - 1];
				}
				for (var j = 0; j < (grammar[type] || []).length; j += 1) {
					var obj = grammar[type][j];
					if (obj.reg.test(content)) return parseReg(obj, location, content);
				}
			});
			session.media = media;
			return session;
		};
		var paramReducer = function(acc, expr) {
			var s = expr.split(/=(.+)/, 2);
			if (s.length === 2) acc[s[0]] = toIntIfInt(s[1]);
			else if (s.length === 1 && expr.length > 1) acc[s[0]] = void 0;
			return acc;
		};
		exports.parseParams = function(str) {
			return str.split(/;\s?/).reduce(paramReducer, {});
		};
		exports.parseFmtpConfig = exports.parseParams;
		exports.parsePayloads = function(str) {
			return str.toString().split(" ").map(Number);
		};
		exports.parseRemoteCandidates = function(str) {
			var candidates = [];
			var parts = str.split(" ").map(toIntIfInt);
			for (var i = 0; i < parts.length; i += 3) candidates.push({
				component: parts[i],
				ip: parts[i + 1],
				port: parts[i + 2]
			});
			return candidates;
		};
		exports.parseImageAttributes = function(str) {
			return str.split(" ").map(function(item) {
				return item.substring(1, item.length - 1).split(",").reduce(paramReducer, {});
			});
		};
		exports.parseSimulcastStreamList = function(str) {
			return str.split(";").map(function(stream) {
				return stream.split(",").map(function(format) {
					var scid, paused = false;
					if (format[0] !== "~") scid = toIntIfInt(format);
					else {
						scid = toIntIfInt(format.substring(1, format.length));
						paused = true;
					}
					return {
						scid,
						paused
					};
				});
			});
		};
	})(parser);
	return parser;
}
var writer;
var hasRequiredWriter;
function requireWriter() {
	if (hasRequiredWriter) return writer;
	hasRequiredWriter = 1;
	var grammar = requireGrammar();
	var formatRegExp = /%[sdv%]/g;
	var format = function(formatStr) {
		var i = 1;
		var args = arguments;
		var len = args.length;
		return formatStr.replace(formatRegExp, function(x) {
			if (i >= len) return x;
			var arg = args[i];
			i += 1;
			switch (x) {
				case "%%": return "%";
				case "%s": return String(arg);
				case "%d": return Number(arg);
				case "%v": return "";
			}
		});
	};
	var makeLine = function(type, obj, location) {
		var str = obj.format instanceof Function ? obj.format(obj.push ? location : location[obj.name]) : obj.format;
		var args = [type + "=" + str];
		if (obj.names) for (var i = 0; i < obj.names.length; i += 1) {
			var n = obj.names[i];
			if (obj.name) args.push(location[obj.name][n]);
			else args.push(location[obj.names[i]]);
		}
		else args.push(location[obj.name]);
		return format.apply(null, args);
	};
	var defaultOuterOrder = [
		"v",
		"o",
		"s",
		"i",
		"u",
		"e",
		"p",
		"c",
		"b",
		"t",
		"r",
		"z",
		"a"
	];
	var defaultInnerOrder = [
		"i",
		"c",
		"b",
		"a"
	];
	writer = function(session, opts) {
		opts = opts || {};
		if (session.version == null) session.version = 0;
		if (session.name == null) session.name = " ";
		session.media.forEach(function(mLine) {
			if (mLine.payloads == null) mLine.payloads = "";
		});
		var outerOrder = opts.outerOrder || defaultOuterOrder;
		var innerOrder = opts.innerOrder || defaultInnerOrder;
		var sdp = [];
		outerOrder.forEach(function(type) {
			grammar[type].forEach(function(obj) {
				if (obj.name in session && session[obj.name] != null) sdp.push(makeLine(type, obj, session));
				else if (obj.push in session && session[obj.push] != null) session[obj.push].forEach(function(el) {
					sdp.push(makeLine(type, obj, el));
				});
			});
		});
		session.media.forEach(function(mLine) {
			sdp.push(makeLine("m", grammar.m[0], mLine));
			innerOrder.forEach(function(type) {
				grammar[type].forEach(function(obj) {
					if (obj.name in mLine && mLine[obj.name] != null) sdp.push(makeLine(type, obj, mLine));
					else if (obj.push in mLine && mLine[obj.push] != null) mLine[obj.push].forEach(function(el) {
						sdp.push(makeLine(type, obj, el));
					});
				});
			});
		});
		return sdp.join("\r\n") + "\r\n";
	};
	return writer;
}
var hasRequiredLib;
function requireLib() {
	if (hasRequiredLib) return lib;
	hasRequiredLib = 1;
	var parser = requireParser();
	var writer = requireWriter();
	lib.grammar = requireGrammar();
	lib.write = writer;
	lib.parse = parser.parse;
	lib.parseParams = parser.parseParams;
	lib.parseFmtpConfig = parser.parseFmtpConfig;
	lib.parsePayloads = parser.parsePayloads;
	lib.parseRemoteCandidates = parser.parseRemoteCandidates;
	lib.parseImageAttributes = parser.parseImageAttributes;
	lib.parseSimulcastStreamList = parser.parseSimulcastStreamList;
	return lib;
}
var libExports = requireLib();
var startBitrateMultiplier = .9;
/** Maximum x-google-start-bitrate in kbps. 1 Mbps prevents BWE from starting too aggressively. */
var maxStartBitrateKbps = 1e3;
var debounceInterval = 20;
/**
* Applies the configured start bitrate when this media section belongs to `cid`.
* This SDP munging is used for a bitrate setting that cannot be applied through
* `RTCRtpEncodingParameters`.
*
* Returns `undefined` when the section does not belong to the track, `0` when
* it does but does not offer the requested codec, and the codec payload when the
* requested codec is present (whether the bitrate was added or already set).
*
* @internal
*/
function applyVideoStartBitrate(media, cid, codec, maxbr) {
	let isScreenShare = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : false;
	var _a, _b, _c;
	if (!((_a = media.msid) === null || _a === void 0 ? void 0 : _a.includes(cid))) return;
	const codecPayload = (_c = (_b = media.rtp.find((rtp) => rtp.codec.toUpperCase() === codec.toUpperCase())) === null || _b === void 0 ? void 0 : _b.payload) !== null && _c !== void 0 ? _c : 0;
	if (codecPayload === 0) return 0;
	const calculatedStartBitrate = Math.round(maxbr * startBitrateMultiplier);
	const startBitrate = isScreenShare ? calculatedStartBitrate : Math.min(calculatedStartBitrate, maxStartBitrateKbps);
	const fmtp = media.fmtp.find((entry) => entry.payload === codecPayload);
	if (fmtp) {
		if (!fmtp.config.includes("x-google-start-bitrate")) fmtp.config += ";x-google-start-bitrate=".concat(startBitrate);
	} else media.fmtp.push({
		payload: codecPayload,
		config: "x-google-start-bitrate=".concat(startBitrate)
	});
	return codecPayload;
}
var PCEvents = {
	NegotiationStarted: "negotiationStarted",
	NegotiationComplete: "negotiationComplete",
	OfferAnswered: "offerAnswered",
	RTPVideoPayloadTypes: "rtpVideoPayloadTypes"
};
/** @internal */
var PCTransport = class extends eventsExports.EventEmitter {
	get pc() {
		if (!this._pc) this._pc = this.createPC();
		return this._pc;
	}
	constructor(config) {
		let loggerOptions = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
		var _a;
		super();
		this.log = livekitLogger;
		this.iceLog = livekitLogger;
		this.ddExtID = 0;
		this.latestOfferId = 0;
		this.latestAcknowledgedOfferId = 0;
		this.pendingCandidates = [];
		this.restartingIce = false;
		this.renegotiate = false;
		this.trackBitrates = [];
		this.remoteStereoMids = [];
		this.remoteNackMids = [];
		this.negotiate = debounce((onError) => __awaiter(this, void 0, void 0, function* () {
			this.emit(PCEvents.NegotiationStarted);
			try {
				yield this.createAndSendOffer();
			} catch (e) {
				if (onError) onError(e);
				else throw e;
			}
		}), debounceInterval);
		this.close = () => {
			if (!this._pc) return;
			this.log.debug("closing peer connection");
			this.pendingInitialOffer = void 0;
			this._pc.close();
			this._pc.onconnectionstatechange = null;
			this._pc.oniceconnectionstatechange = null;
			this._pc.onicegatheringstatechange = null;
			this._pc.ondatachannel = null;
			this._pc.onnegotiationneeded = null;
			this._pc.onsignalingstatechange = null;
			this._pc.onicecandidate = null;
			this._pc.ondatachannel = null;
			this._pc.ontrack = null;
			this._pc.onconnectionstatechange = null;
			this._pc.oniceconnectionstatechange = null;
			this._pc = null;
		};
		this.loggerOptions = loggerOptions;
		this.log = getLogger((_a = loggerOptions.loggerName) !== null && _a !== void 0 ? _a : LoggerNames.PCTransport, () => this.logContext);
		this.iceLog = getLogger(LoggerNames.ICE, () => this.logContext);
		this.config = config;
		this._pc = this.createPC();
		this.offerLock = new _();
	}
	createPC() {
		const pc = new RTCPeerConnection(this.config);
		pc.onicecandidate = (ev) => {
			var _a;
			if (!ev.candidate) return;
			this.iceLog.debug("local ICE candidate gathered", { candidate: ev.candidate.candidate });
			(_a = this.onIceCandidate) === null || _a === void 0 || _a.call(this, ev.candidate);
		};
		pc.onicecandidateerror = (ev) => {
			var _a;
			this.iceLog.debug("ICE candidate error", { event: ev });
			(_a = this.onIceCandidateError) === null || _a === void 0 || _a.call(this, ev);
		};
		pc.oniceconnectionstatechange = () => {
			var _a;
			this.iceLog.debug("ICE connection state: ".concat(pc.iceConnectionState));
			(_a = this.onIceConnectionStateChange) === null || _a === void 0 || _a.call(this, pc.iceConnectionState);
		};
		pc.onsignalingstatechange = () => {
			var _a;
			this.log.debug("signaling state: ".concat(pc.signalingState));
			(_a = this.onSignalingStatechange) === null || _a === void 0 || _a.call(this, pc.signalingState);
		};
		pc.onconnectionstatechange = () => {
			var _a;
			this.log.debug("connection state: ".concat(pc.connectionState));
			(_a = this.onConnectionStateChange) === null || _a === void 0 || _a.call(this, pc.connectionState);
		};
		pc.ondatachannel = (ev) => {
			var _a;
			this.log.debug("data channel opened by peer", {
				label: ev.channel.label,
				id: ev.channel.id
			});
			(_a = this.onDataChannel) === null || _a === void 0 || _a.call(this, ev);
		};
		pc.ontrack = (ev) => {
			var _a;
			(_a = this.onTrack) === null || _a === void 0 || _a.call(this, ev);
		};
		return pc;
	}
	get logContext() {
		var _a, _b;
		return Object.assign({}, (_b = (_a = this.loggerOptions).loggerContextCb) === null || _b === void 0 ? void 0 : _b.call(_a));
	}
	get isICEConnected() {
		return this._pc !== null && (this.pc.iceConnectionState === "connected" || this.pc.iceConnectionState === "completed");
	}
	addIceCandidate(candidate) {
		return __awaiter(this, void 0, void 0, function* () {
			if (this.pc.remoteDescription && !this.restartingIce) return this.pc.addIceCandidate(candidate);
			this.iceLog.debug("queuing remote ICE candidate until remote description applied", { pendingCount: this.pendingCandidates.length + 1 });
			this.pendingCandidates.push(candidate);
		});
	}
	setRemoteDescription(sd, offerId) {
		return __awaiter(this, void 0, void 0, function* () {
			var _a, _b;
			if (sd.type === "answer" && this.latestOfferId > 0 && offerId > 0 && offerId !== this.latestOfferId) {
				this.log.warn("ignoring answer for old offer", {
					offerId,
					latestOfferId: this.latestOfferId
				});
				return false;
			}
			let mungedSDP = void 0;
			if (sd.type === "offer") {
				let _extractStereoAndNack = extractStereoAndNackAudioFromOffer(sd), stereoMids = _extractStereoAndNack.stereoMids, nackMids = _extractStereoAndNack.nackMids;
				this.remoteStereoMids = stereoMids;
				this.remoteNackMids = nackMids;
			} else if (sd.type === "answer") {
				if (this.pendingInitialOffer && this._pc) {
					const initialOffer = this.pendingInitialOffer;
					this.pendingInitialOffer = void 0;
					const sdpParsed = libExports.parse((_a = initialOffer.sdp) !== null && _a !== void 0 ? _a : "");
					sdpParsed.media.forEach((media) => {
						ensureIPAddrMatchVersion(media);
					});
					this.log.debug("setting pending initial offer before processing answer");
					yield this.setMungedSDP(initialOffer, libExports.write(sdpParsed));
				}
				const sdpParsed = libExports.parse((_b = sd.sdp) !== null && _b !== void 0 ? _b : "");
				sdpParsed.media.forEach((media) => {
					const mid = getMidString(media.mid);
					if (media.type === "audio") this.trackBitrates.some((trackbr) => {
						if (!trackbr.transceiver || mid != trackbr.transceiver.mid) return false;
						let codecPayload = 0;
						media.rtp.some((rtp) => {
							if (rtp.codec.toUpperCase() === trackbr.codec.toUpperCase()) {
								codecPayload = rtp.payload;
								return true;
							}
							return false;
						});
						if (codecPayload === 0) return true;
						let fmtpFound = false;
						for (const fmtp of media.fmtp) if (fmtp.payload === codecPayload) {
							fmtp.config = fmtp.config.split(";").filter((attr) => !attr.includes("maxaveragebitrate")).join(";");
							if (trackbr.maxbr > 0) fmtp.config += ";maxaveragebitrate=".concat(trackbr.maxbr * 1e3);
							fmtpFound = true;
							break;
						}
						if (!fmtpFound) {
							if (trackbr.maxbr > 0) media.fmtp.push({
								payload: codecPayload,
								config: "maxaveragebitrate=".concat(trackbr.maxbr * 1e3)
							});
						}
						return true;
					});
				});
				const placeholderMids = this.getPlaceholderMids();
				if (placeholderMids.size > 0) conformBundledCodecFmtp(sdpParsed.media, (media) => placeholderMids.has(getMidString(media.mid)));
				mungedSDP = libExports.write(sdpParsed);
			}
			yield this.setMungedSDP(sd, mungedSDP, true);
			if (this.pendingCandidates.length > 0) this.iceLog.debug("flushing queued ICE candidates", { count: this.pendingCandidates.length });
			this.pendingCandidates.forEach((candidate) => {
				this.pc.addIceCandidate(candidate);
			});
			this.pendingCandidates = [];
			this.restartingIce = false;
			if (sd.type === "answer") {
				this.latestAcknowledgedOfferId = offerId;
				this.emit(PCEvents.OfferAnswered, offerId);
			}
			if (this.renegotiate) {
				this.renegotiate = false;
				yield this.createAndSendOffer();
			} else if (sd.type === "answer") {
				this.emit(PCEvents.NegotiationComplete);
				if (sd.sdp) libExports.parse(sd.sdp).media.forEach((media) => {
					if (media.type === "video") this.emit(PCEvents.RTPVideoPayloadTypes, media.rtp);
				});
			}
			return true;
		});
	}
	createInitialOffer() {
		return __awaiter(this, void 0, void 0, function* () {
			var _a;
			const unlock = yield this.offerLock.lock();
			try {
				if (this.pc.signalingState !== "stable") {
					this.log.warn("signaling state is not stable, cannot create initial offer");
					return;
				}
				const offerId = this.latestOfferId + 1;
				this.latestOfferId = offerId;
				const offer = yield this.pc.createOffer();
				this.pendingInitialOffer = {
					sdp: offer.sdp,
					type: offer.type
				};
				const sdpParsed = libExports.parse((_a = offer.sdp) !== null && _a !== void 0 ? _a : "");
				sdpParsed.media.forEach((media) => {
					ensureIPAddrMatchVersion(media);
				});
				offer.sdp = libExports.write(sdpParsed);
				return {
					offer,
					offerId
				};
			} finally {
				unlock();
			}
		});
	}
	createAndSendOffer(options) {
		return __awaiter(this, void 0, void 0, function* () {
			var _a;
			const unlock = yield this.offerLock.lock();
			try {
				if (this.onOffer === void 0) return;
				if (options === null || options === void 0 ? void 0 : options.iceRestart) {
					this.iceLog.debug("restarting ICE");
					this.restartingIce = true;
				}
				if (this._pc && (this._pc.signalingState === "have-local-offer" || this.pendingInitialOffer)) {
					const currentSD = this._pc.remoteDescription;
					if ((options === null || options === void 0 ? void 0 : options.iceRestart) && currentSD) yield this._pc.setRemoteDescription(currentSD);
					else if (options === null || options === void 0 ? void 0 : options.iceRestart) throw new NegotiationError("ICE restart requested without a remote description, peer connection must be recreated");
					else {
						this.renegotiate = true;
						this.log.debug("requesting renegotiation");
						return;
					}
				} else if (!this._pc || this._pc.signalingState === "closed") {
					this.log.warn("could not createOffer with closed peer connection");
					return;
				}
				this.log.debug("starting to negotiate");
				const offerId = this.latestOfferId + 1;
				this.latestOfferId = offerId;
				const offer = yield this.pc.createOffer(options);
				this.log.debug("original offer", { sdp: offer.sdp });
				const sdpParsed = libExports.parse((_a = offer.sdp) !== null && _a !== void 0 ? _a : "");
				sdpParsed.media.forEach((media) => {
					ensureIPAddrMatchVersion(media);
					if (media.type === "audio") ensureAudioNackAndStereo(media, ["all"], []);
					else if (media.type === "video") this.trackBitrates.some((trackbr) => {
						if (!trackbr.cid) return false;
						const codecPayload = applyVideoStartBitrate(media, trackbr.cid, trackbr.codec, trackbr.maxbr, trackbr.isScreenShare);
						if (codecPayload === void 0) return false;
						if (codecPayload > 0 && isSVCCodec(trackbr.codec) && !isSafari()) this.ddExtID = ensureVideoDDExtension(media, sdpParsed, this.ddExtID);
						return true;
					});
				});
				const placeholderMids = this.getPlaceholderMids();
				if (placeholderMids.size > 0) conformBundledCodecFmtp(sdpParsed.media, (media) => placeholderMids.has(getMidString(media.mid)));
				if (this.latestOfferId > offerId) {
					this.log.warn("latestOfferId mismatch", {
						latestOfferId: this.latestOfferId,
						offerId
					});
					return;
				}
				yield this.setMungedSDP(offer, libExports.write(sdpParsed));
				this.onOffer(offer, this.latestOfferId);
			} finally {
				unlock();
			}
		});
	}
	createAndSetAnswer() {
		return __awaiter(this, void 0, void 0, function* () {
			var _a;
			const answer = yield this.pc.createAnswer();
			const sdpParsed = libExports.parse((_a = answer.sdp) !== null && _a !== void 0 ? _a : "");
			sdpParsed.media.forEach((media) => {
				ensureIPAddrMatchVersion(media);
				if (media.type === "audio") ensureAudioNackAndStereo(media, this.remoteStereoMids, this.remoteNackMids);
			});
			yield this.setMungedSDP(answer, libExports.write(sdpParsed));
			return answer;
		});
	}
	/**
	* Returns the mids of transceivers that carry no outgoing track on this
	* (publisher) connection: the pre-populated placeholders added by
	* `RTCEngine.applyInitialPublisherLayout`, plus any transceiver that was used
	* for a track and reverted on unpublish. Their codec fmtp is conformed to the
	* published tracks so a shared payload type stays consistent across the bundle.
	*/
	getPlaceholderMids() {
		var _a, _b;
		return placeholderMidsFromTransceivers((_b = (_a = this._pc) === null || _a === void 0 ? void 0 : _a.getTransceivers()) !== null && _b !== void 0 ? _b : []);
	}
	createDataChannel(label, dataChannelDict) {
		return this.pc.createDataChannel(label, dataChannelDict);
	}
	addTransceiver(mediaStreamTrack, transceiverInit) {
		return this.pc.addTransceiver(mediaStreamTrack, transceiverInit);
	}
	addTransceiverOfKind(kind, transceiverInit) {
		return this.pc.addTransceiver(kind, transceiverInit);
	}
	addTrack(track) {
		if (!this._pc) throw new UnexpectedConnectionState("PC closed, cannot add track");
		return this._pc.addTrack(track);
	}
	setTrackCodecBitrate(info) {
		this.trackBitrates.push(info);
	}
	setConfiguration(rtcConfig) {
		var _a;
		if (!this._pc) throw new UnexpectedConnectionState("PC closed, cannot configure");
		return (_a = this._pc) === null || _a === void 0 ? void 0 : _a.setConfiguration(rtcConfig);
	}
	canRemoveTrack() {
		var _a;
		return !!((_a = this._pc) === null || _a === void 0 ? void 0 : _a.removeTrack);
	}
	removeTrack(sender) {
		var _a;
		return (_a = this._pc) === null || _a === void 0 ? void 0 : _a.removeTrack(sender);
	}
	getConnectionState() {
		var _a, _b;
		return (_b = (_a = this._pc) === null || _a === void 0 ? void 0 : _a.connectionState) !== null && _b !== void 0 ? _b : "closed";
	}
	getICEConnectionState() {
		var _a, _b;
		return (_b = (_a = this._pc) === null || _a === void 0 ? void 0 : _a.iceConnectionState) !== null && _b !== void 0 ? _b : "closed";
	}
	getSignallingState() {
		var _a, _b;
		return (_b = (_a = this._pc) === null || _a === void 0 ? void 0 : _a.signalingState) !== null && _b !== void 0 ? _b : "closed";
	}
	getTransceivers() {
		var _a, _b;
		return (_b = (_a = this._pc) === null || _a === void 0 ? void 0 : _a.getTransceivers()) !== null && _b !== void 0 ? _b : [];
	}
	getSenders() {
		var _a, _b;
		return (_b = (_a = this._pc) === null || _a === void 0 ? void 0 : _a.getSenders()) !== null && _b !== void 0 ? _b : [];
	}
	getLocalDescription() {
		var _a;
		return (_a = this._pc) === null || _a === void 0 ? void 0 : _a.localDescription;
	}
	getRemoteDescription() {
		var _a;
		return (_a = this.pc) === null || _a === void 0 ? void 0 : _a.remoteDescription;
	}
	getStats() {
		return this.pc.getStats();
	}
	getMaxMessageSize() {
		var _a, _b;
		return (_b = (_a = this._pc) === null || _a === void 0 ? void 0 : _a.sctp) === null || _b === void 0 ? void 0 : _b.maxMessageSize;
	}
	getConnectedAddress() {
		return __awaiter(this, void 0, void 0, function* () {
			var _a;
			if (!this._pc) return;
			let selectedCandidatePairId = "";
			const candidatePairs = /* @__PURE__ */ new Map();
			const candidates = /* @__PURE__ */ new Map();
			(yield this._pc.getStats()).forEach((v) => {
				switch (v.type) {
					case "transport":
						selectedCandidatePairId = v.selectedCandidatePairId;
						break;
					case "candidate-pair":
						if (selectedCandidatePairId === "" && v.selected) selectedCandidatePairId = v.id;
						candidatePairs.set(v.id, v);
						break;
					case "remote-candidate": candidates.set(v.id, "".concat(v.address, ":").concat(v.port));
				}
			});
			if (selectedCandidatePairId === "") return;
			const selectedID = (_a = candidatePairs.get(selectedCandidatePairId)) === null || _a === void 0 ? void 0 : _a.remoteCandidateId;
			if (selectedID === void 0) return;
			return candidates.get(selectedID);
		});
	}
	setMungedSDP(sd, munged, remote) {
		return __awaiter(this, void 0, void 0, function* () {
			var _a, _b;
			const originalSdp = sd.sdp;
			if (munged) {
				sd.sdp = munged;
				try {
					this.log.debug("setting munged ".concat(remote ? "remote" : "local", " description"));
					if (remote) yield this.pc.setRemoteDescription(sd);
					else yield this.pc.setLocalDescription(sd);
					return;
				} catch (e) {
					this.log.warn("not able to set ".concat(sd.type, ", falling back to unmodified sdp"), {
						error: e,
						mungedSdp: munged,
						originalSdp
					});
					sd.sdp = originalSdp;
				}
			}
			try {
				if (remote) yield (_a = this._pc) === null || _a === void 0 ? void 0 : _a.setRemoteDescription(sd);
				else yield (_b = this._pc) === null || _b === void 0 ? void 0 : _b.setLocalDescription(sd);
			} catch (e) {
				let msg = "unknown error";
				if (e instanceof Error) msg = e.message;
				else if (typeof e === "string") msg = e;
				const fields = {
					error: msg,
					sdp: sd.sdp
				};
				if (munged && munged !== originalSdp) fields.mungedSdp = munged;
				if (!remote && this.pc.remoteDescription) fields.remoteSdp = this.pc.remoteDescription;
				this.log.error("unable to set ".concat(sd.type), { fields });
				throw new NegotiationError(msg);
			}
		});
	}
};
/**
* Adds the AV1 dependency descriptor extension to `media` unless it is already there, and
* returns the id it is mapped to so callers can pass it back in as `ddExtID` (0 when no id has
* been chosen yet).
*
* A bundle has to map one URI to one id, so an id already in use for the extension anywhere in
* `sdp` wins over both the cached one and a fresh one: Chrome advertises the extension itself on
* sections it can send on, and an earlier offer may have munged it into others.
* @internal
*/
function ensureVideoDDExtension(media, sdp, ddExtID) {
	var _a, _b;
	const id = ddExtensionIDFor(sdp, ddExtID);
	if (id === void 0) return ddExtID;
	if (!((_a = media.ext) === null || _a === void 0 ? void 0 : _a.some((ext) => ext.uri === ddExtensionURI))) {
		(_b = media.ext) !== null && _b !== void 0 || (media.ext = []);
		media.ext.push({
			value: id,
			uri: ddExtensionURI
		});
	}
	return id;
}
/**
* The id to map the dependency descriptor to throughout `sdp`, or undefined when no id would be
* consistent for the whole bundle and the extension therefore has to be left out.
*/
function ddExtensionIDFor(sdp, cachedID) {
	const mapped = mappedExtensionID(sdp, ddExtensionURI);
	if (mapped !== void 0) return usedForOtherURI(sdp, mapped, ddExtensionURI) ? void 0 : mapped;
	if (cachedID !== 0 && !usedForOtherURI(sdp, cachedID, ddExtensionURI)) return cachedID;
	return unusedExtensionID(sdp);
}
/** The id `uri` is mapped to in `sdp`, if any section maps it. */
function mappedExtensionID(sdp, uri) {
	var _a;
	for (const media of sdp.media) {
		const ext = (_a = media.ext) === null || _a === void 0 ? void 0 : _a.find((candidate) => candidate.uri === uri);
		if (ext) return ext.value;
	}
}
/** Whether `id` stands for anything in `sdp` other than `uri`. */
function usedForOtherURI(sdp, id, uri) {
	return sdp.media.some((media) => {
		var _a;
		return (_a = media.ext) === null || _a === void 0 ? void 0 : _a.some((ext) => ext.value === id && ext.uri !== uri);
	});
}
/**
* An id no extension in `sdp` uses. Stays above every id in use rather than filling gaps, so it
* is less likely to be an id the browser goes on to allocate to another extension, and steps
* over 15, which RFC 8285 reserves.
*/
function unusedExtensionID(sdp) {
	let maxID = 0;
	sdp.media.forEach((media) => {
		var _a;
		(_a = media.ext) === null || _a === void 0 || _a.forEach((ext) => {
			if (ext.value > maxID) maxID = ext.value;
		});
	});
	return maxID + 1 === 15 ? 16 : maxID + 1;
}
/**
* Checks whether an fmtp config declares `param` as an exact, `;`-delimited
* token. A plain substring check conflates distinct opus parameters — e.g.
* `stereo=1` is a substring of `sprop-stereo=1` — so `param` must match a whole
* parameter, not appear anywhere within the config string.
* @internal
*/
function fmtpConfigHasParam(config, param) {
	return config.split(";").some((entry) => entry.trim() === param);
}
/** @internal */
function ensureAudioNackAndStereo(media, stereoMids, nackMids) {
	const mid = getMidString(media.mid);
	let opusPayload = 0;
	media.rtp.some((rtp) => {
		if (rtp.codec.toLowerCase() === "opus") {
			opusPayload = rtp.payload;
			return true;
		}
		return false;
	});
	if (opusPayload > 0) {
		if (!media.rtcpFb) media.rtcpFb = [];
		if (nackMids.includes(mid) && !media.rtcpFb.some((fb) => fb.payload === opusPayload && fb.type === "nack")) media.rtcpFb.push({
			payload: opusPayload,
			type: "nack"
		});
		if (stereoMids.includes(mid) || stereoMids.length === 1 && stereoMids[0] === "all") media.fmtp.some((fmtp) => {
			if (fmtp.payload === opusPayload) {
				if (!fmtpConfigHasParam(fmtp.config, "stereo=1")) fmtp.config += ";stereo=1";
				return true;
			}
			return false;
		});
	}
}
/**
* Returns the mids of transceivers that carry no outgoing track: the
* pre-populated placeholders added by `RTCEngine.applyInitialPublisherLayout`,
* plus any transceiver that was used for a track and reverted on unpublish. The
* `sender.track` check is the reliable signal — an unpublished section keeps its
* `a=msid` (and its stale send-derived fmtp), so it can't be told apart from a
* real send by SDP alone. Transceiver mids are stable across renegotiations, so
* this works for every offer/answer after the first.
* @internal
*/
function placeholderMidsFromTransceivers(transceivers) {
	const mids = /* @__PURE__ */ new Set();
	for (const transceiver of transceivers) if (transceiver.mid && !transceiver.sender.track) mids.add(transceiver.mid);
	return mids;
}
/**
* Within a BUNDLE group a payload type must map to identical codec parameters
* across every m-line. When the same payload type carries different fmtp between
* sections — e.g. opus `usedtx=1` on the published microphone but not on the
* pre-populated recvonly placeholders, or H.265 with different `level-id` between
* a published video track and a placeholder — libwebrtc flags a "bundled payload
* type collision".
*
* Rewrite the placeholder sections so every shared payload type carries the
* same fmtp. Real (non-placeholder) sections always win the canonical value, so
* a published track's encoder parameters are never altered. When no real
* section declares a payload type — e.g. a placeholder that was reused for a
* track and then reverted to recvonly keeps its send-derived `level-id` while
* fresh placeholders use the default — the placeholders still converge on the
* first value seen, so two placeholders can't disagree either. Only placeholder
* sections are ever rewritten, and it is codec-agnostic (opus, H.265, ...).
* `isPlaceholder` identifies the sections to conform.
* @internal
*/
function conformBundledCodecFmtp(media, isPlaceholder) {
	var _a, _b;
	const canonicalByPayload = /* @__PURE__ */ new Map();
	const fromRealSection = /* @__PURE__ */ new Set();
	for (const m of media) {
		const placeholder = isPlaceholder(m);
		for (const fmtp of (_a = m.fmtp) !== null && _a !== void 0 ? _a : []) if (!placeholder) {
			canonicalByPayload.set(fmtp.payload, fmtp.config);
			fromRealSection.add(fmtp.payload);
		} else if (!canonicalByPayload.has(fmtp.payload)) canonicalByPayload.set(fmtp.payload, fmtp.config);
	}
	if (canonicalByPayload.size === 0) return;
	for (const m of media) {
		if (!isPlaceholder(m)) continue;
		for (const fmtp of (_b = m.fmtp) !== null && _b !== void 0 ? _b : []) {
			const config = canonicalByPayload.get(fmtp.payload);
			if (config !== void 0 && fmtp.config !== config) fmtp.config = config;
		}
	}
}
/** @internal */
function extractStereoAndNackAudioFromOffer(offer) {
	var _a;
	const stereoMids = [];
	const nackMids = [];
	const sdpParsed = libExports.parse((_a = offer.sdp) !== null && _a !== void 0 ? _a : "");
	let opusPayload = 0;
	sdpParsed.media.forEach((media) => {
		var _a;
		const mid = getMidString(media.mid);
		if (media.type === "audio") {
			media.rtp.some((rtp) => {
				if (rtp.codec.toLowerCase() === "opus") {
					opusPayload = rtp.payload;
					return true;
				}
				return false;
			});
			if ((_a = media.rtcpFb) === null || _a === void 0 ? void 0 : _a.some((fb) => fb.payload === opusPayload && fb.type === "nack")) nackMids.push(mid);
			media.fmtp.some((fmtp) => {
				if (fmtp.payload === opusPayload) {
					if (fmtpConfigHasParam(fmtp.config, "sprop-stereo=1")) stereoMids.push(mid);
					return true;
				}
				return false;
			});
		}
	});
	return {
		stereoMids,
		nackMids
	};
}
function ensureIPAddrMatchVersion(media) {
	if (media.connection) {
		const isV6 = media.connection.ip.indexOf(":") >= 0;
		if (media.connection.version === 4 && isV6 || media.connection.version === 6 && !isV6) {
			media.connection.ip = "0.0.0.0";
			media.connection.version = 4;
		}
	}
}
function getMidString(mid) {
	return typeof mid === "number" ? mid.toFixed(0) : mid;
}
var defaultVideoCodec = "vp8";
var publishDefaults = {
	audioPreset: AudioPresets.music,
	dtx: true,
	red: true,
	forceStereo: false,
	simulcast: true,
	screenShareEncoding: ScreenSharePresets.h1080fps15.encoding,
	stopMicTrackOnMute: false,
	videoCodec: defaultVideoCodec,
	backupCodec: true,
	preConnectBuffer: false
};
var audioDefaults = {
	deviceId: { ideal: "default" },
	autoGainControl: true,
	echoCancellation: true,
	noiseSuppression: true,
	voiceIsolation: true
};
var videoDefaults = {
	deviceId: { ideal: "default" },
	resolution: VideoPresets.h720.resolution
};
var roomOptionDefaults = {
	adaptiveStream: false,
	dynacast: false,
	stopLocalTrackOnUnpublish: true,
	reconnectPolicy: new DefaultReconnectPolicy(),
	disconnectOnPageLeave: true,
	webAudioMix: false,
	singlePeerConnection: true
};
var roomConnectOptionDefaults = {
	autoSubscribe: true,
	maxRetries: 1,
	peerConnectionTimeout: 15e3,
	websocketTimeout: 15e3
};
var PCTransportState;
(function(PCTransportState) {
	PCTransportState[PCTransportState["NEW"] = 0] = "NEW";
	PCTransportState[PCTransportState["CONNECTING"] = 1] = "CONNECTING";
	PCTransportState[PCTransportState["CONNECTED"] = 2] = "CONNECTED";
	PCTransportState[PCTransportState["FAILED"] = 3] = "FAILED";
	PCTransportState[PCTransportState["CLOSING"] = 4] = "CLOSING";
	PCTransportState[PCTransportState["CLOSED"] = 5] = "CLOSED";
})(PCTransportState || (PCTransportState = {}));
var PCTransportManager = class {
	get needsPublisher() {
		return this.isPublisherConnectionRequired;
	}
	get needsSubscriber() {
		return this.isSubscriberConnectionRequired;
	}
	get currentState() {
		return this.state;
	}
	get mode() {
		return this._mode;
	}
	constructor(mode, loggerOptions, rtcConfig) {
		var _a;
		this.peerConnectionTimeout = roomConnectOptionDefaults.peerConnectionTimeout;
		this.log = livekitLogger;
		this.iceLog = livekitLogger;
		this.updateState = () => {
			var _a, _b;
			const previousState = this.state;
			const connectionStates = this.requiredTransports.map((tr) => tr.getConnectionState());
			if (connectionStates.every((st) => st === "connected")) this.state = PCTransportState.CONNECTED;
			else if (connectionStates.some((st) => st === "failed")) this.state = PCTransportState.FAILED;
			else if (connectionStates.some((st) => st === "connecting")) this.state = PCTransportState.CONNECTING;
			else if (connectionStates.every((st) => st === "closed")) this.state = PCTransportState.CLOSED;
			else if (connectionStates.some((st) => st === "closed")) this.state = PCTransportState.CLOSING;
			else if (connectionStates.every((st) => st === "new")) this.state = PCTransportState.NEW;
			if (previousState !== this.state) {
				this.log.debug("pc state change: from ".concat(PCTransportState[previousState], " to ").concat(PCTransportState[this.state]));
				(_a = this.onStateChange) === null || _a === void 0 || _a.call(this, this.state, this.publisher.getConnectionState(), (_b = this.subscriber) === null || _b === void 0 ? void 0 : _b.getConnectionState());
			}
		};
		this.loggerOptions = loggerOptions;
		this.log = getLogger((_a = loggerOptions.loggerName) !== null && _a !== void 0 ? _a : LoggerNames.PCManager, () => this.logContext);
		this.iceLog = getLogger(LoggerNames.ICE, () => this.logContext);
		this.isPublisherConnectionRequired = mode !== "subscriber-primary";
		this.isSubscriberConnectionRequired = mode === "subscriber-primary";
		this.publisher = new PCTransport(rtcConfig, loggerOptions);
		this._mode = mode;
		if (mode !== "publisher-only") {
			this.subscriber = new PCTransport(rtcConfig, loggerOptions);
			this.subscriber.onConnectionStateChange = this.updateState;
			this.subscriber.onIceConnectionStateChange = this.updateState;
			this.subscriber.onSignalingStatechange = this.updateState;
			this.subscriber.onIceCandidate = (candidate) => {
				var _a;
				(_a = this.onIceCandidate) === null || _a === void 0 || _a.call(this, candidate, SignalTarget.SUBSCRIBER);
			};
			this.subscriber.onDataChannel = (ev) => {
				var _a;
				(_a = this.onDataChannel) === null || _a === void 0 || _a.call(this, ev);
			};
			this.subscriber.onTrack = (ev) => {
				var _a;
				(_a = this.onTrack) === null || _a === void 0 || _a.call(this, ev);
			};
		}
		this.publisher.onConnectionStateChange = this.updateState;
		this.publisher.onIceConnectionStateChange = this.updateState;
		this.publisher.onSignalingStatechange = this.updateState;
		this.publisher.onIceCandidate = (candidate) => {
			var _a;
			(_a = this.onIceCandidate) === null || _a === void 0 || _a.call(this, candidate, SignalTarget.PUBLISHER);
		};
		this.publisher.onTrack = (ev) => {
			var _a;
			(_a = this.onTrack) === null || _a === void 0 || _a.call(this, ev);
		};
		this.publisher.onOffer = (offer, offerId) => {
			var _a;
			(_a = this.onPublisherOffer) === null || _a === void 0 || _a.call(this, offer, offerId);
		};
		this.state = PCTransportState.NEW;
		this.connectionLock = new _();
		this.remoteOfferLock = new _();
	}
	get logContext() {
		var _a, _b;
		return Object.assign({}, (_b = (_a = this.loggerOptions).loggerContextCb) === null || _b === void 0 ? void 0 : _b.call(_a));
	}
	requirePublisher() {
		let require = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : true;
		this.isPublisherConnectionRequired = require;
		this.updateState();
	}
	createAndSendPublisherOffer(options) {
		return this.publisher.createAndSendOffer(options);
	}
	setPublisherAnswer(sd, offerId) {
		return this.publisher.setRemoteDescription(sd, offerId);
	}
	removeTrack(sender) {
		return this.publisher.removeTrack(sender);
	}
	close() {
		return __awaiter(this, void 0, void 0, function* () {
			var _a;
			if (this.publisher && this.publisher.getSignallingState() !== "closed") {
				const publisher = this.publisher;
				for (const sender of publisher.getSenders()) try {
					if (publisher.canRemoveTrack()) publisher.removeTrack(sender);
				} catch (e) {
					this.log.warn("could not removeTrack", { error: e });
				}
			}
			yield Promise.all([this.publisher.close(), (_a = this.subscriber) === null || _a === void 0 ? void 0 : _a.close()]);
			this.updateState();
		});
	}
	triggerIceRestart() {
		return __awaiter(this, void 0, void 0, function* () {
			this.iceLog.warn("triggering ICE restart");
			if (this.subscriber) this.subscriber.restartingIce = true;
			if (this.needsPublisher) yield this.createAndSendPublisherOffer({ iceRestart: true });
		});
	}
	addIceCandidate(candidate, target) {
		return __awaiter(this, void 0, void 0, function* () {
			var _a;
			this.iceLog.debug("adding remote ICE candidate", {
				target,
				candidate
			});
			if (target === SignalTarget.PUBLISHER) yield this.publisher.addIceCandidate(candidate);
			else yield (_a = this.subscriber) === null || _a === void 0 ? void 0 : _a.addIceCandidate(candidate);
		});
	}
	createSubscriberAnswerFromOffer(sd, offerId) {
		return __awaiter(this, void 0, void 0, function* () {
			var _a, _b, _c;
			this.log.debug("received server offer", {
				RTCSdpType: sd.type,
				sdp: sd.sdp,
				signalingState: (_a = this.subscriber) === null || _a === void 0 ? void 0 : _a.getSignallingState().toString()
			});
			const unlock = yield this.remoteOfferLock.lock();
			try {
				if (!(yield (_b = this.subscriber) === null || _b === void 0 ? void 0 : _b.setRemoteDescription(sd, offerId))) return;
				return yield (_c = this.subscriber) === null || _c === void 0 ? void 0 : _c.createAndSetAnswer();
			} finally {
				unlock();
			}
		});
	}
	updateConfiguration(config, iceRestart) {
		var _a;
		this.log.debug("updating rtc configuration", { iceRestart });
		this.publisher.setConfiguration(config);
		(_a = this.subscriber) === null || _a === void 0 || _a.setConfiguration(config);
		if (iceRestart) this.triggerIceRestart();
	}
	ensurePCTransportConnection(abortController, timeout) {
		return __awaiter(this, void 0, void 0, function* () {
			var _a;
			const unlock = yield this.connectionLock.lock();
			try {
				if (this.isPublisherConnectionRequired && this.publisher.getConnectionState() !== "connected" && this.publisher.getConnectionState() !== "connecting") {
					this.log.debug("negotiation required, start negotiating");
					this.publisher.negotiate();
				}
				yield Promise.all((_a = this.requiredTransports) === null || _a === void 0 ? void 0 : _a.map((transport) => this.ensureTransportConnected(transport, abortController, timeout)));
			} finally {
				unlock();
			}
		});
	}
	negotiate(abortController) {
		return __awaiter(this, void 0, void 0, function* () {
			return new TypedPromise((resolve, reject) => {
				const checkpoint = this.publisher.latestOfferId;
				if (this.publisher.latestAcknowledgedOfferId > checkpoint) {
					this.log.debug("negotiation already handled in more recent acknowledged offer", this.logContext);
					resolve();
					return;
				}
				let cleanedUp = false;
				const cleanup = () => {
					if (cleanedUp) return;
					cleanedUp = true;
					clearTimeout(deadlineTimer);
					this.publisher.off(PCEvents.OfferAnswered, onAnswered);
					abortController.signal.removeEventListener("abort", onAbort);
				};
				const onAnswered = (offerId) => {
					if (offerId > checkpoint) {
						cleanup();
						resolve();
					}
				};
				const onAbort = () => {
					cleanup();
					reject(new NegotiationError("negotiation aborted"));
				};
				const deadlineTimer = setTimeout(() => {
					cleanup();
					reject(new NegotiationError("negotiation timed out"));
				}, this.peerConnectionTimeout);
				abortController.signal.addEventListener("abort", onAbort);
				this.publisher.on(PCEvents.OfferAnswered, onAnswered);
				this.publisher.negotiate((e) => {
					cleanup();
					if (e instanceof Error) reject(e);
					else reject(new Error(String(e)));
				});
			});
		});
	}
	addPublisherTransceiver(track, transceiverInit) {
		return this.publisher.addTransceiver(track, transceiverInit);
	}
	addPublisherTransceiverOfKind(kind, transceiverInit) {
		return this.publisher.addTransceiverOfKind(kind, transceiverInit);
	}
	getMidForReceiver(receiver) {
		const matchingTransceiver = (this.subscriber ? this.subscriber.getTransceivers() : this.publisher.getTransceivers()).find((transceiver) => transceiver.receiver === receiver);
		return matchingTransceiver === null || matchingTransceiver === void 0 ? void 0 : matchingTransceiver.mid;
	}
	getMaxPublisherMessageSize() {
		return this.publisher.getMaxMessageSize();
	}
	addPublisherTrack(track) {
		return this.publisher.addTrack(track);
	}
	createPublisherDataChannel(label, dataChannelDict) {
		return this.publisher.createDataChannel(label, dataChannelDict);
	}
	/**
	* Returns the first required transport's address if no explicit target is specified
	*/
	getConnectedAddress(target) {
		if (target === SignalTarget.PUBLISHER) return this.publisher.getConnectedAddress();
		else if (target === SignalTarget.SUBSCRIBER) return this.publisher.getConnectedAddress();
		return this.requiredTransports[0].getConnectedAddress();
	}
	get requiredTransports() {
		const transports = [];
		if (this.isPublisherConnectionRequired) transports.push(this.publisher);
		if (this.isSubscriberConnectionRequired && this.subscriber) transports.push(this.subscriber);
		return transports;
	}
	ensureTransportConnected(pcTransport_1, abortController_1) {
		return __awaiter(this, arguments, void 0, function(pcTransport, abortController) {
			var _this = this;
			let timeout = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : this.peerConnectionTimeout;
			return function* () {
				if (pcTransport.getConnectionState() === "connected") return;
				return new Promise((resolve, reject) => __awaiter(_this, void 0, void 0, function* () {
					const abortHandler = () => {
						this.log.warn("abort transport connection");
						CriticalTimers.clearTimeout(connectTimeout);
						reject(ConnectionError.cancelled("room connection has been cancelled"));
					};
					if (abortController === null || abortController === void 0 ? void 0 : abortController.signal.aborted) abortHandler();
					abortController === null || abortController === void 0 || abortController.signal.addEventListener("abort", abortHandler);
					const connectTimeout = CriticalTimers.setTimeout(() => {
						abortController === null || abortController === void 0 || abortController.signal.removeEventListener("abort", abortHandler);
						reject(ConnectionError.internal("could not establish pc connection"));
					}, timeout);
					while (this.state !== PCTransportState.CONNECTED) {
						yield sleep(50);
						if (abortController === null || abortController === void 0 ? void 0 : abortController.signal.aborted) {
							reject(ConnectionError.cancelled("room connection has been cancelled"));
							return;
						}
					}
					CriticalTimers.clearTimeout(connectTimeout);
					abortController === null || abortController === void 0 || abortController.signal.removeEventListener("abort", abortHandler);
					resolve();
				}));
			}();
		});
	}
};
var FlowControlledDataChannel = class {
	constructor(opts) {
		/** Last emitted low-water status; starts true (an empty buffer is below the mark). */
		this.bufferStatusLow = true;
		this.headroomLock = new _();
		/** Cancels parked headroom waiters when the handle is replaced or torn down. */
		this.waiterAbortController = new AbortController();
		this.kind = opts.kind;
		this.lowWaterMark = opts.lowWaterMark;
		this.highWaterMark = opts.highWaterMark;
		this.isEngineClosed = opts.isEngineClosed;
		this.onBufferStatusChanged = opts.onBufferStatusChanged;
	}
	/** The currently attached RTCDataChannel handle, if any. */
	get channelHandle() {
		return this.handle;
	}
	/**
	* Attaches the channel handle this wrapper controls. Replacing an existing handle rejects
	* parked waiters — their events would never fire again on the abandoned object — and installs a
	* fresh controller, so queued senders re-check against the new channel. Wrappers outlive their
	* handles: this is the one place handle turnover happens, which is what makes stranding a
	* waiter structurally impossible.
	*/
	attach(dc) {
		if (this.handle && this.handle !== dc) this.invalidateWaiters("data channel replaced");
		this.handle = dc;
	}
	/** Detaches the handle on teardown, rejecting parked waiters. */
	detach() {
		let reason = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "data channel torn down";
		if (this.handle) this.invalidateWaiters(reason);
		this.handle = void 0;
	}
	getChannel() {
		return this.handle;
	}
	/**
	* Whether the send buffer has room to accept more data (the send gate). Senders proceed while
	* this is true and block once it goes false. Callers resolve the handle (and decide what an
	* absent one means) before asking.
	*/
	isBelowHighWaterMark(dc) {
		return dc.bufferedAmount <= this.highWaterMark;
	}
	/**
	* Whether the send buffer has drained to its low-water mark. Drives the engine's public
	* DCBufferStatusChanged event.
	*/
	isBelowLowWaterMark(dc) {
		return dc.bufferedAmount <= dc.bufferedAmountLowThreshold;
	}
	/**
	* Acquires the headroom lock, resolving with the unlock function. Batch senders (the resume
	* replay) hold it across all of their sends so no other sender can interleave, calling
	* {@link waitForHeadroomWithoutLock} per message to respect flow control within the batch.
	*/
	lockHeadroom() {
		return this.headroomLock.lock();
	}
	/**
	* Resolves once the caller may send on this channel: immediately while the send buffer is at or
	* below its high-water mark, otherwise once the buffer has drained to the low-water mark (the
	* `bufferedamountlow` event). Callers are serialized through the headroom lock so that, when
	* the buffer drains, they refill it one at a time (up to the high-water mark) rather than all
	* sending at once and overflowing the SCTP send buffer (see livekit/client-sdk-js#1995). The
	* closed/buffer checks run inside the lock so queued callers proceed in FIFO order.
	*/
	waitForHeadroomWithLock() {
		return __awaiter(this, void 0, void 0, function* () {
			const unlock = yield this.lockHeadroom();
			try {
				yield this.waitForHeadroomWithoutLock();
			} finally {
				unlock();
			}
		});
	}
	/** Core wait of {@link waitForHeadroomWithLock}. The caller must hold the headroom lock. */
	waitForHeadroomWithoutLock() {
		return __awaiter(this, void 0, void 0, function* () {
			if (this.isEngineClosed()) throw new UnexpectedConnectionState("engine closed");
			const dc = this.getChannel();
			if (!dc) throw new UnexpectedConnectionState("DataChannel not found, kind: ".concat(this.kind));
			if (this.isBelowHighWaterMark(dc)) return;
			const abortSignal = this.waiterAbortController.signal;
			yield new TypedPromise((resolve, reject) => {
				const onBufferedAmountLow = () => {
					cleanup();
					resolve();
				};
				const onDCClose = () => {
					cleanup();
					reject(new UnexpectedConnectionState("DataChannel ".concat(this.kind, " closed while draining the buffer")));
				};
				const onAbort = () => {
					cleanup();
					reject(new UnexpectedConnectionState("DataChannel ".concat(this.kind, " was replaced or torn down while waiting for headroom")));
				};
				const cleanup = () => {
					dc.removeEventListener("bufferedamountlow", onBufferedAmountLow);
					dc.removeEventListener("close", onDCClose);
					abortSignal.removeEventListener("abort", onAbort);
				};
				if (abortSignal.aborted) {
					onAbort();
					return;
				}
				dc.addEventListener("bufferedamountlow", onBufferedAmountLow);
				dc.addEventListener("close", onDCClose);
				abortSignal.addEventListener("abort", onAbort);
			});
		});
	}
	/** Rejects all parked headroom waiters; the next waiter gets a fresh controller. */
	invalidateWaiters(reason) {
		this.waiterAbortController.abort(reason);
		this.waiterAbortController = new AbortController();
	}
	/**
	* Recomputes whether the buffer has drained to the low-water mark and, if that changed since the
	* last check, notifies the status listener. Two independent triggers land here: a send (which
	* raises the buffer) and the `bufferedamountlow` drain event (which lowers it) — the latter has
	* no send to hang the work off, which is why this is a shared entry point rather than a tail of
	* `send`.
	*/
	refreshBufferStatus() {
		var _a;
		const dc = this.getChannel();
		if (!dc) return;
		const isLow = this.isBelowLowWaterMark(dc);
		if (isLow !== this.bufferStatusLow) {
			this.bufferStatusLow = isLow;
			(_a = this.onBufferStatusChanged) === null || _a === void 0 || _a.call(this, isLow);
		}
	}
};
var LossyDataChannel = class extends FlowControlledDataChannel {
	constructor(opts) {
		super(opts);
		this.statCurrentBytes = 0;
		this.statByterate = 0;
		this.dropCount = 0;
		this.bufferFullBehavior = opts.bufferFullBehavior;
		this.shouldSkipSends = opts.shouldSkipSends;
	}
	/** Sends prepared bytes with this channel's full-buffer policy (drop or wait). */
	send(msg) {
		return __awaiter(this, void 0, void 0, function* () {
			const dc = this.getChannel();
			if (!dc) return;
			switch (this.bufferFullBehavior) {
				case "wait":
					if (!this.isBelowHighWaterMark(dc)) yield this.waitForHeadroomWithLock();
					break;
				case "drop": if (!this.isBelowLowWaterMark(dc)) {
					this.dropCount += 1;
					if (this.dropCount % 100 === 0) livekitLogger.warn("dropping lossy data channel messages, total dropped: ".concat(this.dropCount));
					return;
				}
			}
			this.statCurrentBytes += msg.byteLength;
			if (this.shouldSkipSends()) return;
			try {
				dc.send(msg);
				this.refreshBufferStatus();
			} catch (error) {
				if (error instanceof TypeError) livekitLogger.error(error);
				else throw error;
			}
		});
	}
	/**
	* Starts the once-per-second adjustment of the channel's `bufferedAmountLowThreshold` to the
	* observed byterate, keeping the drop gate at roughly 100ms of buffered latency (clamped to
	* the watermarks). Restarts cleanly if already running.
	*/
	startThresholdTuning() {
		this.stopThresholdTuning();
		this.statInterval = CriticalTimers.setInterval(() => {
			this.statByterate = this.statCurrentBytes;
			this.statCurrentBytes = 0;
			const dc = this.getChannel();
			if (dc) {
				const threshold = this.statByterate / 10;
				dc.bufferedAmountLowThreshold = Math.min(Math.max(threshold, this.lowWaterMark), this.highWaterMark);
			}
		}, 1e3);
	}
	/** Stops the threshold tuning and resets the stats and drop counter. */
	stopThresholdTuning() {
		this.statByterate = 0;
		this.statCurrentBytes = 0;
		if (this.statInterval) {
			CriticalTimers.clearInterval(this.statInterval);
			this.statInterval = void 0;
		}
		this.dropCount = 0;
	}
};
var DataPacketBuffer = class {
	constructor() {
		this.buffer = [];
		this._totalSize = 0;
		this._sentSize = 0;
	}
	push(item) {
		this.buffer.push(item);
		this._totalSize += item.data.byteLength;
		if (item.sent) this._sentSize += item.data.byteLength;
	}
	pop() {
		const item = this.buffer.shift();
		if (item) {
			this._totalSize -= item.data.byteLength;
			if (item.sent) this._sentSize -= item.data.byteLength;
		}
		return item;
	}
	getAll() {
		return this.buffer.slice();
	}
	/** Every queued packet not yet handed to the channel, in sequence order. */
	getUnsent() {
		return this.buffer.filter((item) => !item.sent);
	}
	/** Marks a single queued packet as handed to the channel. */
	markSent(item) {
		if (!item.sent) {
			item.sent = true;
			this._sentSize += item.data.byteLength;
		}
	}
	/**
	* Marks every queued packet as not-yet-sent. Used at the start of a resume replay: whatever is
	* still buffered was sent on the previous channel (or deferred) and must be re-handed to the
	* current one, so none of it counts as sent until the replay actually transmits it.
	*/
	markAllUnsent() {
		for (const item of this.buffer) item.sent = false;
		this._sentSize = 0;
	}
	popToSequence(sequence) {
		while (this.buffer.length > 0) if (this.buffer[0].sequence <= sequence) this.pop();
		else break;
	}
	alignBufferedAmount(bufferedAmount) {
		while (this.buffer.length > 0) {
			const first = this.buffer[0];
			if (!first.sent) break;
			if (this._sentSize - first.data.byteLength <= bufferedAmount) break;
			this.pop();
		}
	}
	get length() {
		return this.buffer.length;
	}
};
var ReliableDataChannel = class extends FlowControlledDataChannel {
	constructor(opts) {
		super(opts);
		this.messageBuffer = new DataPacketBuffer();
		this.sequence = 1;
		this.isDeferringSends = opts.isDeferringSends;
	}
	/**
	* Claims the next packet sequence. The caller stamps it into the packet before serialization,
	* then passes it back to {@link send} so the replay buffer stays keyed by wire sequence.
	*/
	nextSequence() {
		const sequence = this.sequence;
		this.sequence += 1;
		return sequence;
	}
	/**
	* Sends prepared bytes with reliable semantics. Resolves once the packet has either been handed
	* to the channel or queued for the resume replay; throws only when the engine is closed.
	*/
	send(msg, sequence) {
		return __awaiter(this, void 0, void 0, function* () {
			if (this.isDeferringSends()) {
				this.messageBuffer.push({
					data: msg,
					sequence,
					sent: false
				});
				return;
			}
			const dc = this.getChannel();
			if (!dc) return;
			try {
				yield this.waitForHeadroomWithLock();
			} catch (error) {
				if (this.isEngineClosed()) throw error;
				this.messageBuffer.push({
					data: msg,
					sequence,
					sent: false
				});
				return;
			}
			if (this.isDeferringSends()) {
				this.messageBuffer.push({
					data: msg,
					sequence,
					sent: false
				});
				return;
			}
			this.messageBuffer.push({
				data: msg,
				sequence,
				sent: true
			});
			dc.send(msg);
			this.refreshBufferStatus();
		});
	}
	/**
	* Replays the buffered backlog after a resume: drops everything the server acked
	* (`lastMessageSeq`), then re-sends the rest in order. The headroom lock is held across the
	* whole replay — releasing it between messages would let a concurrent send (whose newer
	* sequence was already assigned before it queued on the lock) hit the wire mid-replay, and
	* receivers would then discard the remaining lower-sequence resent messages as duplicates.
	*/
	replay(lastMessageSeq) {
		return __awaiter(this, void 0, void 0, function* () {
			const dc = this.getChannel();
			if (!dc) return;
			this.messageBuffer.popToSequence(lastMessageSeq);
			const unlock = yield this.lockHeadroom();
			try {
				this.messageBuffer.markAllUnsent();
				for (let batch = this.messageBuffer.getUnsent(); batch.length > 0; batch = this.messageBuffer.getUnsent()) for (const item of batch) {
					yield this.waitForHeadroomWithoutLock();
					dc.send(item.data);
					this.messageBuffer.markSent(item);
				}
			} finally {
				unlock();
			}
			this.refreshBufferStatus();
		});
	}
	/**
	* Before recomputing status, trim packets the transport has now delivered — a send or a drain
	* may have acked buffered packets, and the replay buffer is keyed off the channel's buffered
	* bytes.
	*/
	refreshBufferStatus() {
		const dc = this.channelHandle;
		if (dc) this.messageBuffer.alignBufferedAmount(dc.bufferedAmount);
		super.refreshBufferStatus();
	}
	/**
	* Drops all replay state and restarts sequencing. Only valid on a full reconnect, where the
	* session (and the receivers' sequence tracking) starts over.
	*/
	reset() {
		this.messageBuffer = new DataPacketBuffer();
		this.sequence = 1;
	}
};
var DataChannelKind;
(function(DataChannelKind) {
	DataChannelKind[DataChannelKind["RELIABLE"] = 0] = "RELIABLE";
	DataChannelKind[DataChannelKind["LOSSY"] = 1] = "LOSSY";
	DataChannelKind[DataChannelKind["DATA_TRACK_LOSSY"] = 2] = "DATA_TRACK_LOSSY";
})(DataChannelKind || (DataChannelKind = {}));
var reliableDataChannelWaterMarkLow = 65536;
var reliableDataChannelWaterMarkHigh = 1048576;
var lossyDataChannelWaterMarkLow = 8192;
var lossyDataChannelWaterMarkHigh = 262144;
function dataChannelLowWaterMark(kind) {
	return kind === DataChannelKind.RELIABLE ? reliableDataChannelWaterMarkLow : lossyDataChannelWaterMarkLow;
}
function dataChannelHighWaterMark(kind) {
	return kind === DataChannelKind.RELIABLE ? reliableDataChannelWaterMarkHigh : lossyDataChannelWaterMarkHigh;
}
var lossyDataChannelLabel = "_lossy";
var reliableDataChannelLabel = "_reliable";
var dataTrackDataChannelLabel = "_data_track";
/**
* Owns the engine's data channels: the three flow-controlled publisher channel wrappers (which
* live for the engine's lifetime and have RTCDataChannel handles attached/detached as peer
* connections come and go) plus the subscriber-side receive handles adopted by label.
*
* Handle turnover goes through {@link FlowControlledDataChannel.attach}/`detach`, which reject
* parked headroom waiters as a built-in — there is no separate invalidation step to forget.
*/
var DataChannelManager = class {
	constructor(opts) {
		this.opts = opts;
		const flowControlOptions = (kind) => ({
			kind,
			lowWaterMark: dataChannelLowWaterMark(kind),
			highWaterMark: dataChannelHighWaterMark(kind),
			isEngineClosed: opts.isEngineClosed,
			onBufferStatusChanged: (isLow) => opts.onBufferStatusChanged(kind, isLow)
		});
		this.reliable = new ReliableDataChannel(Object.assign(Object.assign({}, flowControlOptions(DataChannelKind.RELIABLE)), { isDeferringSends: opts.isReconnecting }));
		this.lossy = new LossyDataChannel(Object.assign(Object.assign({}, flowControlOptions(DataChannelKind.LOSSY)), {
			bufferFullBehavior: "drop",
			shouldSkipSends: opts.isReconnecting
		}));
		this.dataTrack = new LossyDataChannel(Object.assign(Object.assign({}, flowControlOptions(DataChannelKind.DATA_TRACK_LOSSY)), {
			bufferFullBehavior: "wait",
			shouldSkipSends: opts.isReconnecting
		}));
	}
	/** The flow-control wrapper for `kind`. */
	channelFor(kind) {
		switch (kind) {
			case DataChannelKind.RELIABLE: return this.reliable;
			case DataChannelKind.LOSSY: return this.lossy;
			case DataChannelKind.DATA_TRACK_LOSSY: return this.dataTrack;
		}
	}
	/** The raw RTCDataChannel handle for `kind`, publisher side by default. */
	getHandle(kind) {
		if (!(arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : false)) return this.channelFor(kind).channelHandle;
		switch (kind) {
			case DataChannelKind.RELIABLE: return this.reliableSub;
			case DataChannelKind.LOSSY: return this.lossySub;
			case DataChannelKind.DATA_TRACK_LOSSY: return this.dataTrackSub;
		}
	}
	get hasPublisherChannels() {
		return Boolean(this.reliable.channelHandle || this.lossy.channelHandle || this.dataTrack.channelHandle);
	}
	/**
	* Creates the three publisher data channels on the given transport, wires their handlers, and
	* attaches them to the wrappers — attaching rejects any waiters still parked on replaced
	* channel objects.
	*/
	createPublisherChannels(pcManager) {
		for (const channel of [
			this.lossy,
			this.reliable,
			this.dataTrack
		]) {
			const old = channel.channelHandle;
			if (old) {
				old.onmessage = null;
				old.onerror = null;
				old.onclose = null;
			}
		}
		const wire = (channel, dc, onMessage) => {
			dc.onmessage = onMessage;
			dc.onerror = this.opts.onDataError;
			dc.onclose = () => this.opts.onChannelClose(channel.kind);
			dc.bufferedAmountLowThreshold = channel.lowWaterMark;
			dc.onbufferedamountlow = () => channel.refreshBufferStatus();
			channel.attach(dc);
		};
		wire(this.lossy, pcManager.createPublisherDataChannel(lossyDataChannelLabel, {
			ordered: false,
			maxRetransmits: 0
		}), this.opts.onDataMessage);
		wire(this.reliable, pcManager.createPublisherDataChannel(reliableDataChannelLabel, { ordered: true }), this.opts.onDataMessage);
		wire(this.dataTrack, pcManager.createPublisherDataChannel(dataTrackDataChannelLabel, {
			ordered: false,
			maxRetransmits: 0
		}), this.opts.onDataTrackMessage);
		this.lossy.startThresholdTuning();
	}
	/**
	* Adopts a subscriber-side data channel by label, wiring the matching receive handler.
	* Returns false for labels this manager doesn't own.
	*/
	adoptSubscriberChannel(channel) {
		let handler;
		if (channel.label === reliableDataChannelLabel) {
			this.reliableSub = channel;
			handler = this.opts.onDataMessage;
		} else if (channel.label === lossyDataChannelLabel) {
			this.lossySub = channel;
			handler = this.opts.onDataMessage;
		} else if (channel.label === dataTrackDataChannelLabel) {
			this.dataTrackSub = channel;
			handler = this.opts.onDataTrackMessage;
		} else return false;
		channel.onmessage = handler;
		return true;
	}
	/**
	* Tears down all channels for a peer-connection cleanup: rejects parked waiters (detach — the
	* spec allows `pc.close()` to transition channels to 'closed' without firing events, so waiting
	* for browser close events is not an option), strips handlers, closes the handles, and resets
	* the reliable session state.
	*/
	teardown() {
		const dcCleanup = (dc) => {
			if (!dc) return;
			dc.onbufferedamountlow = null;
			dc.onclose = null;
			dc.onclosing = null;
			dc.onerror = null;
			dc.onmessage = null;
			dc.onopen = null;
			dc.close();
		};
		for (const channel of [
			this.lossy,
			this.reliable,
			this.dataTrack
		]) {
			const dc = channel.channelHandle;
			channel.detach("peer connections cleaned up");
			dcCleanup(dc);
		}
		dcCleanup(this.lossySub);
		dcCleanup(this.reliableSub);
		dcCleanup(this.dataTrackSub);
		this.lossySub = void 0;
		this.reliableSub = void 0;
		this.dataTrackSub = void 0;
		this.reliable.reset();
	}
};
var isMediaRecorderAvailable = typeof MediaRecorder !== "undefined";
var FallbackRecorder = class {
	constructor() {
		throw new Error("MediaRecorder is not available in this environment");
	}
};
var RecorderBase = isMediaRecorderAvailable ? MediaRecorder : FallbackRecorder;
var LocalTrackRecorder = class extends RecorderBase {
	constructor(track, options) {
		if (!isMediaRecorderAvailable) throw new Error("MediaRecorder is not available in this environment");
		super(new MediaStream([track.mediaStreamTrack]), options);
		let dataListener;
		let streamController;
		const isClosed = () => streamController === void 0;
		const onStop = () => {
			this.removeEventListener("dataavailable", dataListener);
			this.removeEventListener("stop", onStop);
			this.removeEventListener("error", onError);
			streamController === null || streamController === void 0 || streamController.close();
			streamController = void 0;
		};
		const onError = (event) => {
			streamController === null || streamController === void 0 || streamController.error(event);
			this.removeEventListener("dataavailable", dataListener);
			this.removeEventListener("stop", onStop);
			this.removeEventListener("error", onError);
			streamController = void 0;
		};
		this.byteStream = new ReadableStream({
			start: (controller) => {
				streamController = controller;
				dataListener = (event) => __awaiter(this, void 0, void 0, function* () {
					let data;
					if (event.data.arrayBuffer) {
						const arrayBuffer = yield event.data.arrayBuffer();
						data = new Uint8Array(arrayBuffer);
					} else if (event.data.byteArray) data = event.data.byteArray;
					else throw new Error("no data available!");
					if (isClosed()) return;
					controller.enqueue(data);
				});
				this.addEventListener("dataavailable", dataListener);
			},
			cancel: () => {
				onStop();
			}
		});
		this.addEventListener("stop", onStop);
		this.addEventListener("error", onError);
	}
};
function isRecordingSupported() {
	return isMediaRecorderAvailable;
}
var DEFAULT_DIMENSIONS_TIMEOUT = 1e3;
var PRE_CONNECT_BUFFER_TIMEOUT = 1e4;
var LocalTrack = class extends Track {
	/** @internal */
	get sender() {
		return this._sender;
	}
	/** @internal */
	set sender(sender) {
		this._sender = sender;
	}
	get constraints() {
		return this._constraints;
	}
	get hasPreConnectBuffer() {
		return !!this.localTrackRecorder;
	}
	/**
	*
	* @param mediaTrack
	* @param kind
	* @param constraints MediaTrackConstraints that are being used when restarting or reacquiring tracks
	* @param userProvidedTrack Signals to the SDK whether or not the mediaTrack should be managed (i.e. released and reacquired) internally by the SDK
	*/
	constructor(mediaTrack, kind, constraints) {
		let userProvidedTrack = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : false;
		let loggerOptions = arguments.length > 4 ? arguments[4] : void 0;
		super(mediaTrack, kind, loggerOptions);
		this.manuallyStopped = false;
		this.pendingDeviceChange = false;
		this._isUpstreamPaused = false;
		this.handleTrackMuteEvent = () => this.debouncedTrackMuteHandler().catch(() => this.log.debug("track mute bounce got cancelled by an unmute event", this.logContext));
		this.debouncedTrackMuteHandler = debounce(() => __awaiter(this, void 0, void 0, function* () {
			yield this.pauseUpstream();
		}), 5e3);
		this.handleTrackUnmuteEvent = () => __awaiter(this, void 0, void 0, function* () {
			this.debouncedTrackMuteHandler.cancel("unmute");
			yield this.resumeUpstream();
		});
		this.handleEnded = () => {
			if (this.isInBackground) this.reacquireTrack = true;
			this._mediaStreamTrack.removeEventListener("mute", this.handleTrackMuteEvent);
			this._mediaStreamTrack.removeEventListener("unmute", this.handleTrackUnmuteEvent);
			this.emit(TrackEvent.Ended, this);
		};
		this.reacquireTrack = false;
		this.providedByUser = userProvidedTrack;
		this.muteLock = new _();
		this.pauseUpstreamLock = new _();
		this.trackChangeLock = new _();
		this.trackChangeLock.lock().then((unlock) => __awaiter(this, void 0, void 0, function* () {
			try {
				yield this.setMediaStreamTrack(mediaTrack, true);
			} finally {
				unlock();
			}
		}));
		this._constraints = mediaTrack.getConstraints();
		if (constraints) this._constraints = constraints;
	}
	get id() {
		return this._mediaStreamTrack.id;
	}
	get dimensions() {
		if (this.kind !== Track.Kind.Video) return;
		const _this$_mediaStreamTra = this._mediaStreamTrack.getSettings(), width = _this$_mediaStreamTra.width, height = _this$_mediaStreamTra.height;
		if (width && height) return {
			width,
			height
		};
	}
	get isUpstreamPaused() {
		return this._isUpstreamPaused;
	}
	get isUserProvided() {
		return this.providedByUser;
	}
	get mediaStreamTrack() {
		var _a, _b;
		return (_b = (_a = this.processor) === null || _a === void 0 ? void 0 : _a.processedTrack) !== null && _b !== void 0 ? _b : this._mediaStreamTrack;
	}
	get isLocal() {
		return true;
	}
	/**
	* @internal
	* returns mediaStreamTrack settings of the capturing mediastreamtrack source - ignoring processors
	*/
	getSourceTrackSettings() {
		return this._mediaStreamTrack.getSettings();
	}
	setMediaStreamTrack(newTrack, force, isUnmuting) {
		return __awaiter(this, void 0, void 0, function* () {
			var _a;
			if (newTrack === this._mediaStreamTrack && !force) return;
			if (this._mediaStreamTrack) {
				this.attachedElements.forEach((el) => {
					detachTrack(this._mediaStreamTrack, el);
				});
				this.debouncedTrackMuteHandler.cancel("new-track");
				this._mediaStreamTrack.removeEventListener("ended", this.handleEnded);
				this._mediaStreamTrack.removeEventListener("mute", this.handleTrackMuteEvent);
				this._mediaStreamTrack.removeEventListener("unmute", this.handleTrackUnmuteEvent);
			}
			this.mediaStream = new MediaStream([newTrack]);
			if (newTrack) {
				newTrack.addEventListener("ended", this.handleEnded);
				newTrack.addEventListener("mute", this.handleTrackMuteEvent);
				newTrack.addEventListener("unmute", this.handleTrackUnmuteEvent);
				this._constraints = newTrack.getConstraints();
			}
			let processedTrack;
			if (this.processor && newTrack) {
				this.log.debug("restarting processor", this.logContext);
				if (this.kind === "unknown") throw TypeError("cannot set processor on track of unknown kind");
				if (this.processorElement) {
					attachToElement(newTrack, this.processorElement);
					this.processorElement.muted = true;
				}
				yield this.processor.restart({
					track: newTrack,
					kind: this.kind,
					element: this.processorElement,
					localTrack: this
				});
				processedTrack = this.processor.processedTrack;
			}
			if (this.sender && ((_a = this.sender.transport) === null || _a === void 0 ? void 0 : _a.state) !== "closed") yield this.sender.replaceTrack(processedTrack !== null && processedTrack !== void 0 ? processedTrack : newTrack);
			if (!this.providedByUser && this._mediaStreamTrack !== newTrack) this._mediaStreamTrack.stop();
			this._mediaStreamTrack = newTrack;
			if (newTrack) {
				this._mediaStreamTrack.enabled = isUnmuting ? true : !this.isMuted;
				yield this.resumeUpstream();
				this.attachedElements.forEach((el) => {
					attachToElement(processedTrack !== null && processedTrack !== void 0 ? processedTrack : newTrack, el);
				});
			}
		});
	}
	waitForDimensions() {
		return __awaiter(this, arguments, void 0, function() {
			var _this = this;
			let timeout = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : DEFAULT_DIMENSIONS_TIMEOUT;
			return function* () {
				var _a;
				if (_this.kind === Track.Kind.Audio) throw new Error("cannot get dimensions for audio tracks");
				if (((_a = getBrowser()) === null || _a === void 0 ? void 0 : _a.os) === "iOS") yield sleep(10);
				const started = Date.now();
				while (Date.now() - started < timeout) {
					const dims = _this.dimensions;
					if (dims) return dims;
					yield sleep(50);
				}
				throw new TrackInvalidError("unable to get track dimensions after timeout");
			}();
		});
	}
	setDeviceId(deviceId) {
		return __awaiter(this, void 0, void 0, function* () {
			if (this._constraints.deviceId === deviceId && this._mediaStreamTrack.getSettings().deviceId === unwrapConstraint(deviceId)) return true;
			this._constraints.deviceId = deviceId;
			if (this.isMuted) {
				this.pendingDeviceChange = true;
				return true;
			}
			yield this.restartTrack();
			return unwrapConstraint(deviceId) === this._mediaStreamTrack.getSettings().deviceId;
		});
	}
	/**
	* @returns DeviceID of the device that is currently being used for this track
	*/
	getDeviceId() {
		return __awaiter(this, arguments, void 0, function() {
			var _this2 = this;
			let normalize = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : true;
			return function* () {
				if (_this2.source === Track.Source.ScreenShare) return;
				const _this2$_mediaStreamTr = _this2._mediaStreamTrack.getSettings(), deviceId = _this2$_mediaStreamTr.deviceId, groupId = _this2$_mediaStreamTr.groupId;
				const kind = _this2.kind === Track.Kind.Audio ? "audioinput" : "videoinput";
				return normalize ? DeviceManager.getInstance().normalizeDeviceId(kind, deviceId, groupId) : deviceId;
			}();
		});
	}
	mute() {
		return __awaiter(this, void 0, void 0, function* () {
			this.setTrackMuted(true);
			return this;
		});
	}
	unmute() {
		return __awaiter(this, void 0, void 0, function* () {
			this.setTrackMuted(false);
			return this;
		});
	}
	replaceTrack(track, userProvidedOrOptions) {
		return __awaiter(this, void 0, void 0, function* () {
			const unlock = yield this.trackChangeLock.lock();
			try {
				if (!this.sender) throw new TrackInvalidError("unable to replace an unpublished track");
				let userProvidedTrack;
				let stopProcessor;
				if (typeof userProvidedOrOptions === "boolean") userProvidedTrack = userProvidedOrOptions;
				else if (userProvidedOrOptions !== void 0) {
					userProvidedTrack = userProvidedOrOptions.userProvidedTrack;
					stopProcessor = userProvidedOrOptions.stopProcessor;
				}
				this.providedByUser = userProvidedTrack !== null && userProvidedTrack !== void 0 ? userProvidedTrack : true;
				this.log.debug("replace MediaStreamTrack", this.logContext);
				yield this.setMediaStreamTrack(track);
				if (stopProcessor && this.processor) yield this.internalStopProcessor();
			} finally {
				unlock();
			}
			yield this.onSenderTrackSwapped();
			return this;
		});
	}
	/**
	* Hook invoked after the MediaStreamTrack on the sender has been swapped
	* (via replaceTrack, setProcessor, or stopProcessor). Fires outside the
	* trackChangeLock so subclasses can do asynchronous work such as polling
	* for new dimensions without blocking other track operations.
	*/
	onSenderTrackSwapped() {
		return __awaiter(this, void 0, void 0, function* () {});
	}
	restart(constraints, isUnmuting) {
		return __awaiter(this, void 0, void 0, function* () {
			this.manuallyStopped = false;
			const unlock = yield this.trackChangeLock.lock();
			try {
				if (!constraints) constraints = this._constraints;
				const _constraints = constraints, deviceId = _constraints.deviceId, facingMode = _constraints.facingMode, otherConstraints = __rest(constraints, ["deviceId", "facingMode"]);
				this.log.debug("restarting track with constraints", Object.assign(Object.assign({}, this.logContext), { constraints }));
				const streamConstraints = {
					audio: false,
					video: false
				};
				if (this.kind === Track.Kind.Video) streamConstraints.video = deviceId || facingMode ? {
					deviceId,
					facingMode
				} : true;
				else streamConstraints.audio = deviceId ? Object.assign({ deviceId }, otherConstraints) : true;
				this.attachedElements.forEach((el) => {
					detachTrack(this.mediaStreamTrack, el);
				});
				this._mediaStreamTrack.removeEventListener("ended", this.handleEnded);
				this._mediaStreamTrack.stop();
				const newTrack = (yield navigator.mediaDevices.getUserMedia(streamConstraints)).getTracks()[0];
				if (this.kind === Track.Kind.Video) yield newTrack.applyConstraints(otherConstraints);
				newTrack.addEventListener("ended", this.handleEnded);
				this.log.debug("re-acquired MediaStreamTrack", this.logContext);
				yield this.setMediaStreamTrack(newTrack, false, isUnmuting);
				this._constraints = constraints;
				this.pendingDeviceChange = false;
				this.emit(TrackEvent.Restarted, this);
				if (this.manuallyStopped) {
					this.log.warn("track was stopped during a restart, stopping restarted track", this.logContext);
					this.stop();
				}
				return this;
			} finally {
				unlock();
			}
		});
	}
	setTrackMuted(muted) {
		this.log.debug("setting ".concat(this.kind, " track ").concat(muted ? "muted" : "unmuted"), this.logContext);
		if (this.isMuted === muted && this._mediaStreamTrack.enabled !== muted) return;
		this.isMuted = muted;
		this._mediaStreamTrack.enabled = !muted;
		this.emit(muted ? TrackEvent.Muted : TrackEvent.Unmuted, this);
	}
	get needsReAcquisition() {
		return this._mediaStreamTrack.readyState !== "live" || this._mediaStreamTrack.muted || !this._mediaStreamTrack.enabled || this.reacquireTrack;
	}
	handleAppVisibilityChanged() {
		const _super = Object.create(null, { handleAppVisibilityChanged: { get: () => super.handleAppVisibilityChanged } });
		return __awaiter(this, void 0, void 0, function* () {
			yield _super.handleAppVisibilityChanged.call(this);
			if (!isMobile()) return;
			this.log.debug("visibility changed, is in Background: ".concat(this.isInBackground), this.logContext);
			if (!this.isInBackground && this.needsReAcquisition && !this.isUserProvided && !this.isMuted) {
				this.log.debug("track needs to be reacquired, restarting ".concat(this.source), this.logContext);
				yield this.restart();
				this.reacquireTrack = false;
			}
		});
	}
	stop() {
		var _a;
		this.manuallyStopped = true;
		super.stop();
		this._mediaStreamTrack.removeEventListener("ended", this.handleEnded);
		this._mediaStreamTrack.removeEventListener("mute", this.handleTrackMuteEvent);
		this._mediaStreamTrack.removeEventListener("unmute", this.handleTrackUnmuteEvent);
		(_a = this.processor) === null || _a === void 0 || _a.destroy();
		this.processor = void 0;
	}
	/**
	* pauses publishing to the server without disabling the local MediaStreamTrack
	* this is used to display a user's own video locally while pausing publishing to
	* the server.
	* this API is unsupported on Safari < 12 due to a bug
	**/
	pauseUpstream() {
		return __awaiter(this, void 0, void 0, function* () {
			var _a;
			const unlock = yield this.pauseUpstreamLock.lock();
			try {
				if (this._isUpstreamPaused === true) return;
				if (!this.sender) {
					this.log.warn("unable to pause upstream for an unpublished track", this.logContext);
					return;
				}
				this._isUpstreamPaused = true;
				this.emit(TrackEvent.UpstreamPaused, this);
				const browser = getBrowser();
				if ((browser === null || browser === void 0 ? void 0 : browser.name) === "Safari" && compareVersions(browser.version, "12.0") < 0) throw new DeviceUnsupportedError("pauseUpstream is not supported on Safari < 12.");
				if (((_a = this.sender.transport) === null || _a === void 0 ? void 0 : _a.state) !== "closed") yield this.sender.replaceTrack(null);
			} finally {
				unlock();
			}
		});
	}
	resumeUpstream() {
		return __awaiter(this, void 0, void 0, function* () {
			var _a;
			const unlock = yield this.pauseUpstreamLock.lock();
			try {
				if (this._isUpstreamPaused === false) return;
				if (!this.sender) {
					this.log.warn("unable to resume upstream for an unpublished track", this.logContext);
					return;
				}
				this._isUpstreamPaused = false;
				this.emit(TrackEvent.UpstreamResumed, this);
				if (((_a = this.sender.transport) === null || _a === void 0 ? void 0 : _a.state) !== "closed") yield this.sender.replaceTrack(this.mediaStreamTrack);
			} finally {
				unlock();
			}
		});
	}
	/**
	* Gets the RTCStatsReport for the LocalTrack's underlying RTCRtpSender
	* See https://developer.mozilla.org/en-US/docs/Web/API/RTCStatsReport
	*
	* @returns Promise<RTCStatsReport> | undefined
	*/
	getRTCStatsReport() {
		return __awaiter(this, void 0, void 0, function* () {
			var _a;
			if (!((_a = this.sender) === null || _a === void 0 ? void 0 : _a.getStats)) return;
			return yield this.sender.getStats();
		});
	}
	/**
	* Sets a processor on this track.
	* See https://github.com/livekit/track-processors-js for example usage
	*
	* @param processor
	* @param showProcessedStreamLocally
	* @returns
	*/
	setProcessor(processor_1) {
		return __awaiter(this, arguments, void 0, function(processor) {
			var _this3 = this;
			let showProcessedStreamLocally = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : true;
			return function* () {
				var _a;
				const unlock = yield _this3.trackChangeLock.lock();
				try {
					_this3.log.debug("setting up processor", _this3.logContext);
					const processorElement = document.createElement(_this3.kind);
					const processorOptions = {
						kind: _this3.kind,
						track: _this3._mediaStreamTrack,
						element: processorElement,
						audioContext: _this3.audioContext,
						localTrack: _this3
					};
					yield processor.init(processorOptions);
					_this3.log.debug("processor initialized", _this3.logContext);
					if (_this3.processor) yield _this3.internalStopProcessor();
					if (_this3.kind === "unknown") throw TypeError("cannot set processor on track of unknown kind");
					attachToElement(_this3._mediaStreamTrack, processorElement);
					processorElement.muted = true;
					processorElement.play().catch((error) => {
						if (error instanceof DOMException && error.name === "AbortError") {
							_this3.log.warn("failed to play processor element, retrying", Object.assign(Object.assign({}, _this3.logContext), { error }));
							setTimeout(() => {
								processorElement.play().catch((err) => {
									_this3.log.error("failed to play processor element", Object.assign(Object.assign({}, _this3.logContext), { err }));
								});
							}, 100);
						} else _this3.log.error("failed to play processor element", Object.assign(Object.assign({}, _this3.logContext), { error }));
					});
					_this3.processor = processor;
					_this3.processorElement = processorElement;
					if (_this3.processor.processedTrack) {
						for (const el of _this3.attachedElements) if (el !== _this3.processorElement && showProcessedStreamLocally) {
							detachTrack(_this3._mediaStreamTrack, el);
							attachToElement(_this3.processor.processedTrack, el);
						}
						yield (_a = _this3.sender) === null || _a === void 0 ? void 0 : _a.replaceTrack(_this3.processor.processedTrack);
					}
					_this3.emit(TrackEvent.TrackProcessorUpdate, _this3.processor);
				} finally {
					unlock();
				}
				yield _this3.onSenderTrackSwapped();
			}();
		});
	}
	getProcessor() {
		return this.processor;
	}
	/**
	* Stops the track processor
	* See https://github.com/livekit/track-processors-js for example usage
	*
	*/
	stopProcessor() {
		return __awaiter(this, arguments, void 0, function() {
			var _this4 = this;
			let keepElement = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : true;
			return function* () {
				const unlock = yield _this4.trackChangeLock.lock();
				try {
					yield _this4.internalStopProcessor(keepElement);
				} finally {
					unlock();
				}
				yield _this4.onSenderTrackSwapped();
			}();
		});
	}
	/**
	* @internal
	* This method assumes the caller has acquired a trackChangeLock already.
	* The public facing method for stopping the processor is `stopProcessor` and it wraps this method in the trackChangeLock.
	*/
	internalStopProcessor() {
		return __awaiter(this, arguments, void 0, function() {
			var _this5 = this;
			let keepElement = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : true;
			return function* () {
				var _a, _b;
				if (!_this5.processor) return;
				_this5.log.debug("stopping processor", _this5.logContext);
				(_a = _this5.processor.processedTrack) === null || _a === void 0 || _a.stop();
				yield _this5.processor.destroy();
				_this5.processor = void 0;
				if (!keepElement) {
					(_b = _this5.processorElement) === null || _b === void 0 || _b.remove();
					_this5.processorElement = void 0;
				}
				yield _this5._mediaStreamTrack.applyConstraints(_this5._constraints);
				yield _this5.setMediaStreamTrack(_this5._mediaStreamTrack, true);
				_this5.emit(TrackEvent.TrackProcessorUpdate);
			}();
		});
	}
	/** @internal */
	startPreConnectBuffer() {
		let timeslice = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 100;
		if (!isRecordingSupported()) {
			this.log.warn("MediaRecorder is not available, cannot start preconnect buffer", this.logContext);
			return;
		}
		if (!this.localTrackRecorder) {
			let mimeType = "audio/webm;codecs=opus";
			if (!MediaRecorder.isTypeSupported(mimeType)) mimeType = "video/mp4";
			this.localTrackRecorder = new LocalTrackRecorder(this, { mimeType });
		} else {
			this.log.warn("preconnect buffer already started");
			return;
		}
		this.localTrackRecorder.start(timeslice);
		this.autoStopPreConnectBuffer = setTimeout(() => {
			this.log.warn("preconnect buffer timed out, stopping recording automatically", this.logContext);
			this.stopPreConnectBuffer();
		}, PRE_CONNECT_BUFFER_TIMEOUT);
	}
	/** @internal */
	stopPreConnectBuffer() {
		clearTimeout(this.autoStopPreConnectBuffer);
		if (this.localTrackRecorder) {
			this.localTrackRecorder.stop();
			this.localTrackRecorder = void 0;
		}
	}
	/** @internal */
	getPreConnectBuffer() {
		var _a;
		return (_a = this.localTrackRecorder) === null || _a === void 0 ? void 0 : _a.byteStream;
	}
	getPreConnectBufferMimeType() {
		var _a;
		return (_a = this.localTrackRecorder) === null || _a === void 0 ? void 0 : _a.mimeType;
	}
};
var LocalAudioTrack = class extends LocalTrack {
	/**
	* boolean indicating whether enhanced noise cancellation is currently being used on this track
	*/
	get enhancedNoiseCancellation() {
		return this.isKrispNoiseFilterEnabled;
	}
	/**
	*
	* @param mediaTrack
	* @param constraints MediaTrackConstraints that are being used when restarting or reacquiring tracks
	* @param userProvidedTrack Signals to the SDK whether or not the mediaTrack should be managed (i.e. released and reacquired) internally by the SDK
	*/
	constructor(mediaTrack, constraints) {
		let userProvidedTrack = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : true;
		let audioContext = arguments.length > 3 ? arguments[3] : void 0;
		let loggerOptions = arguments.length > 4 ? arguments[4] : void 0;
		super(mediaTrack, Track.Kind.Audio, constraints, userProvidedTrack, loggerOptions);
		/** @internal */
		this.stopOnMute = false;
		this.isKrispNoiseFilterEnabled = false;
		this.monitorSender = () => __awaiter(this, void 0, void 0, function* () {
			if (!this.sender) {
				this._currentBitrate = 0;
				return;
			}
			let stats;
			try {
				stats = yield this.getSenderStats();
			} catch (e) {
				this.log.error("could not get audio sender stats", Object.assign(Object.assign({}, this.logContext), { error: e }));
				return;
			}
			if (stats && this.prevStats) this._currentBitrate = computeBitrate(stats, this.prevStats);
			this.prevStats = stats;
		});
		this.handleKrispNoiseFilterEnable = () => {
			this.isKrispNoiseFilterEnabled = true;
			this.log.debug("Krisp noise filter enabled", this.logContext);
			this.emit(TrackEvent.AudioTrackFeatureUpdate, this, AudioTrackFeature.TF_ENHANCED_NOISE_CANCELLATION, true);
		};
		this.handleKrispNoiseFilterDisable = () => {
			this.isKrispNoiseFilterEnabled = false;
			this.log.debug("Krisp noise filter disabled", this.logContext);
			this.emit(TrackEvent.AudioTrackFeatureUpdate, this, AudioTrackFeature.TF_ENHANCED_NOISE_CANCELLATION, false);
		};
		this.audioContext = audioContext;
		this.checkForSilence();
	}
	mute() {
		const _super = Object.create(null, { mute: { get: () => super.mute } });
		return __awaiter(this, void 0, void 0, function* () {
			const unlock = yield this.muteLock.lock();
			try {
				if (this.isMuted) {
					this.log.debug("Track already muted", this.logContext);
					return this;
				}
				if (this.source === Track.Source.Microphone && this.stopOnMute && !this.isUserProvided) {
					this.log.debug("stopping mic track", this.logContext);
					this._mediaStreamTrack.stop();
				}
				yield _super.mute.call(this);
				return this;
			} finally {
				unlock();
			}
		});
	}
	unmute() {
		const _super = Object.create(null, { unmute: { get: () => super.unmute } });
		return __awaiter(this, void 0, void 0, function* () {
			const unlock = yield this.muteLock.lock();
			try {
				if (!this.isMuted) {
					this.log.debug("Track already unmuted", this.logContext);
					return this;
				}
				if (this.source === Track.Source.Microphone && (this.stopOnMute || this._mediaStreamTrack.readyState === "ended" || this.pendingDeviceChange) && !this.isUserProvided) {
					this.log.debug("reacquiring mic track", this.logContext);
					yield this.restart(void 0, true);
				}
				yield _super.unmute.call(this);
				return this;
			} finally {
				unlock();
			}
		});
	}
	restartTrack(options) {
		return __awaiter(this, void 0, void 0, function* () {
			let constraints;
			if (options) {
				const streamConstraints = constraintsForOptions({ audio: options });
				if (typeof streamConstraints.audio !== "boolean") constraints = streamConstraints.audio;
			}
			yield this.restart(constraints);
		});
	}
	applyConstraints(constraints) {
		return __awaiter(this, void 0, void 0, function* () {
			const unlock = yield this.trackChangeLock.lock();
			try {
				const res = yield this._mediaStreamTrack.applyConstraints(constraints);
				this._constraints = Object.assign(Object.assign({}, this._constraints), constraints);
				return res;
			} finally {
				unlock();
			}
		});
	}
	restart(constraints, isUnmuting) {
		const _super = Object.create(null, { restart: { get: () => super.restart } });
		return __awaiter(this, void 0, void 0, function* () {
			const track = yield _super.restart.call(this, constraints, isUnmuting);
			this.checkForSilence();
			return track;
		});
	}
	startMonitor() {
		if (!isWeb()) return;
		if (this.monitorInterval) return;
		this.monitorInterval = setInterval(() => {
			this.monitorSender();
		}, monitorFrequency);
	}
	setProcessor(processor) {
		return __awaiter(this, void 0, void 0, function* () {
			var _a;
			const unlock = yield this.trackChangeLock.lock();
			try {
				if (!isReactNative() && !this.audioContext) throw Error("Audio context needs to be set on LocalAudioTrack in order to enable processors");
				if (this.processor) yield this.internalStopProcessor();
				const processorOptions = {
					kind: this.kind,
					track: this._mediaStreamTrack,
					audioContext: this.audioContext,
					localTrack: this
				};
				this.log.debug("setting up audio processor ".concat(processor.name), this.logContext);
				yield processor.init(processorOptions);
				this.processor = processor;
				if (this.processor.processedTrack) {
					yield (_a = this.sender) === null || _a === void 0 ? void 0 : _a.replaceTrack(this.processor.processedTrack);
					this.processor.processedTrack.addEventListener("enable-lk-krisp-noise-filter", this.handleKrispNoiseFilterEnable);
					this.processor.processedTrack.addEventListener("disable-lk-krisp-noise-filter", this.handleKrispNoiseFilterDisable);
				}
				this.emit(TrackEvent.TrackProcessorUpdate, this.processor);
			} finally {
				unlock();
			}
		});
	}
	/**
	* @internal
	* @experimental
	*/
	setAudioContext(audioContext) {
		this.audioContext = audioContext;
	}
	getSenderStats() {
		return __awaiter(this, void 0, void 0, function* () {
			var _a;
			if (!((_a = this.sender) === null || _a === void 0 ? void 0 : _a.getStats)) return;
			const stats = yield this.sender.getStats();
			let audioStats;
			stats.forEach((v) => {
				if (v.type === "outbound-rtp") audioStats = {
					type: "audio",
					streamId: v.id,
					packetsSent: v.packetsSent,
					packetsLost: v.packetsLost,
					bytesSent: v.bytesSent,
					timestamp: v.timestamp,
					roundTripTime: v.roundTripTime,
					jitter: v.jitter
				};
			});
			return audioStats;
		});
	}
	checkForSilence() {
		return __awaiter(this, void 0, void 0, function* () {
			const trackIsSilent = yield detectSilence(this);
			if (trackIsSilent) {
				if (!this.isMuted) this.log.debug("silence detected on local audio track", this.logContext);
				this.emit(TrackEvent.AudioSilenceDetected);
			}
			return trackIsSilent;
		});
	}
};
function mediaTrackToLocalTrack(mediaStreamTrack, constraints, loggerOptions) {
	switch (mediaStreamTrack.kind) {
		case "audio": return new LocalAudioTrack(mediaStreamTrack, constraints, false, void 0, loggerOptions);
		case "video": return new LocalVideoTrack(mediaStreamTrack, constraints, false, loggerOptions);
		default: throw new TrackInvalidError("unsupported track type: ".concat(mediaStreamTrack.kind));
	}
}
var presets169 = Object.values(VideoPresets);
var presets43 = Object.values(VideoPresets43);
var presetsScreenShare = Object.values(ScreenSharePresets);
var defaultSimulcastPresets169 = [VideoPresets.h180, VideoPresets.h360];
var defaultSimulcastPresets43 = [VideoPresets43.h180, VideoPresets43.h360];
var computeDefaultScreenShareSimulcastPresets = (fromPreset) => {
	return [{
		scaleResolutionDownBy: 2,
		fps: fromPreset.encoding.maxFramerate
	}].map((t) => {
		var _a, _b;
		return new VideoPreset(Math.floor(fromPreset.width / t.scaleResolutionDownBy), Math.floor(fromPreset.height / t.scaleResolutionDownBy), Math.max(15e4, Math.floor(fromPreset.encoding.maxBitrate / (Math.pow(t.scaleResolutionDownBy, 2) * (((_a = fromPreset.encoding.maxFramerate) !== null && _a !== void 0 ? _a : 30) / ((_b = t.fps) !== null && _b !== void 0 ? _b : 30))))), t.fps, fromPreset.encoding.priority);
	});
};
var videoRids = [
	"q",
	"h",
	"f"
];
function computeVideoEncodings(isScreenShare, width, height, options) {
	var _a, _b;
	let videoEncoding = options === null || options === void 0 ? void 0 : options.videoEncoding;
	if (isScreenShare) videoEncoding = options === null || options === void 0 ? void 0 : options.screenShareEncoding;
	const useSimulcast = options === null || options === void 0 ? void 0 : options.simulcast;
	const scalabilityMode = options === null || options === void 0 ? void 0 : options.scalabilityMode;
	const videoCodec = options === null || options === void 0 ? void 0 : options.videoCodec;
	if (!videoEncoding && !useSimulcast && !scalabilityMode || !width || !height) return [{}];
	if (!videoEncoding) {
		videoEncoding = determineAppropriateEncoding(isScreenShare, width, height, videoCodec);
		livekitLogger.debug("using video encoding", videoEncoding);
	}
	const sourceFramerate = videoEncoding.maxFramerate;
	const original = new VideoPreset(width, height, videoEncoding.maxBitrate, videoEncoding.maxFramerate, videoEncoding.priority);
	if (scalabilityMode && isSVCCodec(videoCodec)) {
		const sm = new ScalabilityMode(scalabilityMode);
		const encodings = [];
		if (sm.spatial > 3) throw new Error("unsupported scalabilityMode: ".concat(scalabilityMode));
		const browser = getBrowser();
		if (isSafariBased() || isReactNative() || (browser === null || browser === void 0 ? void 0 : browser.name) === "Chrome" && compareVersions(browser === null || browser === void 0 ? void 0 : browser.version, "113") < 0) {
			const bitratesRatio = sm.suffix == "h" ? 2 : 3;
			const requireScale = isSafariSvcApi(browser);
			for (let i = 0; i < sm.spatial; i += 1) encodings.push({
				rid: videoRids[2 - i],
				maxBitrate: videoEncoding.maxBitrate / Math.pow(bitratesRatio, i),
				maxFramerate: original.encoding.maxFramerate,
				scaleResolutionDownBy: requireScale ? Math.pow(2, i) : void 0
			});
			encodings[0].scalabilityMode = scalabilityMode;
		} else encodings.push({
			maxBitrate: videoEncoding.maxBitrate,
			maxFramerate: original.encoding.maxFramerate,
			scalabilityMode
		});
		if (original.encoding.priority) {
			encodings[0].priority = original.encoding.priority;
			encodings[0].networkPriority = original.encoding.priority;
		}
		livekitLogger.debug("using svc encoding", { encodings });
		return encodings;
	}
	if (!useSimulcast) return [videoEncoding];
	let presets;
	if (isScreenShare) presets = (_a = sortPresets(options === null || options === void 0 ? void 0 : options.screenShareSimulcastLayers)) !== null && _a !== void 0 ? _a : defaultSimulcastLayers(isScreenShare, original);
	else presets = (_b = sortPresets(options === null || options === void 0 ? void 0 : options.videoSimulcastLayers)) !== null && _b !== void 0 ? _b : defaultSimulcastLayers(isScreenShare, original);
	let midPreset;
	if (presets.length > 0) {
		const lowPreset = presets[0];
		if (presets.length > 1) midPreset = _slicedToArray(presets, 2)[1];
		const size = Math.max(width, height);
		if (size >= 960 && midPreset) return encodingsFromPresets(width, height, [
			lowPreset,
			midPreset,
			original
		], sourceFramerate);
		if (size >= 480) return encodingsFromPresets(width, height, [lowPreset, original], sourceFramerate);
	}
	return encodingsFromPresets(width, height, [original]);
}
function computeTrackBackupEncodings(track, videoCodec, opts) {
	var _a, _b, _c, _d;
	if (!opts.backupCodec || opts.backupCodec === true || opts.backupCodec.codec === opts.videoCodec) return;
	if (videoCodec !== opts.backupCodec.codec) livekitLogger.warn("requested a different codec than specified as backup", {
		serverRequested: videoCodec,
		backup: opts.backupCodec.codec
	});
	opts.videoCodec = videoCodec;
	opts.videoEncoding = opts.backupCodec.encoding;
	const settings = track.mediaStreamTrack.getSettings();
	const width = (_a = settings.width) !== null && _a !== void 0 ? _a : (_b = track.dimensions) === null || _b === void 0 ? void 0 : _b.width;
	const height = (_c = settings.height) !== null && _c !== void 0 ? _c : (_d = track.dimensions) === null || _d === void 0 ? void 0 : _d.height;
	if (track.source === Track.Source.ScreenShare && opts.simulcast) opts.simulcast = false;
	return computeVideoEncodings(track.source === Track.Source.ScreenShare, width, height, opts);
}
function determineAppropriateEncoding(isScreenShare, width, height, codec) {
	const presets = presetsForResolution(isScreenShare, width, height);
	let encoding = presets[0].encoding;
	const size = Math.max(width, height);
	for (let i = 0; i < presets.length; i += 1) {
		const preset = presets[i];
		encoding = preset.encoding;
		if (preset.width >= size) break;
	}
	if (codec) switch (codec) {
		case "av1":
		case "h265":
			encoding = Object.assign({}, encoding);
			encoding.maxBitrate = encoding.maxBitrate * .7;
			break;
		case "vp9":
			encoding = Object.assign({}, encoding);
			encoding.maxBitrate = encoding.maxBitrate * .85;
	}
	return encoding;
}
function presetsForResolution(isScreenShare, width, height) {
	if (isScreenShare) return presetsScreenShare;
	const aspect = width > height ? width / height : height / width;
	if (Math.abs(aspect - 16 / 9) < Math.abs(aspect - 4 / 3)) return presets169;
	return presets43;
}
function defaultSimulcastLayers(isScreenShare, original) {
	if (isScreenShare) return computeDefaultScreenShareSimulcastPresets(original);
	const width = original.width, height = original.height;
	const aspect = width > height ? width / height : height / width;
	if (Math.abs(aspect - 16 / 9) < Math.abs(aspect - 4 / 3)) return defaultSimulcastPresets169;
	return defaultSimulcastPresets43;
}
function encodingsFromPresets(width, height, presets, sourceFramerate) {
	const encodings = [];
	presets.forEach((preset, idx) => {
		if (idx >= videoRids.length) return;
		const size = Math.min(width, height);
		const encoding = {
			rid: videoRids[idx],
			scaleResolutionDownBy: Math.max(1, size / Math.min(preset.width, preset.height)),
			maxBitrate: preset.encoding.maxBitrate
		};
		const maxFramerate = sourceFramerate && preset.encoding.maxFramerate ? Math.min(sourceFramerate, preset.encoding.maxFramerate) : preset.encoding.maxFramerate;
		if (maxFramerate) encoding.maxFramerate = maxFramerate;
		const browser = getBrowser();
		const canSetPriority = (browser === null || browser === void 0 ? void 0 : browser.name) === "Firefox" && browser.os !== "iOS" || idx === 0;
		if (preset.encoding.priority && canSetPriority) {
			encoding.priority = preset.encoding.priority;
			encoding.networkPriority = preset.encoding.priority;
		}
		encodings.push(encoding);
	});
	if (isReactNative() && getReactNativeOs() === "ios") {
		let topFramerate = void 0;
		encodings.forEach((encoding) => {
			if (!topFramerate) topFramerate = encoding.maxFramerate;
			else if (encoding.maxFramerate && encoding.maxFramerate > topFramerate) topFramerate = encoding.maxFramerate;
		});
		let notifyOnce = true;
		encodings.forEach((encoding) => {
			var _a;
			if (encoding.maxFramerate != topFramerate) {
				if (notifyOnce) {
					notifyOnce = false;
					livekitLogger.info("Simulcast on iOS React-Native requires all encodings to share the same framerate.");
				}
				livekitLogger.info("Setting framerate of encoding \"".concat((_a = encoding.rid) !== null && _a !== void 0 ? _a : "", "\" to ").concat(topFramerate));
				encoding.maxFramerate = topFramerate;
			}
		});
	}
	return encodings;
}
/** @internal */
function sortPresets(presets) {
	if (!presets) return;
	return presets.slice().sort((a, b) => {
		const aEnc = a.encoding;
		const bEnc = b.encoding;
		if (aEnc.maxBitrate > bEnc.maxBitrate) return 1;
		if (aEnc.maxBitrate < bEnc.maxBitrate) return -1;
		if (aEnc.maxBitrate === bEnc.maxBitrate && aEnc.maxFramerate && bEnc.maxFramerate) return aEnc.maxFramerate > bEnc.maxFramerate ? 1 : -1;
		return 0;
	});
}
/** @internal */
var ScalabilityMode = class {
	constructor(scalabilityMode) {
		const results = scalabilityMode.match(/^L(\d)T(\d)(h|_KEY|_KEY_SHIFT){0,1}$/);
		if (!results) throw new Error("invalid scalability mode");
		this.spatial = parseInt(results[1]);
		this.temporal = parseInt(results[2]);
		if (results.length > 3) switch (results[3]) {
			case "h":
			case "_KEY":
			case "_KEY_SHIFT": this.suffix = results[3];
		}
	}
	toString() {
		var _a;
		return "L".concat(this.spatial, "T").concat(this.temporal).concat((_a = this.suffix) !== null && _a !== void 0 ? _a : "");
	}
};
/**
* Returns the appropriate degradation preference for a video track based on its source.
*
* - Camera: 'maintain-framerate' (smoother video for real-time communication)
* - Screen share: 'maintain-resolution' (clarity is critical for reading text/UI)
* - Other/unknown: 'balanced'
*/
function getDefaultDegradationPreference(track) {
	switch (track.source) {
		case Track.Source.Camera: return "maintain-framerate";
		case Track.Source.ScreenShare: return "maintain-resolution";
		default: return "balanced";
	}
}
var refreshSubscribedCodecAfterNewCodec = 5e3;
var LocalVideoTrack = class extends LocalTrack {
	get sender() {
		return this._sender;
	}
	set sender(sender) {
		this._sender = sender;
		if (this.degradationPreference) this.setDegradationPreference(this.degradationPreference);
	}
	/**
	*
	* @param mediaTrack
	* @param constraints MediaTrackConstraints that are being used when restarting or reacquiring tracks
	* @param userProvidedTrack Signals to the SDK whether or not the mediaTrack should be managed (i.e. released and reacquired) internally by the SDK
	*/
	constructor(mediaTrack, constraints) {
		let userProvidedTrack = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : true;
		let loggerOptions = arguments.length > 3 ? arguments[3] : void 0;
		super(mediaTrack, Track.Kind.Video, constraints, userProvidedTrack, loggerOptions);
		this.simulcastCodecs = /* @__PURE__ */ new Map();
		this.degradationPreference = "balanced";
		this.isCpuConstrained = false;
		this.optimizeForPerformance = false;
		this.monitorSender = () => __awaiter(this, void 0, void 0, function* () {
			if (!this.sender) {
				this._currentBitrate = 0;
				return;
			}
			let stats;
			try {
				stats = yield this.getSenderStats();
			} catch (e) {
				this.log.error("could not get video sender stats", Object.assign(Object.assign({}, this.logContext), { error: e }));
				return;
			}
			const statsMap = new Map(stats.map((s) => [s.rid, s]));
			const isCpuConstrained = stats.some((s) => s.qualityLimitationReason === "cpu");
			if (isCpuConstrained !== this.isCpuConstrained) {
				this.isCpuConstrained = isCpuConstrained;
				if (this.isCpuConstrained) this.emit(TrackEvent.CpuConstrained);
			}
			if (this.prevStats) {
				let totalBitrate = 0;
				statsMap.forEach((s, key) => {
					var _a;
					const prev = (_a = this.prevStats) === null || _a === void 0 ? void 0 : _a.get(key);
					totalBitrate += computeBitrate(s, prev);
				});
				this._currentBitrate = totalBitrate;
			}
			this.prevStats = statsMap;
		});
		this.senderLock = new _();
	}
	get isSimulcast() {
		if (this.sender && this.sender.getParameters().encodings.length > 1) return true;
		return false;
	}
	startMonitor(signalClient) {
		var _a;
		this.signalClient = signalClient;
		if (!isWeb()) return;
		const params = (_a = this.sender) === null || _a === void 0 ? void 0 : _a.getParameters();
		if (params) this.encodings = params.encodings;
		if (this.monitorInterval) return;
		this.monitorInterval = setInterval(() => {
			this.monitorSender();
		}, monitorFrequency);
	}
	stop() {
		this._mediaStreamTrack.getConstraints();
		this.simulcastCodecs.forEach((trackInfo) => {
			trackInfo.mediaStreamTrack.stop();
		});
		super.stop();
	}
	pauseUpstream() {
		const _super = Object.create(null, { pauseUpstream: { get: () => super.pauseUpstream } });
		return __awaiter(this, void 0, void 0, function* () {
			var _a, e_1, _b, _c;
			var _d;
			yield _super.pauseUpstream.call(this);
			try {
				for (var _e = true, _f = __asyncValues(this.simulcastCodecs.values()), _g; _g = yield _f.next(), _a = _g.done, !_a; _e = true) {
					_c = _g.value;
					_e = false;
					yield (_d = _c.sender) === null || _d === void 0 ? void 0 : _d.replaceTrack(null);
				}
			} catch (e_1_1) {
				e_1 = { error: e_1_1 };
			} finally {
				try {
					if (!_e && !_a && (_b = _f.return)) yield _b.call(_f);
				} finally {
					if (e_1) throw e_1.error;
				}
			}
		});
	}
	resumeUpstream() {
		const _super = Object.create(null, { resumeUpstream: { get: () => super.resumeUpstream } });
		return __awaiter(this, void 0, void 0, function* () {
			var _a, e_2, _b, _c;
			var _d;
			yield _super.resumeUpstream.call(this);
			try {
				for (var _e = true, _f = __asyncValues(this.simulcastCodecs.values()), _g; _g = yield _f.next(), _a = _g.done, !_a; _e = true) {
					_c = _g.value;
					_e = false;
					const sc = _c;
					yield (_d = sc.sender) === null || _d === void 0 ? void 0 : _d.replaceTrack(sc.mediaStreamTrack);
				}
			} catch (e_2_1) {
				e_2 = { error: e_2_1 };
			} finally {
				try {
					if (!_e && !_a && (_b = _f.return)) yield _b.call(_f);
				} finally {
					if (e_2) throw e_2.error;
				}
			}
		});
	}
	mute() {
		const _super = Object.create(null, { mute: { get: () => super.mute } });
		return __awaiter(this, void 0, void 0, function* () {
			const unlock = yield this.muteLock.lock();
			try {
				if (this.isMuted) {
					this.log.debug("Track already muted", this.logContext);
					return this;
				}
				if (this.source === Track.Source.Camera && !this.isUserProvided) {
					this.log.debug("stopping camera track", this.logContext);
					this._mediaStreamTrack.stop();
				}
				yield _super.mute.call(this);
				return this;
			} finally {
				unlock();
			}
		});
	}
	unmute() {
		const _super = Object.create(null, { unmute: { get: () => super.unmute } });
		return __awaiter(this, void 0, void 0, function* () {
			const unlock = yield this.muteLock.lock();
			try {
				if (!this.isMuted) {
					this.log.debug("Track already unmuted", this.logContext);
					return this;
				}
				if (this.source === Track.Source.Camera && !this.isUserProvided) {
					this.log.debug("reacquiring camera track", this.logContext);
					yield this.restart(void 0, true);
				}
				yield _super.unmute.call(this);
				return this;
			} finally {
				unlock();
			}
		});
	}
	setTrackMuted(muted) {
		super.setTrackMuted(muted);
		for (const sc of this.simulcastCodecs.values()) sc.mediaStreamTrack.enabled = !muted;
	}
	getSenderStats() {
		return __awaiter(this, void 0, void 0, function* () {
			var _a;
			if (!((_a = this.sender) === null || _a === void 0 ? void 0 : _a.getStats)) return [];
			const items = [];
			const stats = yield this.sender.getStats();
			stats.forEach((v) => {
				var _a;
				if (v.type === "outbound-rtp") {
					const vs = {
						type: "video",
						streamId: v.id,
						frameHeight: v.frameHeight,
						frameWidth: v.frameWidth,
						framesPerSecond: v.framesPerSecond,
						framesSent: v.framesSent,
						firCount: v.firCount,
						pliCount: v.pliCount,
						nackCount: v.nackCount,
						packetsSent: v.packetsSent,
						bytesSent: v.bytesSent,
						qualityLimitationReason: v.qualityLimitationReason,
						qualityLimitationDurations: v.qualityLimitationDurations,
						qualityLimitationResolutionChanges: v.qualityLimitationResolutionChanges,
						rid: (_a = v.rid) !== null && _a !== void 0 ? _a : v.id,
						retransmittedPacketsSent: v.retransmittedPacketsSent,
						targetBitrate: v.targetBitrate,
						timestamp: v.timestamp
					};
					const r = stats.get(v.remoteId);
					if (r) {
						vs.jitter = r.jitter;
						vs.packetsLost = r.packetsLost;
						vs.roundTripTime = r.roundTripTime;
					}
					items.push(vs);
				}
			});
			items.sort((a, b) => {
				var _a, _b;
				return ((_a = b.frameWidth) !== null && _a !== void 0 ? _a : 0) - ((_b = a.frameWidth) !== null && _b !== void 0 ? _b : 0);
			});
			return items;
		});
	}
	setPublishingQuality(maxQuality) {
		const qualities = [];
		for (let q = VideoQuality.LOW; q <= VideoQuality.HIGH; q += 1) qualities.push(new SubscribedQuality({
			quality: q,
			enabled: q <= maxQuality
		}));
		this.log.debug("setting publishing quality. max quality ".concat(maxQuality), this.logContext);
		this.setPublishingLayers(isSVCCodec(this.codec), qualities);
	}
	restartTrack(options) {
		return __awaiter(this, void 0, void 0, function* () {
			var _a, e_3, _b, _c;
			var _d;
			let constraints;
			if (options) {
				const streamConstraints = constraintsForOptions({ video: options });
				if (typeof streamConstraints.video !== "boolean") constraints = streamConstraints.video;
			}
			yield this.restart(constraints);
			this.isCpuConstrained = false;
			try {
				for (var _e = true, _f = __asyncValues(this.simulcastCodecs.values()), _g; _g = yield _f.next(), _a = _g.done, !_a; _e = true) {
					_c = _g.value;
					_e = false;
					const sc = _c;
					if (sc.sender && ((_d = sc.sender.transport) === null || _d === void 0 ? void 0 : _d.state) !== "closed") {
						sc.mediaStreamTrack = this.mediaStreamTrack.clone();
						yield sc.sender.replaceTrack(sc.mediaStreamTrack);
					}
				}
			} catch (e_3_1) {
				e_3 = { error: e_3_1 };
			} finally {
				try {
					if (!_e && !_a && (_b = _f.return)) yield _b.call(_f);
				} finally {
					if (e_3) throw e_3.error;
				}
			}
			yield this.onSenderTrackSwapped();
		});
	}
	onSenderTrackSwapped() {
		return __awaiter(this, void 0, void 0, function* () {
			yield this.refreshSenderEncodings();
		});
	}
	/**
	* Recomputes encoding parameters for this track's senders based on the current
	* MediaStreamTrack dimensions and reapplies them via setParameters. This is a no-op
	* if the track hasn't been published yet or if the track is in performance-optimized
	* mode (which manages its own encodings).
	*/
	refreshSenderEncodings() {
		return __awaiter(this, void 0, void 0, function* () {
			var _a;
			if (!this.sender || !this.publishOptions || this.optimizeForPerformance) return;
			const unlock = yield this.senderLock.lock();
			try {
				let dims;
				try {
					dims = yield this.waitForDimensions();
				} catch (e) {
					this.log.warn("could not determine new track dimensions, skipping encoding recompute", Object.assign(Object.assign({}, this.logContext), { error: e }));
					return;
				}
				if (this.lastEncodedDimensions && this.lastEncodedDimensions.width === dims.width && this.lastEncodedDimensions.height === dims.height) return;
				const newEncodings = computeVideoEncodings(this.source === Track.Source.ScreenShare, dims.width, dims.height, Object.assign({}, this.publishOptions));
				yield this.applyEncodingsToSender(this.sender, newEncodings);
				this.encodings = newEncodings;
				this.lastEncodedDimensions = dims;
				for (const _ref of this.simulcastCodecs) {
					var _ref2 = _slicedToArray(_ref, 2);
					const codec = _ref2[0];
					const sc = _ref2[1];
					if (!sc.sender || ((_a = sc.sender.transport) === null || _a === void 0 ? void 0 : _a.state) === "closed") continue;
					if (!isBackupVideoCodec(codec)) continue;
					const backupOpts = Object.assign({}, this.publishOptions);
					const backupEncodings = computeTrackBackupEncodings(this, codec, backupOpts);
					if (!backupEncodings) continue;
					yield this.applyEncodingsToSender(sc.sender, backupEncodings);
					sc.encodings = backupEncodings;
				}
			} catch (e) {
				this.log.warn("failed to apply recomputed encodings", Object.assign(Object.assign({}, this.logContext), { error: e }));
			} finally {
				unlock();
			}
		});
	}
	applyEncodingsToSender(sender, encodings) {
		return __awaiter(this, void 0, void 0, function* () {
			const params = sender.getParameters();
			if (!params.encodings || params.encodings.length !== encodings.length) return;
			params.encodings.forEach((existing, idx) => {
				if (existing.active === false) return;
				const next = encodings[idx];
				if (next.scaleResolutionDownBy !== void 0) existing.scaleResolutionDownBy = next.scaleResolutionDownBy;
				if (next.maxBitrate !== void 0) existing.maxBitrate = next.maxBitrate;
				if (next.maxFramerate !== void 0) existing.maxFramerate = next.maxFramerate;
				if (next.priority !== void 0) {
					existing.priority = next.priority;
					existing.networkPriority = next.priority;
				}
			});
			this.log.debug("updating sender encodings after track restart", Object.assign(Object.assign({}, this.logContext), { encodings: params.encodings }));
			yield sender.setParameters(params);
		});
	}
	setProcessor(processor_1) {
		const _super = Object.create(null, { setProcessor: { get: () => super.setProcessor } });
		return __awaiter(this, arguments, void 0, function(processor) {
			var _this = this;
			let showProcessedStreamLocally = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : true;
			return function* () {
				var _a, e_4, _b, _c;
				var _d, _e;
				yield _super.setProcessor.call(_this, processor, showProcessedStreamLocally);
				if ((_d = _this.processor) === null || _d === void 0 ? void 0 : _d.processedTrack) try {
					for (var _f = true, _g = __asyncValues(_this.simulcastCodecs.values()), _h; _h = yield _g.next(), _a = _h.done, !_a; _f = true) {
						_c = _h.value;
						_f = false;
						yield (_e = _c.sender) === null || _e === void 0 ? void 0 : _e.replaceTrack(_this.processor.processedTrack);
					}
				} catch (e_4_1) {
					e_4 = { error: e_4_1 };
				} finally {
					try {
						if (!_f && !_a && (_b = _g.return)) yield _b.call(_g);
					} finally {
						if (e_4) throw e_4.error;
					}
				}
			}();
		});
	}
	setDegradationPreference(preference) {
		return __awaiter(this, void 0, void 0, function* () {
			this.degradationPreference = preference;
			yield this.applyDegradationPreference(this.sender);
			for (const sc of this.simulcastCodecs.values()) yield this.applyDegradationPreference(sc.sender);
		});
	}
	/**
	* Degradation preference is a property of the sender, not of the track, so every sender
	* publishing this track needs it applied separately. A backup codec publishes over its
	* own sender, which would otherwise let the browser resolve a preference implicitly and
	* diverge from the primary encoder.
	*
	* Callers apply this sequentially rather than concurrently: `setParameters` is only valid
	* against the parameters most recently returned by `getParameters`, which is why this file
	* serializes other sender parameter updates through `senderLock`.
	*/
	applyDegradationPreference(sender) {
		return __awaiter(this, void 0, void 0, function* () {
			if (!sender) return;
			try {
				this.log.debug("setting degradationPreference to ".concat(this.degradationPreference), this.logContext);
				const params = sender.getParameters();
				params.degradationPreference = this.degradationPreference;
				yield sender.setParameters(params);
			} catch (e) {
				this.log.warn("failed to set degradationPreference", Object.assign({ error: e }, this.logContext));
			}
		});
	}
	addSimulcastTrack(codec, encodings) {
		if (this.simulcastCodecs.has(codec)) {
			this.log.error("".concat(codec, " already added, skipping adding simulcast codec"), this.logContext);
			return;
		}
		const simulcastCodecInfo = {
			codec,
			mediaStreamTrack: this.mediaStreamTrack.clone(),
			sender: void 0,
			encodings
		};
		this.simulcastCodecs.set(codec, simulcastCodecInfo);
		return simulcastCodecInfo;
	}
	setSimulcastTrackSender(codec, sender) {
		return __awaiter(this, void 0, void 0, function* () {
			const simulcastCodecInfo = this.simulcastCodecs.get(codec);
			if (!simulcastCodecInfo) return;
			simulcastCodecInfo.sender = sender;
			yield this.applyDegradationPreference(sender);
			setTimeout(() => {
				if (this.subscribedCodecs) this.setPublishingCodecs(this.subscribedCodecs);
			}, refreshSubscribedCodecAfterNewCodec);
		});
	}
	/**
	* @internal
	* Sets codecs that should be publishing, returns new codecs that have not yet
	* been published
	*/
	setPublishingCodecs(codecs) {
		return __awaiter(this, void 0, void 0, function* () {
			var _a, codecs_1, codecs_1_1;
			var _b, e_5, _c, _d;
			this.log.debug("setting publishing codecs", Object.assign(Object.assign({}, this.logContext), {
				codecs,
				currentCodec: this.codec
			}));
			if (!this.codec && codecs.length > 0) {
				yield this.setPublishingLayers(isSVCCodec(codecs[0].codec), codecs[0].qualities);
				return [];
			}
			this.subscribedCodecs = codecs;
			const newCodecs = [];
			try {
				for (_a = true, codecs_1 = __asyncValues(codecs); codecs_1_1 = yield codecs_1.next(), _b = codecs_1_1.done, !_b; _a = true) {
					_d = codecs_1_1.value;
					_a = false;
					const codec = _d;
					if (!this.codec || this.codec === codec.codec) yield this.setPublishingLayers(isSVCCodec(codec.codec), codec.qualities);
					else {
						const simulcastCodecInfo = this.simulcastCodecs.get(codec.codec);
						this.log.debug("try setPublishingCodec for ".concat(codec.codec), Object.assign(Object.assign({}, this.logContext), { simulcastCodecInfo }));
						if (!simulcastCodecInfo || !simulcastCodecInfo.sender) {
							for (const q of codec.qualities) if (q.enabled) {
								newCodecs.push(codec.codec);
								break;
							}
						} else if (simulcastCodecInfo.encodings) {
							this.log.debug("try setPublishingLayersForSender ".concat(codec.codec), this.logContext);
							yield setPublishingLayersForSender(simulcastCodecInfo.sender, simulcastCodecInfo.encodings, codec.qualities, this.senderLock, isSVCCodec(codec.codec), this.log, this.logContext);
						}
					}
				}
			} catch (e_5_1) {
				e_5 = { error: e_5_1 };
			} finally {
				try {
					if (!_a && !_b && (_c = codecs_1.return)) yield _c.call(codecs_1);
				} finally {
					if (e_5) throw e_5.error;
				}
			}
			return newCodecs;
		});
	}
	/**
	* @internal
	* Sets layers that should be publishing
	*/
	setPublishingLayers(isSvc, qualities) {
		return __awaiter(this, void 0, void 0, function* () {
			if (this.optimizeForPerformance) {
				this.log.info("skipping setPublishingLayers due to optimized publishing performance", Object.assign(Object.assign({}, this.logContext), { qualities }));
				return;
			}
			this.log.debug("setting publishing layers", Object.assign(Object.assign({}, this.logContext), { qualities }));
			if (!this.sender || !this.encodings) return;
			yield setPublishingLayersForSender(this.sender, this.encodings, qualities, this.senderLock, isSvc, this.log, this.logContext);
		});
	}
	/**
	* Designed for lower powered devices, reduces video publishing quality and disables simulcast.
	* @experimental
	*/
	prioritizePerformance() {
		return __awaiter(this, void 0, void 0, function* () {
			if (!this.sender) throw new Error("sender not found");
			const unlock = yield this.senderLock.lock();
			try {
				this.optimizeForPerformance = true;
				const params = this.sender.getParameters();
				params.encodings = params.encodings.map((e, idx) => {
					var _a;
					return Object.assign(Object.assign({}, e), {
						active: idx === 0,
						scaleResolutionDownBy: Math.max(1, Math.ceil(((_a = this.mediaStreamTrack.getSettings().height) !== null && _a !== void 0 ? _a : 360) / 360)),
						scalabilityMode: idx === 0 && isSVCCodec(this.codec) ? "L1T3" : void 0,
						maxFramerate: idx === 0 ? 15 : 0,
						maxBitrate: idx === 0 ? e.maxBitrate : 0
					});
				});
				this.log.debug("setting performance optimised encodings", Object.assign(Object.assign({}, this.logContext), { encodings: params.encodings }));
				this.encodings = params.encodings;
				yield this.sender.setParameters(params);
			} catch (e) {
				this.log.error("failed to set performance optimised encodings", Object.assign(Object.assign({}, this.logContext), { error: e }));
				this.optimizeForPerformance = false;
			} finally {
				unlock();
			}
		});
	}
	handleAppVisibilityChanged() {
		const _super = Object.create(null, { handleAppVisibilityChanged: { get: () => super.handleAppVisibilityChanged } });
		return __awaiter(this, void 0, void 0, function* () {
			yield _super.handleAppVisibilityChanged.call(this);
			if (!isMobile()) return;
			if (this.isInBackground && this.source === Track.Source.Camera) this._mediaStreamTrack.enabled = false;
		});
	}
};
function setPublishingLayersForSender(sender, senderEncodings, qualities, senderLock, isSVC, log, logContext) {
	return __awaiter(this, void 0, void 0, function* () {
		const unlock = yield senderLock.lock();
		log.debug("setPublishingLayersForSender", Object.assign(Object.assign({}, logContext), {
			sender,
			qualities,
			senderEncodings
		}));
		try {
			const params = sender.getParameters();
			const encodings = params.encodings;
			if (!encodings) return;
			if (encodings.length !== senderEncodings.length) {
				log.warn("cannot set publishing layers, encodings mismatch", Object.assign(Object.assign({}, logContext), {
					encodings,
					senderEncodings
				}));
				return;
			}
			let hasChanged = false;
			if (isSVC) {
				if (qualities.some((q) => q.enabled)) qualities.forEach((q) => q.enabled = true);
			}
			encodings.forEach((encoding, idx) => {
				var _a;
				let rid = (_a = encoding.rid) !== null && _a !== void 0 ? _a : "";
				if (rid === "") rid = "q";
				const quality = videoQualityForRid(rid);
				const subscribedQuality = qualities.find((q) => q.quality === quality);
				if (!subscribedQuality) return;
				if (encoding.active !== subscribedQuality.enabled) {
					hasChanged = true;
					encoding.active = subscribedQuality.enabled;
					log.debug("setting layer ".concat(subscribedQuality.quality, " to ").concat(encoding.active ? "enabled" : "disabled"), logContext);
					if (isFireFox()) {
						if (subscribedQuality.enabled) {
							encoding.scaleResolutionDownBy = senderEncodings[idx].scaleResolutionDownBy;
							encoding.maxBitrate = senderEncodings[idx].maxBitrate;
							encoding.maxFrameRate = senderEncodings[idx].maxFrameRate;
						} else {
							encoding.scaleResolutionDownBy = 4;
							encoding.maxBitrate = 10;
							encoding.maxFrameRate = 2;
						}
					}
				}
			});
			if (hasChanged) {
				params.encodings = encodings;
				log.debug("setting encodings", Object.assign(Object.assign({}, logContext), { encodings: params.encodings }));
				yield sender.setParameters(params);
			}
		} finally {
			unlock();
		}
	});
}
function videoQualityForRid(rid) {
	switch (rid) {
		case "f": return VideoQuality.HIGH;
		case "h": return VideoQuality.MEDIUM;
		case "q": return VideoQuality.LOW;
		default: return VideoQuality.HIGH;
	}
}
function videoLayersFromEncodings(width, height, encodings, svc) {
	if (!encodings) return [new VideoLayer({
		quality: VideoQuality.HIGH,
		width,
		height,
		bitrate: 0,
		ssrc: 0
	})];
	if (svc) {
		const encodingSM = encodings[0].scalabilityMode;
		const sm = new ScalabilityMode(encodingSM);
		const layers = [];
		const resRatio = sm.suffix == "h" ? 1.5 : 2;
		const bitratesRatio = sm.suffix == "h" ? 2 : 3;
		for (let i = 0; i < sm.spatial; i += 1) layers.push(new VideoLayer({
			quality: Math.min(VideoQuality.HIGH, sm.spatial - 1) - i,
			width: Math.ceil(width / Math.pow(resRatio, i)),
			height: Math.ceil(height / Math.pow(resRatio, i)),
			bitrate: encodings[0].maxBitrate ? Math.ceil(encodings[0].maxBitrate / Math.pow(bitratesRatio, i)) : 0,
			ssrc: 0
		}));
		return layers;
	}
	return encodings.map((encoding) => {
		var _a, _b, _c;
		const scale = (_a = encoding.scaleResolutionDownBy) !== null && _a !== void 0 ? _a : 1;
		return new VideoLayer({
			quality: videoQualityForRid((_b = encoding.rid) !== null && _b !== void 0 ? _b : ""),
			width: Math.ceil(width / scale),
			height: Math.ceil(height / scale),
			bitrate: (_c = encoding.maxBitrate) !== null && _c !== void 0 ? _c : 0,
			ssrc: 0
		});
	});
}
var minReconnectWait = 2e3;
var leaveReconnect = "leave-reconnect";
/**
* How long local connection quality must stay `LOST` while connected and publishing before we
* force a full reconnect — `LOST` is the server's verdict that it isn't receiving our media.
*/
var connectionQualityLostTimeout = 1e4;
var reliabeReceiveStateTTL = 3e4;
var initialMediaSectionsAudio = 3;
var initialMediaSectionsVideo = 3;
var PCState;
(function(PCState) {
	PCState[PCState["New"] = 0] = "New";
	PCState[PCState["Connected"] = 1] = "Connected";
	PCState[PCState["Disconnected"] = 2] = "Disconnected";
	PCState[PCState["Reconnecting"] = 3] = "Reconnecting";
	PCState[PCState["Closed"] = 4] = "Closed";
})(PCState || (PCState = {}));
var DEFAULT_MAX_MESSAGE_SIZE = 64e3;
/** @internal */
var RTCEngine = class extends eventsExports.EventEmitter {
	get isClosed() {
		return this._isClosed;
	}
	get isNewlyCreated() {
		return this._isNewlyCreated;
	}
	get pendingReconnect() {
		return !!this.reconnectTimeout;
	}
	get reliableChannel() {
		return this.dataChannels.reliable;
	}
	get lossyChannel() {
		return this.dataChannels.lossy;
	}
	get dataTrackChannel() {
		return this.dataChannels.dataTrack;
	}
	constructor(options) {
		var _a;
		super();
		this.options = options;
		this.rtcConfig = {};
		this.peerConnectionTimeout = roomConnectOptionDefaults.peerConnectionTimeout;
		this.fullReconnectOnNext = false;
		/**
		* @internal
		*/
		this.latestRemoteOfferId = 0;
		this.subscriberPrimary = false;
		this.pcState = PCState.New;
		this._isClosed = true;
		this._isNewlyCreated = true;
		this.pendingTrackResolvers = {};
		this.reconnectAttempts = 0;
		this.reconnectStart = 0;
		this.attemptingReconnect = false;
		/** keeps track of how often an initial join connection has been tried */
		this.joinAttempts = 0;
		/** specifies how often an initial join connection is allowed to retry */
		this.maxJoinAttempts = 1;
		this.shouldFailNext = false;
		this.shouldFailOnV1Path = false;
		this.log = livekitLogger;
		this.reliableReceivedState = new TTLMap(reliabeReceiveStateTTL);
		this.midToTrackId = {};
		/** used to indicate whether the browser is currently waiting to reconnect */
		this.isWaitingForNetworkReconnect = false;
		this.handleDataChannel = (_a) => __awaiter(this, [_a], void 0, function(_ref) {
			var _this = this;
			let channel = _ref.channel;
			return function* () {
				if (!channel) return;
				if (_this.dataChannels.adoptSubscriberChannel(channel)) _this.log.debug("on data channel ".concat(channel.id, ", ").concat(channel.label));
			}();
		});
		this.handleDataMessage = (message) => __awaiter(this, void 0, void 0, function* () {
			var _a, _b, _c, _d, _e;
			const unlock = yield this.dataProcessLock.lock();
			try {
				const bytes = yield this.decodeDataMessage(message);
				if (!bytes) return;
				const dp = DataPacket.fromBinary(bytes);
				if (dp.sequence > 0 && dp.participantSid !== "") {
					const lastSeq = this.reliableReceivedState.get(dp.participantSid);
					if (lastSeq && dp.sequence <= lastSeq) return;
					this.reliableReceivedState.set(dp.participantSid, dp.sequence);
				}
				if (((_a = dp.value) === null || _a === void 0 ? void 0 : _a.case) === "speaker") this.emit(EngineEvent.ActiveSpeakersUpdate, dp.value.value.speakers);
				else if (((_b = dp.value) === null || _b === void 0 ? void 0 : _b.case) === "encryptedPacket") {
					if (!this.e2eeManager) {
						this.log.error("Received encrypted packet but E2EE not set up");
						return;
					}
					const decryptedData = yield (_c = this.e2eeManager) === null || _c === void 0 ? void 0 : _c.handleEncryptedData(dp.value.value.encryptedValue, dp.value.value.iv, dp.participantIdentity, dp.value.value.keyIndex);
					const newDp = new DataPacket({
						value: EncryptedPacketPayload.fromBinary(decryptedData.payload).value,
						participantIdentity: dp.participantIdentity,
						participantSid: dp.participantSid
					});
					if (((_d = newDp.value) === null || _d === void 0 ? void 0 : _d.case) === "user") applyUserDataCompat(newDp, newDp.value.value);
					this.emit(EngineEvent.DataPacketReceived, newDp, dp.value.value.encryptionType);
				} else {
					if (((_e = dp.value) === null || _e === void 0 ? void 0 : _e.case) === "user") applyUserDataCompat(dp, dp.value.value);
					this.emit(EngineEvent.DataPacketReceived, dp, Encryption_Type.NONE);
				}
			} finally {
				unlock();
			}
		});
		this.handleDataTrackMessage = (message) => __awaiter(this, void 0, void 0, function* () {
			const bytes = yield this.decodeDataMessage(message);
			if (!bytes) return;
			this.emit("dataTrackPacketReceived", bytes);
		});
		this.handleDataError = (event) => {
			if (this._isClosed) return;
			const channelKind = event.currentTarget.maxRetransmits === 0 ? "lossy" : "reliable";
			if (typeof RTCErrorEvent !== "undefined" && event instanceof RTCErrorEvent && event.error) {
				const error = event.error;
				this.log.error("DataChannel error on ".concat(channelKind, ": ").concat(error.message), {
					error,
					errorDetail: error.errorDetail,
					sctpCauseCode: error.sctpCauseCode
				});
			} else this.log.error("Unknown DataChannel error on ".concat(channelKind), { event });
		};
		this.handleDataChannelClose = (kind) => () => {
			var _a;
			if (!this._isClosed && ((_a = this.pcManager) === null || _a === void 0 ? void 0 : _a.publisher.getConnectionState()) === "connected") this.log.error("publisher data channel '".concat(DataChannelKind[kind], "' closed unexpectedly"), this.logContext);
		};
		this.handleDisconnect = (connection, disconnectReason) => {
			if (this._isClosed) return;
			this.log.warn("".concat(connection, " disconnected"));
			if (this.reconnectAttempts === 0) this.reconnectStart = Date.now();
			const disconnect = (duration) => {
				this.log.warn("could not recover connection after ".concat(this.reconnectAttempts, " attempts, ").concat(duration, "ms. giving up"));
				this.emit(EngineEvent.Disconnected);
				this.close();
			};
			const duration = Date.now() - this.reconnectStart;
			let delay = this.getNextRetryDelay({
				elapsedMs: duration,
				retryCount: this.reconnectAttempts
			});
			if (delay === null) {
				disconnect(duration);
				return;
			}
			if (connection === leaveReconnect) delay = 0;
			this.log.debug("reconnecting in ".concat(delay, "ms"));
			this.clearReconnectTimeout();
			if (this.token) this.emit(EngineEvent.TokenRefreshed, this.token);
			this.reconnectTimeout = CriticalTimers.setTimeout(() => this.attemptReconnect(disconnectReason).finally(() => this.reconnectTimeout = void 0), delay);
		};
		this.waitForRestarted = () => {
			return new Promise((resolve, reject) => {
				if (this.pcState === PCState.Connected) resolve();
				const onRestarted = () => {
					this.off(EngineEvent.Disconnected, onDisconnected);
					resolve();
				};
				const onDisconnected = () => {
					this.off(EngineEvent.Restarted, onRestarted);
					reject();
				};
				this.once(EngineEvent.Restarted, onRestarted);
				this.once(EngineEvent.Disconnected, onDisconnected);
			});
		};
		this.onRtpMapAvailable = (rtpTypes) => {
			const rtpMap = /* @__PURE__ */ new Map();
			rtpTypes.forEach((rtp) => {
				const codec = rtp.codec.toLowerCase();
				if (isVideoCodec(codec)) rtpMap.set(rtp.payload, codec);
			});
			this.emit(EngineEvent.RTPVideoMapUpdate, rtpMap);
		};
		this.handleBrowserOnLine = () => __awaiter(this, void 0, void 0, function* () {
			if (!this.url) return;
			if (!(yield fetch(toHttpUrl(this.url), { method: "HEAD" }).then((resp) => resp.ok).catch(() => false))) return;
			this.log.info("detected network reconnected");
			if (this.client.currentState === SignalConnectionState.RECONNECTING || this.isWaitingForNetworkReconnect && this.client.currentState === SignalConnectionState.CONNECTED) {
				this.clearReconnectTimeout();
				this.attemptReconnect(ReconnectReason.RR_SIGNAL_DISCONNECTED);
				this.isWaitingForNetworkReconnect = false;
			}
		});
		this.handleBrowserOffline = () => __awaiter(this, void 0, void 0, function* () {
			if (!this.url) return;
			try {
				yield Promise.race([fetch(toHttpUrl(this.url), { method: "HEAD" }), sleep(4e3).then(() => Promise.reject())]);
			} catch (e) {
				if (window.navigator.onLine === false) {
					this.log.info("detected network interruption");
					this.isWaitingForNetworkReconnect = true;
				}
			}
		});
		this.log = getLogger((_a = options.loggerName) !== null && _a !== void 0 ? _a : LoggerNames.Engine, () => this.logContext);
		this.loggerOptions = {
			loggerName: options.loggerName,
			loggerContextCb: () => this.logContext
		};
		this.client = new SignalClient(void 0, this.loggerOptions);
		this.client.signalLatency = this.options.expSignalLatency;
		this.reconnectPolicy = this.options.reconnectPolicy;
		this.closingLock = new _();
		this.dataProcessLock = new _();
		this.dataChannels = new DataChannelManager({
			isEngineClosed: () => this.isClosed,
			isReconnecting: () => this.attemptingReconnect,
			onDataMessage: (message) => this.handleDataMessage(message),
			onDataTrackMessage: (message) => this.handleDataTrackMessage(message),
			onDataError: (event) => this.handleDataError(event),
			onChannelClose: (kind) => this.handleDataChannelClose(kind)(),
			onBufferStatusChanged: (kind, isLow) => this.emit(EngineEvent.DCBufferStatusChanged, isLow, kind)
		});
		this.client.onParticipantUpdate = (updates) => this.emit(EngineEvent.ParticipantUpdate, updates);
		this.client.onConnectionQuality = (update) => {
			this.handleLocalConnectionQuality(update);
			this.emit(EngineEvent.ConnectionQualityUpdate, update);
		};
		this.client.onRoomUpdate = (update) => this.emit(EngineEvent.RoomUpdate, update);
		this.client.onSubscriptionError = (resp) => this.emit(EngineEvent.SubscriptionError, resp);
		this.client.onSubscriptionPermissionUpdate = (update) => this.emit(EngineEvent.SubscriptionPermissionUpdate, update);
		this.client.onSpeakersChanged = (update) => this.emit(EngineEvent.SpeakersChanged, update);
		this.client.onStreamStateUpdate = (update) => this.emit(EngineEvent.StreamStateChanged, update);
		this.client.onRequestResponse = (response) => this.emit(EngineEvent.SignalRequestResponse, response);
		this.client.onParticipantUpdate = (updates) => this.emit(EngineEvent.ParticipantUpdate, updates);
		this.client.onJoined = (joinResponse) => this.emit(EngineEvent.Joined, joinResponse);
	}
	/** @internal */
	get logContext() {
		var _a, _b, _c, _d, _e, _f;
		return {
			room: (_b = (_a = this.latestJoinResponse) === null || _a === void 0 ? void 0 : _a.room) === null || _b === void 0 ? void 0 : _b.name,
			roomID: (_d = (_c = this.latestJoinResponse) === null || _c === void 0 ? void 0 : _c.room) === null || _d === void 0 ? void 0 : _d.sid,
			participant: (_f = (_e = this.latestJoinResponse) === null || _e === void 0 ? void 0 : _e.participant) === null || _f === void 0 ? void 0 : _f.identity,
			participantID: this.participantSid
		};
	}
	join(url_1, token_1, opts_1, abortSignal_1) {
		return __awaiter(this, arguments, void 0, function(url, token, opts, abortSignal) {
			var _this2 = this;
			let useV0Path = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : false;
			return function* () {
				var _a, _b, _c;
				_this2._isNewlyCreated = false;
				_this2.url = url;
				_this2.token = token;
				_this2.signalOpts = opts;
				_this2.maxJoinAttempts = opts.maxRetries;
				try {
					_this2.joinAttempts += 1;
					_this2.setupSignalClientCallbacks();
					const sendOfferWithJoin = !useV0Path && isPublisherOfferWithJoinSupported();
					let offerProto;
					if (sendOfferWithJoin) {
						if (!_this2.pcManager) {
							yield _this2.configure();
							_this2.applyInitialPublisherLayout();
						}
						const offer = yield (_a = _this2.pcManager) === null || _a === void 0 ? void 0 : _a.publisher.createInitialOffer();
						if (offer) offerProto = toProtoSessionDescription(offer.offer, offer.offerId);
					}
					if (abortSignal === null || abortSignal === void 0 ? void 0 : abortSignal.aborted) throw ConnectionError.cancelled("Connection aborted");
					if (!useV0Path && _this2.shouldFailOnV1Path) {
						_this2.shouldFailOnV1Path = false;
						throw ConnectionError.serviceNotFound("Simulated v1 path failure", "v0-rtc");
					}
					const joinResponse = yield _this2.client.join(url, token, opts, abortSignal, useV0Path, offerProto);
					_this2._isClosed = false;
					_this2.latestJoinResponse = joinResponse;
					_this2.participantSid = (_b = joinResponse.participant) === null || _b === void 0 ? void 0 : _b.sid;
					_this2.subscriberPrimary = joinResponse.subscriberPrimary;
					if (sendOfferWithJoin) (_c = _this2.pcManager) === null || _c === void 0 || _c.updateConfiguration(_this2.makeRTCConfiguration(joinResponse));
					else {
						if (!_this2.pcManager) {
							yield _this2.configure(joinResponse, !useV0Path);
							if (!useV0Path) _this2.applyInitialPublisherLayout();
						}
						if (!_this2.subscriberPrimary || joinResponse.fastPublish) _this2.negotiate().catch((err) => {
							_this2.log.error(err);
						});
					}
					_this2.registerOnLineListener();
					_this2.clientConfiguration = joinResponse.clientConfiguration;
					_this2.emit(EngineEvent.SignalConnected, joinResponse);
					let serverInfo = joinResponse.serverInfo;
					if (!serverInfo) serverInfo = {
						version: joinResponse.serverVersion,
						region: joinResponse.serverRegion
					};
					_this2.log.info("connected to Livekit Server ".concat(Object.entries(serverInfo).map((_ref2) => {
						let _ref3 = _slicedToArray(_ref2, 2), key = _ref3[0], value = _ref3[1];
						return "".concat(key, ": ").concat(value);
					}).join(", ")));
					return {
						joinResponse,
						serverInfo
					};
				} catch (e) {
					if (e instanceof ConnectionError) {
						if (e.reason === ConnectionErrorReason.ServerUnreachable) {
							_this2.log.warn("Couldn't connect to server, attempt ".concat(_this2.joinAttempts, " of ").concat(_this2.maxJoinAttempts));
							if (_this2.joinAttempts < _this2.maxJoinAttempts) return _this2.join(url, token, opts, abortSignal, useV0Path);
						} else if (e.reason === ConnectionErrorReason.ServiceNotFound) {
							_this2.log.warn("Initial connection failed: ".concat(e.message, " – Retrying"));
							if (_this2.pcManager) {
								_this2.pcManager.onStateChange = void 0;
								yield _this2.cleanupPeerConnections();
							}
							return _this2.join(url, token, opts, abortSignal, true);
						}
					}
					throw e;
				}
			}();
		});
	}
	close() {
		return __awaiter(this, void 0, void 0, function* () {
			const unlock = yield this.closingLock.lock();
			if (this.isClosed) {
				unlock();
				return;
			}
			try {
				this._isClosed = true;
				this.joinAttempts = 0;
				this.emit(EngineEvent.Closing);
				this.removeAllListeners();
				this.deregisterOnLineListener();
				this.clearPendingReconnect();
				this.clearLostQualityTimeout();
				this.cleanupLossyDataStats();
				yield this.cleanupPeerConnections();
				yield this.cleanupClient();
			} finally {
				unlock();
			}
		});
	}
	cleanupPeerConnections() {
		return __awaiter(this, void 0, void 0, function* () {
			var _a;
			this.dataChannels.teardown();
			yield (_a = this.pcManager) === null || _a === void 0 ? void 0 : _a.close();
			this.pcManager = void 0;
			this.transportConnectingSince = void 0;
			this.reliableReceivedState.clear();
		});
	}
	cleanupLossyDataStats() {
		this.lossyChannel.stopThresholdTuning();
	}
	cleanupClient() {
		return __awaiter(this, void 0, void 0, function* () {
			yield this.client.close();
			this.client.resetCallbacks();
			for (const cid of Object.keys(this.pendingTrackResolvers)) this.pendingTrackResolvers[cid].reject();
			this.pendingTrackResolvers = {};
		});
	}
	addTrack(req) {
		if (this.pendingTrackResolvers[req.cid]) throw new TrackInvalidError("a track with the same ID has already been published");
		return new Promise((resolve, reject) => {
			const publicationTimeout = CriticalTimers.setTimeout(() => {
				delete this.pendingTrackResolvers[req.cid];
				reject(ConnectionError.timeout("publication of local track timed out, no response from server"));
			}, 1e4);
			this.pendingTrackResolvers[req.cid] = {
				resolve: (info) => {
					CriticalTimers.clearTimeout(publicationTimeout);
					resolve(info);
				},
				reject: () => {
					CriticalTimers.clearTimeout(publicationTimeout);
					reject(/* @__PURE__ */ new Error("Cancelled publication by calling unpublish"));
				}
			};
			this.client.sendAddTrack(req);
		});
	}
	/**
	* Removes sender from PeerConnection, returning true if it was removed successfully
	* and a negotiation is necessary
	* @param sender
	* @returns
	*/
	removeTrack(sender) {
		if (sender.track && this.pendingTrackResolvers[sender.track.id]) {
			const reject = this.pendingTrackResolvers[sender.track.id].reject;
			if (reject) reject();
			delete this.pendingTrackResolvers[sender.track.id];
		}
		try {
			this.pcManager.removeTrack(sender);
			return true;
		} catch (e) {
			this.log.warn("failed to remove track", { error: e });
		}
		return false;
	}
	updateMuteStatus(trackSid, muted) {
		this.client.sendMuteTrack(trackSid, muted);
	}
	get dataSubscriberReadyState() {
		var _a;
		return (_a = this.dataChannelForKind(DataChannelKind.RELIABLE, true)) === null || _a === void 0 ? void 0 : _a.readyState;
	}
	getConnectedServerAddress() {
		return __awaiter(this, void 0, void 0, function* () {
			var _a;
			return (_a = this.pcManager) === null || _a === void 0 ? void 0 : _a.getConnectedAddress();
		});
	}
	setRegionStrategy(strategy) {
		this.regionStrategy = strategy;
	}
	configure(joinResponse, useSinglePeerConnection) {
		return __awaiter(this, void 0, void 0, function* () {
			var _a;
			if (this.pcManager && this.pcManager.currentState !== PCTransportState.NEW) return;
			if (!joinResponse) {
				const rtcConfig = this.makeRTCConfiguration();
				this.pcManager = new PCTransportManager("publisher-only", this.loggerOptions, rtcConfig);
			} else {
				this.participantSid = (_a = joinResponse.participant) === null || _a === void 0 ? void 0 : _a.sid;
				const rtcConfig = this.makeRTCConfiguration(joinResponse);
				this.pcManager = new PCTransportManager(useSinglePeerConnection ? "publisher-only" : joinResponse.subscriberPrimary ? "subscriber-primary" : "publisher-primary", this.loggerOptions, rtcConfig);
			}
			this.emit(EngineEvent.TransportsCreated, this.pcManager.publisher, this.pcManager.subscriber);
			this.pcManager.onIceCandidate = (candidate, target) => {
				this.client.sendIceCandidate(candidate, target);
			};
			this.pcManager.onPublisherOffer = (offer, offerId) => {
				this.client.sendOffer(offer, offerId);
			};
			this.pcManager.onDataChannel = this.handleDataChannel;
			this.pcManager.onStateChange = (connectionState, publisherState, subscriberState) => __awaiter(this, void 0, void 0, function* () {
				this.log.debug("primary PC state changed ".concat(connectionState));
				if (connectionState === PCTransportState.CONNECTING) this.transportConnectingSince = Date.now();
				else this.transportConnectingSince = void 0;
				if ([
					"closed",
					"disconnected",
					"failed"
				].includes(publisherState)) this.publisherConnectionPromise = void 0;
				if (connectionState === PCTransportState.CONNECTED) {
					const shouldEmit = this.pcState === PCState.New;
					this.pcState = PCState.Connected;
					if (shouldEmit) this.emit(EngineEvent.Connected, this.latestJoinResponse);
				} else if (connectionState === PCTransportState.FAILED) {
					if (this.pcState === PCState.Connected || this.pcState === PCState.Reconnecting) {
						this.pcState = PCState.Disconnected;
						this.handleDisconnect("peerconnection failed", subscriberState === "failed" ? ReconnectReason.RR_SUBSCRIBER_FAILED : ReconnectReason.RR_PUBLISHER_FAILED);
					}
				}
				const isSignalSevered = this.client.isDisconnected || this.client.currentState === SignalConnectionState.RECONNECTING;
				const isPCSevered = [
					PCTransportState.FAILED,
					PCTransportState.CLOSING,
					PCTransportState.CLOSED
				].includes(connectionState);
				if (isSignalSevered && isPCSevered && !this._isClosed) this.emit(EngineEvent.Offline);
			});
			this.pcManager.onTrack = (ev) => {
				if (ev.streams.length === 0) return;
				this.emit(EngineEvent.MediaTrackAdded, ev.track, ev.streams[0], ev.receiver);
			};
		});
	}
	setupSignalClientCallbacks() {
		this.client.onAnswer = (sd, offerId, midToTrackId) => __awaiter(this, void 0, void 0, function* () {
			if (!this.pcManager) return;
			this.log.debug("received server answer", {
				RTCSdpType: sd.type,
				sdp: sd.sdp,
				midToTrackId
			});
			this.midToTrackId = midToTrackId;
			yield this.pcManager.setPublisherAnswer(sd, offerId);
		});
		this.client.onTrickle = (candidate, target) => {
			if (!this.pcManager) return;
			this.log.debug("got ICE candidate from peer", {
				candidate,
				target
			});
			this.pcManager.addIceCandidate(candidate, target);
		};
		this.client.onOffer = (sd, offerId, midToTrackId) => __awaiter(this, void 0, void 0, function* () {
			this.latestRemoteOfferId = offerId;
			if (!this.pcManager) return;
			this.midToTrackId = midToTrackId;
			const answer = yield this.pcManager.createSubscriberAnswerFromOffer(sd, offerId);
			if (answer) this.client.sendAnswer(answer, offerId);
		});
		this.client.onLocalTrackPublished = (res) => {
			var _a;
			this.log.debug("received trackPublishedResponse", {
				cid: res.cid,
				track: (_a = res.track) === null || _a === void 0 ? void 0 : _a.sid
			});
			if (!this.pendingTrackResolvers[res.cid]) {
				this.log.error("missing track resolver for ".concat(res.cid), { cid: res.cid });
				return;
			}
			const resolve = this.pendingTrackResolvers[res.cid].resolve;
			delete this.pendingTrackResolvers[res.cid];
			resolve(res.track);
		};
		this.client.onLocalTrackUnpublished = (response) => {
			this.emit(EngineEvent.LocalTrackUnpublished, response);
		};
		this.client.onLocalTrackSubscribed = (trackSid) => {
			this.emit(EngineEvent.LocalTrackSubscribed, trackSid);
		};
		this.client.onTokenRefresh = (token) => {
			this.token = token;
			this.emit(EngineEvent.TokenRefreshed, token);
		};
		this.client.onRemoteMuteChanged = (trackSid, muted) => {
			this.emit(EngineEvent.RemoteMute, trackSid, muted);
		};
		this.client.onSubscribedQualityUpdate = (update) => {
			this.emit(EngineEvent.SubscribedQualityUpdate, update);
		};
		this.client.onRoomMoved = (res) => {
			var _a;
			this.participantSid = (_a = res.participant) === null || _a === void 0 ? void 0 : _a.sid;
			if (this.latestJoinResponse) this.latestJoinResponse.room = res.room;
			this.emit(EngineEvent.RoomMoved, res);
		};
		this.client.onMediaSectionsRequirement = (requirement) => {
			this.addMediaSections(requirement.numAudios, requirement.numVideos);
			this.negotiate();
		};
		this.client.onPublishDataTrackResponse = (event) => {
			this.emit(EngineEvent.PublishDataTrackResponse, event);
		};
		this.client.onUnPublishDataTrackResponse = (event) => {
			this.emit(EngineEvent.UnPublishDataTrackResponse, event);
		};
		this.client.onDataTrackSubscriberHandles = (event) => {
			this.emit(EngineEvent.DataTrackSubscriberHandles, event);
		};
		this.client.onClose = () => {
			this.handleDisconnect("signal", ReconnectReason.RR_SIGNAL_DISCONNECTED);
		};
		this.client.onLeave = (leave) => {
			this.log.info("client leave request received (action=".concat(leave === null || leave === void 0 ? void 0 : leave.action, ")"), { reason: leave === null || leave === void 0 ? void 0 : leave.reason });
			if (leave.regions) {
				this.log.debug("updating regions");
				this.emit(EngineEvent.ServerRegionsReported, leave.regions);
			}
			switch (leave.action) {
				case LeaveRequest_Action.DISCONNECT:
					this.emit(EngineEvent.Disconnected, leave === null || leave === void 0 ? void 0 : leave.reason);
					this.close();
					break;
				case LeaveRequest_Action.RECONNECT:
					this.fullReconnectOnNext = true;
					this.handleDisconnect(leaveReconnect);
					break;
				case LeaveRequest_Action.RESUME: this.handleDisconnect(leaveReconnect);
			}
		};
	}
	makeRTCConfiguration(serverResponse) {
		var _a;
		const rtcConfig = Object.assign({}, this.rtcConfig);
		if ((((_a = this.signalOpts) === null || _a === void 0 ? void 0 : _a.e2eeEnabled) || this.frameMetadataWorker && !shouldUseFrameMetadataScriptTransform()) && isInsertableStreamSupported()) {
			this.log.debug("E2EE - setting up transports with insertable streams");
			rtcConfig.encodedInsertableStreams = true;
		}
		rtcConfig.sdpSemantics = "unified-plan";
		rtcConfig.continualGatheringPolicy = "gather_continually";
		if (!serverResponse) return rtcConfig;
		if (serverResponse.iceServers && !rtcConfig.iceServers) {
			const rtcIceServers = [];
			serverResponse.iceServers.forEach((iceServer) => {
				const rtcIceServer = { urls: iceServer.urls };
				if (iceServer.username) rtcIceServer.username = iceServer.username;
				if (iceServer.credential) rtcIceServer.credential = iceServer.credential;
				rtcIceServers.push(rtcIceServer);
			});
			rtcConfig.iceServers = rtcIceServers;
		}
		if (serverResponse.clientConfiguration && serverResponse.clientConfiguration.forceRelay === ClientConfigSetting.ENABLED) rtcConfig.iceTransportPolicy = "relay";
		return rtcConfig;
	}
	/**
	* Populate the publisher PC so its first offer carries the data channels + recvonly media
	* sections. Required for every V1 connection: Firefox only binds receive decoders for media
	* present in that first offer, and the offer-with-join path needs the sections to
	* build a meaningful initial offer. Must be called on a configured pcManager.
	*/
	applyInitialPublisherLayout() {
		this.createDataChannels();
		/**
		* Native libwebrtc does not support pre-populating the media sections,
		* so we skip it for React Native.
		*
		* Related: https://github.com/livekit/rust-sdks/pull/1151
		*/
		if (!isReactNative()) this.addMediaSections(initialMediaSectionsAudio, initialMediaSectionsVideo);
	}
	addMediaSections(numAudios, numVideos) {
		var _a, _b, _c;
		const transceiverInit = { direction: "recvonly" };
		for (let i = 0; i < numAudios; i++) (_a = this.pcManager) === null || _a === void 0 || _a.addPublisherTransceiverOfKind("audio", transceiverInit);
		const receivesMedia = ((_b = this.pcManager) === null || _b === void 0 ? void 0 : _b.mode) === "publisher-only";
		for (let i = 0; i < numVideos; i++) {
			const transceiver = (_c = this.pcManager) === null || _c === void 0 ? void 0 : _c.addPublisherTransceiverOfKind("video", transceiverInit);
			if (receivesMedia && transceiver) {
				const negotiated = negotiateDependencyDescriptor(transceiver);
				this.log.debug("dependency descriptor negotiated for received video", { negotiated });
			}
		}
	}
	createDataChannels() {
		if (!this.pcManager) return;
		this.dataChannels.createPublisherChannels(this.pcManager);
	}
	/** Normalizes an incoming data-channel message into bytes, or logs and returns undefined. */
	decodeDataMessage(message) {
		return __awaiter(this, void 0, void 0, function* () {
			if (message.data instanceof ArrayBuffer) return new Uint8Array(message.data);
			if (message.data instanceof Blob) return new Uint8Array(yield message.data.arrayBuffer());
			this.log.error("unsupported data type", { data: message.data });
		});
	}
	createSender(track, opts, encodings) {
		return __awaiter(this, void 0, void 0, function* () {
			let sender;
			if (supportsTransceiver()) sender = yield this.createTransceiverRTCRtpSender(track, opts, encodings);
			else if (supportsAddTrack()) {
				this.log.warn("using add-track fallback");
				sender = yield this.createRTCRtpSender(track.mediaStreamTrack);
			} else throw new UnexpectedConnectionState("Required webRTC APIs not supported on this device");
			this.setupFrameMetadataSender(sender, opts);
			return sender;
		});
	}
	createSimulcastSender(track, simulcastTrack, opts, encodings) {
		return __awaiter(this, void 0, void 0, function* () {
			let sender;
			if (supportsTransceiver()) sender = yield this.createSimulcastTransceiverSender(track, simulcastTrack, opts, encodings);
			else if (supportsAddTrack()) {
				this.log.debug("using add-track fallback");
				sender = yield this.createRTCRtpSender(track.mediaStreamTrack);
			} else throw new UnexpectedConnectionState("Cannot stream on this device");
			if (sender) this.setupFrameMetadataSender(sender, opts);
			return sender;
		});
	}
	get frameMetadataWorker() {
		var _a, _b;
		return (_b = (_a = this.options.frameMetadata) !== null && _a !== void 0 ? _a : this.options.packetTrailer) === null || _b === void 0 ? void 0 : _b.worker;
	}
	setupFrameMetadataSender(sender) {
		let opts = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
		var _a, _b, _c;
		const worker = this.frameMetadataWorker;
		if (!worker || ((_a = this.signalOpts) === null || _a === void 0 ? void 0 : _a.e2eeEnabled)) return;
		const frameMetadata = (_b = opts.frameMetadata) !== null && _b !== void 0 ? _b : opts.packetTrailer;
		const hasMetadata = hasFrameMetadataPublishOptions(frameMetadata);
		if (shouldUseFrameMetadataScriptTransform()) {
			if (hasMetadata) sender.transform = new RTCRtpScriptTransform(worker, {
				kind: "encode",
				packetTrailer: frameMetadata
			});
			return;
		}
		if (!isFrameMetadataSupported((_c = this.options.frameMetadata) !== null && _c !== void 0 ? _c : this.options.packetTrailer) || !("createEncodedStreams" in sender)) {
			if (hasMetadata) this.log.warn("frame metadata transform not supported; skipping write", this.logContext);
			return;
		}
		const _sender$createEncoded = sender.createEncodedStreams(), readable = _sender$createEncoded.readable, writable = _sender$createEncoded.writable;
		if (hasMetadata) worker.postMessage({
			kind: "encode",
			data: {
				readableStream: readable,
				writableStream: writable,
				packetTrailer: frameMetadata
			}
		}, [readable, writable]);
		else readable.pipeTo(writable);
	}
	createTransceiverRTCRtpSender(track, opts, encodings) {
		return __awaiter(this, void 0, void 0, function* () {
			if (!this.pcManager) throw new UnexpectedConnectionState("publisher is closed");
			const streams = [];
			if (track.mediaStream) streams.push(track.mediaStream);
			if (isVideoTrack(track)) track.codec = opts.videoCodec;
			const transceiverInit = {
				direction: "sendonly",
				streams
			};
			if (encodings) transceiverInit.sendEncodings = encodings;
			return (yield this.pcManager.addPublisherTransceiver(track.mediaStreamTrack, transceiverInit)).sender;
		});
	}
	createSimulcastTransceiverSender(track, simulcastTrack, opts, encodings) {
		return __awaiter(this, void 0, void 0, function* () {
			if (!this.pcManager) throw new UnexpectedConnectionState("publisher is closed");
			const transceiverInit = { direction: "sendonly" };
			if (encodings) transceiverInit.sendEncodings = encodings;
			const transceiver = yield this.pcManager.addPublisherTransceiver(simulcastTrack.mediaStreamTrack, transceiverInit);
			if (!opts.videoCodec) return;
			yield track.setSimulcastTrackSender(opts.videoCodec, transceiver.sender);
			return transceiver.sender;
		});
	}
	createRTCRtpSender(track) {
		return __awaiter(this, void 0, void 0, function* () {
			if (!this.pcManager) throw new UnexpectedConnectionState("publisher is closed");
			return this.pcManager.addPublisherTrack(track);
		});
	}
	/**
	* A sustained local `LOST` while connected and publishing means the server isn't receiving
	* our media, so force a full reconnect; any non-`LOST` value cancels a pending trigger.
	*/
	handleLocalConnectionQuality(update) {
		if (!this.participantSid) return;
		const localUpdate = update.updates.find((u) => u.participantSid === this.participantSid);
		if (!localUpdate) return;
		if (localUpdate.quality === ConnectionQuality$1.LOST) this.scheduleLostQualityReconnect();
		else this.clearLostQualityTimeout();
	}
	scheduleLostQualityReconnect() {
		if (this.lostQualityTimeout) return;
		this.lostQualityTimeout = CriticalTimers.setTimeout(() => {
			this.lostQualityTimeout = void 0;
			if (this._isClosed || this.pcState !== PCState.Connected || this.attemptingReconnect) return;
			if (!this.hasActivePublisherSenders()) return;
			this.log.warn("local connection quality lost while publishing, triggering full reconnect", this.logContext);
			this.fullReconnectOnNext = true;
			this.handleDisconnect("connection quality lost", ReconnectReason.RR_PUBLISHER_FAILED);
		}, connectionQualityLostTimeout);
	}
	clearLostQualityTimeout() {
		if (this.lostQualityTimeout) {
			CriticalTimers.clearTimeout(this.lostQualityTimeout);
			this.lostQualityTimeout = void 0;
		}
	}
	/** Whether the publisher currently has any sender with a live track. */
	hasActivePublisherSenders() {
		var _a, _b;
		return (_b = (_a = this.pcManager) === null || _a === void 0 ? void 0 : _a.publisher.getSenders().some((sender) => !!sender.track && sender.track.readyState === "live")) !== null && _b !== void 0 ? _b : false;
	}
	/**
	* Forces a full reconnect while keeping the engine (and its saved credentials) alive. Used by
	* Room's connection-reconcile safety net when the transport silently died but we looked connected.
	* @internal
	*/
	reconnect() {
		let reason = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : ReconnectReason.RR_UNKNOWN;
		this.fullReconnectOnNext = true;
		this.handleDisconnect("reconcile", reason);
	}
	attemptReconnect(reason) {
		return __awaiter(this, void 0, void 0, function* () {
			var _a, _b, _c;
			if (this._isClosed) return;
			if (this.attemptingReconnect) {
				this.log.warn("already attempting reconnect, returning early");
				return;
			}
			this.clearLostQualityTimeout();
			if (((_a = this.clientConfiguration) === null || _a === void 0 ? void 0 : _a.resumeConnection) === ClientConfigSetting.DISABLED || ((_c = (_b = this.pcManager) === null || _b === void 0 ? void 0 : _b.currentState) !== null && _c !== void 0 ? _c : PCTransportState.NEW) === PCTransportState.NEW) this.fullReconnectOnNext = true;
			const fullReconnect = this.fullReconnectOnNext;
			this.fullReconnectOnNext = false;
			let succeeded = false;
			try {
				this.attemptingReconnect = true;
				if (fullReconnect) yield this.restartConnection();
				else yield this.resumeConnection(reason);
				this.clearPendingReconnect();
				succeeded = true;
			} catch (e) {
				this.reconnectAttempts += 1;
				let recoverable = true;
				if (e instanceof UnexpectedConnectionState) {
					this.log.debug("received unrecoverable error", { error: e });
					recoverable = false;
				} else if (fullReconnect || !(e instanceof SignalReconnectError)) this.fullReconnectOnNext = true;
				if (recoverable) this.handleDisconnect("reconnect", ReconnectReason.RR_UNKNOWN);
				else {
					this.log.info("could not recover connection after ".concat(this.reconnectAttempts, " attempts, ").concat(Date.now() - this.reconnectStart, "ms. giving up"));
					this.emit(EngineEvent.Disconnected);
					yield this.close();
				}
			} finally {
				this.attemptingReconnect = false;
				if (succeeded && this.fullReconnectOnNext && !this._isClosed) {
					this.log.debug("full reconnect requested during in-progress attempt, dispatching");
					this.handleDisconnect("reconnect");
				}
			}
		});
	}
	getNextRetryDelay(context) {
		try {
			return this.reconnectPolicy.nextRetryDelayInMs(context);
		} catch (e) {
			this.log.warn("encountered error in reconnect policy", { error: e });
		}
		return null;
	}
	restartConnection(regionUrl) {
		return __awaiter(this, void 0, void 0, function* () {
			var _a, _b, _c;
			try {
				if (!this.url || !this.token) throw new UnexpectedConnectionState("could not reconnect, url or token not saved");
				this.log.info("reconnecting, attempt: ".concat(this.reconnectAttempts));
				this.emit(EngineEvent.Restarting);
				if (!this.client.isDisconnected) yield this.client.sendLeave();
				yield this.cleanupPeerConnections();
				yield this.cleanupClient();
				let joinResponse;
				try {
					if (!this.signalOpts) {
						this.log.warn("attempted connection restart, without signal options present");
						throw new SignalReconnectError();
					}
					joinResponse = (yield this.join(regionUrl !== null && regionUrl !== void 0 ? regionUrl : this.url, this.token, this.signalOpts, void 0, !this.options.singlePeerConnection)).joinResponse;
				} catch (e) {
					if (e instanceof ConnectionError && e.reason === ConnectionErrorReason.NotAllowed) throw new UnexpectedConnectionState("could not reconnect, token might be expired");
					throw new SignalReconnectError();
				}
				if (this.shouldFailNext) {
					this.shouldFailNext = false;
					throw new Error("simulated failure");
				}
				this.client.setReconnected();
				this.emit(EngineEvent.SignalRestarted, joinResponse);
				yield this.waitForPCReconnected();
				if (this.client.currentState !== SignalConnectionState.CONNECTED) throw new SignalReconnectError("Signal connection got severed during reconnect");
				(_a = this.regionStrategy) === null || _a === void 0 || _a.resetAttempts();
				this.emit(EngineEvent.Restarted);
			} catch (error) {
				const nextRegionUrl = yield (_b = this.regionStrategy) === null || _b === void 0 ? void 0 : _b.getNextUrl();
				if (nextRegionUrl) {
					yield this.restartConnection(nextRegionUrl);
					return;
				} else {
					(_c = this.regionStrategy) === null || _c === void 0 || _c.resetAttempts();
					throw error;
				}
			}
		});
	}
	resumeConnection(reason) {
		return __awaiter(this, void 0, void 0, function* () {
			if (!this.url || !this.token) throw new UnexpectedConnectionState("could not reconnect, url or token not saved");
			if (!this.pcManager) throw new UnexpectedConnectionState("publisher and subscriber connections unset");
			this.log.info("resuming signal connection, attempt ".concat(this.reconnectAttempts));
			this.emit(EngineEvent.Resuming);
			let res;
			try {
				this.setupSignalClientCallbacks();
				res = yield this.client.reconnect(this.url, this.token, this.participantSid, reason);
			} catch (error) {
				let message = "";
				if (error instanceof Error) {
					message = error.message;
					this.log.error(error.message, { error });
				}
				if (error instanceof ConnectionError && error.reason === ConnectionErrorReason.NotAllowed) throw new UnexpectedConnectionState("could not reconnect, token might be expired");
				if (error instanceof ConnectionError && error.reason === ConnectionErrorReason.LeaveRequest) throw error;
				throw new SignalReconnectError(message);
			}
			this.emit(EngineEvent.SignalResumed);
			if (res) {
				const rtcConfig = this.makeRTCConfiguration(res);
				this.pcManager.updateConfiguration(rtcConfig);
				if (this.latestJoinResponse) this.latestJoinResponse.serverInfo = res.serverInfo;
			} else this.log.warn("Did not receive reconnect response");
			if (this.shouldFailNext) {
				this.shouldFailNext = false;
				throw new Error("simulated failure");
			}
			yield this.pcManager.triggerIceRestart();
			yield this.waitForPCReconnected();
			if (this.client.currentState !== SignalConnectionState.CONNECTED) throw new SignalReconnectError("Signal connection got severed during reconnect");
			this.client.setReconnected();
			const reliableDC = this.dataChannelForKind(DataChannelKind.RELIABLE);
			if ((reliableDC === null || reliableDC === void 0 ? void 0 : reliableDC.readyState) === "open" && reliableDC.id === null) this.createDataChannels();
			if (res === null || res === void 0 ? void 0 : res.lastMessageSeq) this.resendReliableMessagesForResume(res.lastMessageSeq).catch((error) => {
				this.log.warn("failed to resend reliable messages after resume", Object.assign(Object.assign({}, this.logContext), { error }));
			});
			this.emit(EngineEvent.Resumed);
		});
	}
	waitForPCInitialConnection(timeout, abortController) {
		return __awaiter(this, void 0, void 0, function* () {
			if (!this.pcManager) throw new UnexpectedConnectionState("PC manager is closed");
			yield this.pcManager.ensurePCTransportConnection(abortController, timeout);
		});
	}
	waitForPCReconnected() {
		return __awaiter(this, void 0, void 0, function* () {
			this.pcState = PCState.Reconnecting;
			this.log.debug("waiting for peer connection to reconnect");
			try {
				yield sleep(minReconnectWait);
				if (!this.pcManager) throw new UnexpectedConnectionState("PC manager is closed");
				yield this.pcManager.ensurePCTransportConnection(void 0, this.peerConnectionTimeout);
				this.pcState = PCState.Connected;
			} catch (e) {
				this.pcState = PCState.Disconnected;
				throw ConnectionError.internal("could not establish PC connection, ".concat(e.message));
			}
		});
	}
	/** @internal */
	publishRpcAck(destinationIdentity, requestId) {
		return __awaiter(this, void 0, void 0, function* () {
			const packet = new DataPacket({
				destinationIdentities: [destinationIdentity],
				kind: DataPacket_Kind.RELIABLE,
				value: {
					case: "rpcAck",
					value: new RpcAck({ requestId })
				}
			});
			yield this.sendDataPacket(packet, DataChannelKind.RELIABLE);
		});
	}
	sendDataPacket(packet, kind) {
		return __awaiter(this, void 0, void 0, function* () {
			var _a, _b;
			yield this.ensurePublisherConnected(kind);
			if (this.e2eeManager && this.e2eeManager.isDataChannelEncryptionEnabled) {
				const encryptablePacket = asEncryptablePacket(packet);
				if (encryptablePacket) {
					const encryptedData = yield this.e2eeManager.encryptData(encryptablePacket.toBinary());
					packet.value = {
						case: "encryptedPacket",
						value: new EncryptedPacket({
							encryptedValue: encryptedData.payload,
							iv: encryptedData.iv,
							keyIndex: encryptedData.keyIndex
						})
					};
				}
			}
			if (kind === DataChannelKind.RELIABLE) packet.sequence = this.reliableChannel.nextSequence();
			const msg = packet.toBinary();
			const maxPublisherMessageSizeBytes = Math.min((_b = (_a = this.pcManager) === null || _a === void 0 ? void 0 : _a.getMaxPublisherMessageSize()) !== null && _b !== void 0 ? _b : DEFAULT_MAX_MESSAGE_SIZE, DEFAULT_MAX_MESSAGE_SIZE);
			if (typeof maxPublisherMessageSizeBytes !== "undefined" && maxPublisherMessageSizeBytes !== 0 && msg.byteLength > maxPublisherMessageSizeBytes) throw new PublishDataError("cannot publish data packet larger than ".concat(maxPublisherMessageSizeBytes, " bytes (got ").concat(msg.byteLength, ")"));
			if (kind === DataChannelKind.RELIABLE) yield this.reliableChannel.send(msg, packet.sequence);
			else yield this.lossyChannel.send(msg);
		});
	}
	/**
	* Sends pre-serialized bytes on the data-track channel. This is the one send path that doesn't
	* go through {@link sendDataPacket} — Room's `packetAvailable` handler calls it directly with
	* bytes the data-track pipeline already serialized.
	*
	* @internal
	*/
	sendDataTrackFrame(bytes) {
		return __awaiter(this, void 0, void 0, function* () {
			yield this.ensurePublisherConnected(DataChannelKind.DATA_TRACK_LOSSY);
			yield this.dataTrackChannel.send(bytes);
		});
	}
	resendReliableMessagesForResume(lastMessageSeq) {
		return __awaiter(this, void 0, void 0, function* () {
			yield this.ensurePublisherConnected(DataChannelKind.RELIABLE);
			yield this.reliableChannel.replay(lastMessageSeq);
		});
	}
	/** The flow-control gate for `kind` — see {@link FlowControlledDataChannel}. */
	flowControlFor(kind) {
		return this.dataChannels.channelFor(kind);
	}
	/**
	* Resolves once the caller may send on the `kind` channel — see
	* {@link FlowControlledDataChannel.waitForHeadroomWithLock}.
	*/
	waitForBufferHeadroom(kind) {
		return __awaiter(this, void 0, void 0, function* () {
			return this.flowControlFor(kind).waitForHeadroomWithLock();
		});
	}
	/**
	* @internal
	*/
	ensureDataTransportConnected(kind_1) {
		return __awaiter(this, arguments, void 0, function(kind) {
			var _this3 = this;
			let subscriber = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : this.subscriberPrimary;
			return function* () {
				var _a;
				if (!_this3.pcManager) throw new UnexpectedConnectionState("PC manager is closed");
				const transport = subscriber ? _this3.pcManager.subscriber : _this3.pcManager.publisher;
				const transportName = subscriber ? "Subscriber" : "Publisher";
				if (!transport) throw ConnectionError.internal("".concat(transportName, " connection not set"));
				let needNegotiation = false;
				if (!subscriber && !_this3.dataChannelForKind(kind, subscriber)) {
					_this3.createDataChannels();
					needNegotiation = true;
				}
				if (!needNegotiation && !subscriber && !_this3.pcManager.publisher.isICEConnected && _this3.pcManager.publisher.getICEConnectionState() !== "checking") needNegotiation = true;
				if (needNegotiation) _this3.negotiate().catch((err) => {
					_this3.log.error(err);
				});
				const targetChannel = _this3.dataChannelForKind(kind, subscriber);
				if ((targetChannel === null || targetChannel === void 0 ? void 0 : targetChannel.readyState) === "open") return;
				const endTime = (/* @__PURE__ */ new Date()).getTime() + _this3.peerConnectionTimeout;
				while ((/* @__PURE__ */ new Date()).getTime() < endTime) {
					if (transport.isICEConnected && ((_a = _this3.dataChannelForKind(kind, subscriber)) === null || _a === void 0 ? void 0 : _a.readyState) === "open") return;
					yield sleep(50);
				}
				throw ConnectionError.internal("could not establish ".concat(transportName, " connection, state: ").concat(transport.getICEConnectionState()));
			}();
		});
	}
	ensurePublisherConnected(kind) {
		return __awaiter(this, void 0, void 0, function* () {
			if (!this.publisherConnectionPromise) this.publisherConnectionPromise = this.ensureDataTransportConnected(kind, false);
			yield this.publisherConnectionPromise;
		});
	}
	verifyTransport() {
		if (!this.pcManager) return false;
		const state = this.pcManager.currentState;
		if (![PCTransportState.CONNECTING, PCTransportState.CONNECTED].includes(state)) return false;
		if (!this.client.ws || this.client.ws.readyState === WebSocket.CLOSED) return false;
		if (state === PCTransportState.CONNECTING && this.transportConnectingSince !== void 0 && Date.now() - this.transportConnectingSince > this.peerConnectionTimeout) {
			this.log.warn("transport stuck in connecting state", this.logContext);
			return false;
		}
		return true;
	}
	/** @internal */
	negotiate() {
		return __awaiter(this, void 0, void 0, function* () {
			return new TypedPromise((resolve, reject) => __awaiter(this, void 0, void 0, function* () {
				if (!this.pcManager) {
					reject(new NegotiationError("PC manager is closed"));
					return;
				}
				this.pcManager.requirePublisher();
				if (this.pcManager.publisher.getTransceivers().length == 0 && !this.dataChannels.hasPublisherChannels) this.createDataChannels();
				const abortController = new AbortController();
				const handleClosed = () => {
					abortController.abort();
					this.log.debug("engine disconnected while negotiation was ongoing");
					resolve();
				};
				if (this.isClosed) reject(new NegotiationError("cannot negotiate on closed engine"));
				this.on(EngineEvent.Closing, handleClosed);
				this.on(EngineEvent.Restarting, handleClosed);
				this.pcManager.publisher.off(PCEvents.RTPVideoPayloadTypes, this.onRtpMapAvailable);
				this.pcManager.publisher.once(PCEvents.RTPVideoPayloadTypes, this.onRtpMapAvailable);
				try {
					yield this.pcManager.negotiate(abortController);
					resolve();
				} catch (e) {
					if (abortController.signal.aborted) {
						resolve();
						return;
					}
					if (e instanceof NegotiationError) this.fullReconnectOnNext = true;
					this.handleDisconnect("negotiation", ReconnectReason.RR_UNKNOWN);
					if (e instanceof Error) reject(e);
					else reject(new Error(String(e)));
				} finally {
					this.off(EngineEvent.Closing, handleClosed);
					this.off(EngineEvent.Restarting, handleClosed);
				}
			}));
		});
	}
	dataChannelForKind(kind, sub) {
		return this.dataChannels.getHandle(kind, sub);
	}
	/** @internal */
	sendSyncState(remoteTracks, localTracks, localDataTrackInfos) {
		var _a, _b, _c, _d;
		if (!this.pcManager) {
			this.log.warn("sync state cannot be sent without peer connection setup");
			return;
		}
		const previousPublisherOffer = this.pcManager.publisher.getLocalDescription();
		const previousPublisherAnswer = this.pcManager.publisher.getRemoteDescription();
		const previousSubscriberOffer = (_a = this.pcManager.subscriber) === null || _a === void 0 ? void 0 : _a.getRemoteDescription();
		const previousSubscriberAnswer = (_b = this.pcManager.subscriber) === null || _b === void 0 ? void 0 : _b.getLocalDescription();
		const autoSubscribe = (_d = (_c = this.signalOpts) === null || _c === void 0 ? void 0 : _c.autoSubscribe) !== null && _d !== void 0 ? _d : true;
		const trackSids = new Array();
		const trackSidsDisabled = new Array();
		remoteTracks.forEach((track) => {
			if (track.isDesired !== autoSubscribe) trackSids.push(track.trackSid);
			if (!track.isEnabled) trackSidsDisabled.push(track.trackSid);
		});
		this.client.sendSyncState(new SyncState({
			answer: this.pcManager.mode === "publisher-only" ? previousPublisherAnswer ? toProtoSessionDescription({
				sdp: previousPublisherAnswer.sdp,
				type: previousPublisherAnswer.type
			}) : void 0 : previousSubscriberAnswer ? toProtoSessionDescription({
				sdp: previousSubscriberAnswer.sdp,
				type: previousSubscriberAnswer.type
			}) : void 0,
			offer: this.pcManager.mode === "publisher-only" ? previousPublisherOffer ? toProtoSessionDescription({
				sdp: previousPublisherOffer.sdp,
				type: previousPublisherOffer.type
			}) : void 0 : previousSubscriberOffer ? toProtoSessionDescription({
				sdp: previousSubscriberOffer.sdp,
				type: previousSubscriberOffer.type
			}) : void 0,
			subscription: new UpdateSubscription({
				trackSids,
				subscribe: !autoSubscribe,
				participantTracks: []
			}),
			publishTracks: getTrackPublicationInfo(localTracks),
			dataChannels: this.dataChannelsInfo(),
			trackSidsDisabled,
			datachannelReceiveStates: this.reliableReceivedState.map((seq, sid) => {
				return new DataChannelReceiveState({
					publisherSid: sid,
					lastSeq: seq
				});
			}),
			publishDataTracks: localDataTrackInfos.map((info) => {
				return new PublishDataTrackResponse({ info: DataTrackInfo.toProtobuf(info) });
			})
		}));
	}
	failNext() {
		this.shouldFailNext = true;
	}
	failNextV1Path() {
		this.shouldFailOnV1Path = true;
	}
	dataChannelsInfo() {
		const infos = [];
		const getInfo = (dc, target) => {
			if ((dc === null || dc === void 0 ? void 0 : dc.id) !== void 0 && dc.id !== null) infos.push(new DataChannelInfo({
				label: dc.label,
				id: dc.id,
				target
			}));
		};
		getInfo(this.dataChannelForKind(DataChannelKind.LOSSY), SignalTarget.PUBLISHER);
		getInfo(this.dataChannelForKind(DataChannelKind.RELIABLE), SignalTarget.PUBLISHER);
		getInfo(this.dataChannelForKind(DataChannelKind.LOSSY, true), SignalTarget.SUBSCRIBER);
		getInfo(this.dataChannelForKind(DataChannelKind.RELIABLE, true), SignalTarget.SUBSCRIBER);
		return infos;
	}
	clearReconnectTimeout() {
		if (this.reconnectTimeout) CriticalTimers.clearTimeout(this.reconnectTimeout);
	}
	clearPendingReconnect() {
		this.clearReconnectTimeout();
		this.reconnectAttempts = 0;
	}
	registerOnLineListener() {
		if (isWeb()) {
			window.addEventListener("online", this.handleBrowserOnLine);
			window.addEventListener("offline", this.handleBrowserOffline);
		}
	}
	deregisterOnLineListener() {
		if (isWeb()) {
			window.removeEventListener("online", this.handleBrowserOnLine);
			window.removeEventListener("offline", this.handleBrowserOffline);
		}
	}
	getTrackIdForReceiver(receiver) {
		var _a;
		const mid = (_a = this.pcManager) === null || _a === void 0 ? void 0 : _a.getMidForReceiver(receiver);
		if (mid) {
			const match = Object.entries(this.midToTrackId).find((_ref4) => {
				return _slicedToArray(_ref4, 1)[0] === mid;
			});
			if (match) return match[1];
		}
	}
};
function applyUserDataCompat(newObj, oldObj) {
	const participantIdentity = newObj.participantIdentity ? newObj.participantIdentity : oldObj.participantIdentity;
	newObj.participantIdentity = participantIdentity;
	oldObj.participantIdentity = participantIdentity;
	const destinationIdentities = newObj.destinationIdentities.length !== 0 ? newObj.destinationIdentities : oldObj.destinationIdentities;
	newObj.destinationIdentities = destinationIdentities;
	oldObj.destinationIdentities = destinationIdentities;
}
var log$4 = getLogger(LoggerNames.Region);
var DEFAULT_MAX_AGE_MS = 5e3;
var STOP_REFETCH_DELAY_MS = 3e4;
var RegionUrlProvider = class RegionUrlProvider {
	static fetchRegionSettings(serverUrl, token, signal) {
		return __awaiter(this, void 0, void 0, function* () {
			const unlock = yield RegionUrlProvider.fetchLock.lock();
			try {
				const regionSettingsResponse = yield fetch("".concat(getCloudConfigUrl(serverUrl), "/regions"), {
					headers: { authorization: "Bearer ".concat(token) },
					signal
				});
				if (regionSettingsResponse.ok) {
					const maxAge = extractMaxAgeFromRequestHeaders(regionSettingsResponse.headers);
					const maxAgeInMs = maxAge ? maxAge * 1e3 : DEFAULT_MAX_AGE_MS;
					return {
						regionSettings: yield regionSettingsResponse.json(),
						updatedAtInMs: Date.now(),
						maxAgeInMs
					};
				} else if (regionSettingsResponse.status === 401) throw ConnectionError.notAllowed("Could not fetch region settings: ".concat(regionSettingsResponse.statusText), regionSettingsResponse.status);
				else throw ConnectionError.internal("Could not fetch region settings: ".concat(regionSettingsResponse.statusText));
			} catch (e) {
				if (e instanceof ConnectionError) throw e;
				else if (signal === null || signal === void 0 ? void 0 : signal.aborted) throw ConnectionError.cancelled("Region fetching was aborted");
				else throw ConnectionError.serverUnreachable("Could not fetch region settings, ".concat(e instanceof Error ? "".concat(e.name, ": ").concat(e.message) : e));
			} finally {
				unlock();
			}
		});
	}
	static scheduleRefetch(url, token, maxAgeInMs) {
		return __awaiter(this, void 0, void 0, function* () {
			const timeout = RegionUrlProvider.settingsTimeouts.get(url.hostname);
			clearTimeout(timeout);
			RegionUrlProvider.settingsTimeouts.set(url.hostname, setTimeout(() => __awaiter(this, void 0, void 0, function* () {
				try {
					const newSettings = yield RegionUrlProvider.fetchRegionSettings(url, token);
					RegionUrlProvider.updateCachedRegionSettings(url, token, newSettings);
				} catch (error) {
					if (error instanceof ConnectionError && error.reason === ConnectionErrorReason.NotAllowed) {
						log$4.debug("token is not valid, cancelling auto region refresh");
						return;
					}
					log$4.debug("auto refetching of region settings failed", { error });
					RegionUrlProvider.scheduleRefetch(url, token, maxAgeInMs);
				}
			}), maxAgeInMs));
		});
	}
	static updateCachedRegionSettings(url, token, settings) {
		RegionUrlProvider.cache.set(url.hostname, settings);
		RegionUrlProvider.scheduleRefetch(url, token, settings.maxAgeInMs);
	}
	static stopRefetch(hostname) {
		const timeout = RegionUrlProvider.settingsTimeouts.get(hostname);
		if (timeout) {
			clearTimeout(timeout);
			RegionUrlProvider.settingsTimeouts.delete(hostname);
		}
	}
	static scheduleCleanup(hostname) {
		let tracker = RegionUrlProvider.connectionTrackers.get(hostname);
		if (!tracker) return;
		if (tracker.cleanupTimeout) clearTimeout(tracker.cleanupTimeout);
		tracker.cleanupTimeout = setTimeout(() => {
			const currentTracker = RegionUrlProvider.connectionTrackers.get(hostname);
			if (currentTracker && currentTracker.connectionCount === 0) {
				log$4.debug("stopping region refetch after disconnect delay", { hostname });
				RegionUrlProvider.stopRefetch(hostname);
			}
			if (currentTracker) currentTracker.cleanupTimeout = void 0;
		}, STOP_REFETCH_DELAY_MS);
	}
	static cancelCleanup(hostname) {
		const tracker = RegionUrlProvider.connectionTrackers.get(hostname);
		if (tracker === null || tracker === void 0 ? void 0 : tracker.cleanupTimeout) {
			clearTimeout(tracker.cleanupTimeout);
			tracker.cleanupTimeout = void 0;
		}
	}
	notifyConnected() {
		const hostname = this.serverUrl.hostname;
		let tracker = RegionUrlProvider.connectionTrackers.get(hostname);
		if (!tracker) {
			tracker = { connectionCount: 0 };
			RegionUrlProvider.connectionTrackers.set(hostname, tracker);
		}
		tracker.connectionCount++;
		RegionUrlProvider.cancelCleanup(hostname);
	}
	notifyDisconnected() {
		const hostname = this.serverUrl.hostname;
		const tracker = RegionUrlProvider.connectionTrackers.get(hostname);
		if (!tracker) return;
		tracker.connectionCount = Math.max(0, tracker.connectionCount - 1);
		if (tracker.connectionCount === 0) RegionUrlProvider.scheduleCleanup(hostname);
	}
	constructor(url, token) {
		this.attemptedRegions = [];
		this.serverUrl = new URL(url);
		this.token = token;
	}
	updateToken(token) {
		var _a;
		this.token = token;
		const url = this.getServerUrl();
		const settings = RegionUrlProvider.cache.get(url.hostname);
		RegionUrlProvider.scheduleRefetch(this.serverUrl, this.token, (_a = settings === null || settings === void 0 ? void 0 : settings.maxAgeInMs) !== null && _a !== void 0 ? _a : DEFAULT_MAX_AGE_MS);
	}
	isCloud() {
		return isCloud(this.serverUrl);
	}
	getServerUrl() {
		return this.serverUrl;
	}
	/** @internal */
	fetchRegionSettings(abortSignal) {
		return __awaiter(this, void 0, void 0, function* () {
			return RegionUrlProvider.fetchRegionSettings(this.serverUrl, this.token, abortSignal);
		});
	}
	getNextBestRegionUrl(abortSignal) {
		return __awaiter(this, void 0, void 0, function* () {
			if (!this.isCloud()) throw Error("region availability is only supported for LiveKit Cloud domains");
			let cachedSettings = RegionUrlProvider.cache.get(this.serverUrl.hostname);
			if (!cachedSettings || Date.now() - cachedSettings.updatedAtInMs > cachedSettings.maxAgeInMs) {
				cachedSettings = yield this.fetchRegionSettings(abortSignal);
				RegionUrlProvider.updateCachedRegionSettings(this.serverUrl, this.token, cachedSettings);
			}
			const regionsLeft = cachedSettings.regionSettings.regions.filter((region) => !this.attemptedRegions.find((attempted) => attempted.url === region.url));
			if (regionsLeft.length > 0) {
				const nextRegion = regionsLeft[0];
				this.attemptedRegions.push(nextRegion);
				log$4.info("switching to region: ".concat(nextRegion.region), { region: nextRegion.region });
				return nextRegion.url;
			} else return null;
		});
	}
	resetAttempts() {
		this.attemptedRegions = [];
	}
	setServerReportedRegions(settings) {
		RegionUrlProvider.updateCachedRegionSettings(this.serverUrl, this.token, settings);
	}
};
RegionUrlProvider.cache = /* @__PURE__ */ new Map();
RegionUrlProvider.settingsTimeouts = /* @__PURE__ */ new Map();
RegionUrlProvider.connectionTrackers = /* @__PURE__ */ new Map();
RegionUrlProvider.fetchLock = new _();
function getCloudConfigUrl(serverUrl) {
	return "".concat(serverUrl.protocol.replace("ws", "http"), "//").concat(serverUrl.host, "/settings");
}
function deflateRawTransform() {
	return new CompressionStream("deflate-raw");
}
/**
* A `deflate-raw` decompression transform (inverse of {@link deflateRawTransform}): pipe a
* stream of compressed bytes through it to get the decompressed bytes. Inflate emits output greedily,
* so as long as the sender flushed at write boundaries each write's content is produced as soon as
* its compressed bytes arrive.
*/
function inflateRawTransform() {
	return new DecompressionStream("deflate-raw");
}
/**
* Decompresses a raw-deflate byte array in full (inverse of {@link deflateRawCompress}).
* `maxByteLength` bounds the decompressed output (decompression-bomb guard); exceeding it rejects
* with a `PayloadTooLarge` error.
*/
function deflateRawDecompress(data, maxByteLength) {
	return __awaiter(this, void 0, void 0, function* () {
		const ds = new DecompressionStream("deflate-raw");
		const writer = ds.writable.getWriter();
		writer.write(data).catch(() => {});
		writer.close().catch(() => {});
		return collect(ds.readable, maxByteLength);
	});
}
/**
* Drains a byte stream, concatenating all of its chunks into a single array. When
* `maxByteLength` is given, drops the stream and throws `PayloadTooLarge` as soon as the
* accumulated output exceeds it.
*/
function collect(stream, maxByteLength) {
	return __awaiter(this, void 0, void 0, function* () {
		const reader = stream.getReader();
		const chunks = [];
		let total = 0;
		while (true) {
			const _yield$reader$read = yield reader.read(), done = _yield$reader$read.done, value = _yield$reader$read.value;
			if (done) break;
			chunks.push(value);
			total += value.byteLength;
			if (typeof maxByteLength === "number" && total > maxByteLength) {
				yield reader.cancel();
				throw new DataStreamError("Decompressed payload exceeds the maximum payload size of ".concat(maxByteLength, " bytes"), DataStreamErrorReason.PayloadTooLarge);
			}
		}
		const result = new Uint8Array(total);
		let offset = 0;
		for (const chunk of chunks) {
			result.set(chunk, offset);
			offset += chunk.byteLength;
		}
		return result;
	});
}
var STREAM_CHUNK_SIZE_BYTES = 15e3;
/**
* Default cap on the number of decompressed bytes a single incoming compressed data stream may
* produce (5 GB). A tiny compressed payload can inflate to an arbitrarily large output
* (decompression bomb), so the decompressor's output is bounded rather than trusting the wire
* size; streams exceeding the cap error with `PayloadTooLarge`.
*
* @internal
*/
var DEFAULT_MAX_PAYLOAD_BYTE_LENGTH = 5e9;
var BaseStreamReader = class {
	get info() {
		return this._info;
	}
	/** @internal */
	validateBytesReceived() {
		let doneReceiving = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : false;
		if (typeof this.totalByteSize !== "number" || this.totalByteSize === 0) return;
		if (doneReceiving && this.bytesReceived < this.totalByteSize) throw new DataStreamError("Not enough chunk(s) received - expected ".concat(this.totalByteSize, " bytes of data total, only received ").concat(this.bytesReceived, " bytes"), DataStreamErrorReason.Incomplete);
		else if (this.bytesReceived > this.totalByteSize) throw new DataStreamError("Extra chunk(s) received - expected ".concat(this.totalByteSize, " bytes of data total, received ").concat(this.bytesReceived, " bytes"), DataStreamErrorReason.LengthExceeded);
	}
	constructor(info, stream, totalByteSize) {
		this.reader = stream;
		this.totalByteSize = totalByteSize;
		this._info = info;
		this.bytesReceived = 0;
	}
};
var ByteStreamReader = class extends BaseStreamReader {
	handleChunkReceived(chunk) {
		var _a;
		this.bytesReceived += chunk.content.byteLength;
		this.validateBytesReceived();
		const currentProgress = this.totalByteSize ? this.bytesReceived / this.totalByteSize : void 0;
		(_a = this.onProgress) === null || _a === void 0 || _a.call(this, currentProgress);
	}
	[Symbol.asyncIterator]() {
		const reader = this.reader.getReader();
		reader.closed.catch(() => {});
		const cleanup = () => {
			reader.releaseLock();
			this.signal = void 0;
		};
		return {
			next: () => __awaiter(this, void 0, void 0, function* () {
				var _a;
				try {
					const signal = this.signal;
					if (signal === null || signal === void 0 ? void 0 : signal.aborted) throw signal.reason;
					const result = yield new Promise((resolve, reject) => {
						if (signal) {
							const onAbort = () => reject(signal.reason);
							signal.addEventListener("abort", onAbort, { once: true });
							reader.read().then(resolve, reject).finally(() => {
								signal.removeEventListener("abort", onAbort);
							});
						} else reader.read().then(resolve, reject);
					});
					if (result.done) {
						this.validateBytesReceived(true);
						if (typeof this.totalByteSize === "number") (_a = this.onProgress) === null || _a === void 0 || _a.call(this, 1);
						return {
							done: true,
							value: void 0
						};
					} else {
						this.handleChunkReceived(result.value);
						return {
							done: false,
							value: result.value.content
						};
					}
				} catch (err) {
					cleanup();
					throw err;
				}
			}),
			return() {
				return __awaiter(this, void 0, void 0, function* () {
					cleanup();
					return {
						done: true,
						value: void 0
					};
				});
			}
		};
	}
	/**
	* Injects an AbortSignal, which if aborted, will terminate the currently active
	* stream iteration operation.
	*
	* Note that when using AbortSignal.timeout(...), the timeout applies across
	* the whole iteration operation, not just one individual chunk read.
	*/
	withAbortSignal(signal) {
		this.signal = signal;
		return this;
	}
	readAll() {
		return __awaiter(this, arguments, void 0, function() {
			var _this = this;
			let opts = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
			return function* () {
				var _a, e_1, _b, _c;
				let chunks = /* @__PURE__ */ new Set();
				const iterator = opts.signal ? _this.withAbortSignal(opts.signal) : _this;
				try {
					for (var _d = true, iterator_1 = __asyncValues(iterator), iterator_1_1; iterator_1_1 = yield iterator_1.next(), _a = iterator_1_1.done, !_a; _d = true) {
						_c = iterator_1_1.value;
						_d = false;
						const chunk = _c;
						chunks.add(chunk);
					}
				} catch (e_1_1) {
					e_1 = { error: e_1_1 };
				} finally {
					try {
						if (!_d && !_a && (_b = iterator_1.return)) yield _b.call(iterator_1);
					} finally {
						if (e_1) throw e_1.error;
					}
				}
				return Array.from(chunks);
			}();
		});
	}
};
/**
* A class to read chunks from a ReadableStream and provide them in a structured format.
*/
var TextStreamReader = class extends BaseStreamReader {
	/**
	* A TextStreamReader instance can be used as an AsyncIterator that returns the entire string
	* that has been received up to the current point in time.
	*/
	constructor(info, stream, totalChunkCount) {
		super(info, stream, totalChunkCount);
		this.receivedChunks = /* @__PURE__ */ new Map();
	}
	handleChunkReceived(chunk) {
		var _a;
		const index = bigIntToNumber(chunk.chunkIndex);
		const previousChunkAtIndex = this.receivedChunks.get(index);
		if (previousChunkAtIndex && previousChunkAtIndex.version > chunk.version) return;
		this.receivedChunks.set(index, chunk);
		this.bytesReceived += chunk.content.byteLength;
		this.validateBytesReceived();
		const currentProgress = this.totalByteSize ? this.bytesReceived / this.totalByteSize : void 0;
		(_a = this.onProgress) === null || _a === void 0 || _a.call(this, currentProgress);
	}
	/**
	* Async iterator implementation to allow usage of `for await...of` syntax.
	* Yields structured chunks from the stream.
	*
	*/
	[Symbol.asyncIterator]() {
		const reader = this.reader.getReader();
		reader.closed.catch(() => {});
		const decoder = new TextDecoder("utf-8");
		const signal = this.signal;
		const cleanup = () => {
			reader.releaseLock();
			this.signal = void 0;
		};
		return {
			next: () => __awaiter(this, void 0, void 0, function* () {
				var _a;
				try {
					if (signal === null || signal === void 0 ? void 0 : signal.aborted) throw signal.reason;
					const result = yield new Promise((resolve, reject) => {
						if (signal) {
							const onAbort = () => reject(signal.reason);
							signal.addEventListener("abort", onAbort, { once: true });
							reader.read().then(resolve, reject).finally(() => {
								signal.removeEventListener("abort", onAbort);
							});
						} else reader.read().then(resolve, reject);
					});
					if (result.done) {
						this.validateBytesReceived(true);
						if (typeof this.totalByteSize === "number") (_a = this.onProgress) === null || _a === void 0 || _a.call(this, 1);
						return {
							done: true,
							value: void 0
						};
					} else {
						this.handleChunkReceived(result.value);
						let decodedResult;
						try {
							decodedResult = decoder.decode(result.value.content);
						} catch (err) {
							throw new DataStreamError("Cannot decode datastream chunk ".concat(result.value.chunkIndex, " as text: ").concat(err), DataStreamErrorReason.DecodeFailed);
						}
						return {
							done: false,
							value: decodedResult
						};
					}
				} catch (err) {
					cleanup();
					throw err;
				}
			}),
			return() {
				return __awaiter(this, void 0, void 0, function* () {
					cleanup();
					return {
						done: true,
						value: void 0
					};
				});
			}
		};
	}
	/**
	* Injects an AbortSignal, which if aborted, will terminate the currently active
	* stream iteration operation.
	*
	* Note that when using AbortSignal.timeout(...), the timeout applies across
	* the whole iteration operation, not just one individual chunk read.
	*/
	withAbortSignal(signal) {
		this.signal = signal;
		return this;
	}
	readAll() {
		return __awaiter(this, arguments, void 0, function() {
			var _this2 = this;
			let opts = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
			return function* () {
				var _a, e_2, _b, _c;
				let finalString = "";
				const iterator = opts.signal ? _this2.withAbortSignal(opts.signal) : _this2;
				try {
					for (var _d = true, iterator_2 = __asyncValues(iterator), iterator_2_1; iterator_2_1 = yield iterator_2.next(), _a = iterator_2_1.done, !_a; _d = true) {
						_c = iterator_2_1.value;
						_d = false;
						finalString += _c;
					}
				} catch (e_2_1) {
					e_2 = { error: e_2_1 };
				} finally {
					try {
						if (!_d && !_a && (_b = iterator_2.return)) yield _b.call(iterator_2);
					} finally {
						if (e_2) throw e_2.error;
					}
				}
				return finalString;
			}();
		});
	}
};
var IncomingDataStreamManager = class {
	constructor() {
		let maxPayloadByteLength = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : DEFAULT_MAX_PAYLOAD_BYTE_LENGTH;
		this.log = livekitLogger;
		this.byteStreamControllers = /* @__PURE__ */ new Map();
		this.textStreamControllers = /* @__PURE__ */ new Map();
		this.byteStreamHandlers = /* @__PURE__ */ new Map();
		this.textStreamHandlers = /* @__PURE__ */ new Map();
		this.isConnected = false;
		this.bufferedPackets = [];
		this.maxPayloadByteLength = maxPayloadByteLength;
	}
	setConnected(connected) {
		this.isConnected = connected;
		if (connected) this.flushBufferedPackets();
	}
	flushBufferedPackets() {
		const packets = this.bufferedPackets;
		this.bufferedPackets = [];
		for (const _ref of packets) {
			const packet = _ref.packet;
			const encryptionType = _ref.encryptionType;
			this.handleDataStreamPacket(packet, encryptionType);
		}
	}
	registerTextStreamHandler(topic, callback) {
		if (this.textStreamHandlers.has(topic)) throw new DataStreamError("A text stream handler for topic \"".concat(topic, "\" has already been set."), DataStreamErrorReason.HandlerAlreadyRegistered);
		this.textStreamHandlers.set(topic, callback);
	}
	unregisterTextStreamHandler(topic) {
		this.textStreamHandlers.delete(topic);
	}
	registerByteStreamHandler(topic, callback) {
		if (this.byteStreamHandlers.has(topic)) throw new DataStreamError("A byte stream handler for topic \"".concat(topic, "\" has already been set."), DataStreamErrorReason.HandlerAlreadyRegistered);
		this.byteStreamHandlers.set(topic, callback);
	}
	unregisterByteStreamHandler(topic) {
		this.byteStreamHandlers.delete(topic);
	}
	clearControllers() {
		this.byteStreamControllers.clear();
		this.textStreamControllers.clear();
		this.bufferedPackets = [];
	}
	validateParticipantHasNoActiveDataStreams(participantIdentity) {
		const textStreamsBeingSentByDisconnectingParticipant = Array.from(this.textStreamControllers.entries()).filter((entry) => entry[1].sendingParticipantIdentity === participantIdentity);
		const byteStreamsBeingSentByDisconnectingParticipant = Array.from(this.byteStreamControllers.entries()).filter((entry) => entry[1].sendingParticipantIdentity === participantIdentity);
		if (textStreamsBeingSentByDisconnectingParticipant.length > 0 || byteStreamsBeingSentByDisconnectingParticipant.length > 0) {
			const abnormalEndError = new DataStreamError("Participant ".concat(participantIdentity, " unexpectedly disconnected in the middle of sending data"), DataStreamErrorReason.AbnormalEnd);
			for (const _ref2 of byteStreamsBeingSentByDisconnectingParticipant) {
				var _ref3 = _slicedToArray(_ref2, 2);
				const id = _ref3[0];
				_ref3[1].controller.error(abnormalEndError);
				this.byteStreamControllers.delete(id);
			}
			for (const _ref4 of textStreamsBeingSentByDisconnectingParticipant) {
				var _ref5 = _slicedToArray(_ref4, 2);
				const id = _ref5[0];
				_ref5[1].controller.error(abnormalEndError);
				this.textStreamControllers.delete(id);
			}
		}
	}
	handleDataStreamPacket(packet, encryptionType) {
		if (!this.isConnected) {
			this.bufferedPackets.push({
				packet,
				encryptionType
			});
			return;
		}
		switch (packet.value.case) {
			case "streamHeader": return this.handleStreamHeader(packet.value.value, packet.participantIdentity, encryptionType);
			case "streamChunk": return this.handleStreamChunk(packet.value.value, encryptionType);
			case "streamTrailer": return this.handleStreamTrailer(packet.value.value, encryptionType);
			default: throw new Error("DataPacket of value \"".concat(packet.value.case, "\" is not data stream related!"));
		}
	}
	handleStreamHeader(streamHeader, participantIdentity, encryptionType) {
		var _a;
		switch (streamHeader.contentHeader.case) {
			case "byteHeader": {
				const streamHandlerCallback = this.byteStreamHandlers.get(streamHeader.topic);
				if (!streamHandlerCallback) {
					this.log.debug("ignoring incoming byte stream due to no handler for topic", streamHeader.topic);
					return;
				}
				let streamController;
				const info = {
					id: streamHeader.streamId,
					name: (_a = streamHeader.contentHeader.value.name) !== null && _a !== void 0 ? _a : "unknown",
					mimeType: streamHeader.mimeType,
					size: streamHeader.totalLength ? Number(streamHeader.totalLength) : void 0,
					topic: streamHeader.topic,
					timestamp: bigIntToNumber(streamHeader.timestamp),
					attributes: streamHeader.attributes,
					encryptionType
				};
				let compressed;
				switch (streamHeader.compression) {
					case DataStream_CompressionType.DEFLATE_RAW:
						if (!isCompressionStreamSupported()) {
							livekitLogger.warn("Data stream ".concat(streamHeader.streamId, " received with deflate-raw compression, but this browser does not have support for DecompressionStream. Dropping..."));
							return;
						}
						compressed = true;
						break;
					case DataStream_CompressionType.NONE:
						compressed = false;
						break;
					default:
						livekitLogger.warn("Data stream ".concat(streamHeader.streamId, " received with unknown compression type ").concat(streamHeader.compression, ", dropping..."));
						return;
				}
				const inlineContent = streamHeader.inlineContent;
				if (typeof inlineContent !== "undefined") {
					streamHandlerCallback(new ByteStreamReader(info, createInlineStream(streamHeader.streamId, compressed ? deflateRawDecompress(inlineContent, this.maxPayloadByteLength) : inlineContent), bigIntToNumber(streamHeader.totalLength)), { identity: participantIdentity });
					return;
				}
				const stream = new ReadableStream({ start: (controller) => {
					streamController = controller;
					if (this.byteStreamControllers.has(streamHeader.streamId)) throw new DataStreamError("A data stream read is already in progress for a stream with id ".concat(streamHeader.streamId, "."), DataStreamErrorReason.AlreadyOpened);
					this.byteStreamControllers.set(streamHeader.streamId, {
						info,
						controller: streamController,
						startTime: Date.now(),
						sendingParticipantIdentity: participantIdentity
					});
				} });
				streamHandlerCallback(new ByteStreamReader(info, compressed ? inflateRawByteChunkStream(stream, streamHeader.streamId, this.maxPayloadByteLength) : stream, bigIntToNumber(streamHeader.totalLength)), { identity: participantIdentity });
				return;
			}
			case "textHeader": {
				const streamHandlerCallback = this.textStreamHandlers.get(streamHeader.topic);
				if (!streamHandlerCallback) {
					this.log.debug("ignoring incoming text stream due to no handler for topic", streamHeader.topic);
					return;
				}
				let streamController;
				const info = {
					id: streamHeader.streamId,
					mimeType: streamHeader.mimeType,
					size: streamHeader.totalLength ? Number(streamHeader.totalLength) : void 0,
					topic: streamHeader.topic,
					timestamp: Number(streamHeader.timestamp),
					attributes: streamHeader.attributes,
					encryptionType,
					attachedStreamIds: streamHeader.contentHeader.value.attachedStreamIds
				};
				let compressed;
				switch (streamHeader.compression) {
					case DataStream_CompressionType.DEFLATE_RAW:
						if (!isCompressionStreamSupported()) {
							livekitLogger.warn("Data stream ".concat(streamHeader.streamId, " received with deflate-raw compression, but this browser does not have support for DecompressionStream. Dropping..."));
							return;
						}
						compressed = true;
						break;
					case DataStream_CompressionType.NONE:
						compressed = false;
						break;
					default:
						livekitLogger.warn("Data stream ".concat(streamHeader.streamId, " received with unknown compression type ").concat(streamHeader.compression, ", dropping..."));
						return;
				}
				const inlineContent = streamHeader.inlineContent;
				if (typeof inlineContent !== "undefined") {
					const content = compressed ? deflateRawDecompress(inlineContent, this.maxPayloadByteLength) : inlineContent;
					streamHandlerCallback(new TextStreamReader(info, createInlineStream(streamHeader.streamId, content), bigIntToNumber(streamHeader.totalLength)), { identity: participantIdentity });
					return;
				}
				const stream = new ReadableStream({ start: (controller) => {
					streamController = controller;
					if (this.textStreamControllers.has(streamHeader.streamId)) throw new DataStreamError("A data stream read is already in progress for a stream with id ".concat(streamHeader.streamId, "."), DataStreamErrorReason.AlreadyOpened);
					this.textStreamControllers.set(streamHeader.streamId, {
						info,
						controller: streamController,
						startTime: Date.now(),
						sendingParticipantIdentity: participantIdentity
					});
				} });
				streamHandlerCallback(new TextStreamReader(info, compressed ? inflateRawChunkStream(stream, streamHeader.streamId, this.maxPayloadByteLength) : stream, bigIntToNumber(streamHeader.totalLength)), { identity: participantIdentity });
				return;
			}
		}
	}
	handleStreamChunk(chunk, encryptionType) {
		const fileBuffer = this.byteStreamControllers.get(chunk.streamId);
		if (fileBuffer) {
			if (fileBuffer.info.encryptionType !== encryptionType) {
				fileBuffer.controller.error(new DataStreamError("Encryption type mismatch for stream ".concat(chunk.streamId, ". Expected ").concat(encryptionType, ", got ").concat(fileBuffer.info.encryptionType), DataStreamErrorReason.EncryptionTypeMismatch));
				this.byteStreamControllers.delete(chunk.streamId);
			} else if (chunk.content.length > 0) fileBuffer.controller.enqueue(chunk);
		}
		const textBuffer = this.textStreamControllers.get(chunk.streamId);
		if (textBuffer) {
			if (textBuffer.info.encryptionType !== encryptionType) {
				textBuffer.controller.error(new DataStreamError("Encryption type mismatch for stream ".concat(chunk.streamId, ". Expected ").concat(encryptionType, ", got ").concat(textBuffer.info.encryptionType), DataStreamErrorReason.EncryptionTypeMismatch));
				this.textStreamControllers.delete(chunk.streamId);
			} else if (chunk.content.length > 0) textBuffer.controller.enqueue(chunk);
		}
	}
	handleStreamTrailer(trailer, encryptionType) {
		const textBuffer = this.textStreamControllers.get(trailer.streamId);
		if (textBuffer) {
			if (textBuffer.info.encryptionType !== encryptionType) textBuffer.controller.error(new DataStreamError("Encryption type mismatch for stream ".concat(trailer.streamId, ". Expected ").concat(encryptionType, ", got ").concat(textBuffer.info.encryptionType), DataStreamErrorReason.EncryptionTypeMismatch));
			else {
				textBuffer.info.attributes = Object.assign(Object.assign({}, textBuffer.info.attributes), trailer.attributes);
				if (trailer.reason) textBuffer.controller.error(new DataStreamError("Data stream ".concat(trailer.streamId, " closed abnormally: ").concat(trailer.reason), DataStreamErrorReason.AbnormalEnd));
				else textBuffer.controller.close();
			}
			this.textStreamControllers.delete(trailer.streamId);
		}
		const fileBuffer = this.byteStreamControllers.get(trailer.streamId);
		if (fileBuffer) {
			if (fileBuffer.info.encryptionType !== encryptionType) fileBuffer.controller.error(new DataStreamError("Encryption type mismatch for stream ".concat(trailer.streamId, ". Expected ").concat(encryptionType, ", got ").concat(fileBuffer.info.encryptionType), DataStreamErrorReason.EncryptionTypeMismatch));
			else {
				fileBuffer.info.attributes = Object.assign(Object.assign({}, fileBuffer.info.attributes), trailer.attributes);
				if (trailer.reason) fileBuffer.controller.error(new DataStreamError("Data stream ".concat(trailer.streamId, " closed abnormally: ").concat(trailer.reason), DataStreamErrorReason.AbnormalEnd));
				else fileBuffer.controller.close();
			}
			this.byteStreamControllers.delete(trailer.streamId);
		}
	}
};
/**
* Builds a `ReadableStream` that yields the given content as a single chunk and then immediately
* closes - used to surface an inline (single-packet) data stream as a fully-formed stream. `content`
* may be a promise (e.g. async gzip decompression); a rejection errors the stream.
*/
function createInlineStream(streamId, content) {
	return new ReadableStream({ start: (controller) => __awaiter(this, void 0, void 0, function* () {
		try {
			const bytes = yield content;
			controller.enqueue(new DataStream_Chunk({
				streamId,
				chunkIndex: BigInt(0),
				content: bytes
			}));
			controller.close();
		} catch (err) {
			controller.error(err);
		}
	}) });
}
/**
* Validates that chunks are received in order, dropping duplicates and throwing if gaps are found.
*
* A stateful decompressor silently corrupts on duplicated or out-of-order input, so duplicates are
* dropped (with a warning - in-order delivery is expected on the reliable channel, but reconnect
* handling may replay) and a gap is a hard error. Shared by the text and byte deflate-raw decoders.
*/
function ensureOrderedChunks(streamId) {
	let lastChunkIndex = -1;
	return new TransformStream({ transform: (value, controller) => {
		const index = bigIntToNumber(value.chunkIndex);
		if (index <= lastChunkIndex) {
			livekitLogger.warn("ignoring duplicate chunk ".concat(index, " for compressed data stream ").concat(streamId, " (last processed: ").concat(lastChunkIndex, ")"));
			return;
		}
		if (index > lastChunkIndex + 1) throw new DataStreamError("Missing chunk(s) ".concat(lastChunkIndex + 1, "..").concat(index - 1, " for compressed data stream ").concat(streamId, " - cannot continue decompressing"), DataStreamErrorReason.Incomplete);
		lastChunkIndex = index;
		controller.enqueue(value);
	} });
}
/** Unwraps compressed `DataStream_Chunk`s to their compressed bytes (in `chunkIndex` order), */
function chunksToBytes() {
	return new TransformStream({ transform: (value, controller) => {
		controller.enqueue(value.content);
	} });
}
/** Re-wraps decompressed bytes into contiguous `DataStream_Chunk`s, skipping inflate's empty reads. */
function bytesToChunks(streamId) {
	let outIndex = 0;
	return new TransformStream({ transform: (value, controller) => {
		if (value.byteLength > 0) {
			controller.enqueue(new DataStream_Chunk({
				streamId,
				chunkIndex: numberToBigInt(outIndex),
				content: value
			}));
			outIndex += 1;
		}
	} });
}
/**
* Reframes decompressed byte chunks onto UTF-8 character boundaries via a streaming `TextDecoder`
* (a write larger than the MTU spans several packets, which may split a codepoint), so each
* synthesized text chunk decodes independently. The `flush` emits the decoder's trailing bytes.
*/
function bytesToDecodedUtf8(streamId) {
	const decoder = new TextDecoder("utf-8");
	const encoder = new TextEncoder();
	let outIndex = 0;
	const decodeOrThrow = (bytes) => {
		try {
			return bytes ? decoder.decode(bytes, { stream: true }) : decoder.decode();
		} catch (err) {
			throw new DataStreamError("Cannot decode compressed data stream ".concat(streamId, " as text: ").concat(err), DataStreamErrorReason.DecodeFailed);
		}
	};
	return new TransformStream({
		transform: (value, controller) => {
			const text = decodeOrThrow(value);
			if (text.length > 0) {
				controller.enqueue(new DataStream_Chunk({
					streamId,
					chunkIndex: numberToBigInt(outIndex),
					content: encoder.encode(text)
				}));
				outIndex += 1;
			}
		},
		flush: (controller) => {
			const tail = decodeOrThrow();
			if (tail.length > 0) {
				controller.enqueue(new DataStream_Chunk({
					streamId,
					chunkIndex: numberToBigInt(outIndex),
					content: encoder.encode(tail)
				}));
				outIndex += 1;
			}
		}
	});
}
/**
* Transforms a stream of deflate-raw-compressed byte `DataStream_Chunk`s into a stream of
* decompressed chunks, so `ByteStreamReader` consumes it unchanged. All chunk contents are fed (in
* `chunkIndex` order) through ONE raw-deflate decompressor for the stream's lifetime; decompressed
* output is re-wrapped as chunks as soon as it is produced. The sender (sendFile) compresses the
* whole payload in one shot, but the format also supports a single context-takeover stream
* sync-flushed at write boundaries, so a future incremental streamBytes could compress with no
* protocol change. Errors and cancellation propagate through the pipe chain.
*/
function inflateRawByteChunkStream(raw, streamId, maxPayloadByteLength) {
	return raw.pipeThrough(ensureOrderedChunks(streamId)).pipeThrough(chunksToBytes()).pipeThrough(inflateRawTransform()).pipeThrough(maxDecompressedLengthGuard(streamId, maxPayloadByteLength)).pipeThrough(bytesToChunks(streamId));
}
/**
* Transforms a stream of deflate-raw-compressed text `DataStream_Chunk`s into a stream of
* decompressed chunks, so `TextStreamReader` consumes it unchanged. Builds on
* {@link inflateRawByteChunkStream} (single decompressor + ordering guard) and adds a streaming
* `TextDecoder` that reframes the decompressed bytes on UTF-8 character boundaries so each
* synthesized chunk decodes independently. Errors and cancellation propagate through the pipe chain.
*/
function inflateRawChunkStream(raw, streamId, maxPayloadByteLength) {
	return raw.pipeThrough(ensureOrderedChunks(streamId)).pipeThrough(chunksToBytes()).pipeThrough(inflateRawTransform()).pipeThrough(maxDecompressedLengthGuard(streamId, maxPayloadByteLength)).pipeThrough(bytesToDecodedUtf8(streamId));
}
/**
* Caps the total decompressed byte count of a compressed stream (decompression-bomb guard): a
* tiny compressed payload can inflate to an arbitrarily large output, so the decompressor's
* output is bounded rather than trusting the wire size. Exceeding the cap errors the stream with
* `PayloadTooLarge`.
*/
function maxDecompressedLengthGuard(streamId, maxByteLength) {
	let total = 0;
	return new TransformStream({ transform: (value, controller) => {
		total += value.byteLength;
		if (total > maxByteLength) throw new DataStreamError("Data stream ".concat(streamId, " exceeds the maximum payload size of ").concat(maxByteLength, " bytes"), DataStreamErrorReason.PayloadTooLarge);
		controller.enqueue(value);
	} });
}
var BaseStreamWriter = class {
	constructor(writableStream, info, onClose) {
		this.writableStream = writableStream;
		this.defaultWriter = writableStream.getWriter();
		this.onClose = onClose;
		this.info = info;
	}
	write(chunk) {
		return this.defaultWriter.write(chunk);
	}
	close() {
		return __awaiter(this, void 0, void 0, function* () {
			var _a;
			yield this.defaultWriter.close();
			this.defaultWriter.releaseLock();
			(_a = this.onClose) === null || _a === void 0 || _a.call(this);
		});
	}
};
var TextStreamWriter = class extends BaseStreamWriter {};
var ByteStreamWriter = class extends BaseStreamWriter {};
function buildTextStreamHeader(info, options, v2) {
	var _a;
	return new DataStream_Header({
		streamId: info.id,
		mimeType: info.mimeType,
		topic: info.topic,
		timestamp: numberToBigInt(info.timestamp),
		totalLength: numberToBigInt(info.size),
		attributes: info.attributes,
		compression: (_a = v2 === null || v2 === void 0 ? void 0 : v2.compression) !== null && _a !== void 0 ? _a : DataStream_CompressionType.NONE,
		inlineContent: v2 === null || v2 === void 0 ? void 0 : v2.inlineContent,
		contentHeader: {
			case: "textHeader",
			value: new DataStream_TextHeader({
				version: options === null || options === void 0 ? void 0 : options.version,
				attachedStreamIds: info.attachedStreamIds,
				replyToStreamId: options === null || options === void 0 ? void 0 : options.replyToStreamId,
				operationType: (options === null || options === void 0 ? void 0 : options.type) === "update" ? DataStream_OperationType.UPDATE : DataStream_OperationType.CREATE
			})
		}
	});
}
/** Builds the `DataStream_Header` for a byte stream from its info. */
function buildByteStreamHeader(info, v2) {
	var _a;
	return new DataStream_Header({
		streamId: info.id,
		mimeType: info.mimeType,
		topic: info.topic,
		timestamp: numberToBigInt(info.timestamp),
		totalLength: numberToBigInt(info.size),
		attributes: info.attributes,
		compression: (_a = v2 === null || v2 === void 0 ? void 0 : v2.compression) !== null && _a !== void 0 ? _a : DataStream_CompressionType.NONE,
		inlineContent: v2 === null || v2 === void 0 ? void 0 : v2.inlineContent,
		contentHeader: {
			case: "byteHeader",
			value: new DataStream_ByteHeader({ name: info.name })
		}
	});
}
/** Wraps a `DataStream_Header` in a `DataPacket` ready to be sent over a data channel. */
function createStreamHeaderPacket(header, destinationIdentities) {
	return new DataPacket({
		destinationIdentities,
		value: {
			case: "streamHeader",
			value: header
		}
	});
}
var textEncoder = new TextEncoder();
/**
* Manages sending custom user data via data channels.
* @internal
*/
var OutgoingDataStreamManager = class {
	constructor(engine, log, getRemoteParticipantClientProtocol, getRemoteParticipantCapabilities, getAllRemoteParticipantIdentities) {
		this.engine = engine;
		this.log = log;
		this.getRemoteParticipantClientProtocol = getRemoteParticipantClientProtocol;
		this.getRemoteParticipantCapabilities = getRemoteParticipantCapabilities;
		this.getAllRemoteParticipantIdentities = getAllRemoteParticipantIdentities;
	}
	setupEngine(engine) {
		this.engine = engine;
	}
	/** {@inheritDoc LocalParticipant.sendText} */
	sendText(text, options) {
		return __awaiter(this, void 0, void 0, function* () {
			var _a, _b, _c, _d, _e;
			const streamId = crypto.randomUUID();
			const textInBytes = textEncoder.encode(text);
			const totalTextLength = textInBytes.byteLength;
			const compress = (_a = options === null || options === void 0 ? void 0 : options.compress) !== null && _a !== void 0 ? _a : true;
			let info = {
				id: streamId,
				mimeType: "text/plain",
				timestamp: Date.now(),
				topic: (_b = options === null || options === void 0 ? void 0 : options.topic) !== null && _b !== void 0 ? _b : "",
				size: totalTextLength,
				attributes: options === null || options === void 0 ? void 0 : options.attributes,
				encryptionType: ((_c = this.engine.e2eeManager) === null || _c === void 0 ? void 0 : _c.isDataChannelEncryptionEnabled) ? Encryption_Type.GCM : Encryption_Type.NONE
			};
			let compressedStream = compress && isCompressionStreamSupported() && this.allRecipientsSupportV2(options === null || options === void 0 ? void 0 : options.destinationIdentities) && this.allRecipientsSupportCompression(options === null || options === void 0 ? void 0 : options.destinationIdentities) ? MaybeCollectedStream.fromStream(readableFromBytes(textInBytes).pipeThrough(deflateRawTransform())) : null;
			if ((!(options === null || options === void 0 ? void 0 : options.attachments) || options.attachments.length === 0) && this.allRecipientsSupportV2(options === null || options === void 0 ? void 0 : options.destinationIdentities)) {
				let inlineContent = textInBytes;
				let compression = DataStream_CompressionType.NONE;
				if (compressedStream) {
					const collectedBytes = yield compressedStream.collect();
					if (collectedBytes.byteLength < textInBytes.byteLength) {
						inlineContent = collectedBytes;
						compression = DataStream_CompressionType.DEFLATE_RAW;
					}
				}
				const packet = createStreamHeaderPacket(buildTextStreamHeader(info, void 0, {
					compression,
					inlineContent
				}), options === null || options === void 0 ? void 0 : options.destinationIdentities);
				if (packet.toBinary().byteLength <= STREAM_CHUNK_SIZE_BYTES) {
					yield this.engine.sendDataPacket(packet, DataChannelKind.RELIABLE);
					(_d = options === null || options === void 0 ? void 0 : options.onProgress) === null || _d === void 0 || _d.call(options, 1);
					return info;
				}
			}
			const fileIds = (_e = options === null || options === void 0 ? void 0 : options.attachments) === null || _e === void 0 ? void 0 : _e.map(() => crypto.randomUUID());
			const parts = fileIds ? fileIds.length + 1 : 1;
			const progresses = new Array(parts).fill(0);
			const handleProgress = (progress, idx) => {
				var _a;
				progresses[idx] = progress;
				(_a = options === null || options === void 0 ? void 0 : options.onProgress) === null || _a === void 0 || _a.call(options, progresses.reduce((acc, val) => acc + val, 0) / parts);
			};
			if (compressedStream) {
				info.attachedStreamIds = fileIds;
				const packet = createStreamHeaderPacket(buildTextStreamHeader(info, void 0, { compression: DataStream_CompressionType.DEFLATE_RAW }), options === null || options === void 0 ? void 0 : options.destinationIdentities);
				yield this.sendChunkedByteStream(packet, streamId, options === null || options === void 0 ? void 0 : options.destinationIdentities, compressedStream.stream().pipeThrough(progressReportingStream(textInBytes.length, (progress) => handleProgress(progress, 0))));
				if (textInBytes.length === 0) handleProgress(1, 0);
			} else {
				const writer = yield this.streamText({
					streamId,
					totalSize: totalTextLength,
					destinationIdentities: options === null || options === void 0 ? void 0 : options.destinationIdentities,
					topic: options === null || options === void 0 ? void 0 : options.topic,
					attachedStreamIds: fileIds,
					attributes: options === null || options === void 0 ? void 0 : options.attributes
				});
				yield writer.write(text);
				handleProgress(1, 0);
				yield writer.close();
				info = writer.info;
			}
			if ((options === null || options === void 0 ? void 0 : options.attachments) && fileIds) yield Promise.all(options.attachments.map((file, idx) => __awaiter(this, void 0, void 0, function* () {
				return this._sendFile(fileIds[idx], file, {
					topic: options.topic,
					mimeType: file.type,
					destinationIdentities: options.destinationIdentities,
					compress: options.compress,
					onProgress: (progress) => {
						handleProgress(progress, idx + 1);
					}
				});
			})));
			return info;
		});
	}
	/**
	* Sends a complete in-memory byte payload. Mirrors {@link sendText}'s semantics: when every
	* recipient supports data streams v2 the payload rides inline in a single header packet
	* (optionally deflate-raw compressed), otherwise it is sent as a (optionally compressed)
	* chunked byte stream. Unlike {@link sendFile}, the whole payload is already in memory, so the
	* inline single-packet fast path applies.
	*/
	sendBytes(bytes, options) {
		return __awaiter(this, void 0, void 0, function* () {
			var _a, _b, _c, _d, _e, _f, _g;
			const streamId = crypto.randomUUID();
			const destinationIdentities = options === null || options === void 0 ? void 0 : options.destinationIdentities;
			const compress = (_a = options === null || options === void 0 ? void 0 : options.compress) !== null && _a !== void 0 ? _a : true;
			const info = {
				id: streamId,
				name: (_b = options === null || options === void 0 ? void 0 : options.name) !== null && _b !== void 0 ? _b : "unknown",
				mimeType: (_c = options === null || options === void 0 ? void 0 : options.mimeType) !== null && _c !== void 0 ? _c : "application/octet-stream",
				timestamp: Date.now(),
				topic: (_d = options === null || options === void 0 ? void 0 : options.topic) !== null && _d !== void 0 ? _d : "",
				size: bytes.byteLength,
				attributes: options === null || options === void 0 ? void 0 : options.attributes,
				encryptionType: ((_e = this.engine.e2eeManager) === null || _e === void 0 ? void 0 : _e.isDataChannelEncryptionEnabled) ? Encryption_Type.GCM : Encryption_Type.NONE
			};
			const progressMonitorTap = progressReportingStream(bytes.length, options === null || options === void 0 ? void 0 : options.onProgress);
			let compressedStream = compress && isCompressionStreamSupported() && this.allRecipientsSupportV2(destinationIdentities) && this.allRecipientsSupportCompression(destinationIdentities) ? MaybeCollectedStream.fromStream(readableFromBytes(bytes).pipeThrough(progressMonitorTap).pipeThrough(deflateRawTransform())) : null;
			if (this.allRecipientsSupportV2(destinationIdentities)) {
				let inlineContent = bytes;
				let compression = DataStream_CompressionType.NONE;
				if (compressedStream) {
					const collectedBytes = yield compressedStream.collect();
					if (collectedBytes.byteLength < bytes.byteLength) {
						inlineContent = collectedBytes;
						compression = DataStream_CompressionType.DEFLATE_RAW;
					}
				}
				const packet = createStreamHeaderPacket(buildByteStreamHeader(info, {
					compression,
					inlineContent
				}), destinationIdentities);
				if (packet.toBinary().byteLength <= STREAM_CHUNK_SIZE_BYTES) {
					yield this.engine.sendDataPacket(packet, DataChannelKind.RELIABLE);
					(_f = options === null || options === void 0 ? void 0 : options.onProgress) === null || _f === void 0 || _f.call(options, 1);
					return info;
				}
			}
			const packet = createStreamHeaderPacket(buildByteStreamHeader(info, { compression: compressedStream ? DataStream_CompressionType.DEFLATE_RAW : DataStream_CompressionType.NONE }), destinationIdentities);
			const source = compressedStream ? compressedStream.stream() : readableFromBytes(bytes).pipeThrough(progressMonitorTap);
			yield this.sendChunkedByteStream(packet, streamId, destinationIdentities, source);
			if (bytes.length === 0) (_g = options === null || options === void 0 ? void 0 : options.onProgress) === null || _g === void 0 || _g.call(options, 1);
			return info;
		});
	}
	/**
	* Returns true only if every recipient is known to support data streams v2 (single-packet inline
	* streams and compression). For a targeted send this checks the named destination identities; for
	* a broadcast (no explicit destinations) it checks every remote participant currently in the room.
	* An empty room (nobody to receive) is considered eligible.
	*/
	allRecipientsSupportV2(destinationIdentities) {
		return (destinationIdentities && destinationIdentities.length > 0 ? destinationIdentities : this.getAllRemoteParticipantIdentities()).every((identity) => this.getRemoteParticipantClientProtocol(identity) >= 2);
	}
	/**
	* Returns true only if every recipient advertises the deflate-raw compression capability (so it
	* can decompress a compressed stream). Resolved the same way as {@link allRecipientsSupportV2}:
	* named destinations, or every remote participant for a broadcast; an empty room is eligible.
	*/
	allRecipientsSupportCompression(destinationIdentities) {
		return (destinationIdentities && destinationIdentities.length > 0 ? destinationIdentities : this.getAllRemoteParticipantIdentities()).every((identity) => this.getRemoteParticipantCapabilities(identity).includes(ClientInfo_Capability.CAP_COMPRESSION_DEFLATE_RAW));
	}
	/**
	* Shared chunked-stream send for `sendText`/`sendFile`: sends the prebuilt header packet, then
	* forwards `source` (optionally deflate-raw compressed) as `streamChunk` packets re-chunked to
	* the MTU budget with contiguous indices, then sends the trailer. The source is consumed
	* incrementally, so a `file.stream()` is never buffered in full. The platform compressor can't
	* flush mid-stream, so compression is only used when the whole payload is available as a stream
	* up front (not for incremental writers like `streamText`/`streamBytes`).
	*/
	sendChunkedByteStream(headerPacket, streamId, destinationIdentities, source) {
		return __awaiter(this, void 0, void 0, function* () {
			var _a, e_1, _b, _c;
			const engine = this.engine;
			yield sendHeaderPacket(engine, headerPacket);
			let chunkId = 0;
			try {
				for (var _d = true, _e = __asyncValues(readBytesInChunks(source, STREAM_CHUNK_SIZE_BYTES)), _f; _f = yield _e.next(), _a = _f.done, !_a; _d = true) {
					_c = _f.value;
					_d = false;
					const chunkPacket = new DataPacket({
						destinationIdentities,
						value: {
							case: "streamChunk",
							value: new DataStream_Chunk({
								content: _c,
								streamId,
								chunkIndex: numberToBigInt(chunkId)
							})
						}
					});
					yield engine.sendDataPacket(chunkPacket, DataChannelKind.RELIABLE);
					chunkId += 1;
				}
			} catch (e_1_1) {
				e_1 = { error: e_1_1 };
			} finally {
				try {
					if (!_d && !_a && (_b = _e.return)) yield _b.call(_e);
				} finally {
					if (e_1) throw e_1.error;
				}
			}
			yield sendStreamTrailer(streamId, destinationIdentities, engine);
		});
	}
	/**
	* @internal
	*/
	streamText(options) {
		return __awaiter(this, void 0, void 0, function* () {
			var _a, _b, _c;
			const streamId = (_a = options === null || options === void 0 ? void 0 : options.streamId) !== null && _a !== void 0 ? _a : crypto.randomUUID();
			const destinationIdentities = options === null || options === void 0 ? void 0 : options.destinationIdentities;
			const info = {
				id: streamId,
				mimeType: "text/plain",
				timestamp: Date.now(),
				topic: (_b = options === null || options === void 0 ? void 0 : options.topic) !== null && _b !== void 0 ? _b : "",
				size: options === null || options === void 0 ? void 0 : options.totalSize,
				attributes: options === null || options === void 0 ? void 0 : options.attributes,
				encryptionType: ((_c = this.engine.e2eeManager) === null || _c === void 0 ? void 0 : _c.isDataChannelEncryptionEnabled) ? Encryption_Type.GCM : Encryption_Type.NONE,
				attachedStreamIds: options === null || options === void 0 ? void 0 : options.attachedStreamIds
			};
			const packet = createStreamHeaderPacket(buildTextStreamHeader(info, options), destinationIdentities);
			yield sendHeaderPacket(this.engine, packet);
			let chunkId = 0;
			const engine = this.engine;
			const writableStream = new WritableStream({
				write(text) {
					return __awaiter(this, void 0, void 0, function* () {
						for (const textByteChunk of splitUtf8(text, STREAM_CHUNK_SIZE_BYTES)) {
							const chunk = new DataStream_Chunk({
								content: textByteChunk,
								streamId,
								chunkIndex: numberToBigInt(chunkId)
							});
							const chunkPacket = new DataPacket({
								destinationIdentities,
								value: {
									case: "streamChunk",
									value: chunk
								}
							});
							yield engine.sendDataPacket(chunkPacket, DataChannelKind.RELIABLE);
							chunkId += 1;
						}
					});
				},
				close() {
					return __awaiter(this, void 0, void 0, function* () {
						yield sendStreamTrailer(streamId, destinationIdentities, engine);
					});
				},
				abort(err) {
					console.log("Sink error:", err);
				}
			});
			let onEngineClose = () => __awaiter(this, void 0, void 0, function* () {
				yield writer.close();
			});
			engine.once(EngineEvent.Closing, onEngineClose);
			const writer = new TextStreamWriter(writableStream, info, () => this.engine.off(EngineEvent.Closing, onEngineClose));
			return writer;
		});
	}
	sendFile(file, options) {
		return __awaiter(this, void 0, void 0, function* () {
			const streamId = crypto.randomUUID();
			yield this._sendFile(streamId, file, options);
			return { id: streamId };
		});
	}
	/**
	* Streams a file as a chunked byte stream, compressed (deflate-raw) when the runtime supports it
	* and every recipient is on data streams v2. The file is piped `file.stream()` →
	* (`CompressionStream`) → chunk packets via {@link sendChunkedByteStream}, so it is never fully
	* buffered in memory — unlike {@link sendBytes}, there is no inline single-packet fast path for
	* files (the compressed size can't be known up front without buffering the whole file).
	*/
	_sendFile(streamId, file, options) {
		return __awaiter(this, void 0, void 0, function* () {
			var _a, _b, _c, _d, _e;
			const destinationIdentities = options === null || options === void 0 ? void 0 : options.destinationIdentities;
			const compress = ((_a = options === null || options === void 0 ? void 0 : options.compress) !== null && _a !== void 0 ? _a : true) && isCompressionStreamSupported() && this.allRecipientsSupportV2(destinationIdentities) && this.allRecipientsSupportCompression(destinationIdentities);
			const info = {
				id: streamId,
				name: file.name,
				mimeType: (_b = options === null || options === void 0 ? void 0 : options.mimeType) !== null && _b !== void 0 ? _b : file.type,
				topic: (_c = options === null || options === void 0 ? void 0 : options.topic) !== null && _c !== void 0 ? _c : "",
				timestamp: Date.now(),
				size: file.size,
				encryptionType: ((_d = this.engine.e2eeManager) === null || _d === void 0 ? void 0 : _d.isDataChannelEncryptionEnabled) ? Encryption_Type.GCM : Encryption_Type.NONE
			};
			const packet = createStreamHeaderPacket(buildByteStreamHeader(info, { compression: compress ? DataStream_CompressionType.DEFLATE_RAW : DataStream_CompressionType.NONE }), destinationIdentities);
			const tapped = file.stream().pipeThrough(progressReportingStream(file.size, options === null || options === void 0 ? void 0 : options.onProgress));
			const source = compress ? tapped.pipeThrough(deflateRawTransform()) : tapped;
			yield this.sendChunkedByteStream(packet, streamId, destinationIdentities, source);
			if (file.size === 0) (_e = options === null || options === void 0 ? void 0 : options.onProgress) === null || _e === void 0 || _e.call(options, 1);
			return info;
		});
	}
	streamBytes(options) {
		return __awaiter(this, void 0, void 0, function* () {
			var _a, _b, _c, _d, _e;
			const streamId = (_a = options === null || options === void 0 ? void 0 : options.streamId) !== null && _a !== void 0 ? _a : crypto.randomUUID();
			const destinationIdentities = options === null || options === void 0 ? void 0 : options.destinationIdentities;
			const info = {
				id: streamId,
				mimeType: (_b = options === null || options === void 0 ? void 0 : options.mimeType) !== null && _b !== void 0 ? _b : "application/octet-stream",
				topic: (_c = options === null || options === void 0 ? void 0 : options.topic) !== null && _c !== void 0 ? _c : "",
				timestamp: Date.now(),
				attributes: options === null || options === void 0 ? void 0 : options.attributes,
				size: options === null || options === void 0 ? void 0 : options.totalSize,
				name: (_d = options === null || options === void 0 ? void 0 : options.name) !== null && _d !== void 0 ? _d : "unknown",
				encryptionType: ((_e = this.engine.e2eeManager) === null || _e === void 0 ? void 0 : _e.isDataChannelEncryptionEnabled) ? Encryption_Type.GCM : Encryption_Type.NONE
			};
			const packet = createStreamHeaderPacket(buildByteStreamHeader(info), destinationIdentities);
			yield sendHeaderPacket(this.engine, packet);
			let chunkId = 0;
			const writeMutex = new _();
			const engine = this.engine;
			const logLocal = this.log;
			return new ByteStreamWriter(new WritableStream({
				write(chunk) {
					return __awaiter(this, void 0, void 0, function* () {
						const unlock = yield writeMutex.lock();
						let byteOffset = 0;
						try {
							while (byteOffset < chunk.byteLength) {
								const subChunk = chunk.slice(byteOffset, byteOffset + STREAM_CHUNK_SIZE_BYTES);
								const chunkPacket = new DataPacket({
									destinationIdentities,
									value: {
										case: "streamChunk",
										value: new DataStream_Chunk({
											content: subChunk,
											streamId,
											chunkIndex: numberToBigInt(chunkId)
										})
									}
								});
								yield engine.sendDataPacket(chunkPacket, DataChannelKind.RELIABLE);
								chunkId += 1;
								byteOffset += subChunk.byteLength;
							}
						} finally {
							unlock();
						}
					});
				},
				close() {
					return __awaiter(this, void 0, void 0, function* () {
						yield sendStreamTrailer(streamId, destinationIdentities, engine);
					});
				},
				abort(err) {
					logLocal.error("Sink error:", err);
				}
			}), info);
		});
	}
};
/**
* Wraps a stream of compressed bytes and defers buffering it into memory until (and only if) the
* full length is actually needed.
*
* The ideal way to send a compressed stream is incrementally: compress each chunk and send it as
* soon as it's ready, without ever holding the whole payload in memory. But to decide whether a
* payload is small enough to fit in a single-packet data stream, we have to know the compressed
* length — and the only way to learn that is to compress everything and add up the bytes. That
* forces us to buffer the entire compressed output before we can make the decision.
*
* This class lets us have it both ways. It holds the compressed stream unread by default and only
* collects it into memory ({@link collect}) when a caller needs the length for the single-packet
* size check. If that check never happens, the stream is instead {@link stream|passed straight
* through} to the downstream consumer without ever being fully buffered. Once collected, later
* calls reuse the buffered bytes rather than re-collecting.
*/
var MaybeCollectedStream = class MaybeCollectedStream {
	constructor(state) {
		this.state = state;
	}
	static fromStream(stream) {
		return new MaybeCollectedStream({
			type: "stream",
			stream
		});
	}
	/** Collect data from the stream into memory and return as a Uint8Array. */
	collect() {
		return __awaiter(this, void 0, void 0, function* () {
			switch (this.state.type) {
				case "stream":
					const bytes = yield collect(this.state.stream);
					this.state = {
						type: "collected",
						bytes
					};
					return bytes;
				case "collected": return this.state.bytes;
			}
		});
	}
	/** Pass wrapped stream through to downstream consumer. */
	stream() {
		switch (this.state.type) {
			case "stream": return this.state.stream;
			case "collected": return readableFromBytes(this.state.bytes);
		}
	}
};
/**
* A pass-through byte transform that reports progress as bytes flow through it, measured against
* `totalLength`. Used as a "tap" to instrument the source of a chunked send without threading a
* progress callback through the send primitive.
*
* IMPORTANT: This should be upstream of any compression step.
*
* Emits nothing when the total is unknown or zero.
*/
function progressReportingStream(totalPreCompressionLength, onProgress) {
	let sent = 0;
	return new TransformStream({ transform(chunk, controller) {
		sent += chunk.byteLength;
		if (onProgress && typeof totalPreCompressionLength === "number" && totalPreCompressionLength > 0) onProgress(Math.min(sent / totalPreCompressionLength, 1));
		controller.enqueue(chunk);
	} });
}
/**
* Sends a stream `streamHeader` packet, enforcing that it fits the MTU budget. The header carries
* the user attributes (plus topic/framing), and a single `DataPacket` larger than the MTU can't be
* reliably sent — so an oversized header (almost always due to large attributes) is a hard error
* rather than a malformed packet on the wire. The inline fast path does its own size check and
* falls back to the chunked path instead of calling this.
*/
function sendHeaderPacket(engine, packet) {
	return __awaiter(this, void 0, void 0, function* () {
		if (packet.toBinary().byteLength > STREAM_CHUNK_SIZE_BYTES) throw new DataStreamError("data stream header exceeds the ".concat(STREAM_CHUNK_SIZE_BYTES, "-byte limit; reduce attribute size"), DataStreamErrorReason.HeaderTooLarge);
		yield engine.sendDataPacket(packet, DataChannelKind.RELIABLE);
	});
}
/** Sends a `streamTrailer` packet, marking the end of a stream. */
function sendStreamTrailer(streamId, destinationIdentities, engine) {
	return __awaiter(this, void 0, void 0, function* () {
		const trailerPacket = new DataPacket({
			destinationIdentities,
			value: {
				case: "streamTrailer",
				value: new DataStream_Trailer({ streamId })
			}
		});
		yield engine.sendDataPacket(trailerPacket, DataChannelKind.RELIABLE);
	});
}
function abortSignalAny(signals) {
	if (signals.length === 0) return new AbortController().signal;
	if (signals.length === 1) return signals[0];
	for (const signal of signals) if (signal.aborted) return signal;
	const controller = new AbortController();
	const unlisteners = Array(signals.length);
	const cleanup = () => {
		for (const unsubscribe of unlisteners) unsubscribe();
	};
	signals.forEach((signal, index) => {
		const handler = () => {
			controller.abort(signal.reason);
			cleanup();
		};
		signal.addEventListener("abort", handler);
		unlisteners[index] = () => signal.removeEventListener("abort", handler);
	});
	return controller.signal;
}
/**
* Implementation of AbortSignal.timeout
* Creates a signal that will be aborted after the specified timeout.
* @link https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal/timeout
*/
function abortSignalTimeout(ms) {
	const controller = new AbortController();
	setTimeout(() => {
		controller.abort(new DOMException("signal timed out after ".concat(ms, " ms"), "TimeoutError"));
	}, ms);
	return controller.signal;
}
var U8_LENGTH_BYTES = 1;
var U16_LENGTH_BYTES = 2;
var U32_LENGTH_BYTES = 4;
var U64_LENGTH_BYTES = 8;
var SUPPORTED_VERSION = 0;
var BASE_HEADER_LEN = 12;
var VERSION_SHIFT = 5;
var VERSION_MASK = 7;
var FRAME_MARKER_SHIFT = 3;
var FRAME_MARKER_MASK = 3;
var FRAME_MARKER_START = 2;
var FRAME_MARKER_FINAL = 1;
var FRAME_MARKER_INTER = 0;
var FRAME_MARKER_SINGLE = 3;
var EXT_WORDS_INDICATOR_SIZE = 2;
var EXT_FLAG_SHIFT = 2;
var EXT_FLAG_MASK = 1;
var EXT_TAG_PADDING = 0;
var DataTrackDeserializeErrorReason;
(function(DataTrackDeserializeErrorReason) {
	DataTrackDeserializeErrorReason[DataTrackDeserializeErrorReason["TooShort"] = 0] = "TooShort";
	DataTrackDeserializeErrorReason[DataTrackDeserializeErrorReason["HeaderOverrun"] = 1] = "HeaderOverrun";
	DataTrackDeserializeErrorReason[DataTrackDeserializeErrorReason["MissingExtWords"] = 2] = "MissingExtWords";
	DataTrackDeserializeErrorReason[DataTrackDeserializeErrorReason["UnsupportedVersion"] = 3] = "UnsupportedVersion";
	DataTrackDeserializeErrorReason[DataTrackDeserializeErrorReason["InvalidHandle"] = 4] = "InvalidHandle";
	DataTrackDeserializeErrorReason[DataTrackDeserializeErrorReason["MalformedExt"] = 5] = "MalformedExt";
})(DataTrackDeserializeErrorReason || (DataTrackDeserializeErrorReason = {}));
var DataTrackDeserializeError = class DataTrackDeserializeError extends LivekitReasonedError {
	constructor(message, reason, options) {
		super(19, message, options);
		this.name = "DataTrackDeserializeError";
		this.reason = reason;
		this.reasonName = DataTrackDeserializeErrorReason[reason];
	}
	static tooShort() {
		return new DataTrackDeserializeError("Too short to contain a valid header", DataTrackDeserializeErrorReason.TooShort);
	}
	static headerOverrun() {
		return new DataTrackDeserializeError("Header exceeds total packet length", DataTrackDeserializeErrorReason.HeaderOverrun);
	}
	static missingExtWords() {
		return new DataTrackDeserializeError("Extension word indicator is missing", DataTrackDeserializeErrorReason.MissingExtWords);
	}
	static unsupportedVersion(version) {
		return new DataTrackDeserializeError("Unsupported version ".concat(version), DataTrackDeserializeErrorReason.UnsupportedVersion);
	}
	static invalidHandle(cause) {
		return new DataTrackDeserializeError("invalid track handle: ".concat(cause.message), DataTrackDeserializeErrorReason.InvalidHandle, { cause });
	}
	static malformedExt(tag) {
		return new DataTrackDeserializeError("Extension with tag ".concat(tag, " is malformed"), DataTrackDeserializeErrorReason.MalformedExt);
	}
};
var DataTrackSerializeErrorReason;
(function(DataTrackSerializeErrorReason) {
	DataTrackSerializeErrorReason[DataTrackSerializeErrorReason["TooSmallForHeader"] = 0] = "TooSmallForHeader";
	DataTrackSerializeErrorReason[DataTrackSerializeErrorReason["TooSmallForPayload"] = 1] = "TooSmallForPayload";
})(DataTrackSerializeErrorReason || (DataTrackSerializeErrorReason = {}));
var DataTrackSerializeError = class DataTrackSerializeError extends LivekitReasonedError {
	constructor(message, reason, options) {
		super(19, message, options);
		this.name = "DataTrackSerializeError";
		this.reason = reason;
		this.reasonName = DataTrackSerializeErrorReason[reason];
	}
	static tooSmallForHeader() {
		return new DataTrackSerializeError("Buffer cannot fit header", DataTrackSerializeErrorReason.TooSmallForHeader);
	}
	static tooSmallForPayload() {
		return new DataTrackSerializeError("Buffer cannot fit payload", DataTrackSerializeErrorReason.TooSmallForPayload);
	}
};
var Serializable = class {
	/** Encodes the instance as binary and returns the data as a Uint8Array. */
	toBinary() {
		const lengthBytes = this.toBinaryLengthBytes();
		const output = new ArrayBuffer(lengthBytes);
		const view = new DataView(output);
		const writtenBytes = this.toBinaryInto(view);
		if (lengthBytes !== writtenBytes) throw new Error("".concat(this.constructor.name, ".toBinary: written bytes (").concat(writtenBytes, " bytes) not equal to allocated array buffer length (").concat(lengthBytes, " bytes)."));
		return new Uint8Array(output);
	}
};
var DataTrackExtensionTag;
(function(DataTrackExtensionTag) {
	DataTrackExtensionTag[DataTrackExtensionTag["UserTimestamp"] = 2] = "UserTimestamp";
	DataTrackExtensionTag[DataTrackExtensionTag["E2ee"] = 1] = "E2ee";
})(DataTrackExtensionTag || (DataTrackExtensionTag = {}));
var DataTrackExtension = class extends Serializable {};
var DataTrackUserTimestampExtension = class DataTrackUserTimestampExtension extends DataTrackExtension {
	constructor(timestamp) {
		super();
		this.timestamp = timestamp;
	}
	toBinaryLengthBytes() {
		return 2 + DataTrackUserTimestampExtension.lengthBytes;
	}
	toBinaryInto(dataView) {
		let byteIndex = 0;
		dataView.setUint8(byteIndex, DataTrackUserTimestampExtension.tag);
		byteIndex += U8_LENGTH_BYTES;
		dataView.setUint8(byteIndex, DataTrackUserTimestampExtension.lengthBytes);
		byteIndex += U8_LENGTH_BYTES;
		dataView.setBigUint64(byteIndex, this.timestamp);
		byteIndex += U64_LENGTH_BYTES;
		const totalLengthBytes = this.toBinaryLengthBytes();
		if (byteIndex !== totalLengthBytes) throw new Error("DataTrackUserTimestampExtension.toBinaryInto: Wrote ".concat(byteIndex, " bytes but expected length was ").concat(totalLengthBytes, " bytes"));
		return byteIndex;
	}
	toJSON() {
		return {
			tag: DataTrackUserTimestampExtension.tag,
			lengthBytes: DataTrackUserTimestampExtension.lengthBytes,
			timestamp: this.timestamp
		};
	}
};
DataTrackUserTimestampExtension.tag = DataTrackExtensionTag.UserTimestamp;
DataTrackUserTimestampExtension.lengthBytes = 8;
var DataTrackE2eeExtension = class DataTrackE2eeExtension extends DataTrackExtension {
	constructor(keyIndex, iv) {
		super();
		this.keyIndex = keyIndex;
		this.iv = iv;
	}
	toBinaryLengthBytes() {
		return 2 + DataTrackE2eeExtension.lengthBytes;
	}
	toBinaryInto(dataView) {
		let byteIndex = 0;
		dataView.setUint8(byteIndex, DataTrackE2eeExtension.tag);
		byteIndex += U8_LENGTH_BYTES;
		dataView.setUint8(byteIndex, DataTrackE2eeExtension.lengthBytes);
		byteIndex += U8_LENGTH_BYTES;
		dataView.setUint8(byteIndex, this.keyIndex);
		byteIndex += U8_LENGTH_BYTES;
		for (let i = 0; i < this.iv.length; i += 1) {
			dataView.setUint8(byteIndex, this.iv[i]);
			byteIndex += U8_LENGTH_BYTES;
		}
		const totalLengthBytes = this.toBinaryLengthBytes();
		if (byteIndex !== totalLengthBytes) throw new Error("DataTrackE2eeExtension.toBinaryInto: Wrote ".concat(byteIndex, " bytes but expected length was ").concat(totalLengthBytes, " bytes"));
		return byteIndex;
	}
	toJSON() {
		return {
			tag: DataTrackE2eeExtension.tag,
			lengthBytes: DataTrackE2eeExtension.lengthBytes,
			keyIndex: this.keyIndex,
			iv: this.iv
		};
	}
};
DataTrackE2eeExtension.tag = DataTrackExtensionTag.E2ee;
DataTrackE2eeExtension.lengthBytes = 13;
var DataTrackExtensions = class DataTrackExtensions extends Serializable {
	constructor() {
		let opts = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
		super();
		this.userTimestamp = opts.userTimestamp;
		this.e2ee = opts.e2ee;
	}
	toBinaryLengthBytes() {
		let lengthBytes = 0;
		if (this.userTimestamp) lengthBytes += this.userTimestamp.toBinaryLengthBytes();
		if (this.e2ee) lengthBytes += this.e2ee.toBinaryLengthBytes();
		return lengthBytes;
	}
	toBinaryInto(dataView) {
		let byteIndex = 0;
		if (this.e2ee) {
			const e2eeBytes = this.e2ee.toBinaryInto(dataView);
			byteIndex += e2eeBytes;
		}
		if (this.userTimestamp) {
			const userTimestampBytes = this.userTimestamp.toBinaryInto(new DataView(dataView.buffer, dataView.byteOffset + byteIndex));
			byteIndex += userTimestampBytes;
		}
		const totalLengthBytes = this.toBinaryLengthBytes();
		if (byteIndex !== totalLengthBytes) throw new Error("DataTrackExtensions.toBinaryInto: Wrote ".concat(byteIndex, " bytes but expected length was ").concat(totalLengthBytes, " bytes"));
		return byteIndex;
	}
	static fromBinary(input) {
		const dataView = coerceToDataView(input);
		let userTimestamp;
		let e2ee;
		let byteIndex = 0;
		while (dataView.byteLength - byteIndex >= 2) {
			const tag = dataView.getUint8(byteIndex);
			byteIndex += U8_LENGTH_BYTES;
			const lengthBytes = dataView.getUint8(byteIndex);
			byteIndex += U8_LENGTH_BYTES;
			if (tag === EXT_TAG_PADDING) continue;
			switch (tag) {
				case DataTrackExtensionTag.UserTimestamp:
					if (dataView.byteLength - byteIndex < DataTrackUserTimestampExtension.lengthBytes) throw DataTrackDeserializeError.malformedExt(tag);
					userTimestamp = new DataTrackUserTimestampExtension(dataView.getBigUint64(byteIndex));
					byteIndex += lengthBytes;
					break;
				case DataTrackExtensionTag.E2ee:
					if (dataView.byteLength - byteIndex < DataTrackE2eeExtension.lengthBytes) throw DataTrackDeserializeError.malformedExt(tag);
					const keyIndex = dataView.getUint8(byteIndex);
					const iv = /* @__PURE__ */ new Uint8Array(12);
					for (let i = 0; i < iv.length; i += 1) {
						let byteIndexForIv = byteIndex;
						byteIndexForIv += U8_LENGTH_BYTES;
						byteIndexForIv += i * U8_LENGTH_BYTES;
						iv[i] = dataView.getUint8(byteIndexForIv);
					}
					e2ee = new DataTrackE2eeExtension(keyIndex, iv);
					byteIndex += lengthBytes;
					break;
				default:
					if (dataView.byteLength - byteIndex < lengthBytes) throw DataTrackDeserializeError.malformedExt(tag);
					byteIndex += lengthBytes;
			}
		}
		return [new DataTrackExtensions({
			userTimestamp,
			e2ee
		}), dataView.byteLength];
	}
	toJSON() {
		var _a, _b, _c, _d;
		return {
			userTimestamp: (_b = (_a = this.userTimestamp) === null || _a === void 0 ? void 0 : _a.toJSON()) !== null && _b !== void 0 ? _b : null,
			e2ee: (_d = (_c = this.e2ee) === null || _c === void 0 ? void 0 : _c.toJSON()) !== null && _d !== void 0 ? _d : null
		};
	}
};
var DataTrackFrameInternal = {
	from(frame) {
		return {
			payload: frame.payload,
			extensions: new DataTrackExtensions({ userTimestamp: frame.userTimestamp ? new DataTrackUserTimestampExtension(frame.userTimestamp) : void 0 })
		};
	},
	/** Converts from a DataTrackFrameInternal -> DataTrackFrame. Some internal information is
	* discarded like e2ee encrption extension data. */
	lossyIntoFrame(frame) {
		var _a;
		return {
			payload: frame.payload,
			userTimestamp: (_a = frame.extensions.userTimestamp) === null || _a === void 0 ? void 0 : _a.timestamp
		};
	}
};
var TrackSymbol = Symbol.for("lk.track");
var DataTrackSymbol = Symbol.for("lk.data-track");
var RemoteDataTrack = class {
	/** @internal */
	constructor(info, manager, options) {
		this.trackSymbol = TrackSymbol;
		this.isLocal = false;
		this.typeSymbol = DataTrackSymbol;
		this.info = info;
		this.manager = manager;
		this.publisherIdentity = options.publisherIdentity;
	}
	/** Subscribes to the data track to receive frames.
	*
	* # Returns
	*
	* A stream that yields {@link DataTrackFrame}s as they arrive.
	*
	* # Multiple Subscriptions
	*
	* An application may call `subscribe` more than once to process frames in
	* multiple places. For example, one async task might plot values on a graph
	* while another writes them to a file.
	*
	* Internally, only the first call to `subscribe` communicates with the SFU and
	* allocates the resources required to receive frames. Additional subscriptions
	* reuse the same underlying pipeline and do not trigger additional signaling.
	*
	* Note that newly created subscriptions only receive frames published after
	* the initial subscription is established.
	*/
	subscribe(options) {
		try {
			const _this$manager$openSub2 = _slicedToArray(this.manager.openSubscriptionStream(this.info.sid, options === null || options === void 0 ? void 0 : options.signal, options === null || options === void 0 ? void 0 : options.bufferSize), 2), stream = _this$manager$openSub2[0];
			_this$manager$openSub2[1].catch(() => {});
			return stream;
		} catch (err) {
			throw err;
		}
	}
	/** Configure how incoming frames for this track are processed before they are handed out to
	* subscribers (the "pipeline"). These options apply to all current and future subscriptions
	* of this track, and may be set at any time. */
	setPipelineOptions(options) {
		this.manager.setPipelineOptions(this.info.sid, options);
	}
};
var DataTrackPacketHeader = class DataTrackPacketHeader extends Serializable {
	constructor(opts) {
		var _a;
		super();
		this.marker = opts.marker;
		this.trackHandle = opts.trackHandle;
		this.sequence = opts.sequence;
		this.frameNumber = opts.frameNumber;
		this.timestamp = opts.timestamp;
		this.extensions = (_a = opts.extensions) !== null && _a !== void 0 ? _a : new DataTrackExtensions();
	}
	extensionsMetrics() {
		const lengthBytes = this.extensions.toBinaryLengthBytes();
		const lengthWords = Math.ceil((EXT_WORDS_INDICATOR_SIZE + lengthBytes) / 4);
		return {
			lengthBytes,
			lengthWords,
			paddingLengthBytes: lengthWords * 4 - EXT_WORDS_INDICATOR_SIZE - lengthBytes
		};
	}
	toBinaryLengthBytes() {
		const _this$extensionsMetri = this.extensionsMetrics(), extLengthBytes = _this$extensionsMetri.lengthBytes, extPaddingLengthBytes = _this$extensionsMetri.paddingLengthBytes;
		let totalLengthBytes = BASE_HEADER_LEN;
		if (extLengthBytes > 0) totalLengthBytes += EXT_WORDS_INDICATOR_SIZE + extLengthBytes + extPaddingLengthBytes;
		return totalLengthBytes;
	}
	toBinaryInto(dataView) {
		if (dataView.byteLength < this.toBinaryLengthBytes()) throw DataTrackSerializeError.tooSmallForHeader();
		let initial = 0;
		let marker;
		switch (this.marker) {
			case FrameMarker.Inter:
				marker = FRAME_MARKER_INTER;
				break;
			case FrameMarker.Final:
				marker = FRAME_MARKER_FINAL;
				break;
			case FrameMarker.Start:
				marker = FRAME_MARKER_START;
				break;
			case FrameMarker.Single: marker = FRAME_MARKER_SINGLE;
		}
		initial |= marker << FRAME_MARKER_SHIFT;
		const _this$extensionsMetri2 = this.extensionsMetrics(), extensionsLengthBytes = _this$extensionsMetri2.lengthBytes, extensionsLengthWords = _this$extensionsMetri2.lengthWords, extensionsPaddingLengthBytes = _this$extensionsMetri2.paddingLengthBytes;
		if (extensionsLengthBytes > 0) initial |= 4;
		let byteIndex = 0;
		dataView.setUint8(byteIndex, initial);
		byteIndex += U8_LENGTH_BYTES;
		dataView.setUint8(byteIndex, 0);
		byteIndex += U8_LENGTH_BYTES;
		dataView.setUint16(byteIndex, this.trackHandle);
		byteIndex += U16_LENGTH_BYTES;
		dataView.setUint16(byteIndex, this.sequence.value);
		byteIndex += U16_LENGTH_BYTES;
		dataView.setUint16(byteIndex, this.frameNumber.value);
		byteIndex += U16_LENGTH_BYTES;
		dataView.setUint32(byteIndex, this.timestamp.asTicks());
		byteIndex += U32_LENGTH_BYTES;
		if (extensionsLengthBytes > 0) {
			const rtpOrientedExtensionLengthWords = extensionsLengthWords - 1;
			dataView.setUint16(byteIndex, rtpOrientedExtensionLengthWords);
			byteIndex += U16_LENGTH_BYTES;
			const extensionBytes = this.extensions.toBinaryInto(new DataView(dataView.buffer, dataView.byteOffset + byteIndex));
			byteIndex += extensionBytes;
			for (let i = 0; i < extensionsPaddingLengthBytes; i += 1) {
				dataView.setUint8(byteIndex, 0);
				byteIndex += U8_LENGTH_BYTES;
			}
		}
		const totalLengthBytes = this.toBinaryLengthBytes();
		if (byteIndex !== totalLengthBytes) throw new Error("DataTrackPacketHeader.toBinaryInto: Wrote ".concat(byteIndex, " bytes but expected length was ").concat(totalLengthBytes, " bytes"));
		return totalLengthBytes;
	}
	static fromBinary(input) {
		const dataView = coerceToDataView(input);
		if (dataView.byteLength < BASE_HEADER_LEN) throw DataTrackDeserializeError.tooShort();
		let byteIndex = 0;
		const initial = dataView.getUint8(byteIndex);
		byteIndex += U8_LENGTH_BYTES;
		const version = initial >> VERSION_SHIFT & VERSION_MASK;
		if (version > SUPPORTED_VERSION) throw DataTrackDeserializeError.unsupportedVersion(version);
		let marker;
		switch (initial >> FRAME_MARKER_SHIFT & FRAME_MARKER_MASK) {
			case FRAME_MARKER_START:
				marker = FrameMarker.Start;
				break;
			case FRAME_MARKER_FINAL:
				marker = FrameMarker.Final;
				break;
			case FRAME_MARKER_SINGLE:
				marker = FrameMarker.Single;
				break;
			case FRAME_MARKER_INTER:
			default: marker = FrameMarker.Inter;
		}
		const extensionsFlag = (initial >> EXT_FLAG_SHIFT & EXT_FLAG_MASK) > 0;
		byteIndex += U8_LENGTH_BYTES;
		let trackHandle;
		try {
			trackHandle = DataTrackHandle.fromNumber(dataView.getUint16(byteIndex));
		} catch (e) {
			if (e instanceof DataTrackHandleError && (e.isReason(DataTrackHandleErrorReason.Reserved) || e.isReason(DataTrackHandleErrorReason.TooLarge))) throw DataTrackDeserializeError.invalidHandle(e);
			else throw e;
		}
		byteIndex += U16_LENGTH_BYTES;
		const sequence = WrapAroundUnsignedInt.u16(dataView.getUint16(byteIndex));
		byteIndex += U16_LENGTH_BYTES;
		const frameNumber = WrapAroundUnsignedInt.u16(dataView.getUint16(byteIndex));
		byteIndex += U16_LENGTH_BYTES;
		const timestamp = DataTrackTimestamp.fromRtpTicks(dataView.getUint32(byteIndex));
		byteIndex += U32_LENGTH_BYTES;
		let extensions = new DataTrackExtensions();
		if (extensionsFlag) {
			if (dataView.byteLength - byteIndex < U16_LENGTH_BYTES) throw DataTrackDeserializeError.missingExtWords();
			let rtpOrientedExtensionWords = dataView.getUint16(byteIndex);
			byteIndex += U16_LENGTH_BYTES;
			let extensionLengthBytes = 4 * (rtpOrientedExtensionWords + 1) - EXT_WORDS_INDICATOR_SIZE;
			if (byteIndex + extensionLengthBytes > dataView.byteLength) throw DataTrackDeserializeError.headerOverrun();
			let extensionDataView = new DataView(dataView.buffer, dataView.byteOffset + byteIndex, extensionLengthBytes);
			const _DataTrackExtensions$2 = _slicedToArray(DataTrackExtensions.fromBinary(extensionDataView), 2), result = _DataTrackExtensions$2[0], readBytes = _DataTrackExtensions$2[1];
			extensions = result;
			byteIndex += readBytes;
		}
		return [new DataTrackPacketHeader({
			marker,
			trackHandle,
			sequence,
			frameNumber,
			timestamp,
			extensions
		}), byteIndex];
	}
	toJSON() {
		return {
			marker: this.marker,
			trackHandle: this.trackHandle,
			sequence: this.sequence.value,
			frameNumber: this.frameNumber.value,
			timestamp: this.timestamp.asTicks(),
			extensions: this.extensions.toJSON()
		};
	}
};
/** Marker indicating a packet's position in relation to a frame. */
var FrameMarker;
(function(FrameMarker) {
	/** Packet is the first in a frame. */
	FrameMarker[FrameMarker["Start"] = 0] = "Start";
	/** Packet is within a frame. */
	FrameMarker[FrameMarker["Inter"] = 1] = "Inter";
	/** Packet is the last in a frame. */
	FrameMarker[FrameMarker["Final"] = 2] = "Final";
	/** Packet is the only one in a frame. */
	FrameMarker[FrameMarker["Single"] = 3] = "Single";
})(FrameMarker || (FrameMarker = {}));
/** A class for serializing / deserializing data track packets. */
var DataTrackPacket = class DataTrackPacket extends Serializable {
	constructor(header, payload) {
		super();
		this.header = header;
		this.payload = payload;
	}
	toBinaryLengthBytes() {
		return this.header.toBinaryLengthBytes() + this.payload.byteLength;
	}
	toBinaryInto(dataView) {
		let byteIndex = 0;
		const headerLengthBytes = this.header.toBinaryInto(dataView);
		byteIndex += headerLengthBytes;
		if (dataView.byteLength - byteIndex < this.payload.byteLength) throw DataTrackSerializeError.tooSmallForPayload();
		for (let index = 0; index < this.payload.length; index += 1) {
			dataView.setUint8(byteIndex, this.payload[index]);
			byteIndex += U8_LENGTH_BYTES;
		}
		const totalLengthBytes = this.toBinaryLengthBytes();
		if (byteIndex !== totalLengthBytes) throw new Error("DataTrackPacket.toBinaryInto: Wrote ".concat(byteIndex, " bytes but expected length was ").concat(totalLengthBytes, " bytes"));
		return totalLengthBytes;
	}
	static fromBinary(input) {
		const dataView = coerceToDataView(input);
		const _DataTrackPacketHeade2 = _slicedToArray(DataTrackPacketHeader.fromBinary(dataView), 2), header = _DataTrackPacketHeade2[0], headerByteLength = _DataTrackPacketHeade2[1];
		const payload = dataView.buffer.slice(dataView.byteOffset + headerByteLength, dataView.byteOffset + dataView.byteLength);
		return [new DataTrackPacket(header, new Uint8Array(payload)), dataView.byteLength];
	}
	toJSON() {
		return {
			header: this.header.toJSON(),
			payload: this.payload
		};
	}
};
var log$3 = getLogger(LoggerNames.DataTracks);
/** An error indicating a frame was dropped. */
var DataTrackDepacketizerDropError = class DataTrackDepacketizerDropError extends LivekitReasonedError {
	constructor(message, reason, frameNumber, options) {
		super(19, "Frame ".concat(frameNumber, " dropped: ").concat(message), options);
		this.name = "DataTrackDepacketizerDropError";
		this.reason = reason;
		this.reasonName = DataTrackDepacketizerDropReason[reason];
		this.frameNumber = frameNumber;
	}
	static interrupted(frameNumber, newFrameNumber) {
		return new DataTrackDepacketizerDropError("Interrupted by the start of a new frame ".concat(newFrameNumber), DataTrackDepacketizerDropReason.Interrupted, frameNumber);
	}
	static unknownFrame(frameNumber) {
		return new DataTrackDepacketizerDropError("Initial packet was never received.", DataTrackDepacketizerDropReason.UnknownFrame, frameNumber);
	}
	static bufferFull(frameNumber) {
		return new DataTrackDepacketizerDropError("Reorder buffer is full.", DataTrackDepacketizerDropReason.BufferFull, frameNumber);
	}
	static incomplete(frameNumber, receivedPackets, expectedPackets) {
		return new DataTrackDepacketizerDropError("Not all packets received before final packet. Received ".concat(receivedPackets, " packets, expected ").concat(expectedPackets, " packets."), DataTrackDepacketizerDropReason.Incomplete, frameNumber);
	}
};
/** Reason why a frame was dropped. */
var DataTrackDepacketizerDropReason;
(function(DataTrackDepacketizerDropReason) {
	DataTrackDepacketizerDropReason[DataTrackDepacketizerDropReason["Interrupted"] = 0] = "Interrupted";
	DataTrackDepacketizerDropReason[DataTrackDepacketizerDropReason["UnknownFrame"] = 1] = "UnknownFrame";
	DataTrackDepacketizerDropReason[DataTrackDepacketizerDropReason["BufferFull"] = 2] = "BufferFull";
	DataTrackDepacketizerDropReason[DataTrackDepacketizerDropReason["Incomplete"] = 3] = "Incomplete";
})(DataTrackDepacketizerDropReason || (DataTrackDepacketizerDropReason = {}));
var DataTrackDepacketizer = class DataTrackDepacketizer {
	constructor() {
		/** Partial frames currently being assembled, keyed by frame number. `Map` preserves insertion
		* order, so the oldest entry is the first key. */
		this.partials = /* @__PURE__ */ new Map();
	}
	/** Should be repeatedly called with received {@link DataTrackPacket}s - intermediate calls
	* aggregate the packet's state internally, and return null.
	*
	* Once this method is called with the final packet to form a frame, a new {@link DataTrackFrameInternal}
	* is returned.*/
	push(packet, options) {
		switch (packet.header.marker) {
			case FrameMarker.Single: return this.frameFromSingle(packet, options);
			case FrameMarker.Start: return this.beginPartial(packet, options);
			case FrameMarker.Inter:
			case FrameMarker.Final: return this.pushToPartial(packet);
		}
	}
	reset() {
		this.partials.clear();
	}
	peekOldestPartialFrameNumber() {
		const first = this.partials.keys().next();
		return first.done ? null : first.value;
	}
	frameFromSingle(packet, options) {
		var _a;
		if (packet.header.marker !== FrameMarker.Single) throw new Error("Depacketizer.frameFromSingle: packet.header.marker was not FrameMarker.Single, found ".concat(packet.header.marker, "."));
		const maxPartialFrames = (_a = options === null || options === void 0 ? void 0 : options.maxPartialFrames) !== null && _a !== void 0 ? _a : 1;
		if (this.partials.size >= maxPartialFrames) {
			const oldestPartialFrameNumber = this.peekOldestPartialFrameNumber();
			if (typeof oldestPartialFrameNumber !== "number") throw new Error("Depacketizer.frameFromSingle: no oldest frame number found, but partials.size is ".concat(this.partials.size, "."));
			this.partials.delete(oldestPartialFrameNumber);
			if (options === null || options === void 0 ? void 0 : options.throwOnInterruption) throw DataTrackDepacketizerDropError.interrupted(oldestPartialFrameNumber, packet.header.frameNumber.value);
			log$3.warn("Data track frame ".concat(oldestPartialFrameNumber, " was interrupted by single-packet frame ").concat(packet.header.frameNumber.value, ", dropping."));
		}
		return {
			payload: packet.payload,
			extensions: packet.header.extensions
		};
	}
	/** Begin assembling a new packet. */
	beginPartial(packet, options) {
		var _a;
		if (packet.header.marker !== FrameMarker.Start) throw new Error("Depacketizer.beginPartial: packet.header.marker was not FrameMarker.Start, found ".concat(packet.header.marker, "."));
		const startSequence = packet.header.sequence;
		const frameNumber = packet.header.frameNumber.value;
		const partial = {
			startSequence,
			extensions: packet.header.extensions,
			payloads: /* @__PURE__ */ new Map([[startSequence.value, packet.payload]])
		};
		const maxPartialFrames = (_a = options === null || options === void 0 ? void 0 : options.maxPartialFrames) !== null && _a !== void 0 ? _a : 1;
		while (this.partials.size >= maxPartialFrames) {
			const oldestPartialFrameNumber = this.peekOldestPartialFrameNumber();
			if (typeof oldestPartialFrameNumber !== "number") break;
			this.partials.delete(oldestPartialFrameNumber);
			if (options === null || options === void 0 ? void 0 : options.throwOnInterruption) throw DataTrackDepacketizerDropError.interrupted(oldestPartialFrameNumber, frameNumber);
			log$3.warn("Data track partials full (max ".concat(maxPartialFrames, "), evicted oldest frame ").concat(oldestPartialFrameNumber, " to make room for new frame ").concat(frameNumber, "."));
		}
		this.partials.set(frameNumber, partial);
		return null;
	}
	/** Push to the existing partial frame. */
	pushToPartial(packet) {
		if (packet.header.marker !== FrameMarker.Inter && packet.header.marker !== FrameMarker.Final) throw new Error("Depacketizer.pushToPartial: packet.header.marker was not FrameMarker.Inter or FrameMarker.Final, found ".concat(packet.header.marker, "."));
		const packetFrameNumber = packet.header.frameNumber.value;
		const matchingPartial = this.partials.get(packetFrameNumber);
		if (!matchingPartial) {
			this.partials.delete(packetFrameNumber);
			throw DataTrackDepacketizerDropError.unknownFrame(packetFrameNumber);
		}
		if (matchingPartial.payloads.size >= DataTrackDepacketizer.MAX_BUFFER_PACKETS) {
			this.partials.delete(packetFrameNumber);
			throw DataTrackDepacketizerDropError.bufferFull(packetFrameNumber);
		}
		if (matchingPartial.payloads.has(packet.header.sequence.value)) log$3.warn("Data track frame ".concat(packetFrameNumber, " received duplicate packet for sequence ").concat(packet.header.sequence.value, ", so replacing with newly received packet."));
		matchingPartial.payloads.set(packet.header.sequence.value, packet.payload);
		if (packet.header.marker === FrameMarker.Final) return this.finalize(packetFrameNumber, matchingPartial, packet.header.sequence.value);
		return null;
	}
	/** Try to reassemble the complete frame. */
	finalize(partialFrameNumber, partial, endSequence) {
		const received = partial.payloads.size;
		let payloadLengthBytes = 0;
		for (const p of partial.payloads.values()) payloadLengthBytes += p.length;
		const payload = new Uint8Array(payloadLengthBytes);
		let sequencePointer = partial.startSequence.clone();
		let payloadOffsetPointerBytes = 0;
		while (true) {
			const partialPayload = partial.payloads.get(sequencePointer.value);
			if (!partialPayload) break;
			partial.payloads.delete(sequencePointer.value);
			const payloadRemainingBytes = payload.length - payloadOffsetPointerBytes;
			if (partialPayload.length > payloadRemainingBytes) throw new Error("Depacketizer.finalize: Expected at least ".concat(partialPayload.length, " more bytes left in the payload buffer, only got ").concat(payloadRemainingBytes, " bytes."));
			payload.set(partialPayload, payloadOffsetPointerBytes);
			payloadOffsetPointerBytes += partialPayload.length;
			if (sequencePointer.value != endSequence) {
				sequencePointer.increment();
				continue;
			}
			this.partials.delete(partialFrameNumber);
			return {
				payload,
				extensions: partial.extensions
			};
		}
		this.partials.delete(partialFrameNumber);
		throw DataTrackDepacketizerDropError.incomplete(partialFrameNumber, received, endSequence - partial.startSequence.value + 1);
	}
};
/** Maximum number of packets to buffer per frame before dropping. */
DataTrackDepacketizer.MAX_BUFFER_PACKETS = 128;
var DataTrackSubscribeErrorReason;
(function(DataTrackSubscribeErrorReason) {
	/** The track has been unpublished and is no longer available */
	DataTrackSubscribeErrorReason[DataTrackSubscribeErrorReason["Unpublished"] = 0] = "Unpublished";
	/** Request to subscribe to data track timed-out */
	DataTrackSubscribeErrorReason[DataTrackSubscribeErrorReason["Timeout"] = 1] = "Timeout";
	/** Cannot subscribe to data track when disconnected */
	DataTrackSubscribeErrorReason[DataTrackSubscribeErrorReason["Disconnected"] = 2] = "Disconnected";
	/** Subscription to data track cancelled by caller */
	DataTrackSubscribeErrorReason[DataTrackSubscribeErrorReason["Cancelled"] = 4] = "Cancelled";
})(DataTrackSubscribeErrorReason || (DataTrackSubscribeErrorReason = {}));
var DataTrackSubscribeError = class DataTrackSubscribeError extends LivekitReasonedError {
	constructor(message, reason, options) {
		super(22, message, options);
		this.name = "DataTrackSubscribeError";
		this.reason = reason;
		this.reasonName = DataTrackSubscribeErrorReason[reason];
	}
	static unpublished() {
		return new DataTrackSubscribeError("The track has been unpublished and is no longer available", DataTrackSubscribeErrorReason.Unpublished);
	}
	static timeout() {
		return new DataTrackSubscribeError("Request to subscribe to data track timed-out", DataTrackSubscribeErrorReason.Timeout);
	}
	static disconnected() {
		return new DataTrackSubscribeError("Cannot subscribe to data track when disconnected", DataTrackSubscribeErrorReason.Disconnected);
	}
	static cancelled() {
		return new DataTrackSubscribeError("Subscription to data track cancelled by caller", DataTrackSubscribeErrorReason.Cancelled);
	}
};
var log$2 = getLogger(LoggerNames.DataTracks);
/**
* Pipeline for an individual data track subscription.
*/
var IncomingDataTrackPipeline = class {
	/**
	* Creates a new pipeline with the given options.
	*/
	constructor(options) {
		var _a, _b;
		const hasProvider = options.e2eeManager !== null;
		if (options.info.usesE2ee !== hasProvider) throw new Error("IncomingDataTrackPipeline: DataTrackInfo.usesE2ee must match presence of decryptionProvider");
		const depacketizer = new DataTrackDepacketizer();
		this.publisherIdentity = options.publisherIdentity;
		this.e2eeManager = (_a = options.e2eeManager) !== null && _a !== void 0 ? _a : null;
		this.depacketizer = depacketizer;
		this.options = (_b = options.pipelineOptions) !== null && _b !== void 0 ? _b : {};
	}
	updateE2eeManager(e2eeManager) {
		this.e2eeManager = e2eeManager;
	}
	setOptions(options) {
		this.options = options;
	}
	processPacket(packet) {
		return __awaiter(this, void 0, void 0, function* () {
			const frame = this.depacketize(packet);
			if (!frame) return null;
			const decrypted = yield this.decryptIfNeeded(frame);
			if (!decrypted) return null;
			return decrypted;
		});
	}
	/**
	* Depacketize the given frame, log if a drop occurs.
	*/
	depacketize(packet) {
		let frame;
		try {
			frame = this.depacketizer.push(packet, {
				throwOnInterruption: false,
				maxPartialFrames: this.options.maxPartialFrames
			});
		} catch (err) {
			log$2.warn("Data frame depacketize error: ".concat(err));
			return null;
		}
		return frame;
	}
	/**
	* Decrypt the frame's payload if E2EE is enabled for this track.
	*/
	decryptIfNeeded(frame) {
		return __awaiter(this, void 0, void 0, function* () {
			var _a, _b;
			const e2eeManager = this.e2eeManager;
			if (!e2eeManager) return frame;
			const e2ee = (_b = (_a = frame.extensions) === null || _a === void 0 ? void 0 : _a.e2ee) !== null && _b !== void 0 ? _b : null;
			if (!e2ee) {
				log$2.error("Missing E2EE meta");
				return null;
			}
			let result;
			try {
				result = yield e2eeManager.handleEncryptedData(frame.payload, e2ee.iv, this.publisherIdentity, e2ee.keyIndex);
			} catch (err) {
				log$2.error("Error decrypting packet: ".concat(err));
				return null;
			}
			frame.payload = result.payload;
			return frame;
		});
	}
};
var log$1 = getLogger(LoggerNames.DataTracks);
/** How long to wait when attempting to subscribe before timing out. */
var SUBSCRIBE_TIMEOUT_MILLISECONDS = 1e4;
/** Maximum number of {@link DataTrackFrame}s that are cached for each ReadableStream subscription.
* If data comes in too fast and saturates this threshold, backpressure will be applied. */
var READABLE_STREAM_DEFAULT_BUFFER_SIZE = 16;
var IncomingDataTrackManager = class extends eventsExports.EventEmitter {
	constructor(options) {
		var _a;
		super();
		/** Mapping between track SID and descriptor. */
		this.descriptors = /* @__PURE__ */ new Map();
		/** Mapping between subscriber handle and track SID.
		*
		* This is an index that allows track descriptors to be looked up
		* by subscriber handle in O(1) time, to make routing incoming packets
		* (a hot code path) faster.
		*/
		this.subscriptionHandles = /* @__PURE__ */ new Map();
		this.e2eeManager = (_a = options === null || options === void 0 ? void 0 : options.e2eeManager) !== null && _a !== void 0 ? _a : null;
	}
	/** @internal */
	updateE2eeManager(e2eeManager) {
		this.e2eeManager = e2eeManager;
		for (const descriptor of this.descriptors.values()) if (descriptor.subscription.type === "active") descriptor.subscription.pipeline.updateE2eeManager(e2eeManager);
	}
	/** @internal */
	setPipelineOptions(sid, options) {
		const descriptor = this.descriptors.get(sid);
		if (!descriptor) {
			log$1.warn("Unknown track ".concat(sid, ", cannot set pipeline options."));
			return;
		}
		descriptor.pipelineOptions = options;
		if (descriptor.subscription.type === "active") descriptor.subscription.pipeline.setOptions(options);
	}
	/** Allocates a ReadableStream which emits when a new {@link DataTrackFrame} is received from the
	* SFU. The SFU subscription is initiated lazily when the stream is created.
	*
	* @returns A tuple of the ReadableStream and a Promise that resolves once the SFU subscription
	* is fully established / the stream is ready to receive frames.
	*
	* @internal
	**/
	openSubscriptionStream(sid, signal) {
		let bufferSize = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : READABLE_STREAM_DEFAULT_BUFFER_SIZE;
		let streamController = null;
		const sfuSubscriptionComplete = new Future();
		const descriptor = this.descriptors.get(sid);
		const detachSignal = () => {
			signal === null || signal === void 0 || signal.removeEventListener("abort", onAbort);
		};
		const cleanup = () => {
			detachSignal();
			if (!streamController) {
				log$1.warn("ReadableStream subscribed to ".concat(sid, " was not started."));
				return;
			}
			if (!descriptor || this.descriptors.get(descriptor.info.sid) !== descriptor) {
				log$1.warn("Unknown track ".concat(sid, ", skipping cancel..."));
				return;
			}
			if (descriptor.subscription.type !== "active") {
				log$1.warn("Subscription for track ".concat(sid, " is not active, skipping cancel..."));
				return;
			}
			descriptor.subscription.streamControllers.delete(streamController);
			if (descriptor.subscription.streamControllers.size === 0) this.unSubscribeRequest(descriptor.info.sid);
		};
		const onAbort = () => {
			var _a;
			if (!streamController) return;
			if ((descriptor === null || descriptor === void 0 ? void 0 : descriptor.subscription.type) === "active") descriptor.subscription.streamControllers.delete(streamController);
			streamController.error(DataTrackSubscribeError.cancelled());
			(_a = sfuSubscriptionComplete.reject) === null || _a === void 0 || _a.call(sfuSubscriptionComplete, DataTrackSubscribeError.cancelled());
			cleanup();
		};
		return [new ReadableStream({
			start: (controller) => {
				streamController = controller;
				this.subscribeRequest(sid, signal).then(() => __awaiter(this, void 0, void 0, function* () {
					var _a, _b, _c;
					if (!descriptor || this.descriptors.get(descriptor.info.sid) !== descriptor) {
						log$1.error("Unknown track ".concat(sid));
						const err = DataTrackSubscribeError.disconnected();
						controller.error(err);
						(_a = sfuSubscriptionComplete.reject) === null || _a === void 0 || _a.call(sfuSubscriptionComplete, err);
						return;
					}
					if (descriptor.subscription.type !== "active") {
						log$1.error("Subscription for track ".concat(sid, " is not active"));
						const err = DataTrackSubscribeError.disconnected();
						controller.error(err);
						(_b = sfuSubscriptionComplete.reject) === null || _b === void 0 || _b.call(sfuSubscriptionComplete, err);
						return;
					}
					if (signal === null || signal === void 0 ? void 0 : signal.aborted) {
						onAbort();
						return;
					}
					signal === null || signal === void 0 || signal.addEventListener("abort", onAbort);
					descriptor.subscription.streamControllers.set(controller, detachSignal);
					(_c = sfuSubscriptionComplete.resolve) === null || _c === void 0 || _c.call(sfuSubscriptionComplete);
				})).catch((err) => {
					var _a;
					controller.error(err);
					(_a = sfuSubscriptionComplete.reject) === null || _a === void 0 || _a.call(sfuSubscriptionComplete, err);
				});
			},
			cancel: () => {
				cleanup();
			}
		}, new CountQueuingStrategy({ highWaterMark: bufferSize })), sfuSubscriptionComplete.promise];
	}
	/** Client requested to subscribe to a data track.
	*
	* This is sent when the user calls {@link RemoteDataTrack.subscribe}.
	*
	* Only the first request to subscribe to a given track incurs meaningful overhead; subsequent
	* requests simply attach an additional receiver to the broadcast channel, allowing them to consume
	* frames from the existing subscription pipeline.
	*/
	subscribeRequest(sid, signal) {
		return __awaiter(this, void 0, void 0, function* () {
			const descriptor = this.descriptors.get(sid);
			if (!descriptor) throw new Error("Cannot subscribe to unknown track");
			const waitForCompletionFuture = (currentDescriptor, userProvidedSignal, timeoutSignal) => __awaiter(this, void 0, void 0, function* () {
				if (currentDescriptor.subscription.type === "active") return;
				if (currentDescriptor.subscription.type !== "pending") throw new Error("Descriptor for track ".concat(sid, " is not pending, found ").concat(currentDescriptor.subscription.type));
				const combinedSignal = abortSignalAny([userProvidedSignal, timeoutSignal].filter((s) => typeof s !== "undefined"));
				const proxiedCompletionFuture = new Future();
				currentDescriptor.subscription.completionFuture.promise.then(() => {
					var _a;
					return (_a = proxiedCompletionFuture.resolve) === null || _a === void 0 ? void 0 : _a.call(proxiedCompletionFuture);
				}).catch((err) => {
					var _a;
					return (_a = proxiedCompletionFuture.reject) === null || _a === void 0 ? void 0 : _a.call(proxiedCompletionFuture, err);
				});
				const onAbort = () => {
					var _a;
					if (currentDescriptor.subscription.type !== "pending") return;
					currentDescriptor.subscription.pendingRequestCount -= 1;
					if (timeoutSignal === null || timeoutSignal === void 0 ? void 0 : timeoutSignal.aborted) {
						currentDescriptor.subscription.cancel();
						return;
					}
					if (currentDescriptor.subscription.pendingRequestCount <= 0) {
						currentDescriptor.subscription.cancel();
						return;
					}
					(_a = proxiedCompletionFuture.reject) === null || _a === void 0 || _a.call(proxiedCompletionFuture, DataTrackSubscribeError.cancelled());
				};
				if (combinedSignal.aborted) onAbort();
				combinedSignal.addEventListener("abort", onAbort);
				yield proxiedCompletionFuture.promise;
				combinedSignal.removeEventListener("abort", onAbort);
			});
			switch (descriptor.subscription.type) {
				case "none": {
					descriptor.subscription = {
						type: "pending",
						completionFuture: new Future(),
						pendingRequestCount: 1,
						cancel: () => {
							var _a, _b;
							const previousDescriptorSubscription = descriptor.subscription;
							descriptor.subscription = { type: "none" };
							this.emit("sfuUpdateSubscription", {
								sid: descriptor.info.sid,
								subscribe: false
							});
							if (previousDescriptorSubscription.type === "pending") (_b = (_a = previousDescriptorSubscription.completionFuture).reject) === null || _b === void 0 || _b.call(_a, timeoutSignal.aborted ? DataTrackSubscribeError.timeout() : DataTrackSubscribeError.cancelled());
						}
					};
					this.emit("sfuUpdateSubscription", {
						sid,
						subscribe: true
					});
					const timeoutSignal = abortSignalTimeout(SUBSCRIBE_TIMEOUT_MILLISECONDS);
					yield waitForCompletionFuture(descriptor, signal, timeoutSignal);
					return;
				}
				case "pending":
					descriptor.subscription.pendingRequestCount += 1;
					yield waitForCompletionFuture(descriptor, signal);
					return;
				case "active": return;
			}
		});
	}
	/**
	* Get information about all currently subscribed tracks.
	* @internal */
	querySubscribed() {
		return __awaiter(this, void 0, void 0, function* () {
			return Array.from(this.descriptors.values()).filter((descriptor) => descriptor.subscription.type === "active").map((descriptor) => [descriptor.info, descriptor.publisherIdentity]);
		});
	}
	/** Client requested to unsubscribe from a data track. */
	unSubscribeRequest(sid) {
		var _a;
		const descriptor = this.descriptors.get(sid);
		if (!descriptor) throw new Error("Cannot subscribe to unknown track");
		if (descriptor.subscription.type !== "active") {
			log$1.warn("Unexpected descriptor state in unSubscribeRequest, expected active, found ".concat((_a = descriptor.subscription) === null || _a === void 0 ? void 0 : _a.type));
			return;
		}
		this.closeStreamControllers(descriptor.subscription.streamControllers, sid);
		const previousDescriptorSubscription = descriptor.subscription;
		descriptor.subscription = { type: "none" };
		this.subscriptionHandles.delete(previousDescriptorSubscription.subcriptionHandle);
		this.emit("sfuUpdateSubscription", {
			sid,
			subscribe: false
		});
	}
	/** Detach abort-signal listeners and close all downstream stream controllers for an active
	* subscription. Used when the subscription is being torn down by the manager (unsubscribe,
	* unpublish, or shutdown). */
	closeStreamControllers(streamControllers, sid) {
		for (const _ref of streamControllers) {
			var _ref2 = _slicedToArray(_ref, 2);
			const controller = _ref2[0];
			const detachSignal = _ref2[1];
			detachSignal();
			try {
				controller.close();
			} catch (err) {
				log$1.warn("Failed to close readable stream for track ".concat(sid, ": ").concat(err));
			}
		}
	}
	/** SFU notification that track publications have changed.
	*
	* This event is produced from both {@link JoinResponse} and {@link ParticipantUpdate}
	* to provide a complete view of remote participants' track publications:
	*
	* - From a `JoinResponse`, it captures the initial set of tracks published when a participant joins.
	* - From a `ParticipantUpdate`, it captures subsequent changes (i.e., new tracks being
	*   published and existing tracks unpublished).
	*/
	receiveSfuPublicationUpdates(updates) {
		return __awaiter(this, void 0, void 0, function* () {
			if (updates.size === 0) return;
			const publisherParticipantToSidsInUpdate = /* @__PURE__ */ new Map();
			for (const _ref3 of updates.entries()) {
				var _ref4 = _slicedToArray(_ref3, 2);
				const publisherIdentity = _ref4[0];
				const infos = _ref4[1];
				const sidsInUpdate = /* @__PURE__ */ new Set();
				for (const info of infos) {
					sidsInUpdate.add(info.sid);
					if (this.descriptors.has(info.sid)) continue;
					if (this.handleSidReassigned(publisherIdentity, info)) continue;
					yield this.handleTrackPublished(publisherIdentity, info);
				}
				publisherParticipantToSidsInUpdate.set(publisherIdentity, sidsInUpdate);
			}
			for (const _ref5 of publisherParticipantToSidsInUpdate.entries()) {
				var _ref6 = _slicedToArray(_ref5, 2);
				const publisherIdentity = _ref6[0];
				const sidsInUpdate = _ref6[1];
				let unpublishedSids = Array.from(this.descriptors.entries()).filter((_ref7) => {
					let _ref8 = _slicedToArray(_ref7, 2);
					_ref8[0];
					return _ref8[1].publisherIdentity === publisherIdentity;
				}).map((_ref9) => {
					return _slicedToArray(_ref9, 1)[0];
				}).filter((sid) => !sidsInUpdate.has(sid));
				for (const sid of unpublishedSids) this.handleTrackUnpublished(sid);
			}
		});
	}
	/**
	* Get information about all currently remotely published tracks which could be subscribed to.
	* @internal */
	queryPublications() {
		return __awaiter(this, void 0, void 0, function* () {
			return Array.from(this.descriptors.values()).map((descriptor) => descriptor.info);
		});
	}
	handleTrackPublished(publisherIdentity, info) {
		return __awaiter(this, void 0, void 0, function* () {
			if (this.descriptors.has(info.sid)) {
				log$1.error("Existing descriptor for track ".concat(info.sid));
				return;
			}
			let descriptor = {
				info,
				publisherIdentity,
				subscription: { type: "none" },
				pipelineOptions: {}
			};
			this.descriptors.set(descriptor.info.sid, descriptor);
			const track = new RemoteDataTrack(descriptor.info, this, { publisherIdentity });
			this.emit("trackPublished", { track });
		});
	}
	/**
	* Detects and handles SID reassignment, which occurs when the publisher
	* republishes its tracks after a full reconnect.
	*
	* Returns `true` if an SID reassignment occurred, `false` otherwise.
	*/
	handleSidReassigned(publisherIdentity, info) {
		const existingEntry = Array.from(this.descriptors.entries()).find((_ref1) => {
			let _ref10 = _slicedToArray(_ref1, 2);
			_ref10[0];
			let descriptor = _ref10[1];
			return descriptor.publisherIdentity === publisherIdentity && descriptor.info.pubHandle === info.pubHandle;
		});
		if (!existingEntry) return false;
		const _existingEntry = _slicedToArray(existingEntry, 2), oldSid = _existingEntry[0], descriptor = _existingEntry[1];
		const _descriptor$info = descriptor.info, name = _descriptor$info.name, usesE2ee = _descriptor$info.usesE2ee;
		if (name !== info.name || usesE2ee !== info.usesE2ee) {
			log$1.warn("Info mismatch for ".concat(oldSid, ", treating as new publication"));
			return false;
		}
		const newSid = info.sid;
		log$1.debug("SID reassigned: ".concat(oldSid, " -> ").concat(newSid));
		if (!this.descriptors.delete(oldSid)) return false;
		descriptor.info.sid = newSid;
		switch (descriptor.subscription.type) {
			case "none": break;
			case "pending":
			case "active": this.emit("sfuUpdateSubscription", {
				sid: newSid,
				subscribe: true
			});
		}
		if (descriptor.subscription.type === "active") this.subscriptionHandles.set(descriptor.subscription.subcriptionHandle, newSid);
		this.descriptors.set(newSid, descriptor);
		return true;
	}
	handleTrackUnpublished(sid) {
		const descriptor = this.descriptors.get(sid);
		if (!descriptor) {
			log$1.error("Unknown track ".concat(sid));
			return;
		}
		this.descriptors.delete(sid);
		if (descriptor.subscription.type === "active") {
			this.closeStreamControllers(descriptor.subscription.streamControllers, sid);
			this.subscriptionHandles.delete(descriptor.subscription.subcriptionHandle);
		}
		this.emit("trackUnpublished", {
			sid,
			publisherIdentity: descriptor.publisherIdentity
		});
	}
	/** SFU notification that handles have been assigned for requested subscriptions. */
	receivedSfuSubscriberHandles(mapping) {
		for (const _ref11 of mapping.entries()) {
			var _ref12 = _slicedToArray(_ref11, 2);
			const handle = _ref12[0];
			const sid = _ref12[1];
			this.registerSubscriberHandle(handle, sid);
		}
	}
	registerSubscriberHandle(assignedHandle, sid) {
		var _a, _b;
		const descriptor = this.descriptors.get(sid);
		if (!descriptor) {
			log$1.error("Unknown track ".concat(sid));
			return;
		}
		switch (descriptor.subscription.type) {
			case "none":
				log$1.warn("No subscription for ".concat(sid));
				return;
			case "active":
				this.subscriptionHandles.delete(descriptor.subscription.subcriptionHandle);
				descriptor.subscription.subcriptionHandle = assignedHandle;
				this.subscriptionHandles.set(assignedHandle, sid);
				return;
			case "pending": {
				log$1.debug("data track subscription activated", {
					sid,
					handle: assignedHandle
				});
				const pipeline = new IncomingDataTrackPipeline({
					info: descriptor.info,
					publisherIdentity: descriptor.publisherIdentity,
					e2eeManager: this.e2eeManager,
					pipelineOptions: descriptor.pipelineOptions
				});
				const previousDescriptorSubscription = descriptor.subscription;
				descriptor.subscription = {
					type: "active",
					subcriptionHandle: assignedHandle,
					pipeline,
					streamControllers: /* @__PURE__ */ new Map()
				};
				this.subscriptionHandles.set(assignedHandle, sid);
				(_b = (_a = previousDescriptorSubscription.completionFuture).resolve) === null || _b === void 0 || _b.call(_a);
			}
		}
	}
	/** Packet has been received over the transport. */
	packetReceived(bytes) {
		return __awaiter(this, void 0, void 0, function* () {
			let packet;
			try {
				packet = _slicedToArray(DataTrackPacket.fromBinary(bytes), 1)[0];
			} catch (err) {
				log$1.error("Failed to deserialize packet: ".concat(err));
				return;
			}
			const sid = this.subscriptionHandles.get(packet.header.trackHandle);
			if (!sid) {
				log$1.warn("Unknown subscriber handle ".concat(packet.header.trackHandle));
				return;
			}
			const descriptor = this.descriptors.get(sid);
			if (!descriptor) {
				log$1.error("Missing descriptor for track ".concat(sid));
				return;
			}
			if (descriptor.subscription.type !== "active") {
				log$1.warn("Received packet for track ".concat(sid, " without active subscription"));
				return;
			}
			const internalFrame = yield descriptor.subscription.pipeline.processPacket(packet);
			if (!internalFrame) return;
			for (const controller of descriptor.subscription.streamControllers.keys()) {
				if (controller.desiredSize !== null && controller.desiredSize <= 0) {
					log$1.warn("Cannot send frame to subscribers: readable stream is full (desiredSize is ".concat(controller.desiredSize, "). To increase this threshold, set a higher 'options.highWaterMark' when calling .subscribe()."));
					continue;
				}
				const frame = DataTrackFrameInternal.lossyIntoFrame(internalFrame);
				controller.enqueue(frame);
			}
		});
	}
	/** Resend all subscription updates.
	*
	* This must be sent after a full reconnect to ensure the SFU knows which
	* tracks are subscribed to locally.
	*/
	resendSubscriptionUpdates() {
		for (const _ref13 of this.descriptors) {
			var _ref14 = _slicedToArray(_ref13, 2);
			const sid = _ref14[0];
			if (_ref14[1].subscription.type === "none") continue;
			this.emit("sfuUpdateSubscription", {
				sid,
				subscribe: true
			});
		}
	}
	/** Called when a remote participant is disconnected so that any pending data tracks can be
	* cancelled. */
	handleRemoteParticipantDisconnected(remoteParticipantIdentity) {
		var _a, _b;
		for (const descriptor of this.descriptors.values()) {
			if (descriptor.publisherIdentity !== remoteParticipantIdentity) continue;
			switch (descriptor.subscription.type) {
				case "none": break;
				case "pending":
					(_b = (_a = descriptor.subscription.completionFuture).reject) === null || _b === void 0 || _b.call(_a, DataTrackSubscribeError.disconnected());
					break;
				case "active": this.unSubscribeRequest(descriptor.info.sid);
			}
		}
	}
	/** Resets the manager, ending any subscriptions, and getting it ready for the next room
	* connection.  */
	reset() {
		var _a, _b;
		for (const descriptor of this.descriptors.values()) {
			this.emit("trackUnpublished", {
				sid: descriptor.info.sid,
				publisherIdentity: descriptor.publisherIdentity
			});
			if (descriptor.subscription.type === "pending") (_b = (_a = descriptor.subscription.completionFuture).reject) === null || _b === void 0 || _b.call(_a, DataTrackSubscribeError.disconnected());
			if (descriptor.subscription.type === "active") this.closeStreamControllers(descriptor.subscription.streamControllers, descriptor.info.sid);
		}
		this.descriptors.clear();
		this.subscriptionHandles.clear();
	}
};
var DataTrackPacketizerError = class DataTrackPacketizerError extends LivekitReasonedError {
	constructor(message, reason, options) {
		super(19, message, options);
		this.name = "DataTrackPacketizerError";
		this.reason = reason;
		this.reasonName = DataTrackPacketizerReason[reason];
	}
	static mtuTooShort() {
		return new DataTrackPacketizerError("MTU is too short to send frame", DataTrackPacketizerReason.MtuTooShort);
	}
};
var DataTrackPacketizerReason;
(function(DataTrackPacketizerReason) {
	DataTrackPacketizerReason[DataTrackPacketizerReason["MtuTooShort"] = 0] = "MtuTooShort";
})(DataTrackPacketizerReason || (DataTrackPacketizerReason = {}));
/** A packetizer takes a {@link DataTrackFrameInternal} as input and generates a series
* of {@link DataTrackPacket}s for transmission to other clients over webrtc. */
var DataTrackPacketizer = class DataTrackPacketizer {
	constructor(trackHandle, mtuSizeBytes) {
		this.sequence = WrapAroundUnsignedInt.u16(0);
		this.frameNumber = WrapAroundUnsignedInt.u16(0);
		this.clock = DataTrackClock.rtpStartingNow(DataTrackTimestamp.rtpRandom());
		this.handle = trackHandle;
		this.mtuSizeBytes = mtuSizeBytes;
	}
	/** @internal */
	static computeFrameMarker(index, packetCount) {
		if (packetCount <= 1) return FrameMarker.Single;
		if (index === 0) return FrameMarker.Start;
		else if (index === packetCount - 1) return FrameMarker.Final;
		else return FrameMarker.Inter;
	}
	/** Generates a series of packets for the specified {@link DataTrackFrameInternal}.
	*
	* NOTE: The return value of this function is a generator, so it can be lazily ran if desired,
	* or converted to an array with {@link Array.from}.
	*/
	*packetize(frame, options) {
		var _a;
		const frameNumber = this.frameNumber.getThenIncrement();
		const headerParams = {
			marker: FrameMarker.Inter,
			trackHandle: this.handle,
			sequence: WrapAroundUnsignedInt.u16(0),
			frameNumber,
			timestamp: (_a = options === null || options === void 0 ? void 0 : options.now) !== null && _a !== void 0 ? _a : this.clock.now(),
			extensions: frame.extensions
		};
		const headerSerializedLengthBytes = new DataTrackPacketHeader(headerParams).toBinaryLengthBytes();
		if (headerSerializedLengthBytes >= this.mtuSizeBytes) throw DataTrackPacketizerError.mtuTooShort();
		const maxPayloadSizeBytes = this.mtuSizeBytes - headerSerializedLengthBytes;
		const packetCount = Math.ceil(frame.payload.byteLength / maxPayloadSizeBytes);
		for (let index = 0, indexBytes = 0; indexBytes < frame.payload.byteLength; _ref = [index + 1, indexBytes + maxPayloadSizeBytes], index = _ref[0], indexBytes = _ref[1]) {
			var _ref;
			const sequence = this.sequence.getThenIncrement();
			const packetHeader = new DataTrackPacketHeader(Object.assign(Object.assign({}, headerParams), {
				marker: DataTrackPacketizer.computeFrameMarker(index, packetCount),
				sequence
			}));
			const packetPayloadLengthBytes = Math.min(maxPayloadSizeBytes, frame.payload.byteLength - indexBytes);
			yield new DataTrackPacket(packetHeader, new Uint8Array(frame.payload.buffer, frame.payload.byteOffset + indexBytes, packetPayloadLengthBytes));
		}
	}
};
var DataTrackPublishErrorReason;
(function(DataTrackPublishErrorReason) {
	/**
	* Local participant does not have permission to publish data tracks.
	*
	* Ensure the participant's token contains the `canPublishData` grant.
	*/
	DataTrackPublishErrorReason[DataTrackPublishErrorReason["NotAllowed"] = 0] = "NotAllowed";
	/** A track with the same name is already published by the local participant. */
	DataTrackPublishErrorReason[DataTrackPublishErrorReason["DuplicateName"] = 1] = "DuplicateName";
	/** Request to publish the track took long to complete. */
	DataTrackPublishErrorReason[DataTrackPublishErrorReason["Timeout"] = 2] = "Timeout";
	/** No additional data tracks can be published by the local participant. */
	DataTrackPublishErrorReason[DataTrackPublishErrorReason["LimitReached"] = 3] = "LimitReached";
	/** Cannot publish data track when the room is disconnected. */
	DataTrackPublishErrorReason[DataTrackPublishErrorReason["Disconnected"] = 4] = "Disconnected";
	DataTrackPublishErrorReason[DataTrackPublishErrorReason["Cancelled"] = 5] = "Cancelled";
	/** The name requested is not able to be used when creating the data track. */
	DataTrackPublishErrorReason[DataTrackPublishErrorReason["InvalidName"] = 6] = "InvalidName";
	/** There was an error publishing, but it was not something that could be sorted into a known
	* category. */
	DataTrackPublishErrorReason[DataTrackPublishErrorReason["Unknown"] = 7] = "Unknown";
})(DataTrackPublishErrorReason || (DataTrackPublishErrorReason = {}));
var DataTrackPublishError = class DataTrackPublishError extends LivekitReasonedError {
	constructor(message, reason, options) {
		super(21, message, options);
		this.name = "DataTrackPublishError";
		this.reason = reason;
		this.reasonName = DataTrackPublishErrorReason[reason];
		this.rawMessage = options === null || options === void 0 ? void 0 : options.rawMessage;
	}
	static notAllowed(rawMessage) {
		return new DataTrackPublishError("Data track publishing unauthorized", DataTrackPublishErrorReason.NotAllowed, { rawMessage });
	}
	static duplicateName(rawMessage) {
		return new DataTrackPublishError("Track name already taken", DataTrackPublishErrorReason.DuplicateName, { rawMessage });
	}
	static invalidName(rawMessage) {
		return new DataTrackPublishError("Track name is invalid", DataTrackPublishErrorReason.InvalidName, { rawMessage });
	}
	static timeout() {
		return new DataTrackPublishError("Publish data track timed-out. Does the LiveKit server support data tracks?", DataTrackPublishErrorReason.Timeout);
	}
	static limitReached(rawMessage) {
		return new DataTrackPublishError("Data track publication limit reached", DataTrackPublishErrorReason.LimitReached, { rawMessage });
	}
	static unknown(reason, message) {
		return new DataTrackPublishError("Received RequestResponse for publishDataTrack, but reason was unrecognised (".concat(reason, ", ").concat(message, ")"), DataTrackPublishErrorReason.Unknown);
	}
	static disconnected() {
		return new DataTrackPublishError("Room disconnected", DataTrackPublishErrorReason.Disconnected);
	}
	static cancelled() {
		return new DataTrackPublishError("Publish data track cancelled by caller", DataTrackPublishErrorReason.Cancelled);
	}
};
var DataTrackPushFrameErrorReason;
(function(DataTrackPushFrameErrorReason) {
	/** Track is no longer published. */
	DataTrackPushFrameErrorReason[DataTrackPushFrameErrorReason["TrackUnpublished"] = 0] = "TrackUnpublished";
	/** Frame was dropped. */
	DataTrackPushFrameErrorReason[DataTrackPushFrameErrorReason["Dropped"] = 1] = "Dropped";
})(DataTrackPushFrameErrorReason || (DataTrackPushFrameErrorReason = {}));
var DataTrackPushFrameError = class DataTrackPushFrameError extends LivekitReasonedError {
	constructor(message, reason, options) {
		super(22, message, options);
		this.name = "DataTrackPushFrameError";
		this.reason = reason;
		this.reasonName = DataTrackPushFrameErrorReason[reason];
	}
	static trackUnpublished() {
		return new DataTrackPushFrameError("Track is no longer published", DataTrackPushFrameErrorReason.TrackUnpublished);
	}
	static dropped(cause) {
		return new DataTrackPushFrameError("Frame was dropped", DataTrackPushFrameErrorReason.Dropped, { cause });
	}
};
var DataTrackOutgoingPipelineErrorReason;
(function(DataTrackOutgoingPipelineErrorReason) {
	DataTrackOutgoingPipelineErrorReason[DataTrackOutgoingPipelineErrorReason["Packetizer"] = 0] = "Packetizer";
	DataTrackOutgoingPipelineErrorReason[DataTrackOutgoingPipelineErrorReason["Encryption"] = 1] = "Encryption";
})(DataTrackOutgoingPipelineErrorReason || (DataTrackOutgoingPipelineErrorReason = {}));
var DataTrackOutgoingPipelineError = class DataTrackOutgoingPipelineError extends LivekitReasonedError {
	constructor(message, reason, options) {
		super(21, message, options);
		this.name = "DataTrackOutgoingPipelineError";
		this.reason = reason;
		this.reasonName = DataTrackOutgoingPipelineErrorReason[reason];
	}
	static packetizer(cause) {
		return new DataTrackOutgoingPipelineError("Error packetizing frame", DataTrackOutgoingPipelineErrorReason.Packetizer, { cause });
	}
	static encryption(cause) {
		return new DataTrackOutgoingPipelineError("Error encrypting frame", DataTrackOutgoingPipelineErrorReason.Encryption, { cause });
	}
};
var LocalDataTrack = class LocalDataTrack {
	/** @internal */
	constructor(options, manager) {
		this.trackSymbol = TrackSymbol;
		this.isLocal = true;
		this.typeSymbol = DataTrackSymbol;
		/** Represents the currently active {@link DataTrackHandle} for the publication. */
		this.handle = null;
		this.log = livekitLogger;
		/** Resolves once the data track has sent all pending packets the rtc data channel buffer. */
		this.flushedFuture = new Future();
		this.isFlushed = true;
		this.handleManagerReset = () => {
			var _a, _b;
			(_b = (_a = this.flushedFuture).resolve) === null || _b === void 0 || _b.call(_a);
			this.manager.off("packetsFlushedChange", this.handleManagerPacketsFlushedChange);
			this.manager.off("reset", this.handleManagerReset);
		};
		this.handleManagerPacketsFlushedChange = (event) => {
			var _a, _b;
			this.isFlushed = event.isFlushed;
			if (event.isFlushed) {
				(_b = (_a = this.flushedFuture).resolve) === null || _b === void 0 || _b.call(_a);
				this.flushedFuture = new Future();
			}
		};
		this.options = options;
		this.manager = manager;
		this.log = getLogger(LoggerNames.DataTracks);
		this.manager.on("packetsFlushedChange", this.handleManagerPacketsFlushedChange);
		this.manager.on("reset", this.handleManagerReset);
	}
	/** @internal */
	static withExplicitHandle(options, manager, handle) {
		const track = new LocalDataTrack(options, manager);
		track.handle = handle;
		return track;
	}
	/** Metrics about the data track publication. */
	get info() {
		const descriptor = this.descriptor;
		if ((descriptor === null || descriptor === void 0 ? void 0 : descriptor.type) === "active") return descriptor.info;
		else return;
	}
	/** The raw descriptor from the manager containing the internal state for this local track. */
	get descriptor() {
		return this.handle ? this.manager.getDescriptor(this.handle) : null;
	}
	/**
	* Publish the track to the SFU. This must be done before calling {@link tryPush} for the first time.
	* @internal
	* */
	publish(signal) {
		return __awaiter(this, void 0, void 0, function* () {
			try {
				this.handle = yield this.manager.publishRequest(this.options, signal);
			} catch (err) {
				throw err;
			}
		});
	}
	isPublished() {
		var _a;
		return ((_a = this.descriptor) === null || _a === void 0 ? void 0 : _a.type) === "active" && this.descriptor.publishState !== "unpublished";
	}
	/** Try pushing a frame to subscribers of the track.
	*
	* Pushing a frame can fail for several reasons:
	*
	* - The track has been unpublished by the local participant or SFU
	* - The room is no longer connected
	*/
	tryPush(frame) {
		if (!this.handle) throw DataTrackPushFrameError.trackUnpublished();
		const internalFrame = DataTrackFrameInternal.from(frame);
		try {
			return this.manager.tryProcessAndSend(this.handle, internalFrame);
		} catch (err) {
			throw err;
		}
	}
	/**
	* When called, waits for all in flight packets to be sent before resolving.
	*
	* Use this to:
	*
	* 1. Send frames exactly in order:
	* ```ts
	* await track.tryPush(/* ... *\/);
	* await track.flush();
	* await track.tryPush(/* ... *\/);
	* await track.flush();
	* // ... etc ...
	* ```
	*
	* 2. Wait for frames to all be delivered before unpublishing a local data track:
	*
	* ```ts
	* await track.tryPush(/* ... *\/);
	* await track.tryPush(/* ... *\/);
	* await track.tryPush(/* ... *\/);
	* // ... etc ...
	* await track.flush();
	* await track.unpublish();
	* ```
	**/
	flush() {
		return __awaiter(this, void 0, void 0, function* () {
			if (this.isFlushed) return;
			return this.flushedFuture.promise;
		});
	}
	/**
	* Unpublish the track from the SFU. Once this is called, any further calls to {@link tryPush}
	* will fail.
	* */
	unpublish() {
		return __awaiter(this, void 0, void 0, function* () {
			if (!this.handle) {
				livekitLogger.warn("Data track \"".concat(this.options.name, "\" is not published, so unpublishing has no effect."));
				return;
			}
			try {
				yield this.manager.unpublishRequest(this.handle);
			} catch (err) {
				throw err;
			}
		});
	}
};
var DataTrackOutgoingPipeline = class DataTrackOutgoingPipeline {
	constructor(options) {
		this.e2eeManager = options.e2eeManager;
		this.packetizer = new DataTrackPacketizer(options.info.pubHandle, DataTrackOutgoingPipeline.TRANSPORT_MTU_BYTES);
	}
	updateE2eeManager(e2eeManager) {
		this.e2eeManager = e2eeManager;
	}
	processFrame(frame) {
		return __asyncGenerator(this, arguments, function* processFrame_1() {
			const encryptedFrame = yield __await(this.encryptIfNeeded(frame));
			try {
				yield __await(yield* __asyncDelegator(__asyncValues(this.packetizer.packetize(encryptedFrame))));
			} catch (error) {
				if (error instanceof DataTrackPacketizerError) throw DataTrackOutgoingPipelineError.packetizer(error);
				throw error;
			}
		});
	}
	encryptIfNeeded(frame) {
		return __awaiter(this, void 0, void 0, function* () {
			if (!this.e2eeManager) return frame;
			let encryptedResult;
			try {
				encryptedResult = yield this.e2eeManager.encryptData(frame.payload);
			} catch (err) {
				throw DataTrackOutgoingPipelineError.encryption(err);
			}
			frame.payload = encryptedResult.payload;
			frame.extensions.e2ee = new DataTrackE2eeExtension(encryptedResult.keyIndex, encryptedResult.iv);
			return frame;
		});
	}
};
/** Maximum transmission unit (MTU) of the transport. */
DataTrackOutgoingPipeline.TRANSPORT_MTU_BYTES = 16e3;
var log = getLogger(LoggerNames.DataTracks);
var Descriptor = {
	pending() {
		return {
			type: "pending",
			completionFuture: new Future()
		};
	},
	active(info, e2eeManager) {
		return {
			type: "active",
			info,
			publishState: "published",
			pipeline: new DataTrackOutgoingPipeline({
				info,
				e2eeManager
			}),
			unpublishingFuture: new Future()
		};
	}
};
/** How long to wait when attempting to publish before timing out. */
var PUBLISH_TIMEOUT_MILLISECONDS = 1e4;
var OutgoingDataTrackManager = class OutgoingDataTrackManager extends eventsExports.EventEmitter {
	constructor(options) {
		var _a;
		super();
		this.handleAllocator = new DataTrackHandleAllocator();
		this.descriptors = /* @__PURE__ */ new Map();
		/** Number of packets for each data track which have been emitted via the `packetAvailable` event
		* and which have not yet been sent via the rtc data channel yet. Once this goes to 0, then
		* all in flight packets have been delivered, and the data tracks is "flushed". */
		this.inFlightPacketCounter = /* @__PURE__ */ new Map();
		this.e2eeManager = (_a = options === null || options === void 0 ? void 0 : options.e2eeManager) !== null && _a !== void 0 ? _a : null;
	}
	static withDescriptors(descriptors) {
		const manager = new OutgoingDataTrackManager();
		manager.descriptors = descriptors;
		return manager;
	}
	/** @internal */
	updateE2eeManager(e2eeManager) {
		this.e2eeManager = e2eeManager;
		for (const descriptor of this.descriptors.values()) if (descriptor.type === "active") descriptor.pipeline.updateE2eeManager(e2eeManager);
	}
	/**
	* Used by attached {@link LocalDataTrack} instances to query their associated descriptor info.
	* @internal
	*/
	getDescriptor(handle) {
		var _a;
		return (_a = this.descriptors.get(handle)) !== null && _a !== void 0 ? _a : null;
	}
	/** Used by attached {@link LocalDataTrack} instances to broadcast data track packets to other
	* subscribers.
	* @internal
	*/
	tryProcessAndSend(handle, frame) {
		return __awaiter(this, void 0, void 0, function* () {
			var _a, e_1, _b, _c;
			var _d;
			const descriptor = this.getDescriptor(handle);
			if ((descriptor === null || descriptor === void 0 ? void 0 : descriptor.type) !== "active") throw DataTrackPushFrameError.trackUnpublished();
			if (descriptor.publishState === "unpublished") throw DataTrackPushFrameError.trackUnpublished();
			if (descriptor.publishState === "republishing") throw DataTrackPushFrameError.dropped("Data track republishing");
			try {
				try {
					for (var _e = true, _f = __asyncValues(descriptor.pipeline.processFrame(frame)), _g; _g = yield _f.next(), _a = _g.done, !_a; _e = true) {
						_c = _g.value;
						_e = false;
						const packet = _c;
						const prev = (_d = this.inFlightPacketCounter.get(handle)) !== null && _d !== void 0 ? _d : 0;
						this.inFlightPacketCounter.set(handle, prev + 1);
						if (prev === 0) this.emit("packetsFlushedChange", {
							handle,
							isFlushed: false
						});
						this.emit("packetAvailable", {
							handle,
							bytes: packet.toBinary()
						});
					}
				} catch (e_1_1) {
					e_1 = { error: e_1_1 };
				} finally {
					try {
						if (!_e && !_a && (_b = _f.return)) yield _b.call(_f);
					} finally {
						if (e_1) throw e_1.error;
					}
				}
			} catch (err) {
				throw DataTrackPushFrameError.dropped(err);
			}
		});
	}
	/** The client has sent a packet over the rtc data channel. This signal is used for determining
	* once all packets are sent and a data track has been "flushed".
	*
	* @internal */
	handlePacketSendComplete(handle) {
		var _a;
		let counter = ((_a = this.inFlightPacketCounter.get(handle)) !== null && _a !== void 0 ? _a : 0) - 1;
		if (counter < 0) {
			log.warn("OutgoingDataTrackManager.handlePacketSendComplete: inFlightPacketCounter was decremented below 0 (got ".concat(this.inFlightPacketCounter, " - resetting to 0. Were more packets send than were emitted?"));
			counter = 0;
		}
		this.inFlightPacketCounter.set(handle, counter);
		if (counter === 0) this.emit("packetsFlushedChange", {
			handle,
			isFlushed: true
		});
	}
	/**
	* Client requested to publish a track.
	*
	* If the LiveKit server is too old and doesn't support data tracks, a
	* {@link DataTrackPublishError#timeout} will be thrown.
	*
	* @internal
	**/
	publishRequest(options, signal) {
		return __awaiter(this, void 0, void 0, function* () {
			const handle = this.handleAllocator.get();
			if (!handle) throw DataTrackPublishError.limitReached();
			const timeoutSignal = abortSignalTimeout(PUBLISH_TIMEOUT_MILLISECONDS);
			const combinedSignal = signal ? abortSignalAny([signal, timeoutSignal]) : timeoutSignal;
			if (this.descriptors.has(handle)) throw new Error("Descriptor for handle already exists");
			const descriptor = Descriptor.pending();
			this.descriptors.set(handle, descriptor);
			const onAbort = () => {
				var _a, _b;
				const existingDescriptor = this.descriptors.get(handle);
				if (!existingDescriptor) {
					log.warn("No descriptor for ".concat(handle));
					return;
				}
				this.descriptors.delete(handle);
				this.emit("sfuUnpublishRequest", { handle });
				if (existingDescriptor.type === "pending") (_b = (_a = existingDescriptor.completionFuture).reject) === null || _b === void 0 || _b.call(_a, timeoutSignal.aborted ? DataTrackPublishError.timeout() : DataTrackPublishError.cancelled());
			};
			if (combinedSignal.aborted) {
				onAbort();
				return descriptor.completionFuture.promise.then(() => handle);
			}
			combinedSignal.addEventListener("abort", onAbort);
			this.emit("sfuPublishRequest", {
				handle,
				name: options.name,
				usesE2ee: this.e2eeManager !== null
			});
			yield descriptor.completionFuture.promise;
			combinedSignal.removeEventListener("abort", onAbort);
			this.emit("trackPublished", { track: LocalDataTrack.withExplicitHandle(options, this, handle) });
			return handle;
		});
	}
	/**
	* Get information about all currently published tracks.
	* @internal
	**/
	queryPublished() {
		return Array.from(this.descriptors.values()).filter((descriptor) => descriptor.type === "active").map((descriptor) => descriptor.info);
	}
	/**
	* Client request to unpublish a track.
	* @internal
	**/
	unpublishRequest(handle) {
		return __awaiter(this, void 0, void 0, function* () {
			const descriptor = this.descriptors.get(handle);
			if (!descriptor) {
				log.warn("No descriptor for ".concat(handle));
				return;
			}
			if (descriptor.type !== "active") {
				log.warn("Track ".concat(handle, " not active"));
				return;
			}
			this.emit("sfuUnpublishRequest", { handle });
			yield descriptor.unpublishingFuture.promise;
			this.inFlightPacketCounter.delete(handle);
			this.emit("trackUnpublished", { sid: descriptor.info.sid });
		});
	}
	/**
	* SFU responded to a request to publish a data track.
	* @internal
	**/
	receivedSfuPublishResponse(handle, result) {
		var _a, _b, _c, _d;
		const descriptor = this.descriptors.get(handle);
		if (!descriptor) {
			log.warn("No descriptor for ".concat(handle));
			return;
		}
		this.descriptors.delete(handle);
		switch (descriptor.type) {
			case "pending":
				if (result.type === "ok") {
					const info = result.data;
					log.debug("SFU accepted publish request for handle ".concat(handle), { sid: info.sid });
					const e2eeManager = info.usesE2ee ? this.e2eeManager : null;
					this.descriptors.set(info.pubHandle, Descriptor.active(info, e2eeManager));
					(_b = (_a = descriptor.completionFuture).resolve) === null || _b === void 0 || _b.call(_a);
				} else {
					log.debug("SFU rejected publish request for handle ".concat(handle), { error: result.error });
					(_d = (_c = descriptor.completionFuture).reject) === null || _d === void 0 || _d.call(_c, result.error);
				}
				return;
			case "active":
				if (descriptor.publishState !== "republishing") {
					log.warn("Track ".concat(handle, " already active"));
					return;
				}
				if (result.type === "error") {
					log.warn("Republish failed for track ".concat(handle));
					return;
				}
				log.debug("Track ".concat(handle, " republished"));
				descriptor.info.sid = result.data.sid;
				descriptor.publishState = "published";
				this.descriptors.set(descriptor.info.pubHandle, descriptor);
		}
	}
	/**
	* SFU notification that a track has been unpublished.
	* @internal
	**/
	receivedSfuUnpublishResponse(handle) {
		var _a, _b;
		const descriptor = this.descriptors.get(handle);
		if (!descriptor) {
			log.warn("No descriptor for ".concat(handle));
			return;
		}
		this.descriptors.delete(handle);
		if (descriptor.type !== "active") {
			log.warn("Track ".concat(handle, " not active"));
			return;
		}
		descriptor.publishState = "unpublished";
		(_b = (_a = descriptor.unpublishingFuture).resolve) === null || _b === void 0 || _b.call(_a);
	}
	/** Republish all tracks.
	*
	* This must be sent after a full reconnect in order for existing publications
	* to be recognized by the SFU. Each republished track will be assigned a new SID.
	* @internal
	*/
	sfuWillRepublishTracks() {
		var _a, _b;
		for (const _ref of this.descriptors.entries()) {
			var _ref2 = _slicedToArray(_ref, 2);
			const handle = _ref2[0];
			const descriptor = _ref2[1];
			switch (descriptor.type) {
				case "pending":
					this.descriptors.delete(handle);
					(_b = (_a = descriptor.completionFuture).reject) === null || _b === void 0 || _b.call(_a, DataTrackPublishError.disconnected());
					break;
				case "active":
					descriptor.publishState = "republishing";
					this.emit("sfuPublishRequest", {
						handle: descriptor.info.pubHandle,
						name: descriptor.info.name,
						usesE2ee: descriptor.info.usesE2ee
					});
			}
		}
	}
	/**
	* Reset's the state of the manager and all associated tracks. Run on room disconnect to get
	* the manager ready for the next room connection.
	* @internal
	**/
	reset() {
		return __awaiter(this, void 0, void 0, function* () {
			var _a, _b, _c, _d;
			this.handleAllocator.reset();
			for (const descriptor of this.descriptors.values()) switch (descriptor.type) {
				case "pending":
					(_b = (_a = descriptor.completionFuture).reject) === null || _b === void 0 || _b.call(_a, DataTrackPublishError.disconnected());
					break;
				case "active":
					(_d = (_c = descriptor.unpublishingFuture).resolve) === null || _d === void 0 || _d.call(_c);
					yield this.unpublishRequest(descriptor.info.pubHandle);
			}
			this.descriptors.clear();
			this.inFlightPacketCounter.clear();
			this.emit("reset");
		});
	}
};
var RpcError = class RpcError extends Error {
	/**
	* Creates an error object with the given code and message, plus an optional data payload.
	*
	* If thrown in an RPC method handler, the error will be sent back to the caller.
	*
	* Error codes 1001-1999 are reserved for built-in errors (see RpcError.ErrorCode for their meanings).
	*/
	constructor(code, message, data, options) {
		super(message);
		this.code = code;
		this.message = truncateBytes(message, RpcError.MAX_MESSAGE_BYTES);
		this.data = data ? truncateBytes(data, RpcError.MAX_DATA_BYTES) : void 0;
		if (typeof (options === null || options === void 0 ? void 0 : options.cause) !== "undefined") this.cause = options === null || options === void 0 ? void 0 : options.cause;
	}
	/**
	* @internal
	*/
	static fromProto(proto) {
		return new RpcError(proto.code, proto.message, proto.data);
	}
	/**
	* @internal
	*/
	toProto() {
		return new RpcError$1({
			code: this.code,
			message: this.message,
			data: this.data
		});
	}
	/**
	* Creates an error object from the code, with an auto-populated message.
	*
	* @internal
	*/
	static builtIn(key, data, options) {
		return new RpcError(RpcError.ErrorCode[key], RpcError.ErrorMessage[key], data, options);
	}
};
RpcError.MAX_MESSAGE_BYTES = 256;
RpcError.MAX_DATA_BYTES = 15360;
RpcError.ErrorCode = {
	APPLICATION_ERROR: 1500,
	CONNECTION_TIMEOUT: 1501,
	RESPONSE_TIMEOUT: 1502,
	RECIPIENT_DISCONNECTED: 1503,
	RESPONSE_PAYLOAD_TOO_LARGE: 1504,
	SEND_FAILED: 1505,
	UNSUPPORTED_METHOD: 1400,
	RECIPIENT_NOT_FOUND: 1401,
	REQUEST_PAYLOAD_TOO_LARGE: 1402,
	UNSUPPORTED_SERVER: 1403,
	UNSUPPORTED_VERSION: 1404
};
/**
* @internal
*/
RpcError.ErrorMessage = {
	APPLICATION_ERROR: "Application error in method handler",
	CONNECTION_TIMEOUT: "Connection timeout",
	RESPONSE_TIMEOUT: "Response timeout",
	RECIPIENT_DISCONNECTED: "Recipient disconnected",
	RESPONSE_PAYLOAD_TOO_LARGE: "Response payload too large",
	SEND_FAILED: "Failed to send",
	UNSUPPORTED_METHOD: "Method not supported at destination",
	RECIPIENT_NOT_FOUND: "Recipient not found",
	REQUEST_PAYLOAD_TOO_LARGE: "Request payload too large",
	UNSUPPORTED_SERVER: "RPC not supported by server",
	UNSUPPORTED_VERSION: "Unsupported RPC version"
};
var MAX_V1_PAYLOAD_BYTES = 15360;
/**
* Topic used for v2 RPC request data streams.
* @internal
*/
var RPC_REQUEST_DATA_STREAM_TOPIC = "lk.rpc_request";
/**
* Topic used for v2 RPC response data streams.
* @internal
*/
var RPC_RESPONSE_DATA_STREAM_TOPIC = "lk.rpc_response";
/** @internal */
var RpcRequestAttrs;
(function(RpcRequestAttrs) {
	RpcRequestAttrs["RPC_REQUEST_ID"] = "lk.rpc_request_id";
	RpcRequestAttrs["RPC_REQUEST_METHOD"] = "lk.rpc_request_method";
	RpcRequestAttrs["RPC_REQUEST_RESPONSE_TIMEOUT_MS"] = "lk.rpc_request_response_timeout_ms";
	RpcRequestAttrs["RPC_REQUEST_VERSION"] = "lk.rpc_request_version";
})(RpcRequestAttrs || (RpcRequestAttrs = {}));
/** Initial version of rpc which uses RpcRequest / RpcResponse messages.
* @internal
**/
var RPC_VERSION_V1 = 1;
/** Rpc version backed by data streams instead of RpcRequest / RpcResponse.
* @internal
**/
var RPC_VERSION_V2 = 2;
/**
* @internal
*/
function byteLength(str) {
	return new TextEncoder().encode(str).length;
}
/**
* @internal
*/
function truncateBytes(str, maxBytes) {
	if (byteLength(str) <= maxBytes) return str;
	let low = 0;
	let high = str.length;
	const encoder = new TextEncoder();
	while (low < high) {
		const mid = Math.floor((low + high + 1) / 2);
		if (encoder.encode(str.slice(0, mid)).length <= maxBytes) low = mid;
		else high = mid - 1;
	}
	return str.slice(0, low);
}
var RpcClientManager = class extends eventsExports.EventEmitter {
	constructor(log, outgoingDataStreamManager, getRemoteParticipantClientProtocol, getServerVersion) {
		super();
		this.pendingAcks = /* @__PURE__ */ new Map();
		this.pendingResponses = /* @__PURE__ */ new Map();
		this.log = log;
		this.outgoingDataStreamManager = outgoingDataStreamManager;
		this.getRemoteParticipantClientProtocol = getRemoteParticipantClientProtocol;
		this.getServerVersion = getServerVersion;
	}
	performRpc(_a) {
		return __awaiter(this, arguments, void 0, function(_ref) {
			var _this = this;
			let destinationIdentity = _ref.destinationIdentity, method = _ref.method, payload = _ref.payload, _ref$responseTimeout = _ref.responseTimeout, responseTimeoutMs = _ref$responseTimeout === void 0 ? 15e3 : _ref$responseTimeout;
			return function* () {
				const maxRoundTripLatencyMs = 7e3;
				const minEffectiveTimeoutMs = 8e3;
				const remoteClientProtocol = _this.getRemoteParticipantClientProtocol(destinationIdentity);
				if (byteLength(payload) > MAX_V1_PAYLOAD_BYTES && remoteClientProtocol < 1) throw RpcError.builtIn("REQUEST_PAYLOAD_TOO_LARGE");
				const serverVersion = _this.getServerVersion();
				if (serverVersion && compareVersions(serverVersion, "1.8.0") < 0) throw RpcError.builtIn("UNSUPPORTED_SERVER");
				const effectiveTimeoutMs = Math.max(responseTimeoutMs, minEffectiveTimeoutMs);
				const id = crypto.randomUUID();
				const completionFuture = new Future();
				let responseTimeoutId = null;
				const ackTimeoutId = setTimeout(() => {
					var _a;
					_this.pendingAcks.delete(id);
					(_a = completionFuture.reject) === null || _a === void 0 || _a.call(completionFuture, RpcError.builtIn("CONNECTION_TIMEOUT"));
					_this.pendingResponses.delete(id);
					if (responseTimeoutId !== null) clearTimeout(responseTimeoutId);
				}, maxRoundTripLatencyMs);
				_this.pendingAcks.set(id, {
					resolve: () => {
						clearTimeout(ackTimeoutId);
					},
					participantIdentity: destinationIdentity
				});
				_this.pendingResponses.set(id, {
					completionFuture,
					participantIdentity: destinationIdentity
				});
				yield _this.publishRpcRequest(destinationIdentity, id, method, payload, effectiveTimeoutMs, remoteClientProtocol);
				responseTimeoutId = setTimeout(() => {
					var _a;
					_this.pendingResponses.delete(id);
					(_a = completionFuture.reject) === null || _a === void 0 || _a.call(completionFuture, RpcError.builtIn("RESPONSE_TIMEOUT"));
				}, responseTimeoutMs);
				return [id, completionFuture.promise.finally(() => {
					clearTimeout(responseTimeoutId);
					if (_this.pendingAcks.has(id)) {
						_this.log.warn("RPC response received before ack", id);
						_this.pendingAcks.delete(id);
						clearTimeout(ackTimeoutId);
					}
				})];
			}();
		});
	}
	publishRpcRequest(destinationIdentity, requestId, method, payload, responseTimeout, remoteClientProtocol) {
		return __awaiter(this, void 0, void 0, function* () {
			if (remoteClientProtocol >= 1) {
				yield this.outgoingDataStreamManager.sendText(payload, {
					topic: RPC_REQUEST_DATA_STREAM_TOPIC,
					destinationIdentities: [destinationIdentity],
					attributes: {
						[RpcRequestAttrs.RPC_REQUEST_ID]: requestId,
						[RpcRequestAttrs.RPC_REQUEST_METHOD]: method,
						[RpcRequestAttrs.RPC_REQUEST_RESPONSE_TIMEOUT_MS]: "".concat(responseTimeout),
						[RpcRequestAttrs.RPC_REQUEST_VERSION]: "".concat(RPC_VERSION_V2)
					}
				});
				return;
			}
			this.emit("sendDataPacket", { packet: new DataPacket({
				destinationIdentities: [destinationIdentity],
				kind: DataPacket_Kind.RELIABLE,
				value: {
					case: "rpcRequest",
					value: new RpcRequest({
						id: requestId,
						method,
						payload,
						responseTimeoutMs: responseTimeout,
						version: RPC_VERSION_V1
					})
				}
			}) });
		});
	}
	/**
	* Handle an incoming data stream containing an RPC response payload.
	* @internal
	*/
	handleIncomingDataStream(reader, senderIdentity, attributes) {
		return __awaiter(this, void 0, void 0, function* () {
			const associatedRequestId = attributes[RpcRequestAttrs.RPC_REQUEST_ID];
			if (!associatedRequestId) {
				this.log.warn("RPC data stream malformed: ".concat(RpcRequestAttrs.RPC_REQUEST_ID, " not set."));
				return;
			}
			const pending = this.pendingResponses.get(associatedRequestId);
			if (pending && pending.participantIdentity !== senderIdentity) {
				this.log.warn("RPC response stream for ".concat(associatedRequestId, " arrived from unexpected sender ").concat(senderIdentity, ", expected ").concat(pending.participantIdentity, ". Ignoring."));
				return;
			}
			let payload;
			try {
				payload = yield reader.readAll();
			} catch (e) {
				this.log.warn("Error reading RPC response payload: ".concat(e));
				this.handleIncomingRpcResponseFailure(associatedRequestId, RpcError.builtIn("APPLICATION_ERROR", "Error reading RPC response payload", { cause: e }));
				return;
			}
			this.handleIncomingRpcResponseSuccess(associatedRequestId, payload);
		});
	}
	/** @internal */
	handleIncomingRpcResponseSuccess(requestId, payload) {
		var _a, _b;
		const handler = this.pendingResponses.get(requestId);
		if (handler) {
			(_b = (_a = handler.completionFuture).resolve) === null || _b === void 0 || _b.call(_a, payload);
			this.pendingResponses.delete(requestId);
		} else this.log.error("Response received for unexpected RPC request", requestId);
	}
	/** @internal */
	handleIncomingRpcResponseFailure(requestId, error) {
		var _a, _b;
		const handler = this.pendingResponses.get(requestId);
		if (handler) {
			(_b = (_a = handler.completionFuture).reject) === null || _b === void 0 || _b.call(_a, error);
			this.pendingResponses.delete(requestId);
		} else this.log.error("Response received for unexpected RPC request", requestId);
	}
	/** @internal */
	handleIncomingRpcAck(requestId) {
		const handler = this.pendingAcks.get(requestId);
		if (handler) {
			handler.resolve();
			this.pendingAcks.delete(requestId);
		} else this.log.error("Ack received for unexpected RPC request: ".concat(requestId));
	}
	/** @internal */
	handleParticipantDisconnected(participantIdentity) {
		var _a;
		for (const _ref2 of this.pendingAcks) {
			var _ref3 = _slicedToArray(_ref2, 2);
			const id = _ref3[0];
			if (_ref3[1].participantIdentity === participantIdentity) this.pendingAcks.delete(id);
		}
		for (const _ref4 of this.pendingResponses) {
			var _ref5 = _slicedToArray(_ref4, 2);
			const id = _ref5[0];
			var _ref5$ = _ref5[1];
			const pendingIdentity = _ref5$.participantIdentity;
			const completionFuture = _ref5$.completionFuture;
			if (pendingIdentity === participantIdentity) {
				(_a = completionFuture.reject) === null || _a === void 0 || _a.call(completionFuture, RpcError.builtIn("RECIPIENT_DISCONNECTED"));
				this.pendingResponses.delete(id);
			}
		}
	}
};
var RpcServerManager = class extends eventsExports.EventEmitter {
	constructor(log, outgoingDataStreamManager, getRemoteParticipantClientProtocol) {
		super();
		this.rpcHandlers = /* @__PURE__ */ new Map();
		this.log = log;
		this.outgoingDataStreamManager = outgoingDataStreamManager;
		this.getRemoteParticipantClientProtocol = getRemoteParticipantClientProtocol;
	}
	registerRpcMethod(method, handler) {
		if (this.rpcHandlers.has(method)) throw Error("RPC handler already registered for method ".concat(method, ", unregisterRpcMethod before trying to register again"));
		this.rpcHandlers.set(method, handler);
	}
	unregisterRpcMethod(method) {
		this.rpcHandlers.delete(method);
	}
	/**
	* Handle an incoming RPCRequest message containing a payload.
	* This handles "version 1" of rpc requests.
	* @internal
	*/
	handleIncomingRpcRequest(callerIdentity, rpcRequest) {
		return __awaiter(this, void 0, void 0, function* () {
			var _a;
			this.publishRpcAck(callerIdentity, rpcRequest.id);
			if (rpcRequest.version !== 1) {
				this.publishRpcResponsePacket(callerIdentity, rpcRequest.id, null, RpcError.builtIn("UNSUPPORTED_VERSION"));
				return;
			}
			const handler = this.rpcHandlers.get(rpcRequest.method);
			if (!handler) {
				this.publishRpcResponsePacket(callerIdentity, rpcRequest.id, null, RpcError.builtIn("UNSUPPORTED_METHOD"));
				return;
			}
			let response;
			try {
				response = yield handler({
					requestId: rpcRequest.id,
					callerIdentity,
					payload: rpcRequest.payload,
					responseTimeout: rpcRequest.responseTimeoutMs
				});
			} catch (error) {
				let responseError;
				if (error instanceof RpcError) responseError = error;
				else {
					this.log.warn("Uncaught error returned by RPC handler for ".concat(rpcRequest.method, ". Returning APPLICATION_ERROR instead."), error);
					responseError = RpcError.builtIn("APPLICATION_ERROR", "Uncaught error: ".concat((_a = error === null || error === void 0 ? void 0 : error.message) !== null && _a !== void 0 ? _a : error), { cause: error });
				}
				this.publishRpcResponsePacket(callerIdentity, rpcRequest.id, null, responseError);
				return;
			}
			yield this.publishRpcResponse(callerIdentity, rpcRequest.id, response !== null && response !== void 0 ? response : "");
		});
	}
	/**
	* Handle an incoming data stream containing a RPC request payload.
	* This handles "version 2" of rpc requests.
	* @internal
	*/
	handleIncomingDataStream(reader, callerIdentity, dataStreamAttrs) {
		return __awaiter(this, void 0, void 0, function* () {
			const requestId = dataStreamAttrs[RpcRequestAttrs.RPC_REQUEST_ID];
			const method = dataStreamAttrs[RpcRequestAttrs.RPC_REQUEST_METHOD];
			const responseTimeout = parseInt(dataStreamAttrs[RpcRequestAttrs.RPC_REQUEST_RESPONSE_TIMEOUT_MS], 10);
			const version = parseInt(dataStreamAttrs[RpcRequestAttrs.RPC_REQUEST_VERSION], 10);
			if (!requestId || !method || Number.isNaN(responseTimeout) || Number.isNaN(version)) {
				this.log.warn("RPC data stream malformed: ".concat(RpcRequestAttrs.RPC_REQUEST_ID, " / ").concat(RpcRequestAttrs.RPC_REQUEST_METHOD, " / ").concat(RpcRequestAttrs.RPC_REQUEST_RESPONSE_TIMEOUT_MS, " / ").concat(RpcRequestAttrs.RPC_REQUEST_VERSION, " not set."));
				this.publishRpcResponsePacket(callerIdentity, requestId, null, RpcError.builtIn("APPLICATION_ERROR", "RPC data stream malformed"));
				return;
			}
			this.publishRpcAck(callerIdentity, requestId);
			if (version !== RPC_VERSION_V2) {
				this.publishRpcResponsePacket(callerIdentity, requestId, null, RpcError.builtIn("UNSUPPORTED_VERSION"));
				return;
			}
			let payload;
			try {
				payload = yield reader.readAll();
			} catch (e) {
				this.log.warn("Error reading RPC request payload: ".concat(e));
				this.publishRpcResponsePacket(callerIdentity, requestId, null, RpcError.builtIn("APPLICATION_ERROR", "Error reading RPC request payload", { cause: e }));
				return;
			}
			const handler = this.rpcHandlers.get(method);
			if (!handler) {
				this.publishRpcResponsePacket(callerIdentity, requestId, null, RpcError.builtIn("UNSUPPORTED_METHOD"));
				return;
			}
			let response;
			try {
				response = yield handler({
					requestId,
					callerIdentity,
					payload,
					responseTimeout
				});
			} catch (error) {
				let responseError;
				if (error instanceof RpcError) responseError = error;
				else {
					this.log.warn("Uncaught error returned by RPC handler for ".concat(method, ". Returning APPLICATION_ERROR instead."), error);
					responseError = RpcError.builtIn("APPLICATION_ERROR");
				}
				this.publishRpcResponsePacket(callerIdentity, requestId, null, responseError);
				return;
			}
			yield this.publishRpcResponse(callerIdentity, requestId, response !== null && response !== void 0 ? response : "");
		});
	}
	publishRpcAck(destinationIdentity, requestId) {
		this.emit("sendDataPacket", { packet: new DataPacket({
			destinationIdentities: [destinationIdentity],
			kind: DataPacket_Kind.RELIABLE,
			value: {
				case: "rpcAck",
				value: new RpcAck({ requestId })
			}
		}) });
	}
	publishRpcResponsePacket(destinationIdentity, requestId, payload, error) {
		this.emit("sendDataPacket", { packet: new DataPacket({
			destinationIdentities: [destinationIdentity],
			kind: DataPacket_Kind.RELIABLE,
			value: {
				case: "rpcResponse",
				value: new RpcResponse({
					requestId,
					value: error ? {
						case: "error",
						value: error.toProto()
					} : {
						case: "payload",
						value: payload !== null && payload !== void 0 ? payload : ""
					}
				})
			}
		}) });
	}
	/**
	* Send a successful RPC response payload, choosing the transport based on
	* the caller's client protocol version.
	*/
	publishRpcResponse(destinationIdentity, requestId, payload) {
		return __awaiter(this, void 0, void 0, function* () {
			if (this.getRemoteParticipantClientProtocol(destinationIdentity) >= 1) {
				yield this.outgoingDataStreamManager.sendText(payload, {
					topic: RPC_RESPONSE_DATA_STREAM_TOPIC,
					destinationIdentities: [destinationIdentity],
					attributes: { [RpcRequestAttrs.RPC_REQUEST_ID]: requestId }
				});
				return;
			}
			if (byteLength(payload) > MAX_V1_PAYLOAD_BYTES) {
				this.log.warn("RPC Response payload too large for request ".concat(requestId, ". To send larger responses, consider updating the sending client."));
				this.publishRpcResponsePacket(destinationIdentity, requestId, null, RpcError.builtIn("RESPONSE_PAYLOAD_TOO_LARGE"));
				return;
			}
			this.publishRpcResponsePacket(destinationIdentity, requestId, payload, null);
		});
	}
};
var RemoteAudioTrack = class extends RemoteTrack {
	constructor(mediaTrack, sid, receiver, audioContext, audioOutput, loggerOptions) {
		super(mediaTrack, sid, Track.Kind.Audio, receiver, loggerOptions);
		this.monitorReceiver = () => __awaiter(this, void 0, void 0, function* () {
			if (!this.receiver) {
				this._currentBitrate = 0;
				return;
			}
			const stats = yield this.getReceiverStats();
			if (stats && this.prevStats && this.receiver) this._currentBitrate = computeBitrate(stats, this.prevStats);
			this.prevStats = stats;
		});
		this.audioContext = audioContext;
		this.webAudioPluginNodes = [];
		if (audioOutput) this.sinkId = audioOutput.deviceId;
	}
	/**
	* sets the volume for all attached audio elements
	*/
	setVolume(volume) {
		var _a;
		for (const el of this.attachedElements) if (this.audioContext) (_a = this.gainNode) === null || _a === void 0 || _a.gain.setTargetAtTime(volume, 0, .1);
		else el.volume = volume;
		if (isReactNative()) this._mediaStreamTrack._setVolume(volume);
		this.elementVolume = volume;
	}
	/**
	* gets the volume of attached audio elements (loudest)
	*/
	getVolume() {
		if (this.elementVolume) return this.elementVolume;
		if (isReactNative()) return 1;
		let highestVolume = 0;
		this.attachedElements.forEach((element) => {
			if (element.volume > highestVolume) highestVolume = element.volume;
		});
		return highestVolume;
	}
	/**
	* calls setSinkId on all attached elements, if supported
	* @param deviceId audio output device
	*/
	setSinkId(deviceId) {
		return __awaiter(this, void 0, void 0, function* () {
			this.sinkId = deviceId;
			yield Promise.all(this.attachedElements.map((elm) => {
				if (!supportsSetSinkId(elm)) return;
				return elm.setSinkId(deviceId);
			}));
		});
	}
	attach(element) {
		const needsNewWebAudioConnection = this.attachedElements.length === 0;
		if (!element) element = super.attach();
		else super.attach(element);
		if (this.sinkId && supportsSetSinkId(element)) element.setSinkId(this.sinkId).catch((e) => {
			this.log.error("Failed to set sink id on remote audio track", e, this.logContext);
		});
		if (this.audioContext && needsNewWebAudioConnection) {
			this.log.debug("using audio context mapping", this.logContext);
			this.connectWebAudio(this.audioContext, element);
			element.volume = 0;
			element.muted = true;
		}
		if (this.elementVolume) this.setVolume(this.elementVolume);
		return element;
	}
	detach(element) {
		let detached;
		if (!element) {
			detached = super.detach();
			this.disconnectWebAudio();
		} else {
			detached = super.detach(element);
			if (this.audioContext) {
				if (this.attachedElements.length > 0) this.connectWebAudio(this.audioContext, this.attachedElements[0]);
				else this.disconnectWebAudio();
			}
		}
		return detached;
	}
	/**
	* @internal
	* @experimental
	*/
	setAudioContext(audioContext) {
		this.audioContext = audioContext;
		if (audioContext && this.attachedElements.length > 0) this.connectWebAudio(audioContext, this.attachedElements[0]);
		else if (!audioContext) this.disconnectWebAudio();
	}
	/**
	* @internal
	* @experimental
	* @param {AudioNode[]} nodes - An array of WebAudio nodes. These nodes should not be connected to each other when passed, as the sdk will take care of connecting them in the order of the array.
	*/
	setWebAudioPlugins(nodes) {
		this.webAudioPluginNodes = nodes;
		if (this.attachedElements.length > 0 && this.audioContext) this.connectWebAudio(this.audioContext, this.attachedElements[0]);
	}
	connectWebAudio(context, element) {
		this.disconnectWebAudio();
		this.sourceNode = context.createMediaStreamSource(element.srcObject);
		let lastNode = this.sourceNode;
		this.webAudioPluginNodes.forEach((node) => {
			lastNode.connect(node);
			lastNode = node;
		});
		this.gainNode = context.createGain();
		lastNode.connect(this.gainNode);
		this.gainNode.connect(context.destination);
		if (this.elementVolume) this.gainNode.gain.setTargetAtTime(this.elementVolume, 0, .1);
		if (context.state !== "running") context.resume().then(() => {
			if (context.state !== "running") this.emit(TrackEvent.AudioPlaybackFailed, /* @__PURE__ */ new Error("Audio Context couldn't be started automatically"));
		}).catch((e) => {
			this.emit(TrackEvent.AudioPlaybackFailed, e);
		});
	}
	disconnectWebAudio() {
		var _a, _b;
		(_a = this.gainNode) === null || _a === void 0 || _a.disconnect();
		(_b = this.sourceNode) === null || _b === void 0 || _b.disconnect();
		this.gainNode = void 0;
		this.sourceNode = void 0;
	}
	getReceiverStats() {
		return __awaiter(this, void 0, void 0, function* () {
			if (!this.receiver || !this.receiver.getStats) return;
			const stats = yield this.receiver.getStats();
			let receiverStats;
			stats.forEach((v) => {
				if (v.type === "inbound-rtp") receiverStats = {
					type: "audio",
					streamId: v.id,
					timestamp: v.timestamp,
					jitter: v.jitter,
					bytesReceived: v.bytesReceived,
					concealedSamples: v.concealedSamples,
					concealmentEvents: v.concealmentEvents,
					silentConcealedSamples: v.silentConcealedSamples,
					silentConcealmentEvents: v.silentConcealmentEvents,
					totalAudioEnergy: v.totalAudioEnergy,
					totalSamplesDuration: v.totalSamplesDuration
				};
			});
			return receiverStats;
		});
	}
};
var TrackPublication = class extends eventsExports.EventEmitter {
	constructor(kind, id, name, loggerOptions) {
		var _a;
		super();
		this.metadataMuted = false;
		this.encryption = Encryption_Type.NONE;
		this.log = livekitLogger;
		this.handleMuted = () => {
			this.emit(TrackEvent.Muted);
		};
		this.handleUnmuted = () => {
			this.emit(TrackEvent.Unmuted);
		};
		this.log = getLogger((_a = loggerOptions === null || loggerOptions === void 0 ? void 0 : loggerOptions.loggerName) !== null && _a !== void 0 ? _a : LoggerNames.Publication);
		this.loggerContextCb = this.loggerContextCb;
		this.setMaxListeners(100);
		this.kind = kind;
		this.trackSid = id;
		this.trackName = name;
		this.source = Track.Source.Unknown;
	}
	/** @internal */
	setTrack(track) {
		if (this.track) {
			this.track.off(TrackEvent.Muted, this.handleMuted);
			this.track.off(TrackEvent.Unmuted, this.handleUnmuted);
		}
		this.track = track;
		if (track) {
			track.on(TrackEvent.Muted, this.handleMuted);
			track.on(TrackEvent.Unmuted, this.handleUnmuted);
		}
	}
	get logContext() {
		var _a;
		return Object.assign(Object.assign({}, (_a = this.loggerContextCb) === null || _a === void 0 ? void 0 : _a.call(this)), getLogContextFromTrack(this));
	}
	get isMuted() {
		return this.metadataMuted;
	}
	get isEnabled() {
		return true;
	}
	get isSubscribed() {
		return this.track !== void 0;
	}
	get isEncrypted() {
		return this.encryption !== Encryption_Type.NONE;
	}
	/**
	* an [AudioTrack] if this publication holds an audio track
	*/
	get audioTrack() {
		if (isAudioTrack(this.track)) return this.track;
	}
	/**
	* an [VideoTrack] if this publication holds a video track
	*/
	get videoTrack() {
		if (isVideoTrack(this.track)) return this.track;
	}
	/** @internal */
	updateInfo(info) {
		this.trackSid = info.sid;
		this.trackName = info.name;
		this.source = Track.sourceFromProto(info.source);
		this.mimeType = info.mimeType;
		if (this.kind === Track.Kind.Video && info.width > 0) {
			this.dimensions = {
				width: info.width,
				height: info.height
			};
			this.simulcasted = info.simulcast;
		}
		this.encryption = info.encryption;
		this.trackInfo = info;
		this.log.debug("update publication info", Object.assign(Object.assign({}, this.logContext), { info }));
	}
};
(function(TrackPublication) {
	(function(SubscriptionStatus) {
		SubscriptionStatus["Desired"] = "desired";
		SubscriptionStatus["Subscribed"] = "subscribed";
		SubscriptionStatus["Unsubscribed"] = "unsubscribed";
	})(TrackPublication.SubscriptionStatus || (TrackPublication.SubscriptionStatus = {}));
	(function(PermissionStatus) {
		PermissionStatus["Allowed"] = "allowed";
		PermissionStatus["NotAllowed"] = "not_allowed";
	})(TrackPublication.PermissionStatus || (TrackPublication.PermissionStatus = {}));
})(TrackPublication || (TrackPublication = {}));
var LocalTrackPublication = class extends TrackPublication {
	get isUpstreamPaused() {
		var _a;
		return (_a = this.track) === null || _a === void 0 ? void 0 : _a.isUpstreamPaused;
	}
	constructor(kind, ti, track, loggerOptions) {
		super(kind, ti.sid, ti.name, loggerOptions);
		this.track = void 0;
		this.handleTrackEnded = () => {
			this.emit(TrackEvent.Ended);
		};
		this.handleCpuConstrained = () => {
			if (this.track && isVideoTrack(this.track)) this.emit(TrackEvent.CpuConstrained, this.track);
		};
		this.updateInfo(ti);
		this.setTrack(track);
	}
	setTrack(track) {
		if (this.track) {
			this.track.off(TrackEvent.Ended, this.handleTrackEnded);
			this.track.off(TrackEvent.CpuConstrained, this.handleCpuConstrained);
		}
		super.setTrack(track);
		if (track) {
			track.on(TrackEvent.Ended, this.handleTrackEnded);
			track.on(TrackEvent.CpuConstrained, this.handleCpuConstrained);
		}
	}
	get isMuted() {
		if (this.track) return this.track.isMuted;
		return super.isMuted;
	}
	get audioTrack() {
		return super.audioTrack;
	}
	get videoTrack() {
		return super.videoTrack;
	}
	get isLocal() {
		return true;
	}
	/**
	* Mute the track associated with this publication
	*/
	mute() {
		return __awaiter(this, void 0, void 0, function* () {
			var _a;
			return (_a = this.track) === null || _a === void 0 ? void 0 : _a.mute();
		});
	}
	/**
	* Unmute track associated with this publication
	*/
	unmute() {
		return __awaiter(this, void 0, void 0, function* () {
			var _a;
			return (_a = this.track) === null || _a === void 0 ? void 0 : _a.unmute();
		});
	}
	/**
	* Pauses the media stream track associated with this publication from being sent to the server
	* and signals "muted" event to other participants
	* Useful if you want to pause the stream without pausing the local media stream track
	*/
	pauseUpstream() {
		return __awaiter(this, void 0, void 0, function* () {
			var _a;
			yield (_a = this.track) === null || _a === void 0 ? void 0 : _a.pauseUpstream();
		});
	}
	/**
	* Resumes sending the media stream track associated with this publication to the server after a call to [[pauseUpstream()]]
	* and signals "unmuted" event to other participants (unless the track is explicitly muted)
	*/
	resumeUpstream() {
		return __awaiter(this, void 0, void 0, function* () {
			var _a;
			yield (_a = this.track) === null || _a === void 0 ? void 0 : _a.resumeUpstream();
		});
	}
	getTrackFeatures() {
		var _a;
		if (isAudioTrack(this.track)) {
			const settings = this.track.getSourceTrackSettings();
			const features = /* @__PURE__ */ new Set();
			if (settings.autoGainControl) features.add(AudioTrackFeature.TF_AUTO_GAIN_CONTROL);
			if (settings.echoCancellation) features.add(AudioTrackFeature.TF_ECHO_CANCELLATION);
			if (settings.noiseSuppression) features.add(AudioTrackFeature.TF_NOISE_SUPPRESSION);
			if (settings.channelCount && settings.channelCount > 1) features.add(AudioTrackFeature.TF_STEREO);
			if (!((_a = this.options) === null || _a === void 0 ? void 0 : _a.dtx)) features.add(AudioTrackFeature.TF_NO_DTX);
			if (this.track.enhancedNoiseCancellation) features.add(AudioTrackFeature.TF_ENHANCED_NOISE_CANCELLATION);
			return Array.from(features.values());
		} else return [];
	}
};
function createLocalTracks(options, loggerOptions) {
	return __awaiter(this, void 0, void 0, function* () {
		options !== null && options !== void 0 || (options = {});
		let attemptExactMatch = false;
		const _extractProcessorsFro = extractProcessorsFromOptions(options), audioProcessor = _extractProcessorsFro.audioProcessor, videoProcessor = _extractProcessorsFro.videoProcessor, internalOptions = _extractProcessorsFro.optionsWithoutProcessor;
		let retryAudioOptions = internalOptions.audio;
		let retryVideoOptions = internalOptions.video;
		if (audioProcessor && typeof internalOptions.audio === "object") internalOptions.audio.processor = audioProcessor;
		if (videoProcessor && typeof internalOptions.video === "object") internalOptions.video.processor = videoProcessor;
		if (options.audio && typeof internalOptions.audio === "object" && typeof internalOptions.audio.deviceId === "string") {
			const deviceId = internalOptions.audio.deviceId;
			internalOptions.audio.deviceId = { exact: deviceId };
			attemptExactMatch = true;
			retryAudioOptions = Object.assign(Object.assign({}, internalOptions.audio), { deviceId: { ideal: deviceId } });
		}
		if (internalOptions.video && typeof internalOptions.video === "object" && typeof internalOptions.video.deviceId === "string") {
			const deviceId = internalOptions.video.deviceId;
			internalOptions.video.deviceId = { exact: deviceId };
			attemptExactMatch = true;
			retryVideoOptions = Object.assign(Object.assign({}, internalOptions.video), { deviceId: { ideal: deviceId } });
		}
		if (internalOptions.audio === true) internalOptions.audio = { deviceId: "default" };
		else if (typeof internalOptions.audio === "object" && internalOptions.audio !== null) internalOptions.audio = Object.assign(Object.assign({}, internalOptions.audio), { deviceId: internalOptions.audio.deviceId || "default" });
		if (internalOptions.video === true) internalOptions.video = { deviceId: "default" };
		else if (typeof internalOptions.video === "object" && !internalOptions.video.deviceId) internalOptions.video.deviceId = "default";
		const constraints = constraintsForOptions(mergeDefaultOptions(internalOptions, audioDefaults, videoDefaults));
		const mediaPromise = navigator.mediaDevices.getUserMedia(constraints);
		if (internalOptions.audio) {
			DeviceManager.userMediaPromiseMap.set("audioinput", mediaPromise);
			mediaPromise.catch(() => DeviceManager.userMediaPromiseMap.delete("audioinput"));
		}
		if (internalOptions.video) {
			DeviceManager.userMediaPromiseMap.set("videoinput", mediaPromise);
			mediaPromise.catch(() => DeviceManager.userMediaPromiseMap.delete("videoinput"));
		}
		try {
			const stream = yield mediaPromise;
			return yield Promise.all(stream.getTracks().map((mediaStreamTrack) => __awaiter(this, void 0, void 0, function* () {
				const isAudio = mediaStreamTrack.kind === "audio";
				let trackConstraints;
				const conOrBool = isAudio ? constraints.audio : constraints.video;
				if (typeof conOrBool !== "boolean") trackConstraints = conOrBool;
				const newDeviceId = mediaStreamTrack.getSettings().deviceId;
				if ((trackConstraints === null || trackConstraints === void 0 ? void 0 : trackConstraints.deviceId) && unwrapConstraint(trackConstraints.deviceId) !== newDeviceId) trackConstraints.deviceId = newDeviceId;
				else if (!trackConstraints) trackConstraints = { deviceId: newDeviceId };
				const track = mediaTrackToLocalTrack(mediaStreamTrack, trackConstraints, loggerOptions);
				if (track.kind === Track.Kind.Video) track.source = Track.Source.Camera;
				else if (track.kind === Track.Kind.Audio) track.source = Track.Source.Microphone;
				track.mediaStream = stream;
				if (isAudioTrack(track) && audioProcessor) yield track.setProcessor(audioProcessor);
				else if (isVideoTrack(track) && videoProcessor) yield track.setProcessor(videoProcessor);
				return track;
			})));
		} catch (e) {
			if (!attemptExactMatch) throw e;
			return createLocalTracks(Object.assign(Object.assign({}, options), {
				audio: retryAudioOptions,
				video: retryVideoOptions
			}), loggerOptions);
		}
	});
}
/**
* Creates a [[LocalVideoTrack]] with getUserMedia()
* @param options
*/
function createLocalVideoTrack(options) {
	return __awaiter(this, void 0, void 0, function* () {
		return (yield createLocalTracks({
			audio: false,
			video: options !== null && options !== void 0 ? options : true
		}))[0];
	});
}
function createLocalAudioTrack(options) {
	return __awaiter(this, void 0, void 0, function* () {
		return (yield createLocalTracks({
			audio: options !== null && options !== void 0 ? options : true,
			video: false
		}))[0];
	});
}
var ConnectionQuality;
(function(ConnectionQuality) {
	ConnectionQuality["Excellent"] = "excellent";
	ConnectionQuality["Good"] = "good";
	ConnectionQuality["Poor"] = "poor";
	/**
	* Indicates that a participant has temporarily (or permanently) lost connection to LiveKit.
	* For permanent disconnection a `ParticipantDisconnected` event will be emitted after a timeout
	*/
	ConnectionQuality["Lost"] = "lost";
	ConnectionQuality["Unknown"] = "unknown";
})(ConnectionQuality || (ConnectionQuality = {}));
function qualityFromProto(q) {
	switch (q) {
		case ConnectionQuality$1.EXCELLENT: return ConnectionQuality.Excellent;
		case ConnectionQuality$1.GOOD: return ConnectionQuality.Good;
		case ConnectionQuality$1.POOR: return ConnectionQuality.Poor;
		case ConnectionQuality$1.LOST: return ConnectionQuality.Lost;
		default: return ConnectionQuality.Unknown;
	}
}
var Participant = class extends eventsExports.EventEmitter {
	get logContext() {
		var _a, _b;
		return Object.assign({}, (_b = (_a = this.loggerOptions) === null || _a === void 0 ? void 0 : _a.loggerContextCb) === null || _b === void 0 ? void 0 : _b.call(_a));
	}
	get isEncrypted() {
		return this.trackPublications.size > 0 && Array.from(this.trackPublications.values()).every((tr) => tr.isEncrypted);
	}
	get isAgent() {
		var _a;
		return ((_a = this.permissions) === null || _a === void 0 ? void 0 : _a.agent) || this.kind === ParticipantInfo_Kind.AGENT;
	}
	get isActive() {
		var _a;
		return ((_a = this.participantInfo) === null || _a === void 0 ? void 0 : _a.state) === ParticipantInfo_State.ACTIVE;
	}
	get kind() {
		return this._kind;
	}
	/** participant attributes, similar to metadata, but as a key/value map */
	get attributes() {
		return Object.freeze(Object.assign({}, this._attributes));
	}
	/** @internal */
	constructor(sid, identity, name, metadata, attributes, loggerOptions) {
		let kind = arguments.length > 6 && arguments[6] !== void 0 ? arguments[6] : ParticipantInfo_Kind.STANDARD;
		var _a;
		super();
		/** audio level between 0-1.0, 1 being loudest, 0 being softest */
		this.audioLevel = 0;
		/** if participant is currently speaking */
		this.isSpeaking = false;
		this._connectionQuality = ConnectionQuality.Unknown;
		this.log = livekitLogger;
		this.loggerOptions = loggerOptions;
		this.log = getLogger((_a = loggerOptions === null || loggerOptions === void 0 ? void 0 : loggerOptions.loggerName) !== null && _a !== void 0 ? _a : LoggerNames.Participant, () => this.logContext);
		this.setMaxListeners(100);
		this.sid = sid;
		this.identity = identity;
		this.name = name;
		this.metadata = metadata;
		this.audioTrackPublications = /* @__PURE__ */ new Map();
		this.videoTrackPublications = /* @__PURE__ */ new Map();
		this.trackPublications = /* @__PURE__ */ new Map();
		this._kind = kind;
		this._attributes = attributes !== null && attributes !== void 0 ? attributes : {};
	}
	getTrackPublications() {
		return Array.from(this.trackPublications.values());
	}
	/**
	* Finds the first track that matches the source filter, for example, getting
	* the user's camera track with getTrackBySource(Track.Source.Camera).
	*/
	getTrackPublication(source) {
		for (const _ref of this.trackPublications) {
			const pub = _slicedToArray(_ref, 2)[1];
			if (pub.source === source) return pub;
		}
	}
	/**
	* Finds the first track that matches the track's name.
	*/
	getTrackPublicationByName(name) {
		for (const _ref3 of this.trackPublications) {
			const pub = _slicedToArray(_ref3, 2)[1];
			if (pub.trackName === name) return pub;
		}
	}
	/**
	* Waits until the participant is active and ready to receive data messages
	* @returns a promise that resolves when the participant is active
	*/
	waitUntilActive() {
		if (this.isActive) return Promise.resolve();
		if (this.activeFuture) return this.activeFuture.promise;
		this.activeFuture = new Future();
		this.once(ParticipantEvent.Active, () => {
			var _a, _b;
			(_b = (_a = this.activeFuture) === null || _a === void 0 ? void 0 : _a.resolve) === null || _b === void 0 || _b.call(_a);
			this.activeFuture = void 0;
		});
		return this.activeFuture.promise;
	}
	get connectionQuality() {
		return this._connectionQuality;
	}
	get isCameraEnabled() {
		var _a;
		const track = this.getTrackPublication(Track.Source.Camera);
		return !((_a = track === null || track === void 0 ? void 0 : track.isMuted) !== null && _a !== void 0 ? _a : true);
	}
	get isMicrophoneEnabled() {
		var _a;
		const track = this.getTrackPublication(Track.Source.Microphone);
		return !((_a = track === null || track === void 0 ? void 0 : track.isMuted) !== null && _a !== void 0 ? _a : true);
	}
	get isScreenShareEnabled() {
		return !!this.getTrackPublication(Track.Source.ScreenShare);
	}
	get isLocal() {
		return false;
	}
	/** when participant joined the room */
	get joinedAt() {
		if (this.participantInfo) return /* @__PURE__ */ new Date(Number.parseInt(this.participantInfo.joinedAt.toString()) * 1e3);
		return /* @__PURE__ */ new Date();
	}
	/** @internal */
	updateInfo(info) {
		var _a;
		if (this.participantInfo && this.participantInfo.sid === info.sid && this.participantInfo.version > info.version) return false;
		this.identity = info.identity;
		this.sid = info.sid;
		this._setName(info.name);
		this._setMetadata(info.metadata);
		this._setAttributes(info.attributes);
		if (info.state === ParticipantInfo_State.ACTIVE && ((_a = this.participantInfo) === null || _a === void 0 ? void 0 : _a.state) !== ParticipantInfo_State.ACTIVE) this.emit(ParticipantEvent.Active);
		if (info.permission) this.setPermissions(info.permission);
		this.participantInfo = info;
		return true;
	}
	/**
	* Updates metadata from server
	**/
	_setMetadata(md) {
		const changed = this.metadata !== md;
		const prevMetadata = this.metadata;
		this.metadata = md;
		if (changed) this.emit(ParticipantEvent.ParticipantMetadataChanged, prevMetadata);
	}
	_setName(name) {
		const changed = this.name !== name;
		this.name = name;
		if (changed) this.emit(ParticipantEvent.ParticipantNameChanged, name);
	}
	/**
	* Updates metadata from server
	**/
	_setAttributes(attributes) {
		const diff = diffAttributes(this.attributes, attributes);
		this._attributes = attributes;
		if (Object.keys(diff).length > 0) this.emit(ParticipantEvent.AttributesChanged, diff);
	}
	/** @internal */
	setPermissions(permissions) {
		var _a, _b, _c, _d, _e, _f;
		const prevPermissions = this.permissions;
		const changed = permissions.canPublish !== ((_a = this.permissions) === null || _a === void 0 ? void 0 : _a.canPublish) || permissions.canSubscribe !== ((_b = this.permissions) === null || _b === void 0 ? void 0 : _b.canSubscribe) || permissions.canPublishData !== ((_c = this.permissions) === null || _c === void 0 ? void 0 : _c.canPublishData) || permissions.hidden !== ((_d = this.permissions) === null || _d === void 0 ? void 0 : _d.hidden) || permissions.recorder !== ((_e = this.permissions) === null || _e === void 0 ? void 0 : _e.recorder) || permissions.canPublishSources.length !== this.permissions.canPublishSources.length || permissions.canPublishSources.some((value, index) => {
			var _a;
			return value !== ((_a = this.permissions) === null || _a === void 0 ? void 0 : _a.canPublishSources[index]);
		}) || permissions.canSubscribeMetrics !== ((_f = this.permissions) === null || _f === void 0 ? void 0 : _f.canSubscribeMetrics);
		this.permissions = permissions;
		if (changed) this.emit(ParticipantEvent.ParticipantPermissionsChanged, prevPermissions);
		return changed;
	}
	/** @internal */
	setIsSpeaking(speaking) {
		if (speaking === this.isSpeaking) return;
		this.isSpeaking = speaking;
		if (speaking) this.lastSpokeAt = /* @__PURE__ */ new Date();
		this.emit(ParticipantEvent.IsSpeakingChanged, speaking);
	}
	/** @internal */
	setConnectionQuality(q) {
		const prevQuality = this._connectionQuality;
		this._connectionQuality = qualityFromProto(q);
		if (prevQuality !== this._connectionQuality) this.emit(ParticipantEvent.ConnectionQualityChanged, this._connectionQuality);
	}
	/**
	* @internal
	*/
	setDisconnected() {
		var _a, _b;
		if (this.activeFuture) {
			(_b = (_a = this.activeFuture).reject) === null || _b === void 0 || _b.call(_a, /* @__PURE__ */ new Error("Participant disconnected"));
			this.activeFuture = void 0;
		}
	}
	/**
	* @internal
	*/
	setAudioContext(ctx) {
		this.audioContext = ctx;
		this.audioTrackPublications.forEach((track) => isAudioTrack(track.track) && track.track.setAudioContext(ctx));
	}
	addTrackPublication(publication) {
		this.log.debug("adding track publication", {
			trackSid: publication.trackSid,
			source: publication.source,
			kind: publication.kind
		});
		publication.on(TrackEvent.Muted, () => {
			this.emit(ParticipantEvent.TrackMuted, publication);
		});
		publication.on(TrackEvent.Unmuted, () => {
			this.emit(ParticipantEvent.TrackUnmuted, publication);
		});
		const pub = publication;
		if (pub.track) pub.track.sid = publication.trackSid;
		this.trackPublications.set(publication.trackSid, publication);
		switch (publication.kind) {
			case Track.Kind.Audio:
				this.audioTrackPublications.set(publication.trackSid, publication);
				break;
			case Track.Kind.Video: this.videoTrackPublications.set(publication.trackSid, publication);
		}
	}
};
function trackPermissionToProto(perms) {
	var _a, _b, _c;
	if (!perms.participantSid && !perms.participantIdentity) throw new Error("Invalid track permission, must provide at least one of participantIdentity and participantSid");
	return new TrackPermission({
		participantIdentity: (_a = perms.participantIdentity) !== null && _a !== void 0 ? _a : "",
		participantSid: (_b = perms.participantSid) !== null && _b !== void 0 ? _b : "",
		allTracks: (_c = perms.allowAll) !== null && _c !== void 0 ? _c : false,
		trackSids: perms.allowedTrackSids || []
	});
}
var LocalParticipant = class extends Participant {
	/** @internal */
	constructor(sid, identity, engine, options, roomOutgoingDataStreamManager, roomOutgoingDataTrackManager, rpcClientManager, rpcServerManager) {
		super(sid, identity, void 0, void 0, void 0, {
			loggerName: options.loggerName,
			loggerContextCb: () => this.engine.logContext
		});
		this.pendingPublishing = /* @__PURE__ */ new Set();
		this.pendingPublishPromises = /* @__PURE__ */ new Map();
		this.participantTrackPermissions = [];
		this.allParticipantsAllowedToSubscribe = true;
		this.encryptionType = Encryption_Type.NONE;
		this.e2eeStateMutex = new _();
		this.enabledPublishVideoCodecs = [];
		this.handleReconnecting = () => {
			if (!this.reconnectFuture) this.reconnectFuture = new Future();
		};
		this.handleReconnected = () => {
			var _a, _b;
			(_b = (_a = this.reconnectFuture) === null || _a === void 0 ? void 0 : _a.resolve) === null || _b === void 0 || _b.call(_a);
			this.reconnectFuture = void 0;
			this.updateTrackSubscriptionPermissions();
		};
		this.handleClosing = () => {
			var _a, _b, _c, _d, _e, _f;
			if (this.reconnectFuture) {
				this.reconnectFuture.promise.catch((e) => this.log.warn(e.message));
				(_b = (_a = this.reconnectFuture) === null || _a === void 0 ? void 0 : _a.reject) === null || _b === void 0 || _b.call(_a, /* @__PURE__ */ new Error("Got disconnected during reconnection attempt"));
				this.reconnectFuture = void 0;
			}
			if (this.signalConnectedFuture) {
				(_d = (_c = this.signalConnectedFuture).reject) === null || _d === void 0 || _d.call(_c, /* @__PURE__ */ new Error("Got disconnected without signal connected"));
				this.signalConnectedFuture = void 0;
			}
			(_f = (_e = this.activeAgentFuture) === null || _e === void 0 ? void 0 : _e.reject) === null || _f === void 0 || _f.call(_e, /* @__PURE__ */ new Error("Got disconnected without active agent present"));
			this.activeAgentFuture = void 0;
			this.firstActiveAgent = void 0;
		};
		this.handleSignalConnected = (joinResponse) => {
			var _a, _b;
			if (joinResponse.participant) this.updateInfo(joinResponse.participant);
			if (!this.signalConnectedFuture) this.signalConnectedFuture = new Future();
			(_b = (_a = this.signalConnectedFuture).resolve) === null || _b === void 0 || _b.call(_a);
		};
		this.handleSignalRequestResponse = (response) => {
			const requestId = response.requestId, reason = response.reason, message = response.message;
			const targetRequest = this.pendingSignalRequests.get(requestId);
			if (targetRequest) {
				if (reason !== RequestResponse_Reason.OK) targetRequest.reject(new SignalRequestError(message, reason));
				this.pendingSignalRequests.delete(requestId);
			}
			switch (response.request.case) {
				case "publishDataTrack": {
					let error;
					switch (response.reason) {
						case RequestResponse_Reason.NOT_ALLOWED:
							error = DataTrackPublishError.notAllowed(response.message);
							break;
						case RequestResponse_Reason.DUPLICATE_NAME:
							error = DataTrackPublishError.duplicateName(response.message);
							break;
						case RequestResponse_Reason.INVALID_NAME:
							error = DataTrackPublishError.invalidName(response.message);
							break;
						case RequestResponse_Reason.LIMIT_EXCEEDED:
							error = DataTrackPublishError.limitReached(response.message);
							break;
						default: error = DataTrackPublishError.unknown(response.reason, response.message);
					}
					this.roomOutgoingDataTrackManager.receivedSfuPublishResponse(response.request.value.pubHandle, {
						type: "error",
						error
					});
					break;
				}
			}
		};
		this.updateTrackSubscriptionPermissions = () => {
			this.log.debug("updating track subscription permissions", {
				allParticipantsAllowed: this.allParticipantsAllowedToSubscribe,
				participantTrackPermissions: this.participantTrackPermissions
			});
			this.engine.client.sendUpdateSubscriptionPermissions(this.allParticipantsAllowedToSubscribe, this.participantTrackPermissions.map((p) => trackPermissionToProto(p)));
		};
		/** @internal */
		this.onTrackUnmuted = (track) => {
			this.onTrackMuted(track, track.isUpstreamPaused);
		};
		/** @internal */
		this.onTrackMuted = (track, muted) => {
			if (muted === void 0) muted = true;
			if (!track.sid) {
				this.log.error("could not update mute status for unpublished track", getLogContextFromTrack(track));
				return;
			}
			this.engine.updateMuteStatus(track.sid, muted);
		};
		this.onTrackUpstreamPaused = (track) => {
			this.log.debug("upstream paused", getLogContextFromTrack(track));
			this.onTrackMuted(track, true);
		};
		this.onTrackUpstreamResumed = (track) => {
			this.log.debug("upstream resumed", getLogContextFromTrack(track));
			this.onTrackMuted(track, track.isMuted);
		};
		this.onTrackFeatureUpdate = (track) => {
			const pub = this.audioTrackPublications.get(track.sid);
			if (!pub) {
				this.log.warn("Could not update local audio track settings, missing publication for track ".concat(track.sid));
				return;
			}
			this.engine.client.sendUpdateLocalAudioTrack(pub.trackSid, pub.getTrackFeatures());
		};
		this.onTrackCpuConstrained = (track, publication) => {
			this.log.debug("track cpu constrained", getLogContextFromTrack(publication));
			this.emit(ParticipantEvent.LocalTrackCpuConstrained, track, publication);
		};
		this.handleSubscribedQualityUpdate = (update) => __awaiter(this, void 0, void 0, function* () {
			var _a, e_1, _b, _c;
			var _d;
			if (!((_d = this.roomOptions) === null || _d === void 0 ? void 0 : _d.dynacast)) return;
			const pub = this.videoTrackPublications.get(update.trackSid);
			if (!pub) {
				this.log.warn("received subscribed quality update for unknown track", { trackSid: update.trackSid });
				return;
			}
			if (!pub.videoTrack) return;
			const newCodecs = yield pub.videoTrack.setPublishingCodecs(update.subscribedCodecs);
			try {
				for (var _e = true, newCodecs_1 = __asyncValues(newCodecs), newCodecs_1_1; newCodecs_1_1 = yield newCodecs_1.next(), _a = newCodecs_1_1.done, !_a; _e = true) {
					_c = newCodecs_1_1.value;
					_e = false;
					const codec = _c;
					if (isBackupCodec(codec)) {
						this.log.debug("publish ".concat(codec, " for ").concat(pub.videoTrack.sid), getLogContextFromTrack(pub));
						yield this.publishAdditionalCodecForTrack(pub.videoTrack, codec, pub.options);
					}
				}
			} catch (e_1_1) {
				e_1 = { error: e_1_1 };
			} finally {
				try {
					if (!_e && !_a && (_b = newCodecs_1.return)) yield _b.call(newCodecs_1);
				} finally {
					if (e_1) throw e_1.error;
				}
			}
		});
		this.handleLocalTrackUnpublished = (unpublished) => {
			const track = this.trackPublications.get(unpublished.trackSid);
			if (!track) {
				this.log.warn("received unpublished event for unknown track", { trackSid: unpublished.trackSid });
				return;
			}
			this.unpublishTrack(track.track);
		};
		this.handleTrackEnded = (track) => __awaiter(this, void 0, void 0, function* () {
			if (track.source === Track.Source.ScreenShare || track.source === Track.Source.ScreenShareAudio) {
				this.log.debug("unpublishing local track due to TrackEnded", getLogContextFromTrack(track));
				this.unpublishTrack(track);
			} else if (track.isUserProvided) yield track.mute();
			else if (isLocalAudioTrack(track) || isLocalVideoTrack(track)) try {
				if (isWeb()) try {
					const currentPermissions = yield navigator === null || navigator === void 0 ? void 0 : navigator.permissions.query({ name: track.source === Track.Source.Camera ? "camera" : "microphone" });
					if (currentPermissions && currentPermissions.state === "denied") {
						this.log.warn("user has revoked access to ".concat(track.source), getLogContextFromTrack(track));
						currentPermissions.onchange = () => {
							if (currentPermissions.state !== "denied") {
								if (!track.isMuted) track.restartTrack();
								currentPermissions.onchange = null;
							}
						};
						throw new Error("GetUserMedia Permission denied");
					}
				} catch (e) {}
				if (!track.isMuted) {
					this.log.debug("track ended, attempting to use a different device", getLogContextFromTrack(track));
					if (isLocalAudioTrack(track)) yield track.restartTrack({ deviceId: "default" });
					else yield track.restartTrack();
				}
			} catch (e) {
				this.log.warn("could not restart track, muting instead", getLogContextFromTrack(track));
				yield track.mute();
			}
		});
		this.audioTrackPublications = /* @__PURE__ */ new Map();
		this.videoTrackPublications = /* @__PURE__ */ new Map();
		this.trackPublications = /* @__PURE__ */ new Map();
		this.engine = engine;
		this.roomOptions = options;
		this.setupEngine(engine);
		this.activeDeviceMap = /* @__PURE__ */ new Map([
			["audioinput", "default"],
			["videoinput", "default"],
			["audiooutput", "default"]
		]);
		this.pendingSignalRequests = /* @__PURE__ */ new Map();
		this.roomOutgoingDataStreamManager = roomOutgoingDataStreamManager;
		this.roomOutgoingDataTrackManager = roomOutgoingDataTrackManager;
		this.rpcClientManager = rpcClientManager;
		this.rpcServerManager = rpcServerManager;
	}
	get lastCameraError() {
		return this.cameraError;
	}
	get lastMicrophoneError() {
		return this.microphoneError;
	}
	get isE2EEEnabled() {
		return this.encryptionType !== Encryption_Type.NONE;
	}
	getTrackPublication(source) {
		const track = super.getTrackPublication(source);
		if (track) return track;
	}
	getTrackPublicationByName(name) {
		const track = super.getTrackPublicationByName(name);
		if (track) return track;
	}
	/**
	* @internal
	*/
	setupEngine(engine) {
		var _a;
		this.engine = engine;
		this.engine.on(EngineEvent.RemoteMute, (trackSid, muted) => {
			const pub = this.trackPublications.get(trackSid);
			if (!pub || !pub.track) return;
			if (muted) pub.mute();
			else pub.unmute();
		});
		if ((_a = this.signalConnectedFuture) === null || _a === void 0 ? void 0 : _a.isResolved) this.signalConnectedFuture = void 0;
		this.engine.on(EngineEvent.Connected, this.handleReconnected).on(EngineEvent.SignalConnected, this.handleSignalConnected).on(EngineEvent.SignalRestarted, this.handleReconnected).on(EngineEvent.SignalResumed, this.handleReconnected).on(EngineEvent.Restarting, this.handleReconnecting).on(EngineEvent.Resuming, this.handleReconnecting).on(EngineEvent.LocalTrackUnpublished, this.handleLocalTrackUnpublished).on(EngineEvent.SubscribedQualityUpdate, this.handleSubscribedQualityUpdate).on(EngineEvent.Closing, this.handleClosing).on(EngineEvent.SignalRequestResponse, this.handleSignalRequestResponse);
	}
	/**
	* Sets and updates the metadata of the local participant.
	* Note: this requires `canUpdateOwnMetadata` permission.
	* method will throw if the user doesn't have the required permissions
	* @param metadata
	*/
	setMetadata(metadata) {
		return __awaiter(this, void 0, void 0, function* () {
			yield this.requestMetadataUpdate({ metadata });
		});
	}
	/**
	* Sets and updates the name of the local participant.
	* Note: this requires `canUpdateOwnMetadata` permission.
	* method will throw if the user doesn't have the required permissions
	* @param metadata
	*/
	setName(name) {
		return __awaiter(this, void 0, void 0, function* () {
			yield this.requestMetadataUpdate({ name });
		});
	}
	/**
	* Set or update participant attributes. It will make updates only to keys that
	* are present in `attributes`, and will not override others.
	* Note: this requires `canUpdateOwnMetadata` permission.
	* @param attributes attributes to update
	*/
	setAttributes(attributes) {
		return __awaiter(this, void 0, void 0, function* () {
			yield this.requestMetadataUpdate({ attributes });
		});
	}
	requestMetadataUpdate(_a) {
		return __awaiter(this, arguments, void 0, function(_ref) {
			var _this = this;
			let metadata = _ref.metadata, name = _ref.name, attributes = _ref.attributes;
			return function* () {
				return new TypedPromise((resolve, reject) => __awaiter(_this, void 0, void 0, function* () {
					var _a, _b;
					try {
						let isRejected = false;
						const requestId = yield this.engine.client.sendUpdateLocalMetadata((_a = metadata !== null && metadata !== void 0 ? metadata : this.metadata) !== null && _a !== void 0 ? _a : "", (_b = name !== null && name !== void 0 ? name : this.name) !== null && _b !== void 0 ? _b : "", attributes);
						const startTime = performance.now();
						this.pendingSignalRequests.set(requestId, {
							resolve,
							reject: (error) => {
								reject(error);
								isRejected = true;
							},
							values: {
								name,
								metadata,
								attributes
							}
						});
						while (performance.now() - startTime < 5e3 && !isRejected) {
							if ((!name || this.name === name) && (!metadata || this.metadata === metadata) && (!attributes || Object.entries(attributes).every((_ref2) => {
								let _ref3 = _slicedToArray(_ref2, 2), key = _ref3[0], value = _ref3[1];
								return this.attributes[key] === value || value === "" && !this.attributes[key];
							}))) {
								this.pendingSignalRequests.delete(requestId);
								resolve();
								return;
							}
							yield sleep(50);
						}
						reject(new SignalRequestError("Request to update local metadata timed out", "TimeoutError"));
					} catch (e) {
						if (e instanceof Error) reject(e);
						else reject(new Error(String(e)));
					}
				}));
			}();
		});
	}
	/**
	* Enable or disable a participant's camera track.
	*
	* If a track has already published, it'll mute or unmute the track.
	* Resolves with a `LocalTrackPublication` instance if successful and `undefined` otherwise
	*/
	setCameraEnabled(enabled, options, publishOptions) {
		return this.setTrackEnabled(Track.Source.Camera, enabled, options, publishOptions);
	}
	/**
	* Enable or disable a participant's microphone track.
	*
	* If a track has already published, it'll mute or unmute the track.
	* Resolves with a `LocalTrackPublication` instance if successful and `undefined` otherwise
	*/
	setMicrophoneEnabled(enabled, options, publishOptions) {
		return this.setTrackEnabled(Track.Source.Microphone, enabled, options, publishOptions);
	}
	/**
	* Start or stop sharing a participant's screen
	* Resolves with a `LocalTrackPublication` instance if successful and `undefined` otherwise
	*/
	setScreenShareEnabled(enabled, options, publishOptions) {
		return this.setTrackEnabled(Track.Source.ScreenShare, enabled, options, publishOptions);
	}
	/** @internal */
	setE2EEEnabled(enabled) {
		return __awaiter(this, void 0, void 0, function* () {
			const unlock = yield this.e2eeStateMutex.lock();
			try {
				this.encryptionType = enabled ? Encryption_Type.GCM : Encryption_Type.NONE;
				yield Promise.all(this.pendingPublishPromises.values());
				if (this.trackPublications.size === 0 || Array.from(this.trackPublications.values()).every((pub) => pub.isEncrypted === enabled)) return;
				yield this.republishAllTracks(void 0, false);
			} finally {
				unlock();
			}
		});
	}
	setTrackEnabled(source, enabled, options, publishOptions) {
		return __awaiter(this, void 0, void 0, function* () {
			var _a, _b;
			this.log.debug("setTrackEnabled", {
				source,
				enabled
			});
			if (this.republishPromise) yield this.republishPromise;
			let track = this.getTrackPublication(source);
			if (enabled) {
				if (track) yield track.unmute();
				else {
					let localTracks;
					if (this.pendingPublishing.has(source)) {
						const pendingTrack = yield this.waitForPendingPublicationOfSource(source);
						if (!pendingTrack) this.log.info("waiting for pending publication promise timed out", { source });
						yield pendingTrack === null || pendingTrack === void 0 ? void 0 : pendingTrack.unmute();
						return pendingTrack;
					}
					this.pendingPublishing.add(source);
					try {
						switch (source) {
							case Track.Source.Camera:
								localTracks = yield this.createTracks({ video: (_a = options) !== null && _a !== void 0 ? _a : true });
								break;
							case Track.Source.Microphone:
								localTracks = yield this.createTracks({ audio: (_b = options) !== null && _b !== void 0 ? _b : true });
								break;
							case Track.Source.ScreenShare:
								localTracks = yield this.createScreenTracks(Object.assign({}, options));
								break;
							default: throw new TrackInvalidError(source);
						}
					} catch (e) {
						localTracks === null || localTracks === void 0 || localTracks.forEach((tr) => {
							tr.stop();
						});
						if (e instanceof Error) this.emit(ParticipantEvent.MediaDevicesError, e, sourceToKind(source));
						this.pendingPublishing.delete(source);
						throw e;
					}
					for (const localTrack of localTracks) {
						const opts = Object.assign(Object.assign({}, this.roomOptions.publishDefaults), options);
						if (source === Track.Source.Microphone && isAudioTrack(localTrack) && opts.preConnectBuffer) {
							this.log.info("starting preconnect buffer for microphone");
							localTrack.startPreConnectBuffer();
						}
					}
					try {
						const publishPromises = [];
						for (const localTrack of localTracks) {
							this.log.info("publishing track", getLogContextFromTrack(localTrack));
							publishPromises.push(this.publishTrack(localTrack, publishOptions));
						}
						track = _slicedToArray(yield Promise.all(publishPromises), 1)[0];
					} catch (e) {
						localTracks === null || localTracks === void 0 || localTracks.forEach((tr) => {
							tr.stop();
						});
						throw e;
					} finally {
						this.pendingPublishing.delete(source);
					}
				}
			} else {
				if (!(track === null || track === void 0 ? void 0 : track.track) && this.pendingPublishing.has(source)) {
					track = yield this.waitForPendingPublicationOfSource(source);
					if (!track) this.log.info("waiting for pending publication promise timed out", { source });
				}
				if (track && track.track) {
					if (source === Track.Source.ScreenShare) {
						const unpublishPromises = [this.unpublishTrack(track.track)];
						const screenAudioTrack = this.getTrackPublication(Track.Source.ScreenShareAudio);
						if (screenAudioTrack && screenAudioTrack.track) unpublishPromises.push(this.unpublishTrack(screenAudioTrack.track));
						track = _slicedToArray(yield Promise.all(unpublishPromises), 1)[0];
					} else yield track.mute();
				}
			}
			return track;
		});
	}
	/**
	* Publish both camera and microphone at the same time. This is useful for
	* displaying a single Permission Dialog box to the end user.
	*/
	enableCameraAndMicrophone() {
		return __awaiter(this, void 0, void 0, function* () {
			if (this.pendingPublishing.has(Track.Source.Camera) || this.pendingPublishing.has(Track.Source.Microphone)) return;
			this.pendingPublishing.add(Track.Source.Camera);
			this.pendingPublishing.add(Track.Source.Microphone);
			try {
				const tracks = yield this.createTracks({
					audio: true,
					video: true
				});
				yield Promise.all(tracks.map((track) => this.publishTrack(track)));
			} finally {
				this.pendingPublishing.delete(Track.Source.Camera);
				this.pendingPublishing.delete(Track.Source.Microphone);
			}
		});
	}
	/**
	* Create local camera and/or microphone tracks
	* @param options
	* @returns
	*/
	createTracks(options) {
		return __awaiter(this, void 0, void 0, function* () {
			var _a, _b;
			options !== null && options !== void 0 || (options = {});
			const mergedOptionsWithProcessors = mergeDefaultOptions(options, (_a = this.roomOptions) === null || _a === void 0 ? void 0 : _a.audioCaptureDefaults, (_b = this.roomOptions) === null || _b === void 0 ? void 0 : _b.videoCaptureDefaults);
			try {
				return (yield createLocalTracks(mergedOptionsWithProcessors, {
					loggerName: this.roomOptions.loggerName,
					loggerContextCb: () => this.logContext
				})).map((track) => {
					if (isAudioTrack(track)) {
						this.microphoneError = void 0;
						track.setAudioContext(this.audioContext);
						track.source = Track.Source.Microphone;
						this.emit(ParticipantEvent.AudioStreamAcquired);
					}
					if (isVideoTrack(track)) {
						this.cameraError = void 0;
						track.source = Track.Source.Camera;
					}
					return track;
				});
			} catch (err) {
				if (err instanceof Error) {
					if (options.audio) this.microphoneError = err;
					if (options.video) this.cameraError = err;
				}
				throw err;
			}
		});
	}
	/**
	* Creates a screen capture tracks with getDisplayMedia().
	* A LocalVideoTrack is always created and returned.
	* If { audio: true }, and the browser supports audio capture, a LocalAudioTrack is also created.
	*/
	createScreenTracks(options) {
		return __awaiter(this, void 0, void 0, function* () {
			if (options === void 0) options = {};
			if (navigator.mediaDevices.getDisplayMedia === void 0) throw new DeviceUnsupportedError("getDisplayMedia not supported");
			if (options.resolution === void 0 && !isSafari17Based()) options.resolution = ScreenSharePresets.h1080fps30.resolution;
			const constraints = screenCaptureToDisplayMediaStreamOptions(options);
			const stream = yield navigator.mediaDevices.getDisplayMedia(constraints);
			const tracks = stream.getVideoTracks();
			if (tracks.length === 0) throw new TrackInvalidError("no video track found");
			const screenVideo = new LocalVideoTrack(tracks[0], void 0, false, {
				loggerName: this.roomOptions.loggerName,
				loggerContextCb: () => this.logContext
			});
			screenVideo.source = Track.Source.ScreenShare;
			if (options.contentHint) screenVideo.mediaStreamTrack.contentHint = options.contentHint;
			const localTracks = [screenVideo];
			if (stream.getAudioTracks().length > 0) {
				this.emit(ParticipantEvent.AudioStreamAcquired);
				const screenAudio = new LocalAudioTrack(stream.getAudioTracks()[0], void 0, false, this.audioContext, {
					loggerName: this.roomOptions.loggerName,
					loggerContextCb: () => this.logContext
				});
				screenAudio.source = Track.Source.ScreenShareAudio;
				localTracks.push(screenAudio);
			}
			return localTracks;
		});
	}
	/**
	* Publish a new track to the room
	* @param track
	* @param options
	*/
	publishTrack(track, options) {
		return __awaiter(this, void 0, void 0, function* () {
			return this.publishOrRepublishTrack(track, options);
		});
	}
	/**
	* Waits for the engine's next `Restarted` event. Unlike `engine.waitForRestarted`, this does
	* not short-circuit when `pcState === Connected` — at the point this is called (right after a
	* `NegotiationError`) the PC transport is still connected, but `fullReconnectOnNext` has been
	* set and `attemptReconnect` is queued via setTimeout. We need to wait for that restart to
	* actually complete (which clears `pendingTrackResolvers` via `cleanupClient`) before retrying.
	*/
	waitForNextEngineRestart() {
		let timeoutMs = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 15e3;
		return new Promise((resolve, reject) => {
			const cleanup = () => {
				clearTimeout(timeout);
				this.engine.off(EngineEvent.Restarted, onRestarted);
				this.engine.off(EngineEvent.Closing, onClosing);
			};
			const onRestarted = () => {
				cleanup();
				resolve();
			};
			const onClosing = () => {
				cleanup();
				reject(/* @__PURE__ */ new Error("engine closed before restart completed"));
			};
			const timeout = setTimeout(() => {
				cleanup();
				reject(/* @__PURE__ */ new Error("timed out waiting for engine restart"));
			}, timeoutMs);
			this.engine.once(EngineEvent.Restarted, onRestarted);
			this.engine.once(EngineEvent.Closing, onClosing);
		});
	}
	publishOrRepublishTrack(track_1, options_1) {
		return __awaiter(this, arguments, void 0, function(track, options) {
			var _this2 = this;
			let isRepublish = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : false;
			let hasRetriedAfterNegotiationError = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : false;
			return function* () {
				var _a, _b, _c, _d;
				if (isLocalAudioTrack(track)) track.setAudioContext(_this2.audioContext);
				yield (_a = _this2.reconnectFuture) === null || _a === void 0 ? void 0 : _a.promise;
				if (_this2.republishPromise && !isRepublish) yield _this2.republishPromise;
				if (isLocalTrack(track) && _this2.pendingPublishPromises.has(track)) yield _this2.pendingPublishPromises.get(track);
				let defaultConstraints;
				if (track instanceof MediaStreamTrack) defaultConstraints = track.getConstraints();
				else {
					defaultConstraints = track.constraints;
					let deviceKind = void 0;
					switch (track.source) {
						case Track.Source.Microphone:
							deviceKind = "audioinput";
							break;
						case Track.Source.Camera: deviceKind = "videoinput";
					}
					if (deviceKind && _this2.activeDeviceMap.has(deviceKind)) defaultConstraints = Object.assign(Object.assign({}, defaultConstraints), { deviceId: _this2.activeDeviceMap.get(deviceKind) });
				}
				if (track instanceof MediaStreamTrack) switch (track.kind) {
					case "audio":
						track = new LocalAudioTrack(track, defaultConstraints, true, _this2.audioContext, {
							loggerName: _this2.roomOptions.loggerName,
							loggerContextCb: () => _this2.logContext
						});
						break;
					case "video":
						track = new LocalVideoTrack(track, defaultConstraints, true, {
							loggerName: _this2.roomOptions.loggerName,
							loggerContextCb: () => _this2.logContext
						});
						break;
					default: throw new TrackInvalidError("unsupported MediaStreamTrack kind ".concat(track.kind));
				}
				else track.updateLoggerOptions({
					loggerName: _this2.roomOptions.loggerName,
					loggerContextCb: () => _this2.logContext
				});
				let existingPublication;
				_this2.trackPublications.forEach((publication) => {
					if (!publication.track) return;
					if (publication.track === track) existingPublication = publication;
				});
				if (existingPublication) {
					_this2.log.warn("track has already been published, skipping", getLogContextFromTrack(existingPublication));
					return existingPublication;
				}
				const opts = Object.assign(Object.assign({}, _this2.roomOptions.publishDefaults), options);
				const isStereoInput = "channelCount" in track.mediaStreamTrack.getSettings() && track.mediaStreamTrack.getSettings().channelCount === 2 || track.mediaStreamTrack.getConstraints().channelCount === 2;
				const isStereo = (_b = opts.forceStereo) !== null && _b !== void 0 ? _b : isStereoInput;
				if (isStereo) {
					if (opts.dtx === void 0) _this2.log.debug("Opus DTX will be disabled for stereo tracks by default. Enable them explicitly to make it work.", getLogContextFromTrack(track));
					if (opts.red === void 0) _this2.log.debug("Opus RED will be disabled for stereo tracks by default. Enable them explicitly to make it work.");
					(_c = opts.dtx) !== null && _c !== void 0 || (opts.dtx = false);
					(_d = opts.red) !== null && _d !== void 0 || (opts.red = false);
				}
				if (!isE2EESimulcastSupported() && _this2.roomOptions.e2ee) {
					_this2.log.info("End-to-end encryption is set up, simulcast publishing will be disabled on Safari versions and iOS browsers running iOS < v17.2");
					opts.simulcast = false;
				}
				if (opts.source) track.source = opts.source;
				const publishPromise = new Promise((resolve, reject) => __awaiter(_this2, void 0, void 0, function* () {
					try {
						if (this.engine.client.currentState !== SignalConnectionState.CONNECTED) {
							this.log.debug("deferring track publication until signal is connected", { track: getLogContextFromTrack(track) });
							let publicationTimedOut = false;
							const timeout = setTimeout(() => {
								publicationTimedOut = true;
								track.stop();
								reject(new PublishTrackError("publishing rejected as engine not connected within timeout", 408));
							}, 15e3);
							yield this.waitUntilEngineConnected();
							clearTimeout(timeout);
							if (publicationTimedOut) return;
							resolve(yield this.publish(track, opts, isStereo));
						} else try {
							resolve(yield this.publish(track, opts, isStereo));
						} catch (e) {
							reject(e);
						}
					} catch (e) {
						reject(e);
					}
				}));
				_this2.pendingPublishPromises.set(track, publishPromise);
				try {
					return yield publishPromise;
				} catch (e) {
					if (!hasRetriedAfterNegotiationError && e instanceof NegotiationError) {
						_this2.log.warn("negotiation due to track publish failed, retrying after reconnect", { error: e });
						_this2.pendingPublishPromises.delete(track);
						yield _this2.waitForNextEngineRestart();
						return yield _this2.publishOrRepublishTrack(track, options, isRepublish, true);
					}
					throw e;
				} finally {
					_this2.pendingPublishPromises.delete(track);
				}
			}();
		});
	}
	waitUntilEngineConnected() {
		if (!this.signalConnectedFuture) this.signalConnectedFuture = new Future();
		return this.signalConnectedFuture.promise;
	}
	hasPermissionsToPublish(track) {
		if (!this.permissions) {
			this.log.warn("no permissions present for publishing track", getLogContextFromTrack(track));
			return false;
		}
		const _this$permissions = this.permissions, canPublish = _this$permissions.canPublish, canPublishSources = _this$permissions.canPublishSources;
		if (canPublish && (canPublishSources.length === 0 || canPublishSources.map((source) => getTrackSourceFromProto(source)).includes(track.source))) return true;
		this.log.warn("insufficient permissions to publish", getLogContextFromTrack(track));
		return false;
	}
	publish(track, opts, isStereo) {
		return __awaiter(this, void 0, void 0, function* () {
			var _a, _b, _c, _d, _e, _f, _g, _h, _j, _k;
			if (!this.hasPermissionsToPublish(track)) throw new PublishTrackError("failed to publish track, insufficient permissions", 403);
			if (Array.from(this.trackPublications.values()).find((publishedTrack) => isLocalTrack(track) && publishedTrack.source === track.source) && track.source !== Track.Source.Unknown) this.log.info("publishing a second track with the same source: ".concat(track.source), getLogContextFromTrack(track));
			if (opts.stopMicTrackOnMute && isAudioTrack(track)) track.stopOnMute = true;
			if (track.source === Track.Source.ScreenShare && isFireFox()) opts.simulcast = false;
			if (opts.videoCodec === "av1" && !supportsAV1()) opts.videoCodec = void 0;
			if (opts.videoCodec === "vp9" && !supportsVP9()) opts.videoCodec = void 0;
			if (opts.videoCodec === void 0) opts.videoCodec = defaultVideoCodec;
			if (this.enabledPublishVideoCodecs.length > 0) {
				if (!this.enabledPublishVideoCodecs.some((c) => opts.videoCodec === mimeTypeToVideoCodecString(c.mime))) opts.videoCodec = mimeTypeToVideoCodecString(this.enabledPublishVideoCodecs[0].mime);
			}
			const videoCodec = opts.videoCodec;
			track.on(TrackEvent.Muted, this.onTrackMuted);
			track.on(TrackEvent.Unmuted, this.onTrackUnmuted);
			track.on(TrackEvent.Ended, this.handleTrackEnded);
			track.on(TrackEvent.UpstreamPaused, this.onTrackUpstreamPaused);
			track.on(TrackEvent.UpstreamResumed, this.onTrackUpstreamResumed);
			track.on(TrackEvent.AudioTrackFeatureUpdate, this.onTrackFeatureUpdate);
			const audioFeatures = [];
			const disableDtx = !((_a = opts.dtx) !== null && _a !== void 0 ? _a : true);
			const settings = track.getSourceTrackSettings();
			if (settings.autoGainControl) audioFeatures.push(AudioTrackFeature.TF_AUTO_GAIN_CONTROL);
			if (settings.echoCancellation) audioFeatures.push(AudioTrackFeature.TF_ECHO_CANCELLATION);
			if (settings.noiseSuppression) audioFeatures.push(AudioTrackFeature.TF_NOISE_SUPPRESSION);
			if (settings.channelCount && settings.channelCount > 1) audioFeatures.push(AudioTrackFeature.TF_STEREO);
			if (disableDtx) audioFeatures.push(AudioTrackFeature.TF_NO_DTX);
			if (isLocalAudioTrack(track) && track.hasPreConnectBuffer) audioFeatures.push(AudioTrackFeature.TF_PRECONNECT_BUFFER);
			const packetTrailerFeatures = this.normalizeRequestedFrameMetadataOptions(track, opts);
			const req = new AddTrackRequest({
				cid: track.mediaStreamTrack.id,
				name: opts.name,
				type: Track.kindToProto(track.kind),
				muted: track.isMuted,
				source: Track.sourceToProto(track.source),
				disableDtx,
				encryption: this.encryptionType,
				stereo: isStereo,
				disableRed: this.isE2EEEnabled || !((_b = opts.red) !== null && _b !== void 0 ? _b : true),
				stream: opts === null || opts === void 0 ? void 0 : opts.stream,
				backupCodecPolicy: opts === null || opts === void 0 ? void 0 : opts.backupCodecPolicy,
				audioFeatures,
				packetTrailerFeatures
			});
			let encodings;
			if (track.kind === Track.Kind.Video) {
				let dims;
				try {
					dims = yield track.waitForDimensions();
				} catch (e) {
					const defaultRes = (_d = (_c = this.roomOptions.videoCaptureDefaults) === null || _c === void 0 ? void 0 : _c.resolution) !== null && _d !== void 0 ? _d : VideoPresets.h720.resolution;
					dims = {
						width: defaultRes.width,
						height: defaultRes.height
					};
					this.log.error("could not determine track dimensions, using defaults", Object.assign(Object.assign({}, getLogContextFromTrack(track)), { dims }));
				}
				req.width = dims.width;
				req.height = dims.height;
				if (isLocalVideoTrack(track)) {
					if (isSVCCodec(videoCodec)) {
						if (track.source === Track.Source.ScreenShare) {
							opts.scalabilityMode = "L1T3";
							if ("contentHint" in track.mediaStreamTrack) {
								track.mediaStreamTrack.contentHint = "motion";
								this.log.debug("forcing contentHint to motion for screenshare with SVC codecs", getLogContextFromTrack(track));
							}
						}
						opts.scalabilityMode = (_e = opts.scalabilityMode) !== null && _e !== void 0 ? _e : "L3T3_KEY";
					}
					req.simulcastCodecs = [new SimulcastCodec({
						codec: videoCodec,
						cid: track.mediaStreamTrack.id
					})];
					if (opts.backupCodec === true) opts.backupCodec = { codec: defaultVideoCodec };
					if (opts.backupCodec && videoCodec !== opts.backupCodec.codec && req.encryption === Encryption_Type.NONE) {
						if (!this.roomOptions.dynacast) this.roomOptions.dynacast = true;
						req.simulcastCodecs.push(new SimulcastCodec({
							codec: opts.backupCodec.codec,
							cid: ""
						}));
					}
				}
				encodings = computeVideoEncodings(track.source === Track.Source.ScreenShare, req.width, req.height, opts);
				req.layers = videoLayersFromEncodings(req.width, req.height, encodings, isSVCCodec(opts.videoCodec));
			} else if (track.kind === Track.Kind.Audio) encodings = [{
				maxBitrate: (_f = opts.audioPreset) === null || _f === void 0 ? void 0 : _f.maxBitrate,
				priority: (_h = (_g = opts.audioPreset) === null || _g === void 0 ? void 0 : _g.priority) !== null && _h !== void 0 ? _h : "high",
				networkPriority: (_k = (_j = opts.audioPreset) === null || _j === void 0 ? void 0 : _j.priority) !== null && _k !== void 0 ? _k : "high"
			}];
			if (!this.engine || this.engine.isClosed) throw new UnexpectedConnectionState("cannot publish track when not connected");
			const negotiate = () => __awaiter(this, void 0, void 0, function* () {
				var _a, _b, _c, _d;
				if (!this.engine.pcManager) throw new UnexpectedConnectionState("pcManager is not ready");
				track.sender = yield this.engine.createSender(track, opts, encodings);
				if (isLocalVideoTrack(track)) track.publishOptions = opts;
				this.emit(ParticipantEvent.LocalSenderCreated, track.sender, track);
				if (isLocalVideoTrack(track)) {
					(_a = opts.degradationPreference) !== null && _a !== void 0 || (opts.degradationPreference = getDefaultDegradationPreference(track));
					track.setDegradationPreference(opts.degradationPreference);
				}
				if (encodings) {
					if (isFireFox() && track.kind === Track.Kind.Audio) {
						let trackTransceiver = void 0;
						for (const transceiver of this.engine.pcManager.publisher.getTransceivers()) if (transceiver.sender === track.sender) {
							trackTransceiver = transceiver;
							break;
						}
						if (trackTransceiver) this.engine.pcManager.publisher.setTrackCodecBitrate({
							transceiver: trackTransceiver,
							codec: "opus",
							maxbr: ((_b = encodings[0]) === null || _b === void 0 ? void 0 : _b.maxBitrate) ? encodings[0].maxBitrate / 1e3 : 0
						});
					} else if (track.codec && isVideoCodec(track.codec)) {
						const targetBitrate = isSVCCodec(track.codec) ? (_d = (_c = encodings[0]) === null || _c === void 0 ? void 0 : _c.maxBitrate) !== null && _d !== void 0 ? _d : 0 : encodings.reduce((sum, enc) => {
							var _a;
							return sum + ((_a = enc.maxBitrate) !== null && _a !== void 0 ? _a : 0);
						}, 0);
						if (targetBitrate > 0) this.engine.pcManager.publisher.setTrackCodecBitrate({
							cid: req.cid,
							codec: track.codec,
							maxbr: targetBitrate / 1e3,
							isScreenShare: track.source === Track.Source.ScreenShare
						});
					}
				}
				yield this.engine.negotiate();
			});
			let ti;
			const addTrackPromise = new Promise((resolve, reject) => __awaiter(this, void 0, void 0, function* () {
				var _a;
				try {
					ti = yield this.engine.addTrack(req);
					resolve(ti);
				} catch (err) {
					if (track.sender && ((_a = this.engine.pcManager) === null || _a === void 0 ? void 0 : _a.publisher)) {
						try {
							this.engine.pcManager.publisher.removeTrack(track.sender);
						} catch (e) {
							this.log.error(e);
						}
						yield this.engine.negotiate().catch((negotiateErr) => {
							this.log.error("failed to negotiate after removing track due to failed add track request", Object.assign(Object.assign({}, getLogContextFromTrack(track)), { error: negotiateErr }));
						});
					}
					reject(err);
				}
			}));
			if (this.enabledPublishVideoCodecs.length > 0 && packetTrailerFeatures.length === 0) ti = (yield Promise.all([addTrackPromise, negotiate()]))[0];
			else {
				ti = yield addTrackPromise;
				let primaryCodecMime;
				ti.codecs.forEach((codec) => {
					if (primaryCodecMime === void 0) primaryCodecMime = codec.mimeType;
				});
				if (primaryCodecMime && track.kind === Track.Kind.Video) {
					const updatedCodec = mimeTypeToVideoCodecString(primaryCodecMime);
					if (updatedCodec !== videoCodec) {
						this.log.debug("falling back to server selected codec", Object.assign(Object.assign({}, getLogContextFromTrack(track)), { codec: updatedCodec }));
						opts.videoCodec = updatedCodec;
						encodings = computeVideoEncodings(track.source === Track.Source.ScreenShare, req.width, req.height, opts);
					}
				}
				yield negotiate();
			}
			const publication = new LocalTrackPublication(track.kind, ti, track, {
				loggerName: this.roomOptions.loggerName,
				loggerContextCb: () => this.logContext
			});
			publication.on(TrackEvent.CpuConstrained, (constrainedTrack) => this.onTrackCpuConstrained(constrainedTrack, publication));
			publication.options = opts;
			track.sid = ti.sid;
			if (isLocalVideoTrack(track)) {
				track.publishOptions = opts;
				if (req.width && req.height) track.lastEncodedDimensions = {
					width: req.width,
					height: req.height
				};
			}
			this.log.debug("publishing ".concat(track.kind, " with encodings"), {
				encodings,
				trackInfo: ti
			});
			if (isLocalVideoTrack(track)) track.startMonitor(this.engine.client);
			else if (isLocalAudioTrack(track)) track.startMonitor();
			this.addTrackPublication(publication);
			this.emit(ParticipantEvent.LocalTrackPublished, publication);
			if (isLocalAudioTrack(track) && ti.audioFeatures.includes(AudioTrackFeature.TF_PRECONNECT_BUFFER)) {
				const stream = track.getPreConnectBuffer();
				const mimeType = track.getPreConnectBufferMimeType();
				this.on(ParticipantEvent.LocalTrackSubscribed, (pub) => {
					if (pub.trackSid === ti.sid) {
						if (!track.hasPreConnectBuffer) {
							this.log.warn("subscribe event came to late, buffer already closed");
							return;
						}
						this.log.debug("finished recording preconnect buffer", getLogContextFromTrack(track));
						track.stopPreConnectBuffer();
					}
				});
				if (stream) new Promise((resolve, reject) => __awaiter(this, void 0, void 0, function* () {
					var _a, e_2, _b, _c;
					var _d, _e;
					try {
						this.log.debug("waiting for agent", getLogContextFromTrack(track));
						const agentActiveTimeout = setTimeout(() => {
							reject(/* @__PURE__ */ new Error("agent not active within 10 seconds"));
						}, 1e4);
						const agent = yield this.waitUntilActiveAgentPresent();
						clearTimeout(agentActiveTimeout);
						this.log.debug("sending preconnect buffer", getLogContextFromTrack(track));
						const writer = yield this.streamBytes({
							name: "preconnect-buffer",
							mimeType,
							topic: "lk.agent.pre-connect-audio-buffer",
							destinationIdentities: [agent.identity],
							attributes: {
								trackId: publication.trackSid,
								sampleRate: String((_d = settings.sampleRate) !== null && _d !== void 0 ? _d : "48000"),
								channels: String((_e = settings.channelCount) !== null && _e !== void 0 ? _e : "1")
							}
						});
						try {
							for (var _f = true, stream_1 = __asyncValues(stream), stream_1_1; stream_1_1 = yield stream_1.next(), _a = stream_1_1.done, !_a; _f = true) {
								_c = stream_1_1.value;
								_f = false;
								const chunk = _c;
								yield writer.write(chunk);
							}
						} catch (e_2_1) {
							e_2 = { error: e_2_1 };
						} finally {
							try {
								if (!_f && !_a && (_b = stream_1.return)) yield _b.call(stream_1);
							} finally {
								if (e_2) throw e_2.error;
							}
						}
						yield writer.close();
						resolve();
					} catch (e) {
						reject(e);
					}
				})).then(() => {
					this.log.debug("preconnect buffer sent successfully", getLogContextFromTrack(track));
				}).catch((e) => {
					this.log.error("error sending preconnect buffer", Object.assign(Object.assign({}, getLogContextFromTrack(track)), { error: e }));
				});
			}
			return publication;
		});
	}
	canPublishFrameMetadata() {
		var _a;
		return !!(this.roomOptions.e2ee || this.roomOptions.encryption || isFrameMetadataSupported((_a = this.roomOptions.frameMetadata) !== null && _a !== void 0 ? _a : this.roomOptions.packetTrailer));
	}
	normalizeRequestedFrameMetadataOptions(track, opts) {
		var _a;
		const fmOpts = (_a = opts.frameMetadata) !== null && _a !== void 0 ? _a : opts.packetTrailer;
		if (track.kind !== Track.Kind.Video || !hasFrameMetadataPublishOptions(fmOpts)) {
			opts.frameMetadata = void 0;
			opts.packetTrailer = void 0;
			return [];
		}
		if (!this.canPublishFrameMetadata()) {
			this.log.warn("frame metadata transform not supported; not advertising features", Object.assign(Object.assign({}, this.logContext), getLogContextFromTrack(track)));
			opts.frameMetadata = void 0;
			opts.packetTrailer = void 0;
			return [];
		}
		const features = getFrameMetadataFeatures(fmOpts);
		const normalized = getFrameMetadataPublishOptions(features);
		opts.frameMetadata = normalized;
		opts.packetTrailer = normalized;
		return features;
	}
	get isLocal() {
		return true;
	}
	/** @internal
	* publish additional codec to existing track
	*/
	publishAdditionalCodecForTrack(track, videoCodec, options) {
		return __awaiter(this, void 0, void 0, function* () {
			var _a;
			if (this.encryptionType !== Encryption_Type.NONE) return;
			let existingPublication;
			this.trackPublications.forEach((publication) => {
				if (!publication.track) return;
				if (publication.track === track) existingPublication = publication;
			});
			if (!existingPublication) throw new TrackInvalidError("track is not published");
			if (!isLocalVideoTrack(track)) throw new TrackInvalidError("track is not a video track");
			const opts = Object.assign(Object.assign({}, (_a = this.roomOptions) === null || _a === void 0 ? void 0 : _a.publishDefaults), options);
			const encodings = computeTrackBackupEncodings(track, videoCodec, opts);
			if (!encodings) {
				this.log.info("backup codec has been disabled, ignoring request to add additional codec for track", getLogContextFromTrack(track));
				return;
			}
			const simulcastTrack = track.addSimulcastTrack(videoCodec, encodings);
			if (!simulcastTrack) return;
			const packetTrailerFeatures = this.normalizeRequestedFrameMetadataOptions(track, opts);
			const req = new AddTrackRequest({
				cid: simulcastTrack.mediaStreamTrack.id,
				type: Track.kindToProto(track.kind),
				muted: track.isMuted,
				source: Track.sourceToProto(track.source),
				sid: track.sid,
				packetTrailerFeatures,
				simulcastCodecs: [{
					codec: opts.videoCodec,
					cid: simulcastTrack.mediaStreamTrack.id
				}]
			});
			req.layers = videoLayersFromEncodings(req.width, req.height, encodings);
			if (!this.engine || this.engine.isClosed) throw new UnexpectedConnectionState("cannot publish track when not connected");
			const negotiate = () => __awaiter(this, void 0, void 0, function* () {
				yield this.engine.createSimulcastSender(track, simulcastTrack, opts, encodings);
				yield this.engine.negotiate();
			});
			const ti = (yield Promise.all([this.engine.addTrack(req), negotiate()]))[0];
			this.log.debug("published ".concat(videoCodec, " for track ").concat(track.sid), {
				encodings,
				trackInfo: ti
			});
		});
	}
	unpublishTrack(track, stopOnUnpublish) {
		return __awaiter(this, void 0, void 0, function* () {
			var _a, _b;
			if (isLocalTrack(track)) {
				const publishPromise = this.pendingPublishPromises.get(track);
				if (publishPromise) {
					this.log.debug("awaiting publish promise before attempting to unpublish", getLogContextFromTrack(track));
					yield publishPromise;
				}
			}
			const publication = this.getPublicationForTrack(track);
			const pubLogContext = publication ? getLogContextFromTrack(publication) : void 0;
			this.log.info("unpublishing track", pubLogContext);
			if (!publication || !publication.track) {
				this.log.warn("track was not unpublished because no publication was found", pubLogContext);
				return;
			}
			track = publication.track;
			track.off(TrackEvent.Muted, this.onTrackMuted);
			track.off(TrackEvent.Unmuted, this.onTrackUnmuted);
			track.off(TrackEvent.Ended, this.handleTrackEnded);
			track.off(TrackEvent.UpstreamPaused, this.onTrackUpstreamPaused);
			track.off(TrackEvent.UpstreamResumed, this.onTrackUpstreamResumed);
			track.off(TrackEvent.AudioTrackFeatureUpdate, this.onTrackFeatureUpdate);
			if (stopOnUnpublish === void 0) stopOnUnpublish = (_b = (_a = this.roomOptions) === null || _a === void 0 ? void 0 : _a.stopLocalTrackOnUnpublish) !== null && _b !== void 0 ? _b : true;
			if (stopOnUnpublish) track.stop();
			else track.stopMonitor();
			let negotiationNeeded = false;
			const trackSender = track.sender;
			track.sender = void 0;
			if (this.engine.pcManager && this.engine.pcManager.currentState < PCTransportState.FAILED && trackSender) try {
				for (const transceiver of this.engine.pcManager.publisher.getTransceivers()) if (transceiver.sender === trackSender) {
					transceiver.direction = "inactive";
					negotiationNeeded = true;
				}
				try {
					negotiationNeeded = this.engine.removeTrack(trackSender);
				} catch (e) {
					this.log.warn(e);
					negotiationNeeded = true;
				}
				if (isLocalVideoTrack(track)) {
					for (const _ref4 of track.simulcastCodecs) {
						const trackInfo = _slicedToArray(_ref4, 2)[1];
						if (trackInfo.sender) {
							try {
								negotiationNeeded = this.engine.removeTrack(trackInfo.sender);
							} catch (e) {
								this.log.warn(e);
								negotiationNeeded = true;
							}
							trackInfo.sender = void 0;
						}
					}
					track.simulcastCodecs.clear();
				}
			} catch (e) {
				this.log.warn("failed to unpublish track", Object.assign(Object.assign({}, pubLogContext), { error: e }));
			}
			this.trackPublications.delete(publication.trackSid);
			switch (publication.kind) {
				case Track.Kind.Audio:
					this.audioTrackPublications.delete(publication.trackSid);
					break;
				case Track.Kind.Video: this.videoTrackPublications.delete(publication.trackSid);
			}
			this.emit(ParticipantEvent.LocalTrackUnpublished, publication);
			publication.setTrack(void 0);
			if (negotiationNeeded) yield this.engine.negotiate();
			return publication;
		});
	}
	unpublishTracks(tracks) {
		return __awaiter(this, void 0, void 0, function* () {
			return (yield Promise.all(tracks.map((track) => this.unpublishTrack(track)))).filter((track) => !!track);
		});
	}
	republishAllTracks(options_1) {
		return __awaiter(this, arguments, void 0, function(options) {
			var _this3 = this;
			let restartTracks = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : true;
			return function* () {
				if (_this3.republishPromise) yield _this3.republishPromise;
				_this3.republishPromise = new TypedPromise((resolve, reject) => __awaiter(_this3, void 0, void 0, function* () {
					try {
						const localPubs = [];
						this.trackPublications.forEach((pub) => {
							if (pub.track) {
								if (options) pub.options = Object.assign(Object.assign({}, pub.options), options);
								localPubs.push(pub);
							}
						});
						yield Promise.all(localPubs.map((pub) => __awaiter(this, void 0, void 0, function* () {
							const track = pub.track;
							yield this.unpublishTrack(track, false);
							if (restartTracks && !track.isMuted && track.source !== Track.Source.ScreenShare && track.source !== Track.Source.ScreenShareAudio && (isLocalAudioTrack(track) || isLocalVideoTrack(track)) && !track.isUserProvided) {
								this.log.debug("restarting existing track", { track: pub.trackSid });
								yield track.restartTrack();
							}
							yield this.publishOrRepublishTrack(track, pub.options, true);
						})));
						resolve();
					} catch (error) {
						if (error instanceof Error) reject(error);
						else reject(new Error(String(error)));
					} finally {
						this.republishPromise = void 0;
					}
				}));
				yield _this3.republishPromise;
			}();
		});
	}
	/**
	* Publish a new data payload to the room. Data will be forwarded to each
	* participant in the room if the destination field in publishOptions is empty
	*
	* @param data Uint8Array of the payload. To send string data, use TextEncoder.encode
	* @param options optionally specify a `reliable`, `topic` and `destination`
	*/
	publishData(data_1) {
		return __awaiter(this, arguments, void 0, function(data) {
			var _this4 = this;
			let options = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
			return function* () {
				const kind = options.reliable ? DataChannelKind.RELIABLE : DataChannelKind.LOSSY;
				const dataPacketKind = options.reliable ? DataPacket_Kind.RELIABLE : DataPacket_Kind.LOSSY;
				const destinationIdentities = options.destinationIdentities;
				const topic = options.topic;
				const packet = new DataPacket({
					kind: dataPacketKind,
					value: {
						case: "user",
						value: new UserPacket({
							participantIdentity: _this4.identity,
							payload: data,
							destinationIdentities,
							topic
						})
					}
				});
				yield _this4.engine.sendDataPacket(packet, kind);
			}();
		});
	}
	/**
	* Publish SIP DTMF message to the room.
	*
	* @param code DTMF code
	* @param digit DTMF digit
	*/
	publishDtmf(code, digit) {
		return __awaiter(this, void 0, void 0, function* () {
			const packet = new DataPacket({
				kind: DataPacket_Kind.RELIABLE,
				value: {
					case: "sipDtmf",
					value: new SipDTMF({
						code,
						digit
					})
				}
			});
			yield this.engine.sendDataPacket(packet, DataChannelKind.RELIABLE);
		});
	}
	/** @deprecated Consider migrating to {@link sendText} */
	sendChatMessage(text, options) {
		return __awaiter(this, void 0, void 0, function* () {
			const msg = {
				id: crypto.randomUUID(),
				message: text,
				timestamp: Date.now(),
				attachedFiles: options === null || options === void 0 ? void 0 : options.attachments
			};
			const packet = new DataPacket({ value: {
				case: "chatMessage",
				value: new ChatMessage(Object.assign(Object.assign({}, msg), { timestamp: protoInt64.parse(msg.timestamp) }))
			} });
			yield this.engine.sendDataPacket(packet, DataChannelKind.RELIABLE);
			this.emit(ParticipantEvent.ChatMessage, msg);
			return msg;
		});
	}
	/** @deprecated Consider migrating to {@link sendText} */
	editChatMessage(editText, originalMessage) {
		return __awaiter(this, void 0, void 0, function* () {
			const msg = Object.assign(Object.assign({}, originalMessage), {
				message: editText,
				editTimestamp: Date.now()
			});
			const packet = new DataPacket({ value: {
				case: "chatMessage",
				value: new ChatMessage(Object.assign(Object.assign({}, msg), {
					timestamp: protoInt64.parse(msg.timestamp),
					editTimestamp: protoInt64.parse(msg.editTimestamp)
				}))
			} });
			yield this.engine.sendDataPacket(packet, DataChannelKind.RELIABLE);
			this.emit(ParticipantEvent.ChatMessage, msg);
			return msg;
		});
	}
	/**
	* Sends the given string to participants in the room via the data channel.
	* For longer messages, consider using {@link streamText} instead.
	*
	* @param text The text payload
	* @param options.topic Topic identifier used to route the stream to appropriate handlers.
	*/
	sendText(text, options) {
		return __awaiter(this, void 0, void 0, function* () {
			return this.roomOutgoingDataStreamManager.sendText(text, options);
		});
	}
	/**
	* Creates a new TextStreamWriter which can be used to stream text incrementally
	* to participants in the room via the data channel.
	*
	* @param options.topic Topic identifier used to route the stream to appropriate handlers.
	*
	* @internal
	* @experimental CAUTION, might get removed in a minor release
	*/
	streamText(options) {
		return __awaiter(this, void 0, void 0, function* () {
			return this.roomOutgoingDataStreamManager.streamText(options);
		});
	}
	/** Send a File to all participants in the room via the data channel.
	* @param file The File object payload
	* @param options.topic Topic identifier used to route the stream to appropriate handlers.
	* @param options.onProgress A callback function used to monitor the upload progress percentage.
	*/
	sendFile(file, options) {
		return __awaiter(this, void 0, void 0, function* () {
			return this.roomOutgoingDataStreamManager.sendFile(file, options);
		});
	}
	/**
	* Sends the given bytes to participants in the room via the data channel.
	* For files, consider using {@link sendFile}; for longer/incremental payloads, {@link streamBytes}.
	*
	* @param bytes The byte payload
	* @param options.topic Topic identifier used to route the stream to appropriate handlers.
	*/
	sendBytes(bytes, options) {
		return __awaiter(this, void 0, void 0, function* () {
			return this.roomOutgoingDataStreamManager.sendBytes(bytes, options);
		});
	}
	/**
	* Stream bytes incrementally to participants in the room via the data channel.
	* For sending files, consider using {@link sendFile} instead.
	*
	* @param options.topic Topic identifier used to route the stream to appropriate handlers.
	*/
	streamBytes(options) {
		return __awaiter(this, void 0, void 0, function* () {
			return this.roomOutgoingDataStreamManager.streamBytes(options);
		});
	}
	/**
	* Initiate an RPC call to a remote participant
	* @param params - Parameters for initiating the RPC call, see {@link PerformRpcParams}
	* @returns A promise that resolves with the response payload or rejects with an error.
	* @throws Error on failure. Details in `message`.
	*/
	performRpc(params) {
		return this.rpcClientManager.performRpc(params).then((_ref6) => {
			let _ref7 = _slicedToArray(_ref6, 2);
			_ref7[0];
			return _ref7[1];
		});
	}
	/**
	* @deprecated use `room.registerRpcMethod` instead
	*/
	registerRpcMethod(method, handler) {
		this.rpcServerManager.registerRpcMethod(method, handler);
	}
	/**
	* @deprecated use `room.unregisterRpcMethod` instead
	*/
	unregisterRpcMethod(method) {
		this.rpcServerManager.unregisterRpcMethod(method);
	}
	/**
	* Control who can subscribe to LocalParticipant's published tracks.
	*
	* By default, all participants can subscribe. This allows fine-grained control over
	* who is able to subscribe at a participant and track level.
	*
	* Note: if access is given at a track-level (i.e. both [allParticipantsAllowed] and
	* [ParticipantTrackPermission.allTracksAllowed] are false), any newer published tracks
	* will not grant permissions to any participants and will require a subsequent
	* permissions update to allow subscription.
	*
	* @param allParticipantsAllowed Allows all participants to subscribe all tracks.
	*  Takes precedence over [[participantTrackPermissions]] if set to true.
	*  By default this is set to true.
	* @param participantTrackPermissions Full list of individual permissions per
	*  participant/track. Any omitted participants will not receive any permissions.
	*/
	setTrackSubscriptionPermissions(allParticipantsAllowed) {
		let participantTrackPermissions = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : [];
		this.participantTrackPermissions = participantTrackPermissions;
		this.allParticipantsAllowedToSubscribe = allParticipantsAllowed;
		if (!this.engine.client.isDisconnected) this.updateTrackSubscriptionPermissions();
	}
	/** @internal */
	setEnabledPublishCodecs(codecs) {
		this.enabledPublishVideoCodecs = codecs.filter((c) => c.mime.split("/")[0].toLowerCase() === "video");
	}
	/** @internal */
	updateInfo(info) {
		if (!super.updateInfo(info)) return false;
		info.tracks.forEach((ti) => {
			var _a, _b;
			const pub = this.trackPublications.get(ti.sid);
			if (pub) {
				const mutedOnServer = pub.isMuted || ((_b = (_a = pub.track) === null || _a === void 0 ? void 0 : _a.isUpstreamPaused) !== null && _b !== void 0 ? _b : false);
				if (mutedOnServer !== ti.muted) {
					this.log.debug("updating server mute state after reconcile", Object.assign(Object.assign({}, getLogContextFromTrack(pub)), { mutedOnServer }));
					this.engine.client.sendMuteTrack(ti.sid, mutedOnServer);
				}
			}
		});
		return true;
	}
	/** @internal */
	setActiveAgent(agent) {
		var _a, _b, _c, _d;
		this.firstActiveAgent = agent;
		if (agent && !this.firstActiveAgent) this.firstActiveAgent = agent;
		if (agent) (_b = (_a = this.activeAgentFuture) === null || _a === void 0 ? void 0 : _a.resolve) === null || _b === void 0 || _b.call(_a, agent);
		else (_d = (_c = this.activeAgentFuture) === null || _c === void 0 ? void 0 : _c.reject) === null || _d === void 0 || _d.call(_c, /* @__PURE__ */ new Error("Agent disconnected"));
		this.activeAgentFuture = void 0;
	}
	waitUntilActiveAgentPresent() {
		if (this.firstActiveAgent) return Promise.resolve(this.firstActiveAgent);
		if (!this.activeAgentFuture) this.activeAgentFuture = new Future();
		return this.activeAgentFuture.promise;
	}
	getPublicationForTrack(track) {
		let publication;
		this.trackPublications.forEach((pub) => {
			const localTrack = pub.track;
			if (!localTrack) return;
			if (track instanceof MediaStreamTrack) {
				if (isLocalAudioTrack(localTrack) || isLocalVideoTrack(localTrack)) {
					if (localTrack.mediaStreamTrack === track) publication = pub;
				}
			} else if (track === localTrack) publication = pub;
		});
		return publication;
	}
	waitForPendingPublicationOfSource(source) {
		return __awaiter(this, void 0, void 0, function* () {
			const waitForPendingTimeout = 1e4;
			const startTime = Date.now();
			while (Date.now() < startTime + waitForPendingTimeout) {
				const publishPromiseEntry = Array.from(this.pendingPublishPromises.entries()).find((_ref8) => {
					return _slicedToArray(_ref8, 1)[0].source === source;
				});
				if (publishPromiseEntry) return publishPromiseEntry[1];
				yield sleep(20);
			}
		});
	}
	/** Publishes a data track.
	*
	* Returns the published data track if successful. Use {@link LocalDataTrack#tryPush}
	* to send data frames on the track.
	*/
	publishDataTrack(options) {
		return __awaiter(this, void 0, void 0, function* () {
			const track = new LocalDataTrack(options, this.roomOutgoingDataTrackManager);
			yield track.publish();
			return track;
		});
	}
};
var DeferrableMapAbortError = class extends DOMException {
	constructor(message, reason) {
		super(message, "AbortError");
		this.reason = reason;
	}
};
/**
* A Map-like container keyed by unique strings that supports the ability to wait
* for future keys to show up in the map.
*
* @example
* // An already existing key:
* const value = map.get("key");
* // Wait for a key which will be added soon:
* const value = await map.getDeferred("key");
*/
var DeferrableMap = class extends Map {
	constructor() {
		super(...arguments);
		this.pending = /* @__PURE__ */ new Map();
	}
	set(key, value) {
		var _a, _b;
		super.set(key, value);
		const futures = (_a = this.pending) === null || _a === void 0 ? void 0 : _a.get(key);
		if (futures) {
			for (const future of futures) if (!future.isResolved) (_b = future.resolve) === null || _b === void 0 || _b.call(future, value);
			this.pending.delete(key);
		}
		return this;
	}
	get [Symbol.toStringTag]() {
		return "DeferrableMap";
	}
	getDeferred(key, signal) {
		return __awaiter(this, void 0, void 0, function* () {
			const existing = this.get(key);
			if (typeof existing !== "undefined") return existing;
			if (signal === null || signal === void 0 ? void 0 : signal.aborted) throw new DeferrableMapAbortError("The operation was aborted.", signal.reason);
			const future = new Future(void 0, () => {
				const futures = this.pending.get(key);
				if (!futures) return;
				const idx = futures.indexOf(future);
				if (idx !== -1) futures.splice(idx, 1);
				if (futures.length === 0) this.pending.delete(key);
			});
			const existingFutures = this.pending.get(key);
			if (existingFutures) existingFutures.push(future);
			else this.pending.set(key, [future]);
			if (signal) {
				const onAbort = () => {
					var _a;
					if (!future.isResolved) (_a = future.reject) === null || _a === void 0 || _a.call(future, new DeferrableMapAbortError("The operation was aborted.", signal.reason));
				};
				signal.addEventListener("abort", onAbort, { once: true });
				future.promise.finally(() => {
					signal.removeEventListener("abort", onAbort);
				});
			}
			return future.promise;
		});
	}
};
var RemoteTrackPublication = class extends TrackPublication {
	constructor(kind, ti, autoSubscribe, loggerOptions) {
		super(kind, ti.sid, ti.name, loggerOptions);
		this.track = void 0;
		/** @internal */
		this.allowed = true;
		this.requestedDisabled = void 0;
		this.visible = true;
		this.handleEnded = (track) => {
			this.setTrack(void 0);
			this.emit(TrackEvent.Ended, track);
		};
		this.handleVisibilityChange = (visible) => {
			this.log.debug("adaptivestream video visibility ".concat(this.trackSid, ", visible=").concat(visible), this.logContext);
			this.visible = visible;
			this.emitTrackUpdate();
		};
		this.handleVideoDimensionsChange = (dimensions) => {
			this.log.debug("adaptivestream video dimensions ".concat(dimensions.width, "x").concat(dimensions.height), this.logContext);
			this.videoDimensionsAdaptiveStream = dimensions;
			this.emitTrackUpdate();
		};
		this.subscribed = autoSubscribe;
		this.updateInfo(ti);
	}
	/**
	* Subscribe or unsubscribe to this remote track
	* @param subscribed true to subscribe to a track, false to unsubscribe
	*/
	setSubscribed(subscribed) {
		const prevStatus = this.subscriptionStatus;
		const prevPermission = this.permissionStatus;
		this.subscribed = subscribed;
		if (subscribed) this.allowed = true;
		const sub = new UpdateSubscription({
			trackSids: [this.trackSid],
			subscribe: this.subscribed,
			participantTracks: [new ParticipantTracks({
				participantSid: "",
				trackSids: [this.trackSid]
			})]
		});
		this.emit(TrackEvent.UpdateSubscription, sub);
		this.emitSubscriptionUpdateIfChanged(prevStatus);
		this.emitPermissionUpdateIfChanged(prevPermission);
	}
	get subscriptionStatus() {
		if (this.subscribed === false) return TrackPublication.SubscriptionStatus.Unsubscribed;
		if (!super.isSubscribed) return TrackPublication.SubscriptionStatus.Desired;
		return TrackPublication.SubscriptionStatus.Subscribed;
	}
	get permissionStatus() {
		return this.allowed ? TrackPublication.PermissionStatus.Allowed : TrackPublication.PermissionStatus.NotAllowed;
	}
	/**
	* Returns true if track is subscribed, and ready for playback
	*/
	get isSubscribed() {
		if (this.subscribed === false) return false;
		return super.isSubscribed;
	}
	get isDesired() {
		return this.subscribed !== false;
	}
	get isEnabled() {
		return this.requestedDisabled !== void 0 ? !this.requestedDisabled : this.isAdaptiveStream ? this.visible : true;
	}
	get isLocal() {
		return false;
	}
	/**
	* disable server from sending down data for this track. this is useful when
	* the participant is off screen, you may disable streaming down their video
	* to reduce bandwidth requirements
	* @param enabled
	*/
	setEnabled(enabled) {
		if (!this.isManualOperationAllowed() || this.requestedDisabled === !enabled) return;
		this.requestedDisabled = !enabled;
		this.emitTrackUpdate();
	}
	/**
	* for tracks that support simulcasting, adjust subscribed quality
	*
	* This indicates the highest quality the client can accept. if network
	* bandwidth does not allow, server will automatically reduce quality to
	* optimize for uninterrupted video
	*/
	setVideoQuality(quality) {
		if (!this.isManualOperationAllowed() || this.requestedMaxQuality === quality) return;
		this.requestedMaxQuality = quality;
		this.requestedVideoDimensions = void 0;
		this.emitTrackUpdate();
	}
	/**
	* Explicitly set the video dimensions for this track.
	*
	* This will take precedence over adaptive stream dimensions.
	*
	* @param dimensions The video dimensions to set.
	*/
	setVideoDimensions(dimensions) {
		var _a, _b;
		if (!this.isManualOperationAllowed()) return;
		if (((_a = this.requestedVideoDimensions) === null || _a === void 0 ? void 0 : _a.width) === dimensions.width && ((_b = this.requestedVideoDimensions) === null || _b === void 0 ? void 0 : _b.height) === dimensions.height) return;
		if (isRemoteVideoTrack(this.track)) this.requestedVideoDimensions = dimensions;
		this.requestedMaxQuality = void 0;
		this.emitTrackUpdate();
	}
	setVideoFPS(fps) {
		if (!this.isManualOperationAllowed()) return;
		if (!isRemoteVideoTrack(this.track)) return;
		if (this.fps === fps) return;
		this.fps = fps;
		this.emitTrackUpdate();
	}
	get videoQuality() {
		var _a;
		return (_a = this.requestedMaxQuality) !== null && _a !== void 0 ? _a : VideoQuality.HIGH;
	}
	/** @internal */
	setTrack(track) {
		const prevStatus = this.subscriptionStatus;
		const prevPermission = this.permissionStatus;
		const prevTrack = this.track;
		if (prevTrack === track) return;
		if (prevTrack) {
			prevTrack.off(TrackEvent.VideoDimensionsChanged, this.handleVideoDimensionsChange);
			prevTrack.off(TrackEvent.VisibilityChanged, this.handleVisibilityChange);
			prevTrack.off(TrackEvent.Ended, this.handleEnded);
			prevTrack.detach();
			prevTrack.stopMonitor();
			this.emit(TrackEvent.Unsubscribed, prevTrack);
		}
		super.setTrack(track);
		if (track) {
			track.sid = this.trackSid;
			track.on(TrackEvent.VideoDimensionsChanged, this.handleVideoDimensionsChange);
			track.on(TrackEvent.VisibilityChanged, this.handleVisibilityChange);
			track.on(TrackEvent.Ended, this.handleEnded);
			this.emit(TrackEvent.Subscribed, track);
		}
		this.emitPermissionUpdateIfChanged(prevPermission);
		this.emitSubscriptionUpdateIfChanged(prevStatus);
	}
	/** @internal */
	setAllowed(allowed) {
		const prevStatus = this.subscriptionStatus;
		const prevPermission = this.permissionStatus;
		this.allowed = allowed;
		this.emitPermissionUpdateIfChanged(prevPermission);
		this.emitSubscriptionUpdateIfChanged(prevStatus);
	}
	/** @internal */
	setSubscriptionError(error) {
		this.emit(TrackEvent.SubscriptionFailed, error);
	}
	/** @internal */
	updateInfo(info) {
		super.updateInfo(info);
		const prevMetadataMuted = this.metadataMuted;
		this.metadataMuted = info.muted;
		if (this.track) this.track.setMuted(info.muted);
		else if (prevMetadataMuted !== info.muted) this.emit(info.muted ? TrackEvent.Muted : TrackEvent.Unmuted);
	}
	emitSubscriptionUpdateIfChanged(previousStatus) {
		const currentStatus = this.subscriptionStatus;
		if (previousStatus === currentStatus) return;
		this.emit(TrackEvent.SubscriptionStatusChanged, currentStatus, previousStatus);
	}
	emitPermissionUpdateIfChanged(previousPermissionStatus) {
		if (this.permissionStatus !== previousPermissionStatus) this.emit(TrackEvent.SubscriptionPermissionChanged, this.permissionStatus, previousPermissionStatus);
	}
	isManualOperationAllowed() {
		if (!this.isDesired) {
			this.log.warn("cannot update track settings when not subscribed", this.logContext);
			return false;
		}
		return true;
	}
	get isAdaptiveStream() {
		return isRemoteVideoTrack(this.track) && this.track.isAdaptiveStream;
	}
	emitTrackUpdate() {
		const settings = new UpdateTrackSettings({
			trackSids: [this.trackSid],
			disabled: !this.isEnabled,
			fps: this.fps
		});
		if (this.kind === Track.Kind.Video) {
			let minDimensions = this.requestedVideoDimensions;
			if (this.videoDimensionsAdaptiveStream !== void 0) {
				if (minDimensions) {
					if (areDimensionsSmaller(this.videoDimensionsAdaptiveStream, minDimensions)) {
						this.log.debug("using adaptive stream dimensions instead of requested", Object.assign(Object.assign({}, this.logContext), this.videoDimensionsAdaptiveStream));
						minDimensions = this.videoDimensionsAdaptiveStream;
					}
				} else if (this.requestedMaxQuality !== void 0 && this.trackInfo) {
					const maxQualityLayer = layerDimensionsFor(this.trackInfo, this.requestedMaxQuality);
					if (maxQualityLayer && areDimensionsSmaller(this.videoDimensionsAdaptiveStream, maxQualityLayer)) {
						this.log.debug("using adaptive stream dimensions instead of max quality layer", Object.assign(Object.assign({}, this.logContext), this.videoDimensionsAdaptiveStream));
						minDimensions = this.videoDimensionsAdaptiveStream;
					}
				} else {
					this.log.debug("using adaptive stream dimensions", Object.assign(Object.assign({}, this.logContext), this.videoDimensionsAdaptiveStream));
					minDimensions = this.videoDimensionsAdaptiveStream;
				}
			}
			if (minDimensions) {
				settings.width = Math.ceil(minDimensions.width);
				settings.height = Math.ceil(minDimensions.height);
			} else if (this.requestedMaxQuality !== void 0) {
				this.log.debug("using requested max quality", Object.assign(Object.assign({}, this.logContext), { quality: this.requestedMaxQuality }));
				settings.quality = this.requestedMaxQuality;
			} else {
				this.log.debug("using default quality", Object.assign(Object.assign({}, this.logContext), { quality: VideoQuality.HIGH }));
				settings.quality = VideoQuality.HIGH;
			}
		}
		this.emit(TrackEvent.UpdateSettings, settings);
	}
};
var RemoteParticipant = class RemoteParticipant extends Participant {
	/** @internal */
	static fromParticipantInfo(signalClient, pi, loggerOptions, manager) {
		return new RemoteParticipant(signalClient, pi.sid, pi.identity, pi.name, pi.metadata, pi.attributes, loggerOptions, pi.kind, pi.dataTracks.map((dti) => {
			return new RemoteDataTrack(DataTrackInfo.from(dti), manager, { publisherIdentity: pi.identity });
		}), pi.clientProtocol, pi.capabilities);
	}
	get logContext() {
		return Object.assign(Object.assign({}, super.logContext), {
			remoteParticipantID: this.sid,
			remoteParticipant: this.identity
		});
	}
	/** @internal */
	constructor(signalClient, sid, identity, name, metadata, attributes, loggerOptions) {
		let kind = arguments.length > 7 && arguments[7] !== void 0 ? arguments[7] : ParticipantInfo_Kind.STANDARD;
		let remoteDataTracks = arguments.length > 8 && arguments[8] !== void 0 ? arguments[8] : [];
		let clientProtocol = arguments.length > 9 && arguments[9] !== void 0 ? arguments[9] : 0;
		let capabilities = arguments.length > 10 && arguments[10] !== void 0 ? arguments[10] : [];
		super(sid, identity || "", name, metadata, attributes, loggerOptions, kind);
		this.signalClient = signalClient;
		this.trackPublications = /* @__PURE__ */ new Map();
		this.audioTrackPublications = /* @__PURE__ */ new Map();
		this.videoTrackPublications = /* @__PURE__ */ new Map();
		this.dataTracks = new DeferrableMap(remoteDataTracks.map((remoteDataTrack) => {
			return [remoteDataTrack.info.name, remoteDataTrack];
		}));
		this.volumeMap = /* @__PURE__ */ new Map();
		this.clientProtocol = clientProtocol;
		this.capabilities = capabilities;
	}
	addTrackPublication(publication) {
		super.addTrackPublication(publication);
		publication.on(TrackEvent.UpdateSettings, (settings) => {
			this.log.debug("send update settings", Object.assign(Object.assign(Object.assign({}, this.logContext), getLogContextFromTrack(publication)), { settings }));
			this.signalClient.sendUpdateTrackSettings(settings);
		});
		publication.on(TrackEvent.UpdateSubscription, (sub) => {
			sub.participantTracks.forEach((pt) => {
				pt.participantSid = this.sid;
			});
			this.signalClient.sendUpdateSubscription(sub);
		});
		publication.on(TrackEvent.SubscriptionPermissionChanged, (status) => {
			this.emit(ParticipantEvent.TrackSubscriptionPermissionChanged, publication, status);
		});
		publication.on(TrackEvent.SubscriptionStatusChanged, (status) => {
			this.emit(ParticipantEvent.TrackSubscriptionStatusChanged, publication, status);
		});
		publication.on(TrackEvent.Subscribed, (track) => {
			this.emit(ParticipantEvent.TrackSubscribed, track, publication);
		});
		publication.on(TrackEvent.Unsubscribed, (previousTrack) => {
			this.emit(ParticipantEvent.TrackUnsubscribed, previousTrack, publication);
		});
		publication.on(TrackEvent.SubscriptionFailed, (error) => {
			this.emit(ParticipantEvent.TrackSubscriptionFailed, publication.trackSid, error);
		});
	}
	getTrackPublication(source) {
		const track = super.getTrackPublication(source);
		if (track) return track;
	}
	getTrackPublicationByName(name) {
		const track = super.getTrackPublicationByName(name);
		if (track) return track;
	}
	/**
	* sets the volume on the participant's audio track
	* by default, this affects the microphone publication
	* a different source can be passed in as a second argument
	* if no track exists the volume will be applied when the microphone track is added
	*/
	setVolume(volume) {
		let source = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : Track.Source.Microphone;
		this.volumeMap.set(source, volume);
		const audioPublication = this.getTrackPublication(source);
		if (audioPublication && audioPublication.track) audioPublication.track.setVolume(volume);
	}
	/**
	* gets the volume on the participant's microphone track
	*/
	getVolume() {
		let source = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : Track.Source.Microphone;
		const audioPublication = this.getTrackPublication(source);
		if (audioPublication && audioPublication.track) return audioPublication.track.getVolume();
		return this.volumeMap.get(source);
	}
	/** @internal */
	addSubscribedMediaTrack(mediaTrack, sid, mediaStream, receiver, adaptiveStreamSettings, triesLeft) {
		let publication = this.getTrackPublicationBySid(sid);
		if (!publication) {
			if (!sid.startsWith("TR")) this.trackPublications.forEach((p) => {
				if (!publication && mediaTrack.kind === p.kind.toString()) publication = p;
			});
		}
		if (!publication) {
			if (triesLeft === 0) {
				this.log.error("could not find published track", Object.assign(Object.assign({}, this.logContext), { trackSid: sid }));
				this.emit(ParticipantEvent.TrackSubscriptionFailed, sid);
				return;
			}
			if (triesLeft === void 0) triesLeft = 20;
			setTimeout(() => {
				this.addSubscribedMediaTrack(mediaTrack, sid, mediaStream, receiver, adaptiveStreamSettings, triesLeft - 1);
			}, 150);
			return;
		}
		if (mediaTrack.readyState === "ended") {
			this.log.error("unable to subscribe because MediaStreamTrack is ended. Do not call MediaStreamTrack.stop()", Object.assign(Object.assign({}, this.logContext), getLogContextFromTrack(publication)));
			this.emit(ParticipantEvent.TrackSubscriptionFailed, sid);
			return;
		}
		const isVideo = mediaTrack.kind === "video";
		let track;
		if (isVideo) track = new RemoteVideoTrack(mediaTrack, sid, receiver, adaptiveStreamSettings);
		else track = new RemoteAudioTrack(mediaTrack, sid, receiver, this.audioContext, this.audioOutput);
		track.source = publication.source;
		track.isMuted = publication.isMuted;
		track.setMediaStream(mediaStream);
		track.start();
		publication.setTrack(track);
		if (this.volumeMap.has(publication.source) && isRemoteTrack(track) && isAudioTrack(track)) track.setVolume(this.volumeMap.get(publication.source));
		return publication;
	}
	/** @internal */
	get hasMetadata() {
		return !!this.participantInfo;
	}
	/**
	* @internal
	*/
	getTrackPublicationBySid(sid) {
		return this.trackPublications.get(sid);
	}
	/** @internal */
	updateInfo(info) {
		if (!super.updateInfo(info)) return false;
		const validTracks = /* @__PURE__ */ new Map();
		const newTracks = /* @__PURE__ */ new Map();
		info.tracks.forEach((ti) => {
			var _a, _b;
			let publication = this.getTrackPublicationBySid(ti.sid);
			if (!publication) {
				const kind = Track.kindFromProto(ti.type);
				if (!kind) return;
				publication = new RemoteTrackPublication(kind, ti, (_a = this.signalClient.connectOptions) === null || _a === void 0 ? void 0 : _a.autoSubscribe, {
					loggerContextCb: () => this.logContext,
					loggerName: (_b = this.loggerOptions) === null || _b === void 0 ? void 0 : _b.loggerName
				});
				publication.updateInfo(ti);
				newTracks.set(ti.sid, publication);
				const existingTrackOfSource = Array.from(this.trackPublications.values()).find((publishedTrack) => publishedTrack.source === (publication === null || publication === void 0 ? void 0 : publication.source));
				if (existingTrackOfSource && publication.source !== Track.Source.Unknown) this.log.debug("received a second track publication for ".concat(this.identity, " with the same source: ").concat(publication.source), Object.assign(Object.assign({}, this.logContext), {
					oldTrack: getLogContextFromTrack(existingTrackOfSource),
					newTrack: getLogContextFromTrack(publication)
				}));
				this.addTrackPublication(publication);
			} else publication.updateInfo(ti);
			validTracks.set(ti.sid, publication);
		});
		this.trackPublications.forEach((publication) => {
			if (!validTracks.has(publication.trackSid)) {
				this.log.trace("detected removed track on remote participant, unpublishing", Object.assign(Object.assign({}, this.logContext), getLogContextFromTrack(publication)));
				this.unpublishTrack(publication.trackSid, true);
			}
		});
		newTracks.forEach((publication) => {
			this.emit(ParticipantEvent.TrackPublished, publication);
		});
		return true;
	}
	/** @internal */
	unpublishTrack(sid, sendUnpublish) {
		const publication = this.trackPublications.get(sid);
		if (!publication) return;
		const track = publication.track;
		if (track) {
			track.stop();
			publication.setTrack(void 0);
		}
		this.trackPublications.delete(sid);
		switch (publication.kind) {
			case Track.Kind.Audio:
				this.audioTrackPublications.delete(sid);
				break;
			case Track.Kind.Video: this.videoTrackPublications.delete(sid);
		}
		if (sendUnpublish) this.emit(ParticipantEvent.TrackUnpublished, publication);
	}
	/**
	* @internal
	*/
	setAudioOutput(output) {
		return __awaiter(this, void 0, void 0, function* () {
			this.audioOutput = output;
			const promises = [];
			this.audioTrackPublications.forEach((pub) => {
				var _a;
				if (isAudioTrack(pub.track) && isRemoteTrack(pub.track)) promises.push(pub.track.setSinkId((_a = output.deviceId) !== null && _a !== void 0 ? _a : "default"));
			});
			yield Promise.all(promises);
		});
	}
	/** @internal */
	addRemoteDataTrack(remoteDataTrack) {
		this.dataTracks.set(remoteDataTrack.info.name, remoteDataTrack);
	}
	/** @internal */
	removeRemoteDataTrack(remoteDataTrackSid) {
		for (const _ref of this.dataTracks.entries()) {
			var _ref2 = _slicedToArray(_ref, 2);
			const name = _ref2[0];
			if (remoteDataTrackSid === _ref2[1].info.sid) this.dataTracks.delete(name);
		}
	}
	/** @internal */
	emit(event) {
		for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) args[_key - 1] = arguments[_key];
		this.log.trace("participant event", Object.assign(Object.assign({}, this.logContext), {
			event,
			args
		}));
		return super.emit(event, ...args);
	}
};
var ConnectionState;
(function(ConnectionState) {
	ConnectionState["Disconnected"] = "disconnected";
	ConnectionState["Connecting"] = "connecting";
	ConnectionState["Connected"] = "connected";
	ConnectionState["Reconnecting"] = "reconnecting";
	ConnectionState["SignalReconnecting"] = "signalReconnecting";
})(ConnectionState || (ConnectionState = {}));
var CONNECTION_RECONCILE_FREQUENCY_MS = 4e3;
/**
* In LiveKit, a room is the logical grouping for a list of participants.
* Participants in a room can publish tracks, and subscribe to others' tracks.
*
* a Room fires [[RoomEvent | RoomEvents]].
*
* @noInheritDoc
*/
var Room = class Room extends eventsExports.EventEmitter {
	get hasE2EESetup() {
		return this.e2eeManager !== void 0;
	}
	/**
	* Creates a new Room, the primary construct for a LiveKit session.
	* @param options
	*/
	constructor(options) {
		var _this;
		var _a, _b, _c, _d, _e, _f;
		super();
		_this = this;
		this.state = ConnectionState.Disconnected;
		/**
		* list of participants that are actively speaking. when this changes
		* a [[RoomEvent.ActiveSpeakersChanged]] event is fired
		*/
		this.activeSpeakers = [];
		/** reflects the sender encryption status of the local participant */
		this.isE2EEEnabled = false;
		this.audioEnabled = true;
		this.e2eeStateMutex = new _();
		this.isVideoPlaybackBlocked = false;
		this.log = livekitLogger;
		this.bufferedEvents = [];
		this.isResuming = false;
		this.pendingTrackAddedCallbacks = /* @__PURE__ */ new Map();
		this.connect = (url, token, opts) => __awaiter(this, void 0, void 0, function* () {
			var _a;
			if (!isBrowserSupported()) {
				if (isReactNative()) throw Error("WebRTC isn't detected, have you called registerGlobals?");
				else throw Error("LiveKit doesn't seem to be supported on this browser. Try to update your browser and make sure no browser extensions are disabling webRTC.");
			}
			const unlockDisconnect = yield this.disconnectLock.lock();
			if (this.state === ConnectionState.Connected) {
				this.log.info("already connected to room ".concat(this.name));
				unlockDisconnect();
				return Promise.resolve();
			}
			if (this.connectFuture) {
				unlockDisconnect();
				return this.connectFuture.promise;
			}
			this.setAndEmitConnectionState(ConnectionState.Connecting);
			if (((_a = this.regionUrlProvider) === null || _a === void 0 ? void 0 : _a.getServerUrl().toString()) !== ensureTrailingSlash(url)) {
				this.regionUrl = void 0;
				this.regionUrlProvider = void 0;
			}
			if (isCloud(new URL(url))) {
				if (this.regionUrlProvider === void 0) this.regionUrlProvider = new RegionUrlProvider(url, token);
				else this.regionUrlProvider.updateToken(token);
				this.regionUrlProvider.fetchRegionSettings().then((settings) => {
					var _a;
					(_a = this.regionUrlProvider) === null || _a === void 0 || _a.setServerReportedRegions(settings);
				}).catch((e) => {
					this.log.warn("could not fetch region settings", { error: e });
				});
			}
			const connectFn = (resolve, reject, regionUrl) => __awaiter(this, void 0, void 0, function* () {
				var _a, _b;
				if (this.abortController) this.abortController.abort();
				const abortController = new AbortController();
				this.abortController = abortController;
				unlockDisconnect === null || unlockDisconnect === void 0 || unlockDisconnect();
				try {
					yield BackOffStrategy.getInstance().getBackOffPromise(url);
					if (abortController.signal.aborted) throw ConnectionError.cancelled("Connection attempt aborted");
					yield this.attemptConnection(regionUrl !== null && regionUrl !== void 0 ? regionUrl : url, token, opts, abortController);
					this.abortController = void 0;
					resolve();
				} catch (error) {
					if (this.regionUrlProvider && error instanceof ConnectionError && error.reason !== ConnectionErrorReason.Cancelled && error.reason !== ConnectionErrorReason.NotAllowed) {
						let nextUrl = null;
						try {
							this.log.debug("Fetching next region");
							nextUrl = yield this.regionUrlProvider.getNextBestRegionUrl((_a = this.abortController) === null || _a === void 0 ? void 0 : _a.signal);
						} catch (regionFetchError) {
							if (regionFetchError instanceof ConnectionError && (regionFetchError.status === 401 || regionFetchError.reason === ConnectionErrorReason.Cancelled)) {
								this.handleDisconnect(this.options.stopLocalTrackOnUnpublish);
								reject(regionFetchError);
								return;
							}
						}
						if ([
							ConnectionErrorReason.InternalError,
							ConnectionErrorReason.ServerUnreachable,
							ConnectionErrorReason.Timeout
						].includes(error.reason)) {
							this.log.debug("Adding failed connection attempt to back off");
							BackOffStrategy.getInstance().addFailedConnectionAttempt(url);
						}
						if (nextUrl && !((_b = this.abortController) === null || _b === void 0 ? void 0 : _b.signal.aborted)) {
							this.log.info("Initial connection failed with ConnectionError: ".concat(error.message, ". Retrying with another region: ").concat(nextUrl));
							this.recreateEngine(true);
							yield connectFn(resolve, reject, nextUrl);
						} else {
							this.handleDisconnect(this.options.stopLocalTrackOnUnpublish, getDisconnectReasonFromConnectionError(error));
							reject(error);
						}
					} else {
						let disconnectReason = DisconnectReason.UNKNOWN_REASON;
						if (error instanceof ConnectionError) disconnectReason = getDisconnectReasonFromConnectionError(error);
						this.handleDisconnect(this.options.stopLocalTrackOnUnpublish, disconnectReason);
						reject(error);
					}
				}
			});
			const regionUrl = this.regionUrl;
			this.regionUrl = void 0;
			this.connectFuture = new Future((resolve, reject) => {
				connectFn(resolve, reject, regionUrl);
			}, () => {
				this.clearConnectionFutures();
			});
			return this.connectFuture.promise;
		});
		this.connectSignal = (url, token, engine, connectOptions, roomOptions, abortController) => __awaiter(this, void 0, void 0, function* () {
			const _yield$engine$join = yield engine.join(url, token, {
				autoSubscribe: connectOptions.autoSubscribe,
				adaptiveStream: typeof roomOptions.adaptiveStream === "object" ? true : roomOptions.adaptiveStream,
				clientInfoCapabilities: this.getClientInfoCapabilities(roomOptions),
				maxRetries: connectOptions.maxRetries,
				e2eeEnabled: !!this.e2eeManager,
				websocketTimeout: connectOptions.websocketTimeout
			}, abortController.signal, !roomOptions.singlePeerConnection), joinResponse = _yield$engine$join.joinResponse, serverInfo = _yield$engine$join.serverInfo;
			this.serverInfo = serverInfo;
			if (!serverInfo.version) throw new UnsupportedServer("unknown server version");
			if (serverInfo.version === "0.15.1" && this.options.dynacast) {
				this.log.debug("disabling dynacast due to server version");
				roomOptions.dynacast = false;
			}
			return joinResponse;
		});
		this.applyJoinResponse = (joinResponse) => {
			const pi = joinResponse.participant;
			this.localParticipant.sid = pi.sid;
			this.localParticipant.identity = pi.identity;
			this.localParticipant.setEnabledPublishCodecs(joinResponse.enabledPublishCodecs);
			if (this.e2eeManager) try {
				this.e2eeManager.setSifTrailer(joinResponse.sifTrailer);
			} catch (e) {
				this.log.error(e instanceof Error ? e.message : "Could not set SifTrailer", { error: e });
			}
			this.handleParticipantUpdates([pi, ...joinResponse.otherParticipants]);
			if (joinResponse.room) this.handleRoomUpdate(joinResponse.room);
		};
		this.attemptConnection = (url, token, opts, abortController) => __awaiter(this, void 0, void 0, function* () {
			var _a, _b;
			if (this.state === ConnectionState.Reconnecting || this.isResuming || ((_a = this.engine) === null || _a === void 0 ? void 0 : _a.pendingReconnect)) {
				this.log.info("Reconnection attempt replaced by new connection attempt");
				this.recreateEngine(true);
			} else this.maybeCreateEngine();
			if ((_b = this.regionUrlProvider) === null || _b === void 0 ? void 0 : _b.isCloud()) this.engine.setRegionStrategy(this.createRegionStrategy());
			this.acquireAudioContext();
			this.connOptions = Object.assign(Object.assign({}, roomConnectOptionDefaults), opts);
			if (this.connOptions.rtcConfig) this.engine.rtcConfig = this.connOptions.rtcConfig;
			if (this.connOptions.peerConnectionTimeout) this.engine.peerConnectionTimeout = this.connOptions.peerConnectionTimeout;
			try {
				const joinResponse = yield this.connectSignal(url, token, this.engine, this.connOptions, this.options, abortController);
				this.applyJoinResponse(joinResponse);
				this.setupLocalParticipantEvents();
				this.emit(RoomEvent.SignalConnected);
			} catch (err) {
				yield this.engine.close();
				this.recreateEngine();
				const resultingError = abortController.signal.aborted ? ConnectionError.cancelled("Signal connection aborted") : ConnectionError.serverUnreachable("could not establish signal connection");
				if (err instanceof Error) resultingError.message = "".concat(resultingError.message, ": ").concat(err.message);
				if (err instanceof ConnectionError) {
					resultingError.reason = err.reason;
					resultingError.status = err.status;
				}
				this.log.debug("error trying to establish signal connection", { error: err });
				throw resultingError;
			}
			if (abortController.signal.aborted) {
				yield this.engine.close();
				this.recreateEngine();
				throw ConnectionError.cancelled("Connection attempt aborted");
			}
			try {
				yield this.engine.waitForPCInitialConnection(this.connOptions.peerConnectionTimeout, abortController);
			} catch (e) {
				yield this.engine.close();
				this.recreateEngine();
				throw e;
			}
			if (isWeb() && this.options.disconnectOnPageLeave) {
				window.addEventListener("pagehide", this.onPageLeave);
				window.addEventListener("beforeunload", this.onPageLeave);
			}
			if (isWeb()) window.addEventListener("freeze", this.onPageLeave);
			this.setAndEmitConnectionState(ConnectionState.Connected);
			this.emit(RoomEvent.Connected);
			BackOffStrategy.getInstance().resetFailedConnectionAttempts(url);
			this.registerConnectionReconcile();
			if (this.regionUrlProvider) this.regionUrlProvider.notifyConnected();
		});
		/**
		* disconnects the room, emits [[RoomEvent.Disconnected]]
		*/
		this.disconnect = function() {
			for (var _len = arguments.length, args_1 = new Array(_len), _key = 0; _key < _len; _key++) args_1[_key] = arguments[_key];
			return __awaiter(_this, [...args_1], void 0, function() {
				var _this2 = this;
				let stopTracks = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : true;
				return function* () {
					var _a, _b, _c;
					const unlock = yield _this2.disconnectLock.lock();
					try {
						if (_this2.state === ConnectionState.Disconnected) {
							_this2.log.debug("already disconnected");
							return;
						}
						_this2.log.info("disconnect from room");
						if (_this2.state === ConnectionState.Connecting || _this2.state === ConnectionState.Reconnecting || _this2.isResuming) {
							const msg = "Abort connection attempt due to user initiated disconnect";
							_this2.log.warn(msg);
							(_a = _this2.abortController) === null || _a === void 0 || _a.abort(msg);
							(_c = (_b = _this2.connectFuture) === null || _b === void 0 ? void 0 : _b.reject) === null || _c === void 0 || _c.call(_b, ConnectionError.cancelled("Client initiated disconnect"));
							_this2.connectFuture = void 0;
						}
						if (_this2.engine) {
							if (!_this2.engine.client.isDisconnected) yield _this2.engine.client.sendLeave();
							yield _this2.engine.close();
						}
						_this2.handleDisconnect(stopTracks, DisconnectReason.CLIENT_INITIATED);
						_this2.engine = void 0;
					} finally {
						unlock();
					}
				}();
			});
		};
		this.onPageLeave = () => __awaiter(this, void 0, void 0, function* () {
			this.log.info("Page leave detected, disconnecting");
			yield this.disconnect();
		});
		/**
		* Browsers have different policies regarding audio playback. Most requiring
		* some form of user interaction (click/tap/etc).
		* In those cases, audio will be silent until a click/tap triggering one of the following
		* - `startAudio`
		* - `getUserMedia`
		*/
		this.startAudio = () => __awaiter(this, void 0, void 0, function* () {
			const elements = [];
			const browser = getBrowser();
			if (browser && browser.os === "iOS") {
				/**
				* iOS blocks audio element playback if
				* - user is not publishing audio themselves and
				* - no other audio source is playing
				*
				* as a workaround, we create an audio element with an empty track, so that
				* silent audio is always playing
				*/
				const audioId = "livekit-dummy-audio-el";
				let dummyAudioEl = document.getElementById(audioId);
				if (!dummyAudioEl) {
					dummyAudioEl = document.createElement("audio");
					dummyAudioEl.id = audioId;
					dummyAudioEl.autoplay = true;
					dummyAudioEl.hidden = true;
					const track = getEmptyAudioStreamTrack();
					track.enabled = true;
					const stream = new MediaStream([track]);
					dummyAudioEl.srcObject = stream;
					document.addEventListener("visibilitychange", () => {
						if (!dummyAudioEl) return;
						dummyAudioEl.srcObject = document.hidden ? null : stream;
						if (!document.hidden) {
							this.log.debug("page visible again, triggering startAudio to resume playback and update playback status");
							this.startAudio();
						}
					});
					document.body.append(dummyAudioEl);
					this.once(RoomEvent.Disconnected, () => {
						dummyAudioEl === null || dummyAudioEl === void 0 || dummyAudioEl.remove();
						dummyAudioEl = null;
					});
				}
				elements.push(dummyAudioEl);
			}
			this.remoteParticipants.forEach((p) => {
				p.audioTrackPublications.forEach((t) => {
					if (t.track) t.track.attachedElements.forEach((e) => {
						elements.push(e);
					});
				});
			});
			try {
				yield Promise.all([this.acquireAudioContext(), ...elements.map((e) => {
					if (!this.options.webAudioMix) e.muted = false;
					return e.play();
				})]);
				this.handleAudioPlaybackStarted();
			} catch (err) {
				this.handleAudioPlaybackFailed(err);
				throw err;
			}
		});
		this.startVideo = () => __awaiter(this, void 0, void 0, function* () {
			const elements = [];
			for (const p of this.remoteParticipants.values()) p.videoTrackPublications.forEach((tr) => {
				var _a;
				(_a = tr.track) === null || _a === void 0 || _a.attachedElements.forEach((el) => {
					if (!elements.includes(el)) elements.push(el);
				});
			});
			yield Promise.all(elements.map((el) => el.play())).then(() => {
				this.handleVideoPlaybackStarted();
			}).catch((e) => {
				if (e.name === "NotAllowedError") this.handleVideoPlaybackFailed();
				else this.log.warn("Resuming video playback failed, make sure you call `startVideo` directly in a user gesture handler");
			});
		});
		this.handleRestarting = () => {
			this.clearConnectionReconcile();
			this.isResuming = false;
			for (const p of this.remoteParticipants.values()) this.handleParticipantDisconnected(p.identity, p);
			if (this.setAndEmitConnectionState(ConnectionState.Reconnecting)) this.emit(RoomEvent.Reconnecting);
		};
		this.handleRestarted = () => {
			this.outgoingDataTrackManager.sfuWillRepublishTracks();
			this.incomingDataTrackManager.resendSubscriptionUpdates();
		};
		this.handleSignalRestarted = (joinResponse) => __awaiter(this, void 0, void 0, function* () {
			this.log.debug("signal reconnected to server, region ".concat(joinResponse.serverRegion), { region: joinResponse.serverRegion });
			this.bufferedEvents = [];
			this.applyJoinResponse(joinResponse);
			try {
				yield this.localParticipant.republishAllTracks(void 0, true);
			} catch (error) {
				this.log.error("error trying to re-publish tracks after reconnection", { error });
			}
			try {
				yield this.engine.waitForRestarted();
				this.log.debug("fully reconnected to server", { region: joinResponse.serverRegion });
			} catch (_a) {
				return;
			}
			this.setAndEmitConnectionState(ConnectionState.Connected);
			this.emit(RoomEvent.Reconnected);
			this.registerConnectionReconcile();
			this.emitBufferedEvents();
		});
		this.handleParticipantUpdates = (participantInfos) => {
			var _a;
			for (const info of participantInfos) {
				if (info.identity === this.localParticipant.identity) {
					this.localParticipant.updateInfo(info);
					continue;
				}
				if (info.identity === "") info.identity = (_a = this.sidToIdentity.get(info.sid)) !== null && _a !== void 0 ? _a : "";
				let remoteParticipant = this.remoteParticipants.get(info.identity);
				if (info.state === ParticipantInfo_State.DISCONNECTED) this.handleParticipantDisconnected(info.identity, remoteParticipant);
				else this.getOrCreateParticipant(info.identity, info);
			}
			const mapped = new Map(participantInfos.filter((p) => p.identity !== this.localParticipant.identity).map((info) => {
				return [info.identity, info.dataTracks.map((dataTrack) => DataTrackInfo.from(dataTrack))];
			}));
			this.incomingDataTrackManager.receiveSfuPublicationUpdates(mapped);
		};
		this.handleActiveSpeakersUpdate = (speakers) => {
			const activeSpeakers = [];
			const seenSids = {};
			speakers.forEach((speaker) => {
				seenSids[speaker.sid] = true;
				if (speaker.sid === this.localParticipant.sid) {
					this.localParticipant.audioLevel = speaker.level;
					this.localParticipant.setIsSpeaking(true);
					activeSpeakers.push(this.localParticipant);
				} else {
					const p = this.getRemoteParticipantBySid(speaker.sid);
					if (p) {
						p.audioLevel = speaker.level;
						p.setIsSpeaking(true);
						activeSpeakers.push(p);
					}
				}
			});
			if (!seenSids[this.localParticipant.sid]) {
				this.localParticipant.audioLevel = 0;
				this.localParticipant.setIsSpeaking(false);
			}
			this.remoteParticipants.forEach((p) => {
				if (!seenSids[p.sid]) {
					p.audioLevel = 0;
					p.setIsSpeaking(false);
				}
			});
			this.activeSpeakers = activeSpeakers;
			this.emitWhenConnected(RoomEvent.ActiveSpeakersChanged, activeSpeakers);
		};
		this.handleSpeakersChanged = (speakerUpdates) => {
			const lastSpeakers = /* @__PURE__ */ new Map();
			this.activeSpeakers.forEach((p) => {
				const remoteParticipant = this.remoteParticipants.get(p.identity);
				if (remoteParticipant && remoteParticipant.sid !== p.sid) return;
				lastSpeakers.set(p.sid, p);
			});
			speakerUpdates.forEach((speaker) => {
				let p = this.getRemoteParticipantBySid(speaker.sid);
				if (speaker.sid === this.localParticipant.sid) p = this.localParticipant;
				if (!p) return;
				p.audioLevel = speaker.level;
				p.setIsSpeaking(speaker.active);
				if (speaker.active) lastSpeakers.set(speaker.sid, p);
				else lastSpeakers.delete(speaker.sid);
			});
			const activeSpeakers = Array.from(lastSpeakers.values());
			activeSpeakers.sort((a, b) => b.audioLevel - a.audioLevel);
			this.activeSpeakers = activeSpeakers;
			this.emitWhenConnected(RoomEvent.ActiveSpeakersChanged, activeSpeakers);
		};
		this.handleStreamStateUpdate = (streamStateUpdate) => {
			streamStateUpdate.streamStates.forEach((streamState) => {
				const participant = this.getRemoteParticipantBySid(streamState.participantSid);
				if (!participant) return;
				const pub = participant.getTrackPublicationBySid(streamState.trackSid);
				if (!pub || !pub.track) return;
				const newStreamState = Track.streamStateFromProto(streamState.state);
				const prevStreamState = pub.track.streamState;
				pub.track.setStreamState(newStreamState);
				if (newStreamState !== prevStreamState) {
					participant.emit(ParticipantEvent.TrackStreamStateChanged, pub, pub.track.streamState);
					this.emitWhenConnected(RoomEvent.TrackStreamStateChanged, pub, pub.track.streamState, participant);
				}
			});
		};
		this.handleSubscriptionPermissionUpdate = (update) => {
			const participant = this.getRemoteParticipantBySid(update.participantSid);
			if (!participant) return;
			const pub = participant.getTrackPublicationBySid(update.trackSid);
			if (!pub) return;
			pub.setAllowed(update.allowed);
		};
		this.handleSubscriptionError = (update) => {
			this.cancelPendingTrackAdded(update.trackSid);
			const participant = Array.from(this.remoteParticipants.values()).find((p) => p.trackPublications.has(update.trackSid));
			if (!participant) return;
			const pub = participant.getTrackPublicationBySid(update.trackSid);
			if (!pub) return;
			pub.setSubscriptionError(update.err);
		};
		this.handleDataPacket = (packet, encryptionType) => {
			const participant = this.remoteParticipants.get(packet.participantIdentity);
			if (packet.value.case === "user") this.handleUserPacket(participant, packet.value.value, packet.kind, encryptionType);
			else if (packet.value.case === "transcription") this.handleTranscription(participant, packet.value.value);
			else if (packet.value.case === "sipDtmf") this.handleSipDtmf(participant, packet.value.value);
			else if (packet.value.case === "chatMessage") this.handleChatMessage(participant, packet.value.value);
			else if (packet.value.case === "metrics") this.handleMetrics(packet.value.value, participant);
			else if (packet.value.case === "streamHeader" || packet.value.case === "streamChunk" || packet.value.case === "streamTrailer") this.handleDataStream(packet, encryptionType);
			else if (packet.value.case === "rpcRequest") {
				const rpc = packet.value.value;
				this.rpcServerManager.handleIncomingRpcRequest(packet.participantIdentity, rpc);
			} else if (packet.value.case === "rpcResponse") {
				const rpcResponse = packet.value.value;
				switch (rpcResponse.value.case) {
					case "payload":
						this.rpcClientManager.handleIncomingRpcResponseSuccess(rpcResponse.requestId, rpcResponse.value.value);
						break;
					case "error":
						this.rpcClientManager.handleIncomingRpcResponseFailure(rpcResponse.requestId, RpcError.fromProto(rpcResponse.value.value));
						break;
					default: this.log.warn("Unknown rpcResponse.value.case: ".concat(rpcResponse.value.case), this.logContext);
				}
			} else if (packet.value.case === "rpcAck") this.rpcClientManager.handleIncomingRpcAck(packet.value.value.requestId);
		};
		this.handleUserPacket = (participant, userPacket, kind, encryptionType) => {
			this.emit(RoomEvent.DataReceived, userPacket.payload, participant, kind, userPacket.topic, encryptionType);
			participant === null || participant === void 0 || participant.emit(ParticipantEvent.DataReceived, userPacket.payload, kind, encryptionType);
		};
		this.handleSipDtmf = (participant, dtmf) => {
			this.emit(RoomEvent.SipDTMFReceived, dtmf, participant);
			participant === null || participant === void 0 || participant.emit(ParticipantEvent.SipDTMFReceived, dtmf);
		};
		this.handleTranscription = (_remoteParticipant, transcription) => {
			const participant = transcription.transcribedParticipantIdentity === this.localParticipant.identity ? this.localParticipant : this.getParticipantByIdentity(transcription.transcribedParticipantIdentity);
			const publication = participant === null || participant === void 0 ? void 0 : participant.trackPublications.get(transcription.trackId);
			const segments = extractTranscriptionSegments(transcription, this.transcriptionReceivedTimes);
			publication === null || publication === void 0 || publication.emit(TrackEvent.TranscriptionReceived, segments);
			participant === null || participant === void 0 || participant.emit(ParticipantEvent.TranscriptionReceived, segments, publication);
			this.emit(RoomEvent.TranscriptionReceived, segments, participant, publication);
		};
		this.handleChatMessage = (participant, chatMessage) => {
			const msg = extractChatMessage(chatMessage);
			this.emit(RoomEvent.ChatMessage, msg, participant);
		};
		this.handleMetrics = (metrics, participant) => {
			this.emit(RoomEvent.MetricsReceived, metrics, participant);
		};
		this.handleDataStream = (packet, encryptionType) => {
			this.incomingDataStreamManager.handleDataStreamPacket(packet, encryptionType);
		};
		this.bufferedSegments = /* @__PURE__ */ new Map();
		this.handleAudioPlaybackStarted = () => {
			if (this.canPlaybackAudio) return;
			this.audioEnabled = true;
			this.emit(RoomEvent.AudioPlaybackStatusChanged, true);
		};
		this.handleAudioPlaybackFailed = (e) => {
			this.log.warn("could not playback audio", { error: e });
			if (!this.canPlaybackAudio) return;
			this.audioEnabled = false;
			this.emit(RoomEvent.AudioPlaybackStatusChanged, false);
		};
		this.handleVideoPlaybackStarted = () => {
			if (this.isVideoPlaybackBlocked) {
				this.isVideoPlaybackBlocked = false;
				this.emit(RoomEvent.VideoPlaybackStatusChanged, true);
			}
		};
		this.handleVideoPlaybackFailed = () => {
			if (!this.isVideoPlaybackBlocked) {
				this.isVideoPlaybackBlocked = true;
				this.emit(RoomEvent.VideoPlaybackStatusChanged, false);
			}
		};
		this.handleDeviceChange = () => __awaiter(this, void 0, void 0, function* () {
			var _a;
			if (((_a = getBrowser()) === null || _a === void 0 ? void 0 : _a.os) !== "iOS") yield this.selectDefaultDevices();
			this.emit(RoomEvent.MediaDevicesChanged);
		});
		this.handleRoomUpdate = (room) => {
			const oldRoom = this.roomInfo;
			this.roomInfo = room;
			if (oldRoom && oldRoom.metadata !== room.metadata) this.emitWhenConnected(RoomEvent.RoomMetadataChanged, room.metadata);
			if ((oldRoom === null || oldRoom === void 0 ? void 0 : oldRoom.activeRecording) !== room.activeRecording) this.emitWhenConnected(RoomEvent.RecordingStatusChanged, room.activeRecording);
		};
		this.handleConnectionQualityUpdate = (update) => {
			update.updates.forEach((info) => {
				if (info.participantSid === this.localParticipant.sid) {
					this.localParticipant.setConnectionQuality(info.quality);
					return;
				}
				const participant = this.getRemoteParticipantBySid(info.participantSid);
				if (participant) participant.setConnectionQuality(info.quality);
			});
		};
		this.getRemoteParticipantClientProtocol = (identity) => {
			var _a, _b;
			return (_b = (_a = this.remoteParticipants.get(identity)) === null || _a === void 0 ? void 0 : _a.clientProtocol) !== null && _b !== void 0 ? _b : 0;
		};
		this.getRemoteParticipantCapabilities = (identity) => {
			var _a, _b;
			return (_b = (_a = this.remoteParticipants.get(identity)) === null || _a === void 0 ? void 0 : _a.capabilities) !== null && _b !== void 0 ? _b : [];
		};
		this.getAllRemoteParticipantIdentities = () => {
			return Array.from(this.remoteParticipants.keys());
		};
		this.onLocalParticipantMetadataChanged = (metadata) => {
			this.emit(RoomEvent.ParticipantMetadataChanged, metadata, this.localParticipant);
		};
		this.onLocalParticipantNameChanged = (name) => {
			this.emit(RoomEvent.ParticipantNameChanged, name, this.localParticipant);
		};
		this.onLocalAttributesChanged = (changedAttributes) => {
			this.emit(RoomEvent.ParticipantAttributesChanged, changedAttributes, this.localParticipant);
		};
		this.onLocalTrackMuted = (pub) => {
			this.emit(RoomEvent.TrackMuted, pub, this.localParticipant);
		};
		this.onLocalTrackUnmuted = (pub) => {
			this.emit(RoomEvent.TrackUnmuted, pub, this.localParticipant);
		};
		this.onTrackProcessorUpdate = (processor) => {
			var _a;
			(_a = processor === null || processor === void 0 ? void 0 : processor.onPublish) === null || _a === void 0 || _a.call(processor, this);
		};
		this.onLocalTrackPublished = (pub) => __awaiter(this, void 0, void 0, function* () {
			var _a, _b, _c, _d, _e, _f;
			(_a = pub.track) === null || _a === void 0 || _a.on(TrackEvent.TrackProcessorUpdate, this.onTrackProcessorUpdate);
			(_b = pub.track) === null || _b === void 0 || _b.on(TrackEvent.Restarted, this.onLocalTrackRestarted);
			(_e = (_d = (_c = pub.track) === null || _c === void 0 ? void 0 : _c.getProcessor()) === null || _d === void 0 ? void 0 : _d.onPublish) === null || _e === void 0 || _e.call(_d, this);
			this.emit(RoomEvent.LocalTrackPublished, pub, this.localParticipant);
			if (isLocalAudioTrack(pub.track)) {
				if (yield pub.track.checkForSilence()) this.emit(RoomEvent.LocalAudioSilenceDetected, pub);
			}
			const deviceId = yield (_f = pub.track) === null || _f === void 0 ? void 0 : _f.getDeviceId(false);
			const deviceKind = sourceToKind(pub.source);
			if (deviceKind && deviceId && deviceId !== this.localParticipant.activeDeviceMap.get(deviceKind)) {
				this.localParticipant.activeDeviceMap.set(deviceKind, deviceId);
				this.emit(RoomEvent.ActiveDeviceChanged, deviceKind, deviceId);
			}
		});
		this.onLocalTrackUnpublished = (pub) => {
			var _a, _b;
			(_a = pub.track) === null || _a === void 0 || _a.off(TrackEvent.TrackProcessorUpdate, this.onTrackProcessorUpdate);
			(_b = pub.track) === null || _b === void 0 || _b.off(TrackEvent.Restarted, this.onLocalTrackRestarted);
			this.emit(RoomEvent.LocalTrackUnpublished, pub, this.localParticipant);
		};
		this.onLocalTrackRestarted = (track) => __awaiter(this, void 0, void 0, function* () {
			const deviceId = yield track.getDeviceId(false);
			const deviceKind = sourceToKind(track.source);
			if (deviceKind && deviceId && deviceId !== this.localParticipant.activeDeviceMap.get(deviceKind)) {
				this.log.debug("local track restarted, setting ".concat(deviceKind, " ").concat(deviceId, " active"));
				this.localParticipant.activeDeviceMap.set(deviceKind, deviceId);
				this.emit(RoomEvent.ActiveDeviceChanged, deviceKind, deviceId);
			}
		});
		this.onLocalConnectionQualityChanged = (quality) => {
			this.emit(RoomEvent.ConnectionQualityChanged, quality, this.localParticipant);
		};
		this.onMediaDevicesError = (e, kind) => {
			this.emit(RoomEvent.MediaDevicesError, e, kind);
		};
		this.onLocalParticipantPermissionsChanged = (prevPermissions) => {
			this.emit(RoomEvent.ParticipantPermissionsChanged, prevPermissions, this.localParticipant);
		};
		this.onLocalChatMessageSent = (msg) => {
			this.emit(RoomEvent.ChatMessage, msg, this.localParticipant);
		};
		this.setMaxListeners(100);
		this.remoteParticipants = /* @__PURE__ */ new Map();
		this.sidToIdentity = /* @__PURE__ */ new Map();
		this.options = Object.assign(Object.assign({}, roomOptionDefaults), options);
		this.log = getLogger((_a = this.options.loggerName) !== null && _a !== void 0 ? _a : LoggerNames.Room, () => this.logContext);
		this.transcriptionReceivedTimes = /* @__PURE__ */ new Map();
		this.options.audioCaptureDefaults = Object.assign(Object.assign({}, audioDefaults), options === null || options === void 0 ? void 0 : options.audioCaptureDefaults);
		this.options.videoCaptureDefaults = Object.assign(Object.assign({}, videoDefaults), options === null || options === void 0 ? void 0 : options.videoCaptureDefaults);
		this.options.publishDefaults = Object.assign(Object.assign({}, publishDefaults), options === null || options === void 0 ? void 0 : options.publishDefaults);
		this.maybeCreateEngine();
		this.incomingDataStreamManager = new IncomingDataStreamManager();
		this.outgoingDataStreamManager = new OutgoingDataStreamManager(this.engine, this.log, this.getRemoteParticipantClientProtocol, this.getRemoteParticipantCapabilities, this.getAllRemoteParticipantIdentities);
		this.incomingDataTrackManager = new IncomingDataTrackManager({ e2eeManager: this.e2eeManager });
		this.incomingDataTrackManager.on("sfuUpdateSubscription", (event) => {
			this.engine.client.sendUpdateDataSubscription(event.sid, event.subscribe);
		}).on("trackPublished", (event) => {
			var _a;
			if (event.track.publisherIdentity === this.localParticipant.identity) return;
			this.emit(RoomEvent.DataTrackPublished, event.track);
			(_a = this.remoteParticipants.get(event.track.publisherIdentity)) === null || _a === void 0 || _a.addRemoteDataTrack(event.track);
		}).on("trackUnpublished", (event) => {
			var _a;
			if (event.publisherIdentity === this.localParticipant.identity) return;
			this.emit(RoomEvent.DataTrackUnpublished, event.sid);
			(_a = this.remoteParticipants.get(event.publisherIdentity)) === null || _a === void 0 || _a.removeRemoteDataTrack(event.sid);
		});
		this.outgoingDataTrackManager = new OutgoingDataTrackManager({ e2eeManager: this.e2eeManager });
		this.outgoingDataTrackManager.on("sfuPublishRequest", (event) => {
			this.engine.client.sendPublishDataTrackRequest(event.handle, event.name, event.usesE2ee);
		}).on("sfuUnpublishRequest", (event) => {
			this.engine.client.sendUnPublishDataTrackRequest(event.handle);
		}).on("trackPublished", (event) => {
			this.emit(RoomEvent.LocalDataTrackPublished, event.track);
		}).on("trackUnpublished", (event) => {
			this.emit(RoomEvent.LocalDataTrackUnpublished, event.sid);
		}).on("packetAvailable", (_ref) => {
			let handle = _ref.handle, bytes = _ref.bytes;
			this.engine.sendDataTrackFrame(bytes).finally(() => this.outgoingDataTrackManager.handlePacketSendComplete(handle));
		});
		this.registerRpcDataStreamHandler();
		this.rpcClientManager = new RpcClientManager(this.log, this.outgoingDataStreamManager, this.getRemoteParticipantClientProtocol, () => {
			var _a, _b;
			return (_b = (_a = this.engine.latestJoinResponse) === null || _a === void 0 ? void 0 : _a.serverInfo) === null || _b === void 0 ? void 0 : _b.version;
		});
		this.rpcClientManager.on("sendDataPacket", (_ref2) => {
			let packet = _ref2.packet;
			var _a;
			(_a = this.engine) === null || _a === void 0 || _a.sendDataPacket(packet, DataChannelKind.RELIABLE);
		});
		this.rpcServerManager = new RpcServerManager(this.log, this.outgoingDataStreamManager, this.getRemoteParticipantClientProtocol);
		this.rpcServerManager.on("sendDataPacket", (_ref3) => {
			let packet = _ref3.packet;
			var _a;
			(_a = this.engine) === null || _a === void 0 || _a.sendDataPacket(packet, DataChannelKind.RELIABLE);
		});
		this.disconnectLock = new _();
		this.localParticipant = new LocalParticipant("", "", this.engine, this.options, this.outgoingDataStreamManager, this.outgoingDataTrackManager, this.rpcClientManager, this.rpcServerManager);
		this.setupFrameMetadata();
		if (this.options.e2ee || this.options.encryption) this.setupE2EE();
		this.engine.e2eeManager = this.e2eeManager;
		this.incomingDataTrackManager.updateE2eeManager((_b = this.e2eeManager) !== null && _b !== void 0 ? _b : null);
		this.outgoingDataTrackManager.updateE2eeManager((_c = this.e2eeManager) !== null && _c !== void 0 ? _c : null);
		if (this.options.videoCaptureDefaults.deviceId) this.localParticipant.activeDeviceMap.set("videoinput", unwrapConstraint(this.options.videoCaptureDefaults.deviceId));
		if (this.options.audioCaptureDefaults.deviceId) this.localParticipant.activeDeviceMap.set("audioinput", unwrapConstraint(this.options.audioCaptureDefaults.deviceId));
		if ((_d = this.options.audioOutput) === null || _d === void 0 ? void 0 : _d.deviceId) this.switchActiveDevice("audiooutput", unwrapConstraint(this.options.audioOutput.deviceId)).catch((e) => this.log.warn("Could not set audio output: ".concat(e.message)));
		if (isWeb()) {
			const cleanupController = new AbortController();
			let onDeviceChange;
			if (Room.cleanupRegistry) {
				const roomRef = new WeakRef(this);
				onDeviceChange = () => {
					const self = roomRef.deref();
					if (!self) return;
					self.handleDeviceChange();
				};
				Room.cleanupRegistry.register(this, () => {
					cleanupController.abort();
				});
			} else onDeviceChange = this.handleDeviceChange;
			(_f = (_e = navigator.mediaDevices) === null || _e === void 0 ? void 0 : _e.addEventListener) === null || _f === void 0 || _f.call(_e, "devicechange", onDeviceChange, { signal: cleanupController.signal });
		}
	}
	registerTextStreamHandler(topic, callback) {
		return this.incomingDataStreamManager.registerTextStreamHandler(topic, callback);
	}
	unregisterTextStreamHandler(topic) {
		return this.incomingDataStreamManager.unregisterTextStreamHandler(topic);
	}
	registerByteStreamHandler(topic, callback) {
		return this.incomingDataStreamManager.registerByteStreamHandler(topic, callback);
	}
	unregisterByteStreamHandler(topic) {
		return this.incomingDataStreamManager.unregisterByteStreamHandler(topic);
	}
	/**
	* Establishes the participant as a receiver for calls of the specified RPC method.
	*
	* @param method - The name of the indicated RPC method
	* @param handler - Will be invoked when an RPC request for this method is received
	* @returns A promise that resolves when the method is successfully registered
	* @throws {Error} If a handler for this method is already registered (must call unregisterRpcMethod first)
	*
	* @example
	* ```typescript
	* room.localParticipant?.registerRpcMethod(
	*   'greet',
	*   async (data: RpcInvocationData) => {
	*     console.log(`Received greeting from ${data.callerIdentity}: ${data.payload}`);
	*     return `Hello, ${data.callerIdentity}!`;
	*   }
	* );
	* ```
	*
	* The handler should return a Promise that resolves to a string.
	* If unable to respond within `responseTimeout`, the request will result in an error on the caller's side.
	*
	* You may throw errors of type `RpcError` with a string `message` in the handler,
	* and they will be received on the caller's side with the message intact.
	* Other errors thrown in your handler will not be transmitted as-is, and will instead arrive to the caller as `1500` ("Application Error").
	*/
	registerRpcMethod(method, handler) {
		this.rpcServerManager.registerRpcMethod(method, handler);
	}
	/**
	* Unregisters a previously registered RPC method.
	*
	* @param method - The name of the RPC method to unregister
	*/
	unregisterRpcMethod(method) {
		this.rpcServerManager.unregisterRpcMethod(method);
	}
	/**
	* @experimental
	*/
	setE2EEEnabled(enabled) {
		return __awaiter(this, void 0, void 0, function* () {
			const unlock = yield this.e2eeStateMutex.lock();
			try {
				if (this.e2eeManager) {
					if (this.isE2EEEnabled !== enabled) {
						yield this.localParticipant.setE2EEEnabled(enabled);
						if (this.localParticipant.identity !== "") this.e2eeManager.setParticipantCryptorEnabled(enabled, this.localParticipant.identity);
					}
				} else throw Error("e2ee not configured, please set e2ee settings within the room options");
			} finally {
				unlock();
			}
		});
	}
	setupE2EE() {
		var _a, _b;
		const dcEncryptionEnabled = !!this.options.encryption;
		const e2eeOptions = this.options.encryption || this.options.e2ee;
		if (e2eeOptions) {
			if ("e2eeManager" in e2eeOptions) {
				this.e2eeManager = e2eeOptions.e2eeManager;
				this.e2eeManager.isDataChannelEncryptionEnabled = dcEncryptionEnabled;
			} else this.e2eeManager = new E2EEManager(e2eeOptions, dcEncryptionEnabled);
			this.e2eeManager.on(EncryptionEvent.ParticipantEncryptionStatusChanged, (enabled, participant) => {
				if (isLocalParticipant(participant)) this.isE2EEEnabled = enabled;
				this.emit(RoomEvent.ParticipantEncryptionStatusChanged, enabled, participant);
			});
			this.e2eeManager.on(EncryptionEvent.EncryptionError, (error, participantIdentity) => {
				const participant = participantIdentity ? this.getParticipantByIdentity(participantIdentity) : void 0;
				this.emit(RoomEvent.EncryptionError, error, participant);
			});
			(_a = this.e2eeManager) === null || _a === void 0 || _a.setup(this);
			(_b = this.e2eeManager) === null || _b === void 0 || _b.setupEngine(this.engine);
		}
	}
	setupFrameMetadata() {
		var _a;
		const opts = (_a = this.options.frameMetadata) !== null && _a !== void 0 ? _a : this.options.packetTrailer;
		this.frameMetadataManager = new FrameMetadataManager(opts);
		this.frameMetadataManager.setup(this);
	}
	get logContext() {
		var _a, _b, _c;
		return {
			room: this.name,
			roomID: (_a = this.roomInfo) === null || _a === void 0 ? void 0 : _a.sid,
			participant: (_b = this.localParticipant) === null || _b === void 0 ? void 0 : _b.identity,
			participantID: (_c = this.localParticipant) === null || _c === void 0 ? void 0 : _c.sid
		};
	}
	/**
	* if the current room has a participant with `recorder: true` in its JWT grant
	**/
	get isRecording() {
		var _a, _b;
		return (_b = (_a = this.roomInfo) === null || _a === void 0 ? void 0 : _a.activeRecording) !== null && _b !== void 0 ? _b : false;
	}
	/**
	* server assigned unique room id.
	* returns once a sid has been issued by the server.
	*/
	getSid() {
		if (this.state === ConnectionState.Disconnected) return TypedPromise.resolve("");
		if (this.roomInfo && this.roomInfo.sid !== "") return TypedPromise.resolve(this.roomInfo.sid);
		return new TypedPromise((resolve, reject) => {
			const handleRoomUpdate = (roomInfo) => {
				if (roomInfo.sid !== "") {
					this.engine.off(EngineEvent.RoomUpdate, handleRoomUpdate);
					resolve(roomInfo.sid);
				}
			};
			this.engine.on(EngineEvent.RoomUpdate, handleRoomUpdate);
			this.once(RoomEvent.Disconnected, () => {
				this.engine.off(EngineEvent.RoomUpdate, handleRoomUpdate);
				reject(new UnexpectedConnectionState("Room disconnected before room server id was available"));
			});
		});
	}
	/** user assigned name, derived from JWT token */
	get name() {
		var _a, _b;
		return (_b = (_a = this.roomInfo) === null || _a === void 0 ? void 0 : _a.name) !== null && _b !== void 0 ? _b : "";
	}
	/** room metadata */
	get metadata() {
		var _a;
		return (_a = this.roomInfo) === null || _a === void 0 ? void 0 : _a.metadata;
	}
	get numParticipants() {
		var _a, _b;
		return (_b = (_a = this.roomInfo) === null || _a === void 0 ? void 0 : _a.numParticipants) !== null && _b !== void 0 ? _b : 0;
	}
	get numPublishers() {
		var _a, _b;
		return (_b = (_a = this.roomInfo) === null || _a === void 0 ? void 0 : _a.numPublishers) !== null && _b !== void 0 ? _b : 0;
	}
	maybeCreateEngine() {
		if (this.engine && (this.engine.isNewlyCreated || !this.engine.isClosed)) return;
		this.engine = new RTCEngine(this.options);
		this.engine.e2eeManager = this.e2eeManager;
		this.engine.on(EngineEvent.ParticipantUpdate, this.handleParticipantUpdates).on(EngineEvent.RoomUpdate, this.handleRoomUpdate).on(EngineEvent.SpeakersChanged, this.handleSpeakersChanged).on(EngineEvent.StreamStateChanged, this.handleStreamStateUpdate).on(EngineEvent.ConnectionQualityUpdate, this.handleConnectionQualityUpdate).on(EngineEvent.SubscriptionError, this.handleSubscriptionError).on(EngineEvent.SubscriptionPermissionUpdate, this.handleSubscriptionPermissionUpdate).on(EngineEvent.MediaTrackAdded, (mediaTrack, stream, receiver) => {
			this.onTrackAdded(mediaTrack, stream, receiver);
		}).on(EngineEvent.Disconnected, (reason) => {
			this.handleDisconnect(this.options.stopLocalTrackOnUnpublish, reason);
		}).on(EngineEvent.ActiveSpeakersUpdate, this.handleActiveSpeakersUpdate).on(EngineEvent.DataPacketReceived, this.handleDataPacket).on(EngineEvent.Resuming, () => {
			this.clearConnectionReconcile();
			this.isResuming = true;
			this.log.debug("Resuming signal connection");
			if (this.setAndEmitConnectionState(ConnectionState.SignalReconnecting)) this.emit(RoomEvent.SignalReconnecting);
		}).on(EngineEvent.Resumed, () => {
			this.registerConnectionReconcile();
			this.isResuming = false;
			this.log.debug("Resumed signal connection");
			this.updateSubscriptions();
			if (this.setAndEmitConnectionState(ConnectionState.Connected)) this.emit(RoomEvent.Reconnected);
			this.emitBufferedEvents();
		}).on(EngineEvent.SignalResumed, () => {
			if (this.state === ConnectionState.Reconnecting || this.isResuming) this.sendSyncState();
			this.emitBufferedEvents();
		}).on(EngineEvent.Restarting, this.handleRestarting).on(EngineEvent.Restarted, this.handleRestarted).on(EngineEvent.SignalRestarted, this.handleSignalRestarted).on(EngineEvent.Offline, () => {
			if (this.setAndEmitConnectionState(ConnectionState.Reconnecting)) this.emit(RoomEvent.Reconnecting);
		}).on(EngineEvent.DCBufferStatusChanged, (status, kind) => {
			this.emit(RoomEvent.DCBufferStatusChanged, status, kind);
		}).on(EngineEvent.LocalTrackSubscribed, (subscribedSid) => {
			this.handleLocalTrackSubscribed(subscribedSid);
		}).on(EngineEvent.RoomMoved, (roomMoved) => {
			this.log.debug("room moved", roomMoved);
			if (roomMoved.room) this.handleRoomUpdate(roomMoved.room);
			this.remoteParticipants.forEach((participant, identity) => {
				this.handleParticipantDisconnected(identity, participant);
			});
			this.emit(RoomEvent.Moved, roomMoved.room.name);
			if (roomMoved.participant) this.handleParticipantUpdates([roomMoved.participant, ...roomMoved.otherParticipants]);
			else this.handleParticipantUpdates(roomMoved.otherParticipants);
		}).on(EngineEvent.PublishDataTrackResponse, (event) => {
			if (!event.info) {
				this.log.warn("received PublishDataTrackResponse, but event.info was ".concat(event.info, ", so skipping."));
				return;
			}
			this.outgoingDataTrackManager.receivedSfuPublishResponse(event.info.pubHandle, {
				type: "ok",
				data: {
					sid: event.info.sid,
					pubHandle: event.info.pubHandle,
					name: event.info.name,
					usesE2ee: event.info.encryption !== Encryption_Type.NONE
				}
			});
		}).on(EngineEvent.UnPublishDataTrackResponse, (event) => {
			if (!event.info) {
				this.log.warn("received UnPublishDataTrackResponse, but event.info was ".concat(event.info, ", so skipping."));
				return;
			}
			this.outgoingDataTrackManager.receivedSfuUnpublishResponse(event.info.pubHandle);
		}).on(EngineEvent.DataTrackSubscriberHandles, (event) => {
			const handleToSidMapping = new Map(Object.entries(event.subHandles).map((_ref4) => {
				let _ref5 = _slicedToArray(_ref4, 2), key = _ref5[0], value = _ref5[1];
				return [parseInt(key, 10), value.trackSid];
			}));
			this.incomingDataTrackManager.receivedSfuSubscriberHandles(handleToSidMapping);
		}).on(EngineEvent.DataTrackPacketReceived, (packetBytes) => {
			try {
				this.incomingDataTrackManager.packetReceived(packetBytes);
			} catch (err) {
				throw err;
			}
		}).on(EngineEvent.Joined, (joinResponse) => {
			const mapped = new Map(joinResponse.otherParticipants.map((participant) => {
				return [participant.identity, participant.dataTracks.map((info) => DataTrackInfo.from(info))];
			}));
			this.incomingDataTrackManager.receiveSfuPublicationUpdates(mapped);
		}).on(EngineEvent.TokenRefreshed, (token) => {
			var _a;
			(_a = this.regionUrlProvider) === null || _a === void 0 || _a.updateToken(token);
		}).on(EngineEvent.ServerRegionsReported, (regions) => {
			var _a;
			(_a = this.regionUrlProvider) === null || _a === void 0 || _a.setServerReportedRegions({
				regionSettings: regions,
				updatedAtInMs: Date.now(),
				maxAgeInMs: DEFAULT_MAX_AGE_MS
			});
		});
		if (this.localParticipant) this.localParticipant.setupEngine(this.engine);
		if (this.e2eeManager) this.e2eeManager.setupEngine(this.engine);
		if (this.outgoingDataStreamManager) this.outgoingDataStreamManager.setupEngine(this.engine);
	}
	createRegionStrategy() {
		return {
			getNextUrl: (signal) => __awaiter(this, void 0, void 0, function* () {
				return this.regionUrlProvider ? this.regionUrlProvider.getNextBestRegionUrl(signal) : null;
			}),
			resetAttempts: () => {
				var _a;
				return (_a = this.regionUrlProvider) === null || _a === void 0 ? void 0 : _a.resetAttempts();
			}
		};
	}
	/**
	* getLocalDevices abstracts navigator.mediaDevices.enumerateDevices.
	* In particular, it requests device permissions by default if needed
	* and makes sure the returned device does not consist of dummy devices
	* @param kind
	* @returns a list of available local devices
	*/
	static getLocalDevices(kind) {
		let requestPermissions = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : true;
		return DeviceManager.getInstance().getDevices(kind, requestPermissions);
	}
	/**
	* prepareConnection should be called as soon as the page is loaded, in order
	* to speed up the connection attempt. This function will
	* - perform DNS resolution and pre-warm the DNS cache
	* - establish TLS connection and cache TLS keys
	*
	* With LiveKit Cloud, it will also determine the best edge data center for
	* the current client to connect to if a token is provided.
	*/
	prepareConnection(url, token) {
		return __awaiter(this, void 0, void 0, function* () {
			if (this.state !== ConnectionState.Disconnected) return;
			this.log.debug("prepareConnection to ".concat(url));
			try {
				if (isCloud(new URL(url)) && token) {
					this.regionUrlProvider = new RegionUrlProvider(url, token);
					const regionUrl = yield this.regionUrlProvider.getNextBestRegionUrl();
					if (regionUrl && this.state === ConnectionState.Disconnected) {
						this.regionUrl = regionUrl;
						yield fetch(toHttpUrl(regionUrl), { method: "HEAD" });
						this.log.debug("prepared connection to ".concat(regionUrl));
					}
				} else yield fetch(toHttpUrl(url), { method: "HEAD" });
			} catch (e) {
				this.log.warn("could not prepare connection", { error: e });
			}
		});
	}
	/**
	* retrieves a participant by identity
	* @param identity
	* @returns
	*/
	getParticipantByIdentity(identity) {
		if (this.localParticipant.identity === identity) return this.localParticipant;
		return this.remoteParticipants.get(identity);
	}
	clearConnectionFutures() {
		this.connectFuture = void 0;
	}
	/**
	* @internal for testing
	*/
	simulateScenario(scenario, arg) {
		return __awaiter(this, void 0, void 0, function* () {
			let postAction = () => __awaiter(this, void 0, void 0, function* () {});
			let req;
			switch (scenario) {
				case "signal-reconnect":
					yield this.engine.client.handleOnClose("simulate disconnect");
					break;
				case "fail-on-v1-path":
					this.engine.failNextV1Path();
					break;
				case "speaker":
					req = new SimulateScenario({ scenario: {
						case: "speakerUpdate",
						value: 3
					} });
					break;
				case "node-failure":
					req = new SimulateScenario({ scenario: {
						case: "nodeFailure",
						value: true
					} });
					break;
				case "server-leave":
					req = new SimulateScenario({ scenario: {
						case: "serverLeave",
						value: true
					} });
					break;
				case "migration":
					req = new SimulateScenario({ scenario: {
						case: "migration",
						value: true
					} });
					break;
				case "resume-reconnect":
					this.engine.failNext();
					yield this.engine.client.handleOnClose("simulate resume-disconnect");
					break;
				case "disconnect-signal-on-resume":
					postAction = () => __awaiter(this, void 0, void 0, function* () {
						yield this.engine.client.handleOnClose("simulate resume-disconnect");
					});
					req = new SimulateScenario({ scenario: {
						case: "disconnectSignalOnResume",
						value: true
					} });
					break;
				case "disconnect-signal-on-resume-no-messages":
					postAction = () => __awaiter(this, void 0, void 0, function* () {
						yield this.engine.client.handleOnClose("simulate resume-disconnect");
					});
					req = new SimulateScenario({ scenario: {
						case: "disconnectSignalOnResumeNoMessages",
						value: true
					} });
					break;
				case "full-reconnect":
					this.engine.fullReconnectOnNext = true;
					yield this.engine.client.handleOnClose("simulate full-reconnect");
					break;
				case "force-tcp":
				case "force-tls":
					req = new SimulateScenario({ scenario: {
						case: "switchCandidateProtocol",
						value: scenario === "force-tls" ? 2 : 1
					} });
					postAction = () => __awaiter(this, void 0, void 0, function* () {
						const onLeave = this.engine.client.onLeave;
						if (onLeave) onLeave(new LeaveRequest({
							reason: DisconnectReason.CLIENT_INITIATED,
							action: LeaveRequest_Action.RECONNECT
						}));
					});
					break;
				case "subscriber-bandwidth":
					if (arg === void 0 || typeof arg !== "number") throw new Error("subscriber-bandwidth requires a number as argument");
					req = new SimulateScenario({ scenario: {
						case: "subscriberBandwidth",
						value: numberToBigInt(arg)
					} });
					break;
				case "leave-full-reconnect": req = new SimulateScenario({ scenario: {
					case: "leaveRequestFullReconnect",
					value: true
				} });
			}
			if (req) {
				yield this.engine.client.sendSimulateScenario(req);
				yield postAction();
			}
		});
	}
	/**
	* Returns true if audio playback is enabled
	*/
	get canPlaybackAudio() {
		return this.audioEnabled;
	}
	/**
	* Returns true if video playback is enabled
	*/
	get canPlaybackVideo() {
		return !this.isVideoPlaybackBlocked;
	}
	getActiveDevice(kind) {
		return this.localParticipant.activeDeviceMap.get(kind);
	}
	/**
	* Switches all active devices used in this room to the given device.
	*
	* Note: setting AudioOutput is not supported on some browsers. See [setSinkId](https://developer.mozilla.org/en-US/docs/Web/API/HTMLMediaElement/setSinkId#browser_compatibility)
	*
	* @param kind use `videoinput` for camera track,
	*  `audioinput` for microphone track,
	*  `audiooutput` to set speaker for all incoming audio tracks
	* @param deviceId
	*/
	switchActiveDevice(kind_1, deviceId_1) {
		return __awaiter(this, arguments, void 0, function(kind, deviceId) {
			var _this3 = this;
			let exact = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : true;
			return function* () {
				var _a, _b, _c, _d, _e, _f;
				var _g;
				let success = true;
				let shouldTriggerImmediateDeviceChange = false;
				const deviceConstraint = exact ? { exact: deviceId } : deviceId;
				if (kind === "audioinput") {
					shouldTriggerImmediateDeviceChange = _this3.localParticipant.audioTrackPublications.size === 0;
					const prevDeviceId = (_a = _this3.getActiveDevice(kind)) !== null && _a !== void 0 ? _a : _this3.options.audioCaptureDefaults.deviceId;
					_this3.options.audioCaptureDefaults.deviceId = deviceConstraint;
					const tracks = Array.from(_this3.localParticipant.audioTrackPublications.values()).filter((track) => track.source === Track.Source.Microphone);
					try {
						success = (yield Promise.all(tracks.map((t) => {
							var _a;
							return (_a = t.audioTrack) === null || _a === void 0 ? void 0 : _a.setDeviceId(deviceConstraint);
						}))).every((val) => val === true);
					} catch (e) {
						_this3.options.audioCaptureDefaults.deviceId = prevDeviceId;
						throw e;
					}
					const isMuted = tracks.some((t) => {
						var _a, _b;
						return (_b = (_a = t.track) === null || _a === void 0 ? void 0 : _a.isMuted) !== null && _b !== void 0 ? _b : false;
					});
					if (success && isMuted) shouldTriggerImmediateDeviceChange = true;
				} else if (kind === "videoinput") {
					shouldTriggerImmediateDeviceChange = _this3.localParticipant.videoTrackPublications.size === 0;
					const prevDeviceId = (_b = _this3.getActiveDevice(kind)) !== null && _b !== void 0 ? _b : _this3.options.videoCaptureDefaults.deviceId;
					_this3.options.videoCaptureDefaults.deviceId = deviceConstraint;
					const tracks = Array.from(_this3.localParticipant.videoTrackPublications.values()).filter((track) => track.source === Track.Source.Camera);
					try {
						success = (yield Promise.all(tracks.map((t) => {
							var _a;
							return (_a = t.videoTrack) === null || _a === void 0 ? void 0 : _a.setDeviceId(deviceConstraint);
						}))).every((val) => val === true);
					} catch (e) {
						_this3.options.videoCaptureDefaults.deviceId = prevDeviceId;
						throw e;
					}
					const isMuted = tracks.some((t) => {
						var _a, _b;
						return (_b = (_a = t.track) === null || _a === void 0 ? void 0 : _a.isMuted) !== null && _b !== void 0 ? _b : false;
					});
					if (success && isMuted) shouldTriggerImmediateDeviceChange = true;
				} else if (kind === "audiooutput") {
					shouldTriggerImmediateDeviceChange = true;
					if (!supportsSetSinkId() && !_this3.options.webAudioMix || _this3.options.webAudioMix && _this3.audioContext && !("setSinkId" in _this3.audioContext)) throw new Error("cannot switch audio output, the current browser does not support it");
					if (_this3.options.webAudioMix) deviceId = (_c = yield DeviceManager.getInstance().normalizeDeviceId("audiooutput", deviceId)) !== null && _c !== void 0 ? _c : "";
					(_d = (_g = _this3.options).audioOutput) !== null && _d !== void 0 || (_g.audioOutput = {});
					const prevDeviceId = (_e = _this3.getActiveDevice(kind)) !== null && _e !== void 0 ? _e : _this3.options.audioOutput.deviceId;
					_this3.options.audioOutput.deviceId = deviceId;
					try {
						if (_this3.options.webAudioMix) (_f = _this3.audioContext) === null || _f === void 0 || _f.setSinkId(deviceId);
						yield Promise.all(Array.from(_this3.remoteParticipants.values()).map((p) => p.setAudioOutput({ deviceId })));
					} catch (e) {
						_this3.options.audioOutput.deviceId = prevDeviceId;
						throw e;
					}
				}
				if (shouldTriggerImmediateDeviceChange) {
					_this3.localParticipant.activeDeviceMap.set(kind, deviceId);
					_this3.emit(RoomEvent.ActiveDeviceChanged, kind, deviceId);
				}
				return success;
			}();
		});
	}
	setupLocalParticipantEvents() {
		this.localParticipant.on(ParticipantEvent.ParticipantMetadataChanged, this.onLocalParticipantMetadataChanged).on(ParticipantEvent.ParticipantNameChanged, this.onLocalParticipantNameChanged).on(ParticipantEvent.AttributesChanged, this.onLocalAttributesChanged).on(ParticipantEvent.TrackMuted, this.onLocalTrackMuted).on(ParticipantEvent.TrackUnmuted, this.onLocalTrackUnmuted).on(ParticipantEvent.LocalTrackPublished, this.onLocalTrackPublished).on(ParticipantEvent.LocalTrackUnpublished, this.onLocalTrackUnpublished).on(ParticipantEvent.ConnectionQualityChanged, this.onLocalConnectionQualityChanged).on(ParticipantEvent.MediaDevicesError, this.onMediaDevicesError).on(ParticipantEvent.AudioStreamAcquired, this.startAudio).on(ParticipantEvent.ChatMessage, this.onLocalChatMessageSent).on(ParticipantEvent.ParticipantPermissionsChanged, this.onLocalParticipantPermissionsChanged);
	}
	recreateEngine(sendLeave) {
		const oldEngine = this.engine;
		if (sendLeave && oldEngine && !oldEngine.client.isDisconnected) oldEngine.client.sendLeave().finally(() => oldEngine.close());
		else oldEngine === null || oldEngine === void 0 || oldEngine.close();
		this.engine = void 0;
		this.isResuming = false;
		this.remoteParticipants.clear();
		this.sidToIdentity.clear();
		this.bufferedEvents = [];
		this.maybeCreateEngine();
	}
	onTrackAdded(mediaTrack, stream, receiver) {
		var _a, _b;
		if ([ConnectionState.Connecting, ConnectionState.Reconnecting].includes(this.state)) {
			const pendingTrackSid = extractTrackSid(mediaTrack, stream);
			this.log.debug("deferring on track for later", {
				mediaTrackId: mediaTrack.id,
				mediaStreamId: stream.id,
				tracksInStream: stream.getTracks().map((track) => track.id)
			});
			const reconnectedHandler = () => {
				cleanup();
				this.onTrackAdded(mediaTrack, stream, receiver);
			};
			const cleanup = () => {
				this.off(RoomEvent.Reconnected, reconnectedHandler);
				this.off(RoomEvent.Connected, reconnectedHandler);
				this.off(RoomEvent.Disconnected, cleanup);
				if (pendingTrackSid) {
					const pendingCallbacks = this.pendingTrackAddedCallbacks.get(pendingTrackSid);
					pendingCallbacks === null || pendingCallbacks === void 0 || pendingCallbacks.delete(cleanup);
					if ((pendingCallbacks === null || pendingCallbacks === void 0 ? void 0 : pendingCallbacks.size) === 0) this.pendingTrackAddedCallbacks.delete(pendingTrackSid);
				}
			};
			this.once(RoomEvent.Reconnected, reconnectedHandler);
			this.once(RoomEvent.Connected, reconnectedHandler);
			this.once(RoomEvent.Disconnected, cleanup);
			if (pendingTrackSid) {
				const pendingCallbacks = (_a = this.pendingTrackAddedCallbacks.get(pendingTrackSid)) !== null && _a !== void 0 ? _a : /* @__PURE__ */ new Set();
				pendingCallbacks.add(cleanup);
				this.pendingTrackAddedCallbacks.set(pendingTrackSid, pendingCallbacks);
			}
			return;
		}
		if (this.state === ConnectionState.Disconnected) {
			this.log.warn("skipping incoming track after Room disconnected");
			return;
		}
		if (mediaTrack.readyState === "ended") {
			this.log.debug("skipping incoming track as it already ended");
			return;
		}
		const parts = unpackStreamId(stream.id);
		const participantSid = parts[0];
		const streamId = parts[1];
		let trackId = (_b = extractTrackSid(mediaTrack, stream)) !== null && _b !== void 0 ? _b : mediaTrack.id;
		if (participantSid === this.localParticipant.sid) {
			this.log.warn("tried to create RemoteParticipant for local participant");
			return;
		}
		const participant = Array.from(this.remoteParticipants.values()).find((p) => p.sid === participantSid);
		if (!participant) {
			if (participantSid.startsWith("PA")) this.log.error("Tried to add a track for a participant, that's not present. Sid: ".concat(participantSid));
			return;
		}
		if (!trackId.startsWith("TR")) {
			const id = this.engine.getTrackIdForReceiver(receiver);
			if (!id) {
				this.log.error("Tried to add a track whose 'sid' could not be found for a participant, that's not present. Sid: ".concat(participantSid));
				return;
			}
			trackId = id;
		}
		if (!trackId.startsWith("TR")) this.log.warn("Tried to add a track whose 'sid' could not be determined for a participant, that's not present. Sid: ".concat(participantSid, ", streamId: ").concat(streamId, ", trackId: ").concat(trackId), {
			remoteParticipantID: participantSid,
			streamId,
			trackId
		});
		let adaptiveStreamSettings;
		if (this.options.adaptiveStream) {
			if (typeof this.options.adaptiveStream === "object") adaptiveStreamSettings = this.options.adaptiveStream;
			else adaptiveStreamSettings = {};
		}
		const publication = participant.addSubscribedMediaTrack(mediaTrack, trackId, stream, receiver, adaptiveStreamSettings);
		if ((publication === null || publication === void 0 ? void 0 : publication.isEncrypted) && !this.e2eeManager) this.emit(RoomEvent.EncryptionError, new Error("Encrypted ".concat(publication.source, " track received from participant ").concat(participant.sid, ", but room does not have encryption enabled!")));
	}
	cancelPendingTrackAdded(trackSid) {
		var _a;
		(_a = this.pendingTrackAddedCallbacks.get(trackSid)) === null || _a === void 0 || _a.forEach((cleanup) => cleanup());
	}
	handleLocalTrackSubscribed(subscribedSid) {
		const findPublication = () => this.localParticipant.getTrackPublications().find((_ref6) => {
			return _ref6.trackSid === subscribedSid;
		});
		const trackPublication = findPublication();
		if (trackPublication) {
			this.emitLocalTrackSubscribed(trackPublication);
			return;
		}
		this.log.debug("deferring LocalTrackSubscribed, publication not yet available", { subscribedSid });
		const TIMEOUT_MS = 1e4;
		let timer;
		const onPublished = (pub) => {
			if (pub.trackSid === subscribedSid) {
				cleanup();
				this.emitLocalTrackSubscribed(pub);
			}
		};
		const cleanup = () => {
			clearTimeout(timer);
			this.localParticipant.off(ParticipantEvent.LocalTrackPublished, onPublished);
			this.off(RoomEvent.Disconnected, cleanup);
		};
		this.localParticipant.on(ParticipantEvent.LocalTrackPublished, onPublished);
		this.once(RoomEvent.Disconnected, cleanup);
		timer = setTimeout(() => {
			cleanup();
			const pub = findPublication();
			if (pub) this.emitLocalTrackSubscribed(pub);
			else this.log.warn("could not find local track publication for LocalTrackSubscribed event after timeout", { subscribedSid });
		}, TIMEOUT_MS);
	}
	emitLocalTrackSubscribed(trackPublication) {
		this.localParticipant.emit(ParticipantEvent.LocalTrackSubscribed, trackPublication);
		this.emitWhenConnected(RoomEvent.LocalTrackSubscribed, trackPublication, this.localParticipant);
	}
	handleDisconnect() {
		let shouldStopTracks = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : true;
		let reason = arguments.length > 1 ? arguments[1] : void 0;
		var _a, _b;
		this.clearConnectionReconcile();
		this.isResuming = false;
		this.bufferedEvents = [];
		this.transcriptionReceivedTimes.clear();
		this.incomingDataStreamManager.clearControllers();
		this.incomingDataTrackManager.reset();
		this.outgoingDataTrackManager.reset();
		if (this.state === ConnectionState.Disconnected) return;
		this.regionUrl = void 0;
		if (this.regionUrlProvider) this.regionUrlProvider.notifyDisconnected();
		try {
			this.remoteParticipants.forEach((p) => {
				p.trackPublications.forEach((pub) => {
					p.unpublishTrack(pub.trackSid);
				});
			});
			this.localParticipant.trackPublications.forEach((pub) => {
				var _a, _b, _c;
				if (pub.track) this.localParticipant.unpublishTrack(pub.track, shouldStopTracks);
				if (shouldStopTracks) {
					(_a = pub.track) === null || _a === void 0 || _a.detach();
					(_b = pub.track) === null || _b === void 0 || _b.stop();
				} else (_c = pub.track) === null || _c === void 0 || _c.stopMonitor();
			});
			this.localParticipant.off(ParticipantEvent.ParticipantMetadataChanged, this.onLocalParticipantMetadataChanged).off(ParticipantEvent.ParticipantNameChanged, this.onLocalParticipantNameChanged).off(ParticipantEvent.AttributesChanged, this.onLocalAttributesChanged).off(ParticipantEvent.TrackMuted, this.onLocalTrackMuted).off(ParticipantEvent.TrackUnmuted, this.onLocalTrackUnmuted).off(ParticipantEvent.LocalTrackPublished, this.onLocalTrackPublished).off(ParticipantEvent.LocalTrackUnpublished, this.onLocalTrackUnpublished).off(ParticipantEvent.ConnectionQualityChanged, this.onLocalConnectionQualityChanged).off(ParticipantEvent.MediaDevicesError, this.onMediaDevicesError).off(ParticipantEvent.AudioStreamAcquired, this.startAudio).off(ParticipantEvent.ChatMessage, this.onLocalChatMessageSent).off(ParticipantEvent.ParticipantPermissionsChanged, this.onLocalParticipantPermissionsChanged);
			this.localParticipant.trackPublications.clear();
			this.localParticipant.videoTrackPublications.clear();
			this.localParticipant.audioTrackPublications.clear();
			this.remoteParticipants.clear();
			this.sidToIdentity.clear();
			this.activeSpeakers = [];
			if (this.audioContext && typeof this.options.webAudioMix === "boolean") {
				this.audioContext.close();
				this.audioContext = void 0;
			}
			if (isWeb()) {
				window.removeEventListener("beforeunload", this.onPageLeave);
				window.removeEventListener("pagehide", this.onPageLeave);
				window.removeEventListener("freeze", this.onPageLeave);
				(_b = (_a = navigator.mediaDevices) === null || _a === void 0 ? void 0 : _a.removeEventListener) === null || _b === void 0 || _b.call(_a, "devicechange", this.handleDeviceChange);
			}
		} finally {
			this.setAndEmitConnectionState(ConnectionState.Disconnected);
			this.emit(RoomEvent.Disconnected, reason);
		}
	}
	handleParticipantDisconnected(identity, participant) {
		this.remoteParticipants.delete(identity);
		if (!participant) return;
		this.incomingDataStreamManager.validateParticipantHasNoActiveDataStreams(identity);
		this.incomingDataTrackManager.handleRemoteParticipantDisconnected(identity);
		participant.trackPublications.forEach((publication) => {
			participant.unpublishTrack(publication.trackSid, true);
		});
		this.emit(RoomEvent.ParticipantDisconnected, participant);
		participant.setDisconnected();
		this.rpcClientManager.handleParticipantDisconnected(participant.identity);
	}
	/**
	* attempt to select the default devices if the previously selected devices are no longer available after a device change event
	*/
	selectDefaultDevices() {
		return __awaiter(this, void 0, void 0, function* () {
			var _a, _b, _c;
			const previousDevices = DeviceManager.getInstance().previousDevices;
			const availableDevices = yield DeviceManager.getInstance().getDevices(void 0, false);
			const browser = getBrowser();
			if ((browser === null || browser === void 0 ? void 0 : browser.name) === "Chrome" && browser.os !== "iOS") for (let availableDevice of availableDevices) {
				const previousDevice = previousDevices.find((info) => info.deviceId === availableDevice.deviceId);
				if (previousDevice && previousDevice.label !== "" && previousDevice.kind === availableDevice.kind && previousDevice.label !== availableDevice.label) {
					if (this.getActiveDevice(availableDevice.kind) === "default") this.emit(RoomEvent.ActiveDeviceChanged, availableDevice.kind, availableDevice.deviceId);
				}
			}
			for (let kind of [
				"audiooutput",
				"audioinput",
				"videoinput"
			]) {
				const targetSource = kindToSource(kind);
				const targetPublication = this.localParticipant.getTrackPublication(targetSource);
				if (targetPublication && ((_a = targetPublication.track) === null || _a === void 0 ? void 0 : _a.isUserProvided)) continue;
				const devicesOfKind = availableDevices.filter((d) => d.kind === kind);
				const activeDevice = this.getActiveDevice(kind);
				if (activeDevice === ((_b = previousDevices.filter((info) => info.kind === kind)[0]) === null || _b === void 0 ? void 0 : _b.deviceId)) {
					if (devicesOfKind.length > 0 && ((_c = devicesOfKind[0]) === null || _c === void 0 ? void 0 : _c.deviceId) !== activeDevice) {
						yield this.switchActiveDevice(kind, devicesOfKind[0].deviceId);
						continue;
					}
				}
				if (kind === "audioinput" && !isSafariBased() || kind === "videoinput") continue;
				if (devicesOfKind.length > 0 && !devicesOfKind.find((deviceInfo) => deviceInfo.deviceId === this.getActiveDevice(kind)) && (kind !== "audiooutput" || !isSafariBased())) yield this.switchActiveDevice(kind, devicesOfKind[0].deviceId);
			}
		});
	}
	acquireAudioContext() {
		return __awaiter(this, void 0, void 0, function* () {
			var _a, _b;
			if (typeof this.options.webAudioMix !== "boolean" && this.options.webAudioMix.audioContext) this.audioContext = this.options.webAudioMix.audioContext;
			else if (!this.audioContext || this.audioContext.state === "closed") this.audioContext = (_a = getNewAudioContext()) !== null && _a !== void 0 ? _a : void 0;
			if (this.options.webAudioMix) this.remoteParticipants.forEach((participant) => participant.setAudioContext(this.audioContext));
			this.localParticipant.setAudioContext(this.audioContext);
			if (this.audioContext && this.audioContext.state === "suspended") try {
				yield Promise.race([this.audioContext.resume(), sleep(200)]);
			} catch (e) {
				this.log.warn("Could not resume audio context", { error: e });
			}
			const newContextIsRunning = ((_b = this.audioContext) === null || _b === void 0 ? void 0 : _b.state) === "running";
			if (newContextIsRunning !== this.canPlaybackAudio) {
				this.audioEnabled = newContextIsRunning;
				this.emit(RoomEvent.AudioPlaybackStatusChanged, newContextIsRunning);
			}
		});
	}
	createParticipant(identity, info) {
		var _a;
		let participant;
		if (info) participant = RemoteParticipant.fromParticipantInfo(this.engine.client, info, {
			loggerContextCb: () => this.logContext,
			loggerName: this.options.loggerName
		}, this.incomingDataTrackManager);
		else participant = new RemoteParticipant(this.engine.client, "", identity, void 0, void 0, void 0, {
			loggerContextCb: () => this.logContext,
			loggerName: this.options.loggerName
		});
		if (this.options.webAudioMix) participant.setAudioContext(this.audioContext);
		if ((_a = this.options.audioOutput) === null || _a === void 0 ? void 0 : _a.deviceId) participant.setAudioOutput(this.options.audioOutput).catch((e) => this.log.warn("Could not set audio output: ".concat(e.message)));
		return participant;
	}
	getOrCreateParticipant(identity, info) {
		if (this.remoteParticipants.has(identity)) {
			const existingParticipant = this.remoteParticipants.get(identity);
			if (info) {
				if (existingParticipant.updateInfo(info)) this.sidToIdentity.set(info.sid, info.identity);
			}
			return existingParticipant;
		}
		const participant = this.createParticipant(identity, info);
		this.remoteParticipants.set(identity, participant);
		this.sidToIdentity.set(info.sid, info.identity);
		this.emitWhenConnected(RoomEvent.ParticipantConnected, participant);
		participant.on(ParticipantEvent.TrackPublished, (trackPublication) => {
			this.emitWhenConnected(RoomEvent.TrackPublished, trackPublication, participant);
		}).on(ParticipantEvent.TrackSubscribed, (track, publication) => {
			if (track.kind === Track.Kind.Audio) {
				track.on(TrackEvent.AudioPlaybackStarted, this.handleAudioPlaybackStarted);
				track.on(TrackEvent.AudioPlaybackFailed, this.handleAudioPlaybackFailed);
			} else if (track.kind === Track.Kind.Video) {
				track.on(TrackEvent.VideoPlaybackFailed, this.handleVideoPlaybackFailed);
				track.on(TrackEvent.VideoPlaybackStarted, this.handleVideoPlaybackStarted);
			}
			this.emitWhenConnected(RoomEvent.TrackSubscribed, track, publication, participant);
		}).on(ParticipantEvent.TrackUnpublished, (publication) => {
			this.cancelPendingTrackAdded(publication.trackSid);
			this.emit(RoomEvent.TrackUnpublished, publication, participant);
		}).on(ParticipantEvent.TrackUnsubscribed, (track, publication) => {
			this.emit(RoomEvent.TrackUnsubscribed, track, publication, participant);
		}).on(ParticipantEvent.TrackMuted, (pub) => {
			this.emitWhenConnected(RoomEvent.TrackMuted, pub, participant);
		}).on(ParticipantEvent.TrackUnmuted, (pub) => {
			this.emitWhenConnected(RoomEvent.TrackUnmuted, pub, participant);
		}).on(ParticipantEvent.ParticipantMetadataChanged, (metadata) => {
			this.emitWhenConnected(RoomEvent.ParticipantMetadataChanged, metadata, participant);
		}).on(ParticipantEvent.ParticipantNameChanged, (name) => {
			this.emitWhenConnected(RoomEvent.ParticipantNameChanged, name, participant);
		}).on(ParticipantEvent.AttributesChanged, (changedAttributes) => {
			this.emitWhenConnected(RoomEvent.ParticipantAttributesChanged, changedAttributes, participant);
		}).on(ParticipantEvent.ConnectionQualityChanged, (quality) => {
			this.emitWhenConnected(RoomEvent.ConnectionQualityChanged, quality, participant);
		}).on(ParticipantEvent.ParticipantPermissionsChanged, (prevPermissions) => {
			this.emitWhenConnected(RoomEvent.ParticipantPermissionsChanged, prevPermissions, participant);
		}).on(ParticipantEvent.TrackSubscriptionStatusChanged, (pub, status) => {
			this.emitWhenConnected(RoomEvent.TrackSubscriptionStatusChanged, pub, status, participant);
		}).on(ParticipantEvent.TrackSubscriptionFailed, (trackSid, error) => {
			this.emit(RoomEvent.TrackSubscriptionFailed, trackSid, participant, error);
		}).on(ParticipantEvent.TrackSubscriptionPermissionChanged, (pub, status) => {
			this.emitWhenConnected(RoomEvent.TrackSubscriptionPermissionChanged, pub, status, participant);
		}).on(ParticipantEvent.Active, () => {
			this.emitWhenConnected(RoomEvent.ParticipantActive, participant);
			if (participant.kind === ParticipantInfo_Kind.AGENT) this.localParticipant.setActiveAgent(participant);
		});
		if (info) participant.updateInfo(info);
		return participant;
	}
	sendSyncState() {
		const remoteTracks = Array.from(this.remoteParticipants.values()).reduce((acc, participant) => {
			acc.push(...participant.getTrackPublications());
			return acc;
		}, []);
		const localTracks = this.localParticipant.getTrackPublications();
		const localDataTrackInfos = this.outgoingDataTrackManager.queryPublished();
		this.engine.sendSyncState(remoteTracks, localTracks, localDataTrackInfos);
	}
	/**
	* After resuming, we'll need to notify the server of the current
	* subscription settings.
	*/
	updateSubscriptions() {
		for (const p of this.remoteParticipants.values()) for (const pub of p.videoTrackPublications.values()) if (pub.isSubscribed && isRemotePub(pub)) pub.emitTrackUpdate();
	}
	getRemoteParticipantBySid(sid) {
		const identity = this.sidToIdentity.get(sid);
		if (identity) return this.remoteParticipants.get(identity);
	}
	/** The client capabilities this SDK advertises to other participants in its `ClientInfo`. */
	getClientInfoCapabilities(roomOptions) {
		var _a;
		const capabilities = [];
		if (isFrameMetadataSupported((_a = roomOptions.frameMetadata) !== null && _a !== void 0 ? _a : roomOptions.packetTrailer) || !!this.e2eeManager) capabilities.push(ClientInfo_Capability.CAP_PACKET_TRAILER);
		if (isCompressionStreamSupported()) capabilities.push(ClientInfo_Capability.CAP_COMPRESSION_DEFLATE_RAW);
		return capabilities;
	}
	registerRpcDataStreamHandler() {
		this.incomingDataStreamManager.registerTextStreamHandler(RPC_REQUEST_DATA_STREAM_TOPIC, (reader_1, _a) => __awaiter(this, [reader_1, _a], void 0, function(reader, _ref7) {
			var _this4 = this;
			let identity = _ref7.identity;
			return function* () {
				var _b;
				const attributes = (_b = reader.info.attributes) !== null && _b !== void 0 ? _b : {};
				yield _this4.rpcServerManager.handleIncomingDataStream(reader, identity, attributes);
			}();
		}));
		this.incomingDataStreamManager.registerTextStreamHandler(RPC_RESPONSE_DATA_STREAM_TOPIC, (reader_1, _a) => __awaiter(this, [reader_1, _a], void 0, function(reader, _ref8) {
			var _this5 = this;
			let identity = _ref8.identity;
			return function* () {
				var _b;
				const attributes = (_b = reader.info.attributes) !== null && _b !== void 0 ? _b : {};
				yield _this5.rpcClientManager.handleIncomingDataStream(reader, identity, attributes);
			}();
		}));
	}
	registerConnectionReconcile() {
		this.clearConnectionReconcile();
		let consecutiveFailures = 0;
		this.connectionReconcileInterval = CriticalTimers.setInterval(() => {
			if (!this.engine || this.engine.isClosed || !this.engine.verifyTransport()) {
				consecutiveFailures++;
				this.log.warn("detected connection state mismatch", {
					numFailures: consecutiveFailures,
					engine: this.engine ? {
						closed: this.engine.isClosed,
						transportsConnectedOrConnecting: this.engine.verifyTransport()
					} : void 0
				});
				if (consecutiveFailures >= 3) {
					this.clearConnectionReconcile();
					if (this.engine && !this.engine.isClosed) {
						this.log.warn("detected connection state mismatch, attempting full reconnect");
						this.engine.reconnect();
					} else {
						this.recreateEngine();
						this.handleDisconnect(this.options.stopLocalTrackOnUnpublish, DisconnectReason.STATE_MISMATCH);
					}
				}
			} else consecutiveFailures = 0;
		}, CONNECTION_RECONCILE_FREQUENCY_MS);
	}
	clearConnectionReconcile() {
		if (this.connectionReconcileInterval) CriticalTimers.clearInterval(this.connectionReconcileInterval);
	}
	setAndEmitConnectionState(state) {
		if (state === this.state) return false;
		this.log.info("connection state changed: ".concat(this.state, " -> ").concat(state));
		this.state = state;
		this.incomingDataStreamManager.setConnected(state === ConnectionState.Connected);
		this.emit(RoomEvent.ConnectionStateChanged, this.state);
		return true;
	}
	emitBufferedEvents() {
		this.bufferedEvents.forEach((_ref9) => {
			let _ref0 = _slicedToArray(_ref9, 2), ev = _ref0[0], args = _ref0[1];
			this.emit(ev, ...args);
		});
		this.bufferedEvents = [];
	}
	emitWhenConnected(event) {
		for (var _len2 = arguments.length, args = new Array(_len2 > 1 ? _len2 - 1 : 0), _key2 = 1; _key2 < _len2; _key2++) args[_key2 - 1] = arguments[_key2];
		if (this.state === ConnectionState.Reconnecting || this.isResuming || !this.engine || this.engine.pendingReconnect) this.bufferedEvents.push([event, args]);
		else if (this.state === ConnectionState.Connected) return this.emit(event, ...args);
		return false;
	}
	/**
	* Allows to populate a room with simulated participants.
	* No actual connection to a server will be established, all state is
	* @experimental
	*/
	simulateParticipants(options) {
		return __awaiter(this, void 0, void 0, function* () {
			var _a, _b, _c, _d;
			const publishOptions = Object.assign({
				audio: true,
				video: true,
				useRealTracks: false
			}, options.publish);
			const participantOptions = Object.assign({
				count: 9,
				audio: false,
				video: true,
				aspectRatios: [
					1.66,
					1.7,
					1.3
				]
			}, options.participants);
			this.handleDisconnect();
			this.roomInfo = new Room$1({
				sid: "RM_SIMULATED",
				name: "simulated-room",
				emptyTimeout: 0,
				maxParticipants: 0,
				creationTime: protoInt64.parse((/* @__PURE__ */ new Date()).getTime()),
				metadata: "",
				numParticipants: 1,
				numPublishers: 1,
				turnPassword: "",
				enabledCodecs: [],
				activeRecording: false
			});
			this.localParticipant.updateInfo(new ParticipantInfo({
				identity: "simulated-local",
				name: "local-name"
			}));
			this.setupLocalParticipantEvents();
			this.emit(RoomEvent.SignalConnected);
			this.emit(RoomEvent.Connected);
			this.setAndEmitConnectionState(ConnectionState.Connected);
			if (publishOptions.video) {
				const camPub = new LocalTrackPublication(Track.Kind.Video, new TrackInfo({
					source: TrackSource.CAMERA,
					sid: Math.floor(Math.random() * 1e4).toString(),
					type: TrackType.AUDIO,
					name: "video-dummy"
				}), new LocalVideoTrack(publishOptions.useRealTracks && ((_a = window.navigator.mediaDevices) === null || _a === void 0 ? void 0 : _a.getUserMedia) ? (yield window.navigator.mediaDevices.getUserMedia({ video: true })).getVideoTracks()[0] : createDummyVideoStreamTrack(160 * ((_b = participantOptions.aspectRatios[0]) !== null && _b !== void 0 ? _b : 1), 160, true, true), void 0, false, {
					loggerName: this.options.loggerName,
					loggerContextCb: () => this.logContext
				}), {
					loggerName: this.options.loggerName,
					loggerContextCb: () => this.logContext
				});
				this.localParticipant.addTrackPublication(camPub);
				this.localParticipant.emit(ParticipantEvent.LocalTrackPublished, camPub);
			}
			if (publishOptions.audio) {
				const audioPub = new LocalTrackPublication(Track.Kind.Audio, new TrackInfo({
					source: TrackSource.MICROPHONE,
					sid: Math.floor(Math.random() * 1e4).toString(),
					type: TrackType.AUDIO
				}), new LocalAudioTrack(publishOptions.useRealTracks && ((_c = navigator.mediaDevices) === null || _c === void 0 ? void 0 : _c.getUserMedia) ? (yield navigator.mediaDevices.getUserMedia({ audio: true })).getAudioTracks()[0] : getEmptyAudioStreamTrack(), void 0, false, this.audioContext, {
					loggerName: this.options.loggerName,
					loggerContextCb: () => this.logContext
				}), {
					loggerName: this.options.loggerName,
					loggerContextCb: () => this.logContext
				});
				this.localParticipant.addTrackPublication(audioPub);
				this.localParticipant.emit(ParticipantEvent.LocalTrackPublished, audioPub);
			}
			for (let i = 0; i < participantOptions.count - 1; i += 1) {
				let info = new ParticipantInfo({
					sid: Math.floor(Math.random() * 1e4).toString(),
					identity: "simulated-".concat(i),
					state: ParticipantInfo_State.ACTIVE,
					tracks: [],
					joinedAt: protoInt64.parse(Date.now())
				});
				const p = this.getOrCreateParticipant(info.identity, info);
				if (participantOptions.video) {
					const dummyVideo = createDummyVideoStreamTrack(160 * ((_d = participantOptions.aspectRatios[i % participantOptions.aspectRatios.length]) !== null && _d !== void 0 ? _d : 1), 160, false, true);
					const videoTrack = new TrackInfo({
						source: TrackSource.CAMERA,
						sid: Math.floor(Math.random() * 1e4).toString(),
						type: TrackType.AUDIO
					});
					p.addSubscribedMediaTrack(dummyVideo, videoTrack.sid, new MediaStream([dummyVideo]), new RTCRtpReceiver());
					info.tracks = [...info.tracks, videoTrack];
				}
				if (participantOptions.audio) {
					const dummyTrack = getEmptyAudioStreamTrack();
					const audioTrack = new TrackInfo({
						source: TrackSource.MICROPHONE,
						sid: Math.floor(Math.random() * 1e4).toString(),
						type: TrackType.AUDIO
					});
					p.addSubscribedMediaTrack(dummyTrack, audioTrack.sid, new MediaStream([dummyTrack]), new RTCRtpReceiver());
					info.tracks = [...info.tracks, audioTrack];
				}
				p.updateInfo(info);
			}
		});
	}
	emit(event) {
		for (var _len3 = arguments.length, args = new Array(_len3 > 1 ? _len3 - 1 : 0), _key3 = 1; _key3 < _len3; _key3++) args[_key3 - 1] = arguments[_key3];
		if (event !== RoomEvent.ActiveSpeakersChanged && event !== RoomEvent.TranscriptionReceived) {
			const minimizedArgs = mapArgs(args).filter((arg) => arg !== void 0);
			if (event === RoomEvent.TrackSubscribed || event === RoomEvent.TrackUnsubscribed) this.log.trace("subscribe trace: ".concat(event), {
				event,
				args: minimizedArgs
			});
			this.log.debug("room event ".concat(event), {
				event,
				args: minimizedArgs
			});
		}
		return super.emit(event, ...args);
	}
};
Room.cleanupRegistry = typeof FinalizationRegistry !== "undefined" && typeof WeakRef !== "undefined" && new FinalizationRegistry((cleanup) => {
	cleanup();
});
function mapArgs(args) {
	return args.map((arg) => {
		if (!arg) return;
		if (Array.isArray(arg)) return mapArgs(arg);
		if (typeof arg === "object") return "logContext" in arg ? arg.logContext : void 0;
		return arg;
	});
}
var CheckStatus;
(function(CheckStatus) {
	CheckStatus[CheckStatus["IDLE"] = 0] = "IDLE";
	CheckStatus[CheckStatus["RUNNING"] = 1] = "RUNNING";
	CheckStatus[CheckStatus["SKIPPED"] = 2] = "SKIPPED";
	CheckStatus[CheckStatus["SUCCESS"] = 3] = "SUCCESS";
	CheckStatus[CheckStatus["FAILED"] = 4] = "FAILED";
})(CheckStatus || (CheckStatus = {}));
eventsExports.EventEmitter;
eventsExports.EventEmitter;
/** A Configurable TokenSource is a token source that takes a
* {@link TokenSourceFetchOptions} object as input and returns a deterministic
* {@link TokenSourceResponseObject} output based on the options specified.
*
* For example, if options.participantName is set, it should be expected that
* all tokens that are generated will have participant name field set to the
* provided value.
*
* A few common downstream implementers are {@link TokenSourceEndpoint}
* and {@link TokenSourceCustom}.
*/
var TokenSourceConfigurable = class {};
new TextEncoder();
var decoder = new TextDecoder();
function decodeBase64(encoded) {
	if (Uint8Array.fromBase64) return Uint8Array.fromBase64(encoded);
	const binary = atob(encoded);
	const bytes = new Uint8Array(binary.length);
	for (let i = 0; i < binary.length; i++) bytes[i] = binary.charCodeAt(i);
	return bytes;
}
function decode(input) {
	if (Uint8Array.fromBase64) return Uint8Array.fromBase64(typeof input === "string" ? input : decoder.decode(input), { alphabet: "base64url" });
	let encoded = input;
	if (encoded instanceof Uint8Array) encoded = decoder.decode(encoded);
	encoded = encoded.replace(/-/g, "+").replace(/_/g, "/");
	try {
		return decodeBase64(encoded);
	} catch (_unused) {
		throw new TypeError("The input to be decoded is not correctly encoded.");
	}
}
var JOSEError = class extends Error {
	constructor(message, options) {
		var _Error$captureStackTr;
		super(message, options);
		_defineProperty(this, "code", "ERR_JOSE_GENERIC");
		this.name = this.constructor.name;
		(_Error$captureStackTr = Error.captureStackTrace) === null || _Error$captureStackTr === void 0 || _Error$captureStackTr.call(Error, this, this.constructor);
	}
};
_defineProperty(JOSEError, "code", "ERR_JOSE_GENERIC");
var JWTClaimValidationFailed = class extends JOSEError {
	constructor(message, payload) {
		let claim = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : "unspecified";
		let reason = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : "unspecified";
		super(message, { cause: {
			claim,
			reason,
			payload
		} });
		_defineProperty(this, "code", "ERR_JWT_CLAIM_VALIDATION_FAILED");
		_defineProperty(this, "claim", void 0);
		_defineProperty(this, "reason", void 0);
		_defineProperty(this, "payload", void 0);
		this.claim = claim;
		this.reason = reason;
		this.payload = payload;
	}
};
_defineProperty(JWTClaimValidationFailed, "code", "ERR_JWT_CLAIM_VALIDATION_FAILED");
var JWTExpired = class extends JOSEError {
	constructor(message, payload) {
		let claim = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : "unspecified";
		let reason = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : "unspecified";
		super(message, { cause: {
			claim,
			reason,
			payload
		} });
		_defineProperty(this, "code", "ERR_JWT_EXPIRED");
		_defineProperty(this, "claim", void 0);
		_defineProperty(this, "reason", void 0);
		_defineProperty(this, "payload", void 0);
		this.claim = claim;
		this.reason = reason;
		this.payload = payload;
	}
};
_defineProperty(JWTExpired, "code", "ERR_JWT_EXPIRED");
var JOSEAlgNotAllowed = class extends JOSEError {
	constructor() {
		super(...arguments);
		_defineProperty(this, "code", "ERR_JOSE_ALG_NOT_ALLOWED");
	}
};
_defineProperty(JOSEAlgNotAllowed, "code", "ERR_JOSE_ALG_NOT_ALLOWED");
var JOSENotSupported = class extends JOSEError {
	constructor() {
		super(...arguments);
		_defineProperty(this, "code", "ERR_JOSE_NOT_SUPPORTED");
	}
};
_defineProperty(JOSENotSupported, "code", "ERR_JOSE_NOT_SUPPORTED");
var JWEDecryptionFailed = class extends JOSEError {
	constructor() {
		let message = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "decryption operation failed";
		let options = arguments.length > 1 ? arguments[1] : void 0;
		super(message, options);
		_defineProperty(this, "code", "ERR_JWE_DECRYPTION_FAILED");
	}
};
_defineProperty(JWEDecryptionFailed, "code", "ERR_JWE_DECRYPTION_FAILED");
var JWEInvalid = class extends JOSEError {
	constructor() {
		super(...arguments);
		_defineProperty(this, "code", "ERR_JWE_INVALID");
	}
};
_defineProperty(JWEInvalid, "code", "ERR_JWE_INVALID");
var JWSInvalid = class extends JOSEError {
	constructor() {
		super(...arguments);
		_defineProperty(this, "code", "ERR_JWS_INVALID");
	}
};
_defineProperty(JWSInvalid, "code", "ERR_JWS_INVALID");
var JWTInvalid = class extends JOSEError {
	constructor() {
		super(...arguments);
		_defineProperty(this, "code", "ERR_JWT_INVALID");
	}
};
_defineProperty(JWTInvalid, "code", "ERR_JWT_INVALID");
var JWKInvalid = class extends JOSEError {
	constructor() {
		super(...arguments);
		_defineProperty(this, "code", "ERR_JWK_INVALID");
	}
};
_defineProperty(JWKInvalid, "code", "ERR_JWK_INVALID");
var JWKSInvalid = class extends JOSEError {
	constructor() {
		super(...arguments);
		_defineProperty(this, "code", "ERR_JWKS_INVALID");
	}
};
_defineProperty(JWKSInvalid, "code", "ERR_JWKS_INVALID");
var JWKSNoMatchingKey = class extends JOSEError {
	constructor() {
		let message = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "no applicable key found in the JSON Web Key Set";
		let options = arguments.length > 1 ? arguments[1] : void 0;
		super(message, options);
		_defineProperty(this, "code", "ERR_JWKS_NO_MATCHING_KEY");
	}
};
_defineProperty(JWKSNoMatchingKey, "code", "ERR_JWKS_NO_MATCHING_KEY");
var JWKSMultipleMatchingKeys = class extends JOSEError {
	constructor() {
		let message = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "multiple matching keys found in the JSON Web Key Set";
		let options = arguments.length > 1 ? arguments[1] : void 0;
		super(message, options);
		_defineProperty(this, Symbol.asyncIterator, void 0);
		_defineProperty(this, "code", "ERR_JWKS_MULTIPLE_MATCHING_KEYS");
	}
};
_defineProperty(JWKSMultipleMatchingKeys, "code", "ERR_JWKS_MULTIPLE_MATCHING_KEYS");
var JWKSTimeout = class extends JOSEError {
	constructor() {
		let message = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "request timed out";
		let options = arguments.length > 1 ? arguments[1] : void 0;
		super(message, options);
		_defineProperty(this, "code", "ERR_JWKS_TIMEOUT");
	}
};
_defineProperty(JWKSTimeout, "code", "ERR_JWKS_TIMEOUT");
var JWSSignatureVerificationFailed = class extends JOSEError {
	constructor() {
		let message = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "signature verification failed";
		let options = arguments.length > 1 ? arguments[1] : void 0;
		super(message, options);
		_defineProperty(this, "code", "ERR_JWS_SIGNATURE_VERIFICATION_FAILED");
	}
};
_defineProperty(JWSSignatureVerificationFailed, "code", "ERR_JWS_SIGNATURE_VERIFICATION_FAILED");
var isObjectLike = (value) => typeof value === "object" && value !== null;
function isObject(input) {
	if (!isObjectLike(input) || Object.prototype.toString.call(input) !== "[object Object]") return false;
	if (Object.getPrototypeOf(input) === null) return true;
	let proto = input;
	while (Object.getPrototypeOf(proto) !== null) proto = Object.getPrototypeOf(proto);
	return Object.getPrototypeOf(input) === proto;
}
function decodeJwt(jwt) {
	if (typeof jwt !== "string") throw new JWTInvalid("JWTs must use Compact JWS serialization, JWT must be a string");
	const _jwt$split = jwt.split("."), payload = _jwt$split[1], length = _jwt$split.length;
	if (length === 5) throw new JWTInvalid("Only JWTs using Compact JWS serialization can be decoded");
	if (length !== 3) throw new JWTInvalid("Invalid JWT");
	if (!payload) throw new JWTInvalid("JWTs must contain a payload");
	let decoded;
	try {
		decoded = decode(payload);
	} catch (_unused) {
		throw new JWTInvalid("Failed to base64url decode the payload");
	}
	let result;
	try {
		result = JSON.parse(decoder.decode(decoded));
	} catch (_unused2) {
		throw new JWTInvalid("Failed to parse the decoded payload as JSON");
	}
	if (!isObject(result)) throw new JWTInvalid("Invalid JWT Claims Set");
	return result;
}
/** Given a LiveKit generated participant token, decodes and returns the associated {@link TokenPayload} data. */
function decodeTokenPayload(token) {
	const payload = decodeJwt(token);
	payload.roomConfig;
	const rest = __rest(payload, ["roomConfig"]);
	return Object.assign(Object.assign({}, rest), { roomConfig: payload.roomConfig ? RoomConfiguration.fromJson(payload.roomConfig, { ignoreUnknownFields: true }) : void 0 });
}
function facingModeFromLocalTrack(localTrack) {
	let options = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
	var _a;
	const track = isLocalTrack(localTrack) ? localTrack.mediaStreamTrack : localTrack;
	const trackSettings = track.getSettings();
	let result = {
		facingMode: (_a = options.defaultFacingMode) !== null && _a !== void 0 ? _a : "user",
		confidence: "low"
	};
	if ("facingMode" in trackSettings) {
		const rawFacingMode = trackSettings.facingMode;
		livekitLogger.trace("rawFacingMode", { rawFacingMode });
		if (rawFacingMode && typeof rawFacingMode === "string" && isFacingModeValue(rawFacingMode)) result = {
			facingMode: rawFacingMode,
			confidence: "high"
		};
	}
	if (["low", "medium"].includes(result.confidence)) {
		livekitLogger.trace("Try to get facing mode from device label: (".concat(track.label, ")"));
		const labelAnalysisResult = facingModeFromDeviceLabel(track.label);
		if (labelAnalysisResult !== void 0) result = labelAnalysisResult;
	}
	return result;
}
var knownDeviceLabels = /* @__PURE__ */ new Map([["obs virtual camera", {
	facingMode: "environment",
	confidence: "medium"
}]]);
var knownDeviceLabelSections = /* @__PURE__ */ new Map([["iphone", {
	facingMode: "environment",
	confidence: "medium"
}], ["ipad", {
	facingMode: "environment",
	confidence: "medium"
}]]);
/**
* Attempt to analyze the device label to determine the facing mode.
*
* @experimental
*/
function facingModeFromDeviceLabel(deviceLabel) {
	var _a;
	const label = deviceLabel.trim().toLowerCase();
	if (label === "") return;
	if (knownDeviceLabels.has(label)) return knownDeviceLabels.get(label);
	return (_a = Array.from(knownDeviceLabelSections.entries()).find((_ref) => {
		let section = _slicedToArray(_ref, 1)[0];
		return label.includes(section);
	})) === null || _a === void 0 ? void 0 : _a[1];
}
function isFacingModeValue(item) {
	return item === void 0 || [
		"user",
		"environment",
		"left",
		"right"
	].includes(item);
}
var SerializerSymbol = Symbol.for("lk.serializer");
function isSerializer(v) {
	return typeof v === "object" && v !== null && "symbol" in v && v.symbol === SerializerSymbol;
}
/** @internal */
function base(params) {
	return Object.assign(Object.assign({}, params), { symbol: SerializerSymbol });
}
/**
* JSON serializer — `JSON.parse` on the way in, `JSON.stringify` on the way out.
* Defaults to `any` so individual handlers can annotate their own payload types.
*/
function json() {
	return base({
		parse: (rawString) => JSON.parse(rawString),
		serialize: (val) => JSON.stringify(val)
	});
}
/** Raw string serializer — passes payloads through as plain strings with no encoding. */
function raw() {
	return base({
		parse: (rawString) => rawString,
		serialize: (val) => val
	});
}
/** Custom serializer - allows custom defined parse and serialize functions */
function custom(params) {
	return base(params);
}
/**
* Serializer helpers for message payload encoding.
*
* @example
* ```ts
* const a = serializers.raw(); // Serializer<string, string>
* const b = serializer.json<{ foo: string }, { bar: string }>(); // Serializer<{ foo: string }, { bar: string }>
* ```
*
* @beta
*/
var serializers = {
	json,
	raw,
	custom
};
//#endregion
//#region node_modules/@livekit/components-react/dist/room-Bfb4OWAI.mjs
var Pi = (e) => {
	const t = import_react.useRef(e);
	return import_react.useEffect(() => {
		t.current = e;
	}), t;
};
function Mi(e, t) {
	const n = Li(), r = Pi(t);
	return import_react.useLayoutEffect(() => {
		let i = !1;
		const o = e.current;
		if (!o) return;
		function s(a, c) {
			i || r.current(a, c);
		}
		return n?.subscribe(o, s), () => {
			i = !0, n?.unsubscribe(o, s);
		};
	}, [
		e.current,
		n,
		r
	]), n?.observer;
}
function Ai() {
	let e = !1, t = [];
	const n = /* @__PURE__ */ new Map();
	if (typeof window > "u") return;
	const r = new ResizeObserver((i, o) => {
		t = t.concat(i), e || window.requestAnimationFrame(() => {
			const s = /* @__PURE__ */ new Set();
			for (let a = 0; a < t.length; a++) s.has(t[a].target) || (s.add(t[a].target), n.get(t[a].target)?.forEach((c) => c(t[a], o)));
			t = [], e = !1;
		}), e = !0;
	});
	return {
		observer: r,
		subscribe(i, o) {
			r.observe(i);
			const s = n.get(i) ?? [];
			s.push(o), n.set(i, s);
		},
		unsubscribe(i, o) {
			const s = n.get(i) ?? [];
			if (s.length === 1) {
				r.unobserve(i), n.delete(i);
				return;
			}
			const a = s.indexOf(o);
			a !== -1 && s.splice(a, 1), n.set(i, s);
		}
	};
}
var Nt;
var Li = () => Nt || (Nt = Ai());
var Oi = (e) => {
	const [t, n] = import_react.useState({
		width: 0,
		height: 0
	});
	return import_react.useLayoutEffect(() => {
		if (e.current) {
			const { width: r, height: i } = e.current.getBoundingClientRect();
			n({
				width: r,
				height: i
			});
		}
	}, [e.current]), Mi(e, import_react.useCallback((r) => n(r.contentRect), [])), t;
};
function j$1(e, t, n = !0) {
	const [r, i] = import_react.useState(t);
	return import_react.useEffect(() => {
		if (n && i(t), typeof window > "u" || !e) return;
		const o = e.subscribe(i);
		return () => o.unsubscribe();
	}, [e, n]), r;
}
var lt = Math.min;
var Ae$1 = Math.max;
var ft = Math.round;
var rt = Math.floor;
var fe = (e) => ({
	x: e,
	y: e
});
var _i = {
	left: "right",
	right: "left",
	bottom: "top",
	top: "bottom"
};
function Ii(e, t, n) {
	return Ae$1(e, lt(t, n));
}
function Et(e, t) {
	return typeof e == "function" ? e(t) : e;
}
function We(e) {
	return e.split("-")[0];
}
function Ct(e) {
	return e.split("-")[1];
}
function Zn(e) {
	return e === "x" ? "y" : "x";
}
function er(e) {
	return e === "y" ? "height" : "width";
}
function Ee$1(e) {
	const t = e[0];
	return t === "t" || t === "b" ? "y" : "x";
}
function tr(e) {
	return Zn(Ee$1(e));
}
function Ri(e, t, n) {
	n === void 0 && (n = !1);
	const r = Ct(e), i = tr(e), o = er(i);
	let s = i === "x" ? r === (n ? "end" : "start") ? "right" : "left" : r === "start" ? "bottom" : "top";
	return t.reference[o] > t.floating[o] && (s = dt(s)), [s, dt(s)];
}
function Di(e) {
	const t = dt(e);
	return [
		qt(e),
		t,
		qt(t)
	];
}
function qt(e) {
	return e.includes("start") ? e.replace("start", "end") : e.replace("end", "start");
}
var Sn = ["left", "right"];
var En = ["right", "left"];
var Ni = ["top", "bottom"];
var Fi = ["bottom", "top"];
function Ui(e, t, n) {
	switch (e) {
		case "top":
		case "bottom": return n ? t ? En : Sn : t ? Sn : En;
		case "left":
		case "right": return t ? Ni : Fi;
		default: return [];
	}
}
function $i(e, t, n, r) {
	const i = Ct(e);
	let o = Ui(We(e), n === "start", r);
	return i && (o = o.map((s) => s + "-" + i), t && (o = o.concat(o.map(qt)))), o;
}
function dt(e) {
	const t = We(e);
	return _i[t] + e.slice(t.length);
}
function Wi(e) {
	var t, n, r, i;
	return {
		top: (t = e.top) != null ? t : 0,
		right: (n = e.right) != null ? n : 0,
		bottom: (r = e.bottom) != null ? r : 0,
		left: (i = e.left) != null ? i : 0
	};
}
function ji(e) {
	return typeof e != "number" ? Wi(e) : {
		top: e,
		right: e,
		bottom: e,
		left: e
	};
}
function pt(e) {
	const { x: t, y: n, width: r, height: i } = e;
	return {
		width: r,
		height: i,
		top: n,
		left: t,
		right: t + r,
		bottom: n + i,
		x: t,
		y: n
	};
}
function Cn(e, t, n) {
	let { reference: r, floating: i } = e;
	const o = Ee$1(t), s = tr(t), a = er(s), c = We(t), u = o === "y", f = r.x + r.width / 2 - i.width / 2, d = r.y + r.height / 2 - i.height / 2, p = r[a] / 2 - i[a] / 2;
	let h;
	switch (c) {
		case "top":
			h = {
				x: f,
				y: r.y - i.height
			};
			break;
		case "bottom":
			h = {
				x: f,
				y: r.y + r.height
			};
			break;
		case "right":
			h = {
				x: r.x + r.width,
				y: d
			};
			break;
		case "left":
			h = {
				x: r.x - i.width,
				y: d
			};
			break;
		default: h = {
			x: r.x,
			y: r.y
		};
	}
	const m = Ct(t);
	return m && (h[s] += p * (m === "end" ? 1 : -1) * (n && u ? -1 : 1)), h;
}
async function Bi(e, t) {
	var n;
	t === void 0 && (t = {});
	const { x: r, y: i, platform: o, rects: s, elements: a, strategy: c } = e, { boundary: u = "clippingAncestors", rootBoundary: f = "viewport", elementContext: d = "floating", altBoundary: p = !1, padding: h = 0 } = Et(t, e), m = ji(h), S = a[p ? d === "floating" ? "reference" : "floating" : d], y = pt(await o.getClippingRect({
		element: (n = await (o.isElement == null ? void 0 : o.isElement(S))) == null || n ? S : S.contextElement || await (o.getDocumentElement == null ? void 0 : o.getDocumentElement(a.floating)),
		boundary: u,
		rootBoundary: f,
		strategy: c
	})), b = d === "floating" ? {
		x: r,
		y: i,
		width: s.floating.width,
		height: s.floating.height
	} : s.reference, w = await (o.getOffsetParent == null ? void 0 : o.getOffsetParent(a.floating)), P = await (o.isElement == null ? void 0 : o.isElement(w)) && await (o.getScale == null ? void 0 : o.getScale(w)) || {
		x: 1,
		y: 1
	}, N = pt(o.convertOffsetParentRelativeRectToViewportRelativeRect ? await o.convertOffsetParentRelativeRectToViewportRelativeRect({
		elements: a,
		rect: b,
		offsetParent: w,
		strategy: c
	}) : b);
	return {
		top: (y.top - N.top + m.top) / P.y,
		bottom: (N.bottom - y.bottom + m.bottom) / P.y,
		left: (y.left - N.left + m.left) / P.x,
		right: (N.right - y.right + m.right) / P.x
	};
}
var Vi = 50;
var Hi = async (e, t, n) => {
	const { placement: r = "bottom", strategy: i = "absolute", middleware: o = [], platform: s } = n, a = s.detectOverflow ? s : {
		...s,
		detectOverflow: Bi
	}, c = await (s.isRTL == null ? void 0 : s.isRTL(t));
	let u = await s.getElementRects({
		reference: e,
		floating: t,
		strategy: i
	}), { x: f, y: d } = Cn(u, r, c), p = r, h = 0;
	const m = {};
	for (let S = 0; S < o.length; S++) {
		const y = o[S];
		if (!y) continue;
		const { name: b, fn: w } = y, { x: P, y: N, data: M, reset: v } = await w({
			x: f,
			y: d,
			initialPlacement: r,
			placement: p,
			strategy: i,
			middlewareData: m,
			rects: u,
			platform: a,
			elements: {
				reference: e,
				floating: t
			}
		});
		f = P ?? f, d = N ?? d, m[b] = {
			...m[b],
			...M
		}, v && h < Vi && (h++, typeof v == "object" && (v.placement && (p = v.placement), v.rects && (u = v.rects === !0 ? await s.getElementRects({
			reference: e,
			floating: t,
			strategy: i
		}) : v.rects), {x: f, y: d} = Cn(u, p, c)), S = -1);
	}
	return {
		x: f,
		y: d,
		placement: p,
		strategy: i,
		middlewareData: m
	};
};
var zi = function(e) {
	return e === void 0 && (e = {}), {
		name: "flip",
		options: e,
		async fn(t) {
			var n, r;
			const { placement: i, middlewareData: o, rects: s, initialPlacement: a, platform: c, elements: u } = t, { mainAxis: f = !0, crossAxis: d = !0, fallbackPlacements: p, fallbackStrategy: h = "bestFit", fallbackAxisSideDirection: m = "none", flipAlignment: S = !0, ...y } = Et(e, t);
			if ((n = o.arrow) != null && n.alignmentOffset) return {};
			const b = We(i), w = Ee$1(a), P = We(a) === a, N = await (c.isRTL == null ? void 0 : c.isRTL(u.floating)), M = p || (P || !S ? [dt(a)] : Di(a)), v = m !== "none";
			!p && v && M.push(...$i(a, S, m, N));
			const g = [a, ...M], E = await c.detectOverflow(t, y), x = [];
			let C = ((r = o.flip) == null ? void 0 : r.overflows) || [];
			if (f && x.push(E[b]), d) {
				const D = Ri(i, s, N);
				x.push(E[D[0]], E[D[1]]);
			}
			if (C = [...C, {
				placement: i,
				overflows: x
			}], !x.every((D) => D <= 0)) {
				var I, T;
				const D = (((I = o.flip) == null ? void 0 : I.index) || 0) + 1, U = g[D];
				if (U && (!(d === "alignment" && w !== Ee$1(U)) || C.every((z) => Ee$1(z.placement) === w ? z.overflows[0] > 0 : !0))) return {
					data: {
						index: D,
						overflows: C
					},
					reset: { placement: U }
				};
				let H = (T = C.filter((z) => z.overflows[0] <= 0).sort((z, V) => z.overflows[1] - V.overflows[1])[0]) == null ? void 0 : T.placement;
				if (!H) switch (h) {
					case "bestFit": {
						var A;
						const z = (A = C.filter((V) => {
							if (v) {
								const Q = Ee$1(V.placement);
								return Q === w || Q === "y";
							}
							return !0;
						}).map((V) => [V.placement, V.overflows.filter((Q) => Q > 0).reduce((Q, xe) => Q + xe, 0)]).sort((V, Q) => V[1] - Q[1])[0]) == null ? void 0 : A[0];
						z && (H = z);
						break;
					}
					case "initialPlacement":
						H = a;
						break;
				}
				if (i !== H) return { reset: { placement: H } };
			}
			return {};
		}
	};
};
var qi = /* @__PURE__ */ new Set(["left", "top"]);
async function Ki(e, t) {
	const { placement: n, platform: r, elements: i } = e, o = await (r.isRTL == null ? void 0 : r.isRTL(i.floating)), s = We(n), a = Ct(n), c = Ee$1(n) === "y", u = qi.has(s) ? -1 : 1, f = o && c ? -1 : 1, d = Et(t, e);
	let { mainAxis: p, crossAxis: h, alignmentAxis: m } = typeof d == "number" ? {
		mainAxis: d,
		crossAxis: 0,
		alignmentAxis: null
	} : {
		mainAxis: d.mainAxis || 0,
		crossAxis: d.crossAxis || 0,
		alignmentAxis: d.alignmentAxis
	};
	return a && typeof m == "number" && (h = a === "end" ? m * -1 : m), c ? {
		x: h * f,
		y: p * u
	} : {
		x: p * u,
		y: h * f
	};
}
var Gi = function(e) {
	return e === void 0 && (e = 0), {
		name: "offset",
		options: e,
		async fn(t) {
			var n, r;
			const { x: i, y: o, placement: s, middlewareData: a } = t, c = await Ki(t, e);
			return s === ((n = a.offset) == null ? void 0 : n.placement) && (r = a.arrow) != null && r.alignmentOffset ? {} : {
				x: i + c.x,
				y: o + c.y,
				data: {
					...c,
					placement: s
				}
			};
		}
	};
};
var Ji = function(e) {
	return e === void 0 && (e = {}), {
		name: "shift",
		options: e,
		async fn(t) {
			const { x: n, y: r, placement: i, platform: o } = t, { mainAxis: s = !0, crossAxis: a = !1, limiter: c = { fn: (w) => {
				let { x: P, y: N } = w;
				return {
					x: P,
					y: N
				};
			} }, ...u } = Et(e, t), f = {
				x: n,
				y: r
			}, d = await o.detectOverflow(t, u), p = Ee$1(i), h = Zn(p);
			let m = f[h], S = f[p];
			const y = (w, P) => Ii(P + d[w === "y" ? "top" : "left"], P, P - d[w === "y" ? "bottom" : "right"]);
			s && (m = y(h, m)), a && (S = y(p, S));
			const b = c.fn({
				...t,
				[h]: m,
				[p]: S
			});
			return {
				...b,
				data: {
					x: b.x - n,
					y: b.y - r,
					enabled: {
						[h]: s,
						[p]: a
					}
				}
			};
		}
	};
};
function Tt() {
	return typeof window < "u";
}
function He(e) {
	return nr(e) ? (e.nodeName || "").toLowerCase() : "#document";
}
function ie$1(e) {
	var t;
	return (e == null || (t = e.ownerDocument) == null ? void 0 : t.defaultView) || window;
}
function de(e) {
	var t;
	return (t = (nr(e) ? e.ownerDocument : e.document) || window.document) == null ? void 0 : t.documentElement;
}
function nr(e) {
	return Tt() ? e instanceof Node || e instanceof ie$1(e).Node : !1;
}
function ue(e) {
	return Tt() ? e instanceof Element || e instanceof ie$1(e).Element : !1;
}
function ye(e) {
	return Tt() ? e instanceof HTMLElement || e instanceof ie$1(e).HTMLElement : !1;
}
function Tn(e) {
	return !Tt() || typeof ShadowRoot > "u" ? !1 : e instanceof ShadowRoot || e instanceof ie$1(e).ShadowRoot;
}
function Ze(e) {
	const { overflow: t, overflowX: n, overflowY: r, display: i } = le$1(e);
	return /auto|scroll|overlay|hidden|clip/.test(t + r + n) && i !== "inline" && i !== "contents";
}
function Yi(e) {
	return /^(table|td|th)$/.test(He(e));
}
function xt(e) {
	try {
		if (e.matches(":popover-open")) return !0;
	} catch {}
	try {
		return e.matches(":modal");
	} catch {
		return !1;
	}
}
var Qi = /transform|translate|scale|rotate|perspective|filter/;
var Xi = /paint|layout|strict|content/;
var ke$1 = (e) => !!e && e !== "none";
var Ft;
function nn(e) {
	const t = ue(e) ? le$1(e) : e;
	return ke$1(t.transform) || ke$1(t.translate) || ke$1(t.scale) || ke$1(t.rotate) || ke$1(t.perspective) || !rn() && (ke$1(t.backdropFilter) || ke$1(t.filter)) || Qi.test(t.willChange || "") || Xi.test(t.contain || "");
}
function Zi(e) {
	let t = Ce(e);
	for (; ye(t) && !je(t);) {
		if (nn(t)) return t;
		if (xt(t)) return null;
		t = Ce(t);
	}
	return null;
}
function rn() {
	return Ft ??= typeof CSS < "u" && CSS.supports && CSS.supports("-webkit-backdrop-filter", "none"), Ft;
}
function je(e) {
	return /^(html|body|#document)$/.test(He(e));
}
function le$1(e) {
	return ie$1(e).getComputedStyle(e);
}
function kt(e) {
	return ue(e) ? {
		scrollLeft: e.scrollLeft,
		scrollTop: e.scrollTop
	} : {
		scrollLeft: e.scrollX,
		scrollTop: e.scrollY
	};
}
function Ce(e) {
	if (He(e) === "html") return e;
	const t = e.assignedSlot || e.parentNode || Tn(e) && e.host || de(e);
	return Tn(t) ? t.host : t;
}
function rr(e) {
	const t = Ce(e);
	return je(t) ? (e.ownerDocument || e).body : ye(t) && Ze(t) ? t : rr(t);
}
function Ye(e, t, n) {
	var r;
	t === void 0 && (t = []), n === void 0 && (n = !0);
	const i = rr(e), o = i === ((r = e.ownerDocument) == null ? void 0 : r.body), s = ie$1(i);
	if (o) {
		const a = Kt$1(s);
		return t.concat(s, s.visualViewport || [], Ze(i) ? i : [], a && n ? Ye(a) : []);
	} else return t.concat(i, Ye(i, [], n));
}
function Kt$1(e) {
	return e.parent && Object.getPrototypeOf(e.parent) ? e.frameElement : null;
}
function ir(e) {
	const t = le$1(e);
	let n = parseFloat(t.width) || 0, r = parseFloat(t.height) || 0;
	const i = ye(e), o = i ? e.offsetWidth : n, s = i ? e.offsetHeight : r, a = ft(n) !== o || ft(r) !== s;
	return a && (n = o, r = s), {
		width: n,
		height: r,
		$: a
	};
}
function on(e) {
	return ue(e) ? e : e.contextElement;
}
function Fe$1(e) {
	const t = on(e);
	if (!ye(t)) return fe(1);
	const n = t.getBoundingClientRect(), { width: r, height: i, $: o } = ir(t);
	let s = (o ? ft(n.width) : n.width) / r, a = (o ? ft(n.height) : n.height) / i;
	return (!s || !Number.isFinite(s)) && (s = 1), (!a || !Number.isFinite(a)) && (a = 1), {
		x: s,
		y: a
	};
}
var eo = /* @__PURE__ */ fe(0);
function or(e) {
	const t = ie$1(e);
	return !rn() || !t.visualViewport ? eo : {
		x: t.visualViewport.offsetLeft,
		y: t.visualViewport.offsetTop
	};
}
function to(e, t, n) {
	return t === void 0 && (t = !1), !n || t && n !== ie$1(e) ? !1 : t;
}
function Le$1(e, t, n, r) {
	t === void 0 && (t = !1), n === void 0 && (n = !1);
	const i = e.getBoundingClientRect(), o = on(e);
	let s = fe(1);
	t && (r ? ue(r) && (s = Fe$1(r)) : s = Fe$1(e));
	const a = to(o, n, r) ? or(o) : fe(0);
	let c = (i.left + a.x) / s.x, u = (i.top + a.y) / s.y, f = i.width / s.x, d = i.height / s.y;
	if (o) {
		const p = ie$1(o), h = r && ue(r) ? ie$1(r) : r;
		let m = p, S = Kt$1(m);
		for (; S && r && h !== m;) {
			const y = Fe$1(S), b = S.getBoundingClientRect(), w = le$1(S), P = b.left + (S.clientLeft + parseFloat(w.paddingLeft)) * y.x, N = b.top + (S.clientTop + parseFloat(w.paddingTop)) * y.y;
			c *= y.x, u *= y.y, f *= y.x, d *= y.y, c += P, u += N, m = ie$1(S), S = Kt$1(m);
		}
	}
	return pt({
		width: f,
		height: d,
		x: c,
		y: u
	});
}
function Pt(e, t) {
	const n = kt(e).scrollLeft;
	return t ? t.left + n : Le$1(de(e)).left + n;
}
function sr(e, t) {
	const n = e.getBoundingClientRect();
	return {
		x: n.left + t.scrollLeft - Pt(e, n),
		y: n.top + t.scrollTop
	};
}
function no(e) {
	let { elements: t, rect: n, offsetParent: r, strategy: i } = e;
	const o = i === "fixed", s = de(r), a = t ? xt(t.floating) : !1;
	if (r === s || a && o) return n;
	let c = {
		scrollLeft: 0,
		scrollTop: 0
	}, u = fe(1);
	const f = fe(0), d = ye(r);
	if ((d || !d && !o) && ((He(r) !== "body" || Ze(s)) && (c = kt(r)), d)) {
		const h = Le$1(r);
		u = Fe$1(r), f.x = h.x + r.clientLeft, f.y = h.y + r.clientTop;
	}
	const p = s && !d && !o ? sr(s, c) : fe(0);
	return {
		width: n.width * u.x,
		height: n.height * u.y,
		x: n.x * u.x - c.scrollLeft * u.x + f.x + p.x,
		y: n.y * u.y - c.scrollTop * u.y + f.y + p.y
	};
}
function ro(e) {
	return Array.from(e.getClientRects());
}
function io(e) {
	const t = de(e), n = kt(e), r = e.ownerDocument.body, i = Ae$1(t.scrollWidth, t.clientWidth, r.scrollWidth, r.clientWidth), o = Ae$1(t.scrollHeight, t.clientHeight, r.scrollHeight, r.clientHeight);
	let s = -n.scrollLeft + Pt(e);
	const a = -n.scrollTop;
	return le$1(r).direction === "rtl" && (s += Ae$1(t.clientWidth, r.clientWidth) - i), {
		width: i,
		height: o,
		x: s,
		y: a
	};
}
var xn = 25;
function oo(e, t) {
	const n = ie$1(e), r = de(e), i = n.visualViewport;
	let o = r.clientWidth, s = r.clientHeight, a = 0, c = 0;
	if (i) {
		o = i.width, s = i.height;
		const f = rn();
		(!f || f && t === "fixed") && (a = i.offsetLeft, c = i.offsetTop);
	}
	const u = Pt(r);
	if (u <= 0) {
		const f = r.ownerDocument, d = f.body, p = getComputedStyle(d), h = f.compatMode === "CSS1Compat" && parseFloat(p.marginLeft) + parseFloat(p.marginRight) || 0, m = Math.abs(r.clientWidth - d.clientWidth - h);
		m <= xn && (o -= m);
	} else u <= xn && (o += u);
	return {
		width: o,
		height: s,
		x: a,
		y: c
	};
}
function so(e, t) {
	const n = Le$1(e, !0, t === "fixed"), r = n.top + e.clientTop, i = n.left + e.clientLeft, o = ye(e) ? Fe$1(e) : fe(1);
	return {
		width: e.clientWidth * o.x,
		height: e.clientHeight * o.y,
		x: i * o.x,
		y: r * o.y
	};
}
function kn(e, t, n) {
	let r;
	if (t === "viewport") r = oo(e, n);
	else if (t === "document") r = io(de(e));
	else if (ue(t)) r = so(t, n);
	else {
		const i = or(e);
		r = {
			x: t.x - i.x,
			y: t.y - i.y,
			width: t.width,
			height: t.height
		};
	}
	return pt(r);
}
function ar(e, t) {
	const n = Ce(e);
	return n === t || !ue(n) || je(n) ? !1 : le$1(n).position === "fixed" || ar(n, t);
}
function ao(e, t) {
	const n = t.get(e);
	if (n) return n;
	let r = Ye(e, [], !1).filter((a) => ue(a) && He(a) !== "body"), i = null;
	const o = le$1(e).position === "fixed";
	let s = o ? Ce(e) : e;
	for (; ue(s) && !je(s);) {
		const a = le$1(s), c = nn(s);
		!c && a.position === "fixed" && (i = null), (o ? !c && !i : !c && a.position === "static" && i && (i.position === "absolute" || i.position === "fixed") || Ze(s) && !c && ar(e, s)) ? r = r.filter((u) => u !== s) : i = a, s = Ce(s);
	}
	return t.set(e, r), r;
}
function co(e) {
	let { element: t, boundary: n, rootBoundary: r, strategy: i } = e;
	const o = [...n === "clippingAncestors" ? xt(t) ? [] : ao(t, this._c) : [].concat(n), r], s = kn(t, o[0], i);
	let a = s.top, c = s.right, u = s.bottom, f = s.left;
	for (let d = 1; d < o.length; d++) {
		const p = kn(t, o[d], i);
		a = Ae$1(p.top, a), c = lt(p.right, c), u = lt(p.bottom, u), f = Ae$1(p.left, f);
	}
	return {
		width: c - f,
		height: u - a,
		x: f,
		y: a
	};
}
function uo(e) {
	const { width: t, height: n } = ir(e);
	return {
		width: t,
		height: n
	};
}
function lo(e, t, n) {
	const r = ye(t), i = de(t), o = n === "fixed", s = Le$1(e, !0, o, t);
	let a = {
		scrollLeft: 0,
		scrollTop: 0
	};
	const c = fe(0);
	function u() {
		c.x = Pt(i);
	}
	if (r || !r && !o) if ((He(t) !== "body" || Ze(i)) && (a = kt(t)), r) {
		const d = Le$1(t, !0, o, t);
		c.x = d.x + t.clientLeft, c.y = d.y + t.clientTop;
	} else i && u();
	o && !r && i && u();
	const f = i && !r && !o ? sr(i, a) : fe(0);
	return {
		x: s.left + a.scrollLeft - c.x - f.x,
		y: s.top + a.scrollTop - c.y - f.y,
		width: s.width,
		height: s.height
	};
}
function Ut(e) {
	return le$1(e).position === "static";
}
function Pn(e, t) {
	if (!ye(e) || le$1(e).position === "fixed") return null;
	if (t) return t(e);
	let n = e.offsetParent;
	return de(e) === n && (n = n.ownerDocument.body), n;
}
function cr(e, t) {
	const n = ie$1(e);
	if (xt(e)) return n;
	if (!ye(e)) {
		let i = Ce(e);
		for (; i && !je(i);) {
			if (ue(i) && !Ut(i)) return i;
			i = Ce(i);
		}
		return n;
	}
	let r = Pn(e, t);
	for (; r && Yi(r) && Ut(r);) r = Pn(r, t);
	return r && je(r) && Ut(r) && !nn(r) ? n : r || Zi(e) || n;
}
var fo = async function(e) {
	const t = this.getOffsetParent || cr, n = this.getDimensions, r = await n(e.floating);
	return {
		reference: lo(e.reference, await t(e.floating), e.strategy),
		floating: {
			x: 0,
			y: 0,
			width: r.width,
			height: r.height
		}
	};
};
function po(e) {
	return le$1(e).direction === "rtl";
}
var ho = {
	convertOffsetParentRelativeRectToViewportRelativeRect: no,
	getDocumentElement: de,
	getClippingRect: co,
	getOffsetParent: cr,
	getElementRects: fo,
	getClientRects: ro,
	getDimensions: uo,
	getScale: Fe$1,
	isElement: ue,
	isRTL: po
};
function ur(e, t) {
	return e.x === t.x && e.y === t.y && e.width === t.width && e.height === t.height;
}
function vo(e, t) {
	let n = null, r;
	const i = de(e);
	function o() {
		var a;
		clearTimeout(r), (a = n) == null || a.disconnect(), n = null;
	}
	function s(a, c) {
		a === void 0 && (a = !1), c === void 0 && (c = 1), o();
		const u = e.getBoundingClientRect(), { left: f, top: d, width: p, height: h } = u;
		if (a || t(), !p || !h) return;
		const m = rt(d), S = rt(i.clientWidth - (f + p)), y = rt(i.clientHeight - (d + h)), b = rt(f), w = {
			rootMargin: -m + "px " + -S + "px " + -y + "px " + -b + "px",
			threshold: Ae$1(0, lt(1, c)) || 1
		};
		let P = !0;
		function N(M) {
			const v = M[0].intersectionRatio;
			if (v !== c) {
				if (!P) return s();
				v ? s(!1, v) : r = setTimeout(() => {
					s(!1, 1e-7);
				}, 1e3);
			}
			v === 1 && !ur(u, e.getBoundingClientRect()) && s(), P = !1;
		}
		try {
			n = new IntersectionObserver(N, {
				...w,
				root: i.ownerDocument
			});
		} catch {
			n = new IntersectionObserver(N, w);
		}
		n.observe(e);
	}
	return s(!0), o;
}
function mo(e, t, n, r) {
	r === void 0 && (r = {});
	const { ancestorScroll: i = !0, ancestorResize: o = !0, elementResize: s = typeof ResizeObserver == "function", layoutShift: a = typeof IntersectionObserver == "function", animationFrame: c = !1 } = r, u = on(e), f = i || o ? [...u ? Ye(u) : [], ...t ? Ye(t) : []] : [];
	f.forEach((b) => {
		i && b.addEventListener("scroll", n, { passive: !0 }), o && b.addEventListener("resize", n);
	});
	const d = u && a ? vo(u, n) : null;
	let p = -1, h = null;
	s && (h = new ResizeObserver((b) => {
		let [w] = b;
		w && w.target === u && h && t && (h.unobserve(t), cancelAnimationFrame(p), p = requestAnimationFrame(() => {
			var P;
			(P = h) == null || P.observe(t);
		})), n();
	}), u && !c && h.observe(u), t && h.observe(t));
	let m, S = c ? Le$1(e) : null;
	c && y();
	function y() {
		const b = Le$1(e);
		S && !ur(S, b) && n(), S = b, m = requestAnimationFrame(y);
	}
	return n(), () => {
		var b;
		f.forEach((w) => {
			i && w.removeEventListener("scroll", n), o && w.removeEventListener("resize", n);
		}), d?.(), (b = h) == null || b.disconnect(), h = null, c && cancelAnimationFrame(m);
	};
}
var go = Gi;
var bo = Ji;
var yo = zi;
var wo = (e, t, n) => {
	const r = /* @__PURE__ */ new Map(), i = {
		platform: ho,
		...n
	}, o = {
		...i.platform,
		_c: r
	};
	return Hi(e, t, {
		...i,
		platform: o
	});
};
var So = /* @__PURE__ */ j$2(((e, t) => {
	(function(n, r) {
		"use strict";
		typeof define == "function" && define.amd ? define(r) : typeof t == "object" && t.exports ? t.exports = r() : n.log = r();
	})(e, function() {
		"use strict";
		var n = function() {}, r = "undefined", i = typeof window !== r && typeof window.navigator !== r && /Trident\/|MSIE /.test(window.navigator.userAgent), o = [
			"trace",
			"debug",
			"info",
			"warn",
			"error"
		], s = {}, a = null;
		function c(y, b) {
			var w = y[b];
			if (typeof w.bind == "function") return w.bind(y);
			try {
				return Function.prototype.bind.call(w, y);
			} catch {
				return function() {
					return Function.prototype.apply.apply(w, [y, arguments]);
				};
			}
		}
		function u() {
			console.log && (console.log.apply ? console.log.apply(console, arguments) : Function.prototype.apply.apply(console.log, [console, arguments])), console.trace && console.trace();
		}
		function f(y) {
			return y === "debug" && (y = "log"), typeof console === r ? !1 : y === "trace" && i ? u : console[y] !== void 0 ? c(console, y) : console.log !== void 0 ? c(console, "log") : n;
		}
		function d() {
			for (var y = this.getLevel(), b = 0; b < o.length; b++) {
				var w = o[b];
				this[w] = b < y ? n : this.methodFactory(w, y, this.name);
			}
			if (this.log = this.debug, typeof console === r && y < this.levels.SILENT) return "No console available for logging";
		}
		function p(y) {
			return function() {
				typeof console !== r && (d.call(this), this[y].apply(this, arguments));
			};
		}
		function h(y, b, w) {
			return f(y) || p.apply(this, arguments);
		}
		function m(y, b) {
			var w = this, P, N, M, v = "loglevel";
			typeof y == "string" ? v += ":" + y : typeof y == "symbol" && (v = void 0);
			function g(T) {
				var A = (o[T] || "silent").toUpperCase();
				if (!(typeof window === r || !v)) {
					try {
						window.localStorage[v] = A;
						return;
					} catch {}
					try {
						window.document.cookie = encodeURIComponent(v) + "=" + A + ";";
					} catch {}
				}
			}
			function E() {
				var T;
				if (!(typeof window === r || !v)) {
					try {
						T = window.localStorage[v];
					} catch {}
					if (typeof T === r) try {
						var A = window.document.cookie, D = encodeURIComponent(v), U = A.indexOf(D + "=");
						U !== -1 && (T = /^([^;]+)/.exec(A.slice(U + D.length + 1))[1]);
					} catch {}
					return w.levels[T] === void 0 && (T = void 0), T;
				}
			}
			function x() {
				if (!(typeof window === r || !v)) {
					try {
						window.localStorage.removeItem(v);
					} catch {}
					try {
						window.document.cookie = encodeURIComponent(v) + "=; expires=Thu, 01 Jan 1970 00:00:00 UTC";
					} catch {}
				}
			}
			function C(T) {
				var A = T;
				if (typeof A == "string" && w.levels[A.toUpperCase()] !== void 0 && (A = w.levels[A.toUpperCase()]), typeof A == "number" && A >= 0 && A <= w.levels.SILENT) return A;
				throw new TypeError("log.setLevel() called with invalid level: " + T);
			}
			w.name = y, w.levels = {
				TRACE: 0,
				DEBUG: 1,
				INFO: 2,
				WARN: 3,
				ERROR: 4,
				SILENT: 5
			}, w.methodFactory = b || h, w.getLevel = function() {
				return M ?? N ?? P;
			}, w.setLevel = function(T, A) {
				return M = C(T), A !== !1 && g(M), d.call(w);
			}, w.setDefaultLevel = function(T) {
				N = C(T), E() || w.setLevel(T, !1);
			}, w.resetLevel = function() {
				M = null, x(), d.call(w);
			}, w.enableAll = function(T) {
				w.setLevel(w.levels.TRACE, T);
			}, w.disableAll = function(T) {
				w.setLevel(w.levels.SILENT, T);
			}, w.rebuild = function() {
				if (a !== w && (P = C(a.getLevel())), d.call(w), a === w) for (var T in s) s[T].rebuild();
			}, P = C(a ? a.getLevel() : "WARN");
			var I = E();
			I != null && (M = C(I)), d.call(w);
		}
		a = new m(), a.getLogger = function(b) {
			if (typeof b != "symbol" && typeof b != "string" || b === "") throw new TypeError("You must supply a name when creating a logger.");
			var w = s[b];
			return w || (w = s[b] = new m(b, a.methodFactory)), w;
		};
		var S = typeof window !== r ? window.log : void 0;
		return a.noConflict = function() {
			return typeof window !== r && window.log === a && (window.log = S), a;
		}, a.getLoggers = function() {
			return s;
		}, a.default = a, a;
	});
}));
var Gt$1 = function(e, t) {
	return Gt$1 = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(n, r) {
		n.__proto__ = r;
	} || function(n, r) {
		for (var i in r) Object.prototype.hasOwnProperty.call(r, i) && (n[i] = r[i]);
	}, Gt$1(e, t);
};
function we$1(e, t) {
	if (typeof t != "function" && t !== null) throw new TypeError("Class extends value " + String(t) + " is not a constructor or null");
	Gt$1(e, t);
	function n() {
		this.constructor = e;
	}
	e.prototype = t === null ? Object.create(t) : (n.prototype = t.prototype, new n());
}
function Eo(e, t, n, r) {
	function i(o) {
		return o instanceof n ? o : new n(function(s) {
			s(o);
		});
	}
	return new (n || (n = Promise))(function(o, s) {
		function a(f) {
			try {
				u(r.next(f));
			} catch (d) {
				s(d);
			}
		}
		function c(f) {
			try {
				u(r.throw(f));
			} catch (d) {
				s(d);
			}
		}
		function u(f) {
			f.done ? o(f.value) : i(f.value).then(a, c);
		}
		u((r = r.apply(e, t || [])).next());
	});
}
function lr(e, t) {
	var n = {
		label: 0,
		sent: function() {
			if (o[0] & 1) throw o[1];
			return o[1];
		},
		trys: [],
		ops: []
	}, r, i, o, s = Object.create((typeof Iterator == "function" ? Iterator : Object).prototype);
	return s.next = a(0), s.throw = a(1), s.return = a(2), typeof Symbol == "function" && (s[Symbol.iterator] = function() {
		return this;
	}), s;
	function a(u) {
		return function(f) {
			return c([u, f]);
		};
	}
	function c(u) {
		if (r) throw new TypeError("Generator is already executing.");
		for (; s && (s = 0, u[0] && (n = 0)), n;) try {
			if (r = 1, i && (o = u[0] & 2 ? i.return : u[0] ? i.throw || ((o = i.return) && o.call(i), 0) : i.next) && !(o = o.call(i, u[1])).done) return o;
			switch (i = 0, o && (u = [u[0] & 2, o.value]), u[0]) {
				case 0:
				case 1:
					o = u;
					break;
				case 4: return n.label++, {
					value: u[1],
					done: !1
				};
				case 5:
					n.label++, i = u[1], u = [0];
					continue;
				case 7:
					u = n.ops.pop(), n.trys.pop();
					continue;
				default:
					if (o = n.trys, !(o = o.length > 0 && o[o.length - 1]) && (u[0] === 6 || u[0] === 2)) {
						n = 0;
						continue;
					}
					if (u[0] === 3 && (!o || u[1] > o[0] && u[1] < o[3])) {
						n.label = u[1];
						break;
					}
					if (u[0] === 6 && n.label < o[1]) {
						n.label = o[1], o = u;
						break;
					}
					if (o && n.label < o[2]) {
						n.label = o[2], n.ops.push(u);
						break;
					}
					o[2] && n.ops.pop(), n.trys.pop();
					continue;
			}
			u = t.call(e, n);
		} catch (f) {
			u = [6, f], i = 0;
		} finally {
			r = o = 0;
		}
		if (u[0] & 5) throw u[1];
		return {
			value: u[0] ? u[1] : void 0,
			done: !0
		};
	}
}
function Be$1(e) {
	var t = typeof Symbol == "function" && Symbol.iterator, n = t && e[t], r = 0;
	if (n) return n.call(e);
	if (e && typeof e.length == "number") return { next: function() {
		return e && r >= e.length && (e = void 0), {
			value: e && e[r++],
			done: !e
		};
	} };
	throw new TypeError(t ? "Object is not iterable." : "Symbol.iterator is not defined.");
}
function ge$1(e, t) {
	var n = typeof Symbol == "function" && e[Symbol.iterator];
	if (!n) return e;
	var r = n.call(e), i, o = [], s;
	try {
		for (; (t === void 0 || t-- > 0) && !(i = r.next()).done;) o.push(i.value);
	} catch (a) {
		s = { error: a };
	} finally {
		try {
			i && !i.done && (n = r.return) && n.call(r);
		} finally {
			if (s) throw s.error;
		}
	}
	return o;
}
function Te$1(e, t, n) {
	if (n || arguments.length === 2) for (var r = 0, i = t.length, o; r < i; r++) (o || !(r in t)) && (o || (o = Array.prototype.slice.call(t, 0, r)), o[r] = t[r]);
	return e.concat(o || Array.prototype.slice.call(t));
}
function Ue(e) {
	return this instanceof Ue ? (this.v = e, this) : new Ue(e);
}
function Co(e, t, n) {
	if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
	var r = n.apply(e, t || []), i, o = [];
	return i = Object.create((typeof AsyncIterator == "function" ? AsyncIterator : Object).prototype), a("next"), a("throw"), a("return", s), i[Symbol.asyncIterator] = function() {
		return this;
	}, i;
	function s(h) {
		return function(m) {
			return Promise.resolve(m).then(h, d);
		};
	}
	function a(h, m) {
		r[h] && (i[h] = function(S) {
			return new Promise(function(y, b) {
				o.push([
					h,
					S,
					y,
					b
				]) > 1 || c(h, S);
			});
		}, m && (i[h] = m(i[h])));
	}
	function c(h, m) {
		try {
			u(r[h](m));
		} catch (S) {
			p(o[0][3], S);
		}
	}
	function u(h) {
		h.value instanceof Ue ? Promise.resolve(h.value.v).then(f, d) : p(o[0][2], h);
	}
	function f(h) {
		c("next", h);
	}
	function d(h) {
		c("throw", h);
	}
	function p(h, m) {
		h(m), o.shift(), o.length && c(o[0][0], o[0][1]);
	}
}
function To(e) {
	if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
	var t = e[Symbol.asyncIterator], n;
	return t ? t.call(e) : (e = typeof Be$1 == "function" ? Be$1(e) : e[Symbol.iterator](), n = {}, r("next"), r("throw"), r("return"), n[Symbol.asyncIterator] = function() {
		return this;
	}, n);
	function r(o) {
		n[o] = e[o] && function(s) {
			return new Promise(function(a, c) {
				s = e[o](s), i(a, c, s.done, s.value);
			});
		};
	}
	function i(o, s, a, c) {
		Promise.resolve(c).then(function(u) {
			o({
				value: u,
				done: a
			});
		}, s);
	}
}
function $(e) {
	return typeof e == "function";
}
function sn(e) {
	var t = function(r) {
		Error.call(r), r.stack = (/* @__PURE__ */ new Error()).stack;
	}, n = e(t);
	return n.prototype = Object.create(Error.prototype), n.prototype.constructor = n, n;
}
var $t = sn(function(e) {
	return function(n) {
		e(this), this.message = n ? n.length + ` errors occurred during unsubscription:
` + n.map(function(r, i) {
			return i + 1 + ") " + r.toString();
		}).join(`
  `) : "", this.name = "UnsubscriptionError", this.errors = n;
	};
});
function ht(e, t) {
	if (e) {
		var n = e.indexOf(t);
		0 <= n && e.splice(n, 1);
	}
}
var et = (function() {
	function e(t) {
		this.initialTeardown = t, this.closed = !1, this._parentage = null, this._finalizers = null;
	}
	return e.prototype.unsubscribe = function() {
		var t, n, r, i, o;
		if (!this.closed) {
			this.closed = !0;
			var s = this._parentage;
			if (s) if (this._parentage = null, Array.isArray(s)) try {
				for (var a = Be$1(s), c = a.next(); !c.done; c = a.next()) c.value.remove(this);
			} catch (m) {
				t = { error: m };
			} finally {
				try {
					c && !c.done && (n = a.return) && n.call(a);
				} finally {
					if (t) throw t.error;
				}
			}
			else s.remove(this);
			var u = this.initialTeardown;
			if ($(u)) try {
				u();
			} catch (m) {
				o = m instanceof $t ? m.errors : [m];
			}
			var f = this._finalizers;
			if (f) {
				this._finalizers = null;
				try {
					for (var d = Be$1(f), p = d.next(); !p.done; p = d.next()) {
						var h = p.value;
						try {
							Mn(h);
						} catch (m) {
							o = o ?? [], m instanceof $t ? o = Te$1(Te$1([], ge$1(o)), ge$1(m.errors)) : o.push(m);
						}
					}
				} catch (m) {
					r = { error: m };
				} finally {
					try {
						p && !p.done && (i = d.return) && i.call(d);
					} finally {
						if (r) throw r.error;
					}
				}
			}
			if (o) throw new $t(o);
		}
	}, e.prototype.add = function(t) {
		var n;
		if (t && t !== this) if (this.closed) Mn(t);
		else {
			if (t instanceof e) {
				if (t.closed || t._hasParent(this)) return;
				t._addParent(this);
			}
			(this._finalizers = (n = this._finalizers) !== null && n !== void 0 ? n : []).push(t);
		}
	}, e.prototype._hasParent = function(t) {
		var n = this._parentage;
		return n === t || Array.isArray(n) && n.includes(t);
	}, e.prototype._addParent = function(t) {
		var n = this._parentage;
		this._parentage = Array.isArray(n) ? (n.push(t), n) : n ? [n, t] : t;
	}, e.prototype._removeParent = function(t) {
		var n = this._parentage;
		n === t ? this._parentage = null : Array.isArray(n) && ht(n, t);
	}, e.prototype.remove = function(t) {
		var n = this._finalizers;
		n && ht(n, t), t instanceof e && t._removeParent(this);
	}, e.EMPTY = (function() {
		var t = new e();
		return t.closed = !0, t;
	})(), e;
})();
var fr = et.EMPTY;
function dr(e) {
	return e instanceof et || e && "closed" in e && $(e.remove) && $(e.add) && $(e.unsubscribe);
}
function Mn(e) {
	$(e) ? e() : e.unsubscribe();
}
var Oe$1 = {
	onUnhandledError: null,
	onStoppedNotification: null,
	Promise: void 0,
	useDeprecatedSynchronousErrorHandling: !1,
	useDeprecatedNextContext: !1
};
var vt = {
	setTimeout: function(e, t) {
		for (var n = [], r = 2; r < arguments.length; r++) n[r - 2] = arguments[r];
		var i = vt.delegate;
		return i?.setTimeout ? i.setTimeout.apply(i, Te$1([e, t], ge$1(n))) : setTimeout.apply(void 0, Te$1([e, t], ge$1(n)));
	},
	clearTimeout: function(e) {
		return (vt.delegate?.clearTimeout || clearTimeout)(e);
	},
	delegate: void 0
};
function pr(e) {
	vt.setTimeout(function() {
		var t = Oe$1.onUnhandledError;
		if (t) t(e);
		else throw e;
	});
}
function mt() {}
var xo = (function() {
	return an("C", void 0, void 0);
})();
function ko(e) {
	return an("E", void 0, e);
}
function Po(e) {
	return an("N", e, void 0);
}
function an(e, t, n) {
	return {
		kind: e,
		value: t,
		error: n
	};
}
var Me = null;
function at(e) {
	if (Oe$1.useDeprecatedSynchronousErrorHandling) {
		var t = !Me;
		if (t && (Me = {
			errorThrown: !1,
			error: null
		}), e(), t) {
			var n = Me, r = n.errorThrown, i = n.error;
			if (Me = null, r) throw i;
		}
	} else e();
}
function Mo(e) {
	Oe$1.useDeprecatedSynchronousErrorHandling && Me && (Me.errorThrown = !0, Me.error = e);
}
var cn = (function(e) {
	we$1(t, e);
	function t(n) {
		var r = e.call(this) || this;
		return r.isStopped = !1, n ? (r.destination = n, dr(n) && n.add(r)) : r.destination = _o, r;
	}
	return t.create = function(n, r, i) {
		return new Qe(n, r, i);
	}, t.prototype.next = function(n) {
		this.isStopped ? jt(Po(n), this) : this._next(n);
	}, t.prototype.error = function(n) {
		this.isStopped ? jt(ko(n), this) : (this.isStopped = !0, this._error(n));
	}, t.prototype.complete = function() {
		this.isStopped ? jt(xo, this) : (this.isStopped = !0, this._complete());
	}, t.prototype.unsubscribe = function() {
		this.closed || (this.isStopped = !0, e.prototype.unsubscribe.call(this), this.destination = null);
	}, t.prototype._next = function(n) {
		this.destination.next(n);
	}, t.prototype._error = function(n) {
		try {
			this.destination.error(n);
		} finally {
			this.unsubscribe();
		}
	}, t.prototype._complete = function() {
		try {
			this.destination.complete();
		} finally {
			this.unsubscribe();
		}
	}, t;
})(et);
var Ao = Function.prototype.bind;
function Wt(e, t) {
	return Ao.call(e, t);
}
var Lo = (function() {
	function e(t) {
		this.partialObserver = t;
	}
	return e.prototype.next = function(t) {
		var n = this.partialObserver;
		if (n.next) try {
			n.next(t);
		} catch (r) {
			it(r);
		}
	}, e.prototype.error = function(t) {
		var n = this.partialObserver;
		if (n.error) try {
			n.error(t);
		} catch (r) {
			it(r);
		}
		else it(t);
	}, e.prototype.complete = function() {
		var t = this.partialObserver;
		if (t.complete) try {
			t.complete();
		} catch (n) {
			it(n);
		}
	}, e;
})();
var Qe = (function(e) {
	we$1(t, e);
	function t(n, r, i) {
		var o = e.call(this) || this, s;
		if ($(n) || !n) s = {
			next: n ?? void 0,
			error: r ?? void 0,
			complete: i ?? void 0
		};
		else {
			var a;
			o && Oe$1.useDeprecatedNextContext ? (a = Object.create(n), a.unsubscribe = function() {
				return o.unsubscribe();
			}, s = {
				next: n.next && Wt(n.next, a),
				error: n.error && Wt(n.error, a),
				complete: n.complete && Wt(n.complete, a)
			}) : s = n;
		}
		return o.destination = new Lo(s), o;
	}
	return t;
})(cn);
function it(e) {
	Oe$1.useDeprecatedSynchronousErrorHandling ? Mo(e) : pr(e);
}
function Oo(e) {
	throw e;
}
function jt(e, t) {
	var n = Oe$1.onStoppedNotification;
	n && vt.setTimeout(function() {
		return n(e, t);
	});
}
var _o = {
	closed: !0,
	next: mt,
	error: Oo,
	complete: mt
};
var un = (function() {
	return typeof Symbol == "function" && Symbol.observable || "@@observable";
})();
function Mt(e) {
	return e;
}
function Io(e) {
	return e.length === 0 ? Mt : e.length === 1 ? e[0] : function(n) {
		return e.reduce(function(r, i) {
			return i(r);
		}, n);
	};
}
var B = (function() {
	function e(t) {
		t && (this._subscribe = t);
	}
	return e.prototype.lift = function(t) {
		var n = new e();
		return n.source = this, n.operator = t, n;
	}, e.prototype.subscribe = function(t, n, r) {
		var i = this, o = Do(t) ? t : new Qe(t, n, r);
		return at(function() {
			var s = i, a = s.operator, c = s.source;
			o.add(a ? a.call(o, c) : c ? i._subscribe(o) : i._trySubscribe(o));
		}), o;
	}, e.prototype._trySubscribe = function(t) {
		try {
			return this._subscribe(t);
		} catch (n) {
			t.error(n);
		}
	}, e.prototype.forEach = function(t, n) {
		var r = this;
		return n = An(n), new n(function(i, o) {
			var s = new Qe({
				next: function(a) {
					try {
						t(a);
					} catch (c) {
						o(c), s.unsubscribe();
					}
				},
				error: o,
				complete: i
			});
			r.subscribe(s);
		});
	}, e.prototype._subscribe = function(t) {
		var n;
		return (n = this.source) === null || n === void 0 ? void 0 : n.subscribe(t);
	}, e.prototype[un] = function() {
		return this;
	}, e.prototype.pipe = function() {
		for (var t = [], n = 0; n < arguments.length; n++) t[n] = arguments[n];
		return Io(t)(this);
	}, e.prototype.toPromise = function(t) {
		var n = this;
		return t = An(t), new t(function(r, i) {
			var o;
			n.subscribe(function(s) {
				return o = s;
			}, function(s) {
				return i(s);
			}, function() {
				return r(o);
			});
		});
	}, e.create = function(t) {
		return new e(t);
	}, e;
})();
function An(e) {
	var t;
	return (t = e ?? Oe$1.Promise) !== null && t !== void 0 ? t : Promise;
}
function Ro(e) {
	return e && $(e.next) && $(e.error) && $(e.complete);
}
function Do(e) {
	return e && e instanceof cn || Ro(e) && dr(e);
}
function No(e) {
	return $(e?.lift);
}
function Y(e) {
	return function(t) {
		if (No(t)) return t.lift(function(n) {
			try {
				return e(n, this);
			} catch (r) {
				this.error(r);
			}
		});
		throw new TypeError("Unable to lift unknown Observable type");
	};
}
function te(e, t, n, r, i) {
	return new Fo(e, t, n, r, i);
}
var Fo = (function(e) {
	we$1(t, e);
	function t(n, r, i, o, s, a) {
		var c = e.call(this, n) || this;
		return c.onFinalize = s, c.shouldUnsubscribe = a, c._next = r ? function(u) {
			try {
				r(u);
			} catch (f) {
				n.error(f);
			}
		} : e.prototype._next, c._error = o ? function(u) {
			try {
				o(u);
			} catch (f) {
				n.error(f);
			} finally {
				this.unsubscribe();
			}
		} : e.prototype._error, c._complete = i ? function() {
			try {
				i();
			} catch (u) {
				n.error(u);
			} finally {
				this.unsubscribe();
			}
		} : e.prototype._complete, c;
	}
	return t.prototype.unsubscribe = function() {
		var n;
		if (!this.shouldUnsubscribe || this.shouldUnsubscribe()) {
			var r = this.closed;
			e.prototype.unsubscribe.call(this), !r && ((n = this.onFinalize) === null || n === void 0 || n.call(this));
		}
	}, t;
})(cn);
var Uo = sn(function(e) {
	return function() {
		e(this), this.name = "ObjectUnsubscribedError", this.message = "object unsubscribed";
	};
});
var be$1 = (function(e) {
	we$1(t, e);
	function t() {
		var n = e.call(this) || this;
		return n.closed = !1, n.currentObservers = null, n.observers = [], n.isStopped = !1, n.hasError = !1, n.thrownError = null, n;
	}
	return t.prototype.lift = function(n) {
		var r = new Ln(this, this);
		return r.operator = n, r;
	}, t.prototype._throwIfClosed = function() {
		if (this.closed) throw new Uo();
	}, t.prototype.next = function(n) {
		var r = this;
		at(function() {
			var i, o;
			if (r._throwIfClosed(), !r.isStopped) {
				r.currentObservers || (r.currentObservers = Array.from(r.observers));
				try {
					for (var s = Be$1(r.currentObservers), a = s.next(); !a.done; a = s.next()) a.value.next(n);
				} catch (c) {
					i = { error: c };
				} finally {
					try {
						a && !a.done && (o = s.return) && o.call(s);
					} finally {
						if (i) throw i.error;
					}
				}
			}
		});
	}, t.prototype.error = function(n) {
		var r = this;
		at(function() {
			if (r._throwIfClosed(), !r.isStopped) {
				r.hasError = r.isStopped = !0, r.thrownError = n;
				for (var i = r.observers; i.length;) i.shift().error(n);
			}
		});
	}, t.prototype.complete = function() {
		var n = this;
		at(function() {
			if (n._throwIfClosed(), !n.isStopped) {
				n.isStopped = !0;
				for (var r = n.observers; r.length;) r.shift().complete();
			}
		});
	}, t.prototype.unsubscribe = function() {
		this.isStopped = this.closed = !0, this.observers = this.currentObservers = null;
	}, Object.defineProperty(t.prototype, "observed", {
		get: function() {
			var n;
			return ((n = this.observers) === null || n === void 0 ? void 0 : n.length) > 0;
		},
		enumerable: !1,
		configurable: !0
	}), t.prototype._trySubscribe = function(n) {
		return this._throwIfClosed(), e.prototype._trySubscribe.call(this, n);
	}, t.prototype._subscribe = function(n) {
		return this._throwIfClosed(), this._checkFinalizedStatuses(n), this._innerSubscribe(n);
	}, t.prototype._innerSubscribe = function(n) {
		var r = this, i = this, o = i.hasError, s = i.isStopped, a = i.observers;
		return o || s ? fr : (this.currentObservers = null, a.push(n), new et(function() {
			r.currentObservers = null, ht(a, n);
		}));
	}, t.prototype._checkFinalizedStatuses = function(n) {
		var r = this, i = r.hasError, o = r.thrownError, s = r.isStopped;
		i ? n.error(o) : s && n.complete();
	}, t.prototype.asObservable = function() {
		var n = new B();
		return n.source = this, n;
	}, t.create = function(n, r) {
		return new Ln(n, r);
	}, t;
})(B);
var Ln = (function(e) {
	we$1(t, e);
	function t(n, r) {
		var i = e.call(this) || this;
		return i.destination = n, i.source = r, i;
	}
	return t.prototype.next = function(n) {
		var r, i;
		(i = (r = this.destination) === null || r === void 0 ? void 0 : r.next) === null || i === void 0 || i.call(r, n);
	}, t.prototype.error = function(n) {
		var r, i;
		(i = (r = this.destination) === null || r === void 0 ? void 0 : r.error) === null || i === void 0 || i.call(r, n);
	}, t.prototype.complete = function() {
		var n, r;
		(r = (n = this.destination) === null || n === void 0 ? void 0 : n.complete) === null || r === void 0 || r.call(n);
	}, t.prototype._subscribe = function(n) {
		var r, i;
		return (i = (r = this.source) === null || r === void 0 ? void 0 : r.subscribe(n)) !== null && i !== void 0 ? i : fr;
	}, t;
})(be$1);
var hr = (function(e) {
	we$1(t, e);
	function t(n) {
		var r = e.call(this) || this;
		return r._value = n, r;
	}
	return Object.defineProperty(t.prototype, "value", {
		get: function() {
			return this.getValue();
		},
		enumerable: !1,
		configurable: !0
	}), t.prototype._subscribe = function(n) {
		var r = e.prototype._subscribe.call(this, n);
		return !r.closed && n.next(this._value), r;
	}, t.prototype.getValue = function() {
		var n = this, r = n.hasError, i = n.thrownError, o = n._value;
		if (r) throw i;
		return this._throwIfClosed(), o;
	}, t.prototype.next = function(n) {
		e.prototype.next.call(this, this._value = n);
	}, t;
})(be$1);
var vr = {
	now: function() {
		return (vr.delegate || Date).now();
	},
	delegate: void 0
};
var $o = (function(e) {
	we$1(t, e);
	function t(n, r) {
		return e.call(this) || this;
	}
	return t.prototype.schedule = function(n, r) {
		return r === void 0 && (r = 0), this;
	}, t;
})(et);
var gt = {
	setInterval: function(e, t) {
		for (var n = [], r = 2; r < arguments.length; r++) n[r - 2] = arguments[r];
		var i = gt.delegate;
		return i?.setInterval ? i.setInterval.apply(i, Te$1([e, t], ge$1(n))) : setInterval.apply(void 0, Te$1([e, t], ge$1(n)));
	},
	clearInterval: function(e) {
		return (gt.delegate?.clearInterval || clearInterval)(e);
	},
	delegate: void 0
};
var Wo = (function(e) {
	we$1(t, e);
	function t(n, r) {
		var i = e.call(this, n, r) || this;
		return i.scheduler = n, i.work = r, i.pending = !1, i;
	}
	return t.prototype.schedule = function(n, r) {
		var i;
		if (r === void 0 && (r = 0), this.closed) return this;
		this.state = n;
		var o = this.id, s = this.scheduler;
		return o != null && (this.id = this.recycleAsyncId(s, o, r)), this.pending = !0, this.delay = r, this.id = (i = this.id) !== null && i !== void 0 ? i : this.requestAsyncId(s, this.id, r), this;
	}, t.prototype.requestAsyncId = function(n, r, i) {
		return i === void 0 && (i = 0), gt.setInterval(n.flush.bind(n, this), i);
	}, t.prototype.recycleAsyncId = function(n, r, i) {
		if (i === void 0 && (i = 0), i != null && this.delay === i && this.pending === !1) return r;
		r != null && gt.clearInterval(r);
	}, t.prototype.execute = function(n, r) {
		if (this.closed) return /* @__PURE__ */ new Error("executing a cancelled action");
		this.pending = !1;
		var i = this._execute(n, r);
		if (i) return i;
		this.pending === !1 && this.id != null && (this.id = this.recycleAsyncId(this.scheduler, this.id, null));
	}, t.prototype._execute = function(n, r) {
		var i = !1, o;
		try {
			this.work(n);
		} catch (s) {
			i = !0, o = s || /* @__PURE__ */ new Error("Scheduled action threw falsy error");
		}
		if (i) return this.unsubscribe(), o;
	}, t.prototype.unsubscribe = function() {
		if (!this.closed) {
			var n = this, r = n.id, i = n.scheduler, o = i.actions;
			this.work = this.state = this.scheduler = null, this.pending = !1, ht(o, this), r != null && (this.id = this.recycleAsyncId(i, r, null)), this.delay = null, e.prototype.unsubscribe.call(this);
		}
	}, t;
})($o);
var On = (function() {
	function e(t, n) {
		n === void 0 && (n = e.now), this.schedulerActionCtor = t, this.now = n;
	}
	return e.prototype.schedule = function(t, n, r) {
		return n === void 0 && (n = 0), new this.schedulerActionCtor(this, t).schedule(r, n);
	}, e.now = vr.now, e;
})();
var Bo = new ((function(e) {
	we$1(t, e);
	function t(n, r) {
		r === void 0 && (r = On.now);
		var i = e.call(this, n, r) || this;
		return i.actions = [], i._active = !1, i;
	}
	return t.prototype.flush = function(n) {
		var r = this.actions;
		if (this._active) {
			r.push(n);
			return;
		}
		var i;
		this._active = !0;
		do
			if (i = n.execute(n.state, n.delay)) break;
		while (n = r.shift());
		if (this._active = !1, i) {
			for (; n = r.shift();) n.unsubscribe();
			throw i;
		}
	}, t;
})(On))(Wo);
function Vo(e) {
	return e && $(e.schedule);
}
function Ho(e) {
	return e[e.length - 1];
}
function ln(e) {
	return Vo(Ho(e)) ? e.pop() : void 0;
}
var fn = (function(e) {
	return e && typeof e.length == "number" && typeof e != "function";
});
function mr(e) {
	return $(e?.then);
}
function gr(e) {
	return $(e[un]);
}
function br(e) {
	return Symbol.asyncIterator && $(e?.[Symbol.asyncIterator]);
}
function yr(e) {
	return /* @__PURE__ */ new TypeError("You provided " + (e !== null && typeof e == "object" ? "an invalid object" : "'" + e + "'") + " where a stream was expected. You can provide an Observable, Promise, ReadableStream, Array, AsyncIterable, or Iterable.");
}
function zo() {
	return typeof Symbol != "function" || !Symbol.iterator ? "@@iterator" : Symbol.iterator;
}
var wr = zo();
function Sr(e) {
	return $(e?.[wr]);
}
function Er(e) {
	return Co(this, arguments, function() {
		var n, r, i, o;
		return lr(this, function(s) {
			switch (s.label) {
				case 0: n = e.getReader(), s.label = 1;
				case 1: s.trys.push([
					1,
					,
					9,
					10
				]), s.label = 2;
				case 2: return [4, Ue(n.read())];
				case 3: return r = s.sent(), i = r.value, o = r.done, o ? [4, Ue(void 0)] : [3, 5];
				case 4: return [2, s.sent()];
				case 5: return [4, Ue(i)];
				case 6: return [4, s.sent()];
				case 7: return s.sent(), [3, 2];
				case 8: return [3, 10];
				case 9: return n.releaseLock(), [7];
				case 10: return [2];
			}
		});
	});
}
function Cr(e) {
	return $(e?.getReader);
}
function se(e) {
	if (e instanceof B) return e;
	if (e != null) {
		if (gr(e)) return qo(e);
		if (fn(e)) return Ko(e);
		if (mr(e)) return Go(e);
		if (br(e)) return Tr(e);
		if (Sr(e)) return Jo(e);
		if (Cr(e)) return Yo(e);
	}
	throw yr(e);
}
function qo(e) {
	return new B(function(t) {
		var n = e[un]();
		if ($(n.subscribe)) return n.subscribe(t);
		throw new TypeError("Provided object does not correctly implement Symbol.observable");
	});
}
function Ko(e) {
	return new B(function(t) {
		for (var n = 0; n < e.length && !t.closed; n++) t.next(e[n]);
		t.complete();
	});
}
function Go(e) {
	return new B(function(t) {
		e.then(function(n) {
			t.closed || (t.next(n), t.complete());
		}, function(n) {
			return t.error(n);
		}).then(null, pr);
	});
}
function Jo(e) {
	return new B(function(t) {
		var n, r;
		try {
			for (var i = Be$1(e), o = i.next(); !o.done; o = i.next()) {
				var s = o.value;
				if (t.next(s), t.closed) return;
			}
		} catch (a) {
			n = { error: a };
		} finally {
			try {
				o && !o.done && (r = i.return) && r.call(i);
			} finally {
				if (n) throw n.error;
			}
		}
		t.complete();
	});
}
function Tr(e) {
	return new B(function(t) {
		Qo(e, t).catch(function(n) {
			return t.error(n);
		});
	});
}
function Yo(e) {
	return Tr(Er(e));
}
function Qo(e, t) {
	var n, r, i, o;
	return Eo(this, void 0, void 0, function() {
		var s, a;
		return lr(this, function(c) {
			switch (c.label) {
				case 0: c.trys.push([
					0,
					5,
					6,
					11
				]), n = To(e), c.label = 1;
				case 1: return [4, n.next()];
				case 2:
					if (r = c.sent(), !!r.done) return [3, 4];
					if (s = r.value, t.next(s), t.closed) return [2];
					c.label = 3;
				case 3: return [3, 1];
				case 4: return [3, 11];
				case 5: return a = c.sent(), i = { error: a }, [3, 11];
				case 6: return c.trys.push([
					6,
					,
					9,
					10
				]), r && !r.done && (o = n.return) ? [4, o.call(n)] : [3, 8];
				case 7: c.sent(), c.label = 8;
				case 8: return [3, 10];
				case 9:
					if (i) throw i.error;
					return [7];
				case 10: return [7];
				case 11: return t.complete(), [2];
			}
		});
	});
}
function me(e, t, n, r, i) {
	r === void 0 && (r = 0), i === void 0 && (i = !1);
	var o = t.schedule(function() {
		n(), i ? e.add(this.schedule(null, r)) : this.unsubscribe();
	}, r);
	if (e.add(o), !i) return o;
}
function xr(e, t) {
	return t === void 0 && (t = 0), Y(function(n, r) {
		n.subscribe(te(r, function(i) {
			return me(r, e, function() {
				return r.next(i);
			}, t);
		}, function() {
			return me(r, e, function() {
				return r.complete();
			}, t);
		}, function(i) {
			return me(r, e, function() {
				return r.error(i);
			}, t);
		}));
	});
}
function kr(e, t) {
	return t === void 0 && (t = 0), Y(function(n, r) {
		r.add(e.schedule(function() {
			return n.subscribe(r);
		}, t));
	});
}
function Xo(e, t) {
	return se(e).pipe(kr(t), xr(t));
}
function Zo(e, t) {
	return se(e).pipe(kr(t), xr(t));
}
function es(e, t) {
	return new B(function(n) {
		var r = 0;
		return t.schedule(function() {
			r === e.length ? n.complete() : (n.next(e[r++]), n.closed || this.schedule());
		});
	});
}
function ts$1(e, t) {
	return new B(function(n) {
		var r;
		return me(n, t, function() {
			r = e[wr](), me(n, t, function() {
				var i, o, s;
				try {
					i = r.next(), o = i.value, s = i.done;
				} catch (a) {
					n.error(a);
					return;
				}
				s ? n.complete() : n.next(o);
			}, 0, !0);
		}), function() {
			return $(r?.return) && r.return();
		};
	});
}
function Pr(e, t) {
	if (!e) throw new Error("Iterable cannot be null");
	return new B(function(n) {
		me(n, t, function() {
			var r = e[Symbol.asyncIterator]();
			me(n, t, function() {
				r.next().then(function(i) {
					i.done ? n.complete() : n.next(i.value);
				});
			}, 0, !0);
		});
	});
}
function ns$1(e, t) {
	return Pr(Er(e), t);
}
function rs$1(e, t) {
	if (e != null) {
		if (gr(e)) return Xo(e, t);
		if (fn(e)) return es(e, t);
		if (mr(e)) return Zo(e, t);
		if (br(e)) return Pr(e, t);
		if (Sr(e)) return ts$1(e, t);
		if (Cr(e)) return ns$1(e, t);
	}
	throw yr(e);
}
function $e$1(e, t) {
	return t ? rs$1(e, t) : se(e);
}
function Jt$1() {
	for (var e = [], t = 0; t < arguments.length; t++) e[t] = arguments[t];
	return $e$1(e, ln(e));
}
function is$1(e) {
	return e instanceof Date && !isNaN(e);
}
var os$1 = sn(function(e) {
	return function(n) {
		n === void 0 && (n = null), e(this), this.message = "Timeout has occurred", this.name = "TimeoutError", this.info = n;
	};
});
function ss(e, t) {
	var n = is$1(e) ? { first: e } : typeof e == "number" ? { each: e } : e, r = n.first, i = n.each, o = n.with, s = o === void 0 ? as : o, a = n.scheduler, c = a === void 0 ? t ?? Bo : a, u = n.meta, f = u === void 0 ? null : u;
	if (r == null && i == null) throw new TypeError("No timeout provided.");
	return Y(function(d, p) {
		var h, m, S = null, y = 0, b = function(w) {
			m = me(p, c, function() {
				try {
					h.unsubscribe(), se(s({
						meta: f,
						lastValue: S,
						seen: y
					})).subscribe(p);
				} catch (P) {
					p.error(P);
				}
			}, w);
		};
		h = d.subscribe(te(p, function(w) {
			m?.unsubscribe(), y++, p.next(S = w), i > 0 && b(i);
		}, void 0, void 0, function() {
			m?.closed || m?.unsubscribe(), S = null;
		})), !y && b(r != null ? typeof r == "number" ? r : +r - c.now() : i);
	});
}
function as(e) {
	throw new os$1(e);
}
function W$1(e, t) {
	return Y(function(n, r) {
		var i = 0;
		n.subscribe(te(r, function(o) {
			r.next(e.call(t, o, i++));
		}));
	});
}
var cs$1 = Array.isArray;
function us$1(e, t) {
	return cs$1(t) ? e.apply(void 0, Te$1([], ge$1(t))) : e(t);
}
function ls(e) {
	return W$1(function(t) {
		return us$1(e, t);
	});
}
function fs(e, t, n, r, i, o, s, a) {
	var c = [], u = 0, f = 0, d = !1, p = function() {
		d && !c.length && !u && t.complete();
	}, h = function(S) {
		return u < r ? m(S) : c.push(S);
	}, m = function(S) {
		o && t.next(S), u++;
		var y = !1;
		se(n(S, f++)).subscribe(te(t, function(b) {
			i?.(b), o ? h(b) : t.next(b);
		}, function() {
			y = !0;
		}, void 0, function() {
			if (y) try {
				u--;
				for (var b = function() {
					var w = c.shift();
					s ? me(t, s, function() {
						return m(w);
					}) : m(w);
				}; c.length && u < r;) b();
				p();
			} catch (w) {
				t.error(w);
			}
		}));
	};
	return e.subscribe(te(t, h, function() {
		d = !0, p();
	})), function() {
		a?.();
	};
}
function Ve$1(e, t, n) {
	return n === void 0 && (n = 1 / 0), $(t) ? Ve$1(function(r, i) {
		return W$1(function(o, s) {
			return t(r, o, i, s);
		})(se(e(r, i)));
	}, n) : (typeof t == "number" && (n = t), Y(function(r, i) {
		return fs(r, i, e, n);
	}));
}
function ds(e) {
	return e === void 0 && (e = 1 / 0), Ve$1(Mt, e);
}
function ps() {
	return ds(1);
}
function bt() {
	for (var e = [], t = 0; t < arguments.length; t++) e[t] = arguments[t];
	return ps()($e$1(e, ln(e)));
}
var hs = ["addListener", "removeListener"];
var vs = ["addEventListener", "removeEventListener"];
var ms = ["on", "off"];
function Yt$1(e, t, n, r) {
	if ($(n) && (r = n, n = void 0), r) return Yt$1(e, t, n).pipe(ls(r));
	var i = ge$1(ys(e) ? vs.map(function(a) {
		return function(c) {
			return e[a](t, c, n);
		};
	}) : gs(e) ? hs.map(_n(e, t)) : bs(e) ? ms.map(_n(e, t)) : [], 2), o = i[0], s = i[1];
	if (!o && fn(e)) return Ve$1(function(a) {
		return Yt$1(a, t, n);
	})(se(e));
	if (!o) throw new TypeError("Invalid event target");
	return new B(function(a) {
		var c = function() {
			for (var u = [], f = 0; f < arguments.length; f++) u[f] = arguments[f];
			return a.next(1 < u.length ? u : u[0]);
		};
		return o(c), function() {
			return s(c);
		};
	});
}
function _n(e, t) {
	return function(n) {
		return function(r) {
			return e[n](t, r);
		};
	};
}
function gs(e) {
	return $(e.addListener) && $(e.removeListener);
}
function bs(e) {
	return $(e.on) && $(e.off);
}
function ys(e) {
	return $(e.addEventListener) && $(e.removeEventListener);
}
function At(e, t) {
	return Y(function(n, r) {
		var i = 0;
		n.subscribe(te(r, function(o) {
			return e.call(t, o, i++) && r.next(o);
		}));
	});
}
function ws(e, t, n, r, i) {
	return function(o, s) {
		var a = n, c = t, u = 0;
		o.subscribe(te(s, function(f) {
			var d = u++;
			c = a ? e(c, f, d) : (a = !0, f), r && s.next(c);
		}, i && (function() {
			a && s.next(c), s.complete();
		})));
	};
}
function Ss(e, t) {
	return $(t) ? Ve$1(e, t, 1) : Ve$1(e, 1);
}
function Es(e, t) {
	return t === void 0 && (t = Mt), e = e ?? Cs, Y(function(n, r) {
		var i, o = !0;
		n.subscribe(te(r, function(s) {
			var a = t(s);
			(o || !e(i, a)) && (o = !1, i = a, r.next(s));
		}));
	});
}
function Cs(e, t) {
	return e === t;
}
function Ts(e) {
	return Y(function(t, n) {
		try {
			t.subscribe(n);
		} finally {
			n.add(e);
		}
	});
}
function ct(e, t) {
	return Y(ws(e, t, arguments.length >= 2, !0));
}
function xs(e) {
	e === void 0 && (e = {});
	var t = e.connector, n = t === void 0 ? function() {
		return new be$1();
	} : t, r = e.resetOnError, i = r === void 0 ? !0 : r, o = e.resetOnComplete, s = o === void 0 ? !0 : o, a = e.resetOnRefCountZero, c = a === void 0 ? !0 : a;
	return function(u) {
		var f, d, p, h = 0, m = !1, S = !1, y = function() {
			d?.unsubscribe(), d = void 0;
		}, b = function() {
			y(), f = p = void 0, m = S = !1;
		}, w = function() {
			var P = f;
			b(), P?.unsubscribe();
		};
		return Y(function(P, N) {
			h++, !S && !m && y();
			var M = p = p ?? n();
			N.add(function() {
				h--, h === 0 && !S && !m && (d = Bt(w, c));
			}), M.subscribe(N), !f && h > 0 && (f = new Qe({
				next: function(v) {
					return M.next(v);
				},
				error: function(v) {
					S = !0, y(), d = Bt(b, i, v), M.error(v);
				},
				complete: function() {
					m = !0, y(), d = Bt(b, s), M.complete();
				}
			}), se(P).subscribe(f));
		})(u);
	};
}
function Bt(e, t) {
	for (var n = [], r = 2; r < arguments.length; r++) n[r - 2] = arguments[r];
	if (t === !0) {
		e();
		return;
	}
	if (t !== !1) {
		var i = new Qe({ next: function() {
			i.unsubscribe(), e();
		} });
		return se(t.apply(void 0, Te$1([], ge$1(n)))).subscribe(i);
	}
}
function ks(e) {
	return Y(function(t, n) {
		var r = !1, i = te(n, function() {
			i?.unsubscribe(), r = !0;
		}, mt);
		se(e).subscribe(i), t.subscribe(te(n, function(o) {
			return r && n.next(o);
		}));
	});
}
function J$1() {
	for (var e = [], t = 0; t < arguments.length; t++) e[t] = arguments[t];
	var n = ln(e);
	return Y(function(r, i) {
		(n ? bt(e, r, n) : bt(e, r)).subscribe(i);
	});
}
function Mr(e, t) {
	return Y(function(n, r) {
		var i = null, o = 0, s = !1, a = function() {
			return s && !i && r.complete();
		};
		n.subscribe(te(r, function(c) {
			i?.unsubscribe();
			var u = 0, f = o++;
			se(e(c, f)).subscribe(i = te(r, function(d) {
				return r.next(t ? t(c, d, f, u++) : d);
			}, function() {
				i = null, a();
			}));
		}, function() {
			s = !0, a();
		}));
	});
}
function In(e) {
	return Y(function(t, n) {
		se(e).subscribe(te(n, function() {
			return n.complete();
		}, mt)), !n.closed && t.subscribe(n);
	});
}
function Ps(e, t, n) {
	var r = $(e) || t || n ? {
		next: e,
		error: t,
		complete: n
	} : e;
	return r ? Y(function(i, o) {
		var s;
		(s = r.subscribe) === null || s === void 0 || s.call(r);
		var a = !0;
		i.subscribe(te(o, function(c) {
			var u;
			(u = r.next) === null || u === void 0 || u.call(r, c), o.next(c);
		}, function() {
			var c;
			a = !1, (c = r.complete) === null || c === void 0 || c.call(r), o.complete();
		}, function(c) {
			var u;
			a = !1, (u = r.error) === null || u === void 0 || u.call(r, c), o.error(c);
		}, function() {
			var c, u;
			a && ((c = r.unsubscribe) === null || c === void 0 || c.call(r)), (u = r.finalize) === null || u === void 0 || u.call(r);
		}));
	}) : Mt;
}
var Ms = /* @__PURE__ */ f(So(), 1);
var As = Object.defineProperty;
var Ls = Object.defineProperties;
var Os = Object.getOwnPropertyDescriptors;
var Rn = Object.getOwnPropertySymbols;
var _s = Object.prototype.hasOwnProperty;
var Is = Object.prototype.propertyIsEnumerable;
var Dn = (e, t) => (t = Symbol[e]) ? t : /* @__PURE__ */ Symbol.for("Symbol." + e);
var Nn = (e, t, n) => t in e ? As(e, t, {
	enumerable: !0,
	configurable: !0,
	writable: !0,
	value: n
}) : e[t] = n;
var ae = (e, t) => {
	for (var n in t || (t = {})) _s.call(t, n) && Nn(e, n, t[n]);
	if (Rn) for (var n of Rn(t)) Is.call(t, n) && Nn(e, n, t[n]);
	return e;
};
var Pe$1 = (e, t) => Ls(e, Os(t));
var Z$1 = (e, t, n) => new Promise((r, i) => {
	var o = (c) => {
		try {
			a(n.next(c));
		} catch (u) {
			i(u);
		}
	}, s = (c) => {
		try {
			a(n.throw(c));
		} catch (u) {
			i(u);
		}
	}, a = (c) => c.done ? r(c.value) : Promise.resolve(c.value).then(o, s);
	a((n = n.apply(e, t)).next());
});
var Rs = (e, t, n) => (t = e[Dn("asyncIterator")]) ? t.call(e) : (e = e[Dn("iterator")](), t = {}, n = (r, i) => (i = e[r]) && (t[r] = (o) => new Promise((s, a, c) => (o = i.call(e, o), c = o.done, Promise.resolve(o.value).then((u) => s({
	value: u,
	done: c
}), a)))), n("next"), n("return"), t);
function ee(e) {
	return typeof e > "u" ? !1 : Ds(e) || Ns(e);
}
function Ds(e) {
	var t;
	return e ? e.hasOwnProperty("participant") && e.hasOwnProperty("source") && e.hasOwnProperty("track") && typeof ((t = e.publication) == null ? void 0 : t.track) < "u" : !1;
}
function Ns(e) {
	return e ? e.hasOwnProperty("participant") && e.hasOwnProperty("source") && e.hasOwnProperty("publication") && typeof e.publication < "u" : !1;
}
function Xe(e) {
	return e ? e.hasOwnProperty("participant") && e.hasOwnProperty("source") && typeof e.publication > "u" : !1;
}
function G(e) {
	if (typeof e == "string" || typeof e == "number") return `${e}`;
	if (Xe(e)) return `${e.participant.identity}_${e.source}_placeholder`;
	if (ee(e)) return `${e.participant.identity}_${e.publication.source}_${e.publication.trackSid}`;
	throw new Error(`Can't generate a id for the given track reference: ${e}`);
}
function Hc(e, t) {
	return e === void 0 || t === void 0 ? !1 : ee(e) && ee(t) ? e.publication.trackSid === t.publication.trackSid : G(e) === G(t);
}
function Fs(e, t) {
	return typeof t > "u" ? !1 : ee(e) ? t.some((n) => n.participant.identity === e.participant.identity && ee(n) && n.publication.trackSid === e.publication.trackSid) : Xe(e) ? t.some((n) => n.participant.identity === e.participant.identity && Xe(n) && n.source === e.source) : !1;
}
function Us(e, t) {
	return Xe(e) && ee(t) && t.participant.identity === e.participant.identity && t.source === e.source;
}
function zc() {
	const e = document.createElement("p");
	e.style.width = "100%", e.style.height = "200px";
	const t = document.createElement("div");
	t.style.position = "absolute", t.style.top = "0px", t.style.left = "0px", t.style.visibility = "hidden", t.style.width = "200px", t.style.height = "150px", t.style.overflow = "hidden", t.appendChild(e), document.body.appendChild(t);
	const n = e.offsetWidth;
	t.style.overflow = "scroll";
	let r = e.offsetWidth;
	return n === r && (r = t.clientWidth), document.body.removeChild(t), n - r;
}
function qc() {
	return typeof document < "u";
}
function $s(e) {
	e = ae({}, e);
	const t = `(?:(?:(?:[a-z]+:)?//)?|www\\.)(?:\\S+(?::\\S*)?@)?(?:localhost|${(/* @__PURE__ */ new RegExp("(?:25[0-5]|2[0-4]\\d|1\\d\\d|[1-9]\\d|\\d)(?:\\.(?:25[0-5]|2[0-4]\\d|1\\d\\d|[1-9]\\d|\\d)){3}", "g")).source}|(?:(?:[a-z\\u00a1-\\uffff0-9][-_]*)*[a-z\\u00a1-\\uffff0-9]+)(?:\\.(?:[a-z\\u00a1-\\uffff0-9]-*)*[a-z\\u00a1-\\uffff0-9]+)*(?:\\.(?:[a-z\\u00a1-\\uffff]{2,}))\\.?)(?::\\d{2,5})?(?:[/?#][^\\s"]*)?`;
	return e.exact ? new RegExp(`(?:^${t}$)`, "i") : new RegExp(t, "ig");
}
var Fn = "[^\\.\\s@:](?:[^\\s@:]*[^\\s@:\\.])?@[^\\.\\s@]+(?:\\.[^\\.\\s@]+)*";
function Ws({ exact: e } = {}) {
	return e ? new RegExp(`^${Fn}$`) : new RegExp(Fn, "g");
}
function Kc(e, t, n) {
	return mo(e, t, () => Z$1(null, null, function* () {
		const { x: r, y: i } = yield wo(e, t, {
			placement: "top",
			middleware: [
				go(6),
				yo(),
				bo({ padding: 5 })
			]
		});
		n?.(r, i);
	}));
}
function Gc(e, t) {
	return !e.contains(t.target);
}
var Jc = () => ({
	email: Ws(),
	url: $s({})
});
function Yc(e, t) {
	const n = Object.entries(t).map(([o, s], a) => Array.from(e.matchAll(s)).map(({ index: c, 0: u }) => ({
		type: o,
		weight: a,
		content: u,
		index: c ?? 0
	}))).flat().sort((o, s) => {
		const a = o.index - s.index;
		return a !== 0 ? a : o.weight - s.weight;
	}).filter(({ index: o }, s, a) => {
		if (s === 0) return !0;
		const c = a[s - 1];
		return c.index + c.content.length <= o;
	}), r = [];
	let i = 0;
	for (const { type: o, content: s, index: a } of n) a > i && r.push(e.substring(i, a)), r.push({
		type: o,
		content: s
	}), i = a + s.length;
	return e.length > i && r.push(e.substring(i)), r;
}
var Ar = [
	...[
		RoomEvent.ConnectionStateChanged,
		RoomEvent.RoomMetadataChanged,
		RoomEvent.ActiveSpeakersChanged,
		RoomEvent.ConnectionQualityChanged,
		RoomEvent.ParticipantConnected,
		RoomEvent.ParticipantDisconnected,
		RoomEvent.ParticipantPermissionsChanged,
		RoomEvent.ParticipantMetadataChanged,
		RoomEvent.ParticipantNameChanged,
		RoomEvent.ParticipantAttributesChanged,
		RoomEvent.TrackMuted,
		RoomEvent.TrackUnmuted,
		RoomEvent.TrackPublished,
		RoomEvent.TrackUnpublished,
		RoomEvent.TrackStreamStateChanged,
		RoomEvent.TrackSubscriptionFailed,
		RoomEvent.TrackSubscriptionPermissionChanged,
		RoomEvent.TrackSubscriptionStatusChanged
	],
	RoomEvent.LocalTrackPublished,
	RoomEvent.LocalTrackUnpublished
];
var Bs = [
	ParticipantEvent.TrackPublished,
	ParticipantEvent.TrackUnpublished,
	ParticipantEvent.TrackMuted,
	ParticipantEvent.TrackUnmuted,
	ParticipantEvent.TrackStreamStateChanged,
	ParticipantEvent.TrackSubscribed,
	ParticipantEvent.TrackUnsubscribed,
	ParticipantEvent.TrackSubscriptionPermissionChanged,
	ParticipantEvent.TrackSubscriptionFailed,
	ParticipantEvent.LocalTrackPublished,
	ParticipantEvent.LocalTrackUnpublished
];
var Lr = [
	...[
		ParticipantEvent.ConnectionQualityChanged,
		ParticipantEvent.IsSpeakingChanged,
		ParticipantEvent.ParticipantMetadataChanged,
		ParticipantEvent.ParticipantPermissionsChanged,
		ParticipantEvent.TrackMuted,
		ParticipantEvent.TrackUnmuted,
		ParticipantEvent.TrackPublished,
		ParticipantEvent.TrackUnpublished,
		ParticipantEvent.TrackStreamStateChanged,
		ParticipantEvent.TrackSubscriptionFailed,
		ParticipantEvent.TrackSubscriptionPermissionChanged,
		ParticipantEvent.TrackSubscriptionStatusChanged
	],
	ParticipantEvent.LocalTrackPublished,
	ParticipantEvent.LocalTrackUnpublished
];
var R = Ms.default.getLogger("lk-components-js");
R.setDefaultLevel("WARN");
function Qc(e, t = {}) {
	var n;
	R.setLevel(e), setLogLevel((n = t.liveKitClientLogLevel) != null ? n : e);
}
function Xc(e, t = {}) {
	var n;
	const r = R.methodFactory;
	R.methodFactory = (i, o, s) => {
		const a = r(i, o, s), c = LogLevel[i], u = c >= o && c < LogLevel.silent;
		return (f, d) => {
			d ? a(f, d) : a(f), u && e(c, f, d);
		};
	}, R.setLevel(R.getLevel()), setLogExtension((n = t.liveKitClientLogExtension) != null ? n : e);
}
var Hs = [
	{
		columns: 1,
		rows: 1
	},
	{
		columns: 1,
		rows: 2,
		orientation: "portrait"
	},
	{
		columns: 2,
		rows: 1,
		orientation: "landscape"
	},
	{
		columns: 2,
		rows: 2,
		minWidth: 560
	},
	{
		columns: 3,
		rows: 3,
		minWidth: 700
	},
	{
		columns: 4,
		rows: 4,
		minWidth: 960
	},
	{
		columns: 5,
		rows: 5,
		minWidth: 1100
	}
];
function Or(e, t, n, r) {
	if (e.length < 1) throw new Error("At least one grid layout definition must be provided.");
	const i = zs(e);
	if (n <= 0 || r <= 0) return i[0];
	let o = 0;
	const s = n / r > 1 ? "landscape" : "portrait";
	let a = i.find((c, u, f) => {
		o = u;
		const d = f.findIndex((p, h) => {
			const m = !p.orientation || p.orientation === s, S = h > u, y = p.maxTiles === c.maxTiles;
			return S && y && m;
		}) !== -1;
		return c.maxTiles >= t && !d;
	});
	if (a === void 0) if (a = i[i.length - 1], a) R.warn(`No layout found for: participantCount: ${t}, width/height: ${n}/${r} fallback to biggest available layout (${a}).`);
	else throw new Error("No layout or fallback layout found.");
	if ((n < a.minWidth || r < a.minHeight) && o > 0) {
		const c = i[o - 1];
		a = Or(i.slice(0, o), c.maxTiles, n, r);
	}
	return a;
}
function zs(e) {
	return [...e].map((t) => {
		var n, r;
		return {
			name: `${t.columns}x${t.rows}`,
			columns: t.columns,
			rows: t.rows,
			maxTiles: t.columns * t.rows,
			minWidth: (n = t.minWidth) != null ? n : 0,
			minHeight: (r = t.minHeight) != null ? r : 0,
			orientation: t.orientation
		};
	}).sort((t, n) => t.maxTiles !== n.maxTiles ? t.maxTiles - n.maxTiles : t.minWidth !== 0 || n.minWidth !== 0 ? t.minWidth - n.minWidth : t.minHeight !== 0 || n.minHeight !== 0 ? t.minHeight - n.minHeight : 0);
}
function Zc() {
	return typeof navigator < "u" && navigator.mediaDevices && !!navigator.mediaDevices.getDisplayMedia;
}
function qs(e, t) {
	var n;
	return Pe$1(ae({}, e), {
		receivedAtMediaTimestamp: (n = t.rtpTimestamp) != null ? n : 0,
		receivedAt: t.timestamp
	});
}
function Ks(e, t, n) {
	return [...e, ...t].reduceRight((r, i) => (r.find((o) => o.id === i.id) || r.unshift(i), r), []).slice(0 - n);
}
var ce$1 = /* @__PURE__ */ ((e) => (e.AgentState = "lk.agent.state", e.PublishOnBehalf = "lk.publish_on_behalf", e.TranscriptionFinal = "lk.transcription_final", e.TranscriptionSegmentId = "lk.segment_id", e.TranscribedTrackId = "lk.transcribed_track_id", e.Expression = "lk.expression", e))(ce$1 || {});
var _r = [];
var Ir = {
	showChat: !1,
	unreadMessages: 0,
	showSettings: !1
};
function Rr(e) {
	return typeof e == "object";
}
function Dr(e) {
	return Array.isArray(e) && e.filter(Rr).length > 0;
}
function Nr(e, t) {
	return t.audioLevel - e.audioLevel;
}
function Fr(e, t) {
	return e.isSpeaking === t.isSpeaking ? 0 : e.isSpeaking ? -1 : 1;
}
function Ur(e, t) {
	var n, r, i, o;
	return e.lastSpokeAt !== void 0 || t.lastSpokeAt !== void 0 ? ((r = (n = t.lastSpokeAt) == null ? void 0 : n.getTime()) != null ? r : 0) - ((o = (i = e.lastSpokeAt) == null ? void 0 : i.getTime()) != null ? o : 0) : 0;
}
function yt(e, t) {
	var n, r, i, o;
	return ((r = (n = e.joinedAt) == null ? void 0 : n.getTime()) != null ? r : 0) - ((o = (i = t.joinedAt) == null ? void 0 : i.getTime()) != null ? o : 0);
}
function Gs(e, t) {
	return ee(e) ? ee(t) ? 0 : -1 : ee(t) ? 1 : 0;
}
function Js(e, t) {
	const n = e.participant.isCameraEnabled;
	return n !== t.participant.isCameraEnabled ? n ? -1 : 1 : 0;
}
function Ys(e) {
	const t = [], n = [], r = [], i = [];
	e.forEach((a) => {
		a.participant.isLocal && a.source === Track.Source.Camera ? t.push(a) : a.source === Track.Source.ScreenShare ? n.push(a) : a.source === Track.Source.Camera ? r.push(a) : i.push(a);
	});
	const o = Qs(n), s = Xs(r);
	return [
		...t,
		...o,
		...s,
		...i
	];
}
function Qs(e) {
	const t = [], n = [];
	return e.forEach((r) => {
		r.participant.isLocal ? t.push(r) : n.push(r);
	}), t.sort((r, i) => yt(r.participant, i.participant)), n.sort((r, i) => yt(r.participant, i.participant)), [...n, ...t];
}
function Xs(e) {
	const t = [], n = [];
	return e.forEach((r) => {
		r.participant.isLocal ? t.push(r) : n.push(r);
	}), n.sort((r, i) => r.participant.isSpeaking && i.participant.isSpeaking ? Nr(r.participant, i.participant) : r.participant.isSpeaking !== i.participant.isSpeaking ? Fr(r.participant, i.participant) : r.participant.lastSpokeAt !== i.participant.lastSpokeAt ? Ur(r.participant, i.participant) : ee(r) !== ee(i) ? Gs(r, i) : r.participant.isCameraEnabled !== i.participant.isCameraEnabled ? Js(r, i) : yt(r.participant, i.participant)), [...t, ...n];
}
function Un(e) {
	const t = [...e];
	t.sort((r, i) => {
		if (r.isSpeaking && i.isSpeaking) return Nr(r, i);
		if (r.isSpeaking !== i.isSpeaking) return Fr(r, i);
		if (r.lastSpokeAt !== i.lastSpokeAt) return Ur(r, i);
		const o = r.videoTrackPublications.size > 0;
		return o !== i.videoTrackPublications.size > 0 ? o ? -1 : 1 : yt(r, i);
	});
	const n = t.find((r) => r.isLocal);
	if (n) {
		const r = t.indexOf(n);
		r >= 0 && (t.splice(r, 1), t.length > 0 ? t.splice(0, 0, n) : t.push(n));
	}
	return t;
}
function Zs(e, t) {
	return e.reduce((n, r, i) => i % t === 0 ? [...n, [r]] : [...n.slice(0, -1), [...n.slice(-1)[0], r]], []);
}
function $n(e, t) {
	const n = Math.max(e.length, t.length);
	return new Array(n).fill([]).map((r, i) => [e[i], t[i]]);
}
function wt(e, t, n) {
	return e.filter((r) => !t.map((i) => n(i)).includes(n(r)));
}
function Qt$1(e) {
	return e.map((t) => typeof t == "string" || typeof t == "number" ? `${t}` : G(t));
}
function ea$1(e, t) {
	return {
		dropped: wt(e, t, G),
		added: wt(t, e, G)
	};
}
function ta$1(e) {
	return e.added.length !== 0 || e.dropped.length !== 0;
}
function Xt$1(e, t) {
	const n = t.findIndex((r) => G(r) === G(e));
	if (n === -1) throw new Error(`Element not part of the array: ${G(e)} not in ${Qt$1(t)}`);
	return n;
}
function na$1(e, t, n) {
	const r = Xt$1(e, n), i = Xt$1(t, n);
	return n.splice(r, 1, t), n.splice(i, 1, e), n;
}
function ra$1(e, t) {
	const n = Xt$1(e, t);
	return t.splice(n, 1), t;
}
function ia$1(e, t) {
	return [...t, e];
}
function Vt(e, t) {
	return Zs(e, t);
}
function oa$1(e, t, n) {
	let r = sa$1(e, t);
	if (r.length < t.length) {
		const i = wt(t, r, G);
		r = [...r, ...i];
	}
	if ($n(Vt(r, n), Vt(t, n)).forEach(([i, o], s) => {
		if (i && o) {
			const a = Vt(r, n)[s], c = ea$1(a, o);
			ta$1(c) && (R.debug(`Detected visual changes on page: ${s}, current: ${Qt$1(i)}, next: ${Qt$1(o)}`, { changes: c }), c.added.length === c.dropped.length && $n(c.added, c.dropped).forEach(([u, f]) => {
				if (u && f) r = na$1(u, f, r);
				else throw new Error(`For a swap action we need a addition and a removal one is missing: ${u}, ${f}`);
			}), c.added.length === 0 && c.dropped.length > 0 && c.dropped.forEach((u) => {
				r = ra$1(u, r);
			}), c.added.length > 0 && c.dropped.length === 0 && c.added.forEach((u) => {
				r = ia$1(u, r);
			}));
		}
	}), r.length > t.length) {
		const i = wt(r, t, G);
		r = r.filter((o) => !i.map(G).includes(G(o)));
	}
	return r;
}
function sa$1(e, t) {
	return e.map((n) => {
		return t.find((i) => G(n) === G(i) || typeof n != "number" && Xe(n) && ee(i) && Us(n, i)) ?? n;
	});
}
function ne(e) {
	return `lk-${e}`;
}
function aa$1(e) {
	const t = Zt$1(e), n = dn(e.participant).pipe(W$1(() => Zt$1(e)), J$1(t));
	return {
		className: ne(e.source === Track.Source.Camera || e.source === Track.Source.ScreenShare ? "participant-media-video" : "participant-media-audio"),
		trackObserver: n
	};
}
function Zt$1(e) {
	if (ee(e)) return e.publication;
	{
		const { source: t, name: n, participant: r } = e;
		if (t && n) return r.getTrackPublications().find((i) => i.source === t && i.trackName === n);
		if (n) return r.getTrackPublicationByName(n);
		if (t) return r.getTrackPublication(t);
		throw new Error("At least one of source and name needs to be defined");
	}
}
function _e$1(e, ...t) {
	return new B((n) => {
		const r = () => {
			n.next(e);
		};
		return t.forEach((o) => {
			e.on(o, r);
		}), () => {
			t.forEach((o) => {
				e.off(o, r);
			});
		};
	}).pipe(J$1(e));
}
function ze$1(e, t) {
	return new B((n) => {
		const r = (...o) => {
			n.next(o);
		};
		return e.on(t, r), () => {
			e.off(t, r);
		};
	});
}
function ca$1(e) {
	return ze$1(e, RoomEvent.ConnectionStateChanged).pipe(W$1(([t]) => t), J$1(e.state));
}
function ua$1(e) {
	return _e$1(e, RoomEvent.RoomMetadataChanged, RoomEvent.ConnectionStateChanged).pipe(W$1((t) => ({
		name: t.name,
		metadata: t.metadata
	})));
}
function la$1(e) {
	return ze$1(e, RoomEvent.ActiveSpeakersChanged).pipe(W$1(([t]) => t));
}
function $r(e, t, n = !0) {
	return bt(new B((r) => {
		Room.getLocalDevices(e, n).then((i) => {
			r.next(i), r.complete();
		}).catch((i) => {
			t?.(i), r.next([]), r.complete();
		});
	}), new B((r) => {
		var i;
		const o = () => Z$1(null, null, function* () {
			try {
				const s = yield Room.getLocalDevices(e, n);
				r.next(s);
			} catch (s) {
				t?.(s);
			}
		});
		if (typeof window < "u") {
			if (!window.isSecureContext) throw new Error("Accessing media devices is available only in secure contexts (HTTPS and localhost), in some or all supporting browsers. See: https://developer.mozilla.org/en-US/docs/Web/API/Navigator/mediaDevices");
			(i = navigator?.mediaDevices) == null || i.addEventListener("devicechange", o);
		}
		return () => {
			var s;
			(s = navigator?.mediaDevices) == null || s.removeEventListener("devicechange", o);
		};
	}));
}
function fa$1(e) {
	return ze$1(e, RoomEvent.DataReceived);
}
function Wr(e) {
	return _e$1(e, RoomEvent.AudioPlaybackStatusChanged).pipe(W$1((t) => ({ canPlayAudio: t.canPlaybackAudio })));
}
function da$1(e) {
	return _e$1(e, RoomEvent.VideoPlaybackStatusChanged).pipe(W$1((t) => ({ canPlayVideo: t.canPlaybackVideo })));
}
function pa$1(e, t) {
	return ze$1(e, RoomEvent.ActiveDeviceChanged).pipe(At(([n]) => n === t), W$1(([n, r]) => (R.debug("activeDeviceObservable | RoomEvent.ActiveDeviceChanged", {
		kind: n,
		deviceId: r
	}), r)));
}
function ha$1(e, t) {
	return ze$1(e, RoomEvent.ParticipantEncryptionStatusChanged).pipe(At(([, n]) => t?.identity === n?.identity || !n && t?.identity === e.localParticipant.identity), W$1(([n]) => n), J$1(t?.isLocal ? t.isE2EEEnabled : !!t?.isEncrypted));
}
function va$1(e) {
	return ze$1(e, RoomEvent.RecordingStatusChanged).pipe(W$1(([t]) => t), J$1(e.isRecording));
}
function qe(e, ...t) {
	return new B((n) => {
		const r = () => {
			n.next(e);
		};
		return t.forEach((o) => {
			e.on(o, r);
		}), () => {
			t.forEach((o) => {
				e.off(o, r);
			});
		};
	}).pipe(J$1(e));
}
function dn(e) {
	return qe(e, ParticipantEvent.TrackMuted, ParticipantEvent.TrackUnmuted, ParticipantEvent.ParticipantPermissionsChanged, ParticipantEvent.TrackPublished, ParticipantEvent.TrackUnpublished, ParticipantEvent.LocalTrackPublished, ParticipantEvent.LocalTrackUnpublished, ParticipantEvent.MediaDevicesError, ParticipantEvent.TrackSubscriptionStatusChanged).pipe(W$1((t) => {
		const { isMicrophoneEnabled: n, isCameraEnabled: r, isScreenShareEnabled: i } = t, o = t.getTrackPublication(Track.Source.Microphone);
		return {
			isCameraEnabled: r,
			isMicrophoneEnabled: n,
			isScreenShareEnabled: i,
			cameraTrack: t.getTrackPublication(Track.Source.Camera),
			microphoneTrack: o,
			participant: t
		};
	}));
}
function jr(e) {
	if (e) return qe(e, ParticipantEvent.ParticipantMetadataChanged, ParticipantEvent.ParticipantNameChanged).pipe(W$1(({ name: t, identity: n, metadata: r }) => ({
		name: t,
		identity: n,
		metadata: r
	})), J$1({
		name: e.name,
		identity: e.identity,
		metadata: e.metadata
	}));
}
function ma(e) {
	return Lt(e, ParticipantEvent.ConnectionQualityChanged).pipe(W$1(([t]) => t), J$1(e.connectionQuality));
}
function Lt(e, t) {
	return new B((n) => {
		const r = (...o) => {
			n.next(o);
		};
		return e.on(t, r), () => {
			e.off(t, r);
		};
	});
}
function Br(e) {
	var t, n, r, i;
	return qe(e.participant, ParticipantEvent.TrackMuted, ParticipantEvent.TrackUnmuted, ParticipantEvent.TrackSubscribed, ParticipantEvent.TrackUnsubscribed, ParticipantEvent.LocalTrackPublished, ParticipantEvent.LocalTrackUnpublished).pipe(W$1((o) => {
		var s, a;
		return (a = ((s = e.publication) != null ? s : o.getTrackPublication(e.source))?.isMuted) != null ? a : !0;
	}), J$1((i = (r = (t = e.publication) == null ? void 0 : t.isMuted) != null ? r : (n = e.participant.getTrackPublication(e.source)) == null ? void 0 : n.isMuted) != null ? i : !0));
}
function ga$1(e) {
	return Lt(e, ParticipantEvent.IsSpeakingChanged).pipe(W$1(([t]) => t));
}
function ba$1(e, t = {}) {
	var n;
	let r;
	const i = new B((a) => (r = a, () => s.unsubscribe())).pipe(J$1(Array.from(e.remoteParticipants.values()))), o = (n = t.additionalRoomEvents) != null ? n : Ar, s = _e$1(e, ...Array.from(/* @__PURE__ */ new Set([
		RoomEvent.ParticipantConnected,
		RoomEvent.ParticipantDisconnected,
		RoomEvent.ConnectionStateChanged,
		...o
	]))).subscribe((a) => r?.next(Array.from(a.remoteParticipants.values())));
	return e.remoteParticipants.size > 0 && r?.next(Array.from(e.remoteParticipants.values())), i;
}
function ya$1(e, t, n = {}) {
	var r;
	const i = (r = n.additionalEvents) != null ? r : Lr;
	return _e$1(e, RoomEvent.ParticipantConnected, RoomEvent.ParticipantDisconnected, RoomEvent.ConnectionStateChanged).pipe(Mr((o) => {
		const s = o.getParticipantByIdentity(t);
		return s ? qe(s, ...i) : new B((a) => a.next(void 0));
	}), J$1(e.getParticipantByIdentity(t)));
}
function Vr(e) {
	return Lt(e, ParticipantEvent.ParticipantPermissionsChanged).pipe(W$1(() => e.permissions), J$1(e.permissions));
}
function wa$1(e, { kind: t, identity: n }, r = {}) {
	var i;
	const o = (i = r.additionalEvents) != null ? i : Lr, s = (a) => {
		let c = !0;
		return t && (c = c && a.kind === t), n && (c = c && a.identity === n), c;
	};
	return _e$1(e, RoomEvent.ParticipantConnected, RoomEvent.ParticipantDisconnected, RoomEvent.ConnectionStateChanged).pipe(Mr((a) => {
		const c = Array.from(a.remoteParticipants.values()).find((u) => s(u));
		return c ? qe(c, ...o) : new B((u) => u.next(void 0));
	}), J$1(Array.from(e.remoteParticipants.values()).find((a) => s(a))));
}
function en(e) {
	return typeof e > "u" ? new B() : Lt(e, ParticipantEvent.AttributesChanged).pipe(W$1(([t]) => ({
		changed: t,
		attributes: e.attributes
	})), J$1({
		changed: e.attributes,
		attributes: e.attributes
	}));
}
function Sa$1(e, t, n, r, i) {
	const { localParticipant: o } = t, s = (f, d) => {
		let p = !1;
		switch (f) {
			case Track.Source.Camera:
				p = d.isCameraEnabled;
				break;
			case Track.Source.Microphone:
				p = d.isMicrophoneEnabled;
				break;
			case Track.Source.ScreenShare: p = d.isScreenShareEnabled;
		}
		return p;
	}, a = dn(o).pipe(W$1((f) => s(e, f.participant)), J$1(s(e, o))), c = new be$1(), u = (f, d) => Z$1(null, null, function* () {
		try {
			switch (d ?? (d = n), c.next(!0), e) {
				case Track.Source.Camera: return yield o.setCameraEnabled(f ?? !o.isCameraEnabled, d, r), o.isCameraEnabled;
				case Track.Source.Microphone: return yield o.setMicrophoneEnabled(f ?? !o.isMicrophoneEnabled, d, r), o.isMicrophoneEnabled;
				case Track.Source.ScreenShare: return yield o.setScreenShareEnabled(f ?? !o.isScreenShareEnabled, d, r), o.isScreenShareEnabled;
				default: throw new TypeError("Tried to toggle unsupported source");
			}
		} catch (p) {
			if (i && p instanceof Error) {
				i?.(p);
				return;
			} else throw p;
		} finally {
			c.next(!1);
		}
	});
	return {
		className: ne("button"),
		toggle: u,
		enabledObserver: a,
		pendingObserver: c.asObservable()
	};
}
function Ea$1() {
	let e = !1;
	const t = new be$1(), n = new be$1(), r = (i) => Z$1(null, null, function* () {
		n.next(!0), e = i ?? !e, t.next(e), n.next(!1);
	});
	return {
		className: ne("button"),
		toggle: r,
		enabledObserver: t.asObservable(),
		pendingObserver: n.asObservable()
	};
}
function Ca$1(e, t, n) {
	const r = new hr(void 0), i = pa$1(t, e), o = (s, ...a) => Z$1(null, [s, ...a], function* (c, u = {}) {
		var f, d, p;
		if (n) {
			yield n.setDeviceId(u.exact ? { exact: c } : c);
			const h = yield n.getDeviceId(!1);
			r.next(c === "default" && n.mediaStreamTrack.label.startsWith("Default") ? c : h);
		} else if (t) {
			const h = getBrowser();
			if (e === "audiooutput" && (h?.name === "Safari" || h?.os === "iOS")) {
				R.warn("Switching audio output device is not supported on Safari and iOS.");
				return;
			}
			R.debug(`Switching active device of kind "${e}" with id ${c}.`), yield t.switchActiveDevice(e, c, u.exact);
			const m = (f = t.getActiveDevice(e)) != null ? f : c;
			m !== c && c !== "default" && R.info(`We tried to select the device with id (${c}), but the browser decided to select the device with id (${m}) instead.`);
			let S;
			e === "audioinput" ? S = (d = t.localParticipant.getTrackPublication(Track.Source.Microphone)) == null ? void 0 : d.track : e === "videoinput" && (S = (p = t.localParticipant.getTrackPublication(Track.Source.Camera)) == null ? void 0 : p.track);
			const y = c === "default" && !S || c === "default" && S?.mediaStreamTrack.label.startsWith("Default");
			r.next(y ? c : m);
		}
	});
	return {
		className: ne("media-device-select"),
		activeDeviceObservable: i,
		setActiveMediaDevice: o
	};
}
function Ta$1(e) {
	const t = (n) => {
		e.disconnect(n);
	};
	return {
		className: ne("disconnect-button"),
		disconnect: t
	};
}
function xa$1(e) {
	return {
		className: ne("connection-quality"),
		connectionQualityObserver: ma(e)
	};
}
function ka$1(e) {
	let t = "track-muted-indicator-camera";
	switch (e.source) {
		case Track.Source.Camera:
			t = "track-muted-indicator-camera";
			break;
		case Track.Source.Microphone: t = "track-muted-indicator-microphone";
	}
	return {
		className: ne(t),
		mediaMutedObserver: Br(e)
	};
}
function eu(e) {
	return {
		className: "lk-participant-name",
		infoObserver: jr(e)
	};
}
function Pa$1() {
	return { className: ne("participant-tile") };
}
var Hr = {
	CHAT: "lk.chat",
	TRANSCRIPTION: "lk.transcription"
};
var Ma$1 = { CHAT: "lk-chat-topic" };
function zr(e, t) {
	return Z$1(this, arguments, function* (n, r, i = {}) {
		const { reliable: o, destinationIdentities: s, topic: a } = i;
		yield n.publishData(r, {
			destinationIdentities: s,
			topic: a,
			reliable: o
		});
	});
}
function qr(e, t, n) {
	const r = Array.isArray(t) ? t : [t], i = fa$1(e).pipe(At(([, , , c]) => t === void 0 || c !== void 0 && r.includes(c)), W$1(([c, u, , f]) => {
		const d = {
			payload: c,
			topic: f,
			from: u
		};
		return n?.(d), d;
	}));
	let o;
	return {
		messageObservable: i,
		isSendingObservable: new B((c) => {
			o = c;
		}),
		send: (c, ...u) => Z$1(null, [c, ...u], function* (f, d = {}) {
			o.next(!0);
			try {
				yield zr(e.localParticipant, f, ae({ topic: r[0] }, d));
			} finally {
				o.next(!1);
			}
		})
	};
}
var Aa$1 = class {
	constructor(e, t) {
		this._isResolved = !1, this.onFinally = t, this.promise = new Promise((n, r) => Z$1(this, null, function* () {
			this.resolve = n, this.reject = r, e && (yield e(n, r));
		})).finally(() => {
			var n;
			this._isResolved = !0, (n = this.onFinally) == null || n.call(this);
		});
	}
	get isResolved() {
		return this._isResolved;
	}
};
var ot = /* @__PURE__ */ new WeakMap();
var st = /* @__PURE__ */ new Map();
function La$1(e) {
	return e.ignoreLegacy == !0;
}
var Oa$1 = (e) => JSON.parse(new TextDecoder().decode(e));
var _a$1 = (e) => new TextEncoder().encode(JSON.stringify(e));
function Ia$1(e, t) {
	var n, r, i, o, s, a;
	const c = () => {
		var M, v, g;
		return ((M = e.serverInfo) == null ? void 0 : M.edition) === 1 || !!((v = e.serverInfo) != null && v.version) && compareVersions((g = e.serverInfo) == null ? void 0 : g.version, "1.8.2") > 0;
	}, u = new be$1(), f = (n = t?.channelTopic) != null ? n : Hr.CHAT, d = (r = t?.channelTopic) != null ? r : Ma$1.CHAT;
	let p = !1;
	ot.has(e) || (p = !0);
	const h = (i = ot.get(e)) != null ? i : /* @__PURE__ */ new Map(), m = (o = h.get(f)) != null ? o : new be$1();
	h.set(f, m), ot.set(e, h);
	const S = (s = t?.messageDecoder) != null ? s : Oa$1;
	if (p) {
		e.registerTextStreamHandler(f, (v, g) => Z$1(null, null, function* () {
			const { id: E, timestamp: x, attributes: C, attachedStreamIds: I } = v.info, T = new Map((I ?? []).map((A) => [A, new Aa$1()]));
			st.set(E, T), $e$1(v).pipe(ct((A, D) => A + D), Ve$1((A) => T.size === 0 ? Jt$1({
				chunk: A,
				attachedFiles: []
			}) : $e$1(T.values()).pipe(Ss((D) => $e$1(D.promise)), ct((D, U) => [...D, new File(U.buffer, U.fileName, { type: U.mimeType })], []), W$1((D) => ({
				chunk: A,
				attachedFiles: D
			})))), W$1(({ chunk: A, attachedFiles: D }) => ({
				id: E,
				timestamp: x,
				message: A,
				from: e.getParticipantByIdentity(g.identity),
				type: "chatMessage",
				attributes: C,
				attachedFiles: D
			})), Ts(() => st.delete(E))).subscribe({ next: (A) => m.next(A) });
		})), e.registerByteStreamHandler(f, (v) => Z$1(null, null, function* () {
			var g, E;
			const { id: x } = v.info, C = Array.from(st).find(([, V]) => V.has(x));
			if (!C) return;
			const I = C[0], T = [];
			try {
				for (var A = Rs(v), D, U, H; D = !(U = yield A.next()).done; D = !1) {
					const V = U.value;
					T.push(V);
				}
			} catch (V) {
				H = [V];
			} finally {
				try {
					D && (U = A.return) && (yield U.call(A));
				} finally {
					if (H) throw H[0];
				}
			}
			const z = (g = st.get(I)) == null ? void 0 : g.get(x);
			z && ((E = z.resolve) == null || E.call(z, {
				fileName: v.info.name,
				mimeType: v.info.mimeType,
				buffer: T
			}));
		}));
		const { messageObservable: M } = qr(e, [d]);
		M.pipe(W$1((v) => {
			const g = S(v.payload);
			if (!La$1(g)) return Pe$1(ae({}, g), {
				type: "chatMessage",
				from: v.from
			});
		}), At((v) => !!v), In(u)).subscribe(m);
	}
	const y = m.pipe(ct((M, v) => {
		if ("id" in v && M.find((g) => {
			var E, x;
			return ((E = g.from) == null ? void 0 : E.identity) === ((x = v.from) == null ? void 0 : x.identity) && g.id === v.id;
		})) {
			const g = M.findIndex((E) => E.id === v.id);
			if (g > -1) {
				const E = M[g];
				M[g] = Pe$1(ae({}, v), {
					timestamp: E.timestamp,
					editTimestamp: v.timestamp
				});
			}
			return [...M];
		}
		return [...M, v];
	}, []), In(u)), b = new hr(!1), w = (a = t?.messageEncoder) != null ? a : _a$1, P = (M, v) => Z$1(null, null, function* () {
		v || (v = {}), v.topic ?? (v.topic = f), b.next(!0);
		try {
			const g = {
				id: (yield e.localParticipant.sendText(M, v)).id,
				timestamp: Date.now(),
				message: M
			}, E = Pe$1(ae({}, Pe$1(ae({}, g), { attachedFiles: v.attachments })), {
				type: "chatMessage",
				from: e.localParticipant,
				attributes: v.attributes
			});
			m.next(E);
			const x = w(Pe$1(ae({}, g), { ignoreLegacy: c() }));
			try {
				yield zr(e.localParticipant, x, {
					reliable: !0,
					topic: d
				});
			} catch (C) {
				R.info("could not send message in legacy chat format", C);
			}
			return E;
		} finally {
			b.next(!1);
		}
	});
	function N() {
		u.next(), u.complete(), m.complete(), ot.delete(e), e.unregisterTextStreamHandler(f), e.unregisterByteStreamHandler(f);
	}
	return e.once(RoomEvent.Disconnected, N), {
		messageObservable: y,
		isSendingObservable: b,
		send: P
	};
}
function Ra$1() {
	const e = (t) => Z$1(null, null, function* () {
		R.info("Start Audio for room: ", t), yield t.startAudio();
	});
	return {
		className: ne("start-audio-button"),
		roomAudioPlaybackAllowedObservable: Wr,
		handleStartAudioPlayback: e
	};
}
function Da$1() {
	const e = (t) => Z$1(null, null, function* () {
		R.info("Start Video for room: ", t), yield t.startVideo();
	});
	return {
		className: ne("start-audio-button"),
		roomVideoPlaybackAllowedObservable: da$1,
		handleStartVideoPlayback: e
	};
}
function Na$1() {
	return { className: [ne("button"), ne("chat-toggle")].join(" ") };
}
function Fa$1() {
	return { className: [ne("button"), ne("focus-toggle-button")].join(" ") };
}
function Ua() {
	return { className: "lk-clear-pin-button lk-button" };
}
function $a$1() {
	return { className: "lk-room-container" };
}
function Wn(e, t, n = !0) {
	const r = [e.localParticipant, ...Array.from(e.remoteParticipants.values())], i = [];
	return r.forEach((o) => {
		t.forEach((s) => {
			const a = Array.from(o.trackPublications.values()).filter((c) => c.source === s && (!n || c.track)).map((c) => ({
				participant: o,
				publication: c,
				source: c.source
			}));
			i.push(...a);
		});
	}), {
		trackReferences: i,
		participants: r
	};
}
function jn(e, t, n = !1) {
	const { sources: r, kind: i, name: o } = t;
	return Array.from(e.trackPublications.values()).filter((s) => (!r || r.includes(s.source)) && (!i || s.kind === i) && (!o || s.trackName === o) && (!n || s.track)).map((s) => ({
		participant: e,
		publication: s,
		source: s.source
	}));
}
function Wa(e, t, n) {
	var r, i;
	const o = (r = n.additionalRoomEvents) != null ? r : Ar, s = (i = n.onlySubscribed) != null ? i : !0;
	return _e$1(e, ...Array.from((/* @__PURE__ */ new Set([
		RoomEvent.ParticipantConnected,
		RoomEvent.ParticipantDisconnected,
		RoomEvent.ConnectionStateChanged,
		RoomEvent.LocalTrackPublished,
		RoomEvent.LocalTrackUnpublished,
		RoomEvent.TrackPublished,
		RoomEvent.TrackUnpublished,
		RoomEvent.TrackSubscriptionStatusChanged,
		...o
	])).values())).pipe(W$1((a) => {
		const c = Wn(a, t, s);
		return R.debug(`TrackReference[] was updated. (length ${c.trackReferences.length})`, c), c;
	}), J$1(Wn(e, t, s)));
}
function ja(e, t) {
	return qe(e, ...Bs).pipe(W$1((n) => {
		const r = jn(n, t);
		return R.debug(`TrackReference[] was updated. (length ${r.length})`, r), r;
	}), J$1(jn(e, t)));
}
function Kr(e, t) {
	return new B((n) => {
		const r = (...o) => {
			n.next(o);
		};
		return e.on(t, r), () => {
			e.off(t, r);
		};
	});
}
function Ba$1(e) {
	return Kr(e, TrackEvent.TranscriptionReceived);
}
function Va$1(e) {
	return Kr(e, TrackEvent.TimeSyncUpdate).pipe(W$1(([t]) => t));
}
function tu(e, t = 1e3) {
	if (e === null) return Jt$1(!1);
	const n = Yt$1(e, "mousemove", { passive: !0 }).pipe(W$1(() => !0)), r = n.pipe(ss({
		each: t,
		with: () => bt(Jt$1(!1), r.pipe(ks(n)))
	}), Es());
	return r;
}
function Ha(e, t) {
	if (typeof localStorage > "u") {
		R.error("Local storage is not available.");
		return;
	}
	try {
		if (t) {
			const n = Object.fromEntries(Object.entries(t).filter(([, r]) => r !== ""));
			localStorage.setItem(e, JSON.stringify(n));
		}
	} catch (n) {
		R.error(`Error setting item to local storage: ${n}`);
	}
}
function za$1(e) {
	if (typeof localStorage > "u") {
		R.error("Local storage is not available.");
		return;
	}
	try {
		const t = localStorage.getItem(e);
		if (!t) {
			R.warn(`Item with key ${e} does not exist in local storage.`);
			return;
		}
		return JSON.parse(t);
	} catch (t) {
		R.error(`Error getting item from local storage: ${t}`);
		return;
	}
}
function qa(e) {
	return {
		load: () => za$1(e),
		save: (t) => Ha(e, t)
	};
}
var Ka$1 = "lk-user-choices", Ge = {
	videoEnabled: !0,
	audioEnabled: !0,
	videoDeviceId: "default",
	audioDeviceId: "default",
	username: ""
}, { load: Ga$1, save: Ja$1 } = qa(Ka$1);
function Ya(e, t = !1) {
	t !== !0 && Ja$1(e);
}
function Qa$1(e, t = !1) {
	var n, r, i, o, s;
	const a = {
		videoEnabled: (n = e?.videoEnabled) != null ? n : Ge.videoEnabled,
		audioEnabled: (r = e?.audioEnabled) != null ? r : Ge.audioEnabled,
		videoDeviceId: (i = e?.videoDeviceId) != null ? i : Ge.videoDeviceId,
		audioDeviceId: (o = e?.audioDeviceId) != null ? o : Ge.audioDeviceId,
		username: (s = e?.username) != null ? s : Ge.username
	};
	if (t) return a;
	{
		const c = Ga$1();
		return ae(ae({}, a), c ?? {});
	}
}
var Ht = null;
var zt = null;
var Xa$1 = 0;
function Bn() {
	return Ht || (Ht = /* @__PURE__ */ new Map()), Ht;
}
function Za$1() {
	return zt || (zt = /* @__PURE__ */ new WeakMap()), zt;
}
function ec(e, t) {
	const n = Za$1();
	let r = n.get(e);
	return r || (r = `room_${Xa$1++}`, n.set(e, r)), `${r}:${t}`;
}
function tc(e, t) {
	const n = ec(e, t), r = Bn(), i = r.get(n);
	if (i) return i;
	const o = new be$1();
	let s = [];
	const a = "lk.segment_id", c = o.pipe(Ps({
		subscribe: () => {
			e.registerTextStreamHandler(t, (u, f) => Z$1(null, null, function* () {
				var d;
				const p = $e$1(u).pipe(ct((m, S) => m + S, "")), h = !!((d = u.info.attributes) != null && d[a]);
				p.subscribe((m) => {
					const S = s.findIndex((y) => {
						var b, w;
						return y.streamInfo.id === u.info.id || h && ((b = y.streamInfo.attributes) == null ? void 0 : b[a]) === ((w = u.info.attributes) == null ? void 0 : w[a]);
					});
					S !== -1 ? (s[S] = Pe$1(ae({}, s[S]), {
						text: m,
						streamInfo: u.info
					}), o.next([...s])) : (s.push({
						text: m,
						participantInfo: f,
						streamInfo: u.info
					}), o.next([...s]));
				});
			}));
		},
		finalize: () => {
			e.unregisterTextStreamHandler(t);
		}
	}), xs());
	return r.set(n, c), e.on(RoomEvent.Disconnected, () => {
		Bn().delete(n), s = [], o.next([]);
	}), c;
}
function Gr(e, t) {
	if (t.msg === "show_chat") return {
		...e,
		showChat: !0,
		unreadMessages: 0
	};
	if (t.msg === "hide_chat") return {
		...e,
		showChat: !1
	};
	if (t.msg === "toggle_chat") {
		const n = {
			...e,
			showChat: !e.showChat
		};
		return n.showChat === !0 && (n.unreadMessages = 0), n;
	} else return t.msg === "unread_msg" ? {
		...e,
		unreadMessages: t.count
	} : t.msg === "toggle_settings" ? {
		...e,
		showSettings: !e.showSettings
	} : { ...e };
}
function Jr(e, t) {
	return t.msg === "set_pin" ? [t.trackReference] : t.msg === "clear_pin" ? [] : { ...e };
}
var Yr = import_react.createContext(void 0);
function Qr() {
	const e = import_react.useContext(Yr);
	if (!e) throw Error("Tried to access LayoutContext context outside a LayoutContextProvider provider.");
	return e;
}
function nc(e) {
	const t = Xr();
	if (e ?? (e = t), !e) throw Error("Tried to access LayoutContext context outside a LayoutContextProvider provider.");
	return e;
}
function nu() {
	const [e, t] = import_react.useReducer(Jr, _r), [n, r] = import_react.useReducer(Gr, Ir);
	return {
		pin: {
			dispatch: t,
			state: e
		},
		widget: {
			dispatch: r,
			state: n
		}
	};
}
function ru(e) {
	const [t, n] = import_react.useReducer(Jr, _r), [r, i] = import_react.useReducer(Gr, Ir);
	return e ?? {
		pin: {
			dispatch: n,
			state: t
		},
		widget: {
			dispatch: i,
			state: r
		}
	};
}
function Xr() {
	return import_react.useContext(Yr);
}
var Zr = import_react.createContext(void 0);
function iu() {
	const e = import_react.useContext(Zr);
	if (!e) throw Error("tried to access track context outside of track context provider");
	return e;
}
function ei() {
	return import_react.useContext(Zr);
}
function pn(e) {
	const t = ei(), n = e ?? t;
	if (!n) throw new Error("No TrackRef, make sure you are inside a TrackRefContext or pass the TrackRef explicitly");
	return n;
}
var ti = import_react.createContext(void 0);
function ou() {
	const e = import_react.useContext(ti);
	if (!e) throw Error("tried to access participant context outside of participant context provider");
	return e;
}
function Ot() {
	return import_react.useContext(ti);
}
function Ie(e) {
	const t = Ot(), n = ei(), r = e ?? t ?? n?.participant;
	if (!r) throw new Error("No participant provided, make sure you are inside a participant context or pass the participant explicitly");
	return r;
}
var hn = import_react.createContext(void 0);
function _t() {
	const e = import_react.useContext(hn);
	if (!e) throw Error("tried to access room context outside of livekit room component");
	return e;
}
function It() {
	return import_react.useContext(hn);
}
function oe(e) {
	const t = It(), n = e ?? t;
	if (!n) throw new Error("No room provided, make sure you are inside a Room context or pass the room explicitly");
	return n;
}
var ni = import_react.createContext(void 0);
function su() {
	const e = import_react.useContext(ni);
	if (!e) throw Error("tried to access session context outside of SessionProvider component");
	return e;
}
function ri() {
	return import_react.useContext(ni);
}
function ii(e) {
	const t = ri(), n = e ?? t;
	if (!n) throw new Error("No session provided, make sure you are inside a Session context or pass the session explicitly");
	return n;
}
var oi = import_react.createContext(void 0);
function au(e) {
	const t = import_react.useContext(oi);
	if (e === !0) {
		if (t) return t;
		throw Error("tried to access feature context, but none is present");
	}
	return t;
}
function cu(e) {
	const t = oe(e), n = import_react.useCallback(async () => {
		await t.startAudio();
	}, [t]), { canPlayAudio: r } = j$1(import_react.useMemo(() => Wr(t), [t]), { canPlayAudio: t.canPlaybackAudio });
	return {
		canPlayAudio: r,
		startAudio: n
	};
}
function si(e) {
	var t, n, r = "";
	if (typeof e == "string" || typeof e == "number") r += e;
	else if (typeof e == "object") if (Array.isArray(e)) {
		var i = e.length;
		for (t = 0; t < i; t++) e[t] && (n = si(e[t])) && (r && (r += " "), r += n);
	} else for (n in e) e[n] && (r && (r += " "), r += n);
	return r;
}
function ai() {
	for (var e, t, n = 0, r = "", i = arguments.length; n < i; n++) (e = arguments[n]) && (t = si(e)) && (r && (r += " "), r += t);
	return r;
}
function rc(...e) {
	return (...t) => {
		for (const n of e) if (typeof n == "function") try {
			n(...t);
		} catch (r) {
			console.error(r);
		}
	};
}
function pe(...e) {
	const t = { ...e[0] };
	for (let n = 1; n < e.length; n++) {
		const r = e[n];
		for (const i in r) {
			const o = t[i], s = r[i];
			typeof o == "function" && typeof s == "function" && i[0] === "o" && i[1] === "n" && i.charCodeAt(2) >= 65 && i.charCodeAt(2) <= 90 ? t[i] = rc(o, s) : (i === "className" || i === "UNSAFE_className") && typeof o == "string" && typeof s == "string" ? t[i] = ai(o, s) : t[i] = s !== void 0 ? s : o;
		}
	}
	return t;
}
function uu(e) {
	const { state: t, dispatch: n } = Qr().pin;
	return { buttonProps: import_react.useMemo(() => {
		const { className: r } = Ua();
		return pe(e, {
			className: r,
			disabled: !t?.length,
			onClick: () => {
				n && n({ msg: "clear_pin" });
			}
		});
	}, [
		e,
		n,
		t
	]) };
}
function lu(e = {}) {
	const t = Ie(e.participant), { className: n, connectionQualityObserver: r } = import_react.useMemo(() => xa$1(t), [t]);
	return {
		className: n,
		quality: j$1(r, t.connectionQuality)
	};
}
function tt(e) {
	const t = oe(e);
	return j$1(import_react.useMemo(() => ca$1(t), [t]), t.state);
}
function fu(e, t) {
	const n = typeof e == "function" ? e : t, r = typeof e == "string" ? e : void 0, i = _t(), { send: o, messageObservable: s, isSendingObservable: a } = import_react.useMemo(() => qr(i, r, n), [
		i,
		r,
		n
	]);
	return {
		message: j$1(s, void 0),
		send: o,
		isSending: j$1(a, !1)
	};
}
function du(e) {
	const t = _t(), n = tt(t);
	return { buttonProps: import_react.useMemo(() => {
		const { className: r, disconnect: i } = Ta$1(t);
		return pe(e, {
			className: r,
			onClick: () => i(e.stopTracks ?? !0),
			disabled: n === ConnectionState.Disconnected
		});
	}, [
		t,
		e,
		n
	]) };
}
function ic(e) {
	if (e.publication instanceof LocalTrackPublication) {
		const t = e.publication.track;
		if (t) {
			const { facingMode: n } = facingModeFromLocalTrack(t);
			return n;
		}
	}
	return "undefined";
}
function pu({ trackRef: e, props: t }) {
	const n = pn(e), r = Xr(), { className: i } = import_react.useMemo(() => Fa$1(), []), o = import_react.useMemo(() => Fs(n, r?.pin.state), [n, r?.pin.state]);
	return {
		mergedProps: import_react.useMemo(() => pe(t, {
			className: i,
			onClick: (s) => {
				t.onClick?.(s), o ? r?.pin.dispatch?.({ msg: "clear_pin" }) : r?.pin.dispatch?.({
					msg: "set_pin",
					trackReference: n
				});
			}
		}), [
			t,
			i,
			n,
			o,
			r?.pin
		]),
		inFocus: o
	};
}
function hu(e, t, n = {}) {
	const r = n.gridLayouts ?? Hs, { width: i, height: o } = Oi(e), s = Or(r, t, i, o);
	return import_react.useEffect(() => {
		e.current && s && (e.current.style.setProperty("--lk-col-count", s?.columns.toString()), e.current.style.setProperty("--lk-row-count", s?.rows.toString()));
	}, [e, s]), {
		layout: s,
		containerWidth: i,
		containerHeight: o
	};
}
function Vn(e, t = {}) {
	const n = Ie(typeof e == "string" ? t.participant : e.participant), r = typeof e == "string" ? {
		participant: n,
		source: e
	} : e, [i, o] = import_react.useState(!!(r.publication?.isMuted || n.getTrackPublication(r.source)?.isMuted));
	return import_react.useEffect(() => {
		const s = Br(r).subscribe(o);
		return () => s.unsubscribe();
	}, [G(r)]), i;
}
function oc(e) {
	const t = Ie(e);
	return j$1(import_react.useMemo(() => ga$1(t), [t]), t.isSpeaking);
}
function sc(e) {
	return e !== void 0;
}
function vu(...e) {
	return pe(...e.filter(sc));
}
function mu(e, t, n) {
	return import_react.Children.map(e, (r) => import_react.isValidElement(r) && import_react.Children.only(e) ? (r.props.className && (t ?? (t = {}), t.className = ai(r.props.className, t.className), t.style = {
		...r.props.style,
		...t.style
	}), import_react.cloneElement(r, {
		...t,
		key: n
	})) : r);
}
function gu(e) {
	if (typeof window < "u" && typeof process < "u" && true) {
		const t = e ?? document.querySelector(".lk-room-container");
		t && !getComputedStyle(t).getPropertyValue("--lk-has-imported-styles") && R.warn("It looks like you're not using the `@livekit/components-styles package`. To render the UI with the default styling, please import it in your layout or page.");
	}
}
function ac(e, t) {
	return e === "processor" && t && typeof t == "object" && "name" in t ? t.name : (e === "e2ee" || e === "encryption") && t ? "e2ee-enabled" : t;
}
var cc = {
	connect: !0,
	audio: !1,
	video: !1
};
function uc(e) {
	const { token: t, serverUrl: n, options: r, room: i, connectOptions: o, connect: s, audio: a, video: c, screen: u, onConnected: f, onDisconnected: d, onError: p, onMediaDeviceFailure: h, onEncryptionError: m, simulateParticipants: S, ...y } = {
		...cc,
		...e
	};
	r && i && R.warn("when using a manually created room, the options object will be ignored. set the desired options directly when creating the room instead.");
	const [b, w] = import_react.useState(), P = import_react.useRef(s);
	import_react.useEffect(() => {
		w(i ?? new Room(r));
	}, [i, JSON.stringify(r, ac)]);
	const N = import_react.useMemo(() => {
		const { className: M } = $a$1();
		return pe(y, { className: M });
	}, [y]);
	return import_react.useEffect(() => {
		if (!b) return;
		const M = () => {
			const C = b.localParticipant;
			R.debug("trying to publish local tracks"), Promise.all([
				C.setMicrophoneEnabled(!!a, typeof a != "boolean" ? a : void 0),
				C.setCameraEnabled(!!c, typeof c != "boolean" ? c : void 0),
				C.setScreenShareEnabled(!!u, typeof u != "boolean" ? u : void 0)
			]).catch((I) => {
				R.warn(I), p?.(I);
			});
		}, v = (C, I) => {
			const T = MediaDeviceFailure.getFailure(C);
			h?.(T, I);
		}, g = (C) => {
			m?.(C);
		}, E = (C) => {
			d?.(C);
		}, x = () => {
			f?.();
		};
		return b.on(RoomEvent.SignalConnected, M).on(RoomEvent.MediaDevicesError, v).on(RoomEvent.EncryptionError, g).on(RoomEvent.Disconnected, E).on(RoomEvent.Connected, x), () => {
			b.off(RoomEvent.SignalConnected, M).off(RoomEvent.MediaDevicesError, v).off(RoomEvent.EncryptionError, g).off(RoomEvent.Disconnected, E).off(RoomEvent.Connected, x);
		};
	}, [
		b,
		a,
		c,
		u,
		p,
		m,
		h,
		f,
		d
	]), import_react.useEffect(() => {
		if (b) {
			if (S) {
				b.simulateParticipants({
					participants: { count: S },
					publish: {
						audio: !0,
						useRealTracks: !0
					}
				});
				return;
			}
			if (s) {
				if (P.current = !0, R.debug("connecting"), !t) {
					R.debug("no token yet");
					return;
				}
				if (!n) {
					R.warn("no livekit url provided"), p?.(Error("no livekit url provided"));
					return;
				}
				b.connect(n, t, o).catch((M) => {
					R.warn(M), P.current === !0 && p?.(M);
				});
			} else R.debug("disconnecting because connect is false"), P.current = !1, b.disconnect();
		}
	}, [
		s,
		t,
		JSON.stringify(o),
		b,
		p,
		n,
		S
	]), import_react.useEffect(() => {
		if (b) return () => {
			R.info("disconnecting on onmount"), b.disconnect();
		};
	}, [b]), {
		room: b,
		htmlProps: N
	};
}
function ci(e = {}) {
	const t = oe(e.room), [n, r] = import_react.useState(t.localParticipant), [i, o] = import_react.useState(n.isMicrophoneEnabled), [s, a] = import_react.useState(n.isCameraEnabled), [c, u] = import_react.useState(n.isScreenShareEnabled), [f, d] = import_react.useState(n.lastMicrophoneError), [p, h] = import_react.useState(n.lastCameraError), [m, S] = import_react.useState(void 0), [y, b] = import_react.useState(void 0), w = (P) => {
		a(P.isCameraEnabled), o(P.isMicrophoneEnabled), u(P.isScreenShareEnabled), b(P.cameraTrack), S(P.microphoneTrack), d(P.participant.lastMicrophoneError), h(P.participant.lastCameraError), r(P.participant);
	};
	return import_react.useEffect(() => {
		const P = dn(t.localParticipant).subscribe(w);
		return () => P.unsubscribe();
	}, [t]), {
		isMicrophoneEnabled: i,
		isScreenShareEnabled: c,
		isCameraEnabled: s,
		microphoneTrack: m,
		cameraTrack: y,
		lastMicrophoneError: f,
		lastCameraError: p,
		localParticipant: n
	};
}
function bu() {
	const e = _t();
	return j$1(import_react.useMemo(() => Vr(e.localParticipant), [e]), e.localParticipant.permissions);
}
function yu({ kind: e, room: t, track: n, requestPermissions: r, onError: i }) {
	const o = It(), s = import_react.useMemo(() => t ?? o ?? new Room(), [t, o]), a = j$1(import_react.useMemo(() => $r(e, i, r), [
		e,
		r,
		i
	]), []), [c, u] = import_react.useState(s?.getActiveDevice(e) ?? "default"), { className: f, activeDeviceObservable: d, setActiveMediaDevice: p } = import_react.useMemo(() => Ca$1(e, s, n), [
		e,
		s,
		n
	]);
	return import_react.useEffect(() => {
		const h = d.subscribe((m) => {
			m && (R.info("setCurrentDeviceId", m), u(m));
		});
		return () => {
			h?.unsubscribe();
		};
	}, [d]), {
		devices: a,
		className: f,
		activeDeviceId: c,
		setActiveMediaDevice: p
	};
}
function wu({ kind: e, onError: t }) {
	return j$1(import_react.useMemo(() => $r(e, t), [e, t]), []);
}
function lc(e, t, n = {}) {
	const r = import_react.useRef([]), i = import_react.useRef(-1), o = t !== i.current, s = typeof n.customSortFunction == "function" ? n.customSortFunction(e) : Ys(e);
	let a = [...s];
	if (o === !1) try {
		a = oa$1(r.current, s, t);
	} catch (c) {
		R.error("Error while running updatePages(): ", c);
	}
	return o ? r.current = s : r.current = a, i.current = t, a;
}
function Su(e, t) {
	const [n, r] = import_react.useState(1), i = Math.max(Math.ceil(t.length / e), 1);
	n > i && r(i);
	const o = n * e, s = o - e, a = (u) => {
		r((f) => u === "next" ? f === i ? f : f + 1 : f === 1 ? f : f - 1);
	};
	return {
		totalPageCount: i,
		nextPage: () => a("next"),
		prevPage: () => a("previous"),
		setPage: (u) => {
			u > i ? r(i) : u < 1 ? r(1) : r(u);
		},
		firstItemIndex: s,
		lastItemIndex: o,
		tracks: lc(t, e).slice(s, o),
		currentPage: n
	};
}
function fc(e = {}) {
	let t = Ot();
	e.participant && (t = e.participant);
	const { identity: n, name: r, metadata: i } = j$1(import_react.useMemo(() => jr(t), [t]), {
		name: t?.name,
		identity: t?.identity,
		metadata: t?.metadata
	});
	return {
		identity: n,
		name: r,
		metadata: i
	};
}
function Eu(e = {}) {
	const t = Ie(e.participant);
	return j$1(import_react.useMemo(() => Vr(t), [t]), t.permissions);
}
function Cu({ trackRef: e, onParticipantClick: t, disableSpeakingIndicator: n, htmlProps: r }) {
	const i = pn(e), o = import_react.useMemo(() => {
		const { className: p } = Pa$1();
		return pe(r, {
			className: p,
			onClick: (h) => {
				if (r.onClick?.(h), typeof t == "function") {
					const m = i.publication ?? i.participant.getTrackPublication(i.source);
					t({
						participant: i.participant,
						track: m
					});
				}
			}
		});
	}, [
		r,
		t,
		i.publication,
		i.source,
		i.participant
	]), s = i.participant.getTrackPublication(Track.Source.Microphone), a = import_react.useMemo(() => ({
		participant: i.participant,
		source: Track.Source.Microphone,
		publication: s
	}), [s, i.participant]), c = Vn(i), u = Vn(a), f = oc(i.participant), d = ic(i);
	return { elementProps: {
		"data-lk-audio-muted": u,
		"data-lk-video-muted": c,
		"data-lk-speaking": n === !0 ? !1 : f,
		"data-lk-local-participant": i.participant.isLocal,
		"data-lk-source": i.source,
		"data-lk-facing-mode": d,
		...o
	} };
}
function vn(e = {}) {
	const t = oe(e.room), [n, r] = import_react.useState([]);
	return import_react.useEffect(() => {
		const i = ba$1(t, { additionalRoomEvents: e.updateOnlyOn }).subscribe(r);
		return () => i.unsubscribe();
	}, [t, JSON.stringify(e.updateOnlyOn)]), n;
}
function dc(e = {}) {
	const t = vn(e), { localParticipant: n } = ci(e);
	return import_react.useMemo(() => [n, ...t], [n, t]);
}
function Tu(e) {
	return e = nc(e), import_react.useMemo(() => e?.pin.state !== void 0 && e.pin.state.length >= 1 ? e.pin.state : [], [e.pin.state]);
}
function xu(e, t = {}) {
	const n = _t(), [r] = import_react.useState(t.updateOnlyOn), i = import_react.useMemo(() => typeof e == "string" ? ya$1(n, e, { additionalEvents: r }) : wa$1(n, e, { additionalEvents: r }), [
		n,
		JSON.stringify(e),
		r
	]), [o, s] = import_react.useState({ p: void 0 });
	return import_react.useEffect(() => {
		const a = i.subscribe((c) => s({ p: c }));
		return () => a.unsubscribe();
	}, [i]), o.p;
}
function ku(e = {}) {
	const t = oe(e.room), { name: n, metadata: r } = j$1(import_react.useMemo(() => ua$1(t), [t]), {
		name: t.name,
		metadata: t.metadata
	});
	return {
		name: n,
		metadata: r
	};
}
function pc(e) {
	const t = oe(e?.room);
	return j$1(import_react.useMemo(() => la$1(t), [t]), t.activeSpeakers);
}
function Pu(e) {
	const [t, n] = import_react.useState(Un(e)), r = pc();
	return import_react.useEffect(() => {
		n(Un(e));
	}, [r, e]), t;
}
function Mu({ room: e, props: t }) {
	const n = oe(e), { className: r, roomAudioPlaybackAllowedObservable: i, handleStartAudioPlayback: o } = import_react.useMemo(() => Ra$1(), []), { canPlayAudio: s } = j$1(import_react.useMemo(() => i(n), [n, i]), { canPlayAudio: n.canPlaybackAudio });
	return {
		mergedProps: import_react.useMemo(() => pe(t, {
			className: r,
			onClick: () => {
				o(n);
			},
			style: { display: s ? "none" : "block" }
		}), [
			t,
			r,
			s,
			o,
			n
		]),
		canPlayAudio: s
	};
}
function Au({ room: e, props: t }) {
	const n = oe(e), { className: r, roomVideoPlaybackAllowedObservable: i, handleStartVideoPlayback: o } = import_react.useMemo(() => Da$1(), []), { canPlayVideo: s } = j$1(import_react.useMemo(() => i(n), [n, i]), { canPlayVideo: n.canPlaybackVideo });
	return {
		mergedProps: import_react.useMemo(() => pe(t, {
			className: r,
			onClick: () => {
				o(n);
			},
			style: { display: s ? "none" : "block" }
		}), [
			t,
			r,
			s,
			o,
			n
		]),
		canPlayVideo: s
	};
}
function Lu(e, t = {}) {
	const n = import_react.useRef(null), r = import_react.useRef(null), i = t.minSwipeDistance ?? 50, o = (c) => {
		r.current = null, n.current = c.targetTouches[0].clientX;
	}, s = (c) => {
		r.current = c.targetTouches[0].clientX;
	}, a = import_react.useCallback(() => {
		if (!n.current || !r.current) return;
		const c = n.current - r.current, u = c > i, f = c < -i;
		u && t.onLeftSwipe && t.onLeftSwipe(), f && t.onRightSwipe && t.onRightSwipe();
	}, [i, t]);
	import_react.useEffect(() => {
		const c = e.current;
		return c && (c.addEventListener("touchstart", o, { passive: !0 }), c.addEventListener("touchmove", s, { passive: !0 }), c.addEventListener("touchend", a, { passive: !0 })), () => {
			c && (c.removeEventListener("touchstart", o), c.removeEventListener("touchmove", s), c.removeEventListener("touchend", a));
		};
	}, [e, a]);
}
function Ou({ props: e }) {
	const { dispatch: t, state: n } = Qr().widget, { className: r } = import_react.useMemo(() => Na$1(), []);
	return { mergedProps: import_react.useMemo(() => pe(e, {
		className: r,
		onClick: () => {
			t && t({ msg: "toggle_chat" });
		},
		"aria-pressed": n?.showChat ? "true" : "false",
		"data-lk-unread-msgs": n ? n.unreadMessages < 10 ? n.unreadMessages.toFixed(0) : "9+" : "0"
	}), [
		e,
		r,
		t,
		n
	]) };
}
function _u(e, t, n = {}) {
	const [r, i] = import_react.useState(void 0);
	return import_react.useEffect(() => {
		if (e === void 0) throw Error("token endpoint needs to be defined");
		if (n.userInfo?.identity === void 0) return;
		(async () => {
			R.debug("fetching token");
			const s = new URLSearchParams({
				...n.userInfo,
				roomName: t
			}), a = await fetch(`${e}?${s.toString()}`);
			if (!a.ok) {
				R.error(`Could not fetch token. Server responded with status ${a.status}: ${a.statusText}`);
				return;
			}
			const { accessToken: c } = await a.json();
			i(c);
		})();
	}, [
		e,
		t,
		JSON.stringify(n)
	]), r;
}
function Iu(e) {
	const t = pn(e), { className: n, mediaMutedObserver: r } = import_react.useMemo(() => ka$1(t), [G(t)]);
	return {
		isMuted: j$1(r, !!(t.publication?.isMuted || t.participant.getTrackPublication(t.source)?.isMuted)),
		className: n
	};
}
function Ru({ source: e, onChange: t, initialState: n, captureOptions: r, publishOptions: i, onDeviceError: o, room: s, ...a }) {
	const c = It(), u = import_react.useMemo(() => s ?? c, [s, c]), f = u?.localParticipant?.getTrackPublication(e), d = import_react.useRef(!1), { toggle: p, className: h, pendingObserver: m, enabledObserver: S } = import_react.useMemo(() => u ? Sa$1(e, u, r, i, o) : Ea$1(), [
		u,
		e,
		JSON.stringify(r),
		i
	]), y = j$1(m, !1), b = j$1(S, n ?? !!f?.isEnabled);
	import_react.useEffect(() => {
		t?.(b, d.current), d.current = !1;
	}, [b, t]), import_react.useEffect(() => {
		n !== void 0 && (R.debug("forcing initial toggle state", e, n), p(n));
	}, []);
	const w = import_react.useMemo(() => pe(a, { className: h }), [a, h]), P = import_react.useCallback((N) => {
		d.current = !0, p().catch(() => d.current = !1), a.onClick?.(N);
	}, [a, p]);
	return {
		toggle: p,
		enabled: b,
		pending: y,
		track: f,
		buttonProps: {
			...w,
			"aria-pressed": b,
			"data-lk-source": e,
			"data-lk-enabled": b,
			disabled: y,
			onClick: P
		}
	};
}
function Du(e = [
	Track.Source.Camera,
	Track.Source.Microphone,
	Track.Source.ScreenShare,
	Track.Source.ScreenShareAudio,
	Track.Source.Unknown
], t = {}) {
	const n = oe(t.room), [r, i] = import_react.useState([]), [o, s] = import_react.useState([]), a = import_react.useMemo(() => e.map((c) => Rr(c) ? c.source : c), [JSON.stringify(e)]);
	return import_react.useEffect(() => {
		const c = Wa(n, a, {
			additionalRoomEvents: t.updateOnlyOn,
			onlySubscribed: t.onlySubscribed
		}).subscribe(({ trackReferences: u, participants: f }) => {
			R.debug("setting track bundles", u, f), i(u), s(f);
		});
		return () => c.unsubscribe();
	}, [
		n,
		JSON.stringify(t.onlySubscribed),
		JSON.stringify(t.updateOnlyOn),
		JSON.stringify(e)
	]), import_react.useMemo(() => {
		if (Dr(e)) {
			const c = vc(e, o), u = Array.from(r);
			return o.forEach((f) => {
				c.has(f.identity) && (c.get(f.identity) ?? []).forEach((d) => {
					if (r.find(({ participant: h, publication: m }) => f.identity === h.identity && m.source === d)) return;
					R.debug(`Add ${d} placeholder for participant ${f.identity}.`);
					const p = {
						participant: f,
						source: d
					};
					u.push(p);
				});
			}), u;
		} else return r;
	}, [
		r,
		o,
		e
	]);
}
function hc(e, t) {
	const n = new Set(e);
	for (const r of t) n.delete(r);
	return n;
}
function vc(e, t) {
	const n = /* @__PURE__ */ new Map();
	if (Dr(e)) {
		const r = e.filter((i) => i.withPlaceholder).map((i) => i.source);
		t.forEach((i) => {
			const o = i.getTrackPublications().map((a) => a.track?.source).filter((a) => a !== void 0), s = Array.from(hc(new Set(r), new Set(o)));
			s.length > 0 && n.set(i.identity, s);
		});
	}
	return n;
}
function mc(e) {
	const [t, n] = import_react.useState(Zt$1(e)), { trackObserver: r } = import_react.useMemo(() => aa$1(e), [e.participant.sid ?? e.participant.identity, e.source]);
	return import_react.useEffect(() => {
		const i = r.subscribe((o) => {
			n(o);
		});
		return () => i?.unsubscribe();
	}, [r]), {
		participant: e.participant,
		source: e.source ?? Track.Source.Unknown,
		publication: t
	};
}
function Nu(e, t) {
	return mc({
		name: e,
		participant: Ie(t)
	});
}
function gc(e) {
	const t = oe(e?.room), n = tt(t), r = import_react.useMemo(() => n === ConnectionState.Disconnected, [n]), i = import_react.useMemo(() => Ia$1(t, e), [
		t,
		e,
		r
	]), o = j$1(i.isSendingObservable, !1), s = j$1(i.messageObservable, []);
	return {
		send: i.send,
		chatMessages: s,
		isSending: o
	};
}
function Fu(e = {}) {
	const [t, n] = import_react.useState(Qa$1(e.defaults, e.preventLoad ?? !1)), r = import_react.useCallback((c) => {
		n((u) => ({
			...u,
			audioEnabled: c
		}));
	}, []), i = import_react.useCallback((c) => {
		n((u) => ({
			...u,
			videoEnabled: c
		}));
	}, []), o = import_react.useCallback((c) => {
		n((u) => ({
			...u,
			audioDeviceId: c
		}));
	}, []), s = import_react.useCallback((c) => {
		n((u) => ({
			...u,
			videoDeviceId: c
		}));
	}, []), a = import_react.useCallback((c) => {
		n((u) => ({
			...u,
			username: c
		}));
	}, []);
	return import_react.useEffect(() => {
		Ya(t, e.preventSave ?? !1);
	}, [t, e.preventSave]), {
		userChoices: t,
		saveAudioInputEnabled: r,
		saveVideoInputEnabled: i,
		saveAudioInputDeviceId: o,
		saveVideoInputDeviceId: s,
		saveUsername: a
	};
}
function Uu(e, t = {}) {
	const n = Ie(e), r = oe(t.room);
	return j$1(import_react.useMemo(() => ha$1(r, n), [r, n]), n.isLocal ? n.isE2EEEnabled : !!n?.isEncrypted);
}
function $u(e, t = {
	fftSize: 32,
	smoothingTimeConstant: 0
}) {
	const n = ee(e) ? e.publication.track : e, [r, i] = import_react.useState(0);
	return import_react.useEffect(() => {
		if (!n || !n.mediaStream) return;
		const { cleanup: o, analyser: s } = createAudioAnalyser(n, t), a = s.frequencyBinCount, c = new Uint8Array(a), f = setInterval(() => {
			s.getByteFrequencyData(c);
			let d = 0;
			for (let p = 0; p < c.length; p++) {
				const h = c[p];
				d += h * h;
			}
			i(Math.sqrt(d / c.length) / 255);
		}, 1e3 / 30);
		return () => {
			o(), clearInterval(f);
		};
	}, [
		n,
		n?.mediaStream,
		JSON.stringify(t)
	]), r;
}
var bc = (e) => {
	const t = (n) => {
		let r = 1 - Math.max(-100, Math.min(-10, n)) * -1 / 100;
		return r = Math.sqrt(r), r;
	};
	return e.map((n) => n === -1 / 0 ? 0 : t(n));
};
var yc = {
	bands: 5,
	loPass: 100,
	hiPass: 600,
	updateInterval: 32,
	analyserOptions: { fftSize: 2048 }
};
function Wu(e, t = {}) {
	const n = e instanceof Track ? e : e?.publication?.track, r = {
		...yc,
		...t
	}, [i, o] = import_react.useState(new Array(r.bands).fill(0));
	return import_react.useEffect(() => {
		if (!n || !n?.mediaStream) {
			o((p) => p.slice().fill(0));
			return;
		}
		const { analyser: s, cleanup: a } = createAudioAnalyser(n, r.analyserOptions), c = s.frequencyBinCount, u = new Float32Array(c), d = setInterval(() => {
			s.getFloatFrequencyData(u);
			let p = new Float32Array(u.length);
			for (let y = 0; y < u.length; y++) p[y] = u[y];
			p = p.slice(r.loPass, r.hiPass);
			const h = bc(p), m = h.length, S = [];
			for (let y = 0; y < r.bands; y++) {
				const b = Math.floor(y * m / r.bands), w = Math.floor((y + 1) * m / r.bands), P = h.slice(b, w), N = P.length;
				if (N === 0) S.push(0);
				else {
					const M = P.reduce((v, g) => v += g, 0);
					S.push(M / N);
				}
			}
			o(S);
		}, r.updateInterval);
		return () => {
			a(), clearInterval(d);
		};
	}, [
		n,
		n?.mediaStream,
		JSON.stringify(t)
	]), i;
}
var wc = {
	barCount: 120,
	volMultiplier: 5,
	updateInterval: 20
};
function ju(e, t = {}) {
	const n = e instanceof Track ? e : e?.publication?.track, r = {
		...wc,
		...t
	}, i = import_react.useRef(/* @__PURE__ */ new Float32Array()), o = import_react.useRef(performance.now()), s = import_react.useRef(0), [a, c] = import_react.useState([]), u = import_react.useCallback((f) => {
		c(Array.from(Ec(f, r.barCount).map((d) => Math.sqrt(d) * r.volMultiplier)));
	}, []);
	return import_react.useEffect(() => {
		if (!n || !n?.mediaStream) return;
		const { analyser: f, cleanup: d } = createAudioAnalyser(n, { fftSize: Hn(r.barCount) }), p = Hn(r.barCount), h = new Float32Array(p), m = () => {
			if (S = requestAnimationFrame(m), f.getFloatTimeDomainData(h), i.current.map((y, b) => y + h[b]), s.current += 1, performance.now() - o.current >= r.updateInterval) {
				const y = h.map((b) => b / s.current);
				u(y), o.current = performance.now(), s.current = 0;
			}
		};
		let S = requestAnimationFrame(m);
		return () => {
			d(), cancelAnimationFrame(S);
		};
	}, [
		n,
		n?.mediaStream,
		JSON.stringify(t),
		u
	]), { bars: a };
}
function Hn(e) {
	return e < 32 ? 32 : Sc(e);
}
function Sc(e) {
	let t = 2;
	for (; e >>= 1;) t <<= 1;
	return t;
}
function Ec(e, t) {
	const n = Math.floor(e.length / t), r = new Float32Array(t);
	for (let i = 0; i < t; i++) {
		const o = n * i;
		let s = 0;
		for (let a = 0; a < n; a++) s = s + Math.abs(e[o + a]);
		r[i] = s / n;
	}
	return r;
}
function St(e, t = {}) {
	let n, r;
	typeof t == "string" ? n = t : (n = t?.participantIdentity, r = t?.room);
	const i = Ot(), o = dc({
		room: r,
		updateOnlyOn: []
	}), s = import_react.useMemo(() => n ? o.find((a) => a.identity === n) : i, [
		n,
		o,
		i
	]);
	return j$1(import_react.useMemo(() => {
		if (s) return ja(s, { sources: e });
	}, [s, JSON.stringify(e)]), []);
}
function Cc(e) {
	return j$1(import_react.useMemo(() => e?.publication?.track ? Va$1(e?.publication.track) : void 0, [e?.publication?.track]), {
		timestamp: Date.now(),
		rtpTimestamp: e?.publication?.track?.rtpTimestamp
	});
}
var Tc = { bufferSize: 100 };
function xc(e, t) {
	const n = {
		...Tc,
		...t
	}, [r, i] = import_react.useState([]), o = Cc(e), s = (a) => {
		n.onTranscription?.(a), i((c) => Ks(c, a.map((u) => qs(u, o)), n.bufferSize));
	};
	return import_react.useEffect(() => {
		if (!e?.publication) return;
		const a = Ba$1(e.publication).subscribe((c) => {
			s(...c);
		});
		return () => {
			a.unsubscribe();
		};
	}, [e && G(e), s]), { segments: r };
}
function kc(e = {}) {
	const t = Ot(), n = e.participant ?? t;
	return j$1(import_react.useMemo(() => en(n), [n]), { attributes: n?.attributes });
}
function Bu(e, t = {}) {
	const n = Ie(t.participant), [r, i] = import_react.useState(n.attributes[e]);
	return import_react.useEffect(() => {
		if (!n) return;
		const o = en(n).subscribe((s) => {
			s.changed[e] !== void 0 && i(s.attributes[e]);
		});
		return () => {
			o.unsubscribe();
		};
	}, [n, e]), r;
}
var zn = ce$1.AgentState;
function Vu() {
	const e = vn(), t = e.find((f) => f.kind === ParticipantInfo_Kind.AGENT && !(ce$1.PublishOnBehalf in f.attributes)), n = e.find((f) => f.kind === ParticipantInfo_Kind.AGENT && f.attributes[ce$1.PublishOnBehalf] === t?.identity), r = St([Track.Source.Microphone, Track.Source.Camera], t?.identity), i = St([Track.Source.Microphone, Track.Source.Camera], n?.identity), o = r.find((f) => f.source === Track.Source.Microphone) ?? i.find((f) => f.source === Track.Source.Microphone), s = r.find((f) => f.source === Track.Source.Camera) ?? i.find((f) => f.source === Track.Source.Camera), { segments: a } = xc(o), c = tt(), { attributes: u } = kc({ participant: t });
	return {
		agent: t,
		state: import_react.useMemo(() => c === ConnectionState.Disconnected ? "disconnected" : c === ConnectionState.Connecting || !t || !u?.[zn] ? "connecting" : u[zn], [
			u,
			t,
			c
		]),
		audioTrack: o,
		videoTrack: s,
		agentTranscriptions: a,
		agentAttributes: u
	};
}
function Hu(e) {
	const t = oe(e), n = tt(t);
	return j$1(import_react.useMemo(() => va$1(t), [t, n]), t.isRecording);
}
function Pc(e, t) {
	const n = oe(t?.room), r = tt(n) === ConnectionState.Disconnected, i = import_react.useMemo(() => tc(n, e), [n, e]);
	return { textStreams: j$1(r ? void 0 : i, []) };
}
function ui(e) {
	const { participantIdentities: t, trackSids: n } = e ?? {}, { textStreams: r } = Pc(Hr.TRANSCRIPTION, { room: e?.room });
	return import_react.useMemo(() => r.filter((i) => t ? t.includes(i.participantInfo.identity) : !0).filter((i) => n ? n.includes(i.streamInfo.attributes?.[ce$1.TranscribedTrackId] ?? "") : !0), [
		r,
		t,
		n
	]);
}
var Mc = ce$1.Expression;
var zu = 2;
var Ac = 150;
var Lc = 20;
function Oc(e, t, n) {
	return `${e.streamInfo.id ?? e.streamInfo.attributes?.[ce$1.TranscriptionSegmentId] ?? t}:${e.participantInfo.identity}:${n.mood ?? ""}:${n.expression ?? ""}`;
}
function qn(e) {
	const t = e.streamInfo.attributes?.[Mc];
	if (!t) return null;
	try {
		const n = JSON.parse(t), r = n.expression?.trim() || null;
		return !r && !n.mood ? null : {
			mood: n.mood ?? null,
			expression: r
		};
	} catch {
		return null;
	}
}
function qu(e) {
	const { ttlTurns: t = 2, ...n } = e ?? {}, r = ui(n), [i, o] = import_react.useState(null);
	return import_react.useEffect(() => {
		let s = 0;
		const a = setInterval(() => {
			o((c) => {
				let u = null;
				for (let f = r.length - 1; f >= 0; f--) {
					const d = qn(r[f]);
					if (d) {
						u = Oc(r[f], f, d);
						break;
					}
				}
				return u === c ? c : u;
			}), ++s >= Lc && clearInterval(a);
		}, Ac);
		return () => clearInterval(a);
	}, [r]), import_react.useMemo(() => {
		let s = null, a = null;
		const c = [];
		for (let f = r.length - 1; f >= 0; f--) {
			const d = r[f], p = qn(d);
			if (p) {
				s = p, a = d.participantInfo.identity;
				break;
			}
			c.push(d.participantInfo.identity);
		}
		if (!s) return {
			mood: null,
			expression: null
		};
		const u = c.filter((f) => f === a).length;
		return t > 0 && u >= t ? {
			mood: null,
			expression: null
		} : s;
	}, [
		r,
		i,
		t
	]);
}
var Kn = 2;
var Gn = 400;
var Jn = 3;
var Yn = 1e3;
function Ku(e) {
	const t = (0, import_react.useRef)([]), n = (0, import_react.useMemo)(() => new _(), []), r = (0, import_react.useCallback)(async () => n.lock().then(async (f) => {
		for (;;) {
			const d = t.current.pop();
			if (!d) {
				f();
				break;
			}
			switch (d.type) {
				case "connect":
					await d.room.connect(...d.args).then(d.resolve).catch(d.reject);
					break;
				case "disconnect": await d.room.disconnect(...d.args).then(d.resolve).catch(d.reject);
			}
		}
	}), []), i = (0, import_react.useRef)([]), o = (0, import_react.useCallback)((f) => {
		let d = 0;
		i.current = i.current.filter((p) => {
			const h = f.getTime() - p.getTime() < Yn;
			return h && (d += 1), h;
		}), d > Jn && R.warn(`useSequentialRoomConnectDisconnect: room changed reference rapidly (over ${Jn}x in ${Yn}ms). This is not recommended.`);
	}, []);
	(0, import_react.useEffect)(() => {
		t.current = [];
		const f = /* @__PURE__ */ new Date();
		i.current.push(f), o(f);
	}, [e, o]);
	const s = (0, import_react.useRef)([]), a = (0, import_react.useCallback)((f) => {
		let d = 0;
		s.current = s.current.filter((p) => {
			const h = f.getTime() - p.getTime() < Gn;
			return h && (d += 1), h;
		}), d > Kn && R.warn(`useSequentialRoomConnectDisconnect: room connect / disconnect occurring in rapid sequence (over ${Kn}x in ${Gn}ms). This is not recommended and may be the sign of a bug like a useEffect dependency changing every render.`);
	}, []), c = (0, import_react.useCallback)(async (...f) => new Promise((d, p) => {
		if (!e) throw new Error("Called connect(), but room was unset");
		const h = /* @__PURE__ */ new Date();
		a(h), t.current.push({
			type: "connect",
			room: e,
			args: f,
			resolve: d,
			reject: p
		}), s.current.push(h), r();
	}), [
		e,
		a,
		r
	]), u = (0, import_react.useCallback)(async (...f) => new Promise((d, p) => {
		if (!e) throw new Error("Called discconnect(), but room was unset");
		const h = /* @__PURE__ */ new Date();
		a(h), t.current.push({
			type: "disconnect",
			room: e,
			args: f,
			resolve: d,
			reject: p
		}), s.current.push(h), r();
	}), [
		e,
		a,
		r
	]);
	return {
		connect: e ? c : null,
		disconnect: e ? u : null
	};
}
var mn = (/* @__PURE__ */ j$2(((e, t) => {
	var n = typeof Reflect == "object" ? Reflect : null, r = n && typeof n.apply == "function" ? n.apply : function(g, E, x) {
		return Function.prototype.apply.call(g, E, x);
	}, i;
	n && typeof n.ownKeys == "function" ? i = n.ownKeys : Object.getOwnPropertySymbols ? i = function(g) {
		return Object.getOwnPropertyNames(g).concat(Object.getOwnPropertySymbols(g));
	} : i = function(g) {
		return Object.getOwnPropertyNames(g);
	};
	function o(v) {
		console && console.warn && console.warn(v);
	}
	var s = Number.isNaN || function(g) {
		return g !== g;
	};
	function a() {
		a.init.call(this);
	}
	t.exports = a, t.exports.once = P, a.EventEmitter = a, a.prototype._events = void 0, a.prototype._eventsCount = 0, a.prototype._maxListeners = void 0;
	var c = 10;
	function u(v) {
		if (typeof v != "function") throw new TypeError("The \"listener\" argument must be of type Function. Received type " + typeof v);
	}
	Object.defineProperty(a, "defaultMaxListeners", {
		enumerable: !0,
		get: function() {
			return c;
		},
		set: function(v) {
			if (typeof v != "number" || v < 0 || s(v)) throw new RangeError("The value of \"defaultMaxListeners\" is out of range. It must be a non-negative number. Received " + v + ".");
			c = v;
		}
	}), a.init = function() {
		(this._events === void 0 || this._events === Object.getPrototypeOf(this)._events) && (this._events = /* @__PURE__ */ Object.create(null), this._eventsCount = 0), this._maxListeners = this._maxListeners || void 0;
	}, a.prototype.setMaxListeners = function(g) {
		if (typeof g != "number" || g < 0 || s(g)) throw new RangeError("The value of \"n\" is out of range. It must be a non-negative number. Received " + g + ".");
		return this._maxListeners = g, this;
	};
	function f(v) {
		return v._maxListeners === void 0 ? a.defaultMaxListeners : v._maxListeners;
	}
	a.prototype.getMaxListeners = function() {
		return f(this);
	}, a.prototype.emit = function(g) {
		for (var E = [], x = 1; x < arguments.length; x++) E.push(arguments[x]);
		var C = g === "error", I = this._events;
		if (I !== void 0) C = C && I.error === void 0;
		else if (!C) return !1;
		if (C) {
			var T;
			if (E.length > 0 && (T = E[0]), T instanceof Error) throw T;
			var A = /* @__PURE__ */ new Error("Unhandled error." + (T ? " (" + T.message + ")" : ""));
			throw A.context = T, A;
		}
		var D = I[g];
		if (D === void 0) return !1;
		if (typeof D == "function") r(D, this, E);
		else for (var U = D.length, H = y(D, U), x = 0; x < U; ++x) r(H[x], this, E);
		return !0;
	};
	function d(v, g, E, x) {
		var C, I, T;
		if (u(E), I = v._events, I === void 0 ? (I = v._events = /* @__PURE__ */ Object.create(null), v._eventsCount = 0) : (I.newListener !== void 0 && (v.emit("newListener", g, E.listener ? E.listener : E), I = v._events), T = I[g]), T === void 0) T = I[g] = E, ++v._eventsCount;
		else if (typeof T == "function" ? T = I[g] = x ? [E, T] : [T, E] : x ? T.unshift(E) : T.push(E), C = f(v), C > 0 && T.length > C && !T.warned) {
			T.warned = !0;
			var A = /* @__PURE__ */ new Error("Possible EventEmitter memory leak detected. " + T.length + " " + String(g) + " listeners added. Use emitter.setMaxListeners() to increase limit");
			A.name = "MaxListenersExceededWarning", A.emitter = v, A.type = g, A.count = T.length, o(A);
		}
		return v;
	}
	a.prototype.addListener = function(g, E) {
		return d(this, g, E, !1);
	}, a.prototype.on = a.prototype.addListener, a.prototype.prependListener = function(g, E) {
		return d(this, g, E, !0);
	};
	function p() {
		if (!this.fired) return this.target.removeListener(this.type, this.wrapFn), this.fired = !0, arguments.length === 0 ? this.listener.call(this.target) : this.listener.apply(this.target, arguments);
	}
	function h(v, g, E) {
		var x = {
			fired: !1,
			wrapFn: void 0,
			target: v,
			type: g,
			listener: E
		}, C = p.bind(x);
		return C.listener = E, x.wrapFn = C, C;
	}
	a.prototype.once = function(g, E) {
		return u(E), this.on(g, h(this, g, E)), this;
	}, a.prototype.prependOnceListener = function(g, E) {
		return u(E), this.prependListener(g, h(this, g, E)), this;
	}, a.prototype.removeListener = function(g, E) {
		var x, C, I, T, A;
		if (u(E), C = this._events, C === void 0) return this;
		if (x = C[g], x === void 0) return this;
		if (x === E || x.listener === E) --this._eventsCount === 0 ? this._events = /* @__PURE__ */ Object.create(null) : (delete C[g], C.removeListener && this.emit("removeListener", g, x.listener || E));
		else if (typeof x != "function") {
			for (I = -1, T = x.length - 1; T >= 0; T--) if (x[T] === E || x[T].listener === E) {
				A = x[T].listener, I = T;
				break;
			}
			if (I < 0) return this;
			I === 0 ? x.shift() : b(x, I), x.length === 1 && (C[g] = x[0]), C.removeListener !== void 0 && this.emit("removeListener", g, A || E);
		}
		return this;
	}, a.prototype.off = a.prototype.removeListener, a.prototype.removeAllListeners = function(g) {
		var E, x = this._events, C;
		if (x === void 0) return this;
		if (x.removeListener === void 0) return arguments.length === 0 ? (this._events = /* @__PURE__ */ Object.create(null), this._eventsCount = 0) : x[g] !== void 0 && (--this._eventsCount === 0 ? this._events = /* @__PURE__ */ Object.create(null) : delete x[g]), this;
		if (arguments.length === 0) {
			var I = Object.keys(x), T;
			for (C = 0; C < I.length; ++C) T = I[C], T !== "removeListener" && this.removeAllListeners(T);
			return this.removeAllListeners("removeListener"), this._events = /* @__PURE__ */ Object.create(null), this._eventsCount = 0, this;
		}
		if (E = x[g], typeof E == "function") this.removeListener(g, E);
		else if (E !== void 0) for (C = E.length - 1; C >= 0; C--) this.removeListener(g, E[C]);
		return this;
	};
	function m(v, g, E) {
		var x = v._events;
		if (x === void 0) return [];
		var C = x[g];
		return C === void 0 ? [] : typeof C == "function" ? E ? [C.listener || C] : [C] : E ? w(C) : y(C, C.length);
	}
	a.prototype.listeners = function(g) {
		return m(this, g, !0);
	}, a.prototype.rawListeners = function(g) {
		return m(this, g, !1);
	}, a.listenerCount = function(v, g) {
		return typeof v.listenerCount == "function" ? v.listenerCount(g) : S.call(v, g);
	}, a.prototype.listenerCount = S;
	function S(v) {
		var g = this._events;
		if (g !== void 0) {
			var E = g[v];
			if (typeof E == "function") return 1;
			if (E !== void 0) return E.length;
		}
		return 0;
	}
	a.prototype.eventNames = function() {
		return this._eventsCount > 0 ? i(this._events) : [];
	};
	function y(v, g) {
		for (var E = new Array(g), x = 0; x < g; ++x) E[x] = v[x];
		return E;
	}
	function b(v, g) {
		for (; g + 1 < v.length; g++) v[g] = v[g + 1];
		v.pop();
	}
	function w(v) {
		for (var g = new Array(v.length), E = 0; E < g.length; ++E) g[E] = v[E].listener || v[E];
		return g;
	}
	function P(v, g) {
		return new Promise(function(E, x) {
			function C(T) {
				v.removeListener(g, I), x(T);
			}
			function I() {
				typeof v.removeListener == "function" && v.removeListener("error", C), E([].slice.call(arguments));
			}
			M(v, g, I, { once: !0 }), g !== "error" && N(v, C, { once: !0 });
		});
	}
	function N(v, g, E) {
		typeof v.on == "function" && M(v, "error", g, E);
	}
	function M(v, g, E, x) {
		if (typeof v.on == "function") x.once ? v.once(g, E) : v.on(g, E);
		else if (typeof v.addEventListener == "function") v.addEventListener(g, function C(I) {
			x.once && v.removeEventListener(g, C), E(I);
		});
		else throw new TypeError("The \"emitter\" argument must be of type EventEmitter. Received type " + typeof v);
	}
})))();
var Ic = 2e4;
var Gu = /* @__PURE__ */ (function(e) {
	return e.CameraChanged = "cameraChanged", e.MicrophoneChanged = "microphoneChanged", e.StateChanged = "stateChanged", e;
})({});
var re = (e) => ({
	isConnected: e === "listening" || e === "thinking" || e === "speaking",
	canListen: e === "pre-connect-buffering" || e === "listening" || e === "thinking" || e === "speaking",
	isFinished: e === "disconnected" || e === "failed",
	isPending: e === "connecting" || e === "initializing" || e === "idle"
});
var Rc = () => {
	const [e, t] = import_react.useState(null), [n, r] = import_react.useState(null), i = import_react.useRef("connecting"), o = import_react.useRef(!1), s = (a) => setTimeout(() => {
		if (!o.current) {
			t("Agent did not join the room.");
			return;
		}
		const { isConnected: c } = re(i.current);
		if (!c) {
			t("Agent joined the room but did not complete initializing.");
			return;
		}
	}, a ?? Ic);
	return {
		agentTimeoutFailureReason: e,
		startAgentTimeout: import_react.useCallback((a) => {
			n && clearTimeout(n), t(null), r(s(a)), i.current = "connecting", o.current = !1;
		}, [n]),
		clearAgentTimeout: import_react.useCallback(() => {
			n && clearTimeout(n), t(null), r(null), i.current = "connecting", o.current = !1;
		}, [n]),
		clearAgentTimeoutFailureReason: import_react.useCallback(() => {
			t(null);
		}, []),
		updateAgentTimeoutState: import_react.useCallback((a) => {
			i.current = a;
		}, []),
		updateAgentTimeoutParticipantExists: import_react.useCallback((a) => {
			o.current = a;
		}, [])
	};
};
function Dc(e, t) {
	const n = import_react.useRef(t);
	return import_react.useEffect(() => {
		n.current = t;
	}, [t]), {
		waitUntilConnected: import_react.useCallback(async (r) => {
			const { isConnected: i } = re(n.current);
			if (!i) return new Promise((o, s) => {
				const a = (f) => {
					const { isConnected: d } = re(f);
					d && (u(), o());
				}, c = () => {
					u(), s(/* @__PURE__ */ new Error("useAgent(/* ... */).waitUntilConnected - signal aborted"));
				}, u = () => {
					e.off("stateChanged", a), r?.removeEventListener("abort", c);
				};
				e.on("stateChanged", a), r?.addEventListener("abort", c);
			});
		}, [e]),
		waitUntilCouldBeListening: import_react.useCallback(async (r) => {
			const { canListen: i } = re(n.current);
			if (!i) return new Promise((o, s) => {
				const a = (f) => {
					const { canListen: d } = re(f);
					d && (u(), o());
				}, c = () => {
					u(), s(/* @__PURE__ */ new Error("useAgent(/* ... */).waitUntilCouldBeListening - signal aborted"));
				}, u = () => {
					e.off("stateChanged", a), r?.removeEventListener("abort", c);
				};
				e.on("stateChanged", a), r?.addEventListener("abort", c);
			});
		}, [e]),
		waitUntilFinished: import_react.useCallback(async (r) => {
			const { isFinished: i } = re(n.current);
			if (!i) return new Promise((o, s) => {
				const a = (f) => {
					const { isFinished: d } = re(f);
					d && (u(), o());
				}, c = () => {
					u(), s(/* @__PURE__ */ new Error("useAgent(/* ... */).waitUntilFinished - signal aborted"));
				}, u = () => {
					e.off("stateChanged", a), r?.removeEventListener("abort", c);
				};
				e.on("stateChanged", a), r?.addEventListener("abort", c);
			});
		}, [e])
	};
}
function li(e) {
	const t = ri();
	if (e = e ?? t, !e) throw new Error("No session provided, make sure you are inside a Session context or pass the session explicitly");
	const { room: n, internal: { agentConnectTimeoutMilliseconds: r, agentTimeoutFailureReason: i, startAgentTimeout: o, clearAgentTimeout: s, clearAgentTimeoutFailureReason: a, updateAgentTimeoutState: c, updateAgentTimeoutParticipantExists: u } } = e, f = import_react.useMemo(() => new mn.EventEmitter(), []), d = vn({ room: n }), p = import_react.useMemo(() => d.find((L) => L.kind === ParticipantInfo_Kind.AGENT && !(ce$1.PublishOnBehalf in L.attributes)) ?? null, [d]), h = import_react.useMemo(() => p ? d.find((L) => L.kind === ParticipantInfo_Kind.AGENT && L.attributes[ce$1.PublishOnBehalf] === p.identity) ?? null : null, [p, d]), m = p?.attributes ?? {}, S = St([Track.Source.Camera, Track.Source.Microphone], {
		room: n,
		participantIdentity: p?.identity
	}), y = St([Track.Source.Camera, Track.Source.Microphone], {
		room: n,
		participantIdentity: h?.identity
	}), b = import_react.useMemo(() => S.find((L) => L.source === Track.Source.Camera) ?? y.find((L) => L.source === Track.Source.Camera), [S, y]);
	import_react.useEffect(() => {
		f.emit("cameraChanged", b);
	}, [f, b]);
	const w = import_react.useMemo(() => S.find((L) => L.source === Track.Source.Microphone) ?? y.find((L) => L.source === Track.Source.Microphone), [S, y]);
	import_react.useEffect(() => {
		f.emit("microphoneChanged", w);
	}, [f, w]);
	const [P, N] = import_react.useState(n.state);
	import_react.useEffect(() => {
		const L = (X) => {
			N(X);
		};
		return n.on(RoomEvent.ConnectionStateChanged, L), () => {
			n.off(RoomEvent.ConnectionStateChanged, L);
		};
	}, [n]), import_react.useEffect(() => {
		p && a();
	}, [p]);
	const [M, v] = import_react.useState(null);
	import_react.useEffect(() => {
		if (!p) return;
		const L = (X) => {
			X.identity === p?.identity && v("Agent left the room unexpectedly.");
		};
		return n.on(RoomEvent.ParticipantDisconnected, L), () => {
			n.off(RoomEvent.ParticipantDisconnected, L);
		};
	}, [p, n]), import_react.useEffect(() => {
		P === ConnectionState.Disconnected && v(null);
	}, [P]);
	const [g, E] = import_react.useState(() => n.localParticipant.getTrackPublication(Track.Source.Microphone) ?? null);
	import_react.useEffect(() => {
		const L = () => {
			E(n.localParticipant.getTrackPublication(Track.Source.Microphone) ?? null);
		}, X = () => {
			E(null);
		};
		return n.localParticipant.on(ParticipantEvent.LocalTrackPublished, L), n.localParticipant.on(ParticipantEvent.LocalTrackUnpublished, X), () => {
			n.localParticipant.off(ParticipantEvent.LocalTrackPublished, L), n.localParticipant.off(ParticipantEvent.LocalTrackUnpublished, X);
		};
	}, [n.localParticipant]);
	const x = import_react.useMemo(() => {
		const L = [];
		return i && L.push(i), M && L.push(M), L;
	}, [i, M]), C = import_react.useMemo(() => {
		if (x.length > 0) return "failed";
		let L = "disconnected";
		return P !== ConnectionState.Disconnected && (L = "connecting"), g && (L = "pre-connect-buffering"), p && m[ce$1.AgentState] && (L = m[ce$1.AgentState]), L;
	}, [
		x,
		P,
		g,
		p,
		m
	]);
	import_react.useEffect(() => {
		f.emit("stateChanged", C), c(C);
	}, [f, C]), import_react.useEffect(() => {
		u(p !== null);
	}, [p]);
	const I = e.connectionState === "disconnected";
	import_react.useEffect(() => {
		if (!I) return o(r), () => {
			s();
		};
	}, [I, r]);
	const { identity: T, name: A, metadata: D } = fc({ participant: p ?? void 0 }), U = import_react.useMemo(() => {
		const L = {
			attributes: m,
			internal: {
				agentParticipant: p,
				workerParticipant: h,
				emitter: f
			}
		};
		switch (C) {
			case "disconnected": return {
				...L,
				identity: void 0,
				name: void 0,
				metadata: void 0,
				state: C,
				...re(C),
				failureReasons: null,
				cameraTrack: void 0,
				microphoneTrack: void 0
			};
			case "connecting": return {
				...L,
				identity: void 0,
				name: void 0,
				metadata: void 0,
				state: C,
				...re(C),
				failureReasons: null,
				cameraTrack: void 0,
				microphoneTrack: void 0
			};
			case "initializing":
			case "idle": return {
				...L,
				identity: T,
				name: A,
				metadata: D,
				state: C,
				...re(C),
				failureReasons: null,
				cameraTrack: b,
				microphoneTrack: w
			};
			case "pre-connect-buffering": return {
				...L,
				identity: T,
				name: A,
				metadata: D,
				state: C,
				...re(C),
				failureReasons: null,
				cameraTrack: b,
				microphoneTrack: w
			};
			case "listening":
			case "thinking":
			case "speaking": return {
				...L,
				identity: T,
				name: A,
				metadata: D,
				state: C,
				...re(C),
				failureReasons: null,
				cameraTrack: b,
				microphoneTrack: w
			};
			case "failed": return {
				...L,
				identity: void 0,
				name: void 0,
				metadata: void 0,
				state: "failed",
				...re("failed"),
				failureReasons: x,
				cameraTrack: void 0,
				microphoneTrack: void 0
			};
		}
	}, [
		T,
		A,
		D,
		m,
		f,
		p,
		C,
		b,
		w
	]), { waitUntilConnected: H, waitUntilCouldBeListening: z, waitUntilFinished: V } = Dc(f, C), Q = import_react.useCallback((L) => new Promise((X, Re) => {
		const Se = (K) => {
			K && (F(), X(K));
		}, he = () => {
			F(), Re(/* @__PURE__ */ new Error("useAgent(/* ... */).waitUntilCamera - signal aborted"));
		}, F = () => {
			f.off("cameraChanged", Se), L?.removeEventListener("abort", he);
		};
		f.on("cameraChanged", Se), L?.addEventListener("abort", he);
	}), [f]), xe = import_react.useCallback((L) => new Promise((X, Re) => {
		const Se = (K) => {
			K && (F(), X(K));
		}, he = () => {
			F(), Re(/* @__PURE__ */ new Error("useAgent(/* ... */).waitUntilMicrophone - signal aborted"));
		}, F = () => {
			f.off("microphoneChanged", Se), L?.removeEventListener("abort", he);
		};
		f.on("microphoneChanged", Se), L?.addEventListener("abort", he);
	}), [f]);
	return import_react.useMemo(() => ({
		...U,
		waitUntilConnected: H,
		waitUntilCouldBeListening: z,
		waitUntilFinished: V,
		waitUntilCamera: Q,
		waitUntilMicrophone: xe
	}), [
		U,
		H,
		z,
		V,
		Q,
		xe
	]);
}
var Ju = /* @__PURE__ */ (function(e) {
	return e.ConnectionStateChanged = "connectionStateChanged", e.MediaDevicesError = "mediaDevicesError", e.EncryptionError = "encryptionError", e;
})({});
function Nc(e) {
	return typeof e == "object" && e !== null && "room" in e && "connectionState" in e && "internal" in e;
}
function Fc(e, t) {
	const n = /* @__PURE__ */ new Set([...Object.keys(e), ...Object.keys(t)]);
	for (const r of n) switch (r) {
		case "roomName":
		case "participantName":
		case "participantIdentity":
		case "participantMetadata":
		case "participantAttributes":
		case "agentName":
		case "agentMetadata":
		case "deployment":
			if (e[r] !== t[r]) return !1;
			break;
		default: throw new Error(`Options key ${r} not being checked for equality!`);
	}
	return !0;
}
function Uc(e, t) {
	const n = import_react.useRef(t);
	return import_react.useEffect(() => {
		n.current = t;
	}, [t]), import_react.useCallback(async (r, i) => {
		if (n.current !== r) return new Promise((o, s) => {
			const a = (f) => {
				f === r && (u(), o());
			}, c = () => {
				u(), s(/* @__PURE__ */ new Error(`useSession(/* ... */).waitUntilConnectionState(${r}, /* signal */) - signal aborted`));
			}, u = () => {
				e.off("connectionStateChanged", a), i?.removeEventListener("abort", c);
			};
			e.on("connectionStateChanged", a), i?.addEventListener("abort", c);
		});
	}, [e]);
}
function $c(e, t) {
	const n = e instanceof TokenSourceConfigurable, r = import_react.useRef(n ? t : null);
	return import_react.useEffect(() => {
		if (!n) {
			r.current = null;
			return;
		}
		r.current !== null && Fc(r.current, t) || (r.current = t);
	}, [n, t]), import_react.useCallback(async (i) => {
		if (n) {
			if (!r.current) throw new Error("AgentSession - memoized token fetch options are not set, but the passed tokenSource was an instance of TokenSourceConfigurable. If you are seeing this please make a new GitHub issue!");
			return e.fetch(r.current, i);
		} else return e.fetch();
	}, [n, e]);
}
function Yu(e, t = {}) {
	const { room: n, agentConnectTimeoutMilliseconds: r, encryption: i, ...o } = t, s = i && "e2eeManager" in i ? i.e2eeManager : null, a = i && !("e2eeManager" in i) ? i.key ?? null : null, c = i && !("e2eeManager" in i) ? i.worker ?? null : null, u = It(), f = import_react.useRef(null), d = import_react.useMemo(() => typeof a == "string" || a instanceof ArrayBuffer ? (f.current === null && (f.current = new ExternalE2EEKeyProvider()), f.current.setKey(a).catch((F) => R.error(F)), f.current) : a, [a]), p = import_react.useMemo(() => {
		const F = u ?? n;
		if (F) return F;
		const K = !!(d && c), ve = !!s, Rt = K || ve, De = {};
		K ? De.encryption = {
			keyProvider: d,
			worker: c
		} : ve ? De.encryption = { e2eeManager: s } : i !== void 0 && R.warn("useSession options encryption was set, but neither encryption.key with encryption.worker nor encryption.e2eeManager was provided.");
		const nt = new Room(De);
		return Rt && nt.setE2EEEnabled(!0), nt;
	}, [
		u,
		n,
		d,
		c,
		s
	]);
	import_react.useEffect(() => () => {
		p.disconnect();
	}, [p]);
	const h = import_react.useMemo(() => new mn.EventEmitter(), []), m = import_react.useCallback((F) => ({ isConnected: F === ConnectionState.Connected || F === ConnectionState.Reconnecting || F === ConnectionState.SignalReconnecting }), []), [S, y] = import_react.useState(p.state);
	import_react.useEffect(() => {
		const F = (K) => {
			y(K);
		};
		return p.on(RoomEvent.ConnectionStateChanged, F), () => {
			p.off(RoomEvent.ConnectionStateChanged, F);
		};
	}, [p]), import_react.useEffect(() => {
		const F = async (K) => {
			h.emit("mediaDevicesError", K);
		};
		return p.on(RoomEvent.MediaDevicesError, F), () => {
			p.off(RoomEvent.MediaDevicesError, F);
		};
	}, [p, h]), import_react.useEffect(() => {
		const F = async (K) => {
			h.emit("encryptionError", K);
		};
		return p.on(RoomEvent.EncryptionError, F), () => {
			p.off(RoomEvent.EncryptionError, F);
		};
	}, [p, h]);
	const { localParticipant: b } = ci({ room: p }), w = b.getTrackPublication(Track.Source.Camera), P = import_react.useMemo(() => {
		if (w) return {
			source: Track.Source.Camera,
			participant: b,
			publication: w
		};
	}, [b, w]), N = b.getTrackPublication(Track.Source.Microphone), M = import_react.useMemo(() => {
		if (N) return {
			source: Track.Source.Microphone,
			participant: b,
			publication: N
		};
	}, [b, N]), v = b.getTrackPublication(Track.Source.ScreenShare), g = import_react.useMemo(() => {
		if (v) return {
			source: Track.Source.ScreenShare,
			participant: b,
			publication: v
		};
	}, [b, v]), { agentTimeoutFailureReason: E, startAgentTimeout: x, clearAgentTimeout: C, clearAgentTimeoutFailureReason: I, updateAgentTimeoutState: T, updateAgentTimeoutParticipantExists: A } = Rc(), D = import_react.useMemo(() => ({
		emitter: h,
		tokenSource: e,
		agentConnectTimeoutMilliseconds: r,
		agentTimeoutFailureReason: E,
		startAgentTimeout: x,
		clearAgentTimeout: C,
		clearAgentTimeoutFailureReason: I,
		updateAgentTimeoutState: T,
		updateAgentTimeoutParticipantExists: A
	}), [
		h,
		r,
		e,
		E,
		x,
		C,
		I,
		T,
		A
	]), U = import_react.useMemo(() => {
		const F = {
			room: p,
			internal: D
		};
		switch (S) {
			case ConnectionState.Connecting: return {
				...F,
				connectionState: ConnectionState.Connecting,
				...m(ConnectionState.Connecting),
				local: {
					cameraTrack: void 0,
					microphoneTrack: void 0,
					screenShareTrack: void 0
				}
			};
			case ConnectionState.Connected:
			case ConnectionState.Reconnecting:
			case ConnectionState.SignalReconnecting: return {
				...F,
				connectionState: S,
				...m(S),
				local: {
					cameraTrack: P,
					microphoneTrack: M,
					screenShareTrack: g
				}
			};
			case ConnectionState.Disconnected: return {
				...F,
				connectionState: ConnectionState.Disconnected,
				...m(ConnectionState.Disconnected),
				local: {
					cameraTrack: void 0,
					microphoneTrack: void 0,
					screenShareTrack: void 0
				}
			};
		}
	}, [
		D,
		p,
		S,
		P,
		M,
		m
	]);
	import_react.useEffect(() => {
		h.emit("connectionStateChanged", U.connectionState);
	}, [h, U.connectionState]);
	const H = Uc(h, U.connectionState), z = import_react.useCallback(async (F) => H(ConnectionState.Connected, F), [H]), V = import_react.useCallback(async (F) => H(ConnectionState.Disconnected, F), [H]), Q = import_react.useCallback(async (F) => p.setE2EEEnabled(F), [p]), xe = li(import_react.useMemo(() => ({
		connectionState: U.connectionState,
		room: p,
		internal: D
	}), [
		U,
		p,
		D
	])), L = $c(e, o), X = import_react.useRef(!1), Re = import_react.useCallback(async (F = {}) => {
		const { signal: K, tracks: ve = { microphone: {
			enabled: !0,
			publishOptions: { preConnectBuffer: !0 }
		} }, roomConnectOptions: Rt } = F;
		await V(K), X.current = !1;
		const De = () => {
			p.disconnect();
		};
		K?.addEventListener("abort", De);
		const nt = () => {
			X.current || L(!0);
		};
		p.once(RoomEvent.Disconnected, nt);
		let gn = !1;
		await Promise.all([
			L().then(({ serverUrl: fi, participantToken: bn }) => (gn = (decodeTokenPayload(bn).roomConfig?.agents?.length ?? 0) > 0, p.connect(fi, bn, Rt))),
			ve.microphone?.enabled ? p.localParticipant.setMicrophoneEnabled(!0, void 0, ve.microphone?.publishOptions ?? {}) : Promise.resolve(),
			ve.camera?.enabled ? p.localParticipant.setCameraEnabled(!0, void 0, ve.camera?.publishOptions ?? {}) : Promise.resolve(),
			ve.screenShare?.enabled ? p.localParticipant.setScreenShareEnabled(!0, void 0, ve.screenShare?.publishOptions ?? {}) : Promise.resolve()
		]), await z(K), gn && await xe.waitUntilConnected(K), K?.removeEventListener("abort", De);
	}, [
		p,
		V,
		L,
		z,
		xe.waitUntilConnected
	]), Se = import_react.useCallback(async () => {
		X.current = !0, L(!0), await p.disconnect();
	}, [p, L]), he = import_react.useCallback(async () => {
		const F = await L();
		await p.prepareConnection(F.serverUrl, F.participantToken);
	}, [L, p]);
	return import_react.useEffect(() => {
		he().catch((F) => {
			console.warn("WARNING: Room.prepareConnection failed:", F);
		});
	}, []), import_react.useMemo(() => ({
		...U,
		waitUntilConnected: z,
		waitUntilDisconnected: V,
		prepareConnection: he,
		start: Re,
		end: Se,
		setEncryptionEnabled: Q
	}), [
		U,
		z,
		V,
		he,
		Re,
		Se,
		Q
	]);
}
function Qu(e, t, n, r) {
	const i = import_react.useMemo(() => () => {}, []), o = import_react.useCallback(n ?? i, r ?? []), s = r ? o : n, a = import_react.useMemo(() => e ? "internal" in e ? e.internal.emitter : e : null, [e]);
	import_react.useEffect(() => {
		if (!(!a || !s)) return a.on(t, s), () => {
			a.off(t, s);
		};
	}, [
		a,
		t,
		s
	]);
}
var Xu = /* @__PURE__ */ (function(e) {
	return e.MessageReceived = "messageReceived", e;
})({});
function Zu(e) {
	const { room: t } = ii(e), n = import_react.useMemo(() => new mn.EventEmitter(), []), r = li(e), i = ui({ room: t }), o = gc(import_react.useMemo(() => ({ room: t }), [t])), s = import_react.useMemo(() => i.map((d) => {
		switch (d.participantInfo.identity) {
			case t.localParticipant.identity: return {
				type: "userTranscript",
				message: d.text,
				id: d.streamInfo.id,
				timestamp: d.streamInfo.timestamp,
				attributes: d.streamInfo.attributes,
				from: t.localParticipant
			};
			case r.internal.agentParticipant?.identity:
			case r.internal.workerParticipant?.identity: return {
				type: "agentTranscript",
				message: d.text,
				id: d.streamInfo.id,
				timestamp: d.streamInfo.timestamp,
				attributes: d.streamInfo.attributes,
				from: r.internal.agentParticipant?.identity === d.participantInfo.identity ? r.internal.agentParticipant : r.internal.workerParticipant
			};
			default: return {
				type: "agentTranscript",
				message: d.text,
				id: d.streamInfo.id,
				timestamp: d.streamInfo.timestamp,
				attributes: d.streamInfo.attributes,
				from: Array.from(t.remoteParticipants.values()).find((p) => p.identity === d.participantInfo.identity)
			};
		}
	}), [i, t]), a = import_react.useMemo(() => [...s, ...o.chatMessages], [s, o.chatMessages]), c = import_react.useRef(/* @__PURE__ */ new Map()), u = import_react.useMemo(() => {
		const d = /* @__PURE__ */ new Date();
		for (const p of a) c.current.has(p.id) || c.current.set(p.id, d);
		return a.sort((p, h) => {
			const m = c.current.get(p.id), S = c.current.get(h.id);
			return typeof m > "u" || typeof S > "u" ? 0 : m.getTime() - S.getTime();
		});
	}, [a]), f = import_react.useRef(/* @__PURE__ */ new Set());
	return import_react.useEffect(() => {
		for (const d of u) f.current.has(d.id) || (f.current.add(d.id), n.emit("messageReceived", d));
	}, [u]), import_react.useMemo(() => ({
		messages: u,
		send: o.send,
		isSending: o.isSending,
		internal: { emitter: n }
	}), [
		u,
		o.send,
		o.isSending
	]);
}
function el(e, t, n, r) {
	let i, o, s, a;
	Nc(e) ? (i = e, o = t, s = n, a = r) : (o = e, s = t, a = n);
	const { room: c } = ii(i), u = import_react.useRef(s);
	u.current = s;
	const f = import_react.useRef(a);
	f.current = a, import_react.useEffect(() => {
		if (o) return c.registerRpcMethod(o, async (p) => {
			const h = f.current?.fromIdentity;
			if (h && p.callerIdentity !== h) throw RpcError.builtIn("UNSUPPORTED_METHOD", `Method not available for caller ${p.callerIdentity}`);
			const m = u.current;
			if (!m) throw RpcError.builtIn("APPLICATION_ERROR", `No handler registered for method "${o}"`);
			const S = f.current?.serializer ?? serializers.json();
			let y;
			try {
				y = S.parse(p.payload);
			} catch (w) {
				throw RpcError.builtIn("APPLICATION_ERROR", `Failed to parse RPC payload: ${w}`);
			}
			const b = await m(y, p);
			try {
				return S.serialize(b);
			} catch (w) {
				throw RpcError.builtIn("APPLICATION_ERROR", `Failed to serialize RPC response: ${w}`);
			}
		}), () => {
			c.unregisterRpcMethod(o);
		};
	}, [c, o]);
	const d = import_react.useCallback(async (p, h = serializers.json()) => {
		if (isSerializer(h)) {
			let m;
			try {
				m = h.serialize(p.payload);
			} catch (y) {
				throw RpcError.builtIn("APPLICATION_ERROR", `Failed to serialize RPC payload: ${y}`);
			}
			const S = await c.localParticipant.performRpc({
				destinationIdentity: p.destinationIdentity,
				method: p.method,
				payload: m,
				responseTimeout: p.responseTimeout
			});
			try {
				return h.parse(S);
			} catch (y) {
				throw RpcError.builtIn("APPLICATION_ERROR", `Failed to parse RPC response: ${y}`);
			}
		} else return c.localParticipant.performRpc(p);
	}, [c]);
	return import_react.useMemo(() => ({ perform: d }), [d]);
}
var tl = /* @__PURE__ */ import_react.forwardRef(function(t, n) {
	const { room: r, htmlProps: i } = uc(t);
	return /* @__PURE__ */ import_react.createElement("div", {
		ref: n,
		...i
	}, r && /* @__PURE__ */ import_react.createElement(hn.Provider, { value: r }, /* @__PURE__ */ import_react.createElement(oi.Provider, { value: t.featureFlags }, t.children)));
});
//#endregion
//#region node_modules/@livekit/components-react/dist/prefabs-BEB1UEnC.mjs
function Qt(t) {
	const a = (i) => typeof window < "u" ? window.matchMedia(i).matches : !1, [n, c] = import_react.useState(a(t));
	function r() {
		c(a(t));
	}
	return import_react.useEffect(() => {
		const i = window.matchMedia(t);
		return r(), i.addListener ? i.addListener(r) : i.addEventListener("change", r), () => {
			i.removeListener ? i.removeListener(r) : i.removeEventListener("change", r);
		};
	}, [t]), n;
}
var Te = /* @__PURE__ */ import_react.forwardRef(function(a, n) {
	const { mergedProps: c } = Ou({ props: a });
	return /* @__PURE__ */ import_react.createElement("button", {
		ref: n,
		...c
	}, a.children);
});
var Pe = /* @__PURE__ */ import_react.forwardRef(function(a, n) {
	const { buttonProps: c } = du(a);
	return /* @__PURE__ */ import_react.createElement("button", {
		ref: n,
		...c
	}, a.children);
});
var Gt = (t) => /* @__PURE__ */ import_react.createElement("svg", {
	xmlns: "http://www.w3.org/2000/svg",
	width: 16,
	height: 16,
	fill: "currentColor",
	...t
}, /* @__PURE__ */ import_react.createElement("path", { d: "M1.354.646a.5.5 0 1 0-.708.708l14 14a.5.5 0 0 0 .708-.708L11 10.293V4.5A1.5 1.5 0 0 0 9.5 3H3.707zM0 4.5a1.5 1.5 0 0 1 .943-1.393l9.532 9.533c-.262.224-.603.36-.975.36h-8A1.5 1.5 0 0 1 0 11.5z" }), /* @__PURE__ */ import_react.createElement("path", { d: "m15.2 3.6-2.8 2.1a1 1 0 0 0-.4.8v3a1 1 0 0 0 .4.8l2.8 2.1a.5.5 0 0 0 .8-.4V4a.5.5 0 0 0-.8-.4" }));
var Kt = (t) => /* @__PURE__ */ import_react.createElement("svg", {
	xmlns: "http://www.w3.org/2000/svg",
	width: 16,
	height: 16,
	fill: "currentColor",
	...t
}, /* @__PURE__ */ import_react.createElement("path", { d: "M0 4.5A1.5 1.5 0 0 1 1.5 3h8A1.5 1.5 0 0 1 11 4.5v7A1.5 1.5 0 0 1 9.5 13h-8A1.5 1.5 0 0 1 0 11.5zM15.2 3.6l-2.8 2.1a1 1 0 0 0-.4.8v3a1 1 0 0 0 .4.8l2.8 2.1a.5.5 0 0 0 .8-.4V4a.5.5 0 0 0-.8-.4" }));
var Jt = (t) => /* @__PURE__ */ import_react.createElement("svg", {
	xmlns: "http://www.w3.org/2000/svg",
	width: 16,
	height: 16,
	viewBox: "0 0 24 24",
	...t
}, /* @__PURE__ */ import_react.createElement("path", {
	fill: "#FFF",
	d: "M4.99 3.99a1 1 0 0 0-.697 1.717L10.586 12l-6.293 6.293a1 1 0 1 0 1.414 1.414L12 13.414l6.293 6.293a1 1 0 1 0 1.414-1.414L13.414 12l6.293-6.293a1 1 0 0 0-.727-1.717 1 1 0 0 0-.687.303L12 10.586 5.707 4.293a1 1 0 0 0-.717-.303"
}));
var Zt = (t) => /* @__PURE__ */ import_react.createElement("svg", {
	xmlns: "http://www.w3.org/2000/svg",
	width: 16,
	height: 18,
	fill: "none",
	...t
}, /* @__PURE__ */ import_react.createElement("path", {
	fill: "currentColor",
	fillRule: "evenodd",
	d: "M0 2.75A2.75 2.75 0 0 1 2.75 0h10.5A2.75 2.75 0 0 1 16 2.75v13.594a.75.75 0 0 1-1.234.572l-3.691-3.12a1.25 1.25 0 0 0-.807-.296H2.75A2.75 2.75 0 0 1 0 10.75zM2.75 1.5c-.69 0-1.25.56-1.25 1.25v8c0 .69.56 1.25 1.25 1.25h7.518c.65 0 1.279.23 1.775.65l2.457 2.077V2.75c0-.69-.56-1.25-1.25-1.25z",
	clipRule: "evenodd"
}), /* @__PURE__ */ import_react.createElement("path", {
	fill: "currentColor",
	fillRule: "evenodd",
	d: "M3 4.5a.5.5 0 0 1 .5-.5h9a.5.5 0 0 1 0 1h-9a.5.5 0 0 1-.5-.5m0 2a.5.5 0 0 1 .5-.5h9a.5.5 0 0 1 0 1h-9a.5.5 0 0 1-.5-.5m0 2a.5.5 0 0 1 .5-.5h5a.5.5 0 0 1 0 1h-5a.5.5 0 0 1-.5-.5",
	clipRule: "evenodd"
}));
var Ee = (t) => /* @__PURE__ */ import_react.createElement("svg", {
	xmlns: "http://www.w3.org/2000/svg",
	width: 16,
	height: 16,
	fill: "none",
	...t
}, /* @__PURE__ */ import_react.createElement("path", {
	fill: "currentcolor",
	fillRule: "evenodd",
	d: "M5.293 2.293a1 1 0 0 1 1.414 0l4.823 4.823a1.25 1.25 0 0 1 0 1.768l-4.823 4.823a1 1 0 0 1-1.414-1.414L9.586 8 5.293 3.707a1 1 0 0 1 0-1.414",
	clipRule: "evenodd"
}));
var Xt = (t) => /* @__PURE__ */ import_react.createElement("svg", {
	xmlns: "http://www.w3.org/2000/svg",
	width: 16,
	height: 16,
	fill: "none",
	...t
}, /* @__PURE__ */ import_react.createElement("g", {
	stroke: "currentColor",
	strokeLinecap: "round",
	strokeLinejoin: "round",
	strokeWidth: 1.5
}, /* @__PURE__ */ import_react.createElement("path", { d: "M10 1.75h4.25m0 0V6m0-4.25L9 7M6 14.25H1.75m0 0V10m0 4.25L7 9" })));
var Yt = (t) => /* @__PURE__ */ import_react.createElement("svg", {
	xmlns: "http://www.w3.org/2000/svg",
	width: 16,
	height: 16,
	fill: "none",
	...t
}, /* @__PURE__ */ import_react.createElement("path", {
	fill: "currentcolor",
	fillRule: "evenodd",
	d: "M8.961.894C8.875-.298 7.125-.298 7.04.894c-.066.912-1.246 1.228-1.76.472-.67-.99-2.186-.115-1.664.96.399.824-.465 1.688-1.288 1.289-1.076-.522-1.95.994-.961 1.665.756.513.44 1.693-.472 1.759-1.192.086-1.192 1.836 0 1.922.912.066 1.228 1.246.472 1.76-.99.67-.115 2.186.96 1.664.824-.399 1.688.465 1.289 1.288-.522 1.076.994 1.95 1.665.961.513-.756 1.693-.44 1.759.472.086 1.192 1.836 1.192 1.922 0 .066-.912 1.246-1.228 1.76-.472.67.99 2.186.115 1.664-.96-.399-.824.465-1.687 1.288-1.289 1.076.522 1.95-.994.961-1.665-.756-.513-.44-1.693.472-1.759 1.192-.086 1.192-1.836 0-1.922-.912-.066-1.228-1.246-.472-1.76.99-.67.115-2.186-.96-1.664-.824.399-1.687-.465-1.289-1.288.522-1.076-.994-1.95-1.665-.961-.513.756-1.693.44-1.759-.472M8 13A5 5 0 1 0 8 3a5 5 0 0 0 0 10",
	clipRule: "evenodd"
}));
var ea = (t) => /* @__PURE__ */ import_react.createElement("svg", {
	xmlns: "http://www.w3.org/2000/svg",
	width: 16,
	height: 16,
	fill: "none",
	...t
}, /* @__PURE__ */ import_react.createElement("path", {
	fill: "currentColor",
	fillRule: "evenodd",
	d: "M2 2.75A2.75 2.75 0 0 1 4.75 0h6.5A2.75 2.75 0 0 1 14 2.75v10.5A2.75 2.75 0 0 1 11.25 16h-6.5A2.75 2.75 0 0 1 2 13.25v-.5a.75.75 0 0 1 1.5 0v.5c0 .69.56 1.25 1.25 1.25h6.5c.69 0 1.25-.56 1.25-1.25V2.75c0-.69-.56-1.25-1.25-1.25h-6.5c-.69 0-1.25.56-1.25 1.25v.5a.75.75 0 0 1-1.5 0z",
	clipRule: "evenodd"
}), /* @__PURE__ */ import_react.createElement("path", {
	fill: "currentColor",
	fillRule: "evenodd",
	d: "M8.78 7.47a.75.75 0 0 1 0 1.06l-2.25 2.25a.75.75 0 1 1-1.06-1.06l.97-.97H1.75a.75.75 0 0 1 0-1.5h4.69l-.97-.97a.75.75 0 0 1 1.06-1.06z",
	clipRule: "evenodd"
}));
var ta = (t) => /* @__PURE__ */ import_react.createElement("svg", {
	xmlns: "http://www.w3.org/2000/svg",
	width: 16,
	height: 16,
	fill: "none",
	...t
}, /* @__PURE__ */ import_react.createElement("path", {
	fill: "currentcolor",
	fillRule: "evenodd",
	d: "M4 6.104V4a4 4 0 1 1 8 0v2.104c1.154.326 2 1.387 2 2.646v4.5A2.75 2.75 0 0 1 11.25 16h-6.5A2.75 2.75 0 0 1 2 13.25v-4.5c0-1.259.846-2.32 2-2.646M5.5 4a2.5 2.5 0 0 1 5 0v2h-5z",
	clipRule: "evenodd"
}));
var aa = (t) => /* @__PURE__ */ import_react.createElement("svg", {
	xmlns: "http://www.w3.org/2000/svg",
	width: 16,
	height: 16,
	fill: "currentColor",
	...t
}, /* @__PURE__ */ import_react.createElement("path", { d: "M12.227 11.52a5.48 5.48 0 0 0 1.246-2.97.5.5 0 0 0-.995-.1 4.5 4.5 0 0 1-.962 2.359l-1.07-1.07C10.794 9.247 11 8.647 11 8V3a3 3 0 0 0-6 0v1.293L1.354.646a.5.5 0 1 0-.708.708l14 14a.5.5 0 0 0 .708-.708zM8 12.5c.683 0 1.33-.152 1.911-.425l.743.743c-.649.359-1.378.59-2.154.66V15h2a.5.5 0 0 1 0 1h-5a.5.5 0 0 1 0-1h2v-1.522a5.5 5.5 0 0 1-4.973-4.929.5.5 0 0 1 .995-.098A4.5 4.5 0 0 0 8 12.5" }), /* @__PURE__ */ import_react.createElement("path", { d: "M8.743 10.907 5 7.164V8a3 3 0 0 0 3.743 2.907" }));
var na = (t) => /* @__PURE__ */ import_react.createElement("svg", {
	xmlns: "http://www.w3.org/2000/svg",
	width: 16,
	height: 16,
	fill: "currentColor",
	...t
}, /* @__PURE__ */ import_react.createElement("path", {
	fillRule: "evenodd",
	d: "M2.975 8.002a.5.5 0 0 1 .547.449 4.5 4.5 0 0 0 8.956 0 .5.5 0 1 1 .995.098A5.5 5.5 0 0 1 8.5 13.478V15h2a.5.5 0 0 1 0 1h-5a.5.5 0 0 1 0-1h2v-1.522a5.5 5.5 0 0 1-4.973-4.929.5.5 0 0 1 .448-.547",
	clipRule: "evenodd"
}), /* @__PURE__ */ import_react.createElement("path", { d: "M5 3a3 3 0 1 1 6 0v5a3 3 0 0 1-6 0z" }));
var ra = (t) => /* @__PURE__ */ import_react.createElement("svg", {
	xmlns: "http://www.w3.org/2000/svg",
	width: 16,
	height: 16,
	fill: "currentcolor",
	...t
}, /* @__PURE__ */ import_react.createElement("path", { d: "M0 11.5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v4a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5zm6-5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v9a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5zm6-6a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v15a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5z" }), /* @__PURE__ */ import_react.createElement("path", { d: "M0 11.5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v4a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5zm6-5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v9a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5zm6-6a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v15a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5z" }));
var ca = (t) => /* @__PURE__ */ import_react.createElement("svg", {
	xmlns: "http://www.w3.org/2000/svg",
	width: 16,
	height: 16,
	fill: "currentcolor",
	...t
}, /* @__PURE__ */ import_react.createElement("path", { d: "M0 11.5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v4a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5zm6-5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v9a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5z" }), /* @__PURE__ */ import_react.createElement("path", { d: "M0 11.5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v4a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5zm6-5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v9a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5z" }), /* @__PURE__ */ import_react.createElement("g", { opacity: .25 }, /* @__PURE__ */ import_react.createElement("path", { d: "M12 .5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v15a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5z" }), /* @__PURE__ */ import_react.createElement("path", { d: "M12 .5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v15a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5z" })));
var la = (t) => /* @__PURE__ */ import_react.createElement("svg", {
	xmlns: "http://www.w3.org/2000/svg",
	width: 16,
	height: 16,
	fill: "currentcolor",
	...t
}, /* @__PURE__ */ import_react.createElement("path", { d: "M0 11.5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v4a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5z" }), /* @__PURE__ */ import_react.createElement("path", { d: "M0 11.5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v4a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5z" }), /* @__PURE__ */ import_react.createElement("g", { opacity: .25 }, /* @__PURE__ */ import_react.createElement("path", { d: "M6 6.5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v9a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5z" }), /* @__PURE__ */ import_react.createElement("path", { d: "M6 6.5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v9a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5zm6-6a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v15a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5z" }), /* @__PURE__ */ import_react.createElement("path", { d: "M12 .5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v15a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5z" })));
var ia = (t) => /* @__PURE__ */ import_react.createElement("svg", {
	xmlns: "http://www.w3.org/2000/svg",
	width: 16,
	height: 16,
	fill: "currentColor",
	...t
}, /* @__PURE__ */ import_react.createElement("g", { opacity: .25 }, /* @__PURE__ */ import_react.createElement("path", { d: "M0 11.5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v4a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5zm6-5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v9a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5zm6-6a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v15a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5z" }), /* @__PURE__ */ import_react.createElement("path", { d: "M0 11.5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v4a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5zm6-5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v9a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5zm6-6a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v15a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5z" })));
var Ae = (t) => /* @__PURE__ */ import_react.createElement("svg", {
	xmlns: "http://www.w3.org/2000/svg",
	width: 20,
	height: 16,
	fill: "none",
	...t
}, /* @__PURE__ */ import_react.createElement("path", {
	fill: "currentColor",
	fillRule: "evenodd",
	d: "M0 2.75A2.75 2.75 0 0 1 2.75 0h14.5A2.75 2.75 0 0 1 20 2.75v10.5A2.75 2.75 0 0 1 17.25 16H2.75A2.75 2.75 0 0 1 0 13.25zM2.75 1.5c-.69 0-1.25.56-1.25 1.25v10.5c0 .69.56 1.25 1.25 1.25h14.5c.69 0 1.25-.56 1.25-1.25V2.75c0-.69-.56-1.25-1.25-1.25z",
	clipRule: "evenodd"
}), /* @__PURE__ */ import_react.createElement("path", {
	fill: "currentColor",
	fillRule: "evenodd",
	d: "M9.47 4.22a.75.75 0 0 1 1.06 0l2.25 2.25a.75.75 0 0 1-1.06 1.06l-.97-.97v4.69a.75.75 0 0 1-1.5 0V6.56l-.97.97a.75.75 0 0 1-1.06-1.06z",
	clipRule: "evenodd"
}));
var sa = (t) => /* @__PURE__ */ import_react.createElement("svg", {
	xmlns: "http://www.w3.org/2000/svg",
	width: 20,
	height: 16,
	fill: "none",
	...t
}, /* @__PURE__ */ import_react.createElement("g", { fill: "currentColor" }, /* @__PURE__ */ import_react.createElement("path", { d: "M7.28 4.22a.75.75 0 0 0-1.06 1.06L8.94 8l-2.72 2.72a.75.75 0 1 0 1.06 1.06L10 9.06l2.72 2.72a.75.75 0 1 0 1.06-1.06L11.06 8l2.72-2.72a.75.75 0 0 0-1.06-1.06L10 6.94z" }), /* @__PURE__ */ import_react.createElement("path", {
	fillRule: "evenodd",
	d: "M2.75 0A2.75 2.75 0 0 0 0 2.75v10.5A2.75 2.75 0 0 0 2.75 16h14.5A2.75 2.75 0 0 0 20 13.25V2.75A2.75 2.75 0 0 0 17.25 0zM1.5 2.75c0-.69.56-1.25 1.25-1.25h14.5c.69 0 1.25.56 1.25 1.25v10.5c0 .69-.56 1.25-1.25 1.25H2.75c-.69 0-1.25-.56-1.25-1.25z",
	clipRule: "evenodd"
})));
var ke = (t) => /* @__PURE__ */ import_react.createElement("svg", {
	xmlns: "http://www.w3.org/2000/svg",
	width: 16,
	height: 16,
	fill: "none",
	...t
}, /* @__PURE__ */ import_react.createElement("path", {
	fill: "currentColor",
	fillRule: "evenodd",
	d: "M8 0a.75.75 0 0 1 .75.75v2.5a.75.75 0 0 1-1.5 0V.75A.75.75 0 0 1 8 0",
	clipRule: "evenodd"
}), /* @__PURE__ */ import_react.createElement("path", {
	fill: "currentColor",
	fillRule: "evenodd",
	d: "M8 12a.75.75 0 0 1 .75.75v2.5a.75.75 0 0 1-1.5 0v-2.5A.75.75 0 0 1 8 12",
	clipRule: "evenodd",
	opacity: .7
}), /* @__PURE__ */ import_react.createElement("path", {
	fill: "currentColor",
	fillRule: "evenodd",
	d: "M12 1.072a.75.75 0 0 1 .274 1.024l-1.25 2.165a.75.75 0 0 1-1.299-.75l1.25-2.165A.75.75 0 0 1 12 1.072",
	clipRule: "evenodd"
}), /* @__PURE__ */ import_react.createElement("path", {
	fill: "currentColor",
	fillRule: "evenodd",
	d: "M6 11.464a.75.75 0 0 1 .274 1.025l-1.25 2.165a.75.75 0 0 1-1.299-.75l1.25-2.165A.75.75 0 0 1 6 11.464",
	clipRule: "evenodd",
	opacity: .6
}), /* @__PURE__ */ import_react.createElement("path", {
	fill: "currentColor",
	fillRule: "evenodd",
	d: "M14.928 4a.75.75 0 0 1-.274 1.025l-2.165 1.25a.75.75 0 1 1-.75-1.3l2.165-1.25A.75.75 0 0 1 14.928 4",
	clipRule: "evenodd"
}), /* @__PURE__ */ import_react.createElement("path", {
	fill: "currentColor",
	fillRule: "evenodd",
	d: "M4.536 10a.75.75 0 0 1-.275 1.024l-2.165 1.25a.75.75 0 0 1-.75-1.298l2.165-1.25A.75.75 0 0 1 4.536 10",
	clipRule: "evenodd",
	opacity: .5
}), /* @__PURE__ */ import_react.createElement("path", {
	fill: "currentColor",
	fillRule: "evenodd",
	d: "M16 8a.75.75 0 0 1-.75.75h-2.5a.75.75 0 0 1 0-1.5h2.5A.75.75 0 0 1 16 8",
	clipRule: "evenodd"
}), /* @__PURE__ */ import_react.createElement("path", {
	fill: "currentColor",
	fillRule: "evenodd",
	d: "M4 8a.75.75 0 0 1-.75.75H.75a.75.75 0 0 1 0-1.5h2.5A.75.75 0 0 1 4 8",
	clipRule: "evenodd",
	opacity: .4
}), /* @__PURE__ */ import_react.createElement("path", {
	fill: "currentColor",
	fillRule: "evenodd",
	d: "M14.928 12a.75.75 0 0 1-1.024.274l-2.165-1.25a.75.75 0 0 1 .75-1.299l2.165 1.25A.75.75 0 0 1 14.928 12",
	clipRule: "evenodd",
	opacity: .9
}), /* @__PURE__ */ import_react.createElement("path", {
	fill: "currentColor",
	fillRule: "evenodd",
	d: "M4.536 6a.75.75 0 0 1-1.025.275l-2.165-1.25a.75.75 0 1 1 .75-1.3l2.165 1.25A.75.75 0 0 1 4.536 6",
	clipRule: "evenodd",
	opacity: .3
}), /* @__PURE__ */ import_react.createElement("path", {
	fill: "currentColor",
	fillRule: "evenodd",
	d: "M12 14.928a.75.75 0 0 1-1.024-.274l-1.25-2.165a.75.75 0 0 1 1.298-.75l1.25 2.165A.75.75 0 0 1 12 14.928",
	clipRule: "evenodd",
	opacity: .8
}), /* @__PURE__ */ import_react.createElement("path", {
	fill: "currentColor",
	fillRule: "evenodd",
	d: "M6 4.536a.75.75 0 0 1-1.024-.275l-1.25-2.165a.75.75 0 1 1 1.299-.75l1.25 2.165A.75.75 0 0 1 6 4.536",
	clipRule: "evenodd",
	opacity: .2
}));
var oa = (t) => /* @__PURE__ */ import_react.createElement("svg", {
	xmlns: "http://www.w3.org/2000/svg",
	width: 16,
	height: 16,
	fill: "none",
	...t
}, /* @__PURE__ */ import_react.createElement("g", {
	stroke: "currentColor",
	strokeLinecap: "round",
	strokeLinejoin: "round",
	strokeWidth: 1.5
}, /* @__PURE__ */ import_react.createElement("path", { d: "M13.25 7H9m0 0V2.75M9 7l5.25-5.25M2.75 9H7m0 0v4.25M7 9l-5.25 5.25" })));
var ua = /* @__PURE__ */ import_react.forwardRef(function({ trackRef: a, ...n }, c) {
	const r = ei(), { mergedProps: i, inFocus: o } = pu({
		trackRef: a ?? r,
		props: n
	});
	return /* @__PURE__ */ import_react.createElement(Yr.Consumer, null, (l) => l !== void 0 && /* @__PURE__ */ import_react.createElement("button", {
		ref: c,
		...i
	}, n.children ? n.children : o ? /* @__PURE__ */ import_react.createElement(oa, null) : /* @__PURE__ */ import_react.createElement(Xt, null)));
});
var ce = /* @__PURE__ */ import_react.forwardRef(function({ kind: a, initialSelection: n, onActiveDeviceChange: c, onDeviceListChange: r, onDeviceSelectError: i, exactMatch: o, track: l, requestPermissions: s, onError: d, ...u }, h) {
	const m = It(), g = import_react.useRef("default"), { devices: f, activeDeviceId: E, setActiveMediaDevice: p, className: C } = yu({
		kind: a,
		room: m,
		track: l,
		requestPermissions: s,
		onError: import_react.useCallback((v) => {
			m && m.emit(RoomEvent.MediaDevicesError, v), d?.(v);
		}, [m, d])
	});
	import_react.useEffect(() => {
		n !== void 0 && p(n);
	}, [p]), import_react.useEffect(() => {
		typeof r == "function" && r(f);
	}, [r, f]), import_react.useEffect(() => {
		E !== g.current && c?.(E), g.current = E;
	}, [E]);
	const k = async (v) => {
		try {
			await p(v, { exact: o ?? !0 });
		} catch (w) {
			if (w instanceof Error) i?.(w);
			else throw w;
		}
	}, R = import_react.useMemo(() => vu(u, { className: C }, { className: "lk-list" }), [C, u]), M = !!f.find((v) => v.label.toLowerCase().startsWith("default"));
	function D(v, w, b) {
		return v === w || !M && b === 0 && w === "default";
	}
	return /* @__PURE__ */ import_react.createElement("ul", {
		ref: h,
		...R
	}, f.map((v, w) => /* @__PURE__ */ import_react.createElement("li", {
		key: v.deviceId,
		id: v.deviceId,
		"data-lk-active": D(v.deviceId, E, w),
		"aria-selected": D(v.deviceId, E, w),
		role: "option"
	}, /* @__PURE__ */ import_react.createElement("button", {
		className: "lk-button",
		onClick: () => k(v.deviceId)
	}, v.label))));
});
var Ne = /* @__PURE__ */ import_react.forwardRef(function({ label: a, ...n }, c) {
	const r = _t(), { mergedProps: i, canPlayAudio: o } = Mu({
		room: r,
		props: n
	}), { mergedProps: l, canPlayVideo: s } = Au({
		room: r,
		props: i
	}), { style: d, ...u } = l;
	return d.display = o && s ? "none" : "block", /* @__PURE__ */ import_react.createElement("button", {
		ref: c,
		style: d,
		...u
	}, a ?? `Start ${o ? "Video" : "Audio"}`);
});
function xe(t, a) {
	switch (t) {
		case Track.Source.Microphone: return a ? /* @__PURE__ */ import_react.createElement(na, null) : /* @__PURE__ */ import_react.createElement(aa, null);
		case Track.Source.Camera: return a ? /* @__PURE__ */ import_react.createElement(Kt, null) : /* @__PURE__ */ import_react.createElement(Gt, null);
		case Track.Source.ScreenShare: return a ? /* @__PURE__ */ import_react.createElement(sa, null) : /* @__PURE__ */ import_react.createElement(Ae, null);
		default: return;
	}
}
function da(t) {
	switch (t) {
		case ConnectionQuality.Excellent: return /* @__PURE__ */ import_react.createElement(ra, null);
		case ConnectionQuality.Good: return /* @__PURE__ */ import_react.createElement(ca, null);
		case ConnectionQuality.Poor: return /* @__PURE__ */ import_react.createElement(la, null);
		default: return /* @__PURE__ */ import_react.createElement(ia, null);
	}
}
var j = /* @__PURE__ */ import_react.forwardRef(function({ showIcon: a, ...n }, c) {
	const { buttonProps: r, enabled: i } = Ru(n), [o, l] = import_react.useState(!1);
	return import_react.useEffect(() => {
		l(!0);
	}, []), o && /* @__PURE__ */ import_react.createElement("button", {
		ref: c,
		...r
	}, (a ?? !0) && xe(n.source, i), n.children);
});
var Le = /* @__PURE__ */ import_react.forwardRef(function(a, n) {
	const { className: c, quality: r } = lu(a), i = import_react.useMemo(() => ({
		...vu(a, { className: c }),
		"data-lk-quality": r
	}), [
		r,
		a,
		c
	]);
	return /* @__PURE__ */ import_react.createElement("div", {
		ref: n,
		...i
	}, a.children ?? da(r));
});
var le = /* @__PURE__ */ import_react.forwardRef(function({ participant: a, ...n }, c) {
	const r = Ie(a), { className: i, infoObserver: o } = import_react.useMemo(() => eu(r), [r]), { identity: l, name: s } = j$1(o, {
		name: r.name,
		identity: r.identity,
		metadata: r.metadata
	}), d = import_react.useMemo(() => vu(n, {
		className: i,
		"data-lk-participant-name": s
	}), [
		n,
		i,
		s
	]);
	return /* @__PURE__ */ import_react.createElement("span", {
		ref: c,
		...d
	}, s !== "" ? s : l, n.children);
});
var De = /* @__PURE__ */ import_react.forwardRef(function({ trackRef: a, show: n = "always", ...c }, r) {
	const { className: i, isMuted: o } = Iu(a), l = n === "always" || n === "muted" && o || n === "unmuted" && !o, s = import_react.useMemo(() => vu(c, { className: i }), [i, c]);
	return l ? /* @__PURE__ */ import_react.createElement("div", {
		ref: r,
		...s,
		"data-lk-muted": o
	}, c.children ?? xe(a.source, !o)) : null;
});
var Ve = (t) => /* @__PURE__ */ import_react.createElement("svg", {
	width: 320,
	height: 320,
	viewBox: "0 0 320 320",
	preserveAspectRatio: "xMidYMid meet",
	fill: "none",
	xmlns: "http://www.w3.org/2000/svg",
	...t
}, /* @__PURE__ */ import_react.createElement("path", {
	d: "M160 180C204.182 180 240 144.183 240 100C240 55.8172 204.182 20 160 20C115.817 20 79.9997 55.8172 79.9997 100C79.9997 144.183 115.817 180 160 180Z",
	fill: "white",
	fillOpacity: .25
}), /* @__PURE__ */ import_react.createElement("path", {
	d: "M97.6542 194.614C103.267 191.818 109.841 192.481 115.519 195.141C129.025 201.466 144.1 205 159.999 205C175.899 205 190.973 201.466 204.48 195.141C210.158 192.481 216.732 191.818 222.345 194.614C262.703 214.719 291.985 253.736 298.591 300.062C300.15 310.997 291.045 320 280 320H39.9997C28.954 320 19.8495 310.997 21.4087 300.062C28.014 253.736 57.2966 214.72 97.6542 194.614Z",
	fill: "white",
	fillOpacity: .25
}));
function ze(t, a = {}) {
	const [n, c] = import_react.useState(Zt$1(t)), [r, i] = import_react.useState(n?.isMuted), [o, l] = import_react.useState(n?.isSubscribed), [s, d] = import_react.useState(n?.track), [u, h] = import_react.useState("landscape"), m = import_react.useRef(), { className: g, trackObserver: f } = import_react.useMemo(() => aa$1(t), [
		t.participant.sid ?? t.participant.identity,
		t.source,
		ee(t) && t.publication.trackSid
	]);
	return import_react.useEffect(() => {
		const E = f.subscribe((p) => {
			R.debug("update track", p), c(p), i(p?.isMuted), l(p?.isSubscribed), d(p?.track);
		});
		return () => E?.unsubscribe();
	}, [f]), import_react.useEffect(() => (s && (m.current && s.detach(m.current), a.element?.current && !(t.participant.isLocal && s?.kind === "audio") && s.attach(a.element.current)), m.current = a.element?.current, () => {
		m.current && s?.detach(m.current);
	}), [s, a.element]), import_react.useEffect(() => {
		if (typeof n?.dimensions?.width == "number" && typeof n?.dimensions?.height == "number") {
			const E = n.dimensions.width > n.dimensions.height ? "landscape" : "portrait";
			h(E);
		}
	}, [n]), {
		publication: n,
		isMuted: r,
		isSubscribed: o,
		track: s,
		elementProps: vu(a.props, {
			className: g,
			"data-lk-local-participant": t.participant.isLocal,
			"data-lk-source": n?.source,
			...n?.kind === "video" && { "data-lk-orientation": u }
		})
	};
}
var be = /* @__PURE__ */ f((/* @__PURE__ */ j$2(((t, a) => {
	var n = "Expected a function", c = NaN, r = "[object Symbol]", i = /^\s+|\s+$/g, o = /^[-+]0x[0-9a-f]+$/i, l = /^0b[01]+$/i, s = /^0o[0-7]+$/i, d = parseInt, u = typeof global == "object" && global && global.Object === Object && global, h = typeof self == "object" && self && self.Object === Object && self, m = u || h || Function("return this")(), g = Object.prototype.toString, f = Math.max, E = Math.min, p = function() {
		return m.Date.now();
	};
	function C(v, w, b) {
		var T, V, B, x, N, L, F = 0, A = !1, _ = !1, H = !0;
		if (typeof v != "function") throw new TypeError(n);
		w = D(w) || 0, k(b) && (A = !!b.leading, _ = "maxWait" in b, B = _ ? f(D(b.maxWait) || 0, w) : B, H = "trailing" in b ? !!b.trailing : H);
		function Q(I) {
			var O = T, K = V;
			return T = V = void 0, F = I, x = v.apply(K, O), x;
		}
		function te(I) {
			return F = I, N = setTimeout(y, w), A ? Q(I) : x;
		}
		function G(I) {
			var O = I - L, K = I - F, pe = w - O;
			return _ ? E(pe, B - K) : pe;
		}
		function X(I) {
			var O = I - L, K = I - F;
			return L === void 0 || O >= w || O < 0 || _ && K >= B;
		}
		function y() {
			var I = p();
			if (X(I)) return U(I);
			N = setTimeout(y, G(I));
		}
		function U(I) {
			return N = void 0, H && T ? Q(I) : (T = V = void 0, x);
		}
		function He() {
			N !== void 0 && clearTimeout(N), F = 0, T = L = V = N = void 0;
		}
		function Ue() {
			return N === void 0 ? x : U(p());
		}
		function ae() {
			var I = p(), O = X(I);
			if (T = arguments, V = this, L = I, O) {
				if (N === void 0) return te(L);
				if (_) return N = setTimeout(y, w), Q(L);
			}
			return N === void 0 && (N = setTimeout(y, w)), x;
		}
		return ae.cancel = He, ae.flush = Ue, ae;
	}
	function k(v) {
		var w = typeof v;
		return !!v && (w == "object" || w == "function");
	}
	function R(v) {
		return !!v && typeof v == "object";
	}
	function M(v) {
		return typeof v == "symbol" || R(v) && g.call(v) == r;
	}
	function D(v) {
		if (typeof v == "number") return v;
		if (M(v)) return c;
		if (k(v)) {
			var w = typeof v.valueOf == "function" ? v.valueOf() : v;
			v = k(w) ? w + "" : w;
		}
		if (typeof v != "string") return v === 0 ? v : +v;
		v = v.replace(i, "");
		var b = l.test(v);
		return b || s.test(v) ? d(v.slice(2), b ? 2 : 8) : o.test(v) ? c : +v;
	}
	a.exports = C;
})))(), 1);
function fa(t) {
	const a = (0, import_react.useRef)(t);
	a.current = t, (0, import_react.useEffect)(() => () => {
		a.current();
	}, []);
}
function ha(t, a = 500, n) {
	const c = (0, import_react.useRef)();
	fa(() => {
		c.current && c.current.cancel();
	});
	const r = (0, import_react.useMemo)(() => {
		const i = (0, be.default)(t, a, n), o = (...l) => i(...l);
		return o.cancel = () => {
			i.cancel();
		}, o.isPending = () => !!c.current, o.flush = () => i.flush(), o;
	}, [
		t,
		a,
		n
	]);
	return (0, import_react.useEffect)(() => {
		c.current = (0, be.default)(t, a, n);
	}, [
		t,
		a,
		n
	]), r;
}
function va(t, a, n) {
	const c = n?.equalityFn ?? ((d, u) => d === u), r = t instanceof Function ? t() : t, [i, o] = (0, import_react.useState)(r), l = (0, import_react.useRef)(r), s = ha(o, a, n);
	return c(l.current, r) || (s(r), l.current = r), [i, s];
}
function ga({ threshold: t = 0, root: a = null, rootMargin: n = "0%", freezeOnceVisible: c = !1, initialIsIntersecting: r = !1, onChange: i } = {}) {
	var o;
	const [l, s] = (0, import_react.useState)(null), [d, u] = (0, import_react.useState)(() => ({
		isIntersecting: r,
		entry: void 0
	})), h = (0, import_react.useRef)();
	h.current = i;
	const m = ((o = d.entry) == null ? void 0 : o.isIntersecting) && c;
	(0, import_react.useEffect)(() => {
		if (!l || !("IntersectionObserver" in window) || m) return;
		let E;
		const p = new IntersectionObserver((C) => {
			const k = Array.isArray(p.thresholds) ? p.thresholds : [p.thresholds];
			C.forEach((R) => {
				const M = R.isIntersecting && k.some((D) => R.intersectionRatio >= D);
				u({
					isIntersecting: M,
					entry: R
				}), h.current && h.current(M, R), M && c && E && (E(), E = void 0);
			});
		}, {
			threshold: t,
			root: a,
			rootMargin: n
		});
		return p.observe(l), () => {
			p.disconnect();
		};
	}, [
		l,
		JSON.stringify(t),
		a,
		n,
		m,
		c
	]);
	const g = (0, import_react.useRef)(null);
	(0, import_react.useEffect)(() => {
		var E;
		!l && (E = d.entry) != null && E.target && !c && !m && g.current !== d.entry.target && (g.current = d.entry.target, u({
			isIntersecting: r,
			entry: void 0
		}));
	}, [
		l,
		d.entry,
		c,
		m,
		r
	]);
	const f = [
		s,
		!!d.isIntersecting,
		d.entry
	];
	return f.ref = f[0], f.isIntersecting = f[1], f.entry = f[2], f;
}
var pa = /* @__PURE__ */ import_react.forwardRef(function({ onTrackClick: a, onClick: n, onSubscriptionStatusChanged: c, trackRef: r, manageSubscription: i, ...o }, l) {
	const s = pn(r), d = import_react.useRef(null);
	import_react.useImperativeHandle(l, () => d.current);
	const { ref: u, entry: h } = ga({ root: null }), [m] = va(h, 3e3), g = import_react.useRef(null), f = import_react.useRef(!1);
	import_react.useEffect(() => {
		f.current = !!i, g.current = s.publication instanceof RemoteTrackPublication ? s.publication : null;
	}, [s.publication, i]), import_react.useEffect(() => {
		i && s.publication instanceof RemoteTrackPublication && m?.isIntersecting === !1 && h?.isIntersecting === !1 && s.publication.setSubscribed(!1);
	}, [
		m,
		h,
		s,
		i
	]), import_react.useEffect(() => {
		i && s.publication instanceof RemoteTrackPublication && h?.isIntersecting === !0 && s.publication.setSubscribed(!0);
	}, [
		h,
		s,
		i
	]), import_react.useEffect(() => () => {
		f.current && g.current?.setSubscribed(!1);
	}, []);
	const { elementProps: E, publication: p, isSubscribed: C } = ze(s, {
		element: d,
		props: o
	});
	import_react.useEffect(() => {
		c?.(!!C);
	}, [C, c]);
	const k = (M) => {
		n?.(M), a?.({
			participant: s?.participant,
			track: p
		});
	}, R = import_react.useCallback((M) => {
		d.current = M, u(M);
	}, [u]);
	return /* @__PURE__ */ import_react.createElement("video", {
		ref: R,
		...E,
		muted: !0,
		onClick: k
	});
});
var he = /* @__PURE__ */ import_react.forwardRef(function({ trackRef: a, onSubscriptionStatusChanged: n, volume: c, ...r }, i) {
	const o = pn(a), l = import_react.useRef(null);
	import_react.useImperativeHandle(i, () => l.current);
	const { elementProps: s, isSubscribed: d, track: u, publication: h } = ze(o, {
		element: l,
		props: r
	});
	return import_react.useEffect(() => {
		n?.(!!d);
	}, [d, n]), import_react.useEffect(() => {
		u === void 0 || c === void 0 || (u instanceof RemoteAudioTrack ? u.setVolume(c) : R.warn("Volume can only be set on remote audio tracks."));
	}, [c, u]), import_react.useEffect(() => {
		h === void 0 || r.muted === void 0 || (h instanceof RemoteTrackPublication ? h.setEnabled(!r.muted) : R.warn("Can only call setEnabled on remote track publications."));
	}, [
		r.muted,
		h,
		u
	]), /* @__PURE__ */ import_react.createElement("audio", {
		ref: l,
		...s
	});
});
function Ea(t) {
	const a = !!Ot();
	return t.participant && !a ? /* @__PURE__ */ import_react.createElement(ti.Provider, { value: t.participant }, t.children) : /* @__PURE__ */ import_react.createElement(import_react.Fragment, null, t.children);
}
function ka(t) {
	const a = !!ei();
	return t.trackRef && !a ? /* @__PURE__ */ import_react.createElement(Zr.Provider, { value: t.trackRef }, t.children) : /* @__PURE__ */ import_react.createElement(import_react.Fragment, null, t.children);
}
var ie = /* @__PURE__ */ import_react.forwardRef(function({ trackRef: a, children: n, onParticipantClick: c, disableSpeakingIndicator: r, ...i }, o) {
	const l = pn(a), { elementProps: s } = Cu({
		htmlProps: i,
		disableSpeakingIndicator: r,
		onParticipantClick: c,
		trackRef: l
	}), d = Uu(l.participant), u = Xr(), h = au()?.autoSubscription, m = import_react.useCallback((g) => {
		l.source && !g && u && u.pin.dispatch && Fs(l, u.pin.state) && u.pin.dispatch({ msg: "clear_pin" });
	}, [l, u]);
	return /* @__PURE__ */ import_react.createElement("div", {
		ref: o,
		style: { position: "relative" },
		...s
	}, /* @__PURE__ */ import_react.createElement(ka, { trackRef: l }, /* @__PURE__ */ import_react.createElement(Ea, { participant: l.participant }, n ?? /* @__PURE__ */ import_react.createElement(import_react.Fragment, null, ee(l) && (l.publication?.kind === "video" || l.source === Track.Source.Camera || l.source === Track.Source.ScreenShare) ? /* @__PURE__ */ import_react.createElement(pa, {
		trackRef: l,
		onSubscriptionStatusChanged: m,
		manageSubscription: h
	}) : ee(l) && /* @__PURE__ */ import_react.createElement(he, {
		trackRef: l,
		onSubscriptionStatusChanged: m
	}), /* @__PURE__ */ import_react.createElement("div", { className: "lk-participant-placeholder" }, /* @__PURE__ */ import_react.createElement(Ve, null)), /* @__PURE__ */ import_react.createElement("div", { className: "lk-participant-metadata" }, /* @__PURE__ */ import_react.createElement("div", { className: "lk-participant-metadata-item" }, l.source === Track.Source.Camera ? /* @__PURE__ */ import_react.createElement(import_react.Fragment, null, d && /* @__PURE__ */ import_react.createElement(ta, { style: { marginRight: "0.25rem" } }), /* @__PURE__ */ import_react.createElement(De, {
		trackRef: {
			participant: l.participant,
			source: Track.Source.Microphone
		},
		show: "muted"
	}), /* @__PURE__ */ import_react.createElement(le, null)) : /* @__PURE__ */ import_react.createElement(import_react.Fragment, null, /* @__PURE__ */ import_react.createElement(Ae, { style: { marginRight: "0.25rem" } }), /* @__PURE__ */ import_react.createElement(le, null, "'s screen"))), /* @__PURE__ */ import_react.createElement(Le, { className: "lk-participant-metadata-item" }))), /* @__PURE__ */ import_react.createElement(ua, { trackRef: l }))));
});
function ba(t) {
	const a = vu(t, { className: "lk-focus-layout" });
	return /* @__PURE__ */ import_react.createElement("div", a, t.children);
}
function Sa({ trackRef: t, ...a }) {
	return /* @__PURE__ */ import_react.createElement(ie, {
		trackRef: t,
		...a
	});
}
function ve({ tracks: t, ...a }) {
	return /* @__PURE__ */ import_react.createElement(import_react.Fragment, null, t.map((n) => /* @__PURE__ */ import_react.createElement(Zr.Provider, {
		value: n,
		key: G(n)
	}, mu(a.children))));
}
function wa({ totalPageCount: t, nextPage: a, prevPage: n, currentPage: c, pagesContainer: r }) {
	const [i, o] = import_react.useState(!1);
	return import_react.useEffect(() => {
		let l;
		return r && (l = tu(r.current, 2e3).subscribe(o)), () => {
			l && l.unsubscribe();
		};
	}, [r]), /* @__PURE__ */ import_react.createElement("div", {
		className: "lk-pagination-control",
		"data-lk-user-interaction": i
	}, /* @__PURE__ */ import_react.createElement("button", {
		className: "lk-button",
		onClick: n
	}, /* @__PURE__ */ import_react.createElement(Ee, null)), /* @__PURE__ */ import_react.createElement("span", { className: "lk-pagination-count" }, `${c} of ${t}`), /* @__PURE__ */ import_react.createElement("button", {
		className: "lk-button",
		onClick: a
	}, /* @__PURE__ */ import_react.createElement(Ee, null)));
}
var Ca = /* @__PURE__ */ import_react.forwardRef(function({ totalPageCount: a, currentPage: n }, c) {
	const r = new Array(a).fill("").map((i, o) => o + 1 === n ? /* @__PURE__ */ import_react.createElement("span", {
		"data-lk-active": !0,
		key: o
	}) : /* @__PURE__ */ import_react.createElement("span", { key: o }));
	return /* @__PURE__ */ import_react.createElement("div", {
		ref: c,
		className: "lk-pagination-indicator"
	}, r);
});
function Ma({ tracks: t, ...a }) {
	const n = import_react.createRef(), c = import_react.useMemo(() => vu(a, { className: "lk-grid-layout" }), [a]), { layout: r } = hu(n, t.length), i = Su(r.maxTiles, t);
	return Lu(n, {
		onLeftSwipe: i.nextPage,
		onRightSwipe: i.prevPage
	}), /* @__PURE__ */ import_react.createElement("div", {
		ref: n,
		"data-lk-pagination": i.totalPageCount > 1,
		...c
	}, /* @__PURE__ */ import_react.createElement(ve, { tracks: i.tracks }, a.children), t.length > r.maxTiles && /* @__PURE__ */ import_react.createElement(import_react.Fragment, null, /* @__PURE__ */ import_react.createElement(Ca, {
		totalPageCount: i.totalPageCount,
		currentPage: i.currentPage
	}), /* @__PURE__ */ import_react.createElement(wa, {
		pagesContainer: n,
		...i
	})));
}
var ya = 130;
var Ra = 140;
var Se = 1;
var Fe = 16 / 10;
var Ia = (1 - Fe) * -1;
function Ta({ tracks: t, orientation: a, ...n }) {
	const c = import_react.useRef(null), [r, i] = import_react.useState(0), { width: o, height: l } = Oi(c), s = a || (l >= o ? "vertical" : "horizontal"), d = s === "vertical" ? Math.max(o * Ia, ya) : Math.max(l * Fe, Ra), u = zc(), h = Math.max(s === "vertical" ? (l - u) / d : (o - u) / d, Se);
	let m = Math.round(h);
	Math.abs(h - r) < .5 ? m = Math.round(r) : r !== h && i(h);
	const g = lc(t, m);
	return import_react.useLayoutEffect(() => {
		c.current && (c.current.dataset.lkOrientation = s, c.current.style.setProperty("--lk-max-visible-tiles", m.toString()));
	}, [m, s]), /* @__PURE__ */ import_react.createElement("aside", {
		key: s,
		className: "lk-carousel",
		ref: c,
		...n
	}, /* @__PURE__ */ import_react.createElement(ve, { tracks: g }, n.children));
}
function Oe({ value: t, onPinChange: a, onWidgetChange: n, children: c }) {
	const r = ru(t);
	return import_react.useEffect(() => {
		R.debug("PinState Updated", { state: r.pin.state }), a && r.pin.state && a(r.pin.state);
	}, [r.pin.state, a]), import_react.useEffect(() => {
		R.debug("Widget Updated", { widgetState: r.widget.state }), n && r.widget.state && n(r.widget.state);
	}, [n, r.widget.state]), /* @__PURE__ */ import_react.createElement(Yr.Provider, { value: r }, c);
}
function Pa({ room: t, volume: a, muted: n }) {
	const c = Du([
		Track.Source.Microphone,
		Track.Source.ScreenShareAudio,
		Track.Source.Unknown
	], {
		updateOnlyOn: [],
		onlySubscribed: !0,
		room: t
	}).filter((r) => !r.participant.isLocal && r.publication.kind === Track.Kind.Audio);
	return /* @__PURE__ */ import_react.createElement("div", { style: { display: "none" } }, c.map((r) => /* @__PURE__ */ import_react.createElement(he, {
		key: G(r),
		trackRef: r,
		volume: a,
		muted: n
	})));
}
function Aa(t) {
	const a = import_react.useMemo(() => vu(t, { className: "lk-toast" }), [t]);
	return /* @__PURE__ */ import_react.createElement("div", a, t.children);
}
var Na = (t) => {
	const a = [];
	for (let n = 0; n < t; n++) a.push([n, t - 1 - n]);
	return a;
};
var we = (t) => [[Math.floor(t / 2)], [-1]];
var xa = (t, a, n) => {
	const [c, r] = (0, import_react.useState)(0), [i, o] = (0, import_react.useState)([[]]);
	(0, import_react.useEffect)(() => {
		if (t === "thinking") o(we(a));
		else if (t === "connecting" || t === "initializing") {
			const s = [...Na(a)];
			o(s);
		} else o(t === "listening" ? we(a) : t === void 0 || t === "speaking" ? [new Array(a).fill(0).map((s, d) => d)] : [[]]);
		r(0);
	}, [t, a]);
	const l = (0, import_react.useRef)(null);
	return (0, import_react.useEffect)(() => {
		let s = performance.now();
		const d = (u) => {
			u - s >= n && (r((h) => h + 1), s = u), l.current = requestAnimationFrame(d);
		};
		return l.current = requestAnimationFrame(d), () => {
			l.current !== null && cancelAnimationFrame(l.current);
		};
	}, [
		n,
		a,
		t,
		i.length
	]), i[c % i.length];
};
var La = /* @__PURE__ */ new Map([
	["connecting", 2e3],
	["initializing", 2e3],
	["listening", 500],
	["thinking", 150]
]);
var Da = (t, a) => {
	if (t === void 0) return 1e3;
	let n = La.get(t);
	return n && t === "connecting" && (n /= a), n;
};
var _e = /* @__PURE__ */ import_react.forwardRef(function({ state: a, options: n, barCount: c = 15, trackRef: r, track: i, children: o, ...l }, s) {
	const d = vu(l, { className: "lk-audio-bar-visualizer" });
	let u = ei();
	(r || i) && (u = r || i);
	const h = Wu(u, {
		bands: c,
		loPass: 100,
		hiPass: 200
	}), m = n?.minHeight ?? 20, g = n?.maxHeight ?? 100, f = xa(a, c, Da(a, c) ?? 100);
	return /* @__PURE__ */ import_react.createElement("div", {
		ref: s,
		...d,
		"data-lk-va-state": a
	}, h.map((E, p) => o ? mu(o, {
		"data-lk-highlighted": f.includes(p),
		"data-lk-bar-index": p,
		className: "lk-audio-bar",
		style: { height: `${Math.min(g, Math.max(m, E * 100 + 5))}%` }
	}) : /* @__PURE__ */ import_react.createElement("span", {
		key: p,
		"data-lk-highlighted": f.includes(p),
		"data-lk-bar-index": p,
		className: `lk-audio-bar ${f.includes(p) && "lk-highlighted"}`,
		style: { height: `${Math.min(g, Math.max(m, E * 100 + 5))}%` }
	})));
});
var Va = /* @__PURE__ */ import_react.forwardRef(function({ children: a, disableSpeakingIndicator: n, onParticipantClick: c, trackRef: r, ...i }, o) {
	const l = pn(r), { elementProps: s } = Cu({
		trackRef: l,
		htmlProps: i,
		disableSpeakingIndicator: n,
		onParticipantClick: c
	});
	return /* @__PURE__ */ import_react.createElement("div", {
		ref: o,
		style: {
			position: "relative",
			minHeight: "160px"
		},
		...s
	}, /* @__PURE__ */ import_react.createElement(Zr.Provider, { value: l }, a ?? /* @__PURE__ */ import_react.createElement(import_react.Fragment, null, ee(l) && /* @__PURE__ */ import_react.createElement(he, { trackRef: l }), /* @__PURE__ */ import_react.createElement(_e, {
		barCount: 7,
		options: { minHeight: 8 }
	}), /* @__PURE__ */ import_react.createElement("div", { className: "lk-participant-metadata" }, /* @__PURE__ */ import_react.createElement("div", { className: "lk-participant-metadata-item" }, /* @__PURE__ */ import_react.createElement(De, { trackRef: l }), /* @__PURE__ */ import_react.createElement(le, null)), /* @__PURE__ */ import_react.createElement(Le, { className: "lk-participant-metadata-item" })))));
});
function za(t) {
	const [a, n] = import_react.useState(void 0), c = tt(t.room);
	return import_react.useEffect(() => {
		switch (c) {
			case ConnectionState.Reconnecting:
				n(/* @__PURE__ */ import_react.createElement(import_react.Fragment, null, /* @__PURE__ */ import_react.createElement(ke, { className: "lk-spinner" }), " Reconnecting"));
				break;
			case ConnectionState.Connecting:
				n(/* @__PURE__ */ import_react.createElement(import_react.Fragment, null, /* @__PURE__ */ import_react.createElement(ke, { className: "lk-spinner" }), " Connecting"));
				break;
			case ConnectionState.Disconnected:
				n(/* @__PURE__ */ import_react.createElement(import_react.Fragment, null, "Disconnected"));
				break;
			default: n(void 0);
		}
	}, [c]), a ? /* @__PURE__ */ import_react.createElement(Aa, { className: "lk-toast-connection-state" }, a) : /* @__PURE__ */ import_react.createElement(import_react.Fragment, null);
}
var Fa = /* @__PURE__ */ import_react.forwardRef(function({ entry: a, hideName: n = !1, hideTimestamp: c = !1, messageFormatter: r, ...i }, o) {
	const l = import_react.useMemo(() => r ? r(a.message) : a.message, [a.message, r]), s = !!a.editTimestamp, d = new Date(a.timestamp), u = typeof navigator < "u" ? navigator.language : "en-US", h = a.from?.name ?? a.from?.identity;
	return /* @__PURE__ */ import_react.createElement("li", {
		ref: o,
		className: "lk-chat-entry",
		title: d.toLocaleTimeString(u, { timeStyle: "full" }),
		"data-lk-message-origin": a.from?.isLocal ? "local" : "remote",
		...i
	}, (!c || !n || s) && /* @__PURE__ */ import_react.createElement("span", { className: "lk-meta-data" }, !n && /* @__PURE__ */ import_react.createElement("strong", { className: "lk-participant-name" }, h), (!c || s) && /* @__PURE__ */ import_react.createElement("span", { className: "lk-timestamp" }, s && "edited ", d.toLocaleTimeString(u, { timeStyle: "short" }))), /* @__PURE__ */ import_react.createElement("span", { className: "lk-message-body" }, l), /* @__PURE__ */ import_react.createElement("span", { className: "lk-message-attachements" }, a.attachedFiles?.map((m) => m.type.startsWith("image/") && /* @__PURE__ */ import_react.createElement("img", {
		style: {
			maxWidth: "300px",
			maxHeight: "300px"
		},
		key: m.name,
		src: URL.createObjectURL(m),
		alt: m.name
	}))));
});
function Qa(t) {
	return Yc(t, Jc()).map((a, n) => {
		if (typeof a == "string") return a;
		{
			const c = a.content.toString(), r = a.type === "url" ? /^http(s?):\/\//.test(c) ? c : `https://${c}` : `mailto:${c}`;
			return /* @__PURE__ */ import_react.createElement("a", {
				className: "lk-chat-link",
				key: n,
				href: r,
				target: "_blank",
				rel: "noreferrer"
			}, c);
		}
	});
}
function $e({ messageFormatter: t, messageDecoder: a, messageEncoder: n, channelTopic: c, ...r }) {
	const i = import_react.useRef(null), o = import_react.useRef(null), { chatMessages: l, send: s, isSending: d } = gc(import_react.useMemo(() => ({
		messageDecoder: a,
		messageEncoder: n,
		channelTopic: c
	}), [
		a,
		n,
		c
	])), u = Xr(), h = import_react.useRef(0);
	async function m(g) {
		g.preventDefault(), o.current && o.current.value.trim() !== "" && (await s(o.current.value), o.current.value = "", o.current.focus());
	}
	return import_react.useEffect(() => {
		i && i.current?.scrollTo({ top: i.current.scrollHeight });
	}, [i, l]), import_react.useEffect(() => {
		if (!u || l.length === 0) return;
		if (u.widget.state?.showChat && l.length > 0 && h.current !== l[l.length - 1]?.timestamp) {
			h.current = l[l.length - 1]?.timestamp;
			return;
		}
		const g = l.filter((E) => !h.current || E.timestamp > h.current).length, { widget: f } = u;
		g > 0 && f.state?.unreadMessages !== g && f.dispatch?.({
			msg: "unread_msg",
			count: g
		});
	}, [l, u?.widget]), /* @__PURE__ */ import_react.createElement("div", {
		...r,
		className: "lk-chat"
	}, /* @__PURE__ */ import_react.createElement("div", { className: "lk-chat-header" }, "Messages", u && /* @__PURE__ */ import_react.createElement(Te, { className: "lk-close-button" }, /* @__PURE__ */ import_react.createElement(Jt, null))), /* @__PURE__ */ import_react.createElement("ul", {
		className: "lk-list lk-chat-messages",
		ref: i
	}, r.children ? l.map((g, f) => mu(r.children, {
		entry: g,
		key: g.id ?? f,
		messageFormatter: t
	})) : l.map((g, f, E) => {
		const p = f >= 1 && E[f - 1].from === g.from, C = f >= 1 && g.timestamp - E[f - 1].timestamp < 6e4;
		return /* @__PURE__ */ import_react.createElement(Fa, {
			key: g.id ?? f,
			hideName: p,
			hideTimestamp: p === !1 ? !1 : C,
			entry: g,
			messageFormatter: t
		});
	})), /* @__PURE__ */ import_react.createElement("form", {
		className: "lk-chat-form",
		onSubmit: m
	}, /* @__PURE__ */ import_react.createElement("input", {
		className: "lk-form-control lk-chat-form-input",
		disabled: d,
		ref: o,
		type: "text",
		placeholder: "Enter a message...",
		onInput: (g) => g.stopPropagation(),
		onKeyDown: (g) => g.stopPropagation(),
		onKeyUp: (g) => g.stopPropagation()
	}), /* @__PURE__ */ import_react.createElement("button", {
		type: "submit",
		className: "lk-button lk-chat-form-button",
		disabled: d
	}, "Send")));
}
function Z({ kind: t, initialSelection: a, onActiveDeviceChange: n, tracks: c, requestPermissions: r = !1, ...i }) {
	const [o, l] = import_react.useState(!1), [s, d] = import_react.useState([]), [u, h] = import_react.useState(!0), [m, g] = import_react.useState(r), f = (k, R$1) => {
		R.debug("handle device change"), l(!1), n?.(k, R$1);
	}, E = import_react.useRef(null), p = import_react.useRef(null);
	import_react.useLayoutEffect(() => {
		o && g(!0);
	}, [o]), import_react.useLayoutEffect(() => {
		let k;
		return E.current && p.current && (s || u) && (k = Kc(E.current, p.current, (R, M) => {
			p.current && Object.assign(p.current.style, {
				left: `${R}px`,
				top: `${M}px`
			});
		})), h(!1), () => {
			k?.();
		};
	}, [
		E,
		p,
		s,
		u
	]);
	const C = import_react.useCallback((k) => {
		p.current && k.target !== E.current && o && Gc(p.current, k) && l(!1);
	}, [
		o,
		p,
		E
	]);
	return import_react.useEffect(() => (document.addEventListener("click", C), () => {
		document.removeEventListener("click", C);
	}), [C]), /* @__PURE__ */ import_react.createElement(import_react.Fragment, null, /* @__PURE__ */ import_react.createElement("button", {
		className: "lk-button lk-button-menu",
		"aria-pressed": o,
		...i,
		onClick: () => l(!o),
		ref: E
	}, i.children), !i.disabled && /* @__PURE__ */ import_react.createElement("div", {
		className: "lk-device-menu",
		ref: p,
		style: { visibility: o ? "visible" : "hidden" }
	}, t ? /* @__PURE__ */ import_react.createElement(ce, {
		initialSelection: a,
		onActiveDeviceChange: (k) => f(t, k),
		onDeviceListChange: d,
		kind: t,
		track: c?.[t],
		requestPermissions: m
	}) : /* @__PURE__ */ import_react.createElement(import_react.Fragment, null, /* @__PURE__ */ import_react.createElement("div", { className: "lk-device-menu-heading" }, "Audio inputs"), /* @__PURE__ */ import_react.createElement(ce, {
		kind: "audioinput",
		onActiveDeviceChange: (k) => f("audioinput", k),
		onDeviceListChange: d,
		track: c?.audioinput,
		requestPermissions: m
	}), /* @__PURE__ */ import_react.createElement("div", { className: "lk-device-menu-heading" }, "Video inputs"), /* @__PURE__ */ import_react.createElement(ce, {
		kind: "videoinput",
		onActiveDeviceChange: (k) => f("videoinput", k),
		onDeviceListChange: d,
		track: c?.videoinput,
		requestPermissions: m
	}))));
}
function ge() {
	import_react.useEffect(() => {
		gu();
	}, []);
}
function Oa(t, a) {
	const [n, c] = import_react.useState(), r = import_react.useMemo(() => new _(), []);
	return import_react.useEffect(() => {
		let i = !1, o = [];
		return r.lock().then(async (l) => {
			try {
				(t.audio || t.video) && (o = await createLocalTracks(t), i ? o.forEach((s) => s.stop()) : c(o));
			} catch (s) {
				a && s instanceof Error ? a(s) : R.error(s);
			} finally {
				l();
			}
		}), () => {
			i = !0, o.forEach((l) => {
				l.stop();
			});
		};
	}, [
		JSON.stringify(t, ac),
		a,
		r
	]), n;
}
function Ga(t, a, n) {
	const [c, r] = import_react.useState(null), [i, o] = import_react.useState(!1), l = wu({ kind: n }), [s, d] = import_react.useState(void 0), [u, h] = import_react.useState(), [m, g] = import_react.useState(a);
	import_react.useEffect(() => {
		g(a);
	}, [a]);
	const f = async (C, k) => {
		try {
			const R = k === "videoinput" ? await createLocalVideoTrack({
				deviceId: C,
				resolution: VideoPresets.h720.resolution
			}) : await createLocalAudioTrack({ deviceId: C }), M = await R.getDeviceId(!1);
			M && C !== M && (p.current = M, g(M)), h(R);
		} catch (R) {
			R instanceof Error && r(R);
		}
	}, E = async (C, k) => {
		await C.setDeviceId(k), p.current = k;
	}, p = import_react.useRef(m);
	return import_react.useEffect(() => {
		t && !u && !c && !i && (R.debug("creating track", n), o(!0), f(m, n).finally(() => {
			o(!1);
		}));
	}, [
		t,
		u,
		c,
		i
	]), import_react.useEffect(() => {
		u && (t ? s?.deviceId && p.current !== s?.deviceId ? (R.debug(`switching ${n} device from`, p.current, s.deviceId), E(u, s.deviceId)) : (R.debug(`unmuting local ${n} track`), u.unmute()) : (R.debug(`muting ${n} track`), u.mute().then(() => R.debug(u.mediaStreamTrack))));
	}, [
		u,
		s,
		t,
		n
	]), import_react.useEffect(() => () => {
		u && (R.debug(`stopping local ${n} track`), u.stop(), u.mute());
	}, []), import_react.useEffect(() => {
		d(l?.find((C) => C.deviceId === m));
	}, [m, l]), {
		selectedDevice: s,
		localTrack: u,
		deviceError: c
	};
}
function Ka({ defaults: t = {}, onValidate: a, onSubmit: n, onError: c, debug: r, joinLabel: i = "Join Room", micLabel: o = "Microphone", camLabel: l = "Camera", userLabel: s = "Username", persistUserChoices: d = !0, videoProcessor: u, ...h }) {
	const { userChoices: m, saveAudioInputDeviceId: g, saveAudioInputEnabled: f, saveVideoInputDeviceId: E, saveVideoInputEnabled: p, saveUsername: C } = Fu({
		defaults: t,
		preventSave: !d,
		preventLoad: !d
	}), [k, R$2] = import_react.useState(m), [M, D] = import_react.useState(k.audioEnabled), [v, w] = import_react.useState(k.videoEnabled), [b, T] = import_react.useState(k.audioDeviceId), [V, B] = import_react.useState(k.videoDeviceId), [x, N] = import_react.useState(k.username);
	import_react.useEffect(() => {
		f(M);
	}, [M, f]), import_react.useEffect(() => {
		p(v);
	}, [v, p]), import_react.useEffect(() => {
		g(b);
	}, [b, g]), import_react.useEffect(() => {
		E(V);
	}, [V, E]), import_react.useEffect(() => {
		C(x);
	}, [x, C]);
	const L = Oa({
		audio: M ? { deviceId: m.audioDeviceId } : !1,
		video: v ? {
			deviceId: m.videoDeviceId,
			processor: u
		} : !1
	}, c), F = import_react.useRef(null), A = import_react.useMemo(() => L?.filter((y) => y.kind === Track.Kind.Video)[0], [L]), _ = import_react.useMemo(() => {
		if (A) {
			const { facingMode: y } = facingModeFromLocalTrack(A);
			return y;
		} else return "undefined";
	}, [A]), H = import_react.useMemo(() => L?.filter((y) => y.kind === Track.Kind.Audio)[0], [L]);
	import_react.useEffect(() => (F.current && A && (A.unmute(), A.attach(F.current)), () => {
		A?.detach();
	}), [A]);
	const [Q, te] = import_react.useState(), G = import_react.useCallback((y) => typeof a == "function" ? a(y) : y.username !== "", [a]);
	import_react.useEffect(() => {
		const y = {
			username: x,
			videoEnabled: v,
			videoDeviceId: V,
			audioEnabled: M,
			audioDeviceId: b
		};
		R$2(y), te(G(y));
	}, [
		x,
		v,
		G,
		M,
		b,
		V
	]);
	function X(y) {
		y.preventDefault(), G(k) ? typeof n == "function" && n(k) : R.warn("Validation failed with: ", k);
	}
	return ge(), /* @__PURE__ */ import_react.createElement("div", {
		className: "lk-prejoin",
		...h
	}, /* @__PURE__ */ import_react.createElement("div", { className: "lk-video-container" }, A && /* @__PURE__ */ import_react.createElement("video", {
		ref: F,
		width: "1280",
		height: "720",
		"data-lk-facing-mode": _
	}), (!A || !v) && /* @__PURE__ */ import_react.createElement("div", { className: "lk-camera-off-note" }, /* @__PURE__ */ import_react.createElement(Ve, null))), /* @__PURE__ */ import_react.createElement("div", { className: "lk-button-group-container" }, /* @__PURE__ */ import_react.createElement("div", { className: "lk-button-group audio" }, /* @__PURE__ */ import_react.createElement(j, {
		initialState: M,
		source: Track.Source.Microphone,
		onChange: (y) => D(y)
	}, o), /* @__PURE__ */ import_react.createElement("div", { className: "lk-button-group-menu" }, /* @__PURE__ */ import_react.createElement(Z, {
		initialSelection: b,
		kind: "audioinput",
		disabled: !H,
		tracks: { audioinput: H },
		onActiveDeviceChange: (y, U) => T(U)
	}))), /* @__PURE__ */ import_react.createElement("div", { className: "lk-button-group video" }, /* @__PURE__ */ import_react.createElement(j, {
		initialState: v,
		source: Track.Source.Camera,
		onChange: (y) => w(y)
	}, l), /* @__PURE__ */ import_react.createElement("div", { className: "lk-button-group-menu" }, /* @__PURE__ */ import_react.createElement(Z, {
		initialSelection: V,
		kind: "videoinput",
		disabled: !A,
		tracks: { videoinput: A },
		onActiveDeviceChange: (y, U) => B(U)
	})))), /* @__PURE__ */ import_react.createElement("form", { className: "lk-username-container" }, /* @__PURE__ */ import_react.createElement("input", {
		className: "lk-form-control",
		id: "username",
		name: "username",
		type: "text",
		defaultValue: x,
		placeholder: s,
		onChange: (y) => N(y.target.value),
		autoComplete: "off"
	}), /* @__PURE__ */ import_react.createElement("button", {
		className: "lk-button lk-join-button",
		type: "submit",
		onClick: X,
		disabled: !Q
	}, i)), r && /* @__PURE__ */ import_react.createElement(import_react.Fragment, null, /* @__PURE__ */ import_react.createElement("strong", null, "User Choices:"), /* @__PURE__ */ import_react.createElement("ul", {
		className: "lk-list",
		style: {
			overflow: "hidden",
			maxWidth: "15rem"
		}
	}, /* @__PURE__ */ import_react.createElement("li", null, "Username: ", `${k.username}`), /* @__PURE__ */ import_react.createElement("li", null, "Video Enabled: ", `${k.videoEnabled}`), /* @__PURE__ */ import_react.createElement("li", null, "Audio Enabled: ", `${k.audioEnabled}`), /* @__PURE__ */ import_react.createElement("li", null, "Video Device: ", `${k.videoDeviceId}`), /* @__PURE__ */ import_react.createElement("li", null, "Audio Device: ", `${k.audioDeviceId}`))));
}
function _a({ props: t }) {
	const { dispatch: a, state: n } = Qr().widget, c = "lk-button lk-settings-toggle";
	return { mergedProps: import_react.useMemo(() => pe(t, {
		className: c,
		onClick: () => {
			a && a({ msg: "toggle_settings" });
		},
		"aria-pressed": n?.showSettings ? "true" : "false"
	}), [
		t,
		c,
		a,
		n
	]) };
}
var $a = /* @__PURE__ */ import_react.forwardRef(function(a, n) {
	const { mergedProps: c } = _a({ props: a });
	return /* @__PURE__ */ import_react.createElement("button", {
		ref: n,
		...c
	}, a.children);
});
var Ba = (t) => {
	switch (t) {
		case Track.Source.Camera: return 1;
		case Track.Source.Microphone: return 2;
		case Track.Source.ScreenShare: return 3;
		default: return 0;
	}
};
function Be({ variation: t, controls: a, saveUserChoices: n = !0, onDeviceError: c, ...r }) {
	const [i, o] = import_react.useState(!1), l = Xr();
	import_react.useEffect(() => {
		l?.widget.state?.showChat !== void 0 && o(l?.widget.state?.showChat);
	}, [l?.widget.state?.showChat]);
	const s = Qt(`(max-width: ${i ? 1e3 : 760}px)`) ? "minimal" : "verbose";
	t ?? (t = s);
	const d = {
		leave: !0,
		...a
	}, u = bu();
	if (!u) d.camera = !1, d.chat = !1, d.microphone = !1, d.screenShare = !1;
	else {
		const b = (T) => u.canPublish && (u.canPublishSources.length === 0 || u.canPublishSources.includes(Ba(T)));
		d.camera ?? (d.camera = b(Track.Source.Camera)), d.microphone ?? (d.microphone = b(Track.Source.Microphone)), d.screenShare ?? (d.screenShare = b(Track.Source.ScreenShare)), d.chat ?? (d.chat = u.canPublishData && a?.chat);
	}
	const h = import_react.useMemo(() => t === "minimal" || t === "verbose", [t]), m = import_react.useMemo(() => t === "textOnly" || t === "verbose", [t]), g = Zc(), [f, E] = import_react.useState(!1), p = import_react.useCallback((b) => {
		E(b);
	}, [E]), C = vu({ className: "lk-control-bar" }, r), { saveAudioInputEnabled: k, saveVideoInputEnabled: R, saveAudioInputDeviceId: M, saveVideoInputDeviceId: D } = Fu({ preventSave: !n }), v = import_react.useCallback((b, T) => T ? k(b) : null, [k]), w = import_react.useCallback((b, T) => T ? R(b) : null, [R]);
	return /* @__PURE__ */ import_react.createElement("div", C, d.microphone && /* @__PURE__ */ import_react.createElement("div", { className: "lk-button-group" }, /* @__PURE__ */ import_react.createElement(j, {
		source: Track.Source.Microphone,
		showIcon: h,
		onChange: v,
		onDeviceError: (b) => c?.({
			source: Track.Source.Microphone,
			error: b
		})
	}, m && "Microphone"), /* @__PURE__ */ import_react.createElement("div", { className: "lk-button-group-menu" }, /* @__PURE__ */ import_react.createElement(Z, {
		kind: "audioinput",
		onActiveDeviceChange: (b, T) => M(T ?? "default")
	}))), d.camera && /* @__PURE__ */ import_react.createElement("div", { className: "lk-button-group" }, /* @__PURE__ */ import_react.createElement(j, {
		source: Track.Source.Camera,
		showIcon: h,
		onChange: w,
		onDeviceError: (b) => c?.({
			source: Track.Source.Camera,
			error: b
		})
	}, m && "Camera"), /* @__PURE__ */ import_react.createElement("div", { className: "lk-button-group-menu" }, /* @__PURE__ */ import_react.createElement(Z, {
		kind: "videoinput",
		onActiveDeviceChange: (b, T) => D(T ?? "default")
	}))), d.screenShare && g && /* @__PURE__ */ import_react.createElement(j, {
		source: Track.Source.ScreenShare,
		captureOptions: {
			audio: !0,
			selfBrowserSurface: "include"
		},
		showIcon: h,
		onChange: p,
		onDeviceError: (b) => c?.({
			source: Track.Source.ScreenShare,
			error: b
		})
	}, m && (f ? "Stop screen share" : "Share screen")), d.chat && /* @__PURE__ */ import_react.createElement(Te, null, h && /* @__PURE__ */ import_react.createElement(Zt, null), m && "Chat"), d.settings && /* @__PURE__ */ import_react.createElement($a, null, h && /* @__PURE__ */ import_react.createElement(Yt, null), m && "Settings"), d.leave && /* @__PURE__ */ import_react.createElement(Pe, null, h && /* @__PURE__ */ import_react.createElement(ea, null), m && "Leave"), /* @__PURE__ */ import_react.createElement(Ne, null));
}
function Ja({ chatMessageFormatter: t, chatMessageDecoder: a, chatMessageEncoder: n, SettingsComponent: c, ...r }) {
	const [i, o] = import_react.useState({
		showChat: !1,
		unreadMessages: 0,
		showSettings: !1
	}), l = import_react.useRef(null), s = Du([{
		source: Track.Source.Camera,
		withPlaceholder: !0
	}, {
		source: Track.Source.ScreenShare,
		withPlaceholder: !1
	}], {
		updateOnlyOn: [RoomEvent.ActiveSpeakersChanged],
		onlySubscribed: !1
	}), d = (f) => {
		R.debug("updating widget state", f), o(f);
	}, u = nu(), h = s.filter(ee).filter((f) => f.publication.source === Track.Source.ScreenShare), m = Tu(u)?.[0], g = s.filter((f) => !Hc(f, m));
	return import_react.useEffect(() => {
		if (h.some((f) => f.publication.isSubscribed) && l.current === null ? (R.debug("Auto set screen share focus:", { newScreenShareTrack: h[0] }), u.pin.dispatch?.({
			msg: "set_pin",
			trackReference: h[0]
		}), l.current = h[0]) : l.current && !h.some((f) => f.publication.trackSid === l.current?.publication?.trackSid) && (R.debug("Auto clearing screen share focus."), u.pin.dispatch?.({ msg: "clear_pin" }), l.current = null), m && !ee(m)) {
			const f = s.find((E) => E.participant.identity === m.participant.identity && E.source === m.source);
			f !== m && ee(f) && u.pin.dispatch?.({
				msg: "set_pin",
				trackReference: f
			});
		}
	}, [
		h.map((f) => `${f.publication.trackSid}_${f.publication.isSubscribed}`).join(),
		m?.publication?.trackSid,
		s
	]), ge(), /* @__PURE__ */ import_react.createElement("div", {
		className: "lk-video-conference",
		...r
	}, qc() && /* @__PURE__ */ import_react.createElement(Oe, {
		value: u,
		onWidgetChange: d
	}, /* @__PURE__ */ import_react.createElement("div", { className: "lk-video-conference-inner" }, m ? /* @__PURE__ */ import_react.createElement("div", { className: "lk-focus-layout-wrapper" }, /* @__PURE__ */ import_react.createElement(ba, null, /* @__PURE__ */ import_react.createElement(Ta, { tracks: g }, /* @__PURE__ */ import_react.createElement(ie, null)), m && /* @__PURE__ */ import_react.createElement(Sa, { trackRef: m }))) : /* @__PURE__ */ import_react.createElement("div", { className: "lk-grid-layout-wrapper" }, /* @__PURE__ */ import_react.createElement(Ma, { tracks: s }, /* @__PURE__ */ import_react.createElement(ie, null))), /* @__PURE__ */ import_react.createElement(Be, { controls: {
		chat: !0,
		settings: !!c
	} })), /* @__PURE__ */ import_react.createElement($e, {
		style: { display: i.showChat ? "grid" : "none" },
		messageFormatter: t,
		messageEncoder: n,
		messageDecoder: a
	}), c && /* @__PURE__ */ import_react.createElement("div", {
		className: "lk-settings-menu-modal",
		style: { display: i.showSettings ? "block" : "none" }
	}, /* @__PURE__ */ import_react.createElement(c, null))), /* @__PURE__ */ import_react.createElement(Pa, null), /* @__PURE__ */ import_react.createElement(za, null));
}
function Za({ ...t }) {
	const [a, n] = import_react.useState({
		showChat: !1,
		unreadMessages: 0
	}), c = Du([Track.Source.Microphone]);
	return ge(), /* @__PURE__ */ import_react.createElement(Oe, { onWidgetChange: n }, /* @__PURE__ */ import_react.createElement("div", {
		className: "lk-audio-conference",
		...t
	}, /* @__PURE__ */ import_react.createElement("div", { className: "lk-audio-conference-stage" }, /* @__PURE__ */ import_react.createElement(ve, { tracks: c }, /* @__PURE__ */ import_react.createElement(Va, null))), /* @__PURE__ */ import_react.createElement(Be, { controls: {
		microphone: !0,
		screenShare: !1,
		camera: !1,
		chat: !0
	} }), a.showChat && /* @__PURE__ */ import_react.createElement($e, null)));
}
function Xa({ controls: t, saveUserChoices: a = !0, onDeviceError: n, ...c }) {
	const r = {
		leave: !0,
		microphone: !0,
		...t
	}, i = bu(), { microphoneTrack: o, localParticipant: l } = ci(), s = import_react.useMemo(() => ({
		participant: l,
		source: Track.Source.Microphone,
		publication: o
	}), [l, o]);
	i ? r.microphone ?? (r.microphone = i.canPublish) : r.microphone = !1;
	const d = vu({ className: "lk-agent-control-bar" }, c), { saveAudioInputEnabled: u, saveAudioInputDeviceId: h } = Fu({ preventSave: !a }), m = import_react.useCallback((g, f) => {
		f && u(g);
	}, [u]);
	return /* @__PURE__ */ import_react.createElement("div", d, r.microphone && /* @__PURE__ */ import_react.createElement("div", { className: "lk-button-group" }, /* @__PURE__ */ import_react.createElement(j, {
		source: Track.Source.Microphone,
		showIcon: !0,
		onChange: m,
		onDeviceError: (g) => n?.({
			source: Track.Source.Microphone,
			error: g
		})
	}, /* @__PURE__ */ import_react.createElement(_e, {
		trackRef: s,
		barCount: 7,
		options: { minHeight: 5 }
	})), /* @__PURE__ */ import_react.createElement("div", { className: "lk-button-group-menu" }, /* @__PURE__ */ import_react.createElement(Z, {
		kind: "audioinput",
		onActiveDeviceChange: (g, f) => h(f ?? "default")
	}))), r.leave && /* @__PURE__ */ import_react.createElement(Pe, null, "Disconnect"), /* @__PURE__ */ import_react.createElement(Ne, null));
}
//#endregion
//#region node_modules/@livekit/components-react/dist/index.mjs
var ts = /* @__PURE__ */ import_react.forwardRef(function(s, a) {
	const { buttonProps: o } = uu(s);
	return /* @__PURE__ */ import_react.createElement("button", {
		ref: a,
		...o
	}, s.children);
});
var os = /* @__PURE__ */ import_react.forwardRef(function({ room: s, ...a }, o) {
	const n = tt(s);
	return /* @__PURE__ */ import_react.createElement("div", {
		ref: o,
		...a
	}, n);
});
var ns = /* @__PURE__ */ import_react.forwardRef(function({ label: s = "Allow Audio", ...a }, o) {
	const { mergedProps: n } = Mu({
		room: oe(a.room),
		props: a
	});
	return /* @__PURE__ */ import_react.createElement("button", {
		ref: o,
		...n
	}, s);
});
var is = /* @__PURE__ */ import_react.forwardRef(function({ trackRef: s, ...a }, o) {
	const u = Wu(pn(s), {
		bands: 7,
		loPass: 300
	});
	return /* @__PURE__ */ import_react.createElement("svg", {
		ref: o,
		width: "100%",
		height: "100%",
		viewBox: "0 0 200 90",
		...a,
		className: "lk-audio-visualizer"
	}, /* @__PURE__ */ import_react.createElement("rect", {
		x: "0",
		y: "0",
		width: "100%",
		height: "100%"
	}), /* @__PURE__ */ import_react.createElement("g", { style: { transform: `translate(65px, 0)` } }, u.map((r, c) => /* @__PURE__ */ import_react.createElement("rect", {
		key: c,
		x: c * 10,
		y: 45 - r * 50 / 2,
		width: 6,
		height: r * 50
	}))));
});
function rs({ participants: t, ...s }) {
	return /* @__PURE__ */ import_react.createElement(import_react.Fragment, null, t.map((a) => /* @__PURE__ */ import_react.createElement(ti.Provider, {
		value: a,
		key: a.identity
	}, mu(s.children))));
}
var cs = /* @__PURE__ */ import_react.forwardRef(function({ childrenPosition: s = "before", children: a, ...o }, n) {
	const { name: i } = ku();
	return /* @__PURE__ */ import_react.createElement("span", {
		ref: n,
		...o
	}, s === "before" && a, i, s === "after" && a);
});
function us(t) {
	return /* @__PURE__ */ import_react.createElement(ni.Provider, { value: t.session }, /* @__PURE__ */ import_react.createElement(hn.Provider, { value: t.session.room }, t.children));
}
//#endregion
export { Gu as AgentEvent, Za as AudioConference, he as AudioTrack, is as AudioVisualizer, _e as BarVisualizer, Gt as CameraDisabledIcon, Kt as CameraIcon, Ta as CarouselLayout, $e as Chat, Jt as ChatCloseIcon, Fa as ChatEntry, Zt as ChatIcon, Te as ChatToggle, Ee as Chevron, ts as ClearPinButton, Le as ConnectionQualityIndicator, os as ConnectionState, za as ConnectionStateToast, Be as ControlBar, zu as DEFAULT_MOOD_TTL_TURNS, Pe as DisconnectButton, Mc as EXPRESSION_ATTRIBUTE, Sa as FocusLayout, ba as FocusLayoutContainer, ua as FocusToggle, Xt as FocusToggleIcon, Yt as GearIcon, Ma as GridLayout, oi as LKFeatureContext, Yr as LayoutContext, Oe as LayoutContextProvider, ea as LeaveIcon, tl as LiveKitRoom, ta as LockLockedIcon, Z as MediaDeviceMenu, ce as MediaDeviceSelect, Xu as MessagesEvent, aa as MicDisabledIcon, na as MicIcon, Va as ParticipantAudioTile, ti as ParticipantContext, Ea as ParticipantContextIfNeeded, rs as ParticipantLoop, le as ParticipantName, Ve as ParticipantPlaceholder, ie as ParticipantTile, Ka as PreJoin, ra as QualityExcellentIcon, ca as QualityGoodIcon, la as QualityPoorIcon, ia as QualityUnknownIcon, Pa as RoomAudioRenderer, hn as RoomContext, cs as RoomName, Ae as ScreenShareIcon, sa as ScreenShareStopIcon, Ju as SessionEvent, us as SessionProvider, ke as SpinnerIcon, ns as StartAudio, Ne as StartMediaButton, Aa as Toast, ve as TrackLoop, De as TrackMutedIndicator, Zr as TrackRefContext, ka as TrackRefContextIfNeeded, j as TrackToggle, oa as UnfocusToggleIcon, Ja as VideoConference, pa as VideoTrack, Xa as VoiceAssistantControlBar, Qa as formatChatMessageLinks, ee as isTrackReference, Nc as isUseSessionReturn, qn as parseExpression, Xc as setLogExtension, Qc as setLogLevel, li as useAgent, qu as useAgentExpression, cu as useAudioPlayback, ju as useAudioWaveform, gc as useChat, Ou as useChatToggle, uu as useClearPinButton, lu as useConnectionQualityIndicator, tt as useConnectionState, nu as useCreateLayoutContext, fu as useDataChannel, du as useDisconnectButton, ru as useEnsureCreateLayoutContext, nc as useEnsureLayoutContext, Ie as useEnsureParticipant, oe as useEnsureRoom, ii as useEnsureSession, pn as useEnsureTrackRef, Qu as useEvents, ic as useFacingMode, au as useFeatureContext, pu as useFocusToggle, hu as useGridLayout, Uu as useIsEncrypted, Vn as useIsMuted, Hu as useIsRecording, oc as useIsSpeaking, Qr as useLayoutContext, uc as useLiveKitRoom, ci as useLocalParticipant, bu as useLocalParticipantPermissions, Xr as useMaybeLayoutContext, Ot as useMaybeParticipantContext, It as useMaybeRoomContext, ri as useMaybeSessionContext, ei as useMaybeTrackRefContext, yu as useMediaDeviceSelect, wu as useMediaDevices, Wu as useMultibandTrackVolume, Su as usePagination, Bu as useParticipantAttribute, kc as useParticipantAttributes, ou as useParticipantContext, fc as useParticipantInfo, Eu as useParticipantPermissions, Cu as useParticipantTile, St as useParticipantTracks, dc as useParticipants, Fu as usePersistentUserChoices, Tu as usePinnedTracks, Ga as usePreviewDevice, Oa as usePreviewTracks, xu as useRemoteParticipant, vn as useRemoteParticipants, _t as useRoomContext, ku as useRoomInfo, el as useRpc, Ku as useSequentialRoomConnectDisconnect, Yu as useSession, su as useSessionContext, Zu as useSessionMessages, Pu as useSortedParticipants, pc as useSpeakingParticipants, Mu as useStartAudio, Au as useStartVideo, Lu as useSwipe, Pc as useTextStream, _u as useToken, Nu as useTrackByName, Iu as useTrackMutedIndicator, iu as useTrackRefContext, Ru as useTrackToggle, xc as useTrackTranscription, $u as useTrackVolume, Du as useTracks, ui as useTranscriptions, lc as useVisualStableUpdate, Vu as useVoiceAssistant };
